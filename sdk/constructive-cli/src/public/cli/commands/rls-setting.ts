/**
 * CLI commands for RlsSetting
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateRlsSettingInput,
  RlsSettingPatch,
  RlsSettingSelect,
  RlsSettingFilter,
  RlsSettingOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  databaseId: 'uuid',
  authenticateSchemaId: 'uuid',
  roleSchemaId: 'uuid',
  authenticateFunctionId: 'uuid',
  authenticateStrictFunctionId: 'uuid',
  currentRoleFunctionId: 'uuid',
  currentRoleIdFunctionId: 'uuid',
  currentUserAgentFunctionId: 'uuid',
  currentIpAddressFunctionId: 'uuid',
};
const usage =
  '\nrls-setting <command>\n\nCommands:\n  list                  List rlsSetting records\n  find-first            Find first matching rlsSetting record\n  get                   Get a rlsSetting by ID\n  create                Create a new rlsSetting\n  update                Update an existing rlsSetting\n  delete                Delete a rlsSetting\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      id: true,
      databaseId: true,
      authenticateSchemaId: true,
      roleSchemaId: true,
      authenticateFunctionId: true,
      authenticateStrictFunctionId: true,
      currentRoleFunctionId: true,
      currentRoleIdFunctionId: true,
      currentUserAgentFunctionId: true,
      currentIpAddressFunctionId: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<RlsSettingSelect, RlsSettingFilter, RlsSettingOrderBy> & {
        select: RlsSettingSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.rlsSetting.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      id: true,
      databaseId: true,
      authenticateSchemaId: true,
      roleSchemaId: true,
      authenticateFunctionId: true,
      authenticateStrictFunctionId: true,
      currentRoleFunctionId: true,
      currentRoleIdFunctionId: true,
      currentUserAgentFunctionId: true,
      currentIpAddressFunctionId: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<RlsSettingSelect, RlsSettingFilter> & {
        select: RlsSettingSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.rlsSetting.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.rlsSetting
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          databaseId: true,
          authenticateSchemaId: true,
          roleSchemaId: true,
          authenticateFunctionId: true,
          authenticateStrictFunctionId: true,
          currentRoleFunctionId: true,
          currentRoleIdFunctionId: true,
          currentUserAgentFunctionId: true,
          currentIpAddressFunctionId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'authenticateSchemaId',
        message: 'authenticateSchemaId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'roleSchemaId',
        message: 'roleSchemaId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'authenticateFunctionId',
        message: 'authenticateFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'authenticateStrictFunctionId',
        message: 'authenticateStrictFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentRoleFunctionId',
        message: 'currentRoleFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentRoleIdFunctionId',
        message: 'currentRoleIdFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentUserAgentFunctionId',
        message: 'currentUserAgentFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentIpAddressFunctionId',
        message: 'currentIpAddressFunctionId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateRlsSettingInput['rlsSetting'];
    const client = getClient();
    const result = await client.rlsSetting
      .create({
        data: {
          databaseId: cleanedData.databaseId,
          authenticateSchemaId: cleanedData.authenticateSchemaId,
          roleSchemaId: cleanedData.roleSchemaId,
          authenticateFunctionId: cleanedData.authenticateFunctionId,
          authenticateStrictFunctionId: cleanedData.authenticateStrictFunctionId,
          currentRoleFunctionId: cleanedData.currentRoleFunctionId,
          currentRoleIdFunctionId: cleanedData.currentRoleIdFunctionId,
          currentUserAgentFunctionId: cleanedData.currentUserAgentFunctionId,
          currentIpAddressFunctionId: cleanedData.currentIpAddressFunctionId,
        },
        select: {
          id: true,
          databaseId: true,
          authenticateSchemaId: true,
          roleSchemaId: true,
          authenticateFunctionId: true,
          authenticateStrictFunctionId: true,
          currentRoleFunctionId: true,
          currentRoleIdFunctionId: true,
          currentUserAgentFunctionId: true,
          currentIpAddressFunctionId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'authenticateSchemaId',
        message: 'authenticateSchemaId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'roleSchemaId',
        message: 'roleSchemaId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'authenticateFunctionId',
        message: 'authenticateFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'authenticateStrictFunctionId',
        message: 'authenticateStrictFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentRoleFunctionId',
        message: 'currentRoleFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentRoleIdFunctionId',
        message: 'currentRoleIdFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentUserAgentFunctionId',
        message: 'currentUserAgentFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'currentIpAddressFunctionId',
        message: 'currentIpAddressFunctionId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as RlsSettingPatch;
    const client = getClient();
    const result = await client.rlsSetting
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          databaseId: cleanedData.databaseId,
          authenticateSchemaId: cleanedData.authenticateSchemaId,
          roleSchemaId: cleanedData.roleSchemaId,
          authenticateFunctionId: cleanedData.authenticateFunctionId,
          authenticateStrictFunctionId: cleanedData.authenticateStrictFunctionId,
          currentRoleFunctionId: cleanedData.currentRoleFunctionId,
          currentRoleIdFunctionId: cleanedData.currentRoleIdFunctionId,
          currentUserAgentFunctionId: cleanedData.currentUserAgentFunctionId,
          currentIpAddressFunctionId: cleanedData.currentIpAddressFunctionId,
        },
        select: {
          id: true,
          databaseId: true,
          authenticateSchemaId: true,
          roleSchemaId: true,
          authenticateFunctionId: true,
          authenticateStrictFunctionId: true,
          currentRoleFunctionId: true,
          currentRoleIdFunctionId: true,
          currentUserAgentFunctionId: true,
          currentIpAddressFunctionId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.rlsSetting
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
