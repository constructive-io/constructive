/**
 * CLI command for query captureAppLimitDefaults
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'capture-app-limit-defaults - captureAppLimitDefaults\n\nUsage: capture-app-limit-defaults [OPTIONS]\n'
      );
      process.exit(0);
    }
    const client = getClient();
    const result = await client.query.captureAppLimitDefaults().execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: captureAppLimitDefaults');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
