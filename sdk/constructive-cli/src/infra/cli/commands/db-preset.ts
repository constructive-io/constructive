/**
 * CLI commands for DbPreset
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateDbPresetInput,
  DbPresetPatch,
  DbPresetSelect,
  DbPresetFilter,
  DbPresetOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  active: 'boolean',
  commitId: 'uuid',
  createdAt: 'string',
  definition: 'json',
  description: 'string',
  id: 'uuid',
  label: 'string',
  modulesHash: 'uuid',
  slug: 'string',
  storeId: 'uuid',
  updatedAt: 'string',
};
const usage =
  '\ndb-preset <command>\n\nCommands:\n  list                  List dbPreset records\n  find-first            Find first matching dbPreset record\n  get                   Get a dbPreset by ID\n  create                Create a new dbPreset\n  update                Update an existing dbPreset\n  delete                Delete a dbPreset\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      active: true,
      commitId: true,
      createdAt: true,
      definition: true,
      description: true,
      id: true,
      label: true,
      modulesHash: true,
      slug: true,
      storeId: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<DbPresetSelect, DbPresetFilter, DbPresetOrderBy> & {
        select: DbPresetSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.dbPreset.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      active: true,
      commitId: true,
      createdAt: true,
      definition: true,
      description: true,
      id: true,
      label: true,
      modulesHash: true,
      slug: true,
      storeId: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<DbPresetSelect, DbPresetFilter, DbPresetOrderBy> & {
        select: DbPresetSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.dbPreset.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.dbPreset
      .findOne({
        id: answers.id as string,
        select: {
          active: true,
          commitId: true,
          createdAt: true,
          definition: true,
          description: true,
          id: true,
          label: true,
          modulesHash: true,
          slug: true,
          storeId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'boolean',
        name: 'active',
        message: 'active',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'commitId',
        message: 'commitId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'definition',
        message: 'definition',
        required: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'label',
        message: 'label',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'modulesHash',
        message: 'modulesHash',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: true,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateDbPresetInput['dbPreset'];
    const client = getClient();
    const result = await client.dbPreset
      .create({
        data: {
          active: cleanedData.active,
          commitId: cleanedData.commitId,
          definition: cleanedData.definition,
          description: cleanedData.description,
          label: cleanedData.label,
          modulesHash: cleanedData.modulesHash,
          slug: cleanedData.slug,
          storeId: cleanedData.storeId,
        },
        select: {
          active: true,
          commitId: true,
          createdAt: true,
          definition: true,
          description: true,
          id: true,
          label: true,
          modulesHash: true,
          slug: true,
          storeId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'boolean',
        name: 'active',
        message: 'active',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'commitId',
        message: 'commitId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'definition',
        message: 'definition',
        required: false,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'label',
        message: 'label',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'modulesHash',
        message: 'modulesHash',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: false,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as DbPresetPatch;
    const client = getClient();
    const result = await client.dbPreset
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          active: cleanedData.active,
          commitId: cleanedData.commitId,
          definition: cleanedData.definition,
          description: cleanedData.description,
          label: cleanedData.label,
          modulesHash: cleanedData.modulesHash,
          slug: cleanedData.slug,
          storeId: cleanedData.storeId,
        },
        select: {
          active: true,
          commitId: true,
          createdAt: true,
          definition: true,
          description: true,
          id: true,
          label: true,
          modulesHash: true,
          slug: true,
          storeId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.dbPreset
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
