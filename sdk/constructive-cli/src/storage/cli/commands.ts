/**
 * CLI command map and entry point
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import contextCmd from './commands/context';
import authCmd from './commands/auth';
import bucketCmd from './commands/bucket';
import fileCmd from './commands/file';
import platformBucketCmd from './commands/platform-bucket';
import platformFileCmd from './commands/platform-file';
import filesRenameCmd from './commands/files-rename';
import platformFilesRenameCmd from './commands/platform-files-rename';
import provisionBucketCmd from './commands/provision-bucket';
import uploadFileCmd from './commands/upload-file';
import uploadFilesCmd from './commands/upload-files';
import uploadPlatformFileCmd from './commands/upload-platform-file';
import uploadPlatformFilesCmd from './commands/upload-platform-files';
const createCommandMap: () => Record<
  string,
  (
    argv: Partial<Record<string, unknown>>,
    prompter: Inquirerer,
    options: CLIOptions
  ) => Promise<void>
> = () => ({
  context: contextCmd,
  auth: authCmd,
  bucket: bucketCmd,
  file: fileCmd,
  'platform-bucket': platformBucketCmd,
  'platform-file': platformFileCmd,
  'files-rename': filesRenameCmd,
  'platform-files-rename': platformFilesRenameCmd,
  'provision-bucket': provisionBucketCmd,
  'upload-file': uploadFileCmd,
  'upload-files': uploadFilesCmd,
  'upload-platform-file': uploadPlatformFileCmd,
  'upload-platform-files': uploadPlatformFilesCmd,
});
const usage =
  '\ncsdk <command>\n\nCommands:\n  context               Manage API contexts\n  auth                  Manage authentication\n  bucket               bucket CRUD operations\n  file                 file CRUD operations\n  platform-bucket      platformBucket CRUD operations\n  platform-file        platformFile CRUD operations\n  files-rename         filesRename\n  platform-files-rename platformFilesRename\n  provision-bucket     Provision an S3 bucket for a logical bucket in the database.\nReads the bucket config via RLS, then creates and configures\nthe S3 bucket with the appropriate privacy policies, CORS rules,\nand lifecycle settings.\n  upload-file          Upload a file: resolves the bucket by key, creates the file row, and returns a presigned PUT URL.\n  upload-files         Upload multiple files: resolves the bucket by key, creates file rows, and returns presigned PUT URLs for each.\n  upload-platform-file Upload a file: resolves the bucket by key, creates the file row, and returns a presigned PUT URL.\n  upload-platform-files Upload multiple files: resolves the bucket by key, creates file rows, and returns presigned PUT URLs for each.\n\n  --help, -h            Show this help message\n  --version, -v         Show version\n';
export const commands = async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  let { first: command, newArgv } = extractFirst(argv);
  const commandMap = createCommandMap();
  if (!command) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'command',
        message: 'What do you want to do?',
        options: Object.keys(commandMap),
      },
    ]);
    command = answer.command as string;
  }
  const commandFn = commandMap[command];
  if (!commandFn) {
    console.log(usage);
    console.error(`Unknown command: ${command}`);
    process.exit(1);
  }
  await commandFn(newArgv, prompter, options);
  prompter.close();
  return argv;
};
