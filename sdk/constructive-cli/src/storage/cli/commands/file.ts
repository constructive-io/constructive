/**
 * CLI commands for File
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateFileInput,
  FilePatch,
  FileSelect,
  FileFilter,
  FileOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  actorId: 'uuid',
  bucketId: 'uuid',
  contentHash: 'string',
  createdAt: 'string',
  databaseId: 'uuid',
  description: 'string',
  downloadUrl: 'string',
  expiryEnqueuedAt: 'string',
  filePath: 'string',
  filename: 'string',
  id: 'uuid',
  isPublic: 'boolean',
  key: 'string',
  mimeType: 'string',
  promotedAt: 'string',
  size: 'int',
  status: 'string',
  tags: 'string',
  updatedAt: 'string',
  upload: 'string',
};
const usage =
  '\nfile <command>\n\nCommands:\n  list                  List file records\n  find-first            Find first matching file record\n  get                   Get a file by ID\n  create                Create a new file\n  update                Update an existing file\n  delete                Delete a file\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      bucketId: true,
      contentHash: true,
      createdAt: true,
      databaseId: true,
      description: true,
      expiryEnqueuedAt: true,
      filename: true,
      id: true,
      isPublic: true,
      key: true,
      mimeType: true,
      promotedAt: true,
      size: true,
      status: true,
      tags: true,
      updatedAt: true,
      upload: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<FileSelect, FileFilter, FileOrderBy> & {
        select: FileSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.file.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      bucketId: true,
      contentHash: true,
      createdAt: true,
      databaseId: true,
      description: true,
      expiryEnqueuedAt: true,
      filename: true,
      id: true,
      isPublic: true,
      key: true,
      mimeType: true,
      promotedAt: true,
      size: true,
      status: true,
      tags: true,
      updatedAt: true,
      upload: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<FileSelect, FileFilter, FileOrderBy> & {
        select: FileSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.file.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.file
      .findOne({
        id: answers.id as string,
        select: {
          actorId: true,
          bucketId: true,
          contentHash: true,
          createdAt: true,
          databaseId: true,
          description: true,
          expiryEnqueuedAt: true,
          filename: true,
          id: true,
          isPublic: true,
          key: true,
          mimeType: true,
          promotedAt: true,
          size: true,
          status: true,
          tags: true,
          updatedAt: true,
          upload: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: true,
      },
      {
        type: 'text',
        name: 'bucketId',
        message: 'bucketId',
        required: true,
      },
      {
        type: 'text',
        name: 'contentHash',
        message: 'contentHash',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'expiryEnqueuedAt',
        message: 'expiryEnqueuedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'filename',
        message: 'filename',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isPublic',
        message: 'isPublic',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'key',
        message: 'key',
        required: true,
      },
      {
        type: 'text',
        name: 'mimeType',
        message: 'mimeType',
        required: true,
      },
      {
        type: 'text',
        name: 'promotedAt',
        message: 'promotedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'size',
        message: 'size',
        required: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'upload',
        message: 'upload',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateFileInput['file'];
    const client = getClient();
    const result = await client.file
      .create({
        data: {
          actorId: cleanedData.actorId,
          bucketId: cleanedData.bucketId,
          contentHash: cleanedData.contentHash,
          databaseId: cleanedData.databaseId,
          description: cleanedData.description,
          expiryEnqueuedAt: cleanedData.expiryEnqueuedAt,
          filename: cleanedData.filename,
          isPublic: cleanedData.isPublic,
          key: cleanedData.key,
          mimeType: cleanedData.mimeType,
          promotedAt: cleanedData.promotedAt,
          size: cleanedData.size,
          status: cleanedData.status,
          tags: cleanedData.tags,
          upload: cleanedData.upload,
        },
        select: {
          actorId: true,
          bucketId: true,
          contentHash: true,
          createdAt: true,
          databaseId: true,
          description: true,
          expiryEnqueuedAt: true,
          filename: true,
          id: true,
          isPublic: true,
          key: true,
          mimeType: true,
          promotedAt: true,
          size: true,
          status: true,
          tags: true,
          updatedAt: true,
          upload: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
      },
      {
        type: 'text',
        name: 'bucketId',
        message: 'bucketId',
        required: false,
      },
      {
        type: 'text',
        name: 'contentHash',
        message: 'contentHash',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'expiryEnqueuedAt',
        message: 'expiryEnqueuedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'filename',
        message: 'filename',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isPublic',
        message: 'isPublic',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'key',
        message: 'key',
        required: false,
      },
      {
        type: 'text',
        name: 'mimeType',
        message: 'mimeType',
        required: false,
      },
      {
        type: 'text',
        name: 'promotedAt',
        message: 'promotedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'size',
        message: 'size',
        required: false,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'upload',
        message: 'upload',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as FilePatch;
    const client = getClient();
    const result = await client.file
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          actorId: cleanedData.actorId,
          bucketId: cleanedData.bucketId,
          contentHash: cleanedData.contentHash,
          databaseId: cleanedData.databaseId,
          description: cleanedData.description,
          expiryEnqueuedAt: cleanedData.expiryEnqueuedAt,
          filename: cleanedData.filename,
          isPublic: cleanedData.isPublic,
          key: cleanedData.key,
          mimeType: cleanedData.mimeType,
          promotedAt: cleanedData.promotedAt,
          size: cleanedData.size,
          status: cleanedData.status,
          tags: cleanedData.tags,
          upload: cleanedData.upload,
        },
        select: {
          actorId: true,
          bucketId: true,
          contentHash: true,
          createdAt: true,
          databaseId: true,
          description: true,
          expiryEnqueuedAt: true,
          filename: true,
          id: true,
          isPublic: true,
          key: true,
          mimeType: true,
          promotedAt: true,
          size: true,
          status: true,
          tags: true,
          updatedAt: true,
          upload: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.file
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
