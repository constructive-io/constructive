/**
 * CLI command for query getMfaStatus
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { buildSelectFromPaths } from '../utils';
import type { GetMfaStatusRecordSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log('get-mfa-status - getMfaStatus\n\nUsage: get-mfa-status [OPTIONS]\n');
      process.exit(0);
    }
    const client = getClient();
    const selectFields = buildSelectFromPaths((argv.select as string) ?? '');
    const result = await client.query
      .getMfaStatus({
        select: selectFields,
      } as unknown as {
        select: GetMfaStatusRecordSelect;
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: getMfaStatus');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
