/**
 * CLI command for mutation signInMagicLink
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { SignInMagicLinkVariables } from '../../orm/mutation';
import type { SignInMagicLinkPayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log('sign-in-magic-link - signInMagicLink\n\nUsage: sign-in-magic-link [OPTIONS]\n');
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .signInMagicLink(
        parsedAnswers as unknown as SignInMagicLinkVariables,
        {
          select: selectFields,
        } as unknown as {
          select: SignInMagicLinkPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: signInMagicLink');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
