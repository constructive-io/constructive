/**
 * CLI command for query sitesSiteOrigin
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import type { SitesSiteOriginVariables } from '../../orm/query';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log('sites-site-origin - sitesSiteOrigin\n\nUsage: sites-site-origin [OPTIONS]\n');
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'targetSiteId',
        message: 'targetSiteId',
      },
    ]);
    const client = getClient();
    const result = await client.query
      .sitesSiteOrigin(answers as unknown as SitesSiteOriginVariables)
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: sitesSiteOrigin');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
