/**
 * CLI commands for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateTriggerInput,
  TriggerPatch,
  TriggerSelect,
  TriggerFilter,
  TriggerOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  category: 'string',
  createdAt: 'string',
  databaseId: 'uuid',
  event: 'string',
  events: 'string',
  forEach: 'string',
  functionName: 'string',
  id: 'uuid',
  name: 'string',
  smartTags: 'json',
  tableId: 'uuid',
  tags: 'string',
  timing: 'string',
  transitionNewName: 'string',
  transitionOldName: 'string',
  updatedAt: 'string',
  whenClause: 'json',
};
const usage =
  '\ntrigger <command>\n\nCommands:\n  list                  List trigger records\n  find-first            Find first matching trigger record\n  get                   Get a trigger by ID\n  create                Create a new trigger\n  update                Update an existing trigger\n  delete                Delete a trigger\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      category: true,
      createdAt: true,
      databaseId: true,
      event: true,
      events: true,
      forEach: true,
      functionName: true,
      id: true,
      name: true,
      smartTags: true,
      tableId: true,
      tags: true,
      timing: true,
      transitionNewName: true,
      transitionOldName: true,
      updatedAt: true,
      whenClause: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<TriggerSelect, TriggerFilter, TriggerOrderBy> & {
        select: TriggerSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.trigger.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      category: true,
      createdAt: true,
      databaseId: true,
      event: true,
      events: true,
      forEach: true,
      functionName: true,
      id: true,
      name: true,
      smartTags: true,
      tableId: true,
      tags: true,
      timing: true,
      transitionNewName: true,
      transitionOldName: true,
      updatedAt: true,
      whenClause: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<TriggerSelect, TriggerFilter, TriggerOrderBy> & {
        select: TriggerSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.trigger.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.trigger
      .findOne({
        id: answers.id as string,
        select: {
          category: true,
          createdAt: true,
          databaseId: true,
          event: true,
          events: true,
          forEach: true,
          functionName: true,
          id: true,
          name: true,
          smartTags: true,
          tableId: true,
          tags: true,
          timing: true,
          transitionNewName: true,
          transitionOldName: true,
          updatedAt: true,
          whenClause: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'category',
        message: 'category',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'event',
        message: 'event',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'events',
        message: 'events',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'forEach',
        message: 'forEach',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'functionName',
        message: 'functionName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'json',
        name: 'smartTags',
        message: 'smartTags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tableId',
        message: 'tableId',
        required: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'timing',
        message: 'timing',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transitionNewName',
        message: 'transitionNewName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transitionOldName',
        message: 'transitionOldName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'whenClause',
        message: 'whenClause',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateTriggerInput['trigger'];
    const client = getClient();
    const result = await client.trigger
      .create({
        data: {
          category: cleanedData.category,
          databaseId: cleanedData.databaseId,
          event: cleanedData.event,
          events: cleanedData.events,
          forEach: cleanedData.forEach,
          functionName: cleanedData.functionName,
          name: cleanedData.name,
          smartTags: cleanedData.smartTags,
          tableId: cleanedData.tableId,
          tags: cleanedData.tags,
          timing: cleanedData.timing,
          transitionNewName: cleanedData.transitionNewName,
          transitionOldName: cleanedData.transitionOldName,
          whenClause: cleanedData.whenClause,
        },
        select: {
          category: true,
          createdAt: true,
          databaseId: true,
          event: true,
          events: true,
          forEach: true,
          functionName: true,
          id: true,
          name: true,
          smartTags: true,
          tableId: true,
          tags: true,
          timing: true,
          transitionNewName: true,
          transitionOldName: true,
          updatedAt: true,
          whenClause: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'category',
        message: 'category',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'event',
        message: 'event',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'events',
        message: 'events',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'forEach',
        message: 'forEach',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'functionName',
        message: 'functionName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'json',
        name: 'smartTags',
        message: 'smartTags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tableId',
        message: 'tableId',
        required: false,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'timing',
        message: 'timing',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transitionNewName',
        message: 'transitionNewName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transitionOldName',
        message: 'transitionOldName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'whenClause',
        message: 'whenClause',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as TriggerPatch;
    const client = getClient();
    const result = await client.trigger
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          category: cleanedData.category,
          databaseId: cleanedData.databaseId,
          event: cleanedData.event,
          events: cleanedData.events,
          forEach: cleanedData.forEach,
          functionName: cleanedData.functionName,
          name: cleanedData.name,
          smartTags: cleanedData.smartTags,
          tableId: cleanedData.tableId,
          tags: cleanedData.tags,
          timing: cleanedData.timing,
          transitionNewName: cleanedData.transitionNewName,
          transitionOldName: cleanedData.transitionOldName,
          whenClause: cleanedData.whenClause,
        },
        select: {
          category: true,
          createdAt: true,
          databaseId: true,
          event: true,
          events: true,
          forEach: true,
          functionName: true,
          id: true,
          name: true,
          smartTags: true,
          tableId: true,
          tags: true,
          timing: true,
          transitionNewName: true,
          transitionOldName: true,
          updatedAt: true,
          whenClause: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.trigger
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
