/**
 * CLI commands for PlatformSiteRelease
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformSiteReleaseInput,
  PlatformSiteReleasePatch,
  PlatformSiteReleaseSelect,
  PlatformSiteReleaseFilter,
  PlatformSiteReleaseOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  commitId: 'uuid',
  createdAt: 'string',
  id: 'uuid',
  manifest: 'json',
  siteId: 'uuid',
  storeId: 'uuid',
  updatedAt: 'string',
};
const usage =
  '\nplatform-site-release <command>\n\nCommands:\n  list                  List platformSiteRelease records\n  find-first            Find first matching platformSiteRelease record\n  get                   Get a platformSiteRelease by ID\n  create                Create a new platformSiteRelease\n  update                Update an existing platformSiteRelease\n  delete                Delete a platformSiteRelease\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      commitId: true,
      createdAt: true,
      id: true,
      manifest: true,
      siteId: true,
      storeId: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformSiteReleaseSelect,
        PlatformSiteReleaseFilter,
        PlatformSiteReleaseOrderBy
      > & {
        select: PlatformSiteReleaseSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformSiteRelease.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      commitId: true,
      createdAt: true,
      id: true,
      manifest: true,
      siteId: true,
      storeId: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformSiteReleaseSelect,
        PlatformSiteReleaseFilter,
        PlatformSiteReleaseOrderBy
      > & {
        select: PlatformSiteReleaseSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformSiteRelease.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformSiteRelease
      .findOne({
        id: answers.id as string,
        select: {
          commitId: true,
          createdAt: true,
          id: true,
          manifest: true,
          siteId: true,
          storeId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'commitId',
        message: 'commitId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'manifest',
        message: 'manifest',
        required: true,
      },
      {
        type: 'text',
        name: 'siteId',
        message: 'siteId',
        required: true,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformSiteReleaseInput['platformSiteRelease'];
    const client = getClient();
    const result = await client.platformSiteRelease
      .create({
        data: {
          commitId: cleanedData.commitId,
          manifest: cleanedData.manifest,
          siteId: cleanedData.siteId,
          storeId: cleanedData.storeId,
        },
        select: {
          commitId: true,
          createdAt: true,
          id: true,
          manifest: true,
          siteId: true,
          storeId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'commitId',
        message: 'commitId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'manifest',
        message: 'manifest',
        required: false,
      },
      {
        type: 'text',
        name: 'siteId',
        message: 'siteId',
        required: false,
      },
      {
        type: 'text',
        name: 'storeId',
        message: 'storeId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformSiteReleasePatch;
    const client = getClient();
    const result = await client.platformSiteRelease
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          commitId: cleanedData.commitId,
          manifest: cleanedData.manifest,
          siteId: cleanedData.siteId,
          storeId: cleanedData.storeId,
        },
        select: {
          commitId: true,
          createdAt: true,
          id: true,
          manifest: true,
          siteId: true,
          storeId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformSiteRelease
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
