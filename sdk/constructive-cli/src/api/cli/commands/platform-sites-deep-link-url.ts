/**
 * CLI command for query platformSitesDeepLinkUrl
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import type { PlatformSitesDeepLinkUrlVariables } from '../../orm/query';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'platform-sites-deep-link-url - platformSitesDeepLinkUrl\n\nUsage: platform-sites-deep-link-url [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'linkSlug',
        message: 'linkSlug',
      },
      {
        type: 'text',
        name: 'targetSiteId',
        message: 'targetSiteId',
      },
    ]);
    const client = getClient();
    const result = await client.query
      .platformSitesDeepLinkUrl(answers as unknown as PlatformSitesDeepLinkUrlVariables)
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: platformSitesDeepLinkUrl');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
