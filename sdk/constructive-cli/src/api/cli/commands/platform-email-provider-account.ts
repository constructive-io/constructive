/**
 * CLI commands for PlatformEmailProviderAccount
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformEmailProviderAccountInput,
  PlatformEmailProviderAccountPatch,
  PlatformEmailProviderAccountSelect,
  PlatformEmailProviderAccountFilter,
  PlatformEmailProviderAccountOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  apiBaseUrl: 'string',
  createdAt: 'string',
  credentialsSecretName: 'string',
  id: 'uuid',
  isActive: 'boolean',
  name: 'string',
  provider: 'string',
  providerAccountName: 'string',
  region: 'string',
  smtpHost: 'string',
  smtpPort: 'int',
  smtpSecure: 'boolean',
  smtpUser: 'string',
  updatedAt: 'string',
  webhookSigningSecretName: 'string',
};
const usage =
  '\nplatform-email-provider-account <command>\n\nCommands:\n  list                  List platformEmailProviderAccount records\n  find-first            Find first matching platformEmailProviderAccount record\n  get                   Get a platformEmailProviderAccount by ID\n  create                Create a new platformEmailProviderAccount\n  update                Update an existing platformEmailProviderAccount\n  delete                Delete a platformEmailProviderAccount\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      apiBaseUrl: true,
      createdAt: true,
      credentialsSecretName: true,
      id: true,
      isActive: true,
      name: true,
      provider: true,
      providerAccountName: true,
      region: true,
      smtpHost: true,
      smtpPort: true,
      smtpSecure: true,
      smtpUser: true,
      updatedAt: true,
      webhookSigningSecretName: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformEmailProviderAccountSelect,
        PlatformEmailProviderAccountFilter,
        PlatformEmailProviderAccountOrderBy
      > & {
        select: PlatformEmailProviderAccountSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformEmailProviderAccount.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      apiBaseUrl: true,
      createdAt: true,
      credentialsSecretName: true,
      id: true,
      isActive: true,
      name: true,
      provider: true,
      providerAccountName: true,
      region: true,
      smtpHost: true,
      smtpPort: true,
      smtpSecure: true,
      smtpUser: true,
      updatedAt: true,
      webhookSigningSecretName: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformEmailProviderAccountSelect,
        PlatformEmailProviderAccountFilter,
        PlatformEmailProviderAccountOrderBy
      > & {
        select: PlatformEmailProviderAccountSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformEmailProviderAccount.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformEmailProviderAccount
      .findOne({
        id: answers.id as string,
        select: {
          apiBaseUrl: true,
          createdAt: true,
          credentialsSecretName: true,
          id: true,
          isActive: true,
          name: true,
          provider: true,
          providerAccountName: true,
          region: true,
          smtpHost: true,
          smtpPort: true,
          smtpSecure: true,
          smtpUser: true,
          updatedAt: true,
          webhookSigningSecretName: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'apiBaseUrl',
        message: 'apiBaseUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'credentialsSecretName',
        message: 'credentialsSecretName',
        required: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'text',
        name: 'provider',
        message: 'provider',
        required: true,
      },
      {
        type: 'text',
        name: 'providerAccountName',
        message: 'providerAccountName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'region',
        message: 'region',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'smtpHost',
        message: 'smtpHost',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'smtpPort',
        message: 'smtpPort',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'smtpSecure',
        message: 'smtpSecure',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'smtpUser',
        message: 'smtpUser',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'webhookSigningSecretName',
        message: 'webhookSigningSecretName',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformEmailProviderAccountInput['platformEmailProviderAccount'];
    const client = getClient();
    const result = await client.platformEmailProviderAccount
      .create({
        data: {
          apiBaseUrl: cleanedData.apiBaseUrl,
          credentialsSecretName: cleanedData.credentialsSecretName,
          isActive: cleanedData.isActive,
          name: cleanedData.name,
          provider: cleanedData.provider,
          providerAccountName: cleanedData.providerAccountName,
          region: cleanedData.region,
          smtpHost: cleanedData.smtpHost,
          smtpPort: cleanedData.smtpPort,
          smtpSecure: cleanedData.smtpSecure,
          smtpUser: cleanedData.smtpUser,
          webhookSigningSecretName: cleanedData.webhookSigningSecretName,
        },
        select: {
          apiBaseUrl: true,
          createdAt: true,
          credentialsSecretName: true,
          id: true,
          isActive: true,
          name: true,
          provider: true,
          providerAccountName: true,
          region: true,
          smtpHost: true,
          smtpPort: true,
          smtpSecure: true,
          smtpUser: true,
          updatedAt: true,
          webhookSigningSecretName: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'apiBaseUrl',
        message: 'apiBaseUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'credentialsSecretName',
        message: 'credentialsSecretName',
        required: false,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'text',
        name: 'provider',
        message: 'provider',
        required: false,
      },
      {
        type: 'text',
        name: 'providerAccountName',
        message: 'providerAccountName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'region',
        message: 'region',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'smtpHost',
        message: 'smtpHost',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'smtpPort',
        message: 'smtpPort',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'smtpSecure',
        message: 'smtpSecure',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'smtpUser',
        message: 'smtpUser',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'webhookSigningSecretName',
        message: 'webhookSigningSecretName',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformEmailProviderAccountPatch;
    const client = getClient();
    const result = await client.platformEmailProviderAccount
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          apiBaseUrl: cleanedData.apiBaseUrl,
          credentialsSecretName: cleanedData.credentialsSecretName,
          isActive: cleanedData.isActive,
          name: cleanedData.name,
          provider: cleanedData.provider,
          providerAccountName: cleanedData.providerAccountName,
          region: cleanedData.region,
          smtpHost: cleanedData.smtpHost,
          smtpPort: cleanedData.smtpPort,
          smtpSecure: cleanedData.smtpSecure,
          smtpUser: cleanedData.smtpUser,
          webhookSigningSecretName: cleanedData.webhookSigningSecretName,
        },
        select: {
          apiBaseUrl: true,
          createdAt: true,
          credentialsSecretName: true,
          id: true,
          isActive: true,
          name: true,
          provider: true,
          providerAccountName: true,
          region: true,
          smtpHost: true,
          smtpPort: true,
          smtpSecure: true,
          smtpUser: true,
          updatedAt: true,
          webhookSigningSecretName: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformEmailProviderAccount
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
