/**
 * CLI command for query resolveHttpRoute
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { buildSelectFromPaths } from '../utils';
import type { ResolveHttpRouteVariables } from '../../orm/query';
import type { ResolveHttpRouteRecordSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log('resolve-http-route - resolveHttpRoute\n\nUsage: resolve-http-route [OPTIONS]\n');
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'requestHost',
        message: 'requestHost',
      },
      {
        type: 'text',
        name: 'requestMethod',
        message: 'requestMethod',
      },
      {
        type: 'text',
        name: 'requestPath',
        message: 'requestPath',
      },
    ]);
    const client = getClient();
    const selectFields = buildSelectFromPaths((argv.select as string) ?? '');
    const result = await client.query
      .resolveHttpRoute(
        answers as unknown as ResolveHttpRouteVariables,
        {
          select: selectFields,
        } as unknown as {
          select: ResolveHttpRouteRecordSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: resolveHttpRoute');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
