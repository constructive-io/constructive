/**
 * CLI commands for ExclusionConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateExclusionConstraintInput,
  ExclusionConstraintPatch,
  ExclusionConstraintSelect,
  ExclusionConstraintFilter,
  ExclusionConstraintOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  accessMethod: 'string',
  category: 'string',
  createdAt: 'string',
  databaseId: 'uuid',
  elementExpr: 'json',
  fieldIds: 'uuid',
  id: 'uuid',
  name: 'string',
  operators: 'string',
  smartTags: 'json',
  tableId: 'uuid',
  tags: 'string',
  type: 'string',
  updatedAt: 'string',
  whereClause: 'json',
};
const usage =
  '\nexclusion-constraint <command>\n\nCommands:\n  list                  List exclusionConstraint records\n  find-first            Find first matching exclusionConstraint record\n  get                   Get a exclusionConstraint by ID\n  create                Create a new exclusionConstraint\n  update                Update an existing exclusionConstraint\n  delete                Delete a exclusionConstraint\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      accessMethod: true,
      category: true,
      createdAt: true,
      databaseId: true,
      elementExpr: true,
      fieldIds: true,
      id: true,
      name: true,
      operators: true,
      smartTags: true,
      tableId: true,
      tags: true,
      type: true,
      updatedAt: true,
      whereClause: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        ExclusionConstraintSelect,
        ExclusionConstraintFilter,
        ExclusionConstraintOrderBy
      > & {
        select: ExclusionConstraintSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.exclusionConstraint.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      accessMethod: true,
      category: true,
      createdAt: true,
      databaseId: true,
      elementExpr: true,
      fieldIds: true,
      id: true,
      name: true,
      operators: true,
      smartTags: true,
      tableId: true,
      tags: true,
      type: true,
      updatedAt: true,
      whereClause: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        ExclusionConstraintSelect,
        ExclusionConstraintFilter,
        ExclusionConstraintOrderBy
      > & {
        select: ExclusionConstraintSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.exclusionConstraint.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.exclusionConstraint
      .findOne({
        id: answers.id as string,
        select: {
          accessMethod: true,
          category: true,
          createdAt: true,
          databaseId: true,
          elementExpr: true,
          fieldIds: true,
          id: true,
          name: true,
          operators: true,
          smartTags: true,
          tableId: true,
          tags: true,
          type: true,
          updatedAt: true,
          whereClause: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'accessMethod',
        message: 'accessMethod',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'category',
        message: 'category',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'elementExpr',
        message: 'elementExpr',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'fieldIds',
        message: 'fieldIds',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'operators',
        message: 'operators',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'smartTags',
        message: 'smartTags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tableId',
        message: 'tableId',
        required: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'type',
        message: 'type',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'whereClause',
        message: 'whereClause',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateExclusionConstraintInput['exclusionConstraint'];
    const client = getClient();
    const result = await client.exclusionConstraint
      .create({
        data: {
          accessMethod: cleanedData.accessMethod,
          category: cleanedData.category,
          databaseId: cleanedData.databaseId,
          elementExpr: cleanedData.elementExpr,
          fieldIds: cleanedData.fieldIds,
          name: cleanedData.name,
          operators: cleanedData.operators,
          smartTags: cleanedData.smartTags,
          tableId: cleanedData.tableId,
          tags: cleanedData.tags,
          type: cleanedData.type,
          whereClause: cleanedData.whereClause,
        },
        select: {
          accessMethod: true,
          category: true,
          createdAt: true,
          databaseId: true,
          elementExpr: true,
          fieldIds: true,
          id: true,
          name: true,
          operators: true,
          smartTags: true,
          tableId: true,
          tags: true,
          type: true,
          updatedAt: true,
          whereClause: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'accessMethod',
        message: 'accessMethod',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'category',
        message: 'category',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'elementExpr',
        message: 'elementExpr',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'fieldIds',
        message: 'fieldIds',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'operators',
        message: 'operators',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'smartTags',
        message: 'smartTags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tableId',
        message: 'tableId',
        required: false,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'type',
        message: 'type',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'whereClause',
        message: 'whereClause',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ExclusionConstraintPatch;
    const client = getClient();
    const result = await client.exclusionConstraint
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          accessMethod: cleanedData.accessMethod,
          category: cleanedData.category,
          databaseId: cleanedData.databaseId,
          elementExpr: cleanedData.elementExpr,
          fieldIds: cleanedData.fieldIds,
          name: cleanedData.name,
          operators: cleanedData.operators,
          smartTags: cleanedData.smartTags,
          tableId: cleanedData.tableId,
          tags: cleanedData.tags,
          type: cleanedData.type,
          whereClause: cleanedData.whereClause,
        },
        select: {
          accessMethod: true,
          category: true,
          createdAt: true,
          databaseId: true,
          elementExpr: true,
          fieldIds: true,
          id: true,
          name: true,
          operators: true,
          smartTags: true,
          tableId: true,
          tags: true,
          type: true,
          updatedAt: true,
          whereClause: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.exclusionConstraint
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
