/**
 * CLI commands for IdentityProviderRegistry
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateIdentityProviderRegistryInput,
  IdentityProviderRegistryPatch,
  IdentityProviderRegistrySelect,
  IdentityProviderRegistryFilter,
  IdentityProviderRegistryOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  authorizationUrl: 'string',
  displayName: 'string',
  issuerUrl: 'string',
  kind: 'string',
  scopes: 'string',
  slug: 'string',
  tokenUrl: 'string',
  userinfoUrl: 'string',
};
const usage =
  '\nidentity-provider-registry <command>\n\nCommands:\n  list                  List identityProviderRegistry records\n  find-first            Find first matching identityProviderRegistry record\n  get                   Get a identityProviderRegistry by ID\n  create                Create a new identityProviderRegistry\n  update                Update an existing identityProviderRegistry\n  delete                Delete a identityProviderRegistry\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      authorizationUrl: true,
      displayName: true,
      issuerUrl: true,
      kind: true,
      scopes: true,
      slug: true,
      tokenUrl: true,
      userinfoUrl: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        IdentityProviderRegistrySelect,
        IdentityProviderRegistryFilter,
        IdentityProviderRegistryOrderBy
      > & {
        select: IdentityProviderRegistrySelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.identityProviderRegistry.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      authorizationUrl: true,
      displayName: true,
      issuerUrl: true,
      kind: true,
      scopes: true,
      slug: true,
      tokenUrl: true,
      userinfoUrl: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        IdentityProviderRegistrySelect,
        IdentityProviderRegistryFilter,
        IdentityProviderRegistryOrderBy
      > & {
        select: IdentityProviderRegistrySelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.identityProviderRegistry.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.identityProviderRegistry
      .findOne({
        slug: answers.slug as string,
        select: {
          authorizationUrl: true,
          displayName: true,
          issuerUrl: true,
          kind: true,
          scopes: true,
          slug: true,
          tokenUrl: true,
          userinfoUrl: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'authorizationUrl',
        message: 'authorizationUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'displayName',
        message: 'displayName',
        required: true,
      },
      {
        type: 'text',
        name: 'issuerUrl',
        message: 'issuerUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: true,
      },
      {
        type: 'text',
        name: 'scopes',
        message: 'scopes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: true,
      },
      {
        type: 'text',
        name: 'tokenUrl',
        message: 'tokenUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'userinfoUrl',
        message: 'userinfoUrl',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateIdentityProviderRegistryInput['identityProviderRegistry'];
    const client = getClient();
    const result = await client.identityProviderRegistry
      .create({
        data: {
          authorizationUrl: cleanedData.authorizationUrl,
          displayName: cleanedData.displayName,
          issuerUrl: cleanedData.issuerUrl,
          kind: cleanedData.kind,
          scopes: cleanedData.scopes,
          slug: cleanedData.slug,
          tokenUrl: cleanedData.tokenUrl,
          userinfoUrl: cleanedData.userinfoUrl,
        },
        select: {
          authorizationUrl: true,
          displayName: true,
          issuerUrl: true,
          kind: true,
          scopes: true,
          slug: true,
          tokenUrl: true,
          userinfoUrl: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: true,
      },
      {
        type: 'text',
        name: 'authorizationUrl',
        message: 'authorizationUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'displayName',
        message: 'displayName',
        required: false,
      },
      {
        type: 'text',
        name: 'issuerUrl',
        message: 'issuerUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
      },
      {
        type: 'text',
        name: 'scopes',
        message: 'scopes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tokenUrl',
        message: 'tokenUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'userinfoUrl',
        message: 'userinfoUrl',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as IdentityProviderRegistryPatch;
    const client = getClient();
    const result = await client.identityProviderRegistry
      .update({
        where: {
          slug: answers.slug as string,
        },
        data: {
          authorizationUrl: cleanedData.authorizationUrl,
          displayName: cleanedData.displayName,
          issuerUrl: cleanedData.issuerUrl,
          kind: cleanedData.kind,
          scopes: cleanedData.scopes,
          tokenUrl: cleanedData.tokenUrl,
          userinfoUrl: cleanedData.userinfoUrl,
        },
        select: {
          authorizationUrl: true,
          displayName: true,
          issuerUrl: true,
          kind: true,
          scopes: true,
          slug: true,
          tokenUrl: true,
          userinfoUrl: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.identityProviderRegistry
      .delete({
        where: {
          slug: answers.slug as string,
        },
        select: {
          slug: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
