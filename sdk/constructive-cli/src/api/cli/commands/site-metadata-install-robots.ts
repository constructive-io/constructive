/**
 * CLI command for mutation siteMetadataInstallRobots
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { SiteMetadataInstallRobotsVariables } from '../../orm/mutation';
import type { SiteMetadataInstallRobotsPayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'site-metadata-install-robots - siteMetadataInstallRobots\n\nUsage: site-metadata-install-robots [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .siteMetadataInstallRobots(
        parsedAnswers as unknown as SiteMetadataInstallRobotsVariables,
        {
          select: selectFields,
        } as unknown as {
          select: SiteMetadataInstallRobotsPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: siteMetadataInstallRobots');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
