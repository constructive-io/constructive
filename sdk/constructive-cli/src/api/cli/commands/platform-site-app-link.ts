/**
 * CLI commands for PlatformSiteAppLink
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformSiteAppLinkInput,
  PlatformSiteAppLinkPatch,
  PlatformSiteAppLinkSelect,
  PlatformSiteAppLinkFilter,
  PlatformSiteAppLinkOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  appIdentifier: 'string',
  createdAt: 'string',
  id: 'uuid',
  pathComponents: 'string',
  platform: 'string',
  sha256CertFingerprints: 'string',
  siteId: 'uuid',
  storeUrl: 'string',
  teamId: 'string',
  updatedAt: 'string',
  webcredentials: 'boolean',
};
const usage =
  '\nplatform-site-app-link <command>\n\nCommands:\n  list                  List platformSiteAppLink records\n  find-first            Find first matching platformSiteAppLink record\n  get                   Get a platformSiteAppLink by ID\n  create                Create a new platformSiteAppLink\n  update                Update an existing platformSiteAppLink\n  delete                Delete a platformSiteAppLink\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      appIdentifier: true,
      createdAt: true,
      id: true,
      pathComponents: true,
      platform: true,
      sha256CertFingerprints: true,
      siteId: true,
      storeUrl: true,
      teamId: true,
      updatedAt: true,
      webcredentials: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformSiteAppLinkSelect,
        PlatformSiteAppLinkFilter,
        PlatformSiteAppLinkOrderBy
      > & {
        select: PlatformSiteAppLinkSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformSiteAppLink.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      appIdentifier: true,
      createdAt: true,
      id: true,
      pathComponents: true,
      platform: true,
      sha256CertFingerprints: true,
      siteId: true,
      storeUrl: true,
      teamId: true,
      updatedAt: true,
      webcredentials: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformSiteAppLinkSelect,
        PlatformSiteAppLinkFilter,
        PlatformSiteAppLinkOrderBy
      > & {
        select: PlatformSiteAppLinkSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformSiteAppLink.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformSiteAppLink
      .findOne({
        id: answers.id as string,
        select: {
          appIdentifier: true,
          createdAt: true,
          id: true,
          pathComponents: true,
          platform: true,
          sha256CertFingerprints: true,
          siteId: true,
          storeUrl: true,
          teamId: true,
          updatedAt: true,
          webcredentials: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'appIdentifier',
        message: 'appIdentifier',
        required: true,
      },
      {
        type: 'text',
        name: 'pathComponents',
        message: 'pathComponents',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'platform',
        message: 'platform',
        required: true,
      },
      {
        type: 'text',
        name: 'sha256CertFingerprints',
        message: 'sha256CertFingerprints',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'siteId',
        message: 'siteId',
        required: true,
      },
      {
        type: 'text',
        name: 'storeUrl',
        message: 'storeUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'teamId',
        message: 'teamId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'webcredentials',
        message: 'webcredentials',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformSiteAppLinkInput['platformSiteAppLink'];
    const client = getClient();
    const result = await client.platformSiteAppLink
      .create({
        data: {
          appIdentifier: cleanedData.appIdentifier,
          pathComponents: cleanedData.pathComponents,
          platform: cleanedData.platform,
          sha256CertFingerprints: cleanedData.sha256CertFingerprints,
          siteId: cleanedData.siteId,
          storeUrl: cleanedData.storeUrl,
          teamId: cleanedData.teamId,
          webcredentials: cleanedData.webcredentials,
        },
        select: {
          appIdentifier: true,
          createdAt: true,
          id: true,
          pathComponents: true,
          platform: true,
          sha256CertFingerprints: true,
          siteId: true,
          storeUrl: true,
          teamId: true,
          updatedAt: true,
          webcredentials: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'appIdentifier',
        message: 'appIdentifier',
        required: false,
      },
      {
        type: 'text',
        name: 'pathComponents',
        message: 'pathComponents',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'platform',
        message: 'platform',
        required: false,
      },
      {
        type: 'text',
        name: 'sha256CertFingerprints',
        message: 'sha256CertFingerprints',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'siteId',
        message: 'siteId',
        required: false,
      },
      {
        type: 'text',
        name: 'storeUrl',
        message: 'storeUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'teamId',
        message: 'teamId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'webcredentials',
        message: 'webcredentials',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformSiteAppLinkPatch;
    const client = getClient();
    const result = await client.platformSiteAppLink
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          appIdentifier: cleanedData.appIdentifier,
          pathComponents: cleanedData.pathComponents,
          platform: cleanedData.platform,
          sha256CertFingerprints: cleanedData.sha256CertFingerprints,
          siteId: cleanedData.siteId,
          storeUrl: cleanedData.storeUrl,
          teamId: cleanedData.teamId,
          webcredentials: cleanedData.webcredentials,
        },
        select: {
          appIdentifier: true,
          createdAt: true,
          id: true,
          pathComponents: true,
          platform: true,
          sha256CertFingerprints: true,
          siteId: true,
          storeUrl: true,
          teamId: true,
          updatedAt: true,
          webcredentials: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformSiteAppLink
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
