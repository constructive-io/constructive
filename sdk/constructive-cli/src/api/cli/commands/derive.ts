/**
 * CLI commands for Derive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateDeriveInput,
  DerivePatch,
  DeriveSelect,
  DeriveFilter,
  DeriveOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  createdAt: 'string',
  databaseId: 'uuid',
  id: 'uuid',
  includeGrants: 'boolean',
  includeMutations: 'boolean',
  kind: 'string',
  policyPrefix: 'string',
  sourceTableId: 'uuid',
  tableId: 'uuid',
  updatedAt: 'string',
};
const usage =
  '\nderive <command>\n\nCommands:\n  list                  List derive records\n  find-first            Find first matching derive record\n  get                   Get a derive by ID\n  create                Create a new derive\n  update                Update an existing derive\n  delete                Delete a derive\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      databaseId: true,
      id: true,
      includeGrants: true,
      includeMutations: true,
      kind: true,
      policyPrefix: true,
      sourceTableId: true,
      tableId: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<DeriveSelect, DeriveFilter, DeriveOrderBy> & {
        select: DeriveSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.derive.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      databaseId: true,
      id: true,
      includeGrants: true,
      includeMutations: true,
      kind: true,
      policyPrefix: true,
      sourceTableId: true,
      tableId: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<DeriveSelect, DeriveFilter, DeriveOrderBy> & {
        select: DeriveSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.derive.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.derive
      .findOne({
        id: answers.id as string,
        select: {
          createdAt: true,
          databaseId: true,
          id: true,
          includeGrants: true,
          includeMutations: true,
          kind: true,
          policyPrefix: true,
          sourceTableId: true,
          tableId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'includeGrants',
        message: 'includeGrants',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'includeMutations',
        message: 'includeMutations',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: true,
      },
      {
        type: 'text',
        name: 'policyPrefix',
        message: 'policyPrefix',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'sourceTableId',
        message: 'sourceTableId',
        required: true,
      },
      {
        type: 'text',
        name: 'tableId',
        message: 'tableId',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateDeriveInput['derive'];
    const client = getClient();
    const result = await client.derive
      .create({
        data: {
          databaseId: cleanedData.databaseId,
          includeGrants: cleanedData.includeGrants,
          includeMutations: cleanedData.includeMutations,
          kind: cleanedData.kind,
          policyPrefix: cleanedData.policyPrefix,
          sourceTableId: cleanedData.sourceTableId,
          tableId: cleanedData.tableId,
        },
        select: {
          createdAt: true,
          databaseId: true,
          id: true,
          includeGrants: true,
          includeMutations: true,
          kind: true,
          policyPrefix: true,
          sourceTableId: true,
          tableId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'includeGrants',
        message: 'includeGrants',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'includeMutations',
        message: 'includeMutations',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
      },
      {
        type: 'text',
        name: 'policyPrefix',
        message: 'policyPrefix',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'sourceTableId',
        message: 'sourceTableId',
        required: false,
      },
      {
        type: 'text',
        name: 'tableId',
        message: 'tableId',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as DerivePatch;
    const client = getClient();
    const result = await client.derive
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          databaseId: cleanedData.databaseId,
          includeGrants: cleanedData.includeGrants,
          includeMutations: cleanedData.includeMutations,
          kind: cleanedData.kind,
          policyPrefix: cleanedData.policyPrefix,
          sourceTableId: cleanedData.sourceTableId,
          tableId: cleanedData.tableId,
        },
        select: {
          createdAt: true,
          databaseId: true,
          id: true,
          includeGrants: true,
          includeMutations: true,
          kind: true,
          policyPrefix: true,
          sourceTableId: true,
          tableId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.derive
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
