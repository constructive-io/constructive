/**
 * CLI commands for Route
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateRouteInput,
  RoutePatch,
  RouteSelect,
  RouteFilter,
  RouteOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  config: 'json',
  createdAt: 'string',
  databaseId: 'uuid',
  domainId: 'uuid',
  id: 'uuid',
  isActive: 'boolean',
  method: 'string',
  path: 'string',
  priority: 'int',
  targetApiId: 'uuid',
  targetBucketId: 'uuid',
  targetFunctionId: 'uuid',
  targetRedirectId: 'uuid',
  targetServiceId: 'uuid',
  targetSiteId: 'uuid',
  updatedAt: 'string',
};
const usage =
  '\nroute <command>\n\nCommands:\n  list                  List route records\n  find-first            Find first matching route record\n  get                   Get a route by ID\n  create                Create a new route\n  update                Update an existing route\n  delete                Delete a route\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      config: true,
      createdAt: true,
      databaseId: true,
      domainId: true,
      id: true,
      isActive: true,
      method: true,
      path: true,
      priority: true,
      targetApiId: true,
      targetBucketId: true,
      targetFunctionId: true,
      targetRedirectId: true,
      targetServiceId: true,
      targetSiteId: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<RouteSelect, RouteFilter, RouteOrderBy> & {
        select: RouteSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.route.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      config: true,
      createdAt: true,
      databaseId: true,
      domainId: true,
      id: true,
      isActive: true,
      method: true,
      path: true,
      priority: true,
      targetApiId: true,
      targetBucketId: true,
      targetFunctionId: true,
      targetRedirectId: true,
      targetServiceId: true,
      targetSiteId: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<RouteSelect, RouteFilter, RouteOrderBy> & {
        select: RouteSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.route.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.route
      .findOne({
        id: answers.id as string,
        select: {
          config: true,
          createdAt: true,
          databaseId: true,
          domainId: true,
          id: true,
          isActive: true,
          method: true,
          path: true,
          priority: true,
          targetApiId: true,
          targetBucketId: true,
          targetFunctionId: true,
          targetRedirectId: true,
          targetServiceId: true,
          targetSiteId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'json',
        name: 'config',
        message: 'config',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'domainId',
        message: 'domainId',
        required: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'method',
        message: 'method',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'path',
        message: 'path',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'priority',
        message: 'priority',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetApiId',
        message: 'targetApiId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetBucketId',
        message: 'targetBucketId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetFunctionId',
        message: 'targetFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetRedirectId',
        message: 'targetRedirectId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetServiceId',
        message: 'targetServiceId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetSiteId',
        message: 'targetSiteId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateRouteInput['route'];
    const client = getClient();
    const result = await client.route
      .create({
        data: {
          config: cleanedData.config,
          databaseId: cleanedData.databaseId,
          domainId: cleanedData.domainId,
          isActive: cleanedData.isActive,
          method: cleanedData.method,
          path: cleanedData.path,
          priority: cleanedData.priority,
          targetApiId: cleanedData.targetApiId,
          targetBucketId: cleanedData.targetBucketId,
          targetFunctionId: cleanedData.targetFunctionId,
          targetRedirectId: cleanedData.targetRedirectId,
          targetServiceId: cleanedData.targetServiceId,
          targetSiteId: cleanedData.targetSiteId,
        },
        select: {
          config: true,
          createdAt: true,
          databaseId: true,
          domainId: true,
          id: true,
          isActive: true,
          method: true,
          path: true,
          priority: true,
          targetApiId: true,
          targetBucketId: true,
          targetFunctionId: true,
          targetRedirectId: true,
          targetServiceId: true,
          targetSiteId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'json',
        name: 'config',
        message: 'config',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'domainId',
        message: 'domainId',
        required: false,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'method',
        message: 'method',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'path',
        message: 'path',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'priority',
        message: 'priority',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetApiId',
        message: 'targetApiId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetBucketId',
        message: 'targetBucketId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetFunctionId',
        message: 'targetFunctionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetRedirectId',
        message: 'targetRedirectId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetServiceId',
        message: 'targetServiceId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetSiteId',
        message: 'targetSiteId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as RoutePatch;
    const client = getClient();
    const result = await client.route
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          config: cleanedData.config,
          databaseId: cleanedData.databaseId,
          domainId: cleanedData.domainId,
          isActive: cleanedData.isActive,
          method: cleanedData.method,
          path: cleanedData.path,
          priority: cleanedData.priority,
          targetApiId: cleanedData.targetApiId,
          targetBucketId: cleanedData.targetBucketId,
          targetFunctionId: cleanedData.targetFunctionId,
          targetRedirectId: cleanedData.targetRedirectId,
          targetServiceId: cleanedData.targetServiceId,
          targetSiteId: cleanedData.targetSiteId,
        },
        select: {
          config: true,
          createdAt: true,
          databaseId: true,
          domainId: true,
          id: true,
          isActive: true,
          method: true,
          path: true,
          priority: true,
          targetApiId: true,
          targetBucketId: true,
          targetFunctionId: true,
          targetRedirectId: true,
          targetServiceId: true,
          targetSiteId: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.route
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
