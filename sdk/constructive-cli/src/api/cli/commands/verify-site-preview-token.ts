/**
 * CLI command for query verifySitePreviewToken
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import type { VerifySitePreviewTokenVariables } from '../../orm/query';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'verify-site-preview-token - verifySitePreviewToken\n\nUsage: verify-site-preview-token [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'siteId',
        message: 'siteId',
      },
      {
        type: 'text',
        name: 'token',
        message: 'token',
      },
    ]);
    const client = getClient();
    const result = await client.query
      .verifySitePreviewToken(answers as unknown as VerifySitePreviewTokenVariables)
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: verifySitePreviewToken');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
