/**
 * CLI command for query applyRegistryDefaults
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import type { ApplyRegistryDefaultsVariables } from '../../orm/query';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'apply-registry-defaults - applyRegistryDefaults\n\nUsage: apply-registry-defaults [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'data',
        message: 'data',
      },
      {
        type: 'text',
        name: 'nodeType',
        message: 'nodeType',
      },
    ]);
    const client = getClient();
    const result = await client.query
      .applyRegistryDefaults(answers as unknown as ApplyRegistryDefaultsVariables)
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: applyRegistryDefaults');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
