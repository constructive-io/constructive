/**
 * CLI commands for DomainType
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateDomainTypeInput,
  DomainTypePatch,
  DomainTypeSelect,
  DomainTypeFilter,
  DomainTypeOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  baseType: 'json',
  category: 'string',
  checkExpr: 'json',
  databaseId: 'uuid',
  defaultExpr: 'json',
  description: 'string',
  id: 'uuid',
  label: 'string',
  name: 'string',
  notNull: 'boolean',
  schemaId: 'uuid',
  smartTags: 'json',
  tags: 'string',
};
const usage =
  '\ndomain-type <command>\n\nCommands:\n  list                  List domainType records\n  find-first            Find first matching domainType record\n  get                   Get a domainType by ID\n  create                Create a new domainType\n  update                Update an existing domainType\n  delete                Delete a domainType\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      baseType: true,
      category: true,
      checkExpr: true,
      databaseId: true,
      defaultExpr: true,
      description: true,
      id: true,
      label: true,
      name: true,
      notNull: true,
      schemaId: true,
      smartTags: true,
      tags: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<DomainTypeSelect, DomainTypeFilter, DomainTypeOrderBy> & {
        select: DomainTypeSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.domainType.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      baseType: true,
      category: true,
      checkExpr: true,
      databaseId: true,
      defaultExpr: true,
      description: true,
      id: true,
      label: true,
      name: true,
      notNull: true,
      schemaId: true,
      smartTags: true,
      tags: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<DomainTypeSelect, DomainTypeFilter, DomainTypeOrderBy> & {
        select: DomainTypeSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.domainType.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.domainType
      .findOne({
        id: answers.id as string,
        select: {
          baseType: true,
          category: true,
          checkExpr: true,
          databaseId: true,
          defaultExpr: true,
          description: true,
          id: true,
          label: true,
          name: true,
          notNull: true,
          schemaId: true,
          smartTags: true,
          tags: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'json',
        name: 'baseType',
        message: 'baseType',
        required: true,
      },
      {
        type: 'text',
        name: 'category',
        message: 'category',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'checkExpr',
        message: 'checkExpr',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'json',
        name: 'defaultExpr',
        message: 'defaultExpr',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'label',
        message: 'label',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'boolean',
        name: 'notNull',
        message: 'notNull',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'schemaId',
        message: 'schemaId',
        required: true,
      },
      {
        type: 'json',
        name: 'smartTags',
        message: 'smartTags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateDomainTypeInput['domainType'];
    const client = getClient();
    const result = await client.domainType
      .create({
        data: {
          baseType: cleanedData.baseType,
          category: cleanedData.category,
          checkExpr: cleanedData.checkExpr,
          databaseId: cleanedData.databaseId,
          defaultExpr: cleanedData.defaultExpr,
          description: cleanedData.description,
          label: cleanedData.label,
          name: cleanedData.name,
          notNull: cleanedData.notNull,
          schemaId: cleanedData.schemaId,
          smartTags: cleanedData.smartTags,
          tags: cleanedData.tags,
        },
        select: {
          baseType: true,
          category: true,
          checkExpr: true,
          databaseId: true,
          defaultExpr: true,
          description: true,
          id: true,
          label: true,
          name: true,
          notNull: true,
          schemaId: true,
          smartTags: true,
          tags: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'json',
        name: 'baseType',
        message: 'baseType',
        required: false,
      },
      {
        type: 'text',
        name: 'category',
        message: 'category',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'checkExpr',
        message: 'checkExpr',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'json',
        name: 'defaultExpr',
        message: 'defaultExpr',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'label',
        message: 'label',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'boolean',
        name: 'notNull',
        message: 'notNull',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'schemaId',
        message: 'schemaId',
        required: false,
      },
      {
        type: 'json',
        name: 'smartTags',
        message: 'smartTags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as DomainTypePatch;
    const client = getClient();
    const result = await client.domainType
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          baseType: cleanedData.baseType,
          category: cleanedData.category,
          checkExpr: cleanedData.checkExpr,
          databaseId: cleanedData.databaseId,
          defaultExpr: cleanedData.defaultExpr,
          description: cleanedData.description,
          label: cleanedData.label,
          name: cleanedData.name,
          notNull: cleanedData.notNull,
          schemaId: cleanedData.schemaId,
          smartTags: cleanedData.smartTags,
          tags: cleanedData.tags,
        },
        select: {
          baseType: true,
          category: true,
          checkExpr: true,
          databaseId: true,
          defaultExpr: true,
          description: true,
          id: true,
          label: true,
          name: true,
          notNull: true,
          schemaId: true,
          smartTags: true,
          tags: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.domainType
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
