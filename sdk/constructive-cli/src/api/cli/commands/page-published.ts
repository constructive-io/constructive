/**
 * CLI command for query pagePublished
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import type { PagePublishedVariables } from '../../orm/query';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log('page-published - pagePublished\n\nUsage: page-published [OPTIONS]\n');
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'pageSlug',
        message: 'pageSlug',
      },
      {
        type: 'text',
        name: 'targetSiteId',
        message: 'targetSiteId',
      },
    ]);
    const client = getClient();
    const result = await client.query
      .pagePublished(answers as unknown as PagePublishedVariables)
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: pagePublished');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
