/**
 * CLI commands for PlatformDomainVerification
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformDomainVerificationInput,
  PlatformDomainVerificationPatch,
  PlatformDomainVerificationSelect,
  PlatformDomainVerificationFilter,
  PlatformDomainVerificationOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  attempts: 'int',
  createdAt: 'string',
  domainId: 'uuid',
  error: 'string',
  expiresAt: 'string',
  id: 'uuid',
  lastCheckedAt: 'string',
  managedDomainId: 'uuid',
  method: 'string',
  recordName: 'string',
  recordType: 'string',
  recordValue: 'string',
  status: 'string',
  updatedAt: 'string',
  verifiedAt: 'string',
};
const usage =
  '\nplatform-domain-verification <command>\n\nCommands:\n  list                  List platformDomainVerification records\n  find-first            Find first matching platformDomainVerification record\n  get                   Get a platformDomainVerification by ID\n  create                Create a new platformDomainVerification\n  update                Update an existing platformDomainVerification\n  delete                Delete a platformDomainVerification\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      attempts: true,
      createdAt: true,
      domainId: true,
      error: true,
      expiresAt: true,
      id: true,
      lastCheckedAt: true,
      managedDomainId: true,
      method: true,
      recordName: true,
      recordType: true,
      recordValue: true,
      status: true,
      updatedAt: true,
      verifiedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformDomainVerificationSelect,
        PlatformDomainVerificationFilter,
        PlatformDomainVerificationOrderBy
      > & {
        select: PlatformDomainVerificationSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformDomainVerification.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      attempts: true,
      createdAt: true,
      domainId: true,
      error: true,
      expiresAt: true,
      id: true,
      lastCheckedAt: true,
      managedDomainId: true,
      method: true,
      recordName: true,
      recordType: true,
      recordValue: true,
      status: true,
      updatedAt: true,
      verifiedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformDomainVerificationSelect,
        PlatformDomainVerificationFilter,
        PlatformDomainVerificationOrderBy
      > & {
        select: PlatformDomainVerificationSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformDomainVerification.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformDomainVerification
      .findOne({
        id: answers.id as string,
        select: {
          attempts: true,
          createdAt: true,
          domainId: true,
          error: true,
          expiresAt: true,
          id: true,
          lastCheckedAt: true,
          managedDomainId: true,
          method: true,
          recordName: true,
          recordType: true,
          recordValue: true,
          status: true,
          updatedAt: true,
          verifiedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'attempts',
        message: 'attempts',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'domainId',
        message: 'domainId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'expiresAt',
        message: 'expiresAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'lastCheckedAt',
        message: 'lastCheckedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'managedDomainId',
        message: 'managedDomainId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'method',
        message: 'method',
        required: true,
      },
      {
        type: 'text',
        name: 'recordName',
        message: 'recordName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'recordType',
        message: 'recordType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'recordValue',
        message: 'recordValue',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'verifiedAt',
        message: 'verifiedAt',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformDomainVerificationInput['platformDomainVerification'];
    const client = getClient();
    const result = await client.platformDomainVerification
      .create({
        data: {
          attempts: cleanedData.attempts,
          domainId: cleanedData.domainId,
          error: cleanedData.error,
          expiresAt: cleanedData.expiresAt,
          lastCheckedAt: cleanedData.lastCheckedAt,
          managedDomainId: cleanedData.managedDomainId,
          method: cleanedData.method,
          recordName: cleanedData.recordName,
          recordType: cleanedData.recordType,
          recordValue: cleanedData.recordValue,
          status: cleanedData.status,
          verifiedAt: cleanedData.verifiedAt,
        },
        select: {
          attempts: true,
          createdAt: true,
          domainId: true,
          error: true,
          expiresAt: true,
          id: true,
          lastCheckedAt: true,
          managedDomainId: true,
          method: true,
          recordName: true,
          recordType: true,
          recordValue: true,
          status: true,
          updatedAt: true,
          verifiedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'attempts',
        message: 'attempts',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'domainId',
        message: 'domainId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'expiresAt',
        message: 'expiresAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'lastCheckedAt',
        message: 'lastCheckedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'managedDomainId',
        message: 'managedDomainId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'method',
        message: 'method',
        required: false,
      },
      {
        type: 'text',
        name: 'recordName',
        message: 'recordName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'recordType',
        message: 'recordType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'recordValue',
        message: 'recordValue',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'verifiedAt',
        message: 'verifiedAt',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformDomainVerificationPatch;
    const client = getClient();
    const result = await client.platformDomainVerification
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          attempts: cleanedData.attempts,
          domainId: cleanedData.domainId,
          error: cleanedData.error,
          expiresAt: cleanedData.expiresAt,
          lastCheckedAt: cleanedData.lastCheckedAt,
          managedDomainId: cleanedData.managedDomainId,
          method: cleanedData.method,
          recordName: cleanedData.recordName,
          recordType: cleanedData.recordType,
          recordValue: cleanedData.recordValue,
          status: cleanedData.status,
          verifiedAt: cleanedData.verifiedAt,
        },
        select: {
          attempts: true,
          createdAt: true,
          domainId: true,
          error: true,
          expiresAt: true,
          id: true,
          lastCheckedAt: true,
          managedDomainId: true,
          method: true,
          recordName: true,
          recordType: true,
          recordValue: true,
          status: true,
          updatedAt: true,
          verifiedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformDomainVerification
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
