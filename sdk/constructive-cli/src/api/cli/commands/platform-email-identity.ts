/**
 * CLI commands for PlatformEmailIdentity
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformEmailIdentityInput,
  PlatformEmailIdentityPatch,
  PlatformEmailIdentitySelect,
  PlatformEmailIdentityFilter,
  PlatformEmailIdentityOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  createdAt: 'string',
  fromAddress: 'string',
  fromName: 'string',
  id: 'uuid',
  isActive: 'boolean',
  isDefault: 'boolean',
  name: 'string',
  providerAccountId: 'uuid',
  replyToAddress: 'string',
  supportAddress: 'string',
  transportMode: 'string',
  updatedAt: 'string',
};
const usage =
  '\nplatform-email-identity <command>\n\nCommands:\n  list                  List platformEmailIdentity records\n  find-first            Find first matching platformEmailIdentity record\n  get                   Get a platformEmailIdentity by ID\n  create                Create a new platformEmailIdentity\n  update                Update an existing platformEmailIdentity\n  delete                Delete a platformEmailIdentity\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      fromAddress: true,
      fromName: true,
      id: true,
      isActive: true,
      isDefault: true,
      name: true,
      providerAccountId: true,
      replyToAddress: true,
      supportAddress: true,
      transportMode: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformEmailIdentitySelect,
        PlatformEmailIdentityFilter,
        PlatformEmailIdentityOrderBy
      > & {
        select: PlatformEmailIdentitySelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformEmailIdentity.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      fromAddress: true,
      fromName: true,
      id: true,
      isActive: true,
      isDefault: true,
      name: true,
      providerAccountId: true,
      replyToAddress: true,
      supportAddress: true,
      transportMode: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformEmailIdentitySelect,
        PlatformEmailIdentityFilter,
        PlatformEmailIdentityOrderBy
      > & {
        select: PlatformEmailIdentitySelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformEmailIdentity.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformEmailIdentity
      .findOne({
        id: answers.id as string,
        select: {
          createdAt: true,
          fromAddress: true,
          fromName: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          providerAccountId: true,
          replyToAddress: true,
          supportAddress: true,
          transportMode: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'fromAddress',
        message: 'fromAddress',
        required: true,
      },
      {
        type: 'text',
        name: 'fromName',
        message: 'fromName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isDefault',
        message: 'isDefault',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'text',
        name: 'providerAccountId',
        message: 'providerAccountId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'replyToAddress',
        message: 'replyToAddress',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'supportAddress',
        message: 'supportAddress',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transportMode',
        message: 'transportMode',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformEmailIdentityInput['platformEmailIdentity'];
    const client = getClient();
    const result = await client.platformEmailIdentity
      .create({
        data: {
          fromAddress: cleanedData.fromAddress,
          fromName: cleanedData.fromName,
          isActive: cleanedData.isActive,
          isDefault: cleanedData.isDefault,
          name: cleanedData.name,
          providerAccountId: cleanedData.providerAccountId,
          replyToAddress: cleanedData.replyToAddress,
          supportAddress: cleanedData.supportAddress,
          transportMode: cleanedData.transportMode,
        },
        select: {
          createdAt: true,
          fromAddress: true,
          fromName: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          providerAccountId: true,
          replyToAddress: true,
          supportAddress: true,
          transportMode: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'fromAddress',
        message: 'fromAddress',
        required: false,
      },
      {
        type: 'text',
        name: 'fromName',
        message: 'fromName',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isDefault',
        message: 'isDefault',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'text',
        name: 'providerAccountId',
        message: 'providerAccountId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'replyToAddress',
        message: 'replyToAddress',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'supportAddress',
        message: 'supportAddress',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transportMode',
        message: 'transportMode',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformEmailIdentityPatch;
    const client = getClient();
    const result = await client.platformEmailIdentity
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          fromAddress: cleanedData.fromAddress,
          fromName: cleanedData.fromName,
          isActive: cleanedData.isActive,
          isDefault: cleanedData.isDefault,
          name: cleanedData.name,
          providerAccountId: cleanedData.providerAccountId,
          replyToAddress: cleanedData.replyToAddress,
          supportAddress: cleanedData.supportAddress,
          transportMode: cleanedData.transportMode,
        },
        select: {
          createdAt: true,
          fromAddress: true,
          fromName: true,
          id: true,
          isActive: true,
          isDefault: true,
          name: true,
          providerAccountId: true,
          replyToAddress: true,
          supportAddress: true,
          transportMode: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformEmailIdentity
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
