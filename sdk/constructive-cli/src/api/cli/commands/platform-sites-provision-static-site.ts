/**
 * CLI command for mutation platformSitesProvisionStaticSite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { PlatformSitesProvisionStaticSiteVariables } from '../../orm/mutation';
import type { PlatformSitesProvisionStaticSitePayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'platform-sites-provision-static-site - platformSitesProvisionStaticSite\n\nUsage: platform-sites-provision-static-site [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .platformSitesProvisionStaticSite(
        parsedAnswers as unknown as PlatformSitesProvisionStaticSiteVariables,
        {
          select: selectFields,
        } as unknown as {
          select: PlatformSitesProvisionStaticSitePayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: platformSitesProvisionStaticSite');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
