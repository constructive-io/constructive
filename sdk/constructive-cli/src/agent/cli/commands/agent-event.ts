/**
 * CLI commands for AgentEvent
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateAgentEventInput,
  AgentEventPatch,
  AgentEventSelect,
  AgentEventFilter,
  AgentEventOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  actorId: 'uuid',
  createdAt: 'string',
  databaseId: 'uuid',
  entry: 'json',
  id: 'uuid',
  recordedAt: 'string',
  runId: 'uuid',
  seq: 'int',
  transcriptFormat: 'string',
  transcriptVersion: 'int',
  updatedAt: 'string',
  visibility: 'string',
};
const usage =
  '\nagent-event <command>\n\nCommands:\n  list                  List agentEvent records\n  find-first            Find first matching agentEvent record\n  get                   Get a agentEvent by ID\n  create                Create a new agentEvent\n  update                Update an existing agentEvent\n  delete                Delete a agentEvent\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      entry: true,
      id: true,
      recordedAt: true,
      runId: true,
      seq: true,
      transcriptFormat: true,
      transcriptVersion: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<AgentEventSelect, AgentEventFilter, AgentEventOrderBy> & {
        select: AgentEventSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.agentEvent.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      createdAt: true,
      entry: true,
      id: true,
      recordedAt: true,
      runId: true,
      seq: true,
      transcriptFormat: true,
      transcriptVersion: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<AgentEventSelect, AgentEventFilter, AgentEventOrderBy> & {
        select: AgentEventSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.agentEvent.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.agentEvent
      .findOne({
        id: answers.id as string,
        select: {
          createdAt: true,
          entry: true,
          id: true,
          recordedAt: true,
          runId: true,
          seq: true,
          transcriptFormat: true,
          transcriptVersion: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'json',
        name: 'entry',
        message: 'entry',
        required: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: true,
      },
      {
        type: 'text',
        name: 'runId',
        message: 'runId',
        required: true,
      },
      {
        type: 'text',
        name: 'seq',
        message: 'seq',
        required: true,
      },
      {
        type: 'text',
        name: 'transcriptFormat',
        message: 'transcriptFormat',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transcriptVersion',
        message: 'transcriptVersion',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateAgentEventInput['agentEvent'];
    const client = getClient();
    const result = await client.agentEvent
      .create({
        data: {
          entry: cleanedData.entry,
          recordedAt: cleanedData.recordedAt,
          runId: cleanedData.runId,
          seq: cleanedData.seq,
          transcriptFormat: cleanedData.transcriptFormat,
          transcriptVersion: cleanedData.transcriptVersion,
        },
        select: {
          createdAt: true,
          entry: true,
          id: true,
          recordedAt: true,
          runId: true,
          seq: true,
          transcriptFormat: true,
          transcriptVersion: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'json',
        name: 'entry',
        message: 'entry',
        required: false,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: false,
      },
      {
        type: 'text',
        name: 'runId',
        message: 'runId',
        required: false,
      },
      {
        type: 'text',
        name: 'seq',
        message: 'seq',
        required: false,
      },
      {
        type: 'text',
        name: 'transcriptFormat',
        message: 'transcriptFormat',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'transcriptVersion',
        message: 'transcriptVersion',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as AgentEventPatch;
    const client = getClient();
    const result = await client.agentEvent
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entry: cleanedData.entry,
          recordedAt: cleanedData.recordedAt,
          runId: cleanedData.runId,
          seq: cleanedData.seq,
          transcriptFormat: cleanedData.transcriptFormat,
          transcriptVersion: cleanedData.transcriptVersion,
        },
        select: {
          createdAt: true,
          entry: true,
          id: true,
          recordedAt: true,
          runId: true,
          seq: true,
          transcriptFormat: true,
          transcriptVersion: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.agentEvent
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
