/**
 * CLI commands for PlatformProposal
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformProposalInput,
  PlatformProposalPatch,
  PlatformProposalSelect,
  PlatformProposalFilter,
  PlatformProposalOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  actorId: 'uuid',
  body: 'string',
  bodyTrgmSimilarity: 'float',
  closedReason: 'string',
  closedReasonTrgmSimilarity: 'float',
  createdAt: 'string',
  createdBy: 'uuid',
  createdByPrincipal: 'uuid',
  decidedAt: 'string',
  dueAt: 'string',
  embedding: 'string',
  embeddingUpdatedAt: 'string',
  embeddingVectorDistance: 'float',
  id: 'uuid',
  kind: 'string',
  kindTrgmSimilarity: 'float',
  labels: 'string',
  mergeCommit: 'string',
  mergeCommitTrgmSimilarity: 'float',
  mergeMethod: 'string',
  mergeMethodTrgmSimilarity: 'float',
  mergeRequestedAt: 'string',
  mergedAt: 'string',
  metadata: 'json',
  parentId: 'uuid',
  priority: 'string',
  repositoryId: 'uuid',
  resolution: 'string',
  resolutionTrgmSimilarity: 'float',
  search: 'string',
  searchScore: 'float',
  searchTsvRank: 'float',
  sourceRef: 'string',
  sourceRefTrgmSimilarity: 'float',
  status: 'string',
  statusTrgmSimilarity: 'float',
  targetRef: 'string',
  targetRefTrgmSimilarity: 'float',
  title: 'string',
  titleTrgmSimilarity: 'float',
  updatedAt: 'string',
  updatedBy: 'uuid',
  updatedByPrincipal: 'uuid',
};
import { resolveEmbedder, autoEmbedWhere, autoEmbedInput } from '../embedder';
const usage =
  '\nplatform-proposal <command>\n\nCommands:\n  list                  List platformProposal records\n  find-first            Find first matching platformProposal record\n  search <query>        Search platformProposal records\n  get                   Get a platformProposal by ID\n  create                Create a new platformProposal\n  update                Update an existing platformProposal\n\nCreate/Update Options:\n  --auto-embed          Convert text values in vector fields to embeddings before saving\n  delete                Delete a platformProposal\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nSearch Options:\n  <query>               Search query string (required)\n  --limit <n>           Max number of records to return\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --orderBy <values>    Comma-separated list of ordering values\n  --auto-embed          Convert text queries to vectors via configured embedder\n\nEmbedding Options (for --auto-embed):\n  Set EMBEDDER_PROVIDER=ollama to enable text-to-vector embedding.\n  Optional: EMBEDDER_MODEL (default: nomic-embed-text)\n  Optional: EMBEDDER_BASE_URL (default: http://localhost:11434)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'search', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'search':
      return handleSearch(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      body: true,
      closedReason: true,
      createdAt: true,
      createdBy: true,
      createdByPrincipal: true,
      decidedAt: true,
      dueAt: true,
      embedding: true,
      id: true,
      kind: true,
      labels: true,
      mergeCommit: true,
      mergeMethod: true,
      mergeRequestedAt: true,
      mergedAt: true,
      metadata: true,
      parentId: true,
      priority: true,
      repositoryId: true,
      resolution: true,
      sourceRef: true,
      status: true,
      targetRef: true,
      title: true,
      updatedAt: true,
      updatedBy: true,
      updatedByPrincipal: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<PlatformProposalSelect, PlatformProposalFilter, PlatformProposalOrderBy> & {
        select: PlatformProposalSelect;
      }
    >(argv, defaultSelect);
    if (argv['auto-embed']) {
      const embedder = resolveEmbedder();
      if (!embedder) {
        console.error(
          '--auto-embed requires an embedder. Set EMBEDDER_PROVIDER=ollama (and optionally EMBEDDER_MODEL, EMBEDDER_BASE_URL).'
        );
        process.exit(1);
      }
      findManyArgs.where = await autoEmbedWhere(findManyArgs.where ?? {}, ['embedding'], embedder);
    }
    const client = getClient();
    const result = await client.platformProposal.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      body: true,
      closedReason: true,
      createdAt: true,
      createdBy: true,
      createdByPrincipal: true,
      decidedAt: true,
      dueAt: true,
      embedding: true,
      id: true,
      kind: true,
      labels: true,
      mergeCommit: true,
      mergeMethod: true,
      mergeRequestedAt: true,
      mergedAt: true,
      metadata: true,
      parentId: true,
      priority: true,
      repositoryId: true,
      resolution: true,
      sourceRef: true,
      status: true,
      targetRef: true,
      title: true,
      updatedAt: true,
      updatedBy: true,
      updatedByPrincipal: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<PlatformProposalSelect, PlatformProposalFilter, PlatformProposalOrderBy> & {
        select: PlatformProposalSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformProposal.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleSearch(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const query = Array.isArray(argv._) && argv._.length > 0 ? String(argv._[0]) : undefined;
    if (!query) {
      console.error('Error: search requires a <query> argument');
      process.exit(1);
    }
    const searchWhere = {
      embedding: {
        vector: query,
      },
      trgmBody: {
        value: query,
        threshold: 0.3,
      },
      trgmClosedReason: {
        value: query,
        threshold: 0.3,
      },
      trgmKind: {
        value: query,
        threshold: 0.3,
      },
      trgmMergeCommit: {
        value: query,
        threshold: 0.3,
      },
      trgmMergeMethod: {
        value: query,
        threshold: 0.3,
      },
      trgmResolution: {
        value: query,
        threshold: 0.3,
      },
      search: {
        query,
      },
      trgmSourceRef: {
        value: query,
        threshold: 0.3,
      },
      trgmStatus: {
        value: query,
        threshold: 0.3,
      },
      trgmTargetRef: {
        value: query,
        threshold: 0.3,
      },
      trgmTitle: {
        value: query,
        threshold: 0.3,
      },
    };
    if (argv['auto-embed']) {
      const embedder = resolveEmbedder();
      if (!embedder) {
        console.error(
          '--auto-embed requires an embedder. Set EMBEDDER_PROVIDER=ollama (and optionally EMBEDDER_MODEL, EMBEDDER_BASE_URL).'
        );
        process.exit(1);
      }
      await autoEmbedWhere(searchWhere ?? {}, ['embedding'], embedder);
    }
    const defaultSelect = {
      actorId: true,
      body: true,
      closedReason: true,
      createdAt: true,
      createdBy: true,
      createdByPrincipal: true,
      decidedAt: true,
      dueAt: true,
      embedding: true,
      id: true,
      kind: true,
      labels: true,
      mergeCommit: true,
      mergeMethod: true,
      mergeRequestedAt: true,
      mergedAt: true,
      metadata: true,
      parentId: true,
      priority: true,
      repositoryId: true,
      resolution: true,
      sourceRef: true,
      status: true,
      targetRef: true,
      title: true,
      updatedAt: true,
      updatedBy: true,
      updatedByPrincipal: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<PlatformProposalSelect, PlatformProposalFilter, PlatformProposalOrderBy> & {
        select: PlatformProposalSelect;
      }
    >(argv, defaultSelect, searchWhere);
    const client = getClient();
    const result = await client.platformProposal.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to search records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformProposal
      .findOne({
        id: answers.id as string,
        select: {
          actorId: true,
          body: true,
          closedReason: true,
          createdAt: true,
          createdBy: true,
          createdByPrincipal: true,
          decidedAt: true,
          dueAt: true,
          embedding: true,
          id: true,
          kind: true,
          labels: true,
          mergeCommit: true,
          mergeMethod: true,
          mergeRequestedAt: true,
          mergedAt: true,
          metadata: true,
          parentId: true,
          priority: true,
          repositoryId: true,
          resolution: true,
          sourceRef: true,
          status: true,
          targetRef: true,
          title: true,
          updatedAt: true,
          updatedBy: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'body',
        message: 'body',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'closedReason',
        message: 'closedReason',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdBy',
        message: 'createdBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'decidedAt',
        message: 'decidedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'dueAt',
        message: 'dueAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'labels',
        message: 'labels',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergeCommit',
        message: 'mergeCommit',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergeMethod',
        message: 'mergeMethod',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergeRequestedAt',
        message: 'mergeRequestedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergedAt',
        message: 'mergedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'metadata',
        message: 'metadata',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'parentId',
        message: 'parentId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'priority',
        message: 'priority',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'repositoryId',
        message: 'repositoryId',
        required: true,
      },
      {
        type: 'text',
        name: 'resolution',
        message: 'resolution',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'sourceRef',
        message: 'sourceRef',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetRef',
        message: 'targetRef',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'title',
        message: 'title',
        required: true,
      },
      {
        type: 'text',
        name: 'updatedBy',
        message: 'updatedBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformProposalInput['platformProposal'];
    if (argv['auto-embed']) {
      const embedder = resolveEmbedder();
      if (!embedder) {
        console.error(
          '--auto-embed requires an embedder. Set EMBEDDER_PROVIDER=ollama (and optionally EMBEDDER_MODEL, EMBEDDER_BASE_URL).'
        );
        process.exit(1);
      }
      await autoEmbedInput(cleanedData, ['embedding'], embedder);
    }
    const client = getClient();
    const result = await client.platformProposal
      .create({
        data: {
          actorId: cleanedData.actorId,
          body: cleanedData.body,
          closedReason: cleanedData.closedReason,
          createdBy: cleanedData.createdBy,
          createdByPrincipal: cleanedData.createdByPrincipal,
          decidedAt: cleanedData.decidedAt,
          dueAt: cleanedData.dueAt,
          embedding: cleanedData.embedding,
          kind: cleanedData.kind,
          labels: cleanedData.labels,
          mergeCommit: cleanedData.mergeCommit,
          mergeMethod: cleanedData.mergeMethod,
          mergeRequestedAt: cleanedData.mergeRequestedAt,
          mergedAt: cleanedData.mergedAt,
          metadata: cleanedData.metadata,
          parentId: cleanedData.parentId,
          priority: cleanedData.priority,
          repositoryId: cleanedData.repositoryId,
          resolution: cleanedData.resolution,
          sourceRef: cleanedData.sourceRef,
          status: cleanedData.status,
          targetRef: cleanedData.targetRef,
          title: cleanedData.title,
          updatedBy: cleanedData.updatedBy,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
        },
        select: {
          actorId: true,
          body: true,
          closedReason: true,
          createdAt: true,
          createdBy: true,
          createdByPrincipal: true,
          decidedAt: true,
          dueAt: true,
          embedding: true,
          id: true,
          kind: true,
          labels: true,
          mergeCommit: true,
          mergeMethod: true,
          mergeRequestedAt: true,
          mergedAt: true,
          metadata: true,
          parentId: true,
          priority: true,
          repositoryId: true,
          resolution: true,
          sourceRef: true,
          status: true,
          targetRef: true,
          title: true,
          updatedAt: true,
          updatedBy: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'body',
        message: 'body',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'closedReason',
        message: 'closedReason',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdBy',
        message: 'createdBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'decidedAt',
        message: 'decidedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'dueAt',
        message: 'dueAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'labels',
        message: 'labels',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergeCommit',
        message: 'mergeCommit',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergeMethod',
        message: 'mergeMethod',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergeRequestedAt',
        message: 'mergeRequestedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'mergedAt',
        message: 'mergedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'metadata',
        message: 'metadata',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'parentId',
        message: 'parentId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'priority',
        message: 'priority',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'repositoryId',
        message: 'repositoryId',
        required: false,
      },
      {
        type: 'text',
        name: 'resolution',
        message: 'resolution',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'sourceRef',
        message: 'sourceRef',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'targetRef',
        message: 'targetRef',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'title',
        message: 'title',
        required: false,
      },
      {
        type: 'text',
        name: 'updatedBy',
        message: 'updatedBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformProposalPatch;
    if (argv['auto-embed']) {
      const embedder = resolveEmbedder();
      if (!embedder) {
        console.error(
          '--auto-embed requires an embedder. Set EMBEDDER_PROVIDER=ollama (and optionally EMBEDDER_MODEL, EMBEDDER_BASE_URL).'
        );
        process.exit(1);
      }
      await autoEmbedInput(cleanedData, ['embedding'], embedder);
    }
    const client = getClient();
    const result = await client.platformProposal
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          actorId: cleanedData.actorId,
          body: cleanedData.body,
          closedReason: cleanedData.closedReason,
          createdBy: cleanedData.createdBy,
          createdByPrincipal: cleanedData.createdByPrincipal,
          decidedAt: cleanedData.decidedAt,
          dueAt: cleanedData.dueAt,
          embedding: cleanedData.embedding,
          kind: cleanedData.kind,
          labels: cleanedData.labels,
          mergeCommit: cleanedData.mergeCommit,
          mergeMethod: cleanedData.mergeMethod,
          mergeRequestedAt: cleanedData.mergeRequestedAt,
          mergedAt: cleanedData.mergedAt,
          metadata: cleanedData.metadata,
          parentId: cleanedData.parentId,
          priority: cleanedData.priority,
          repositoryId: cleanedData.repositoryId,
          resolution: cleanedData.resolution,
          sourceRef: cleanedData.sourceRef,
          status: cleanedData.status,
          targetRef: cleanedData.targetRef,
          title: cleanedData.title,
          updatedBy: cleanedData.updatedBy,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
        },
        select: {
          actorId: true,
          body: true,
          closedReason: true,
          createdAt: true,
          createdBy: true,
          createdByPrincipal: true,
          decidedAt: true,
          dueAt: true,
          embedding: true,
          id: true,
          kind: true,
          labels: true,
          mergeCommit: true,
          mergeMethod: true,
          mergeRequestedAt: true,
          mergedAt: true,
          metadata: true,
          parentId: true,
          priority: true,
          repositoryId: true,
          resolution: true,
          sourceRef: true,
          status: true,
          targetRef: true,
          title: true,
          updatedAt: true,
          updatedBy: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformProposal
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
