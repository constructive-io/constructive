/**
 * CLI commands for ImageGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateImageGrantInput,
  ImageGrantPatch,
  ImageGrantSelect,
  ImageGrantFilter,
  ImageGrantOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  actions: 'string',
  createdAt: 'string',
  createdByPrincipal: 'uuid',
  databaseId: 'uuid',
  expiresAt: 'string',
  grantedBy: 'uuid',
  granteeKey: 'uuid',
  granteeScope: 'string',
  id: 'uuid',
  imageId: 'uuid',
  updatedAt: 'string',
  updatedByPrincipal: 'uuid',
};
const usage =
  '\nimage-grant <command>\n\nCommands:\n  list                  List imageGrant records\n  find-first            Find first matching imageGrant record\n  get                   Get a imageGrant by ID\n  create                Create a new imageGrant\n  update                Update an existing imageGrant\n  delete                Delete a imageGrant\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actions: true,
      createdAt: true,
      createdByPrincipal: true,
      databaseId: true,
      expiresAt: true,
      grantedBy: true,
      granteeKey: true,
      granteeScope: true,
      id: true,
      imageId: true,
      updatedAt: true,
      updatedByPrincipal: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<ImageGrantSelect, ImageGrantFilter, ImageGrantOrderBy> & {
        select: ImageGrantSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.imageGrant.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actions: true,
      createdAt: true,
      createdByPrincipal: true,
      databaseId: true,
      expiresAt: true,
      grantedBy: true,
      granteeKey: true,
      granteeScope: true,
      id: true,
      imageId: true,
      updatedAt: true,
      updatedByPrincipal: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<ImageGrantSelect, ImageGrantFilter, ImageGrantOrderBy> & {
        select: ImageGrantSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.imageGrant.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.imageGrant
      .findOne({
        id: answers.id as string,
        select: {
          actions: true,
          createdAt: true,
          createdByPrincipal: true,
          databaseId: true,
          expiresAt: true,
          grantedBy: true,
          granteeKey: true,
          granteeScope: true,
          id: true,
          imageId: true,
          updatedAt: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'actions',
        message: 'actions',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'expiresAt',
        message: 'expiresAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'grantedBy',
        message: 'grantedBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'granteeKey',
        message: 'granteeKey',
        required: true,
      },
      {
        type: 'text',
        name: 'granteeScope',
        message: 'granteeScope',
        required: true,
      },
      {
        type: 'text',
        name: 'imageId',
        message: 'imageId',
        required: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateImageGrantInput['imageGrant'];
    const client = getClient();
    const result = await client.imageGrant
      .create({
        data: {
          actions: cleanedData.actions,
          createdByPrincipal: cleanedData.createdByPrincipal,
          databaseId: cleanedData.databaseId,
          expiresAt: cleanedData.expiresAt,
          grantedBy: cleanedData.grantedBy,
          granteeKey: cleanedData.granteeKey,
          granteeScope: cleanedData.granteeScope,
          imageId: cleanedData.imageId,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
        },
        select: {
          actions: true,
          createdAt: true,
          createdByPrincipal: true,
          databaseId: true,
          expiresAt: true,
          grantedBy: true,
          granteeKey: true,
          granteeScope: true,
          id: true,
          imageId: true,
          updatedAt: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'actions',
        message: 'actions',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'expiresAt',
        message: 'expiresAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'grantedBy',
        message: 'grantedBy',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'granteeKey',
        message: 'granteeKey',
        required: false,
      },
      {
        type: 'text',
        name: 'granteeScope',
        message: 'granteeScope',
        required: false,
      },
      {
        type: 'text',
        name: 'imageId',
        message: 'imageId',
        required: false,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ImageGrantPatch;
    const client = getClient();
    const result = await client.imageGrant
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          actions: cleanedData.actions,
          createdByPrincipal: cleanedData.createdByPrincipal,
          databaseId: cleanedData.databaseId,
          expiresAt: cleanedData.expiresAt,
          grantedBy: cleanedData.grantedBy,
          granteeKey: cleanedData.granteeKey,
          granteeScope: cleanedData.granteeScope,
          imageId: cleanedData.imageId,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
        },
        select: {
          actions: true,
          createdAt: true,
          createdByPrincipal: true,
          databaseId: true,
          expiresAt: true,
          grantedBy: true,
          granteeKey: true,
          granteeScope: true,
          id: true,
          imageId: true,
          updatedAt: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.imageGrant
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
