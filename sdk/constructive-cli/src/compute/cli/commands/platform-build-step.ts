/**
 * CLI commands for PlatformBuildStep
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformBuildStepInput,
  PlatformBuildStepPatch,
  PlatformBuildStepSelect,
  PlatformBuildStepFilter,
  PlatformBuildStepOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  buildId: 'uuid',
  createdByPrincipal: 'uuid',
  exitCode: 'int',
  finishedAt: 'string',
  id: 'uuid',
  kind: 'string',
  logBytes: 'int',
  logOffset: 'int',
  name: 'string',
  parentSeq: 'int',
  recordedAt: 'string',
  seq: 'int',
  startedAt: 'string',
  status: 'string',
  summary: 'json',
};
const usage =
  '\nplatform-build-step <command>\n\nCommands:\n  list                  List platformBuildStep records\n  find-first            Find first matching platformBuildStep record\n  get                   Get a platformBuildStep by ID\n  create                Create a new platformBuildStep\n  update                Update an existing platformBuildStep\n  delete                Delete a platformBuildStep\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      buildId: true,
      createdByPrincipal: true,
      exitCode: true,
      finishedAt: true,
      id: true,
      kind: true,
      logBytes: true,
      logOffset: true,
      name: true,
      parentSeq: true,
      recordedAt: true,
      seq: true,
      startedAt: true,
      status: true,
      summary: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<PlatformBuildStepSelect, PlatformBuildStepFilter, PlatformBuildStepOrderBy> & {
        select: PlatformBuildStepSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformBuildStep.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      buildId: true,
      createdByPrincipal: true,
      exitCode: true,
      finishedAt: true,
      id: true,
      kind: true,
      logBytes: true,
      logOffset: true,
      name: true,
      parentSeq: true,
      recordedAt: true,
      seq: true,
      startedAt: true,
      status: true,
      summary: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<PlatformBuildStepSelect, PlatformBuildStepFilter, PlatformBuildStepOrderBy> & {
        select: PlatformBuildStepSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformBuildStep.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformBuildStep
      .findOne({
        id: answers.id as string,
        select: {
          buildId: true,
          createdByPrincipal: true,
          exitCode: true,
          finishedAt: true,
          id: true,
          kind: true,
          logBytes: true,
          logOffset: true,
          name: true,
          parentSeq: true,
          recordedAt: true,
          seq: true,
          startedAt: true,
          status: true,
          summary: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'buildId',
        message: 'buildId',
        required: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'exitCode',
        message: 'exitCode',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'finishedAt',
        message: 'finishedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'logBytes',
        message: 'logBytes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'logOffset',
        message: 'logOffset',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'text',
        name: 'parentSeq',
        message: 'parentSeq',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'seq',
        message: 'seq',
        required: true,
      },
      {
        type: 'text',
        name: 'startedAt',
        message: 'startedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'summary',
        message: 'summary',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformBuildStepInput['platformBuildStep'];
    const client = getClient();
    const result = await client.platformBuildStep
      .create({
        data: {
          buildId: cleanedData.buildId,
          createdByPrincipal: cleanedData.createdByPrincipal,
          exitCode: cleanedData.exitCode,
          finishedAt: cleanedData.finishedAt,
          kind: cleanedData.kind,
          logBytes: cleanedData.logBytes,
          logOffset: cleanedData.logOffset,
          name: cleanedData.name,
          parentSeq: cleanedData.parentSeq,
          recordedAt: cleanedData.recordedAt,
          seq: cleanedData.seq,
          startedAt: cleanedData.startedAt,
          status: cleanedData.status,
          summary: cleanedData.summary,
        },
        select: {
          buildId: true,
          createdByPrincipal: true,
          exitCode: true,
          finishedAt: true,
          id: true,
          kind: true,
          logBytes: true,
          logOffset: true,
          name: true,
          parentSeq: true,
          recordedAt: true,
          seq: true,
          startedAt: true,
          status: true,
          summary: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: true,
      },
      {
        type: 'text',
        name: 'buildId',
        message: 'buildId',
        required: false,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'exitCode',
        message: 'exitCode',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'finishedAt',
        message: 'finishedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'logBytes',
        message: 'logBytes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'logOffset',
        message: 'logOffset',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'text',
        name: 'parentSeq',
        message: 'parentSeq',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'seq',
        message: 'seq',
        required: false,
      },
      {
        type: 'text',
        name: 'startedAt',
        message: 'startedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'summary',
        message: 'summary',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformBuildStepPatch;
    const client = getClient();
    const result = await client.platformBuildStep
      .update({
        where: {
          id: answers.id as string,
          recordedAt: answers.recordedAt as string,
        },
        data: {
          buildId: cleanedData.buildId,
          createdByPrincipal: cleanedData.createdByPrincipal,
          exitCode: cleanedData.exitCode,
          finishedAt: cleanedData.finishedAt,
          kind: cleanedData.kind,
          logBytes: cleanedData.logBytes,
          logOffset: cleanedData.logOffset,
          name: cleanedData.name,
          parentSeq: cleanedData.parentSeq,
          seq: cleanedData.seq,
          startedAt: cleanedData.startedAt,
          status: cleanedData.status,
          summary: cleanedData.summary,
        },
        select: {
          buildId: true,
          createdByPrincipal: true,
          exitCode: true,
          finishedAt: true,
          id: true,
          kind: true,
          logBytes: true,
          logOffset: true,
          name: true,
          parentSeq: true,
          recordedAt: true,
          seq: true,
          startedAt: true,
          status: true,
          summary: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformBuildStep
      .delete({
        where: {
          id: answers.id as string,
          recordedAt: answers.recordedAt as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
