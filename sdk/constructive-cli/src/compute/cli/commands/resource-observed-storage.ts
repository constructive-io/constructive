/**
 * CLI commands for ResourceObservedStorage
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateResourceObservedStorageInput,
  ResourceObservedStoragePatch,
  ResourceObservedStorageSelect,
  ResourceObservedStorageFilter,
  ResourceObservedStorageOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  capacity: 'string',
  capacityBytes: 'int',
  claimName: 'string',
  declaredStorageClass: 'string',
  declaredStorageSizeBytes: 'int',
  declaredStorageTotalBytes: 'int',
  installationId: 'uuid',
  isBound: 'boolean',
  kind: 'string',
  namespaceId: 'uuid',
  phase: 'string',
  requested: 'string',
  requestedBytes: 'int',
  resourceId: 'uuid',
  resourceStatus: 'string',
  slug: 'string',
  storageClass: 'string',
  storageName: 'string',
};
const usage =
  '\nresource-observed-storage <command>\n\nCommands:\n  list                  List resourceObservedStorage records\n  find-first            Find first matching resourceObservedStorage record\n  create                Create a new resourceObservedStorage\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'create'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      capacity: true,
      capacityBytes: true,
      claimName: true,
      declaredStorageClass: true,
      declaredStorageSizeBytes: true,
      declaredStorageTotalBytes: true,
      installationId: true,
      isBound: true,
      kind: true,
      namespaceId: true,
      phase: true,
      requested: true,
      requestedBytes: true,
      resourceId: true,
      resourceStatus: true,
      slug: true,
      storageClass: true,
      storageName: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        ResourceObservedStorageSelect,
        ResourceObservedStorageFilter,
        ResourceObservedStorageOrderBy
      > & {
        select: ResourceObservedStorageSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.resourceObservedStorage.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      capacity: true,
      capacityBytes: true,
      claimName: true,
      declaredStorageClass: true,
      declaredStorageSizeBytes: true,
      declaredStorageTotalBytes: true,
      installationId: true,
      isBound: true,
      kind: true,
      namespaceId: true,
      phase: true,
      requested: true,
      requestedBytes: true,
      resourceId: true,
      resourceStatus: true,
      slug: true,
      storageClass: true,
      storageName: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        ResourceObservedStorageSelect,
        ResourceObservedStorageFilter,
        ResourceObservedStorageOrderBy
      > & {
        select: ResourceObservedStorageSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.resourceObservedStorage.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'capacity',
        message: 'capacity',
        required: true,
      },
      {
        type: 'text',
        name: 'capacityBytes',
        message: 'capacityBytes',
        required: true,
      },
      {
        type: 'text',
        name: 'claimName',
        message: 'claimName',
        required: true,
      },
      {
        type: 'text',
        name: 'declaredStorageClass',
        message: 'declaredStorageClass',
        required: true,
      },
      {
        type: 'text',
        name: 'declaredStorageSizeBytes',
        message: 'declaredStorageSizeBytes',
        required: true,
      },
      {
        type: 'text',
        name: 'declaredStorageTotalBytes',
        message: 'declaredStorageTotalBytes',
        required: true,
      },
      {
        type: 'text',
        name: 'installationId',
        message: 'installationId',
        required: true,
      },
      {
        type: 'boolean',
        name: 'isBound',
        message: 'isBound',
        required: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: true,
      },
      {
        type: 'text',
        name: 'namespaceId',
        message: 'namespaceId',
        required: true,
      },
      {
        type: 'text',
        name: 'phase',
        message: 'phase',
        required: true,
      },
      {
        type: 'text',
        name: 'requested',
        message: 'requested',
        required: true,
      },
      {
        type: 'text',
        name: 'requestedBytes',
        message: 'requestedBytes',
        required: true,
      },
      {
        type: 'text',
        name: 'resourceId',
        message: 'resourceId',
        required: true,
      },
      {
        type: 'text',
        name: 'resourceStatus',
        message: 'resourceStatus',
        required: true,
      },
      {
        type: 'text',
        name: 'slug',
        message: 'slug',
        required: true,
      },
      {
        type: 'text',
        name: 'storageClass',
        message: 'storageClass',
        required: true,
      },
      {
        type: 'text',
        name: 'storageName',
        message: 'storageName',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateResourceObservedStorageInput['resourceObservedStorage'];
    const client = getClient();
    const result = await client.resourceObservedStorage
      .create({
        data: {
          capacity: cleanedData.capacity,
          capacityBytes: cleanedData.capacityBytes,
          claimName: cleanedData.claimName,
          declaredStorageClass: cleanedData.declaredStorageClass,
          declaredStorageSizeBytes: cleanedData.declaredStorageSizeBytes,
          declaredStorageTotalBytes: cleanedData.declaredStorageTotalBytes,
          installationId: cleanedData.installationId,
          isBound: cleanedData.isBound,
          kind: cleanedData.kind,
          namespaceId: cleanedData.namespaceId,
          phase: cleanedData.phase,
          requested: cleanedData.requested,
          requestedBytes: cleanedData.requestedBytes,
          resourceId: cleanedData.resourceId,
          resourceStatus: cleanedData.resourceStatus,
          slug: cleanedData.slug,
          storageClass: cleanedData.storageClass,
          storageName: cleanedData.storageName,
        },
        select: {
          capacity: true,
          capacityBytes: true,
          claimName: true,
          declaredStorageClass: true,
          declaredStorageSizeBytes: true,
          declaredStorageTotalBytes: true,
          installationId: true,
          isBound: true,
          kind: true,
          namespaceId: true,
          phase: true,
          requested: true,
          requestedBytes: true,
          resourceId: true,
          resourceStatus: true,
          slug: true,
          storageClass: true,
          storageName: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
