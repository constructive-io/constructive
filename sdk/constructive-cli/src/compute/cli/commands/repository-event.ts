/**
 * CLI commands for RepositoryEvent
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateRepositoryEventInput,
  RepositoryEventPatch,
  RepositoryEventSelect,
  RepositoryEventFilter,
  RepositoryEventOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  actorId: 'uuid',
  commitSha: 'string',
  createdAt: 'string',
  createdByPrincipal: 'uuid',
  databaseId: 'uuid',
  deliveryId: 'string',
  eventType: 'string',
  id: 'uuid',
  metadata: 'json',
  payload: 'json',
  ref: 'string',
  repositoryId: 'uuid',
  updatedAt: 'string',
  updatedByPrincipal: 'uuid',
};
const usage =
  '\nrepository-event <command>\n\nCommands:\n  list                  List repositoryEvent records\n  find-first            Find first matching repositoryEvent record\n  get                   Get a repositoryEvent by ID\n  create                Create a new repositoryEvent\n  update                Update an existing repositoryEvent\n  delete                Delete a repositoryEvent\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      commitSha: true,
      createdAt: true,
      createdByPrincipal: true,
      databaseId: true,
      deliveryId: true,
      eventType: true,
      id: true,
      metadata: true,
      payload: true,
      ref: true,
      repositoryId: true,
      updatedAt: true,
      updatedByPrincipal: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<RepositoryEventSelect, RepositoryEventFilter, RepositoryEventOrderBy> & {
        select: RepositoryEventSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.repositoryEvent.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      commitSha: true,
      createdAt: true,
      createdByPrincipal: true,
      databaseId: true,
      deliveryId: true,
      eventType: true,
      id: true,
      metadata: true,
      payload: true,
      ref: true,
      repositoryId: true,
      updatedAt: true,
      updatedByPrincipal: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<RepositoryEventSelect, RepositoryEventFilter, RepositoryEventOrderBy> & {
        select: RepositoryEventSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.repositoryEvent.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.repositoryEvent
      .findOne({
        id: answers.id as string,
        select: {
          actorId: true,
          commitSha: true,
          createdAt: true,
          createdByPrincipal: true,
          databaseId: true,
          deliveryId: true,
          eventType: true,
          id: true,
          metadata: true,
          payload: true,
          ref: true,
          repositoryId: true,
          updatedAt: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'commitSha',
        message: 'commitSha',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: true,
      },
      {
        type: 'text',
        name: 'deliveryId',
        message: 'deliveryId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'eventType',
        message: 'eventType',
        required: true,
      },
      {
        type: 'json',
        name: 'metadata',
        message: 'metadata',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'payload',
        message: 'payload',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'ref',
        message: 'ref',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'repositoryId',
        message: 'repositoryId',
        required: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateRepositoryEventInput['repositoryEvent'];
    const client = getClient();
    const result = await client.repositoryEvent
      .create({
        data: {
          actorId: cleanedData.actorId,
          commitSha: cleanedData.commitSha,
          createdByPrincipal: cleanedData.createdByPrincipal,
          databaseId: cleanedData.databaseId,
          deliveryId: cleanedData.deliveryId,
          eventType: cleanedData.eventType,
          metadata: cleanedData.metadata,
          payload: cleanedData.payload,
          ref: cleanedData.ref,
          repositoryId: cleanedData.repositoryId,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
        },
        select: {
          actorId: true,
          commitSha: true,
          createdAt: true,
          createdByPrincipal: true,
          databaseId: true,
          deliveryId: true,
          eventType: true,
          id: true,
          metadata: true,
          payload: true,
          ref: true,
          repositoryId: true,
          updatedAt: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'commitSha',
        message: 'commitSha',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'databaseId',
        message: 'databaseId',
        required: false,
      },
      {
        type: 'text',
        name: 'deliveryId',
        message: 'deliveryId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'eventType',
        message: 'eventType',
        required: false,
      },
      {
        type: 'json',
        name: 'metadata',
        message: 'metadata',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'payload',
        message: 'payload',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'ref',
        message: 'ref',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'repositoryId',
        message: 'repositoryId',
        required: false,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as RepositoryEventPatch;
    const client = getClient();
    const result = await client.repositoryEvent
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          actorId: cleanedData.actorId,
          commitSha: cleanedData.commitSha,
          createdByPrincipal: cleanedData.createdByPrincipal,
          databaseId: cleanedData.databaseId,
          deliveryId: cleanedData.deliveryId,
          eventType: cleanedData.eventType,
          metadata: cleanedData.metadata,
          payload: cleanedData.payload,
          ref: cleanedData.ref,
          repositoryId: cleanedData.repositoryId,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
        },
        select: {
          actorId: true,
          commitSha: true,
          createdAt: true,
          createdByPrincipal: true,
          databaseId: true,
          deliveryId: true,
          eventType: true,
          id: true,
          metadata: true,
          payload: true,
          ref: true,
          repositoryId: true,
          updatedAt: true,
          updatedByPrincipal: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.repositoryEvent
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
