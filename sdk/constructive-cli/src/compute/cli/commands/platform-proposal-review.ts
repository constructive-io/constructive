/**
 * CLI commands for PlatformProposalReview
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformProposalReviewInput,
  PlatformProposalReviewPatch,
  PlatformProposalReviewSelect,
  PlatformProposalReviewFilter,
  PlatformProposalReviewOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  body: 'string',
  bodyTrgmSimilarity: 'float',
  commitSha: 'string',
  commitShaTrgmSimilarity: 'float',
  createdAt: 'string',
  createdByPrincipal: 'uuid',
  id: 'uuid',
  proposalId: 'uuid',
  reviewerId: 'uuid',
  search: 'string',
  searchScore: 'float',
  searchTsvRank: 'float',
  submittedAt: 'string',
  updatedAt: 'string',
  updatedByPrincipal: 'uuid',
  verdict: 'string',
  verdictTrgmSimilarity: 'float',
};
const usage =
  '\nplatform-proposal-review <command>\n\nCommands:\n  list                  List platformProposalReview records\n  find-first            Find first matching platformProposalReview record\n  search <query>        Search platformProposalReview records\n  get                   Get a platformProposalReview by ID\n  create                Create a new platformProposalReview\n  update                Update an existing platformProposalReview\n  delete                Delete a platformProposalReview\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nSearch Options:\n  <query>               Search query string (required)\n  --limit <n>           Max number of records to return\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --orderBy <values>    Comma-separated list of ordering values\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'search', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'search':
      return handleSearch(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      body: true,
      commitSha: true,
      createdAt: true,
      createdByPrincipal: true,
      id: true,
      proposalId: true,
      reviewerId: true,
      submittedAt: true,
      updatedAt: true,
      updatedByPrincipal: true,
      verdict: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformProposalReviewSelect,
        PlatformProposalReviewFilter,
        PlatformProposalReviewOrderBy
      > & {
        select: PlatformProposalReviewSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformProposalReview.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      body: true,
      commitSha: true,
      createdAt: true,
      createdByPrincipal: true,
      id: true,
      proposalId: true,
      reviewerId: true,
      submittedAt: true,
      updatedAt: true,
      updatedByPrincipal: true,
      verdict: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformProposalReviewSelect,
        PlatformProposalReviewFilter,
        PlatformProposalReviewOrderBy
      > & {
        select: PlatformProposalReviewSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformProposalReview.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleSearch(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const query = Array.isArray(argv._) && argv._.length > 0 ? String(argv._[0]) : undefined;
    if (!query) {
      console.error('Error: search requires a <query> argument');
      process.exit(1);
    }
    const searchWhere = {
      trgmBody: {
        value: query,
        threshold: 0.3,
      },
      trgmCommitSha: {
        value: query,
        threshold: 0.3,
      },
      search: {
        query,
      },
      trgmVerdict: {
        value: query,
        threshold: 0.3,
      },
    };
    const defaultSelect = {
      body: true,
      commitSha: true,
      createdAt: true,
      createdByPrincipal: true,
      id: true,
      proposalId: true,
      reviewerId: true,
      submittedAt: true,
      updatedAt: true,
      updatedByPrincipal: true,
      verdict: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformProposalReviewSelect,
        PlatformProposalReviewFilter,
        PlatformProposalReviewOrderBy
      > & {
        select: PlatformProposalReviewSelect;
      }
    >(argv, defaultSelect, searchWhere);
    const client = getClient();
    const result = await client.platformProposalReview.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to search records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformProposalReview
      .findOne({
        id: answers.id as string,
        select: {
          body: true,
          commitSha: true,
          createdAt: true,
          createdByPrincipal: true,
          id: true,
          proposalId: true,
          reviewerId: true,
          submittedAt: true,
          updatedAt: true,
          updatedByPrincipal: true,
          verdict: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'body',
        message: 'body',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'commitSha',
        message: 'commitSha',
        required: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'proposalId',
        message: 'proposalId',
        required: true,
      },
      {
        type: 'text',
        name: 'reviewerId',
        message: 'reviewerId',
        required: true,
      },
      {
        type: 'text',
        name: 'submittedAt',
        message: 'submittedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'verdict',
        message: 'verdict',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformProposalReviewInput['platformProposalReview'];
    const client = getClient();
    const result = await client.platformProposalReview
      .create({
        data: {
          body: cleanedData.body,
          commitSha: cleanedData.commitSha,
          createdByPrincipal: cleanedData.createdByPrincipal,
          proposalId: cleanedData.proposalId,
          reviewerId: cleanedData.reviewerId,
          submittedAt: cleanedData.submittedAt,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
          verdict: cleanedData.verdict,
        },
        select: {
          body: true,
          commitSha: true,
          createdAt: true,
          createdByPrincipal: true,
          id: true,
          proposalId: true,
          reviewerId: true,
          submittedAt: true,
          updatedAt: true,
          updatedByPrincipal: true,
          verdict: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'body',
        message: 'body',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'commitSha',
        message: 'commitSha',
        required: false,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'proposalId',
        message: 'proposalId',
        required: false,
      },
      {
        type: 'text',
        name: 'reviewerId',
        message: 'reviewerId',
        required: false,
      },
      {
        type: 'text',
        name: 'submittedAt',
        message: 'submittedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'verdict',
        message: 'verdict',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformProposalReviewPatch;
    const client = getClient();
    const result = await client.platformProposalReview
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          body: cleanedData.body,
          commitSha: cleanedData.commitSha,
          createdByPrincipal: cleanedData.createdByPrincipal,
          proposalId: cleanedData.proposalId,
          reviewerId: cleanedData.reviewerId,
          submittedAt: cleanedData.submittedAt,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
          verdict: cleanedData.verdict,
        },
        select: {
          body: true,
          commitSha: true,
          createdAt: true,
          createdByPrincipal: true,
          id: true,
          proposalId: true,
          reviewerId: true,
          submittedAt: true,
          updatedAt: true,
          updatedByPrincipal: true,
          verdict: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformProposalReview
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
