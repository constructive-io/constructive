/**
 * CLI command for query readFunctionGraph
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import type { ReadFunctionGraphVariables } from '../../orm/query';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'read-function-graph - readFunctionGraph\n\nUsage: read-function-graph [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'graphId',
        message: 'graphId',
      },
    ]);
    const client = getClient();
    const result = await client.query
      .readFunctionGraph(answers as unknown as ReadFunctionGraphVariables)
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: readFunctionGraph');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
