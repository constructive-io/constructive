/**
 * CLI commands for PlatformProposalFileView
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreatePlatformProposalFileViewInput,
  PlatformProposalFileViewPatch,
  PlatformProposalFileViewSelect,
  PlatformProposalFileViewFilter,
  PlatformProposalFileViewOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  blobSha: 'string',
  createdAt: 'string',
  createdByPrincipal: 'uuid',
  id: 'uuid',
  path: 'string',
  proposalId: 'uuid',
  reviewerId: 'uuid',
  updatedAt: 'string',
  updatedByPrincipal: 'uuid',
  viewedAt: 'string',
};
const usage =
  '\nplatform-proposal-file-view <command>\n\nCommands:\n  list                  List platformProposalFileView records\n  find-first            Find first matching platformProposalFileView record\n  get                   Get a platformProposalFileView by ID\n  create                Create a new platformProposalFileView\n  update                Update an existing platformProposalFileView\n  delete                Delete a platformProposalFileView\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      blobSha: true,
      createdAt: true,
      createdByPrincipal: true,
      id: true,
      path: true,
      proposalId: true,
      reviewerId: true,
      updatedAt: true,
      updatedByPrincipal: true,
      viewedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<
        PlatformProposalFileViewSelect,
        PlatformProposalFileViewFilter,
        PlatformProposalFileViewOrderBy
      > & {
        select: PlatformProposalFileViewSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformProposalFileView.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      blobSha: true,
      createdAt: true,
      createdByPrincipal: true,
      id: true,
      path: true,
      proposalId: true,
      reviewerId: true,
      updatedAt: true,
      updatedByPrincipal: true,
      viewedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<
        PlatformProposalFileViewSelect,
        PlatformProposalFileViewFilter,
        PlatformProposalFileViewOrderBy
      > & {
        select: PlatformProposalFileViewSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.platformProposalFileView.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.platformProposalFileView
      .findOne({
        id: answers.id as string,
        select: {
          blobSha: true,
          createdAt: true,
          createdByPrincipal: true,
          id: true,
          path: true,
          proposalId: true,
          reviewerId: true,
          updatedAt: true,
          updatedByPrincipal: true,
          viewedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'blobSha',
        message: 'blobSha',
        required: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'path',
        message: 'path',
        required: true,
      },
      {
        type: 'text',
        name: 'proposalId',
        message: 'proposalId',
        required: true,
      },
      {
        type: 'text',
        name: 'reviewerId',
        message: 'reviewerId',
        required: true,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'viewedAt',
        message: 'viewedAt',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreatePlatformProposalFileViewInput['platformProposalFileView'];
    const client = getClient();
    const result = await client.platformProposalFileView
      .create({
        data: {
          blobSha: cleanedData.blobSha,
          createdByPrincipal: cleanedData.createdByPrincipal,
          path: cleanedData.path,
          proposalId: cleanedData.proposalId,
          reviewerId: cleanedData.reviewerId,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
          viewedAt: cleanedData.viewedAt,
        },
        select: {
          blobSha: true,
          createdAt: true,
          createdByPrincipal: true,
          id: true,
          path: true,
          proposalId: true,
          reviewerId: true,
          updatedAt: true,
          updatedByPrincipal: true,
          viewedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'blobSha',
        message: 'blobSha',
        required: false,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'path',
        message: 'path',
        required: false,
      },
      {
        type: 'text',
        name: 'proposalId',
        message: 'proposalId',
        required: false,
      },
      {
        type: 'text',
        name: 'reviewerId',
        message: 'reviewerId',
        required: false,
      },
      {
        type: 'text',
        name: 'updatedByPrincipal',
        message: 'updatedByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'viewedAt',
        message: 'viewedAt',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as PlatformProposalFileViewPatch;
    const client = getClient();
    const result = await client.platformProposalFileView
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          blobSha: cleanedData.blobSha,
          createdByPrincipal: cleanedData.createdByPrincipal,
          path: cleanedData.path,
          proposalId: cleanedData.proposalId,
          reviewerId: cleanedData.reviewerId,
          updatedByPrincipal: cleanedData.updatedByPrincipal,
          viewedAt: cleanedData.viewedAt,
        },
        select: {
          blobSha: true,
          createdAt: true,
          createdByPrincipal: true,
          id: true,
          path: true,
          proposalId: true,
          reviewerId: true,
          updatedAt: true,
          updatedByPrincipal: true,
          viewedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.platformProposalFileView
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
