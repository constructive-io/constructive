/**
 * CLI command for mutation databaseAddEdgeAndSave
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { DatabaseAddEdgeAndSaveVariables } from '../../orm/mutation';
import type { DatabaseAddEdgeAndSavePayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'database-add-edge-and-save - databaseAddEdgeAndSave\n\nUsage: database-add-edge-and-save [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .databaseAddEdgeAndSave(
        parsedAnswers as unknown as DatabaseAddEdgeAndSaveVariables,
        {
          select: selectFields,
        } as unknown as {
          select: DatabaseAddEdgeAndSavePayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: databaseAddEdgeAndSave');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
