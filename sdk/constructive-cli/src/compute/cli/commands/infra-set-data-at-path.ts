/**
 * CLI command for mutation infraSetDataAtPath
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { InfraSetDataAtPathVariables } from '../../orm/mutation';
import type { InfraSetDataAtPathPayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'infra-set-data-at-path - infraSetDataAtPath\n\nUsage: infra-set-data-at-path [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .infraSetDataAtPath(
        parsedAnswers as unknown as InfraSetDataAtPathVariables,
        {
          select: selectFields,
        } as unknown as {
          select: InfraSetDataAtPathPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: infraSetDataAtPath');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
