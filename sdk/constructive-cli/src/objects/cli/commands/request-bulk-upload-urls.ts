/**
 * CLI command for mutation requestBulkUploadUrls
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { RequestBulkUploadUrlsVariables } from '../../orm/mutation';
import type { RequestBulkUploadUrlsPayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'request-bulk-upload-urls - Request presigned URLs for uploading multiple files in a single batch.\nSubject to per-storage-module limits (max_bulk_files, max_bulk_total_size).\nEach file is processed independently \u2014 some may dedup while others get fresh URLs.\n\nUsage: request-bulk-upload-urls [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .requestBulkUploadUrls(
        parsedAnswers as unknown as RequestBulkUploadUrlsVariables,
        {
          select: selectFields,
        } as unknown as {
          select: RequestBulkUploadUrlsPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: requestBulkUploadUrls');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
