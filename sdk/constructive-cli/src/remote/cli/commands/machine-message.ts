/**
 * CLI commands for MachineMessage
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateMachineMessageInput,
  MachineMessagePatch,
  MachineMessageSelect,
  MachineMessageFilter,
  MachineMessageOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  actorId: 'uuid',
  content: 'json',
  createdByPrincipal: 'uuid',
  entityId: 'uuid',
  id: 'uuid',
  kind: 'string',
  ownerId: 'uuid',
  recordedAt: 'string',
  seq: 'int',
  sessionId: 'uuid',
};
const usage =
  '\nmachine-message <command>\n\nCommands:\n  list                  List machineMessage records\n  find-first            Find first matching machineMessage record\n  get                   Get a machineMessage by ID\n  create                Create a new machineMessage\n  update                Update an existing machineMessage\n  delete                Delete a machineMessage\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      content: true,
      createdByPrincipal: true,
      entityId: true,
      id: true,
      kind: true,
      ownerId: true,
      recordedAt: true,
      seq: true,
      sessionId: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<MachineMessageSelect, MachineMessageFilter, MachineMessageOrderBy> & {
        select: MachineMessageSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.machineMessage.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      actorId: true,
      content: true,
      createdByPrincipal: true,
      entityId: true,
      id: true,
      kind: true,
      ownerId: true,
      recordedAt: true,
      seq: true,
      sessionId: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<MachineMessageSelect, MachineMessageFilter, MachineMessageOrderBy> & {
        select: MachineMessageSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.machineMessage.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.machineMessage
      .findOne({
        id: answers.id as string,
        select: {
          actorId: true,
          content: true,
          createdByPrincipal: true,
          entityId: true,
          id: true,
          kind: true,
          ownerId: true,
          recordedAt: true,
          seq: true,
          sessionId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: true,
      },
      {
        type: 'json',
        name: 'content',
        message: 'content',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: true,
      },
      {
        type: 'text',
        name: 'ownerId',
        message: 'ownerId',
        required: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'seq',
        message: 'seq',
        required: true,
      },
      {
        type: 'text',
        name: 'sessionId',
        message: 'sessionId',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateMachineMessageInput['machineMessage'];
    const client = getClient();
    const result = await client.machineMessage
      .create({
        data: {
          actorId: cleanedData.actorId,
          content: cleanedData.content,
          createdByPrincipal: cleanedData.createdByPrincipal,
          entityId: cleanedData.entityId,
          kind: cleanedData.kind,
          ownerId: cleanedData.ownerId,
          recordedAt: cleanedData.recordedAt,
          seq: cleanedData.seq,
          sessionId: cleanedData.sessionId,
        },
        select: {
          actorId: true,
          content: true,
          createdByPrincipal: true,
          entityId: true,
          id: true,
          kind: true,
          ownerId: true,
          recordedAt: true,
          seq: true,
          sessionId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: true,
      },
      {
        type: 'text',
        name: 'actorId',
        message: 'actorId',
        required: false,
      },
      {
        type: 'json',
        name: 'content',
        message: 'content',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'createdByPrincipal',
        message: 'createdByPrincipal',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'kind',
        message: 'kind',
        required: false,
      },
      {
        type: 'text',
        name: 'ownerId',
        message: 'ownerId',
        required: false,
      },
      {
        type: 'text',
        name: 'seq',
        message: 'seq',
        required: false,
      },
      {
        type: 'text',
        name: 'sessionId',
        message: 'sessionId',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as MachineMessagePatch;
    const client = getClient();
    const result = await client.machineMessage
      .update({
        where: {
          id: answers.id as string,
          recordedAt: answers.recordedAt as string,
        },
        data: {
          actorId: cleanedData.actorId,
          content: cleanedData.content,
          createdByPrincipal: cleanedData.createdByPrincipal,
          entityId: cleanedData.entityId,
          kind: cleanedData.kind,
          ownerId: cleanedData.ownerId,
          seq: cleanedData.seq,
          sessionId: cleanedData.sessionId,
        },
        select: {
          actorId: true,
          content: true,
          createdByPrincipal: true,
          entityId: true,
          id: true,
          kind: true,
          ownerId: true,
          recordedAt: true,
          seq: true,
          sessionId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'recordedAt',
        message: 'recordedAt',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.machineMessage
      .delete({
        where: {
          id: answers.id as string,
          recordedAt: answers.recordedAt as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
