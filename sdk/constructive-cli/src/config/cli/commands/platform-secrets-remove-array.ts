/**
 * CLI command for mutation platformSecretsRemoveArray
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer } from 'inquirerer';
import { getClient } from '../executor';
import { unflattenDotNotation, buildSelectFromPaths } from '../utils';
import type { PlatformSecretsRemoveArrayVariables } from '../../orm/mutation';
import type { PlatformSecretsRemoveArrayPayloadSelect } from '../../orm/input-types';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  try {
    if (argv.help || argv.h) {
      console.log(
        'platform-secrets-remove-array - platformSecretsRemoveArray\n\nUsage: platform-secrets-remove-array [OPTIONS]\n'
      );
      process.exit(0);
    }
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'input',
        message:
          'The exclusive input argument for this mutation. An object type, make sure to see documentation for this object\u2019s fields.',
        required: true,
      },
    ]);
    const client = getClient();
    const parsedAnswers = unflattenDotNotation(answers);
    const selectFields = buildSelectFromPaths((argv.select as string) ?? 'clientMutationId');
    const result = await client.mutation
      .platformSecretsRemoveArray(
        parsedAnswers as unknown as PlatformSecretsRemoveArrayVariables,
        {
          select: selectFields,
        } as unknown as {
          select: PlatformSecretsRemoveArrayPayloadSelect;
        }
      )
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed: platformSecretsRemoveArray');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
};
