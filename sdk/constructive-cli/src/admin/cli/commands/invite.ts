/**
 * CLI commands for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateInviteInput, InvitePatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  email: 'string',
  senderId: 'uuid',
  inviteToken: 'string',
  inviteValid: 'boolean',
  inviteLimit: 'int',
  inviteCount: 'int',
  multiple: 'boolean',
  data: 'json',
  expiresAt: 'string',
  createdAt: 'string',
  updatedAt: 'string',
};
const usage =
  '\ninvite <command>\n\nCommands:\n  list                  List all invite records\n  create                Create a new invite\n  update                Update an existing invite\n  delete                Delete a invite\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.invite
      .findMany({
        select: {
          id: true,
          email: true,
          senderId: true,
          inviteToken: true,
          inviteValid: true,
          inviteLimit: true,
          inviteCount: true,
          multiple: true,
          data: true,
          expiresAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'email',
        message: 'email',
        required: false,
      },
      {
        type: 'text',
        name: 'senderId',
        message: 'senderId',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteToken',
        message: 'inviteToken',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteValid',
        message: 'inviteValid',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteLimit',
        message: 'inviteLimit',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteCount',
        message: 'inviteCount',
        required: false,
      },
      {
        type: 'text',
        name: 'multiple',
        message: 'multiple',
        required: false,
      },
      {
        type: 'text',
        name: 'data',
        message: 'data',
        required: false,
      },
      {
        type: 'text',
        name: 'expiresAt',
        message: 'expiresAt',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateInviteInput['invite'];
    const client = getClient();
    const result = await client.invite
      .create({
        data: {
          email: cleanedData.email,
          senderId: cleanedData.senderId,
          inviteToken: cleanedData.inviteToken,
          inviteValid: cleanedData.inviteValid,
          inviteLimit: cleanedData.inviteLimit,
          inviteCount: cleanedData.inviteCount,
          multiple: cleanedData.multiple,
          data: cleanedData.data,
          expiresAt: cleanedData.expiresAt,
        },
        select: {
          id: true,
          email: true,
          senderId: true,
          inviteToken: true,
          inviteValid: true,
          inviteLimit: true,
          inviteCount: true,
          multiple: true,
          data: true,
          expiresAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'email',
        message: 'email',
        required: false,
      },
      {
        type: 'text',
        name: 'senderId',
        message: 'senderId',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteToken',
        message: 'inviteToken',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteValid',
        message: 'inviteValid',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteLimit',
        message: 'inviteLimit',
        required: false,
      },
      {
        type: 'text',
        name: 'inviteCount',
        message: 'inviteCount',
        required: false,
      },
      {
        type: 'text',
        name: 'multiple',
        message: 'multiple',
        required: false,
      },
      {
        type: 'text',
        name: 'data',
        message: 'data',
        required: false,
      },
      {
        type: 'text',
        name: 'expiresAt',
        message: 'expiresAt',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as InvitePatch;
    const client = getClient();
    const result = await client.invite
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          email: cleanedData.email,
          senderId: cleanedData.senderId,
          inviteToken: cleanedData.inviteToken,
          inviteValid: cleanedData.inviteValid,
          inviteLimit: cleanedData.inviteLimit,
          inviteCount: cleanedData.inviteCount,
          multiple: cleanedData.multiple,
          data: cleanedData.data,
          expiresAt: cleanedData.expiresAt,
        },
        select: {
          id: true,
          email: true,
          senderId: true,
          inviteToken: true,
          inviteValid: true,
          inviteLimit: true,
          inviteCount: true,
          multiple: true,
          data: true,
          expiresAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.invite
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
