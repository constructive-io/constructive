/**
 * Update mutation hook for ContentPresetModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { contentPresetModuleKeys } from '../query-keys';
import { contentPresetModuleMutationKeys } from '../mutation-keys';
import type {
  ContentPresetModuleSelect,
  ContentPresetModuleWithRelations,
  ContentPresetModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ContentPresetModuleSelect,
  ContentPresetModuleWithRelations,
  ContentPresetModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a ContentPresetModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateContentPresetModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', contentPresetModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateContentPresetModuleMutation<S extends ContentPresetModuleSelect>(
  params: {
    selection: {
      fields: S & ContentPresetModuleSelect;
    } & HookStrictSelect<NoInfer<S>, ContentPresetModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateContentPresetModule: {
          contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        contentPresetModulePatch: ContentPresetModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateContentPresetModule: {
      contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    contentPresetModulePatch: ContentPresetModulePatch;
  }
>;
export function useUpdateContentPresetModuleMutation(
  params: {
    selection: SelectionConfig<ContentPresetModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        contentPresetModulePatch: ContentPresetModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ContentPresetModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: contentPresetModuleMutationKeys.all,
    mutationFn: ({
      id,
      contentPresetModulePatch,
    }: {
      id: string;
      contentPresetModulePatch: ContentPresetModulePatch;
    }) =>
      getClient()
        .contentPresetModule.update({
          where: {
            id,
          },
          data: contentPresetModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: contentPresetModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: contentPresetModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
