/**
 * Per-preset configuration for the warm database pool: sizing, TTL, and the platform service org that owns unclaimed warm databases.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPoolConfigKeys } from '../query-keys';
import { dbPoolConfigMutationKeys } from '../mutation-keys';
import type { DbPoolConfigSelect, DbPoolConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DbPoolConfigSelect, DbPoolConfigWithRelations } from '../../orm/input-types';
/**
 * Per-preset configuration for the warm database pool: sizing, TTL, and the platform service org that owns unclaimed warm databases.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDbPoolConfigMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteDbPoolConfigMutation<S extends DbPoolConfigSelect>(
  params: {
    selection: {
      fields: S & DbPoolConfigSelect;
    } & HookStrictSelect<NoInfer<S>, DbPoolConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteDbPoolConfig: {
          dbPoolConfig: InferSelectResult<DbPoolConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteDbPoolConfig: {
      dbPoolConfig: InferSelectResult<DbPoolConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteDbPoolConfigMutation(
  params: {
    selection: SelectionConfig<DbPoolConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DbPoolConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: dbPoolConfigMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .dbPoolConfig.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: dbPoolConfigKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: dbPoolConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
