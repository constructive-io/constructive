/**
 * Delete mutation hook for RateLimitsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { rateLimitsModuleKeys } from '../query-keys';
import { rateLimitsModuleMutationKeys } from '../mutation-keys';
import type { RateLimitsModuleSelect, RateLimitsModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RateLimitsModuleSelect, RateLimitsModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a RateLimitsModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRateLimitsModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRateLimitsModuleMutation<S extends RateLimitsModuleSelect>(
  params: {
    selection: {
      fields: S & RateLimitsModuleSelect;
    } & HookStrictSelect<NoInfer<S>, RateLimitsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRateLimitsModule: {
          rateLimitsModule: InferSelectResult<RateLimitsModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRateLimitsModule: {
      rateLimitsModule: InferSelectResult<RateLimitsModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRateLimitsModuleMutation(
  params: {
    selection: SelectionConfig<RateLimitsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RateLimitsModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: rateLimitsModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .rateLimitsModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: rateLimitsModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: rateLimitsModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
