/**
 * Per-preset configuration for the warm database pool: sizing, TTL, and the platform service org that owns unclaimed warm databases.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPoolConfigKeys } from '../query-keys';
import { dbPoolConfigMutationKeys } from '../mutation-keys';
import type {
  DbPoolConfigSelect,
  DbPoolConfigWithRelations,
  CreateDbPoolConfigInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DbPoolConfigSelect,
  DbPoolConfigWithRelations,
  CreateDbPoolConfigInput,
} from '../../orm/input-types';
/**
 * Per-preset configuration for the warm database pool: sizing, TTL, and the platform service org that owns unclaimed warm databases.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDbPoolConfigMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateDbPoolConfigMutation<S extends DbPoolConfigSelect>(
  params: {
    selection: {
      fields: S & DbPoolConfigSelect;
    } & HookStrictSelect<NoInfer<S>, DbPoolConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createDbPoolConfig: {
          dbPoolConfig: InferSelectResult<DbPoolConfigWithRelations, S>;
        };
      },
      Error,
      CreateDbPoolConfigInput['dbPoolConfig']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createDbPoolConfig: {
      dbPoolConfig: InferSelectResult<DbPoolConfigWithRelations, S>;
    };
  },
  Error,
  CreateDbPoolConfigInput['dbPoolConfig']
>;
export function useCreateDbPoolConfigMutation(
  params: {
    selection: SelectionConfig<DbPoolConfigSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateDbPoolConfigInput['dbPoolConfig']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<DbPoolConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: dbPoolConfigMutationKeys.create(),
    mutationFn: (data: CreateDbPoolConfigInput['dbPoolConfig']) =>
      getClient()
        .dbPoolConfig.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: dbPoolConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
