/**
 * Delete mutation hook for RouteModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { routeModuleKeys } from '../query-keys';
import { routeModuleMutationKeys } from '../mutation-keys';
import type { RouteModuleSelect, RouteModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RouteModuleSelect, RouteModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a RouteModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRouteModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRouteModuleMutation<S extends RouteModuleSelect>(
  params: {
    selection: {
      fields: S & RouteModuleSelect;
    } & HookStrictSelect<NoInfer<S>, RouteModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRouteModule: {
          routeModule: InferSelectResult<RouteModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRouteModule: {
      routeModule: InferSelectResult<RouteModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRouteModuleMutation(
  params: {
    selection: SelectionConfig<RouteModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RouteModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: routeModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .routeModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: routeModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: routeModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
