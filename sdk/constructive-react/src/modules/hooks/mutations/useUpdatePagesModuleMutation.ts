/**
 * Update mutation hook for PagesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { pagesModuleKeys } from '../query-keys';
import { pagesModuleMutationKeys } from '../mutation-keys';
import type {
  PagesModuleSelect,
  PagesModuleWithRelations,
  PagesModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PagesModuleSelect,
  PagesModuleWithRelations,
  PagesModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a PagesModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePagesModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', pagesModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePagesModuleMutation<S extends PagesModuleSelect>(
  params: {
    selection: {
      fields: S & PagesModuleSelect;
    } & HookStrictSelect<NoInfer<S>, PagesModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePagesModule: {
          pagesModule: InferSelectResult<PagesModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        pagesModulePatch: PagesModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePagesModule: {
      pagesModule: InferSelectResult<PagesModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    pagesModulePatch: PagesModulePatch;
  }
>;
export function useUpdatePagesModuleMutation(
  params: {
    selection: SelectionConfig<PagesModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        pagesModulePatch: PagesModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PagesModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: pagesModuleMutationKeys.all,
    mutationFn: ({ id, pagesModulePatch }: { id: string; pagesModulePatch: PagesModulePatch }) =>
      getClient()
        .pagesModule.update({
          where: {
            id,
          },
          data: pagesModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: pagesModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: pagesModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
