/**
 * Update mutation hook for EmailsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailsModuleKeys } from '../query-keys';
import { emailsModuleMutationKeys } from '../mutation-keys';
import type {
  EmailsModuleSelect,
  EmailsModuleWithRelations,
  EmailsModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailsModuleSelect,
  EmailsModuleWithRelations,
  EmailsModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a EmailsModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailsModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', emailsModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateEmailsModuleMutation<S extends EmailsModuleSelect>(
  params: {
    selection: {
      fields: S & EmailsModuleSelect;
    } & HookStrictSelect<NoInfer<S>, EmailsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateEmailsModule: {
          emailsModule: InferSelectResult<EmailsModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        emailsModulePatch: EmailsModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateEmailsModule: {
      emailsModule: InferSelectResult<EmailsModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    emailsModulePatch: EmailsModulePatch;
  }
>;
export function useUpdateEmailsModuleMutation(
  params: {
    selection: SelectionConfig<EmailsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        emailsModulePatch: EmailsModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailsModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailsModuleMutationKeys.all,
    mutationFn: ({ id, emailsModulePatch }: { id: string; emailsModulePatch: EmailsModulePatch }) =>
      getClient()
        .emailsModule.update({
          where: {
            id,
          },
          data: emailsModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: emailsModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: emailsModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
