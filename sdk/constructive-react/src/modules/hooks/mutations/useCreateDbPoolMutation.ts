/**
 * Warm database pool entries. Rows are inserted as warming (which enqueues a db_pool:warm_database job), become ready once provisioned, and are handed to users via metaschema_private.db_pool_claim (invoked by metaschema_public.request_database).
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPoolKeys } from '../query-keys';
import { dbPoolMutationKeys } from '../mutation-keys';
import type { DbPoolSelect, DbPoolWithRelations, CreateDbPoolInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DbPoolSelect, DbPoolWithRelations, CreateDbPoolInput } from '../../orm/input-types';
/**
 * Warm database pool entries. Rows are inserted as warming (which enqueues a db_pool:warm_database job), become ready once provisioned, and are handed to users via metaschema_private.db_pool_claim (invoked by metaschema_public.request_database).
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDbPoolMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateDbPoolMutation<S extends DbPoolSelect>(
  params: {
    selection: {
      fields: S & DbPoolSelect;
    } & HookStrictSelect<NoInfer<S>, DbPoolSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createDbPool: {
          dbPool: InferSelectResult<DbPoolWithRelations, S>;
        };
      },
      Error,
      CreateDbPoolInput['dbPool']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createDbPool: {
      dbPool: InferSelectResult<DbPoolWithRelations, S>;
    };
  },
  Error,
  CreateDbPoolInput['dbPool']
>;
export function useCreateDbPoolMutation(
  params: {
    selection: SelectionConfig<DbPoolSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateDbPoolInput['dbPool']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<DbPoolSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: dbPoolMutationKeys.create(),
    mutationFn: (data: CreateDbPoolInput['dbPool']) =>
      getClient()
        .dbPool.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: dbPoolKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
