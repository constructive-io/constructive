/**
 * Update mutation hook for SiteSurfaceModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteSurfaceModuleKeys } from '../query-keys';
import { siteSurfaceModuleMutationKeys } from '../mutation-keys';
import type {
  SiteSurfaceModuleSelect,
  SiteSurfaceModuleWithRelations,
  SiteSurfaceModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteSurfaceModuleSelect,
  SiteSurfaceModuleWithRelations,
  SiteSurfaceModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a SiteSurfaceModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteSurfaceModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteSurfaceModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteSurfaceModuleMutation<S extends SiteSurfaceModuleSelect>(
  params: {
    selection: {
      fields: S & SiteSurfaceModuleSelect;
    } & HookStrictSelect<NoInfer<S>, SiteSurfaceModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteSurfaceModule: {
          siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteSurfaceModulePatch: SiteSurfaceModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteSurfaceModule: {
      siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteSurfaceModulePatch: SiteSurfaceModulePatch;
  }
>;
export function useUpdateSiteSurfaceModuleMutation(
  params: {
    selection: SelectionConfig<SiteSurfaceModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteSurfaceModulePatch: SiteSurfaceModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteSurfaceModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteSurfaceModuleMutationKeys.all,
    mutationFn: ({
      id,
      siteSurfaceModulePatch,
    }: {
      id: string;
      siteSurfaceModulePatch: SiteSurfaceModulePatch;
    }) =>
      getClient()
        .siteSurfaceModule.update({
          where: {
            id,
          },
          data: siteSurfaceModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteSurfaceModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteSurfaceModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
