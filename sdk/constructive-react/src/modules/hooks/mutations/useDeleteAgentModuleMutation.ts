/**
 * Delete mutation hook for AgentModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentModuleKeys } from '../query-keys';
import { agentModuleMutationKeys } from '../mutation-keys';
import type { AgentModuleSelect, AgentModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentModuleSelect, AgentModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a AgentModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAgentModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAgentModuleMutation<S extends AgentModuleSelect>(
  params: {
    selection: {
      fields: S & AgentModuleSelect;
    } & HookStrictSelect<NoInfer<S>, AgentModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAgentModule: {
          agentModule: InferSelectResult<AgentModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAgentModule: {
      agentModule: InferSelectResult<AgentModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAgentModuleMutation(
  params: {
    selection: SelectionConfig<AgentModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .agentModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: agentModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
