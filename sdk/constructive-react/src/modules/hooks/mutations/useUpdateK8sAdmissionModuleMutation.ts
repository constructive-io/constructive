/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { k8sAdmissionModuleKeys } from '../query-keys';
import { k8sAdmissionModuleMutationKeys } from '../mutation-keys';
import type {
  K8sAdmissionModuleSelect,
  K8sAdmissionModuleWithRelations,
  K8sAdmissionModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  K8sAdmissionModuleSelect,
  K8sAdmissionModuleWithRelations,
  K8sAdmissionModulePatch,
} from '../../orm/input-types';
/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateK8sAdmissionModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', k8sAdmissionModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateK8sAdmissionModuleMutation<S extends K8sAdmissionModuleSelect>(
  params: {
    selection: {
      fields: S & K8sAdmissionModuleSelect;
    } & HookStrictSelect<NoInfer<S>, K8sAdmissionModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateK8sAdmissionModule: {
          k8sAdmissionModule: InferSelectResult<K8sAdmissionModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        k8sAdmissionModulePatch: K8sAdmissionModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateK8sAdmissionModule: {
      k8sAdmissionModule: InferSelectResult<K8sAdmissionModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    k8sAdmissionModulePatch: K8sAdmissionModulePatch;
  }
>;
export function useUpdateK8sAdmissionModuleMutation(
  params: {
    selection: SelectionConfig<K8sAdmissionModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        k8sAdmissionModulePatch: K8sAdmissionModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<K8sAdmissionModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: k8sAdmissionModuleMutationKeys.all,
    mutationFn: ({
      id,
      k8sAdmissionModulePatch,
    }: {
      id: string;
      k8sAdmissionModulePatch: K8sAdmissionModulePatch;
    }) =>
      getClient()
        .k8sAdmissionModule.update({
          where: {
            id,
          },
          data: k8sAdmissionModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: k8sAdmissionModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: k8sAdmissionModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
