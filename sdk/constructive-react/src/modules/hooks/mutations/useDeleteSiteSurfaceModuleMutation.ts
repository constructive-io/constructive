/**
 * Delete mutation hook for SiteSurfaceModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteSurfaceModuleKeys } from '../query-keys';
import { siteSurfaceModuleMutationKeys } from '../mutation-keys';
import type {
  SiteSurfaceModuleSelect,
  SiteSurfaceModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteSurfaceModuleSelect,
  SiteSurfaceModuleWithRelations,
} from '../../orm/input-types';
/**
 * Mutation hook for deleting a SiteSurfaceModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteSurfaceModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteSiteSurfaceModuleMutation<S extends SiteSurfaceModuleSelect>(
  params: {
    selection: {
      fields: S & SiteSurfaceModuleSelect;
    } & HookStrictSelect<NoInfer<S>, SiteSurfaceModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteSiteSurfaceModule: {
          siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteSiteSurfaceModule: {
      siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteSiteSurfaceModuleMutation(
  params: {
    selection: SelectionConfig<SiteSurfaceModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteSurfaceModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteSurfaceModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .siteSurfaceModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: siteSurfaceModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteSurfaceModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
