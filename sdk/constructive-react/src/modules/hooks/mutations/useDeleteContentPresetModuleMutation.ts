/**
 * Delete mutation hook for ContentPresetModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { contentPresetModuleKeys } from '../query-keys';
import { contentPresetModuleMutationKeys } from '../mutation-keys';
import type {
  ContentPresetModuleSelect,
  ContentPresetModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ContentPresetModuleSelect,
  ContentPresetModuleWithRelations,
} from '../../orm/input-types';
/**
 * Mutation hook for deleting a ContentPresetModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteContentPresetModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteContentPresetModuleMutation<S extends ContentPresetModuleSelect>(
  params: {
    selection: {
      fields: S & ContentPresetModuleSelect;
    } & HookStrictSelect<NoInfer<S>, ContentPresetModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteContentPresetModule: {
          contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteContentPresetModule: {
      contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteContentPresetModuleMutation(
  params: {
    selection: SelectionConfig<ContentPresetModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ContentPresetModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: contentPresetModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .contentPresetModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: contentPresetModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: contentPresetModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
