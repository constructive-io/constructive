/**
 * Update mutation hook for RouteModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { routeModuleKeys } from '../query-keys';
import { routeModuleMutationKeys } from '../mutation-keys';
import type {
  RouteModuleSelect,
  RouteModuleWithRelations,
  RouteModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RouteModuleSelect,
  RouteModuleWithRelations,
  RouteModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a RouteModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRouteModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', routeModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRouteModuleMutation<S extends RouteModuleSelect>(
  params: {
    selection: {
      fields: S & RouteModuleSelect;
    } & HookStrictSelect<NoInfer<S>, RouteModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRouteModule: {
          routeModule: InferSelectResult<RouteModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        routeModulePatch: RouteModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRouteModule: {
      routeModule: InferSelectResult<RouteModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    routeModulePatch: RouteModulePatch;
  }
>;
export function useUpdateRouteModuleMutation(
  params: {
    selection: SelectionConfig<RouteModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        routeModulePatch: RouteModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RouteModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: routeModuleMutationKeys.all,
    mutationFn: ({ id, routeModulePatch }: { id: string; routeModulePatch: RouteModulePatch }) =>
      getClient()
        .routeModule.update({
          where: {
            id,
          },
          data: routeModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: routeModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: routeModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
