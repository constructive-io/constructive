/**
 * Update mutation hook for RefusalLogModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { refusalLogModuleKeys } from '../query-keys';
import { refusalLogModuleMutationKeys } from '../mutation-keys';
import type {
  RefusalLogModuleSelect,
  RefusalLogModuleWithRelations,
  RefusalLogModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RefusalLogModuleSelect,
  RefusalLogModuleWithRelations,
  RefusalLogModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a RefusalLogModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRefusalLogModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', refusalLogModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRefusalLogModuleMutation<S extends RefusalLogModuleSelect>(
  params: {
    selection: {
      fields: S & RefusalLogModuleSelect;
    } & HookStrictSelect<NoInfer<S>, RefusalLogModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRefusalLogModule: {
          refusalLogModule: InferSelectResult<RefusalLogModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        refusalLogModulePatch: RefusalLogModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRefusalLogModule: {
      refusalLogModule: InferSelectResult<RefusalLogModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    refusalLogModulePatch: RefusalLogModulePatch;
  }
>;
export function useUpdateRefusalLogModuleMutation(
  params: {
    selection: SelectionConfig<RefusalLogModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        refusalLogModulePatch: RefusalLogModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RefusalLogModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: refusalLogModuleMutationKeys.all,
    mutationFn: ({
      id,
      refusalLogModulePatch,
    }: {
      id: string;
      refusalLogModulePatch: RefusalLogModulePatch;
    }) =>
      getClient()
        .refusalLogModule.update({
          where: {
            id,
          },
          data: refusalLogModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: refusalLogModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: refusalLogModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
