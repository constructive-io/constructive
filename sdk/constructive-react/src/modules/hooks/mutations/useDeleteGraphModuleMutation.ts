/**
 * Delete mutation hook for GraphModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { graphModuleKeys } from '../query-keys';
import { graphModuleMutationKeys } from '../mutation-keys';
import type { GraphModuleSelect, GraphModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { GraphModuleSelect, GraphModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a GraphModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteGraphModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteGraphModuleMutation<S extends GraphModuleSelect>(
  params: {
    selection: {
      fields: S & GraphModuleSelect;
    } & HookStrictSelect<NoInfer<S>, GraphModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteGraphModule: {
          graphModule: InferSelectResult<GraphModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteGraphModule: {
      graphModule: InferSelectResult<GraphModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteGraphModuleMutation(
  params: {
    selection: SelectionConfig<GraphModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<GraphModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: graphModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .graphModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: graphModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: graphModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
