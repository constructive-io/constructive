/**
 * Delete mutation hook for FileRefField
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { fileRefFieldKeys } from '../query-keys';
import { fileRefFieldMutationKeys } from '../mutation-keys';
import type { FileRefFieldSelect, FileRefFieldWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FileRefFieldSelect, FileRefFieldWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a FileRefField with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFileRefFieldMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteFileRefFieldMutation<S extends FileRefFieldSelect>(
  params: {
    selection: {
      fields: S & FileRefFieldSelect;
    } & HookStrictSelect<NoInfer<S>, FileRefFieldSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteFileRefField: {
          fileRefField: InferSelectResult<FileRefFieldWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteFileRefField: {
      fileRefField: InferSelectResult<FileRefFieldWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteFileRefFieldMutation(
  params: {
    selection: SelectionConfig<FileRefFieldSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<FileRefFieldSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: fileRefFieldMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .fileRefField.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: fileRefFieldKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: fileRefFieldKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
