/**
 * Delete mutation hook for LimitsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { limitsModuleKeys } from '../query-keys';
import { limitsModuleMutationKeys } from '../mutation-keys';
import type { LimitsModuleSelect, LimitsModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { LimitsModuleSelect, LimitsModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a LimitsModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteLimitsModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteLimitsModuleMutation<S extends LimitsModuleSelect>(
  params: {
    selection: {
      fields: S & LimitsModuleSelect;
    } & HookStrictSelect<NoInfer<S>, LimitsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteLimitsModule: {
          limitsModule: InferSelectResult<LimitsModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteLimitsModule: {
      limitsModule: InferSelectResult<LimitsModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteLimitsModuleMutation(
  params: {
    selection: SelectionConfig<LimitsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<LimitsModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: limitsModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .limitsModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: limitsModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: limitsModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
