/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { k8sAdmissionModuleKeys } from '../query-keys';
import { k8sAdmissionModuleMutationKeys } from '../mutation-keys';
import type {
  K8sAdmissionModuleSelect,
  K8sAdmissionModuleWithRelations,
  CreateK8sAdmissionModuleInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  K8sAdmissionModuleSelect,
  K8sAdmissionModuleWithRelations,
  CreateK8sAdmissionModuleInput,
} from '../../orm/input-types';
/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateK8sAdmissionModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateK8sAdmissionModuleMutation<S extends K8sAdmissionModuleSelect>(
  params: {
    selection: {
      fields: S & K8sAdmissionModuleSelect;
    } & HookStrictSelect<NoInfer<S>, K8sAdmissionModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createK8sAdmissionModule: {
          k8sAdmissionModule: InferSelectResult<K8sAdmissionModuleWithRelations, S>;
        };
      },
      Error,
      CreateK8sAdmissionModuleInput['k8sAdmissionModule']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createK8sAdmissionModule: {
      k8sAdmissionModule: InferSelectResult<K8sAdmissionModuleWithRelations, S>;
    };
  },
  Error,
  CreateK8sAdmissionModuleInput['k8sAdmissionModule']
>;
export function useCreateK8sAdmissionModuleMutation(
  params: {
    selection: SelectionConfig<K8sAdmissionModuleSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateK8sAdmissionModuleInput['k8sAdmissionModule']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<K8sAdmissionModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: k8sAdmissionModuleMutationKeys.create(),
    mutationFn: (data: CreateK8sAdmissionModuleInput['k8sAdmissionModule']) =>
      getClient()
        .k8sAdmissionModule.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: k8sAdmissionModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
