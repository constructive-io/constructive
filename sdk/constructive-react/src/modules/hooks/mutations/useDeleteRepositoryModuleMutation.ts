/**
 * Delete mutation hook for RepositoryModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryModuleKeys } from '../query-keys';
import { repositoryModuleMutationKeys } from '../mutation-keys';
import type { RepositoryModuleSelect, RepositoryModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RepositoryModuleSelect, RepositoryModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a RepositoryModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRepositoryModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRepositoryModuleMutation<S extends RepositoryModuleSelect>(
  params: {
    selection: {
      fields: S & RepositoryModuleSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRepositoryModule: {
          repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRepositoryModule: {
      repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRepositoryModuleMutation(
  params: {
    selection: SelectionConfig<RepositoryModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .repositoryModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: repositoryModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
