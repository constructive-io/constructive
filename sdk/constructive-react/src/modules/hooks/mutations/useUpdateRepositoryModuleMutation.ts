/**
 * Update mutation hook for RepositoryModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryModuleKeys } from '../query-keys';
import { repositoryModuleMutationKeys } from '../mutation-keys';
import type {
  RepositoryModuleSelect,
  RepositoryModuleWithRelations,
  RepositoryModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryModuleSelect,
  RepositoryModuleWithRelations,
  RepositoryModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a RepositoryModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRepositoryModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', repositoryModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRepositoryModuleMutation<S extends RepositoryModuleSelect>(
  params: {
    selection: {
      fields: S & RepositoryModuleSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRepositoryModule: {
          repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        repositoryModulePatch: RepositoryModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRepositoryModule: {
      repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    repositoryModulePatch: RepositoryModulePatch;
  }
>;
export function useUpdateRepositoryModuleMutation(
  params: {
    selection: SelectionConfig<RepositoryModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        repositoryModulePatch: RepositoryModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryModuleMutationKeys.all,
    mutationFn: ({
      id,
      repositoryModulePatch,
    }: {
      id: string;
      repositoryModulePatch: RepositoryModulePatch;
    }) =>
      getClient()
        .repositoryModule.update({
          where: {
            id,
          },
          data: repositoryModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: repositoryModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
