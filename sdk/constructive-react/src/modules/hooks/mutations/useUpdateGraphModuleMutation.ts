/**
 * Update mutation hook for GraphModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { graphModuleKeys } from '../query-keys';
import { graphModuleMutationKeys } from '../mutation-keys';
import type {
  GraphModuleSelect,
  GraphModuleWithRelations,
  GraphModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  GraphModuleSelect,
  GraphModuleWithRelations,
  GraphModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a GraphModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateGraphModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', graphModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateGraphModuleMutation<S extends GraphModuleSelect>(
  params: {
    selection: {
      fields: S & GraphModuleSelect;
    } & HookStrictSelect<NoInfer<S>, GraphModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateGraphModule: {
          graphModule: InferSelectResult<GraphModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        graphModulePatch: GraphModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateGraphModule: {
      graphModule: InferSelectResult<GraphModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    graphModulePatch: GraphModulePatch;
  }
>;
export function useUpdateGraphModuleMutation(
  params: {
    selection: SelectionConfig<GraphModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        graphModulePatch: GraphModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<GraphModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: graphModuleMutationKeys.all,
    mutationFn: ({ id, graphModulePatch }: { id: string; graphModulePatch: GraphModulePatch }) =>
      getClient()
        .graphModule.update({
          where: {
            id,
          },
          data: graphModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: graphModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: graphModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
