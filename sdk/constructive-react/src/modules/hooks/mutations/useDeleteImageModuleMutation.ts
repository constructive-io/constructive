/**
 * Delete mutation hook for ImageModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { imageModuleKeys } from '../query-keys';
import { imageModuleMutationKeys } from '../mutation-keys';
import type { ImageModuleSelect, ImageModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ImageModuleSelect, ImageModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a ImageModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteImageModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteImageModuleMutation<S extends ImageModuleSelect>(
  params: {
    selection: {
      fields: S & ImageModuleSelect;
    } & HookStrictSelect<NoInfer<S>, ImageModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteImageModule: {
          imageModule: InferSelectResult<ImageModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteImageModule: {
      imageModule: InferSelectResult<ImageModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteImageModuleMutation(
  params: {
    selection: SelectionConfig<ImageModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ImageModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: imageModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .imageModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: imageModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: imageModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
