/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { oauthRequestsModuleKeys } from '../query-keys';
import { oauthRequestsModuleMutationKeys } from '../mutation-keys';
import type {
  OauthRequestsModuleSelect,
  OauthRequestsModuleWithRelations,
  CreateOauthRequestsModuleInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OauthRequestsModuleSelect,
  OauthRequestsModuleWithRelations,
  CreateOauthRequestsModuleInput,
} from '../../orm/input-types';
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOauthRequestsModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 * 
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOauthRequestsModuleMutation<S extends OauthRequestsModuleSelect>(
  params: {
    selection: {
      fields: S & OauthRequestsModuleSelect;
    } & HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOauthRequestsModule: {
          oauthRequestsModule: InferSelectResult<OauthRequestsModuleWithRelations, S>;
        };
      },
      Error,
      CreateOauthRequestsModuleInput['oauthRequestsModule']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOauthRequestsModule: {
      oauthRequestsModule: InferSelectResult<OauthRequestsModuleWithRelations, S>;
    };
  },
  Error,
  CreateOauthRequestsModuleInput['oauthRequestsModule']
>;
export function useCreateOauthRequestsModuleMutation(
  params: {
    selection: SelectionConfig<OauthRequestsModuleSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateOauthRequestsModuleInput['oauthRequestsModule']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OauthRequestsModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: oauthRequestsModuleMutationKeys.create(),
    mutationFn: (data: CreateOauthRequestsModuleInput['oauthRequestsModule']) =>
      getClient()
        .oauthRequestsModule.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: oauthRequestsModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
