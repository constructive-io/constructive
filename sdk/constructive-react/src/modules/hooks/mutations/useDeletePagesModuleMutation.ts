/**
 * Delete mutation hook for PagesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { pagesModuleKeys } from '../query-keys';
import { pagesModuleMutationKeys } from '../mutation-keys';
import type { PagesModuleSelect, PagesModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PagesModuleSelect, PagesModuleWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a PagesModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePagesModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePagesModuleMutation<S extends PagesModuleSelect>(
  params: {
    selection: {
      fields: S & PagesModuleSelect;
    } & HookStrictSelect<NoInfer<S>, PagesModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePagesModule: {
          pagesModule: InferSelectResult<PagesModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePagesModule: {
      pagesModule: InferSelectResult<PagesModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePagesModuleMutation(
  params: {
    selection: SelectionConfig<PagesModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PagesModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: pagesModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .pagesModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: pagesModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: pagesModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
