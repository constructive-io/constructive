/**
 * Scope-aware plaintext internal config store. No namespace_module dependency and no K8s synchronization: values are read from the database at invocation time. Configuration that must be projected into a Kubernetes ConfigMap belongs in infra_config_module.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { internalConfigModuleKeys } from '../query-keys';
import type {
  InternalConfigModuleSelect,
  InternalConfigModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  InternalConfigModuleSelect,
  InternalConfigModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const internalConfigModuleQueryKey = internalConfigModuleKeys.detail;
/**
 * Scope-aware plaintext internal config store. No namespace_module dependency and no K8s synchronization: values are read from the database at invocation time. Configuration that must be projected into a Kubernetes ConfigMap belongs in infra_config_module.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInternalConfigModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useInternalConfigModuleQuery<
  S extends InternalConfigModuleSelect,
  TData = {
    internalConfigModule: InferSelectResult<InternalConfigModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InternalConfigModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        internalConfigModule: InferSelectResult<InternalConfigModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useInternalConfigModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<InternalConfigModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<InternalConfigModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: internalConfigModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .internalConfigModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Scope-aware plaintext internal config store. No namespace_module dependency and no K8s synchronization: values are read from the database at invocation time. Configuration that must be projected into a Kubernetes ConfigMap belongs in infra_config_module.
 *
 * @example
 * ```ts
 * const data = await fetchInternalConfigModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchInternalConfigModuleQuery<S extends InternalConfigModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, InternalConfigModuleSelect>;
}): Promise<{
  internalConfigModule: InferSelectResult<InternalConfigModuleWithRelations, S> | null;
}>;
export async function fetchInternalConfigModuleQuery(params: {
  id: string;
  selection: SelectionConfig<InternalConfigModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<InternalConfigModuleSelect>(params.selection);
  return getClient()
    .internalConfigModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Scope-aware plaintext internal config store. No namespace_module dependency and no K8s synchronization: values are read from the database at invocation time. Configuration that must be projected into a Kubernetes ConfigMap belongs in infra_config_module.
 *
 * @example
 * ```ts
 * await prefetchInternalConfigModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchInternalConfigModuleQuery<S extends InternalConfigModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InternalConfigModuleSelect>;
  }
): Promise<void>;
export async function prefetchInternalConfigModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<InternalConfigModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<InternalConfigModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: internalConfigModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .internalConfigModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
