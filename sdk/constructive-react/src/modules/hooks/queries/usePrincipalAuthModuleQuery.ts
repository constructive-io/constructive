/**
 * Provisions the principals subsystem: a principals table, a principal_entities junction table, create/delete mutations, and org API key management. Supports both human-owned principals (AuthzDirectOwner, AuthzHumanOnly) and org-owned principals (AuthzEntityMembership with is_admin). Org principal and org API key functions are only generated when an org-scoped memberships_module exists for the database.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { principalAuthModuleKeys } from '../query-keys';
import type {
  PrincipalAuthModuleSelect,
  PrincipalAuthModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PrincipalAuthModuleSelect,
  PrincipalAuthModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const principalAuthModuleQueryKey = principalAuthModuleKeys.detail;
/**
 * Provisions the principals subsystem: a principals table, a principal_entities junction table, create/delete mutations, and org API key management. Supports both human-owned principals (AuthzDirectOwner, AuthzHumanOnly) and org-owned principals (AuthzEntityMembership with is_admin). Org principal and org API key functions are only generated when an org-scoped memberships_module exists for the database.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePrincipalAuthModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePrincipalAuthModuleQuery<
  S extends PrincipalAuthModuleSelect,
  TData = {
    principalAuthModule: InferSelectResult<PrincipalAuthModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PrincipalAuthModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        principalAuthModule: InferSelectResult<PrincipalAuthModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePrincipalAuthModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<PrincipalAuthModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PrincipalAuthModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: principalAuthModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .principalAuthModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Provisions the principals subsystem: a principals table, a principal_entities junction table, create/delete mutations, and org API key management. Supports both human-owned principals (AuthzDirectOwner, AuthzHumanOnly) and org-owned principals (AuthzEntityMembership with is_admin). Org principal and org API key functions are only generated when an org-scoped memberships_module exists for the database.
 *
 * @example
 * ```ts
 * const data = await fetchPrincipalAuthModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPrincipalAuthModuleQuery<S extends PrincipalAuthModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PrincipalAuthModuleSelect>;
}): Promise<{
  principalAuthModule: InferSelectResult<PrincipalAuthModuleWithRelations, S> | null;
}>;
export async function fetchPrincipalAuthModuleQuery(params: {
  id: string;
  selection: SelectionConfig<PrincipalAuthModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PrincipalAuthModuleSelect>(params.selection);
  return getClient()
    .principalAuthModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Provisions the principals subsystem: a principals table, a principal_entities junction table, create/delete mutations, and org API key management. Supports both human-owned principals (AuthzDirectOwner, AuthzHumanOnly) and org-owned principals (AuthzEntityMembership with is_admin). Org principal and org API key functions are only generated when an org-scoped memberships_module exists for the database.
 *
 * @example
 * ```ts
 * await prefetchPrincipalAuthModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPrincipalAuthModuleQuery<S extends PrincipalAuthModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PrincipalAuthModuleSelect>;
  }
): Promise<void>;
export async function prefetchPrincipalAuthModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PrincipalAuthModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PrincipalAuthModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: principalAuthModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .principalAuthModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
