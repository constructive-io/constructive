/**
 * Single item query hook for ContentPresetModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { contentPresetModuleKeys } from '../query-keys';
import type {
  ContentPresetModuleSelect,
  ContentPresetModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ContentPresetModuleSelect,
  ContentPresetModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const contentPresetModuleQueryKey = contentPresetModuleKeys.detail;
/**
 * Query hook for fetching a single ContentPresetModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useContentPresetModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useContentPresetModuleQuery<
  S extends ContentPresetModuleSelect,
  TData = {
    contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ContentPresetModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useContentPresetModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<ContentPresetModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ContentPresetModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: contentPresetModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .contentPresetModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single ContentPresetModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchContentPresetModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchContentPresetModuleQuery<S extends ContentPresetModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ContentPresetModuleSelect>;
}): Promise<{
  contentPresetModule: InferSelectResult<ContentPresetModuleWithRelations, S> | null;
}>;
export async function fetchContentPresetModuleQuery(params: {
  id: string;
  selection: SelectionConfig<ContentPresetModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ContentPresetModuleSelect>(params.selection);
  return getClient()
    .contentPresetModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single ContentPresetModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchContentPresetModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchContentPresetModuleQuery<S extends ContentPresetModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ContentPresetModuleSelect>;
  }
): Promise<void>;
export async function prefetchContentPresetModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ContentPresetModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ContentPresetModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: contentPresetModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .contentPresetModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
