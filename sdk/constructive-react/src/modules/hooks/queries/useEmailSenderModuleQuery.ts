/**
 * Single item query hook for EmailSenderModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailSenderModuleKeys } from '../query-keys';
import type {
  EmailSenderModuleSelect,
  EmailSenderModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailSenderModuleSelect,
  EmailSenderModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const emailSenderModuleQueryKey = emailSenderModuleKeys.detail;
/**
 * Query hook for fetching a single EmailSenderModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailSenderModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useEmailSenderModuleQuery<
  S extends EmailSenderModuleSelect,
  TData = {
    emailSenderModule: InferSelectResult<EmailSenderModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, EmailSenderModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        emailSenderModule: InferSelectResult<EmailSenderModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEmailSenderModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<EmailSenderModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<EmailSenderModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: emailSenderModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .emailSenderModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single EmailSenderModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchEmailSenderModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchEmailSenderModuleQuery<S extends EmailSenderModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, EmailSenderModuleSelect>;
}): Promise<{
  emailSenderModule: InferSelectResult<EmailSenderModuleWithRelations, S> | null;
}>;
export async function fetchEmailSenderModuleQuery(params: {
  id: string;
  selection: SelectionConfig<EmailSenderModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<EmailSenderModuleSelect>(params.selection);
  return getClient()
    .emailSenderModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single EmailSenderModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchEmailSenderModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchEmailSenderModuleQuery<S extends EmailSenderModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, EmailSenderModuleSelect>;
  }
): Promise<void>;
export async function prefetchEmailSenderModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<EmailSenderModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<EmailSenderModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: emailSenderModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .emailSenderModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
