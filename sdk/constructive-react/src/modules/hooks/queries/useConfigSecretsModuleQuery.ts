/**
 * Entity-aware PGP-encrypted key-value config/secrets module. Supports app-level (admin-only)
     and org-scoped (per-org secrets with manage_secrets permission) via the scope column.
     User-scoped bcrypt credentials are handled by user_credentials_module.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { configSecretsModuleKeys } from '../query-keys';
import type {
  ConfigSecretsModuleSelect,
  ConfigSecretsModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ConfigSecretsModuleSelect,
  ConfigSecretsModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const configSecretsModuleQueryKey = configSecretsModuleKeys.detail;
/**
 * Entity-aware PGP-encrypted key-value config/secrets module. Supports app-level (admin-only)
     and org-scoped (per-org secrets with manage_secrets permission) via the scope column.
     User-scoped bcrypt credentials are handled by user_credentials_module.
 * 
 * @example
 * ```tsx
 * const { data, isLoading } = useConfigSecretsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useConfigSecretsModuleQuery<
  S extends ConfigSecretsModuleSelect,
  TData = {
    configSecretsModule: InferSelectResult<ConfigSecretsModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ConfigSecretsModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        configSecretsModule: InferSelectResult<ConfigSecretsModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useConfigSecretsModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<ConfigSecretsModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ConfigSecretsModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: configSecretsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .configSecretsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Entity-aware PGP-encrypted key-value config/secrets module. Supports app-level (admin-only)
     and org-scoped (per-org secrets with manage_secrets permission) via the scope column.
     User-scoped bcrypt credentials are handled by user_credentials_module.
 * 
 * @example
 * ```ts
 * const data = await fetchConfigSecretsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchConfigSecretsModuleQuery<S extends ConfigSecretsModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ConfigSecretsModuleSelect>;
}): Promise<{
  configSecretsModule: InferSelectResult<ConfigSecretsModuleWithRelations, S> | null;
}>;
export async function fetchConfigSecretsModuleQuery(params: {
  id: string;
  selection: SelectionConfig<ConfigSecretsModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ConfigSecretsModuleSelect>(params.selection);
  return getClient()
    .configSecretsModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Entity-aware PGP-encrypted key-value config/secrets module. Supports app-level (admin-only)
     and org-scoped (per-org secrets with manage_secrets permission) via the scope column.
     User-scoped bcrypt credentials are handled by user_credentials_module.
 * 
 * @example
 * ```ts
 * await prefetchConfigSecretsModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchConfigSecretsModuleQuery<S extends ConfigSecretsModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ConfigSecretsModuleSelect>;
  }
): Promise<void>;
export async function prefetchConfigSecretsModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ConfigSecretsModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ConfigSecretsModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: configSecretsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .configSecretsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
