/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { k8sAdmissionModuleKeys } from '../query-keys';
import type {
  K8sAdmissionModuleSelect,
  K8sAdmissionModuleWithRelations,
  K8sAdmissionModuleFilter,
  K8sAdmissionModuleOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  K8sAdmissionModuleSelect,
  K8sAdmissionModuleWithRelations,
  K8sAdmissionModuleFilter,
  K8sAdmissionModuleOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const k8sAdmissionModulesQueryKey = k8sAdmissionModuleKeys.list;
/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useK8sAdmissionModulesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useK8sAdmissionModulesQuery<
  S extends K8sAdmissionModuleSelect,
  TData = {
    k8sAdmissionModules: ConnectionResult<InferSelectResult<K8sAdmissionModuleWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, K8sAdmissionModuleFilter, K8sAdmissionModuleOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, K8sAdmissionModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        k8sAdmissionModules: ConnectionResult<
          InferSelectResult<K8sAdmissionModuleWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useK8sAdmissionModulesQuery(
  params: {
    selection: ListSelectionConfig<
      K8sAdmissionModuleSelect,
      K8sAdmissionModuleFilter,
      K8sAdmissionModuleOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    K8sAdmissionModuleSelect,
    K8sAdmissionModuleFilter,
    K8sAdmissionModuleOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: k8sAdmissionModuleKeys.list(args),
    queryFn: () => getClient().k8sAdmissionModule.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 *
 * @example
 * ```ts
 * const data = await fetchK8sAdmissionModulesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchK8sAdmissionModulesQuery<S extends K8sAdmissionModuleSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, K8sAdmissionModuleFilter, K8sAdmissionModuleOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, K8sAdmissionModuleSelect>;
}): Promise<{
  k8sAdmissionModules: ConnectionResult<InferSelectResult<K8sAdmissionModuleWithRelations, S>>;
}>;
export async function fetchK8sAdmissionModulesQuery(params: {
  selection: ListSelectionConfig<
    K8sAdmissionModuleSelect,
    K8sAdmissionModuleFilter,
    K8sAdmissionModuleOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    K8sAdmissionModuleSelect,
    K8sAdmissionModuleFilter,
    K8sAdmissionModuleOrderBy
  >(params.selection);
  return getClient().k8sAdmissionModule.findMany(args).unwrap();
}
/**
 * Provisions the platform-managed Kubernetes admission catalogs: a kinds table and a spec-rules table, Merkle-versioned through the referenced merkle_store_module, whose rows the generated admission gate on resources/resource_definitions reads. Writes are platform-admin and human-only; every scope reads the one catalog.
 *
 * @example
 * ```ts
 * await prefetchK8sAdmissionModulesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchK8sAdmissionModulesQuery<S extends K8sAdmissionModuleSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, K8sAdmissionModuleFilter, K8sAdmissionModuleOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, K8sAdmissionModuleSelect>;
  }
): Promise<void>;
export async function prefetchK8sAdmissionModulesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      K8sAdmissionModuleSelect,
      K8sAdmissionModuleFilter,
      K8sAdmissionModuleOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    K8sAdmissionModuleSelect,
    K8sAdmissionModuleFilter,
    K8sAdmissionModuleOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: k8sAdmissionModuleKeys.list(args),
    queryFn: () => getClient().k8sAdmissionModule.findMany(args).unwrap(),
  });
}
