/**
 * Single item query hook for DataCapabilitiesField
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dataCapabilitiesFieldKeys } from '../query-keys';
import type {
  DataCapabilitiesFieldSelect,
  DataCapabilitiesFieldWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DataCapabilitiesFieldSelect,
  DataCapabilitiesFieldWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const dataCapabilitiesFieldQueryKey = dataCapabilitiesFieldKeys.detail;
/**
 * Query hook for fetching a single DataCapabilitiesField
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDataCapabilitiesFieldQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDataCapabilitiesFieldQuery<
  S extends DataCapabilitiesFieldSelect,
  TData = {
    dataCapabilitiesField: InferSelectResult<DataCapabilitiesFieldWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DataCapabilitiesFieldSelect>;
  } & Omit<
    UseQueryOptions<
      {
        dataCapabilitiesField: InferSelectResult<DataCapabilitiesFieldWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDataCapabilitiesFieldQuery(
  params: {
    id: string;
    selection: SelectionConfig<DataCapabilitiesFieldSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DataCapabilitiesFieldSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: dataCapabilitiesFieldKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .dataCapabilitiesField.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single DataCapabilitiesField without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDataCapabilitiesFieldQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDataCapabilitiesFieldQuery<
  S extends DataCapabilitiesFieldSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DataCapabilitiesFieldSelect>;
}): Promise<{
  dataCapabilitiesField: InferSelectResult<DataCapabilitiesFieldWithRelations, S> | null;
}>;
export async function fetchDataCapabilitiesFieldQuery(params: {
  id: string;
  selection: SelectionConfig<DataCapabilitiesFieldSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DataCapabilitiesFieldSelect>(params.selection);
  return getClient()
    .dataCapabilitiesField.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single DataCapabilitiesField for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDataCapabilitiesFieldQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDataCapabilitiesFieldQuery<S extends DataCapabilitiesFieldSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DataCapabilitiesFieldSelect>;
  }
): Promise<void>;
export async function prefetchDataCapabilitiesFieldQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DataCapabilitiesFieldSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DataCapabilitiesFieldSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: dataCapabilitiesFieldKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .dataCapabilitiesField.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
