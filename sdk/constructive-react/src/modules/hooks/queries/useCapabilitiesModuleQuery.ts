/**
 * Single item query hook for CapabilitiesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { capabilitiesModuleKeys } from '../query-keys';
import type {
  CapabilitiesModuleSelect,
  CapabilitiesModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  CapabilitiesModuleSelect,
  CapabilitiesModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const capabilitiesModuleQueryKey = capabilitiesModuleKeys.detail;
/**
 * Query hook for fetching a single CapabilitiesModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCapabilitiesModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useCapabilitiesModuleQuery<
  S extends CapabilitiesModuleSelect,
  TData = {
    capabilitiesModule: InferSelectResult<CapabilitiesModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CapabilitiesModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        capabilitiesModule: InferSelectResult<CapabilitiesModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCapabilitiesModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<CapabilitiesModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<CapabilitiesModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: capabilitiesModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .capabilitiesModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single CapabilitiesModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCapabilitiesModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchCapabilitiesModuleQuery<S extends CapabilitiesModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, CapabilitiesModuleSelect>;
}): Promise<{
  capabilitiesModule: InferSelectResult<CapabilitiesModuleWithRelations, S> | null;
}>;
export async function fetchCapabilitiesModuleQuery(params: {
  id: string;
  selection: SelectionConfig<CapabilitiesModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<CapabilitiesModuleSelect>(params.selection);
  return getClient()
    .capabilitiesModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single CapabilitiesModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCapabilitiesModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchCapabilitiesModuleQuery<S extends CapabilitiesModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CapabilitiesModuleSelect>;
  }
): Promise<void>;
export async function prefetchCapabilitiesModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<CapabilitiesModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<CapabilitiesModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: capabilitiesModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .capabilitiesModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
