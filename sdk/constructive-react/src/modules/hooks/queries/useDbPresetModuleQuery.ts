/**
 * Single item query hook for DbPresetModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPresetModuleKeys } from '../query-keys';
import type { DbPresetModuleSelect, DbPresetModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DbPresetModuleSelect, DbPresetModuleWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const dbPresetModuleQueryKey = dbPresetModuleKeys.detail;
/**
 * Query hook for fetching a single DbPresetModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDbPresetModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDbPresetModuleQuery<
  S extends DbPresetModuleSelect,
  TData = {
    dbPresetModule: InferSelectResult<DbPresetModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DbPresetModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        dbPresetModule: InferSelectResult<DbPresetModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDbPresetModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<DbPresetModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DbPresetModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: dbPresetModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .dbPresetModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single DbPresetModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDbPresetModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDbPresetModuleQuery<S extends DbPresetModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DbPresetModuleSelect>;
}): Promise<{
  dbPresetModule: InferSelectResult<DbPresetModuleWithRelations, S> | null;
}>;
export async function fetchDbPresetModuleQuery(params: {
  id: string;
  selection: SelectionConfig<DbPresetModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DbPresetModuleSelect>(params.selection);
  return getClient()
    .dbPresetModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single DbPresetModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDbPresetModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDbPresetModuleQuery<S extends DbPresetModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DbPresetModuleSelect>;
  }
): Promise<void>;
export async function prefetchDbPresetModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DbPresetModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DbPresetModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: dbPresetModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .dbPresetModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
