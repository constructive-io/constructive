/**
 * Single item query hook for RepositoryModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryModuleKeys } from '../query-keys';
import type { RepositoryModuleSelect, RepositoryModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RepositoryModuleSelect, RepositoryModuleWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const repositoryModuleQueryKey = repositoryModuleKeys.detail;
/**
 * Query hook for fetching a single RepositoryModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRepositoryModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRepositoryModuleQuery<
  S extends RepositoryModuleSelect,
  TData = {
    repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RepositoryModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRepositoryModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<RepositoryModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RepositoryModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: repositoryModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .repositoryModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single RepositoryModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchRepositoryModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRepositoryModuleQuery<S extends RepositoryModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RepositoryModuleSelect>;
}): Promise<{
  repositoryModule: InferSelectResult<RepositoryModuleWithRelations, S> | null;
}>;
export async function fetchRepositoryModuleQuery(params: {
  id: string;
  selection: SelectionConfig<RepositoryModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RepositoryModuleSelect>(params.selection);
  return getClient()
    .repositoryModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single RepositoryModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRepositoryModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRepositoryModuleQuery<S extends RepositoryModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RepositoryModuleSelect>;
  }
): Promise<void>;
export async function prefetchRepositoryModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RepositoryModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RepositoryModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: repositoryModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .repositoryModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
