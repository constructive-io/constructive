/**
 * Single item query hook for RefusalLogModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { refusalLogModuleKeys } from '../query-keys';
import type { RefusalLogModuleSelect, RefusalLogModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RefusalLogModuleSelect, RefusalLogModuleWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const refusalLogModuleQueryKey = refusalLogModuleKeys.detail;
/**
 * Query hook for fetching a single RefusalLogModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRefusalLogModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRefusalLogModuleQuery<
  S extends RefusalLogModuleSelect,
  TData = {
    refusalLogModule: InferSelectResult<RefusalLogModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RefusalLogModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        refusalLogModule: InferSelectResult<RefusalLogModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRefusalLogModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<RefusalLogModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RefusalLogModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: refusalLogModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .refusalLogModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single RefusalLogModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchRefusalLogModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRefusalLogModuleQuery<S extends RefusalLogModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RefusalLogModuleSelect>;
}): Promise<{
  refusalLogModule: InferSelectResult<RefusalLogModuleWithRelations, S> | null;
}>;
export async function fetchRefusalLogModuleQuery(params: {
  id: string;
  selection: SelectionConfig<RefusalLogModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RefusalLogModuleSelect>(params.selection);
  return getClient()
    .refusalLogModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single RefusalLogModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRefusalLogModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRefusalLogModuleQuery<S extends RefusalLogModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RefusalLogModuleSelect>;
  }
): Promise<void>;
export async function prefetchRefusalLogModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RefusalLogModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RefusalLogModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: refusalLogModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .refusalLogModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
