/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { oauthRequestsModuleKeys } from '../query-keys';
import type {
  OauthRequestsModuleSelect,
  OauthRequestsModuleWithRelations,
  OauthRequestsModuleFilter,
  OauthRequestsModuleOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  OauthRequestsModuleSelect,
  OauthRequestsModuleWithRelations,
  OauthRequestsModuleFilter,
  OauthRequestsModuleOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const oauthRequestsModulesQueryKey = oauthRequestsModuleKeys.list;
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```tsx
 * const { data, isLoading } = useOauthRequestsModulesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useOauthRequestsModulesQuery<
  S extends OauthRequestsModuleSelect,
  TData = {
    oauthRequestsModules: ConnectionResult<InferSelectResult<OauthRequestsModuleWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, OauthRequestsModuleFilter, OauthRequestsModuleOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        oauthRequestsModules: ConnectionResult<
          InferSelectResult<OauthRequestsModuleWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOauthRequestsModulesQuery(
  params: {
    selection: ListSelectionConfig<
      OauthRequestsModuleSelect,
      OauthRequestsModuleFilter,
      OauthRequestsModuleOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    OauthRequestsModuleSelect,
    OauthRequestsModuleFilter,
    OauthRequestsModuleOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: oauthRequestsModuleKeys.list(args),
    queryFn: () => getClient().oauthRequestsModule.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```ts
 * const data = await fetchOauthRequestsModulesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchOauthRequestsModulesQuery<S extends OauthRequestsModuleSelect>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, OauthRequestsModuleFilter, OauthRequestsModuleOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
}): Promise<{
  oauthRequestsModules: ConnectionResult<InferSelectResult<OauthRequestsModuleWithRelations, S>>;
}>;
export async function fetchOauthRequestsModulesQuery(params: {
  selection: ListSelectionConfig<
    OauthRequestsModuleSelect,
    OauthRequestsModuleFilter,
    OauthRequestsModuleOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    OauthRequestsModuleSelect,
    OauthRequestsModuleFilter,
    OauthRequestsModuleOrderBy
  >(params.selection);
  return getClient().oauthRequestsModule.findMany(args).unwrap();
}
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```ts
 * await prefetchOauthRequestsModulesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchOauthRequestsModulesQuery<S extends OauthRequestsModuleSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, OauthRequestsModuleFilter, OauthRequestsModuleOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
  }
): Promise<void>;
export async function prefetchOauthRequestsModulesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      OauthRequestsModuleSelect,
      OauthRequestsModuleFilter,
      OauthRequestsModuleOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    OauthRequestsModuleSelect,
    OauthRequestsModuleFilter,
    OauthRequestsModuleOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: oauthRequestsModuleKeys.list(args),
    queryFn: () => getClient().oauthRequestsModule.findMany(args).unwrap(),
  });
}
