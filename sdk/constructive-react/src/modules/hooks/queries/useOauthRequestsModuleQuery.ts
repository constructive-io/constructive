/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { oauthRequestsModuleKeys } from '../query-keys';
import type {
  OauthRequestsModuleSelect,
  OauthRequestsModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OauthRequestsModuleSelect,
  OauthRequestsModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const oauthRequestsModuleQueryKey = oauthRequestsModuleKeys.detail;
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```tsx
 * const { data, isLoading } = useOauthRequestsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOauthRequestsModuleQuery<
  S extends OauthRequestsModuleSelect,
  TData = {
    oauthRequestsModule: InferSelectResult<OauthRequestsModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        oauthRequestsModule: InferSelectResult<OauthRequestsModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOauthRequestsModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<OauthRequestsModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OauthRequestsModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: oauthRequestsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .oauthRequestsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```ts
 * const data = await fetchOauthRequestsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOauthRequestsModuleQuery<S extends OauthRequestsModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
}): Promise<{
  oauthRequestsModule: InferSelectResult<OauthRequestsModuleWithRelations, S> | null;
}>;
export async function fetchOauthRequestsModuleQuery(params: {
  id: string;
  selection: SelectionConfig<OauthRequestsModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OauthRequestsModuleSelect>(params.selection);
  return getClient()
    .oauthRequestsModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Config row for the oauth_requests_module, which provisions the in-flight half of an SSO
     sign-in: the OAuth authorization requests table (state + PKCE code_verifier) and the
     pending identity links table (a verified identity parked under a single-use ticket),
     both private, plus the five SECURITY DEFINER procedures that are their only surface.
     Sibling of identity_providers_module (durable provider configuration) rather than part
     of it: this is ephemeral, purged flow state with its own retention.
 * 
 * @example
 * ```ts
 * await prefetchOauthRequestsModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOauthRequestsModuleQuery<S extends OauthRequestsModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OauthRequestsModuleSelect>;
  }
): Promise<void>;
export async function prefetchOauthRequestsModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OauthRequestsModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OauthRequestsModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: oauthRequestsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .oauthRequestsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
