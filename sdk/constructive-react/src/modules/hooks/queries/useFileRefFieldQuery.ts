/**
 * Single item query hook for FileRefField
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { fileRefFieldKeys } from '../query-keys';
import type { FileRefFieldSelect, FileRefFieldWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FileRefFieldSelect, FileRefFieldWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const fileRefFieldQueryKey = fileRefFieldKeys.detail;
/**
 * Query hook for fetching a single FileRefField
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFileRefFieldQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFileRefFieldQuery<
  S extends FileRefFieldSelect,
  TData = {
    fileRefField: InferSelectResult<FileRefFieldWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FileRefFieldSelect>;
  } & Omit<
    UseQueryOptions<
      {
        fileRefField: InferSelectResult<FileRefFieldWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFileRefFieldQuery(
  params: {
    id: string;
    selection: SelectionConfig<FileRefFieldSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FileRefFieldSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: fileRefFieldKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .fileRefField.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single FileRefField without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchFileRefFieldQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFileRefFieldQuery<S extends FileRefFieldSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FileRefFieldSelect>;
}): Promise<{
  fileRefField: InferSelectResult<FileRefFieldWithRelations, S> | null;
}>;
export async function fetchFileRefFieldQuery(params: {
  id: string;
  selection: SelectionConfig<FileRefFieldSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<FileRefFieldSelect>(params.selection);
  return getClient()
    .fileRefField.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single FileRefField for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchFileRefFieldQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFileRefFieldQuery<S extends FileRefFieldSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FileRefFieldSelect>;
  }
): Promise<void>;
export async function prefetchFileRefFieldQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FileRefFieldSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FileRefFieldSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: fileRefFieldKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .fileRefField.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
