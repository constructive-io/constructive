/**
 * Single item query hook for SiteSurfaceModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteSurfaceModuleKeys } from '../query-keys';
import type {
  SiteSurfaceModuleSelect,
  SiteSurfaceModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteSurfaceModuleSelect,
  SiteSurfaceModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteSurfaceModuleQueryKey = siteSurfaceModuleKeys.detail;
/**
 * Query hook for fetching a single SiteSurfaceModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteSurfaceModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSiteSurfaceModuleQuery<
  S extends SiteSurfaceModuleSelect,
  TData = {
    siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteSurfaceModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteSurfaceModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<SiteSurfaceModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SiteSurfaceModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteSurfaceModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteSurfaceModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single SiteSurfaceModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSiteSurfaceModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSiteSurfaceModuleQuery<S extends SiteSurfaceModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SiteSurfaceModuleSelect>;
}): Promise<{
  siteSurfaceModule: InferSelectResult<SiteSurfaceModuleWithRelations, S> | null;
}>;
export async function fetchSiteSurfaceModuleQuery(params: {
  id: string;
  selection: SelectionConfig<SiteSurfaceModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SiteSurfaceModuleSelect>(params.selection);
  return getClient()
    .siteSurfaceModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single SiteSurfaceModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSiteSurfaceModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSiteSurfaceModuleQuery<S extends SiteSurfaceModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteSurfaceModuleSelect>;
  }
): Promise<void>;
export async function prefetchSiteSurfaceModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SiteSurfaceModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SiteSurfaceModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteSurfaceModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteSurfaceModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
