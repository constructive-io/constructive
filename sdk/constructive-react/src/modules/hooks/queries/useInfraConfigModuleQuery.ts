/**
 * Namespace-backed plaintext key-value config module. Requires namespace_module and emits namespace:sync_config job triggers for K8s ConfigMap synchronization.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraConfigModuleKeys } from '../query-keys';
import type {
  InfraConfigModuleSelect,
  InfraConfigModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  InfraConfigModuleSelect,
  InfraConfigModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const infraConfigModuleQueryKey = infraConfigModuleKeys.detail;
/**
 * Namespace-backed plaintext key-value config module. Requires namespace_module and emits namespace:sync_config job triggers for K8s ConfigMap synchronization.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInfraConfigModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useInfraConfigModuleQuery<
  S extends InfraConfigModuleSelect,
  TData = {
    infraConfigModule: InferSelectResult<InfraConfigModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InfraConfigModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        infraConfigModule: InferSelectResult<InfraConfigModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useInfraConfigModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<InfraConfigModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<InfraConfigModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: infraConfigModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .infraConfigModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Namespace-backed plaintext key-value config module. Requires namespace_module and emits namespace:sync_config job triggers for K8s ConfigMap synchronization.
 *
 * @example
 * ```ts
 * const data = await fetchInfraConfigModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchInfraConfigModuleQuery<S extends InfraConfigModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, InfraConfigModuleSelect>;
}): Promise<{
  infraConfigModule: InferSelectResult<InfraConfigModuleWithRelations, S> | null;
}>;
export async function fetchInfraConfigModuleQuery(params: {
  id: string;
  selection: SelectionConfig<InfraConfigModuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<InfraConfigModuleSelect>(params.selection);
  return getClient()
    .infraConfigModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Namespace-backed plaintext key-value config module. Requires namespace_module and emits namespace:sync_config job triggers for K8s ConfigMap synchronization.
 *
 * @example
 * ```ts
 * await prefetchInfraConfigModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchInfraConfigModuleQuery<S extends InfraConfigModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InfraConfigModuleSelect>;
  }
): Promise<void>;
export async function prefetchInfraConfigModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<InfraConfigModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<InfraConfigModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: infraConfigModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .infraConfigModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
