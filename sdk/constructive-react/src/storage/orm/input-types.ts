/**
 * GraphQL types for ORM client
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
// ============ Scalar Filter Types ============
export interface StringFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
  lessThan?: string;
  lessThanOrEqualTo?: string;
  greaterThan?: string;
  greaterThanOrEqualTo?: string;
  includes?: string;
  notIncludes?: string;
  includesInsensitive?: string;
  notIncludesInsensitive?: string;
  startsWith?: string;
  notStartsWith?: string;
  startsWithInsensitive?: string;
  notStartsWithInsensitive?: string;
  endsWith?: string;
  notEndsWith?: string;
  endsWithInsensitive?: string;
  notEndsWithInsensitive?: string;
  like?: string;
  notLike?: string;
  likeInsensitive?: string;
  notLikeInsensitive?: string;
}
export interface IntFilter {
  isNull?: boolean;
  equalTo?: number;
  notEqualTo?: number;
  distinctFrom?: number;
  notDistinctFrom?: number;
  in?: number[];
  notIn?: number[];
  lessThan?: number;
  lessThanOrEqualTo?: number;
  greaterThan?: number;
  greaterThanOrEqualTo?: number;
}
export interface FloatFilter {
  isNull?: boolean;
  equalTo?: number;
  notEqualTo?: number;
  distinctFrom?: number;
  notDistinctFrom?: number;
  in?: number[];
  notIn?: number[];
  lessThan?: number;
  lessThanOrEqualTo?: number;
  greaterThan?: number;
  greaterThanOrEqualTo?: number;
}
export interface BooleanFilter {
  isNull?: boolean;
  equalTo?: boolean;
  notEqualTo?: boolean;
}
export interface UUIDFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
}
export interface DatetimeFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
  lessThan?: string;
  lessThanOrEqualTo?: string;
  greaterThan?: string;
  greaterThanOrEqualTo?: string;
}
export interface DateFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
  lessThan?: string;
  lessThanOrEqualTo?: string;
  greaterThan?: string;
  greaterThanOrEqualTo?: string;
}
export interface JSONFilter {
  isNull?: boolean;
  equalTo?: Record<string, unknown>;
  notEqualTo?: Record<string, unknown>;
  distinctFrom?: Record<string, unknown>;
  notDistinctFrom?: Record<string, unknown>;
  contains?: Record<string, unknown>;
  containedBy?: Record<string, unknown>;
  containsKey?: string;
  containsAllKeys?: string[];
  containsAnyKeys?: string[];
}
export interface BigIntFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
  lessThan?: string;
  lessThanOrEqualTo?: string;
  greaterThan?: string;
  greaterThanOrEqualTo?: string;
}
export interface BigFloatFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
  lessThan?: string;
  lessThanOrEqualTo?: string;
  greaterThan?: string;
  greaterThanOrEqualTo?: string;
}
export interface BitStringFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
}
export interface InternetAddressFilter {
  isNull?: boolean;
  equalTo?: string;
  notEqualTo?: string;
  distinctFrom?: string;
  notDistinctFrom?: string;
  in?: string[];
  notIn?: string[];
  lessThan?: string;
  lessThanOrEqualTo?: string;
  greaterThan?: string;
  greaterThanOrEqualTo?: string;
  contains?: string;
  containsOrEqualTo?: string;
  containedBy?: string;
  containedByOrEqualTo?: string;
  containsOrContainedBy?: string;
}
export interface FullTextFilter {
  matches?: string;
}
export interface VectorFilter {
  isNull?: boolean;
  equalTo?: number[];
  notEqualTo?: number[];
  distinctFrom?: number[];
  notDistinctFrom?: number[];
}
export interface StringListFilter {
  isNull?: boolean;
  equalTo?: string[];
  notEqualTo?: string[];
  distinctFrom?: string[];
  notDistinctFrom?: string[];
  lessThan?: string[];
  lessThanOrEqualTo?: string[];
  greaterThan?: string[];
  greaterThanOrEqualTo?: string[];
  contains?: string[];
  containedBy?: string[];
  overlaps?: string[];
  anyEqualTo?: string;
  anyNotEqualTo?: string;
  anyLessThan?: string;
  anyLessThanOrEqualTo?: string;
  anyGreaterThan?: string;
  anyGreaterThanOrEqualTo?: string;
}
export interface IntListFilter {
  isNull?: boolean;
  equalTo?: number[];
  notEqualTo?: number[];
  distinctFrom?: number[];
  notDistinctFrom?: number[];
  lessThan?: number[];
  lessThanOrEqualTo?: number[];
  greaterThan?: number[];
  greaterThanOrEqualTo?: number[];
  contains?: number[];
  containedBy?: number[];
  overlaps?: number[];
  anyEqualTo?: number;
  anyNotEqualTo?: number;
  anyLessThan?: number;
  anyLessThanOrEqualTo?: number;
  anyGreaterThan?: number;
  anyGreaterThanOrEqualTo?: number;
}
export interface UUIDListFilter {
  isNull?: boolean;
  equalTo?: string[];
  notEqualTo?: string[];
  distinctFrom?: string[];
  notDistinctFrom?: string[];
  lessThan?: string[];
  lessThanOrEqualTo?: string[];
  greaterThan?: string[];
  greaterThanOrEqualTo?: string[];
  contains?: string[];
  containedBy?: string[];
  overlaps?: string[];
  anyEqualTo?: string;
  anyNotEqualTo?: string;
  anyLessThan?: string;
  anyLessThanOrEqualTo?: string;
  anyGreaterThan?: string;
  anyGreaterThanOrEqualTo?: string;
}
// ============ Enum Types ============
export type BucketType = 'PRIVATE' | 'PUBLIC' | 'TEMP';
export type FileStatus = 'EXPIRED' | 'PROCESSED' | 'REJECTED' | 'REQUESTED' | 'UPLOADED';
// ============ Custom Scalar Types ============
export type ConstructiveInternalTypeUpload = unknown;
/** Logical storage containers that group files with shared access policies and CDN behavior */
// ============ Entity Types ============
export interface Bucket {
  /** User who created this bucket. Forced to current_user_id() on INSERT. */
  actorId?: string | null;
  /** When true, clients can provide custom S3 keys (e.g. reports/2024/Q1.pdf). When false (default), S3 key = content hash (automatic dedup). contentHash always required for integrity. */
  allowCustomKeys?: boolean | null;
  /** Whitelist of allowed MIME types for files in this bucket (NULL = all allowed, enforcement deferred) */
  allowedMimeTypes?: string[] | null;
  /** Per-bucket CORS allowed origins override (NULL = inherit from storage_module/plugin defaults). Use ARRAY['*'] for open/CDN mode. */
  allowedOrigins?: string[] | null;
  createdAt?: string | null;
  /** Database that owns this resource (database-scoped isolation) */
  databaseId?: string | null;
  /** Human-readable description of the bucket purpose */
  description?: string | null;
  /** For a temp bucket, the permanent bucket its uploads are promoted into. NULL on non-temp buckets; required on temp buckets. */
  destinationBucketId?: string | null;
  id: string;
  /** Whether bucket contents are publicly readable. Set to true when type is public. */
  isPublic?: boolean | null;
  /** Unique bucket identifier used in S3 key paths (e.g. avatars, documents, temp) */
  key?: string | null;
  /** Maximum file size in bytes allowed in this bucket (NULL = no limit, enforcement deferred) */
  maxFileSize?: string | null;
  /** Physical S3/MinIO bucket name recorded at provision time (NULL until the physical bucket exists) */
  physicalName?: string | null;
  /** For a temp bucket, how long an unpromoted staged file survives before it is eligible for expiry. NULL on non-temp buckets; required on temp buckets. */
  stagingTtl?: string | null;
  /** Selector labels for capability resolution and grouping (e.g. uploads, variants, exports). A function's required bucket key is matched against these plus type. */
  tags?: string[] | null;
  /** Bucket CDN access type: public (CDN-served), private (presigned GET), temp (staging uploads) */
  type?: BucketType | null;
  updatedAt?: string | null;
}
/** Individual file records within buckets, with immutable identity fields and mutable metadata */
export interface File {
  /** User who uploaded this file. Forced to current_user_id() on INSERT. Used for UPDATE/DELETE authorization. */
  actorId?: string | null;
  /** Bucket this file belongs to. Determines owner_id and is_public via inheritance trigger. */
  bucketId?: string | null;
  /** SHA-256 content hash for integrity verification and dedup. In default mode, equals the S3 key. In custom key mode, stored separately. */
  contentHash?: string | null;
  createdAt?: string | null;
  /** Database that owns this resource (database-scoped isolation) */
  databaseId?: string | null;
  /** Human-readable description or alt text for the file (mutable) */
  description?: string | null;
  /** URL to download this file. For public files, returns the public URL. For private files, returns a time-limited presigned URL. */
  downloadUrl?: string | null;
  /** When the staged-expiry sweep enqueued deletion of this file's object. NULL until it expires; set once so expiry is enqueued exactly once. */
  expiryEnqueuedAt?: string | null;
  filePath?: string | null;
  /** Original filename provided by the uploader. Used for display and Content-Disposition header on download. */
  filename?: string | null;
  id: string;
  /** Whether this file is publicly readable. Inherited from bucket on INSERT. */
  isPublic?: boolean | null;
  /** S3 object key for this file, unique within its bucket */
  key?: string | null;
  /** MIME type of the file (e.g. image/png, application/pdf). Immutable after INSERT. */
  mimeType?: string | null;
  /** When this file was promoted out of its staging bucket into its destination bucket. NULL for a file that was never staged, or is still staged. */
  promotedAt?: string | null;
  /** File size in bytes. Immutable after INSERT. */
  size?: string | null;
  /** File lifecycle status: requested (presigned URL generated, not yet in S3), uploaded (file in S3 and content validated, ready for use), processed (image resized, text extracted, embedding computed), rejected (declared type disagreed with the bytes), expired (bytes never arrived before the confirm window closed). */
  status?: FileStatus | null;
  /** User-defined tags for categorization and search (mutable) */
  tags?: string[] | null;
  updatedAt?: string | null;
  /** Processed file reference (upload domain). Populated by processing jobs after file is uploaded. Copy this value to other tables to reference the file. */
  upload?: ConstructiveInternalTypeUpload | null;
}
/** Logical storage containers that group files with shared access policies and CDN behavior */
export interface PlatformBucket {
  /** User who created this bucket. Forced to current_user_id() on INSERT. */
  actorId?: string | null;
  /** When true, clients can provide custom S3 keys (e.g. reports/2024/Q1.pdf). When false (default), S3 key = content hash (automatic dedup). contentHash always required for integrity. */
  allowCustomKeys?: boolean | null;
  /** Whitelist of allowed MIME types for files in this bucket (NULL = all allowed, enforcement deferred) */
  allowedMimeTypes?: string[] | null;
  /** Per-bucket CORS allowed origins override (NULL = inherit from storage_module/plugin defaults). Use ARRAY['*'] for open/CDN mode. */
  allowedOrigins?: string[] | null;
  createdAt?: string | null;
  /** Human-readable description of the bucket purpose */
  description?: string | null;
  /** For a temp bucket, the permanent bucket its uploads are promoted into. NULL on non-temp buckets; required on temp buckets. */
  destinationBucketId?: string | null;
  id: string;
  /** Whether bucket contents are publicly readable. Set to true when type is public. */
  isPublic?: boolean | null;
  /** Unique bucket identifier used in S3 key paths (e.g. avatars, documents, temp) */
  key?: string | null;
  /** Maximum file size in bytes allowed in this bucket (NULL = no limit, enforcement deferred) */
  maxFileSize?: string | null;
  /** Physical S3/MinIO bucket name recorded at provision time (NULL until the physical bucket exists) */
  physicalName?: string | null;
  /** For a temp bucket, how long an unpromoted staged file survives before it is eligible for expiry. NULL on non-temp buckets; required on temp buckets. */
  stagingTtl?: string | null;
  /** Selector labels for capability resolution and grouping (e.g. uploads, variants, exports). A function's required bucket key is matched against these plus type. */
  tags?: string[] | null;
  /** Bucket CDN access type: public (CDN-served), private (presigned GET), temp (staging uploads) */
  type?: BucketType | null;
  updatedAt?: string | null;
}
/** Individual file records within buckets, with immutable identity fields and mutable metadata */
export interface PlatformFile {
  /** User who uploaded this file. Forced to current_user_id() on INSERT. Used for UPDATE/DELETE authorization. */
  actorId?: string | null;
  /** Bucket this file belongs to. Determines owner_id and is_public via inheritance trigger. */
  bucketId?: string | null;
  /** SHA-256 content hash for integrity verification and dedup. In default mode, equals the S3 key. In custom key mode, stored separately. */
  contentHash?: string | null;
  createdAt?: string | null;
  /** Human-readable description or alt text for the file (mutable) */
  description?: string | null;
  /** URL to download this file. For public files, returns the public URL. For private files, returns a time-limited presigned URL. */
  downloadUrl?: string | null;
  /** When the staged-expiry sweep enqueued deletion of this file's object. NULL until it expires; set once so expiry is enqueued exactly once. */
  expiryEnqueuedAt?: string | null;
  filePath?: string | null;
  /** Original filename provided by the uploader. Used for display and Content-Disposition header on download. */
  filename?: string | null;
  id: string;
  /** Whether this file is publicly readable. Inherited from bucket on INSERT. */
  isPublic?: boolean | null;
  /** S3 object key for this file, unique within its bucket */
  key?: string | null;
  /** MIME type of the file (e.g. image/png, application/pdf). Immutable after INSERT. */
  mimeType?: string | null;
  /** When this file was promoted out of its staging bucket into its destination bucket. NULL for a file that was never staged, or is still staged. */
  promotedAt?: string | null;
  /** File size in bytes. Immutable after INSERT. */
  size?: string | null;
  /** File lifecycle status: requested (presigned URL generated, not yet in S3), uploaded (file in S3 and content validated, ready for use), processed (image resized, text extracted, embedding computed), rejected (declared type disagreed with the bytes), expired (bytes never arrived before the confirm window closed). */
  status?: FileStatus | null;
  /** User-defined tags for categorization and search (mutable) */
  tags?: string[] | null;
  updatedAt?: string | null;
  /** Processed file reference (upload domain). Populated by processing jobs after file is uploaded. Copy this value to other tables to reference the file. */
  upload?: ConstructiveInternalTypeUpload | null;
}
// ============ Relation Helper Types ============
export interface ConnectionResult<T> {
  nodes: T[];
  totalCount: number;
  pageInfo: PageInfo;
}
export interface PageInfo {
  hasNextPage: boolean;
  hasPreviousPage: boolean;
  startCursor?: string | null;
  endCursor?: string | null;
}
// ============ Entity Relation Types ============
export interface BucketRelations {
  destinationBucket?: Bucket | null;
  bucketsByDestinationBucketId?: ConnectionResult<Bucket>;
  files?: ConnectionResult<File>;
}
export interface FileRelations {
  bucket?: Bucket | null;
}
export interface PlatformBucketRelations {
  destinationBucket?: PlatformBucket | null;
  platformBucketsByDestinationBucketId?: ConnectionResult<PlatformBucket>;
  platformFilesByBucketId?: ConnectionResult<PlatformFile>;
}
export interface PlatformFileRelations {
  bucket?: PlatformBucket | null;
}
// ============ Entity Types With Relations ============
export type BucketWithRelations = Bucket & BucketRelations;
export type FileWithRelations = File & FileRelations;
export type PlatformBucketWithRelations = PlatformBucket & PlatformBucketRelations;
export type PlatformFileWithRelations = PlatformFile & PlatformFileRelations;
// ============ Entity Select Types ============
export type BucketSelect = {
  actorId?: boolean;
  allowCustomKeys?: boolean;
  allowedMimeTypes?: boolean;
  allowedOrigins?: boolean;
  createdAt?: boolean;
  databaseId?: boolean;
  description?: boolean;
  destinationBucketId?: boolean;
  id?: boolean;
  isPublic?: boolean;
  key?: boolean;
  maxFileSize?: boolean;
  physicalName?: boolean;
  stagingTtl?: boolean;
  tags?: boolean;
  type?: boolean;
  updatedAt?: boolean;
  destinationBucket?: {
    select: BucketSelect;
  };
  bucketsByDestinationBucketId?: {
    select: BucketSelect;
    first?: number;
    filter?: BucketFilter;
    orderBy?: BucketOrderBy[];
  };
  files?: {
    select: FileSelect;
    first?: number;
    filter?: FileFilter;
    orderBy?: FileOrderBy[];
  };
};
export type FileSelect = {
  actorId?: boolean;
  bucketId?: boolean;
  contentHash?: boolean;
  createdAt?: boolean;
  databaseId?: boolean;
  description?: boolean;
  downloadUrl?: boolean;
  expiryEnqueuedAt?: boolean;
  filePath?: boolean;
  filename?: boolean;
  id?: boolean;
  isPublic?: boolean;
  key?: boolean;
  mimeType?: boolean;
  promotedAt?: boolean;
  size?: boolean;
  status?: boolean;
  tags?: boolean;
  updatedAt?: boolean;
  upload?: boolean;
  bucket?: {
    select: BucketSelect;
  };
};
export type PlatformBucketSelect = {
  actorId?: boolean;
  allowCustomKeys?: boolean;
  allowedMimeTypes?: boolean;
  allowedOrigins?: boolean;
  createdAt?: boolean;
  description?: boolean;
  destinationBucketId?: boolean;
  id?: boolean;
  isPublic?: boolean;
  key?: boolean;
  maxFileSize?: boolean;
  physicalName?: boolean;
  stagingTtl?: boolean;
  tags?: boolean;
  type?: boolean;
  updatedAt?: boolean;
  destinationBucket?: {
    select: PlatformBucketSelect;
  };
  platformBucketsByDestinationBucketId?: {
    select: PlatformBucketSelect;
    first?: number;
    filter?: PlatformBucketFilter;
    orderBy?: PlatformBucketOrderBy[];
  };
  platformFilesByBucketId?: {
    select: PlatformFileSelect;
    first?: number;
    filter?: PlatformFileFilter;
    orderBy?: PlatformFileOrderBy[];
  };
};
export type PlatformFileSelect = {
  actorId?: boolean;
  bucketId?: boolean;
  contentHash?: boolean;
  createdAt?: boolean;
  description?: boolean;
  downloadUrl?: boolean;
  expiryEnqueuedAt?: boolean;
  filePath?: boolean;
  filename?: boolean;
  id?: boolean;
  isPublic?: boolean;
  key?: boolean;
  mimeType?: boolean;
  promotedAt?: boolean;
  size?: boolean;
  status?: boolean;
  tags?: boolean;
  updatedAt?: boolean;
  upload?: boolean;
  bucket?: {
    select: PlatformBucketSelect;
  };
};
// ============ Table Filter Types ============
export interface BucketFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Filter by the object’s `allowCustomKeys` field. */
  allowCustomKeys?: BooleanFilter;
  /** Filter by the object’s `allowedMimeTypes` field. */
  allowedMimeTypes?: StringListFilter;
  /** Filter by the object’s `allowedOrigins` field. */
  allowedOrigins?: StringListFilter;
  /** Checks for all expressions in this list. */
  and?: BucketFilter[];
  /** Filter by the object’s `bucketsByDestinationBucketId` relation. */
  bucketsByDestinationBucketId?: BucketToManyBucketFilter;
  /** `bucketsByDestinationBucketId` exist. */
  bucketsByDestinationBucketIdExist?: boolean;
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `databaseId` field. */
  databaseId?: UUIDFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `destinationBucket` relation. */
  destinationBucket?: BucketFilter;
  /** A related `destinationBucket` exists. */
  destinationBucketExists?: boolean;
  /** Filter by the object’s `destinationBucketId` field. */
  destinationBucketId?: UUIDFilter;
  /** Filter by the object’s `files` relation. */
  files?: BucketToManyFileFilter;
  /** `files` exist. */
  filesExist?: boolean;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `maxFileSize` field. */
  maxFileSize?: BigIntFilter;
  /** Negates the expression. */
  not?: BucketFilter;
  /** Checks for any expressions in this list. */
  or?: BucketFilter[];
  /** Filter by the object’s `physicalName` field. */
  physicalName?: StringFilter;
  /** Filter by the object’s `stagingTtl` field. */
  stagingTtl?: IntervalFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `type` field. */
  type?: BucketTypeFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
}
export interface FileFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Checks for all expressions in this list. */
  and?: FileFilter[];
  /** Filter by the object’s `bucket` relation. */
  bucket?: BucketFilter;
  /** Filter by the object’s `bucketId` field. */
  bucketId?: UUIDFilter;
  /** Filter by the object’s `contentHash` field. */
  contentHash?: StringFilter;
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `databaseId` field. */
  databaseId?: UUIDFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `expiryEnqueuedAt` field. */
  expiryEnqueuedAt?: DatetimeFilter;
  /** Filter by the object’s `filename` field. */
  filename?: StringFilter;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `mimeType` field. */
  mimeType?: StringFilter;
  /** Negates the expression. */
  not?: FileFilter;
  /** Checks for any expressions in this list. */
  or?: FileFilter[];
  /** Filter by the object’s `promotedAt` field. */
  promotedAt?: DatetimeFilter;
  /** Filter by the object’s `size` field. */
  size?: BigIntFilter;
  /** Filter by the object’s `status` field. */
  status?: FileStatusFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
  /** Filter by the object’s `upload` field. */
  upload?: ConstructiveInternalTypeUploadFilter;
}
export interface PlatformBucketFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Filter by the object’s `allowCustomKeys` field. */
  allowCustomKeys?: BooleanFilter;
  /** Filter by the object’s `allowedMimeTypes` field. */
  allowedMimeTypes?: StringListFilter;
  /** Filter by the object’s `allowedOrigins` field. */
  allowedOrigins?: StringListFilter;
  /** Checks for all expressions in this list. */
  and?: PlatformBucketFilter[];
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `destinationBucket` relation. */
  destinationBucket?: PlatformBucketFilter;
  /** A related `destinationBucket` exists. */
  destinationBucketExists?: boolean;
  /** Filter by the object’s `destinationBucketId` field. */
  destinationBucketId?: UUIDFilter;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `maxFileSize` field. */
  maxFileSize?: BigIntFilter;
  /** Negates the expression. */
  not?: PlatformBucketFilter;
  /** Checks for any expressions in this list. */
  or?: PlatformBucketFilter[];
  /** Filter by the object’s `physicalName` field. */
  physicalName?: StringFilter;
  /** Filter by the object’s `platformBucketsByDestinationBucketId` relation. */
  platformBucketsByDestinationBucketId?: PlatformBucketToManyPlatformBucketFilter;
  /** `platformBucketsByDestinationBucketId` exist. */
  platformBucketsByDestinationBucketIdExist?: boolean;
  /** Filter by the object’s `platformFilesByBucketId` relation. */
  platformFilesByBucketId?: PlatformBucketToManyPlatformFileFilter;
  /** `platformFilesByBucketId` exist. */
  platformFilesByBucketIdExist?: boolean;
  /** Filter by the object’s `stagingTtl` field. */
  stagingTtl?: IntervalFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `type` field. */
  type?: BucketTypeFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
}
export interface PlatformFileFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Checks for all expressions in this list. */
  and?: PlatformFileFilter[];
  /** Filter by the object’s `bucket` relation. */
  bucket?: PlatformBucketFilter;
  /** Filter by the object’s `bucketId` field. */
  bucketId?: UUIDFilter;
  /** Filter by the object’s `contentHash` field. */
  contentHash?: StringFilter;
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `expiryEnqueuedAt` field. */
  expiryEnqueuedAt?: DatetimeFilter;
  /** Filter by the object’s `filename` field. */
  filename?: StringFilter;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `mimeType` field. */
  mimeType?: StringFilter;
  /** Negates the expression. */
  not?: PlatformFileFilter;
  /** Checks for any expressions in this list. */
  or?: PlatformFileFilter[];
  /** Filter by the object’s `promotedAt` field. */
  promotedAt?: DatetimeFilter;
  /** Filter by the object’s `size` field. */
  size?: BigIntFilter;
  /** Filter by the object’s `status` field. */
  status?: FileStatusFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
  /** Filter by the object’s `upload` field. */
  upload?: ConstructiveInternalTypeUploadFilter;
}
// ============ OrderBy Types ============
export type BucketOrderBy =
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'ALLOWED_MIME_TYPES_ASC'
  | 'ALLOWED_MIME_TYPES_DESC'
  | 'ALLOWED_ORIGINS_ASC'
  | 'ALLOWED_ORIGINS_DESC'
  | 'ALLOW_CUSTOM_KEYS_ASC'
  | 'ALLOW_CUSTOM_KEYS_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'DESTINATION_BUCKET_ID_ASC'
  | 'DESTINATION_BUCKET_ID_DESC'
  | 'ID_ASC'
  | 'ID_DESC'
  | 'IS_PUBLIC_ASC'
  | 'IS_PUBLIC_DESC'
  | 'KEY_ASC'
  | 'KEY_DESC'
  | 'MAX_FILE_SIZE_ASC'
  | 'MAX_FILE_SIZE_DESC'
  | 'NATURAL'
  | 'PHYSICAL_NAME_ASC'
  | 'PHYSICAL_NAME_DESC'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC'
  | 'STAGING_TTL_ASC'
  | 'STAGING_TTL_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'TYPE_ASC'
  | 'TYPE_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC';
export type FileOrderBy =
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'BUCKET_ID_ASC'
  | 'BUCKET_ID_DESC'
  | 'CONTENT_HASH_ASC'
  | 'CONTENT_HASH_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'DATABASE_ID_ASC'
  | 'DATABASE_ID_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'EXPIRY_ENQUEUED_AT_ASC'
  | 'EXPIRY_ENQUEUED_AT_DESC'
  | 'FILENAME_ASC'
  | 'FILENAME_DESC'
  | 'ID_ASC'
  | 'ID_DESC'
  | 'IS_PUBLIC_ASC'
  | 'IS_PUBLIC_DESC'
  | 'KEY_ASC'
  | 'KEY_DESC'
  | 'MIME_TYPE_ASC'
  | 'MIME_TYPE_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC'
  | 'PROMOTED_AT_ASC'
  | 'PROMOTED_AT_DESC'
  | 'SIZE_ASC'
  | 'SIZE_DESC'
  | 'STATUS_ASC'
  | 'STATUS_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'UPLOAD_ASC'
  | 'UPLOAD_DESC';
export type PlatformBucketOrderBy =
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'ALLOWED_MIME_TYPES_ASC'
  | 'ALLOWED_MIME_TYPES_DESC'
  | 'ALLOWED_ORIGINS_ASC'
  | 'ALLOWED_ORIGINS_DESC'
  | 'ALLOW_CUSTOM_KEYS_ASC'
  | 'ALLOW_CUSTOM_KEYS_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'DESTINATION_BUCKET_ID_ASC'
  | 'DESTINATION_BUCKET_ID_DESC'
  | 'ID_ASC'
  | 'ID_DESC'
  | 'IS_PUBLIC_ASC'
  | 'IS_PUBLIC_DESC'
  | 'KEY_ASC'
  | 'KEY_DESC'
  | 'MAX_FILE_SIZE_ASC'
  | 'MAX_FILE_SIZE_DESC'
  | 'NATURAL'
  | 'PHYSICAL_NAME_ASC'
  | 'PHYSICAL_NAME_DESC'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC'
  | 'STAGING_TTL_ASC'
  | 'STAGING_TTL_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'TYPE_ASC'
  | 'TYPE_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC';
export type PlatformFileOrderBy =
  | 'ACTOR_ID_ASC'
  | 'ACTOR_ID_DESC'
  | 'BUCKET_ID_ASC'
  | 'BUCKET_ID_DESC'
  | 'CONTENT_HASH_ASC'
  | 'CONTENT_HASH_DESC'
  | 'CREATED_AT_ASC'
  | 'CREATED_AT_DESC'
  | 'DESCRIPTION_ASC'
  | 'DESCRIPTION_DESC'
  | 'EXPIRY_ENQUEUED_AT_ASC'
  | 'EXPIRY_ENQUEUED_AT_DESC'
  | 'FILENAME_ASC'
  | 'FILENAME_DESC'
  | 'ID_ASC'
  | 'ID_DESC'
  | 'IS_PUBLIC_ASC'
  | 'IS_PUBLIC_DESC'
  | 'KEY_ASC'
  | 'KEY_DESC'
  | 'MIME_TYPE_ASC'
  | 'MIME_TYPE_DESC'
  | 'NATURAL'
  | 'PRIMARY_KEY_ASC'
  | 'PRIMARY_KEY_DESC'
  | 'PROMOTED_AT_ASC'
  | 'PROMOTED_AT_DESC'
  | 'SIZE_ASC'
  | 'SIZE_DESC'
  | 'STATUS_ASC'
  | 'STATUS_DESC'
  | 'TAGS_ASC'
  | 'TAGS_DESC'
  | 'UPDATED_AT_ASC'
  | 'UPDATED_AT_DESC'
  | 'UPLOAD_ASC'
  | 'UPLOAD_DESC';
// ============ CRUD Input Types ============
export interface CreateBucketInput {
  clientMutationId?: string;
  bucket: {
    actorId: string;
    allowCustomKeys?: boolean;
    allowedMimeTypes?: string[];
    allowedOrigins?: string[];
    databaseId: string;
    description?: string;
    destinationBucketId?: string;
    isPublic?: boolean;
    key: string;
    maxFileSize?: string;
    physicalName?: string;
    stagingTtl?: IntervalInput;
    tags?: string[];
    type?: BucketType;
  };
}
export interface BucketPatch {
  actorId?: string | null;
  allowCustomKeys?: boolean | null;
  allowedMimeTypes?: string[] | null;
  allowedOrigins?: string[] | null;
  databaseId?: string | null;
  description?: string | null;
  destinationBucketId?: string | null;
  isPublic?: boolean | null;
  key?: string | null;
  maxFileSize?: string | null;
  physicalName?: string | null;
  stagingTtl?: IntervalInput | null;
  tags?: string[] | null;
  type?: BucketType | null;
}
export interface UpdateBucketInput {
  clientMutationId?: string;
  id: string;
  bucketPatch: BucketPatch;
}
export interface DeleteBucketInput {
  clientMutationId?: string;
  id: string;
}
export interface CreateFileInput {
  clientMutationId?: string;
  file: {
    actorId: string;
    bucketId: string;
    contentHash?: string;
    databaseId: string;
    description?: string;
    expiryEnqueuedAt?: string;
    filename?: string;
    isPublic?: boolean;
    key: string;
    mimeType: string;
    promotedAt?: string;
    size: string;
    status?: FileStatus;
    tags?: string[];
    upload?: ConstructiveInternalTypeUpload;
  };
}
export interface FilePatch {
  actorId?: string | null;
  bucketId?: string | null;
  contentHash?: string | null;
  databaseId?: string | null;
  description?: string | null;
  expiryEnqueuedAt?: string | null;
  filename?: string | null;
  isPublic?: boolean | null;
  key?: string | null;
  mimeType?: string | null;
  promotedAt?: string | null;
  size?: string | null;
  status?: FileStatus | null;
  tags?: string[] | null;
  upload?: ConstructiveInternalTypeUpload | null;
  uploadUpload?: File | null;
}
export interface UpdateFileInput {
  clientMutationId?: string;
  id: string;
  filePatch: FilePatch;
}
export interface DeleteFileInput {
  clientMutationId?: string;
  id: string;
}
export interface CreatePlatformBucketInput {
  clientMutationId?: string;
  platformBucket: {
    actorId: string;
    allowCustomKeys?: boolean;
    allowedMimeTypes?: string[];
    allowedOrigins?: string[];
    description?: string;
    destinationBucketId?: string;
    isPublic?: boolean;
    key: string;
    maxFileSize?: string;
    physicalName?: string;
    stagingTtl?: IntervalInput;
    tags?: string[];
    type?: BucketType;
  };
}
export interface PlatformBucketPatch {
  actorId?: string | null;
  allowCustomKeys?: boolean | null;
  allowedMimeTypes?: string[] | null;
  allowedOrigins?: string[] | null;
  description?: string | null;
  destinationBucketId?: string | null;
  isPublic?: boolean | null;
  key?: string | null;
  maxFileSize?: string | null;
  physicalName?: string | null;
  stagingTtl?: IntervalInput | null;
  tags?: string[] | null;
  type?: BucketType | null;
}
export interface UpdatePlatformBucketInput {
  clientMutationId?: string;
  id: string;
  platformBucketPatch: PlatformBucketPatch;
}
export interface DeletePlatformBucketInput {
  clientMutationId?: string;
  id: string;
}
export interface CreatePlatformFileInput {
  clientMutationId?: string;
  platformFile: {
    actorId: string;
    bucketId: string;
    contentHash?: string;
    description?: string;
    expiryEnqueuedAt?: string;
    filename?: string;
    isPublic?: boolean;
    key: string;
    mimeType: string;
    promotedAt?: string;
    size: string;
    status?: FileStatus;
    tags?: string[];
    upload?: ConstructiveInternalTypeUpload;
  };
}
export interface PlatformFilePatch {
  actorId?: string | null;
  bucketId?: string | null;
  contentHash?: string | null;
  description?: string | null;
  expiryEnqueuedAt?: string | null;
  filename?: string | null;
  isPublic?: boolean | null;
  key?: string | null;
  mimeType?: string | null;
  promotedAt?: string | null;
  size?: string | null;
  status?: FileStatus | null;
  tags?: string[] | null;
  upload?: ConstructiveInternalTypeUpload | null;
  uploadUpload?: File | null;
}
export interface UpdatePlatformFileInput {
  clientMutationId?: string;
  id: string;
  platformFilePatch: PlatformFilePatch;
}
export interface DeletePlatformFileInput {
  clientMutationId?: string;
  id: string;
}
// ============ Connection Fields Map ============
export const connectionFieldsMap = {
  Bucket: {
    bucketsByDestinationBucketId: 'Bucket',
    files: 'File',
  },
  PlatformBucket: {
    platformBucketsByDestinationBucketId: 'PlatformBucket',
    platformFilesByBucketId: 'PlatformFile',
  },
} as Record<string, Record<string, string>>;
// ============ Custom Input Types (from schema) ============
export interface FilesRenameInput {
  clientMutationId?: string;
  fileId?: string;
  newFilename?: string;
}
export interface PlatformFilesRenameInput {
  clientMutationId?: string;
  fileId?: string;
  newFilename?: string;
}
export interface ProvisionBucketInput {
  /** The logical bucket key (e.g., "public", "private") */
  bucketKey: string;
  /**
   * Owner entity ID for entity-scoped bucket provisioning.
   * Omit for app-level (database-wide) storage.
   */
  ownerId?: string;
}
export interface UploadPlatformFileInput {
  /** Bucket key (e.g., "public", "private"). Omit to use the database's default bucket for the requested access. */
  bucketKey?: string;
  /** SHA-256 content hash (hex-encoded, 64 chars) */
  contentHash: string;
  /** MIME type of the file */
  contentType: string;
  /** Original filename (optional) */
  filename?: string;
  /** Which default bucket to resolve when bucketKey is omitted: the public one (true) or the private one (default false). Ignored when bucketKey is given. */
  isPublic?: boolean;
  /** Custom S3 key (only when bucket has allow_custom_keys=true) */
  key?: string;
  /** File size in bytes */
  size: number;
}
export interface UploadPlatformFileBulkInput {
  /** Bucket key (e.g., "public", "private"). Omit to use the database's default bucket for the requested access. */
  bucketKey?: string;
  /** Array of files to upload */
  files: UploadPlatformFileBulkFileInput[];
  /** Which default bucket to resolve when bucketKey is omitted. Ignored when bucketKey is given. */
  isPublic?: boolean;
}
/** A filter to be used against many `Bucket` object types. All fields are combined with a logical ‘and.’ */
export interface BucketToManyBucketFilter {
  /** Filters to entities where every related entity matches. */
  every?: BucketFilter;
  /** Filters to entities where no related entity matches. */
  none?: BucketFilter;
  /** Filters to entities where at least one related entity matches. */
  some?: BucketFilter;
}
/** A filter to be used against many `File` object types. All fields are combined with a logical ‘and.’ */
export interface BucketToManyFileFilter {
  /** Filters to entities where every related entity matches. */
  every?: FileFilter;
  /** Filters to entities where no related entity matches. */
  none?: FileFilter;
  /** Filters to entities where at least one related entity matches. */
  some?: FileFilter;
}
/** A filter to be used against Interval fields. All fields are combined with a logical ‘and.’ */
export interface IntervalFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: IntervalInput;
  /** Equal to the specified value. */
  equalTo?: IntervalInput;
  /** Greater than the specified value. */
  greaterThan?: IntervalInput;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: IntervalInput;
  /** Included in the specified list. */
  in?: IntervalInput[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: IntervalInput;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: IntervalInput;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: IntervalInput;
  /** Not equal to the specified value. */
  notEqualTo?: IntervalInput;
  /** Not included in the specified list. */
  notIn?: IntervalInput[];
}
/** A filter to be used against BucketType fields. All fields are combined with a logical ‘and.’ */
export interface BucketTypeFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: BucketType;
  /** Equal to the specified value. */
  equalTo?: BucketType;
  /** Greater than the specified value. */
  greaterThan?: BucketType;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: BucketType;
  /** Included in the specified list. */
  in?: BucketType[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: BucketType;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: BucketType;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: BucketType;
  /** Not equal to the specified value. */
  notEqualTo?: BucketType;
  /** Not included in the specified list. */
  notIn?: BucketType[];
}
/** A filter to be used against FileStatus fields. All fields are combined with a logical ‘and.’ */
export interface FileStatusFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: FileStatus;
  /** Equal to the specified value. */
  equalTo?: FileStatus;
  /** Greater than the specified value. */
  greaterThan?: FileStatus;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: FileStatus;
  /** Included in the specified list. */
  in?: FileStatus[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: FileStatus;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: FileStatus;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: FileStatus;
  /** Not equal to the specified value. */
  notEqualTo?: FileStatus;
  /** Not included in the specified list. */
  notIn?: FileStatus[];
}
/** A filter to be used against ConstructiveInternalTypeUpload fields. All fields are combined with a logical ‘and.’ */
export interface ConstructiveInternalTypeUploadFilter {
  /** Contained by the specified JSON. */
  containedBy?: ConstructiveInternalTypeUpload;
  /** Contains the specified JSON. */
  contains?: ConstructiveInternalTypeUpload;
  /** Contains all of the specified keys. */
  containsAllKeys?: string[];
  /** Contains any of the specified keys. */
  containsAnyKeys?: string[];
  /** Contains the specified key. */
  containsKey?: string;
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: ConstructiveInternalTypeUpload;
  /** Equal to the specified value. */
  equalTo?: ConstructiveInternalTypeUpload;
  /** Greater than the specified value. */
  greaterThan?: ConstructiveInternalTypeUpload;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: ConstructiveInternalTypeUpload;
  /** Included in the specified list. */
  in?: ConstructiveInternalTypeUpload[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: ConstructiveInternalTypeUpload;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: ConstructiveInternalTypeUpload;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: ConstructiveInternalTypeUpload;
  /** Not equal to the specified value. */
  notEqualTo?: ConstructiveInternalTypeUpload;
  /** Not included in the specified list. */
  notIn?: ConstructiveInternalTypeUpload[];
}
/** A filter to be used against many `PlatformBucket` object types. All fields are combined with a logical ‘and.’ */
export interface PlatformBucketToManyPlatformBucketFilter {
  /** Filters to entities where every related entity matches. */
  every?: PlatformBucketFilter;
  /** Filters to entities where no related entity matches. */
  none?: PlatformBucketFilter;
  /** Filters to entities where at least one related entity matches. */
  some?: PlatformBucketFilter;
}
/** A filter to be used against many `PlatformFile` object types. All fields are combined with a logical ‘and.’ */
export interface PlatformBucketToManyPlatformFileFilter {
  /** Filters to entities where every related entity matches. */
  every?: PlatformFileFilter;
  /** Filters to entities where no related entity matches. */
  none?: PlatformFileFilter;
  /** Filters to entities where at least one related entity matches. */
  some?: PlatformFileFilter;
}
/** An input for mutations affecting `Bucket` */
export interface BucketInput {
  /** User who created this bucket. Forced to current_user_id() on INSERT. */
  actorId: string;
  /** When true, clients can provide custom S3 keys (e.g. reports/2024/Q1.pdf). When false (default), S3 key = content hash (automatic dedup). contentHash always required for integrity. */
  allowCustomKeys?: boolean;
  /** Whitelist of allowed MIME types for files in this bucket (NULL = all allowed, enforcement deferred) */
  allowedMimeTypes?: string[];
  /** Per-bucket CORS allowed origins override (NULL = inherit from storage_module/plugin defaults). Use ARRAY['*'] for open/CDN mode. */
  allowedOrigins?: string[];
  createdAt?: string;
  /** Database that owns this resource (database-scoped isolation) */
  databaseId: string;
  /** Human-readable description of the bucket purpose */
  description?: string;
  /** For a temp bucket, the permanent bucket its uploads are promoted into. NULL on non-temp buckets; required on temp buckets. */
  destinationBucketId?: string;
  id?: string;
  /** Whether bucket contents are publicly readable. Set to true when type is public. */
  isPublic?: boolean;
  /** Unique bucket identifier used in S3 key paths (e.g. avatars, documents, temp) */
  key: string;
  /** Maximum file size in bytes allowed in this bucket (NULL = no limit, enforcement deferred) */
  maxFileSize?: string;
  /** Physical S3/MinIO bucket name recorded at provision time (NULL until the physical bucket exists) */
  physicalName?: string;
  /** For a temp bucket, how long an unpromoted staged file survives before it is eligible for expiry. NULL on non-temp buckets; required on temp buckets. */
  stagingTtl?: IntervalInput;
  /** Selector labels for capability resolution and grouping (e.g. uploads, variants, exports). A function's required bucket key is matched against these plus type. */
  tags?: string[];
  /** Bucket CDN access type: public (CDN-served), private (presigned GET), temp (staging uploads) */
  type?: BucketType;
  updatedAt?: string;
}
/** An interval of time that has passed where the smallest distinct unit is a second. */
export interface IntervalInput {
  /** A quantity of days. */
  days?: number;
  /** A quantity of hours. */
  hours?: number;
  /** A quantity of minutes. */
  minutes?: number;
  /** A quantity of months. */
  months?: number;
  /**
   * A quantity of seconds. This is the only non-integer field, as all the other
   * fields will dump their overflow into a smaller unit of time. Intervals don’t
   * have a smaller unit than seconds.
   */
  seconds?: number;
  /** A quantity of years. */
  years?: number;
}
/** An input for mutations affecting `File` */
export interface FileInput {
  /** User who uploaded this file. Forced to current_user_id() on INSERT. Used for UPDATE/DELETE authorization. */
  actorId: string;
  /** Bucket this file belongs to. Determines owner_id and is_public via inheritance trigger. */
  bucketId: string;
  /** SHA-256 content hash for integrity verification and dedup. In default mode, equals the S3 key. In custom key mode, stored separately. */
  contentHash?: string;
  createdAt?: string;
  /** Database that owns this resource (database-scoped isolation) */
  databaseId: string;
  /** Human-readable description or alt text for the file (mutable) */
  description?: string;
  /** When the staged-expiry sweep enqueued deletion of this file's object. NULL until it expires; set once so expiry is enqueued exactly once. */
  expiryEnqueuedAt?: string;
  /** Original filename provided by the uploader. Used for display and Content-Disposition header on download. */
  filename?: string;
  id?: string;
  /** Whether this file is publicly readable. Inherited from bucket on INSERT. */
  isPublic?: boolean;
  /** S3 object key for this file, unique within its bucket */
  key: string;
  /** MIME type of the file (e.g. image/png, application/pdf). Immutable after INSERT. */
  mimeType: string;
  /** When this file was promoted out of its staging bucket into its destination bucket. NULL for a file that was never staged, or is still staged. */
  promotedAt?: string;
  /** File size in bytes. Immutable after INSERT. */
  size: string;
  /** File lifecycle status: requested (presigned URL generated, not yet in S3), uploaded (file in S3 and content validated, ready for use), processed (image resized, text extracted, embedding computed), rejected (declared type disagreed with the bytes), expired (bytes never arrived before the confirm window closed). */
  status?: FileStatus;
  /** User-defined tags for categorization and search (mutable) */
  tags?: string[];
  updatedAt?: string;
  /** Processed file reference (upload domain). Populated by processing jobs after file is uploaded. Copy this value to other tables to reference the file. */
  upload?: ConstructiveInternalTypeUpload;
}
/** An input for mutations affecting `PlatformBucket` */
export interface PlatformBucketInput {
  /** User who created this bucket. Forced to current_user_id() on INSERT. */
  actorId: string;
  /** When true, clients can provide custom S3 keys (e.g. reports/2024/Q1.pdf). When false (default), S3 key = content hash (automatic dedup). contentHash always required for integrity. */
  allowCustomKeys?: boolean;
  /** Whitelist of allowed MIME types for files in this bucket (NULL = all allowed, enforcement deferred) */
  allowedMimeTypes?: string[];
  /** Per-bucket CORS allowed origins override (NULL = inherit from storage_module/plugin defaults). Use ARRAY['*'] for open/CDN mode. */
  allowedOrigins?: string[];
  createdAt?: string;
  /** Human-readable description of the bucket purpose */
  description?: string;
  /** For a temp bucket, the permanent bucket its uploads are promoted into. NULL on non-temp buckets; required on temp buckets. */
  destinationBucketId?: string;
  id?: string;
  /** Whether bucket contents are publicly readable. Set to true when type is public. */
  isPublic?: boolean;
  /** Unique bucket identifier used in S3 key paths (e.g. avatars, documents, temp) */
  key: string;
  /** Maximum file size in bytes allowed in this bucket (NULL = no limit, enforcement deferred) */
  maxFileSize?: string;
  /** Physical S3/MinIO bucket name recorded at provision time (NULL until the physical bucket exists) */
  physicalName?: string;
  /** For a temp bucket, how long an unpromoted staged file survives before it is eligible for expiry. NULL on non-temp buckets; required on temp buckets. */
  stagingTtl?: IntervalInput;
  /** Selector labels for capability resolution and grouping (e.g. uploads, variants, exports). A function's required bucket key is matched against these plus type. */
  tags?: string[];
  /** Bucket CDN access type: public (CDN-served), private (presigned GET), temp (staging uploads) */
  type?: BucketType;
  updatedAt?: string;
}
/** An input for mutations affecting `PlatformFile` */
export interface PlatformFileInput {
  /** User who uploaded this file. Forced to current_user_id() on INSERT. Used for UPDATE/DELETE authorization. */
  actorId: string;
  /** Bucket this file belongs to. Determines owner_id and is_public via inheritance trigger. */
  bucketId: string;
  /** SHA-256 content hash for integrity verification and dedup. In default mode, equals the S3 key. In custom key mode, stored separately. */
  contentHash?: string;
  createdAt?: string;
  /** Human-readable description or alt text for the file (mutable) */
  description?: string;
  /** When the staged-expiry sweep enqueued deletion of this file's object. NULL until it expires; set once so expiry is enqueued exactly once. */
  expiryEnqueuedAt?: string;
  /** Original filename provided by the uploader. Used for display and Content-Disposition header on download. */
  filename?: string;
  id?: string;
  /** Whether this file is publicly readable. Inherited from bucket on INSERT. */
  isPublic?: boolean;
  /** S3 object key for this file, unique within its bucket */
  key: string;
  /** MIME type of the file (e.g. image/png, application/pdf). Immutable after INSERT. */
  mimeType: string;
  /** When this file was promoted out of its staging bucket into its destination bucket. NULL for a file that was never staged, or is still staged. */
  promotedAt?: string;
  /** File size in bytes. Immutable after INSERT. */
  size: string;
  /** File lifecycle status: requested (presigned URL generated, not yet in S3), uploaded (file in S3 and content validated, ready for use), processed (image resized, text extracted, embedding computed), rejected (declared type disagreed with the bytes), expired (bytes never arrived before the confirm window closed). */
  status?: FileStatus;
  /** User-defined tags for categorization and search (mutable) */
  tags?: string[];
  updatedAt?: string;
  /** Processed file reference (upload domain). Populated by processing jobs after file is uploaded. Copy this value to other tables to reference the file. */
  upload?: ConstructiveInternalTypeUpload;
}
export interface UploadPlatformFileBulkFileInput {
  /** SHA-256 content hash (hex-encoded, 64 chars) */
  contentHash: string;
  /** MIME type of the file */
  contentType: string;
  /** Original filename (optional) */
  filename?: string;
  /** Custom S3 key (only when bucket has allow_custom_keys=true) */
  key?: string;
  /** File size in bytes */
  size: number;
}
/** A filter to be used against `Bucket` object types. All fields are combined with a logical ‘and.’ */
export interface BucketFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Filter by the object’s `allowCustomKeys` field. */
  allowCustomKeys?: BooleanFilter;
  /** Filter by the object’s `allowedMimeTypes` field. */
  allowedMimeTypes?: StringListFilter;
  /** Filter by the object’s `allowedOrigins` field. */
  allowedOrigins?: StringListFilter;
  /** Checks for all expressions in this list. */
  and?: BucketFilter[];
  /** Filter by the object’s `bucketsByDestinationBucketId` relation. */
  bucketsByDestinationBucketId?: BucketToManyBucketFilter;
  /** `bucketsByDestinationBucketId` exist. */
  bucketsByDestinationBucketIdExist?: boolean;
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `databaseId` field. */
  databaseId?: UUIDFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `destinationBucket` relation. */
  destinationBucket?: BucketFilter;
  /** A related `destinationBucket` exists. */
  destinationBucketExists?: boolean;
  /** Filter by the object’s `destinationBucketId` field. */
  destinationBucketId?: UUIDFilter;
  /** Filter by the object’s `files` relation. */
  files?: BucketToManyFileFilter;
  /** `files` exist. */
  filesExist?: boolean;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `maxFileSize` field. */
  maxFileSize?: BigIntFilter;
  /** Negates the expression. */
  not?: BucketFilter;
  /** Checks for any expressions in this list. */
  or?: BucketFilter[];
  /** Filter by the object’s `physicalName` field. */
  physicalName?: StringFilter;
  /** Filter by the object’s `stagingTtl` field. */
  stagingTtl?: IntervalFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `type` field. */
  type?: BucketTypeFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
}
/** A filter to be used against `File` object types. All fields are combined with a logical ‘and.’ */
export interface FileFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Checks for all expressions in this list. */
  and?: FileFilter[];
  /** Filter by the object’s `bucket` relation. */
  bucket?: BucketFilter;
  /** Filter by the object’s `bucketId` field. */
  bucketId?: UUIDFilter;
  /** Filter by the object’s `contentHash` field. */
  contentHash?: StringFilter;
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `databaseId` field. */
  databaseId?: UUIDFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `expiryEnqueuedAt` field. */
  expiryEnqueuedAt?: DatetimeFilter;
  /** Filter by the object’s `filename` field. */
  filename?: StringFilter;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `mimeType` field. */
  mimeType?: StringFilter;
  /** Negates the expression. */
  not?: FileFilter;
  /** Checks for any expressions in this list. */
  or?: FileFilter[];
  /** Filter by the object’s `promotedAt` field. */
  promotedAt?: DatetimeFilter;
  /** Filter by the object’s `size` field. */
  size?: BigIntFilter;
  /** Filter by the object’s `status` field. */
  status?: FileStatusFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
  /** Filter by the object’s `upload` field. */
  upload?: ConstructiveInternalTypeUploadFilter;
}
/** A filter to be used against `PlatformBucket` object types. All fields are combined with a logical ‘and.’ */
export interface PlatformBucketFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Filter by the object’s `allowCustomKeys` field. */
  allowCustomKeys?: BooleanFilter;
  /** Filter by the object’s `allowedMimeTypes` field. */
  allowedMimeTypes?: StringListFilter;
  /** Filter by the object’s `allowedOrigins` field. */
  allowedOrigins?: StringListFilter;
  /** Checks for all expressions in this list. */
  and?: PlatformBucketFilter[];
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `destinationBucket` relation. */
  destinationBucket?: PlatformBucketFilter;
  /** A related `destinationBucket` exists. */
  destinationBucketExists?: boolean;
  /** Filter by the object’s `destinationBucketId` field. */
  destinationBucketId?: UUIDFilter;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `maxFileSize` field. */
  maxFileSize?: BigIntFilter;
  /** Negates the expression. */
  not?: PlatformBucketFilter;
  /** Checks for any expressions in this list. */
  or?: PlatformBucketFilter[];
  /** Filter by the object’s `physicalName` field. */
  physicalName?: StringFilter;
  /** Filter by the object’s `platformBucketsByDestinationBucketId` relation. */
  platformBucketsByDestinationBucketId?: PlatformBucketToManyPlatformBucketFilter;
  /** `platformBucketsByDestinationBucketId` exist. */
  platformBucketsByDestinationBucketIdExist?: boolean;
  /** Filter by the object’s `platformFilesByBucketId` relation. */
  platformFilesByBucketId?: PlatformBucketToManyPlatformFileFilter;
  /** `platformFilesByBucketId` exist. */
  platformFilesByBucketIdExist?: boolean;
  /** Filter by the object’s `stagingTtl` field. */
  stagingTtl?: IntervalFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `type` field. */
  type?: BucketTypeFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
}
/** A filter to be used against `PlatformFile` object types. All fields are combined with a logical ‘and.’ */
export interface PlatformFileFilter {
  /** Filter by the object’s `actorId` field. */
  actorId?: UUIDFilter;
  /** Checks for all expressions in this list. */
  and?: PlatformFileFilter[];
  /** Filter by the object’s `bucket` relation. */
  bucket?: PlatformBucketFilter;
  /** Filter by the object’s `bucketId` field. */
  bucketId?: UUIDFilter;
  /** Filter by the object’s `contentHash` field. */
  contentHash?: StringFilter;
  /** Filter by the object’s `createdAt` field. */
  createdAt?: DatetimeFilter;
  /** Filter by the object’s `description` field. */
  description?: StringFilter;
  /** Filter by the object’s `expiryEnqueuedAt` field. */
  expiryEnqueuedAt?: DatetimeFilter;
  /** Filter by the object’s `filename` field. */
  filename?: StringFilter;
  /** Filter by the object’s `id` field. */
  id?: UUIDFilter;
  /** Filter by the object’s `isPublic` field. */
  isPublic?: BooleanFilter;
  /** Filter by the object’s `key` field. */
  key?: StringFilter;
  /** Filter by the object’s `mimeType` field. */
  mimeType?: StringFilter;
  /** Negates the expression. */
  not?: PlatformFileFilter;
  /** Checks for any expressions in this list. */
  or?: PlatformFileFilter[];
  /** Filter by the object’s `promotedAt` field. */
  promotedAt?: DatetimeFilter;
  /** Filter by the object’s `size` field. */
  size?: BigIntFilter;
  /** Filter by the object’s `status` field. */
  status?: FileStatusFilter;
  /** Filter by the object’s `tags` field. */
  tags?: StringListFilter;
  /** Filter by the object’s `updatedAt` field. */
  updatedAt?: DatetimeFilter;
  /** Filter by the object’s `upload` field. */
  upload?: ConstructiveInternalTypeUploadFilter;
}
/** A filter to be used against UUID fields. All fields are combined with a logical ‘and.’ */
export interface UUIDFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: string;
  /** Equal to the specified value. */
  equalTo?: string;
  /** Greater than the specified value. */
  greaterThan?: string;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: string;
  /** Included in the specified list. */
  in?: string[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: string;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: string;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: string;
  /** Not equal to the specified value. */
  notEqualTo?: string;
  /** Not included in the specified list. */
  notIn?: string[];
}
/** A filter to be used against Boolean fields. All fields are combined with a logical ‘and.’ */
export interface BooleanFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: boolean;
  /** Equal to the specified value. */
  equalTo?: boolean;
  /** Greater than the specified value. */
  greaterThan?: boolean;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: boolean;
  /** Included in the specified list. */
  in?: boolean[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: boolean;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: boolean;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: boolean;
  /** Not equal to the specified value. */
  notEqualTo?: boolean;
  /** Not included in the specified list. */
  notIn?: boolean[];
}
/** A filter to be used against String List fields. All fields are combined with a logical ‘and.’ */
export interface StringListFilter {
  /** Any array item is equal to the specified value. */
  anyEqualTo?: string;
  /** Any array item is greater than the specified value. */
  anyGreaterThan?: string;
  /** Any array item is greater than or equal to the specified value. */
  anyGreaterThanOrEqualTo?: string;
  /** Any array item is less than the specified value. */
  anyLessThan?: string;
  /** Any array item is less than or equal to the specified value. */
  anyLessThanOrEqualTo?: string;
  /** Any array item is not equal to the specified value. */
  anyNotEqualTo?: string;
  /** Contained by the specified list of values. */
  containedBy?: string[];
  /** Contains the specified list of values. */
  contains?: string[];
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: string[];
  /** Equal to the specified value. */
  equalTo?: string[];
  /** Greater than the specified value. */
  greaterThan?: string[];
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: string[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: string[];
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: string[];
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: string[];
  /** Not equal to the specified value. */
  notEqualTo?: string[];
  /** Overlaps the specified list of values. */
  overlaps?: string[];
}
/** A filter to be used against Datetime fields. All fields are combined with a logical ‘and.’ */
export interface DatetimeFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: string;
  /** Equal to the specified value. */
  equalTo?: string;
  /** Greater than the specified value. */
  greaterThan?: string;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: string;
  /** Included in the specified list. */
  in?: string[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: string;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: string;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: string;
  /** Not equal to the specified value. */
  notEqualTo?: string;
  /** Not included in the specified list. */
  notIn?: string[];
}
/** A filter to be used against String fields. All fields are combined with a logical ‘and.’ */
export interface StringFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: string;
  /** Not equal to the specified value, treating null like an ordinary value (case-insensitive). */
  distinctFromInsensitive?: string;
  /** Ends with the specified string (case-sensitive). */
  endsWith?: string;
  /** Ends with the specified string (case-insensitive). */
  endsWithInsensitive?: string;
  /** Equal to the specified value. */
  equalTo?: string;
  /** Equal to the specified value (case-insensitive). */
  equalToInsensitive?: string;
  /** Greater than the specified value. */
  greaterThan?: string;
  /** Greater than the specified value (case-insensitive). */
  greaterThanInsensitive?: string;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: string;
  /** Greater than or equal to the specified value (case-insensitive). */
  greaterThanOrEqualToInsensitive?: string;
  /** Included in the specified list. */
  in?: string[];
  /** Included in the specified list (case-insensitive). */
  inInsensitive?: string[];
  /** Contains the specified string (case-sensitive). */
  includes?: string;
  /** Contains the specified string (case-insensitive). */
  includesInsensitive?: string;
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: string;
  /** Less than the specified value (case-insensitive). */
  lessThanInsensitive?: string;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: string;
  /** Less than or equal to the specified value (case-insensitive). */
  lessThanOrEqualToInsensitive?: string;
  /** Matches the specified pattern (case-sensitive). An underscore (_) matches any single character; a percent sign (%) matches any sequence of zero or more characters. */
  like?: string;
  /** Matches the specified pattern (case-insensitive). An underscore (_) matches any single character; a percent sign (%) matches any sequence of zero or more characters. */
  likeInsensitive?: string;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: string;
  /** Equal to the specified value, treating null like an ordinary value (case-insensitive). */
  notDistinctFromInsensitive?: string;
  /** Does not end with the specified string (case-sensitive). */
  notEndsWith?: string;
  /** Does not end with the specified string (case-insensitive). */
  notEndsWithInsensitive?: string;
  /** Not equal to the specified value. */
  notEqualTo?: string;
  /** Not equal to the specified value (case-insensitive). */
  notEqualToInsensitive?: string;
  /** Not included in the specified list. */
  notIn?: string[];
  /** Not included in the specified list (case-insensitive). */
  notInInsensitive?: string[];
  /** Does not contain the specified string (case-sensitive). */
  notIncludes?: string;
  /** Does not contain the specified string (case-insensitive). */
  notIncludesInsensitive?: string;
  /** Does not match the specified pattern (case-sensitive). An underscore (_) matches any single character; a percent sign (%) matches any sequence of zero or more characters. */
  notLike?: string;
  /** Does not match the specified pattern (case-insensitive). An underscore (_) matches any single character; a percent sign (%) matches any sequence of zero or more characters. */
  notLikeInsensitive?: string;
  /** Does not start with the specified string (case-sensitive). */
  notStartsWith?: string;
  /** Does not start with the specified string (case-insensitive). */
  notStartsWithInsensitive?: string;
  /** Starts with the specified string (case-sensitive). */
  startsWith?: string;
  /** Starts with the specified string (case-insensitive). */
  startsWithInsensitive?: string;
}
/** A filter to be used against BigInt fields. All fields are combined with a logical ‘and.’ */
export interface BigIntFilter {
  /** Not equal to the specified value, treating null like an ordinary value. */
  distinctFrom?: string;
  /** Equal to the specified value. */
  equalTo?: string;
  /** Greater than the specified value. */
  greaterThan?: string;
  /** Greater than or equal to the specified value. */
  greaterThanOrEqualTo?: string;
  /** Included in the specified list. */
  in?: string[];
  /** Is null (if `true` is specified) or is not null (if `false` is specified). */
  isNull?: boolean;
  /** Less than the specified value. */
  lessThan?: string;
  /** Less than or equal to the specified value. */
  lessThanOrEqualTo?: string;
  /** Equal to the specified value, treating null like an ordinary value. */
  notDistinctFrom?: string;
  /** Not equal to the specified value. */
  notEqualTo?: string;
  /** Not included in the specified list. */
  notIn?: string[];
}
// ============ Payload/Return Types (for custom operations) ============
export interface FilesRenamePayload {
  clientMutationId?: string | null;
  fileEdge?: FileEdge | null;
  result?: File | null;
}
export type FilesRenamePayloadSelect = {
  clientMutationId?: boolean;
  fileEdge?: {
    select: FileEdgeSelect;
  };
  result?: {
    select: FileSelect;
  };
};
export interface PlatformFilesRenamePayload {
  clientMutationId?: string | null;
  platformFileEdge?: PlatformFileEdge | null;
  result?: PlatformFile | null;
}
export type PlatformFilesRenamePayloadSelect = {
  clientMutationId?: boolean;
  platformFileEdge?: {
    select: PlatformFileEdgeSelect;
  };
  result?: {
    select: PlatformFileSelect;
  };
};
export interface ProvisionBucketPayload {
  /** The access type applied */
  accessType: string;
  /** The S3 bucket name that was provisioned */
  bucketName: string;
  /** The S3 endpoint (null for AWS S3 default) */
  endpoint?: string | null;
  /** Error message if provisioning failed */
  error?: string | null;
  /** The storage provider used */
  provider: string;
  /** Whether provisioning succeeded */
  success: boolean;
}
export type ProvisionBucketPayloadSelect = {
  accessType?: boolean;
  bucketName?: boolean;
  endpoint?: boolean;
  error?: boolean;
  provider?: boolean;
  success?: boolean;
};
export interface UploadPlatformFilePayload {
  /** Whether this file was deduplicated (content already exists) */
  deduplicated: boolean;
  /** Presigned URL expiry time (null if deduplicated) */
  expiresAt?: string | null;
  /** The projection document for the created file: {id, key, bucket_id, mime, size, filename, url?}. Store this verbatim in an image/upload column — its `id` is what keeps the object from being garbage collected while the column still references it. */
  file?: Record<string, unknown> | null;
  /** The file ID (UUID) */
  fileId: string;
  /** The S3 object key */
  key: string;
  /** ID of the previous version (when using custom keys) */
  previousVersionId?: string | null;
  /** Presigned PUT URL (null if deduplicated) */
  uploadUrl?: string | null;
}
export type UploadPlatformFilePayloadSelect = {
  deduplicated?: boolean;
  expiresAt?: boolean;
  file?: boolean;
  fileId?: boolean;
  key?: boolean;
  previousVersionId?: boolean;
  uploadUrl?: boolean;
};
export interface UploadPlatformFileBulkPayload {
  files: UploadPlatformFileBulkFilePayload[];
}
export type UploadPlatformFileBulkPayloadSelect = {
  files?: {
    select: UploadPlatformFileBulkFilePayloadSelect;
  };
};
export interface CreateBucketPayload {
  /** The `Bucket` that was created by this mutation. */
  bucket?: Bucket | null;
  bucketEdge?: BucketEdge | null;
  clientMutationId?: string | null;
}
export type CreateBucketPayloadSelect = {
  bucket?: {
    select: BucketSelect;
  };
  bucketEdge?: {
    select: BucketEdgeSelect;
  };
  clientMutationId?: boolean;
};
export interface UpdateBucketPayload {
  /** The `Bucket` that was updated by this mutation. */
  bucket?: Bucket | null;
  bucketEdge?: BucketEdge | null;
  clientMutationId?: string | null;
}
export type UpdateBucketPayloadSelect = {
  bucket?: {
    select: BucketSelect;
  };
  bucketEdge?: {
    select: BucketEdgeSelect;
  };
  clientMutationId?: boolean;
};
export interface DeleteBucketPayload {
  /** The `Bucket` that was deleted by this mutation. */
  bucket?: Bucket | null;
  bucketEdge?: BucketEdge | null;
  clientMutationId?: string | null;
}
export type DeleteBucketPayloadSelect = {
  bucket?: {
    select: BucketSelect;
  };
  bucketEdge?: {
    select: BucketEdgeSelect;
  };
  clientMutationId?: boolean;
};
export interface CreateFilePayload {
  clientMutationId?: string | null;
  /** The `File` that was created by this mutation. */
  file?: File | null;
  fileEdge?: FileEdge | null;
}
export type CreateFilePayloadSelect = {
  clientMutationId?: boolean;
  file?: {
    select: FileSelect;
  };
  fileEdge?: {
    select: FileEdgeSelect;
  };
};
export interface UpdateFilePayload {
  clientMutationId?: string | null;
  /** The `File` that was updated by this mutation. */
  file?: File | null;
  fileEdge?: FileEdge | null;
}
export type UpdateFilePayloadSelect = {
  clientMutationId?: boolean;
  file?: {
    select: FileSelect;
  };
  fileEdge?: {
    select: FileEdgeSelect;
  };
};
export interface DeleteFilePayload {
  clientMutationId?: string | null;
  /** The `File` that was deleted by this mutation. */
  file?: File | null;
  fileEdge?: FileEdge | null;
}
export type DeleteFilePayloadSelect = {
  clientMutationId?: boolean;
  file?: {
    select: FileSelect;
  };
  fileEdge?: {
    select: FileEdgeSelect;
  };
};
export interface CreatePlatformBucketPayload {
  clientMutationId?: string | null;
  /** The `PlatformBucket` that was created by this mutation. */
  platformBucket?: PlatformBucket | null;
  platformBucketEdge?: PlatformBucketEdge | null;
}
export type CreatePlatformBucketPayloadSelect = {
  clientMutationId?: boolean;
  platformBucket?: {
    select: PlatformBucketSelect;
  };
  platformBucketEdge?: {
    select: PlatformBucketEdgeSelect;
  };
};
export interface UpdatePlatformBucketPayload {
  clientMutationId?: string | null;
  /** The `PlatformBucket` that was updated by this mutation. */
  platformBucket?: PlatformBucket | null;
  platformBucketEdge?: PlatformBucketEdge | null;
}
export type UpdatePlatformBucketPayloadSelect = {
  clientMutationId?: boolean;
  platformBucket?: {
    select: PlatformBucketSelect;
  };
  platformBucketEdge?: {
    select: PlatformBucketEdgeSelect;
  };
};
export interface DeletePlatformBucketPayload {
  clientMutationId?: string | null;
  /** The `PlatformBucket` that was deleted by this mutation. */
  platformBucket?: PlatformBucket | null;
  platformBucketEdge?: PlatformBucketEdge | null;
}
export type DeletePlatformBucketPayloadSelect = {
  clientMutationId?: boolean;
  platformBucket?: {
    select: PlatformBucketSelect;
  };
  platformBucketEdge?: {
    select: PlatformBucketEdgeSelect;
  };
};
export interface CreatePlatformFilePayload {
  clientMutationId?: string | null;
  /** The `PlatformFile` that was created by this mutation. */
  platformFile?: PlatformFile | null;
  platformFileEdge?: PlatformFileEdge | null;
}
export type CreatePlatformFilePayloadSelect = {
  clientMutationId?: boolean;
  platformFile?: {
    select: PlatformFileSelect;
  };
  platformFileEdge?: {
    select: PlatformFileEdgeSelect;
  };
};
export interface UpdatePlatformFilePayload {
  clientMutationId?: string | null;
  /** The `PlatformFile` that was updated by this mutation. */
  platformFile?: PlatformFile | null;
  platformFileEdge?: PlatformFileEdge | null;
}
export type UpdatePlatformFilePayloadSelect = {
  clientMutationId?: boolean;
  platformFile?: {
    select: PlatformFileSelect;
  };
  platformFileEdge?: {
    select: PlatformFileEdgeSelect;
  };
};
export interface DeletePlatformFilePayload {
  clientMutationId?: string | null;
  /** The `PlatformFile` that was deleted by this mutation. */
  platformFile?: PlatformFile | null;
  platformFileEdge?: PlatformFileEdge | null;
}
export type DeletePlatformFilePayloadSelect = {
  clientMutationId?: boolean;
  platformFile?: {
    select: PlatformFileSelect;
  };
  platformFileEdge?: {
    select: PlatformFileEdgeSelect;
  };
};
/** A `File` edge in the connection. */
export interface FileEdge {
  cursor?: string | null;
  /** The `File` at the end of the edge. */
  node?: File | null;
}
export type FileEdgeSelect = {
  cursor?: boolean;
  node?: {
    select: FileSelect;
  };
};
/** A `PlatformFile` edge in the connection. */
export interface PlatformFileEdge {
  cursor?: string | null;
  /** The `PlatformFile` at the end of the edge. */
  node?: PlatformFile | null;
}
export type PlatformFileEdgeSelect = {
  cursor?: boolean;
  node?: {
    select: PlatformFileSelect;
  };
};
export interface UploadPlatformFileBulkFilePayload {
  deduplicated: boolean;
  expiresAt?: string | null;
  /** The projection document for the created file. */
  file?: Record<string, unknown> | null;
  fileId: string;
  key: string;
  previousVersionId?: string | null;
  uploadUrl?: string | null;
}
export type UploadPlatformFileBulkFilePayloadSelect = {
  deduplicated?: boolean;
  expiresAt?: boolean;
  file?: boolean;
  fileId?: boolean;
  key?: boolean;
  previousVersionId?: boolean;
  uploadUrl?: boolean;
};
/** A `Bucket` edge in the connection. */
export interface BucketEdge {
  cursor?: string | null;
  /** The `Bucket` at the end of the edge. */
  node?: Bucket | null;
}
export type BucketEdgeSelect = {
  cursor?: boolean;
  node?: {
    select: BucketSelect;
  };
};
/** A `PlatformBucket` edge in the connection. */
export interface PlatformBucketEdge {
  cursor?: string | null;
  /** The `PlatformBucket` at the end of the edge. */
  node?: PlatformBucket | null;
}
export type PlatformBucketEdgeSelect = {
  cursor?: boolean;
  node?: {
    select: PlatformBucketSelect;
  };
};
