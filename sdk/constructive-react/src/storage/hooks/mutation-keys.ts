/**
 * Centralized mutation key factory
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

// ============================================================================
// Mutation keys for tracking in-flight mutations
//
// Benefits:
// - Track mutation state with useIsMutating
// - Implement optimistic updates with proper rollback
// - Deduplicate identical mutations
// - Coordinate related mutations
// ============================================================================

// ============================================================================
// Entity Mutation Keys
// ============================================================================

export const bucketMutationKeys = {
  /** All bucket mutation keys */ all: ['mutation', 'bucket'] as const,
  /** Create bucket mutation key */ create: () => ['mutation', 'bucket', 'create'] as const,
  /** Update bucket mutation key */ update: (id: string | number) =>
    ['mutation', 'bucket', 'update', id] as const,
  /** Delete bucket mutation key */ delete: (id: string | number) =>
    ['mutation', 'bucket', 'delete', id] as const,
} as const;
export const fileMutationKeys = {
  /** All file mutation keys */ all: ['mutation', 'file'] as const,
  /** Create file mutation key */ create: () => ['mutation', 'file', 'create'] as const,
  /** Update file mutation key */ update: (id: string | number) =>
    ['mutation', 'file', 'update', id] as const,
  /** Delete file mutation key */ delete: (id: string | number) =>
    ['mutation', 'file', 'delete', id] as const,
} as const;
export const platformBucketMutationKeys = {
  /** All platformBucket mutation keys */ all: ['mutation', 'platformbucket'] as const,
  /** Create platformBucket mutation key */ create: () =>
    ['mutation', 'platformbucket', 'create'] as const,
  /** Update platformBucket mutation key */ update: (id: string | number) =>
    ['mutation', 'platformbucket', 'update', id] as const,
  /** Delete platformBucket mutation key */ delete: (id: string | number) =>
    ['mutation', 'platformbucket', 'delete', id] as const,
} as const;
export const platformFileMutationKeys = {
  /** All platformFile mutation keys */ all: ['mutation', 'platformfile'] as const,
  /** Create platformFile mutation key */ create: () =>
    ['mutation', 'platformfile', 'create'] as const,
  /** Update platformFile mutation key */ update: (id: string | number) =>
    ['mutation', 'platformfile', 'update', id] as const,
  /** Delete platformFile mutation key */ delete: (id: string | number) =>
    ['mutation', 'platformfile', 'delete', id] as const,
} as const;

// ============================================================================
// Custom Mutation Keys
// ============================================================================

export const customMutationKeys = {
  /** Mutation key for filesRename */ filesRename: (identifier?: string) =>
    identifier
      ? (['mutation', 'filesRename', identifier] as const)
      : (['mutation', 'filesRename'] as const),
  /** Mutation key for platformFilesRename */ platformFilesRename: (identifier?: string) =>
    identifier
      ? (['mutation', 'platformFilesRename', identifier] as const)
      : (['mutation', 'platformFilesRename'] as const),
  /** Mutation key for provisionBucket */ provisionBucket: (identifier?: string) =>
    identifier
      ? (['mutation', 'provisionBucket', identifier] as const)
      : (['mutation', 'provisionBucket'] as const),
  /** Mutation key for uploadPlatformFile */ uploadPlatformFile: (identifier?: string) =>
    identifier
      ? (['mutation', 'uploadPlatformFile', identifier] as const)
      : (['mutation', 'uploadPlatformFile'] as const),
  /** Mutation key for uploadPlatformFiles */ uploadPlatformFiles: (identifier?: string) =>
    identifier
      ? (['mutation', 'uploadPlatformFiles', identifier] as const)
      : (['mutation', 'uploadPlatformFiles'] as const),
} as const;
/**

// ============================================================================
// Unified Mutation Key Store
// ============================================================================

 * Unified mutation key store
 * 
 * Use this for tracking in-flight mutations with useIsMutating.
 * 
 * @example
 * ```ts
 * import { useIsMutating } from '@tanstack/react-query';
 * import { mutationKeys } from './generated';
 * 
 * // Check if any user mutations are in progress
 * const isMutatingUser = useIsMutating({ mutationKey: mutationKeys.user.all });
 * 
 * // Check if a specific user is being updated
 * const isUpdating = useIsMutating({ mutationKey: mutationKeys.user.update(userId) });
 * ```
 */
export const mutationKeys = {
  bucket: bucketMutationKeys,
  file: fileMutationKeys,
  platformBucket: platformBucketMutationKeys,
  platformFile: platformFileMutationKeys,
  custom: customMutationKeys,
} as const;
