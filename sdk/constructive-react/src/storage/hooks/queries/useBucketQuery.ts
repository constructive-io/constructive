/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { bucketKeys } from '../query-keys';
import type { BucketSelect, BucketWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BucketSelect, BucketWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const bucketQueryKey = bucketKeys.detail;
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBucketQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useBucketQuery<
  S extends BucketSelect,
  TData = {
    bucket: InferSelectResult<BucketWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BucketSelect>;
  } & Omit<
    UseQueryOptions<
      {
        bucket: InferSelectResult<BucketWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBucketQuery(
  params: {
    id: string;
    selection: SelectionConfig<BucketSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<BucketSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: bucketKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .bucket.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * const data = await fetchBucketQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchBucketQuery<S extends BucketSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, BucketSelect>;
}): Promise<{
  bucket: InferSelectResult<BucketWithRelations, S> | null;
}>;
export async function fetchBucketQuery(params: {
  id: string;
  selection: SelectionConfig<BucketSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<BucketSelect>(params.selection);
  return getClient()
    .bucket.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * await prefetchBucketQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchBucketQuery<S extends BucketSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BucketSelect>;
  }
): Promise<void>;
export async function prefetchBucketQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<BucketSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<BucketSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: bucketKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .bucket.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
