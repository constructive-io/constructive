/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { fileKeys } from '../query-keys';
import type { FileSelect, FileWithRelations, FileFilter, FileOrderBy } from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type { FileSelect, FileWithRelations, FileFilter, FileOrderBy } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const filesQueryKey = fileKeys.list;
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFilesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useFilesQuery<
  S extends FileSelect,
  TData = {
    files: ConnectionResult<InferSelectResult<FileWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, FileFilter, FileOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, FileSelect>;
  } & Omit<
    UseQueryOptions<
      {
        files: ConnectionResult<InferSelectResult<FileWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFilesQuery(
  params: {
    selection: ListSelectionConfig<FileSelect, FileFilter, FileOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<FileSelect, FileFilter, FileOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: fileKeys.list(args),
    queryFn: () => getClient().file.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```ts
 * const data = await fetchFilesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchFilesQuery<S extends FileSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, FileFilter, FileOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, FileSelect>;
}): Promise<{
  files: ConnectionResult<InferSelectResult<FileWithRelations, S>>;
}>;
export async function fetchFilesQuery(params: {
  selection: ListSelectionConfig<FileSelect, FileFilter, FileOrderBy>;
}) {
  const args = buildListSelectionArgs<FileSelect, FileFilter, FileOrderBy>(params.selection);
  return getClient().file.findMany(args).unwrap();
}
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```ts
 * await prefetchFilesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchFilesQuery<S extends FileSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, FileFilter, FileOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, FileSelect>;
  }
): Promise<void>;
export async function prefetchFilesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<FileSelect, FileFilter, FileOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<FileSelect, FileFilter, FileOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: fileKeys.list(args),
    queryFn: () => getClient().file.findMany(args).unwrap(),
  });
}
