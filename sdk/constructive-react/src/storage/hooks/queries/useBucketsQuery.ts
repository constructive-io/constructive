/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { bucketKeys } from '../query-keys';
import type {
  BucketSelect,
  BucketWithRelations,
  BucketFilter,
  BucketOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  BucketSelect,
  BucketWithRelations,
  BucketFilter,
  BucketOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const bucketsQueryKey = bucketKeys.list;
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBucketsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useBucketsQuery<
  S extends BucketSelect,
  TData = {
    buckets: ConnectionResult<InferSelectResult<BucketWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, BucketFilter, BucketOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, BucketSelect>;
  } & Omit<
    UseQueryOptions<
      {
        buckets: ConnectionResult<InferSelectResult<BucketWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBucketsQuery(
  params: {
    selection: ListSelectionConfig<BucketSelect, BucketFilter, BucketOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<BucketSelect, BucketFilter, BucketOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: bucketKeys.list(args),
    queryFn: () => getClient().bucket.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * const data = await fetchBucketsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchBucketsQuery<S extends BucketSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, BucketFilter, BucketOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, BucketSelect>;
}): Promise<{
  buckets: ConnectionResult<InferSelectResult<BucketWithRelations, S>>;
}>;
export async function fetchBucketsQuery(params: {
  selection: ListSelectionConfig<BucketSelect, BucketFilter, BucketOrderBy>;
}) {
  const args = buildListSelectionArgs<BucketSelect, BucketFilter, BucketOrderBy>(params.selection);
  return getClient().bucket.findMany(args).unwrap();
}
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```ts
 * await prefetchBucketsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchBucketsQuery<S extends BucketSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, BucketFilter, BucketOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, BucketSelect>;
  }
): Promise<void>;
export async function prefetchBucketsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<BucketSelect, BucketFilter, BucketOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<BucketSelect, BucketFilter, BucketOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: bucketKeys.list(args),
    queryFn: () => getClient().bucket.findMany(args).unwrap(),
  });
}
