/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { fileKeys } from '../query-keys';
import type { FileSelect, FileWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FileSelect, FileWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const fileQueryKey = fileKeys.detail;
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFileQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFileQuery<
  S extends FileSelect,
  TData = {
    file: InferSelectResult<FileWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FileSelect>;
  } & Omit<
    UseQueryOptions<
      {
        file: InferSelectResult<FileWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFileQuery(
  params: {
    id: string;
    selection: SelectionConfig<FileSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FileSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: fileKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .file.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```ts
 * const data = await fetchFileQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFileQuery<S extends FileSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FileSelect>;
}): Promise<{
  file: InferSelectResult<FileWithRelations, S> | null;
}>;
export async function fetchFileQuery(params: {
  id: string;
  selection: SelectionConfig<FileSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<FileSelect>(params.selection);
  return getClient()
    .file.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Individual file records within buckets, with immutable identity fields and mutable metadata
 *
 * @example
 * ```ts
 * await prefetchFileQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFileQuery<S extends FileSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FileSelect>;
  }
): Promise<void>;
export async function prefetchFileQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FileSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FileSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: fileKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .file.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
