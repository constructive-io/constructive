/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { bucketKeys } from '../query-keys';
import { bucketMutationKeys } from '../mutation-keys';
import type { BucketSelect, BucketWithRelations, CreateBucketInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BucketSelect, BucketWithRelations, CreateBucketInput } from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateBucketMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateBucketMutation<S extends BucketSelect>(
  params: {
    selection: {
      fields: S & BucketSelect;
    } & HookStrictSelect<NoInfer<S>, BucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createBucket: {
          bucket: InferSelectResult<BucketWithRelations, S>;
        };
      },
      Error,
      CreateBucketInput['bucket']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createBucket: {
      bucket: InferSelectResult<BucketWithRelations, S>;
    };
  },
  Error,
  CreateBucketInput['bucket']
>;
export function useCreateBucketMutation(
  params: {
    selection: SelectionConfig<BucketSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateBucketInput['bucket']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<BucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: bucketMutationKeys.create(),
    mutationFn: (data: CreateBucketInput['bucket']) =>
      getClient()
        .bucket.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: bucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
