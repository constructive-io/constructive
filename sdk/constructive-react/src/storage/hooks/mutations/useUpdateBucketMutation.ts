/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { bucketKeys } from '../query-keys';
import { bucketMutationKeys } from '../mutation-keys';
import type { BucketSelect, BucketWithRelations, BucketPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BucketSelect, BucketWithRelations, BucketPatch } from '../../orm/input-types';
/**
 * Logical storage containers that group files with shared access policies and CDN behavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateBucketMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', bucketPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateBucketMutation<S extends BucketSelect>(
  params: {
    selection: {
      fields: S & BucketSelect;
    } & HookStrictSelect<NoInfer<S>, BucketSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateBucket: {
          bucket: InferSelectResult<BucketWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        bucketPatch: BucketPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateBucket: {
      bucket: InferSelectResult<BucketWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    bucketPatch: BucketPatch;
  }
>;
export function useUpdateBucketMutation(
  params: {
    selection: SelectionConfig<BucketSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        bucketPatch: BucketPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BucketSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: bucketMutationKeys.all,
    mutationFn: ({ id, bucketPatch }: { id: string; bucketPatch: BucketPatch }) =>
      getClient()
        .bucket.update({
          where: {
            id,
          },
          data: bucketPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: bucketKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: bucketKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
