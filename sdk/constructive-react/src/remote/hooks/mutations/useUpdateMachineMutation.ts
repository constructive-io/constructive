/**
 * Computers enrolled for remote control, one row per database enrollment
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { machineKeys } from '../query-keys';
import { machineMutationKeys } from '../mutation-keys';
import type { MachineSelect, MachineWithRelations, MachinePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MachineSelect, MachineWithRelations, MachinePatch } from '../../orm/input-types';
/**
 * Computers enrolled for remote control, one row per database enrollment
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateMachineMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', machinePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateMachineMutation<S extends MachineSelect>(
  params: {
    selection: {
      fields: S & MachineSelect;
    } & HookStrictSelect<NoInfer<S>, MachineSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateMachine: {
          machine: InferSelectResult<MachineWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        machinePatch: MachinePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateMachine: {
      machine: InferSelectResult<MachineWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    machinePatch: MachinePatch;
  }
>;
export function useUpdateMachineMutation(
  params: {
    selection: SelectionConfig<MachineSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        machinePatch: MachinePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MachineSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: machineMutationKeys.all,
    mutationFn: ({ id, machinePatch }: { id: string; machinePatch: MachinePatch }) =>
      getClient()
        .machine.update({
          where: {
            id,
          },
          data: machinePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: machineKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: machineKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
