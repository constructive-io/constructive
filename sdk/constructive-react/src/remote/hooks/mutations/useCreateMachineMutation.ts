/**
 * Computers enrolled for remote control, one row per database enrollment
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { machineKeys } from '../query-keys';
import { machineMutationKeys } from '../mutation-keys';
import type {
  MachineSelect,
  MachineWithRelations,
  CreateMachineInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  MachineSelect,
  MachineWithRelations,
  CreateMachineInput,
} from '../../orm/input-types';
/**
 * Computers enrolled for remote control, one row per database enrollment
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateMachineMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateMachineMutation<S extends MachineSelect>(
  params: {
    selection: {
      fields: S & MachineSelect;
    } & HookStrictSelect<NoInfer<S>, MachineSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createMachine: {
          machine: InferSelectResult<MachineWithRelations, S>;
        };
      },
      Error,
      CreateMachineInput['machine']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createMachine: {
      machine: InferSelectResult<MachineWithRelations, S>;
    };
  },
  Error,
  CreateMachineInput['machine']
>;
export function useCreateMachineMutation(
  params: {
    selection: SelectionConfig<MachineSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateMachineInput['machine']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<MachineSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: machineMutationKeys.create(),
    mutationFn: (data: CreateMachineInput['machine']) =>
      getClient()
        .machine.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: machineKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
