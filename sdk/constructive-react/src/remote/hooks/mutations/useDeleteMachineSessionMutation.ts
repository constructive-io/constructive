/**
 * Command or terminal sessions running on an enrolled machine
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { machineSessionKeys } from '../query-keys';
import { machineSessionMutationKeys } from '../mutation-keys';
import type { MachineSessionSelect, MachineSessionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MachineSessionSelect, MachineSessionWithRelations } from '../../orm/input-types';
/**
 * Command or terminal sessions running on an enrolled machine
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteMachineSessionMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteMachineSessionMutation<S extends MachineSessionSelect>(
  params: {
    selection: {
      fields: S & MachineSessionSelect;
    } & HookStrictSelect<NoInfer<S>, MachineSessionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteMachineSession: {
          machineSession: InferSelectResult<MachineSessionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteMachineSession: {
      machineSession: InferSelectResult<MachineSessionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteMachineSessionMutation(
  params: {
    selection: SelectionConfig<MachineSessionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MachineSessionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: machineSessionMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .machineSession.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: machineSessionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: machineSessionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
