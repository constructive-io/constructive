/**
 * Command or terminal sessions running on an enrolled machine
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { machineSessionKeys } from '../query-keys';
import type { MachineSessionSelect, MachineSessionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MachineSessionSelect, MachineSessionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const machineSessionQueryKey = machineSessionKeys.detail;
/**
 * Command or terminal sessions running on an enrolled machine
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMachineSessionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMachineSessionQuery<
  S extends MachineSessionSelect,
  TData = {
    machineSession: InferSelectResult<MachineSessionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MachineSessionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        machineSession: InferSelectResult<MachineSessionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMachineSessionQuery(
  params: {
    id: string;
    selection: SelectionConfig<MachineSessionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MachineSessionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: machineSessionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .machineSession.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Command or terminal sessions running on an enrolled machine
 *
 * @example
 * ```ts
 * const data = await fetchMachineSessionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMachineSessionQuery<S extends MachineSessionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MachineSessionSelect>;
}): Promise<{
  machineSession: InferSelectResult<MachineSessionWithRelations, S> | null;
}>;
export async function fetchMachineSessionQuery(params: {
  id: string;
  selection: SelectionConfig<MachineSessionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MachineSessionSelect>(params.selection);
  return getClient()
    .machineSession.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Command or terminal sessions running on an enrolled machine
 *
 * @example
 * ```ts
 * await prefetchMachineSessionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMachineSessionQuery<S extends MachineSessionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MachineSessionSelect>;
  }
): Promise<void>;
export async function prefetchMachineSessionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MachineSessionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MachineSessionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: machineSessionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .machineSession.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
