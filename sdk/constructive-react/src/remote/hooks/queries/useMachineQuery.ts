/**
 * Computers enrolled for remote control, one row per database enrollment
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { machineKeys } from '../query-keys';
import type { MachineSelect, MachineWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MachineSelect, MachineWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const machineQueryKey = machineKeys.detail;
/**
 * Computers enrolled for remote control, one row per database enrollment
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMachineQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMachineQuery<
  S extends MachineSelect,
  TData = {
    machine: InferSelectResult<MachineWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MachineSelect>;
  } & Omit<
    UseQueryOptions<
      {
        machine: InferSelectResult<MachineWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMachineQuery(
  params: {
    id: string;
    selection: SelectionConfig<MachineSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MachineSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: machineKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .machine.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Computers enrolled for remote control, one row per database enrollment
 *
 * @example
 * ```ts
 * const data = await fetchMachineQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMachineQuery<S extends MachineSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MachineSelect>;
}): Promise<{
  machine: InferSelectResult<MachineWithRelations, S> | null;
}>;
export async function fetchMachineQuery(params: {
  id: string;
  selection: SelectionConfig<MachineSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MachineSelect>(params.selection);
  return getClient()
    .machine.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Computers enrolled for remote control, one row per database enrollment
 *
 * @example
 * ```ts
 * await prefetchMachineQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMachineQuery<S extends MachineSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MachineSelect>;
  }
): Promise<void>;
export async function prefetchMachineQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MachineSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MachineSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: machineKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .machine.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
