/**
 * Partitioned append-only ledger of session input, output and lifecycle
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { machineMessageKeys } from '../query-keys';
import type { MachineMessageSelect, MachineMessageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MachineMessageSelect, MachineMessageWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const machineMessageQueryKey = machineMessageKeys.detail;
/**
 * Partitioned append-only ledger of session input, output and lifecycle
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMachineMessageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMachineMessageQuery<
  S extends MachineMessageSelect,
  TData = {
    machineMessage: InferSelectResult<MachineMessageWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MachineMessageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        machineMessage: InferSelectResult<MachineMessageWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMachineMessageQuery(
  params: {
    id: string;
    selection: SelectionConfig<MachineMessageSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MachineMessageSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: machineMessageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .machineMessage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Partitioned append-only ledger of session input, output and lifecycle
 *
 * @example
 * ```ts
 * const data = await fetchMachineMessageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMachineMessageQuery<S extends MachineMessageSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MachineMessageSelect>;
}): Promise<{
  machineMessage: InferSelectResult<MachineMessageWithRelations, S> | null;
}>;
export async function fetchMachineMessageQuery(params: {
  id: string;
  selection: SelectionConfig<MachineMessageSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MachineMessageSelect>(params.selection);
  return getClient()
    .machineMessage.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Partitioned append-only ledger of session input, output and lifecycle
 *
 * @example
 * ```ts
 * await prefetchMachineMessageQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMachineMessageQuery<S extends MachineMessageSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MachineMessageSelect>;
  }
): Promise<void>;
export async function prefetchMachineMessageQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MachineMessageSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MachineMessageSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: machineMessageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .machineMessage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
