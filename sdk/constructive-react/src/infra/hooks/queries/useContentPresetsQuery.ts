/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { contentPresetKeys } from '../query-keys';
import type {
  ContentPresetSelect,
  ContentPresetWithRelations,
  ContentPresetFilter,
  ContentPresetOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  ContentPresetSelect,
  ContentPresetWithRelations,
  ContentPresetFilter,
  ContentPresetOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const contentPresetsQueryKey = contentPresetKeys.list;
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useContentPresetsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useContentPresetsQuery<
  S extends ContentPresetSelect,
  TData = {
    contentPresets: ConnectionResult<InferSelectResult<ContentPresetWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ContentPresetFilter, ContentPresetOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
  } & Omit<
    UseQueryOptions<
      {
        contentPresets: ConnectionResult<InferSelectResult<ContentPresetWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useContentPresetsQuery(
  params: {
    selection: ListSelectionConfig<ContentPresetSelect, ContentPresetFilter, ContentPresetOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    ContentPresetSelect,
    ContentPresetFilter,
    ContentPresetOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: contentPresetKeys.list(args),
    queryFn: () => getClient().contentPreset.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * const data = await fetchContentPresetsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchContentPresetsQuery<S extends ContentPresetSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, ContentPresetFilter, ContentPresetOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
}): Promise<{
  contentPresets: ConnectionResult<InferSelectResult<ContentPresetWithRelations, S>>;
}>;
export async function fetchContentPresetsQuery(params: {
  selection: ListSelectionConfig<ContentPresetSelect, ContentPresetFilter, ContentPresetOrderBy>;
}) {
  const args = buildListSelectionArgs<
    ContentPresetSelect,
    ContentPresetFilter,
    ContentPresetOrderBy
  >(params.selection);
  return getClient().contentPreset.findMany(args).unwrap();
}
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * await prefetchContentPresetsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchContentPresetsQuery<S extends ContentPresetSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ContentPresetFilter, ContentPresetOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
  }
): Promise<void>;
export async function prefetchContentPresetsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<ContentPresetSelect, ContentPresetFilter, ContentPresetOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    ContentPresetSelect,
    ContentPresetFilter,
    ContentPresetOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: contentPresetKeys.list(args),
    queryFn: () => getClient().contentPreset.findMany(args).unwrap(),
  });
}
