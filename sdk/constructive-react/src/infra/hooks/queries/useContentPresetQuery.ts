/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { contentPresetKeys } from '../query-keys';
import type { ContentPresetSelect, ContentPresetWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ContentPresetSelect, ContentPresetWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const contentPresetQueryKey = contentPresetKeys.detail;
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useContentPresetQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useContentPresetQuery<
  S extends ContentPresetSelect,
  TData = {
    contentPreset: InferSelectResult<ContentPresetWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
  } & Omit<
    UseQueryOptions<
      {
        contentPreset: InferSelectResult<ContentPresetWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useContentPresetQuery(
  params: {
    id: string;
    selection: SelectionConfig<ContentPresetSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ContentPresetSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: contentPresetKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .contentPreset.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * const data = await fetchContentPresetQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchContentPresetQuery<S extends ContentPresetSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
}): Promise<{
  contentPreset: InferSelectResult<ContentPresetWithRelations, S> | null;
}>;
export async function fetchContentPresetQuery(params: {
  id: string;
  selection: SelectionConfig<ContentPresetSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ContentPresetSelect>(params.selection);
  return getClient()
    .contentPreset.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * await prefetchContentPresetQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchContentPresetQuery<S extends ContentPresetSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
  }
): Promise<void>;
export async function prefetchContentPresetQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ContentPresetSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ContentPresetSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: contentPresetKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .contentPreset.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
