/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraStoreKeys } from '../query-keys';
import type { InfraStoreSelect, InfraStoreWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InfraStoreSelect, InfraStoreWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const infraStoreQueryKey = infraStoreKeys.detail;
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInfraStoreQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useInfraStoreQuery<
  S extends InfraStoreSelect,
  TData = {
    infraStore: InferSelectResult<InfraStoreWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InfraStoreSelect>;
  } & Omit<
    UseQueryOptions<
      {
        infraStore: InferSelectResult<InfraStoreWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useInfraStoreQuery(
  params: {
    id: string;
    selection: SelectionConfig<InfraStoreSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<InfraStoreSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: infraStoreKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .infraStore.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * const data = await fetchInfraStoreQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchInfraStoreQuery<S extends InfraStoreSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, InfraStoreSelect>;
}): Promise<{
  infraStore: InferSelectResult<InfraStoreWithRelations, S> | null;
}>;
export async function fetchInfraStoreQuery(params: {
  id: string;
  selection: SelectionConfig<InfraStoreSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<InfraStoreSelect>(params.selection);
  return getClient()
    .infraStore.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * await prefetchInfraStoreQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchInfraStoreQuery<S extends InfraStoreSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InfraStoreSelect>;
  }
): Promise<void>;
export async function prefetchInfraStoreQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<InfraStoreSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<InfraStoreSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: infraStoreKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .infraStore.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
