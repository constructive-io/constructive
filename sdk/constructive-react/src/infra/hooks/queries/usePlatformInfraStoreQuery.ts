/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformInfraStoreKeys } from '../query-keys';
import type {
  PlatformInfraStoreSelect,
  PlatformInfraStoreWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformInfraStoreSelect,
  PlatformInfraStoreWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformInfraStoreQueryKey = platformInfraStoreKeys.detail;
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformInfraStoreQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformInfraStoreQuery<
  S extends PlatformInfraStoreSelect,
  TData = {
    platformInfraStore: InferSelectResult<PlatformInfraStoreWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformInfraStoreSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformInfraStore: InferSelectResult<PlatformInfraStoreWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformInfraStoreQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformInfraStoreSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformInfraStoreSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformInfraStoreKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformInfraStore.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * const data = await fetchPlatformInfraStoreQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformInfraStoreQuery<S extends PlatformInfraStoreSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformInfraStoreSelect>;
}): Promise<{
  platformInfraStore: InferSelectResult<PlatformInfraStoreWithRelations, S> | null;
}>;
export async function fetchPlatformInfraStoreQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformInfraStoreSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformInfraStoreSelect>(params.selection);
  return getClient()
    .platformInfraStore.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * await prefetchPlatformInfraStoreQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformInfraStoreQuery<S extends PlatformInfraStoreSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformInfraStoreSelect>;
  }
): Promise<void>;
export async function prefetchPlatformInfraStoreQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformInfraStoreSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformInfraStoreSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformInfraStoreKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformInfraStore.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
