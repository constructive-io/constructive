/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { dbPresetKeys } from '../query-keys';
import type {
  DbPresetSelect,
  DbPresetWithRelations,
  DbPresetFilter,
  DbPresetOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  DbPresetSelect,
  DbPresetWithRelations,
  DbPresetFilter,
  DbPresetOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const dbPresetsQueryKey = dbPresetKeys.list;
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDbPresetsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useDbPresetsQuery<
  S extends DbPresetSelect,
  TData = {
    dbPresets: ConnectionResult<InferSelectResult<DbPresetWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DbPresetFilter, DbPresetOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DbPresetSelect>;
  } & Omit<
    UseQueryOptions<
      {
        dbPresets: ConnectionResult<InferSelectResult<DbPresetWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDbPresetsQuery(
  params: {
    selection: ListSelectionConfig<DbPresetSelect, DbPresetFilter, DbPresetOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<DbPresetSelect, DbPresetFilter, DbPresetOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: dbPresetKeys.list(args),
    queryFn: () => getClient().dbPreset.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * const data = await fetchDbPresetsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchDbPresetsQuery<S extends DbPresetSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, DbPresetFilter, DbPresetOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, DbPresetSelect>;
}): Promise<{
  dbPresets: ConnectionResult<InferSelectResult<DbPresetWithRelations, S>>;
}>;
export async function fetchDbPresetsQuery(params: {
  selection: ListSelectionConfig<DbPresetSelect, DbPresetFilter, DbPresetOrderBy>;
}) {
  const args = buildListSelectionArgs<DbPresetSelect, DbPresetFilter, DbPresetOrderBy>(
    params.selection
  );
  return getClient().dbPreset.findMany(args).unwrap();
}
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * await prefetchDbPresetsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchDbPresetsQuery<S extends DbPresetSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DbPresetFilter, DbPresetOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DbPresetSelect>;
  }
): Promise<void>;
export async function prefetchDbPresetsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<DbPresetSelect, DbPresetFilter, DbPresetOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<DbPresetSelect, DbPresetFilter, DbPresetOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: dbPresetKeys.list(args),
    queryFn: () => getClient().dbPreset.findMany(args).unwrap(),
  });
}
