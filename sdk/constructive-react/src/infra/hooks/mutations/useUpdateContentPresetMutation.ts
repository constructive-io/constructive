/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { contentPresetKeys } from '../query-keys';
import { contentPresetMutationKeys } from '../mutation-keys';
import type {
  ContentPresetSelect,
  ContentPresetWithRelations,
  ContentPresetPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ContentPresetSelect,
  ContentPresetWithRelations,
  ContentPresetPatch,
} from '../../orm/input-types';
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateContentPresetMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', contentPresetPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateContentPresetMutation<S extends ContentPresetSelect>(
  params: {
    selection: {
      fields: S & ContentPresetSelect;
    } & HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateContentPreset: {
          contentPreset: InferSelectResult<ContentPresetWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        contentPresetPatch: ContentPresetPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateContentPreset: {
      contentPreset: InferSelectResult<ContentPresetWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    contentPresetPatch: ContentPresetPatch;
  }
>;
export function useUpdateContentPresetMutation(
  params: {
    selection: SelectionConfig<ContentPresetSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        contentPresetPatch: ContentPresetPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ContentPresetSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: contentPresetMutationKeys.all,
    mutationFn: ({
      id,
      contentPresetPatch,
    }: {
      id: string;
      contentPresetPatch: ContentPresetPatch;
    }) =>
      getClient()
        .contentPreset.update({
          where: {
            id,
          },
          data: contentPresetPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: contentPresetKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: contentPresetKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
