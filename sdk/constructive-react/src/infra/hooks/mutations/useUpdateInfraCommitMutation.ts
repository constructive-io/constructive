/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraCommitKeys } from '../query-keys';
import { infraCommitMutationKeys } from '../mutation-keys';
import type {
  InfraCommitSelect,
  InfraCommitWithRelations,
  InfraCommitPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  InfraCommitSelect,
  InfraCommitWithRelations,
  InfraCommitPatch,
} from '../../orm/input-types';
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateInfraCommitMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', infraCommitPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateInfraCommitMutation<S extends InfraCommitSelect>(
  params: {
    selection: {
      fields: S & InfraCommitSelect;
    } & HookStrictSelect<NoInfer<S>, InfraCommitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateInfraCommit: {
          infraCommit: InferSelectResult<InfraCommitWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        scopeId: string;
        infraCommitPatch: InfraCommitPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateInfraCommit: {
      infraCommit: InferSelectResult<InfraCommitWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    scopeId: string;
    infraCommitPatch: InfraCommitPatch;
  }
>;
export function useUpdateInfraCommitMutation(
  params: {
    selection: SelectionConfig<InfraCommitSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        scopeId: string;
        infraCommitPatch: InfraCommitPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InfraCommitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: infraCommitMutationKeys.all,
    mutationFn: ({
      id,
      scopeId,
      infraCommitPatch,
    }: {
      id: string;
      scopeId: string;
      infraCommitPatch: InfraCommitPatch;
    }) =>
      getClient()
        .infraCommit.update({
          where: {
            id,
            scopeId,
          },
          data: infraCommitPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: infraCommitKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: infraCommitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
