/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPresetKeys } from '../query-keys';
import { dbPresetMutationKeys } from '../mutation-keys';
import type { DbPresetSelect, DbPresetWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DbPresetSelect, DbPresetWithRelations } from '../../orm/input-types';
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDbPresetMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteDbPresetMutation<S extends DbPresetSelect>(
  params: {
    selection: {
      fields: S & DbPresetSelect;
    } & HookStrictSelect<NoInfer<S>, DbPresetSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteDbPreset: {
          dbPreset: InferSelectResult<DbPresetWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteDbPreset: {
      dbPreset: InferSelectResult<DbPresetWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteDbPresetMutation(
  params: {
    selection: SelectionConfig<DbPresetSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DbPresetSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: dbPresetMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .dbPreset.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: dbPresetKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: dbPresetKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
