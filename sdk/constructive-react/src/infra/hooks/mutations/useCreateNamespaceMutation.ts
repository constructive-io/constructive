/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { namespaceKeys } from '../query-keys';
import { namespaceMutationKeys } from '../mutation-keys';
import type {
  NamespaceSelect,
  NamespaceWithRelations,
  CreateNamespaceInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  NamespaceSelect,
  NamespaceWithRelations,
  CreateNamespaceInput,
} from '../../orm/input-types';
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateNamespaceMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateNamespaceMutation<S extends NamespaceSelect>(
  params: {
    selection: {
      fields: S & NamespaceSelect;
    } & HookStrictSelect<NoInfer<S>, NamespaceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createNamespace: {
          namespace: InferSelectResult<NamespaceWithRelations, S>;
        };
      },
      Error,
      CreateNamespaceInput['namespace']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createNamespace: {
      namespace: InferSelectResult<NamespaceWithRelations, S>;
    };
  },
  Error,
  CreateNamespaceInput['namespace']
>;
export function useCreateNamespaceMutation(
  params: {
    selection: SelectionConfig<NamespaceSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateNamespaceInput['namespace']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<NamespaceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: namespaceMutationKeys.create(),
    mutationFn: (data: CreateNamespaceInput['namespace']) =>
      getClient()
        .namespace.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: namespaceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
