/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { contentPresetKeys } from '../query-keys';
import { contentPresetMutationKeys } from '../mutation-keys';
import type { ContentPresetSelect, ContentPresetWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ContentPresetSelect, ContentPresetWithRelations } from '../../orm/input-types';
/**
 * Seed-content preset catalog (limit defaults, trust ladders, ...) — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteContentPresetMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteContentPresetMutation<S extends ContentPresetSelect>(
  params: {
    selection: {
      fields: S & ContentPresetSelect;
    } & HookStrictSelect<NoInfer<S>, ContentPresetSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteContentPreset: {
          contentPreset: InferSelectResult<ContentPresetWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteContentPreset: {
      contentPreset: InferSelectResult<ContentPresetWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteContentPresetMutation(
  params: {
    selection: SelectionConfig<ContentPresetSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ContentPresetSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: contentPresetMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .contentPreset.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: contentPresetKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: contentPresetKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
