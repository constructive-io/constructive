/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformInfraStoreKeys } from '../query-keys';
import { platformInfraStoreMutationKeys } from '../mutation-keys';
import type {
  PlatformInfraStoreSelect,
  PlatformInfraStoreWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformInfraStoreSelect,
  PlatformInfraStoreWithRelations,
} from '../../orm/input-types';
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformInfraStoreMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformInfraStoreMutation<S extends PlatformInfraStoreSelect>(
  params: {
    selection: {
      fields: S & PlatformInfraStoreSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformInfraStoreSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformInfraStore: {
          platformInfraStore: InferSelectResult<PlatformInfraStoreWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformInfraStore: {
      platformInfraStore: InferSelectResult<PlatformInfraStoreWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformInfraStoreMutation(
  params: {
    selection: SelectionConfig<PlatformInfraStoreSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformInfraStoreSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformInfraStoreMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformInfraStore.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformInfraStoreKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformInfraStoreKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
