/**
 * Delete mutation hook for Schema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { schemaKeys } from '../query-keys';
import { schemaMutationKeys } from '../mutation-keys';
import type { SchemaSelect, SchemaWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SchemaSelect, SchemaWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Schema with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSchemaMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteSchemaMutation<S extends SchemaSelect>(
  params: {
    selection: {
      fields: S & SchemaSelect;
    } & HookStrictSelect<NoInfer<S>, SchemaSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteSchema: {
          schema: InferSelectResult<SchemaWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteSchema: {
      schema: InferSelectResult<SchemaWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteSchemaMutation(
  params: {
    selection: SelectionConfig<SchemaSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SchemaSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: schemaMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .schema.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: schemaKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: schemaKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
