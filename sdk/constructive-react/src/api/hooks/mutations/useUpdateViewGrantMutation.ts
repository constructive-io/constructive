/**
 * Update mutation hook for ViewGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { viewGrantKeys } from '../query-keys';
import { viewGrantMutationKeys } from '../mutation-keys';
import type {
  ViewGrantSelect,
  ViewGrantWithRelations,
  ViewGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ViewGrantSelect,
  ViewGrantWithRelations,
  ViewGrantPatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a ViewGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateViewGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', viewGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateViewGrantMutation<S extends ViewGrantSelect>(
  params: {
    selection: {
      fields: S & ViewGrantSelect;
    } & HookStrictSelect<NoInfer<S>, ViewGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateViewGrant: {
          viewGrant: InferSelectResult<ViewGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        viewGrantPatch: ViewGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateViewGrant: {
      viewGrant: InferSelectResult<ViewGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    viewGrantPatch: ViewGrantPatch;
  }
>;
export function useUpdateViewGrantMutation(
  params: {
    selection: SelectionConfig<ViewGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        viewGrantPatch: ViewGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ViewGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: viewGrantMutationKeys.all,
    mutationFn: ({ id, viewGrantPatch }: { id: string; viewGrantPatch: ViewGrantPatch }) =>
      getClient()
        .viewGrant.update({
          where: {
            id,
          },
          data: viewGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: viewGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: viewGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
