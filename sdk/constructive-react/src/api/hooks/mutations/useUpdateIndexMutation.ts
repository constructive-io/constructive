/**
 * Update mutation hook for Index
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { indexKeys } from '../query-keys';
import { indexMutationKeys } from '../mutation-keys';
import type { IndexSelect, IndexWithRelations, IndexPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { IndexSelect, IndexWithRelations, IndexPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Index
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateIndexMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', indexPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateIndexMutation<S extends IndexSelect>(
  params: {
    selection: {
      fields: S & IndexSelect;
    } & HookStrictSelect<NoInfer<S>, IndexSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateIndex: {
          index: InferSelectResult<IndexWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        indexPatch: IndexPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateIndex: {
      index: InferSelectResult<IndexWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    indexPatch: IndexPatch;
  }
>;
export function useUpdateIndexMutation(
  params: {
    selection: SelectionConfig<IndexSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        indexPatch: IndexPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<IndexSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: indexMutationKeys.all,
    mutationFn: ({ id, indexPatch }: { id: string; indexPatch: IndexPatch }) =>
      getClient()
        .index.update({
          where: {
            id,
          },
          data: indexPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: indexKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: indexKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
