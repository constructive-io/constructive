/**
 * Update mutation hook for Derive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { deriveKeys } from '../query-keys';
import { deriveMutationKeys } from '../mutation-keys';
import type { DeriveSelect, DeriveWithRelations, DerivePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DeriveSelect, DeriveWithRelations, DerivePatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Derive
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDeriveMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', derivePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateDeriveMutation<S extends DeriveSelect>(
  params: {
    selection: {
      fields: S & DeriveSelect;
    } & HookStrictSelect<NoInfer<S>, DeriveSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateDerive: {
          derive: InferSelectResult<DeriveWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        derivePatch: DerivePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateDerive: {
      derive: InferSelectResult<DeriveWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    derivePatch: DerivePatch;
  }
>;
export function useUpdateDeriveMutation(
  params: {
    selection: SelectionConfig<DeriveSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        derivePatch: DerivePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DeriveSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: deriveMutationKeys.all,
    mutationFn: ({ id, derivePatch }: { id: string; derivePatch: DerivePatch }) =>
      getClient()
        .derive.update({
          where: {
            id,
          },
          data: derivePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: deriveKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: deriveKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
