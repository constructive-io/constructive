/**
 * Delete mutation hook for Derive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { deriveKeys } from '../query-keys';
import { deriveMutationKeys } from '../mutation-keys';
import type { DeriveSelect, DeriveWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DeriveSelect, DeriveWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Derive with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDeriveMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteDeriveMutation<S extends DeriveSelect>(
  params: {
    selection: {
      fields: S & DeriveSelect;
    } & HookStrictSelect<NoInfer<S>, DeriveSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteDerive: {
          derive: InferSelectResult<DeriveWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteDerive: {
      derive: InferSelectResult<DeriveWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteDeriveMutation(
  params: {
    selection: SelectionConfig<DeriveSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DeriveSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: deriveMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .derive.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: deriveKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: deriveKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
