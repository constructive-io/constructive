/**
 * Update mutation hook for Table
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { tableKeys } from '../query-keys';
import { tableMutationKeys } from '../mutation-keys';
import type { TableSelect, TableWithRelations, TablePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TableSelect, TableWithRelations, TablePatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Table
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateTableMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', tablePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateTableMutation<S extends TableSelect>(
  params: {
    selection: {
      fields: S & TableSelect;
    } & HookStrictSelect<NoInfer<S>, TableSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateTable: {
          table: InferSelectResult<TableWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        tablePatch: TablePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateTable: {
      table: InferSelectResult<TableWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    tablePatch: TablePatch;
  }
>;
export function useUpdateTableMutation(
  params: {
    selection: SelectionConfig<TableSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        tablePatch: TablePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<TableSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: tableMutationKeys.all,
    mutationFn: ({ id, tablePatch }: { id: string; tablePatch: TablePatch }) =>
      getClient()
        .table.update({
          where: {
            id,
          },
          data: tablePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: tableKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: tableKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
