/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteWebConfigKeys } from '../query-keys';
import { siteWebConfigMutationKeys } from '../mutation-keys';
import type { SiteWebConfigSelect, SiteWebConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteWebConfigSelect, SiteWebConfigWithRelations } from '../../orm/input-types';
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteWebConfigMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteSiteWebConfigMutation<S extends SiteWebConfigSelect>(
  params: {
    selection: {
      fields: S & SiteWebConfigSelect;
    } & HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteSiteWebConfig: {
          siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteSiteWebConfig: {
      siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteSiteWebConfigMutation(
  params: {
    selection: SelectionConfig<SiteWebConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteWebConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteWebConfigMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .siteWebConfig.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: siteWebConfigKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteWebConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
