/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailSiteIdentityKeys } from '../query-keys';
import { emailSiteIdentityMutationKeys } from '../mutation-keys';
import type {
  EmailSiteIdentitySelect,
  EmailSiteIdentityWithRelations,
  EmailSiteIdentityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailSiteIdentitySelect,
  EmailSiteIdentityWithRelations,
  EmailSiteIdentityPatch,
} from '../../orm/input-types';
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailSiteIdentityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', emailSiteIdentityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateEmailSiteIdentityMutation<S extends EmailSiteIdentitySelect>(
  params: {
    selection: {
      fields: S & EmailSiteIdentitySelect;
    } & HookStrictSelect<NoInfer<S>, EmailSiteIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateEmailSiteIdentity: {
          emailSiteIdentity: InferSelectResult<EmailSiteIdentityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        emailSiteIdentityPatch: EmailSiteIdentityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateEmailSiteIdentity: {
      emailSiteIdentity: InferSelectResult<EmailSiteIdentityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    emailSiteIdentityPatch: EmailSiteIdentityPatch;
  }
>;
export function useUpdateEmailSiteIdentityMutation(
  params: {
    selection: SelectionConfig<EmailSiteIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        emailSiteIdentityPatch: EmailSiteIdentityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailSiteIdentitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailSiteIdentityMutationKeys.all,
    mutationFn: ({
      id,
      emailSiteIdentityPatch,
    }: {
      id: string;
      emailSiteIdentityPatch: EmailSiteIdentityPatch;
    }) =>
      getClient()
        .emailSiteIdentity.update({
          where: {
            id,
          },
          data: emailSiteIdentityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: emailSiteIdentityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: emailSiteIdentityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
