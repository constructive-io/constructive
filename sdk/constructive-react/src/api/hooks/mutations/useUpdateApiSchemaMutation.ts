/**
 * Join table linking APIs to the database schemas they expose; controls which schemas are accessible through each API
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { apiSchemaKeys } from '../query-keys';
import { apiSchemaMutationKeys } from '../mutation-keys';
import type {
  ApiSchemaSelect,
  ApiSchemaWithRelations,
  ApiSchemaPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ApiSchemaSelect,
  ApiSchemaWithRelations,
  ApiSchemaPatch,
} from '../../orm/input-types';
/**
 * Join table linking APIs to the database schemas they expose; controls which schemas are accessible through each API
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateApiSchemaMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', apiSchemaPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateApiSchemaMutation<S extends ApiSchemaSelect>(
  params: {
    selection: {
      fields: S & ApiSchemaSelect;
    } & HookStrictSelect<NoInfer<S>, ApiSchemaSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateApiSchema: {
          apiSchema: InferSelectResult<ApiSchemaWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        apiSchemaPatch: ApiSchemaPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateApiSchema: {
      apiSchema: InferSelectResult<ApiSchemaWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    apiSchemaPatch: ApiSchemaPatch;
  }
>;
export function useUpdateApiSchemaMutation(
  params: {
    selection: SelectionConfig<ApiSchemaSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        apiSchemaPatch: ApiSchemaPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ApiSchemaSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: apiSchemaMutationKeys.all,
    mutationFn: ({ id, apiSchemaPatch }: { id: string; apiSchemaPatch: ApiSchemaPatch }) =>
      getClient()
        .apiSchema.update({
          where: {
            id,
          },
          data: apiSchemaPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: apiSchemaKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: apiSchemaKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
