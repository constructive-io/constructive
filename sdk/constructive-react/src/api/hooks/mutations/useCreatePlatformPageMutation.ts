/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformPageKeys } from '../query-keys';
import { platformPageMutationKeys } from '../mutation-keys';
import type {
  PlatformPageSelect,
  PlatformPageWithRelations,
  CreatePlatformPageInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformPageSelect,
  PlatformPageWithRelations,
  CreatePlatformPageInput,
} from '../../orm/input-types';
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlatformPageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlatformPageMutation<S extends PlatformPageSelect>(
  params: {
    selection: {
      fields: S & PlatformPageSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlatformPage: {
          platformPage: InferSelectResult<PlatformPageWithRelations, S>;
        };
      },
      Error,
      CreatePlatformPageInput['platformPage']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlatformPage: {
      platformPage: InferSelectResult<PlatformPageWithRelations, S>;
    };
  },
  Error,
  CreatePlatformPageInput['platformPage']
>;
export function useCreatePlatformPageMutation(
  params: {
    selection: SelectionConfig<PlatformPageSelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePlatformPageInput['platformPage']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PlatformPageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformPageMutationKeys.create(),
    mutationFn: (data: CreatePlatformPageInput['platformPage']) =>
      getClient()
        .platformPage.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: platformPageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
