/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailSiteIdentityKeys } from '../query-keys';
import { emailSiteIdentityMutationKeys } from '../mutation-keys';
import type {
  EmailSiteIdentitySelect,
  EmailSiteIdentityWithRelations,
  CreateEmailSiteIdentityInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailSiteIdentitySelect,
  EmailSiteIdentityWithRelations,
  CreateEmailSiteIdentityInput,
} from '../../orm/input-types';
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateEmailSiteIdentityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateEmailSiteIdentityMutation<S extends EmailSiteIdentitySelect>(
  params: {
    selection: {
      fields: S & EmailSiteIdentitySelect;
    } & HookStrictSelect<NoInfer<S>, EmailSiteIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createEmailSiteIdentity: {
          emailSiteIdentity: InferSelectResult<EmailSiteIdentityWithRelations, S>;
        };
      },
      Error,
      CreateEmailSiteIdentityInput['emailSiteIdentity']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createEmailSiteIdentity: {
      emailSiteIdentity: InferSelectResult<EmailSiteIdentityWithRelations, S>;
    };
  },
  Error,
  CreateEmailSiteIdentityInput['emailSiteIdentity']
>;
export function useCreateEmailSiteIdentityMutation(
  params: {
    selection: SelectionConfig<EmailSiteIdentitySelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateEmailSiteIdentityInput['emailSiteIdentity']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailSiteIdentitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailSiteIdentityMutationKeys.create(),
    mutationFn: (data: CreateEmailSiteIdentityInput['emailSiteIdentity']) =>
      getClient()
        .emailSiteIdentity.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: emailSiteIdentityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
