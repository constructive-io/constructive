/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { pageKeys } from '../query-keys';
import { pageMutationKeys } from '../mutation-keys';
import type { PageSelect, PageWithRelations, CreatePageInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PageSelect, PageWithRelations, CreatePageInput } from '../../orm/input-types';
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePageMutation<S extends PageSelect>(
  params: {
    selection: {
      fields: S & PageSelect;
    } & HookStrictSelect<NoInfer<S>, PageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPage: {
          page: InferSelectResult<PageWithRelations, S>;
        };
      },
      Error,
      CreatePageInput['page']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPage: {
      page: InferSelectResult<PageWithRelations, S>;
    };
  },
  Error,
  CreatePageInput['page']
>;
export function useCreatePageMutation(
  params: {
    selection: SelectionConfig<PageSelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePageInput['page']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: pageMutationKeys.create(),
    mutationFn: (data: CreatePageInput['page']) =>
      getClient()
        .page.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: pageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
