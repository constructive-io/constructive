/**
 * One row per outstanding/completed ownership-verification challenge for a managed_domain. Holds the PUBLIC challenge the user must publish (e.g. a DNS TXT record value) — this is a verification token, NOT a secret. Entity-owned via owner_id and read/written through the AuthzEntityMembership scoped-module security path gated on manage_domains.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { domainVerificationKeys } from '../query-keys';
import { domainVerificationMutationKeys } from '../mutation-keys';
import type {
  DomainVerificationSelect,
  DomainVerificationWithRelations,
  DomainVerificationPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DomainVerificationSelect,
  DomainVerificationWithRelations,
  DomainVerificationPatch,
} from '../../orm/input-types';
/**
 * One row per outstanding/completed ownership-verification challenge for a managed_domain. Holds the PUBLIC challenge the user must publish (e.g. a DNS TXT record value) — this is a verification token, NOT a secret. Entity-owned via owner_id and read/written through the AuthzEntityMembership scoped-module security path gated on manage_domains.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDomainVerificationMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', domainVerificationPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateDomainVerificationMutation<S extends DomainVerificationSelect>(
  params: {
    selection: {
      fields: S & DomainVerificationSelect;
    } & HookStrictSelect<NoInfer<S>, DomainVerificationSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateDomainVerification: {
          domainVerification: InferSelectResult<DomainVerificationWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        domainVerificationPatch: DomainVerificationPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateDomainVerification: {
      domainVerification: InferSelectResult<DomainVerificationWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    domainVerificationPatch: DomainVerificationPatch;
  }
>;
export function useUpdateDomainVerificationMutation(
  params: {
    selection: SelectionConfig<DomainVerificationSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        domainVerificationPatch: DomainVerificationPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DomainVerificationSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: domainVerificationMutationKeys.all,
    mutationFn: ({
      id,
      domainVerificationPatch,
    }: {
      id: string;
      domainVerificationPatch: DomainVerificationPatch;
    }) =>
      getClient()
        .domainVerification.update({
          where: {
            id,
          },
          data: domainVerificationPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: domainVerificationKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: domainVerificationKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
