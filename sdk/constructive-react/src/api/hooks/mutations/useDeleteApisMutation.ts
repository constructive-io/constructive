/**
 * API surfaces exposed by this scope; publication makes a surface bindable from other scopes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { apisKeys } from '../query-keys';
import { apisMutationKeys } from '../mutation-keys';
import type { ApisSelect, ApisWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ApisSelect, ApisWithRelations } from '../../orm/input-types';
/**
 * API surfaces exposed by this scope; publication makes a surface bindable from other scopes
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteApisMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteApisMutation<S extends ApisSelect>(
  params: {
    selection: {
      fields: S & ApisSelect;
    } & HookStrictSelect<NoInfer<S>, ApisSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteApis: {
          apis: InferSelectResult<ApisWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteApis: {
      apis: InferSelectResult<ApisWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteApisMutation(
  params: {
    selection: SelectionConfig<ApisSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ApisSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: apisMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .apis.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: apisKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: apisKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
