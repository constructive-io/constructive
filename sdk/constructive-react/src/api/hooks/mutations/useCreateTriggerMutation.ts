/**
 * Create mutation hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { triggerKeys } from '../query-keys';
import { triggerMutationKeys } from '../mutation-keys';
import type {
  TriggerSelect,
  TriggerWithRelations,
  CreateTriggerInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  TriggerSelect,
  TriggerWithRelations,
  CreateTriggerInput,
} from '../../orm/input-types';
/**
 * Mutation hook for creating a Trigger
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateTriggerMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateTriggerMutation<S extends TriggerSelect>(
  params: {
    selection: {
      fields: S & TriggerSelect;
    } & HookStrictSelect<NoInfer<S>, TriggerSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createTrigger: {
          trigger: InferSelectResult<TriggerWithRelations, S>;
        };
      },
      Error,
      CreateTriggerInput['trigger']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createTrigger: {
      trigger: InferSelectResult<TriggerWithRelations, S>;
    };
  },
  Error,
  CreateTriggerInput['trigger']
>;
export function useCreateTriggerMutation(
  params: {
    selection: SelectionConfig<TriggerSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateTriggerInput['trigger']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<TriggerSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: triggerMutationKeys.create(),
    mutationFn: (data: CreateTriggerInput['trigger']) =>
      getClient()
        .trigger.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: triggerKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
