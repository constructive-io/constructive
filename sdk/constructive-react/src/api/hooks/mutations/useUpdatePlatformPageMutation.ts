/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformPageKeys } from '../query-keys';
import { platformPageMutationKeys } from '../mutation-keys';
import type {
  PlatformPageSelect,
  PlatformPageWithRelations,
  PlatformPagePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformPageSelect,
  PlatformPageWithRelations,
  PlatformPagePatch,
} from '../../orm/input-types';
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformPageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformPagePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformPageMutation<S extends PlatformPageSelect>(
  params: {
    selection: {
      fields: S & PlatformPageSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformPage: {
          platformPage: InferSelectResult<PlatformPageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformPagePatch: PlatformPagePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformPage: {
      platformPage: InferSelectResult<PlatformPageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformPagePatch: PlatformPagePatch;
  }
>;
export function useUpdatePlatformPageMutation(
  params: {
    selection: SelectionConfig<PlatformPageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformPagePatch: PlatformPagePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformPageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformPageMutationKeys.all,
    mutationFn: ({ id, platformPagePatch }: { id: string; platformPagePatch: PlatformPagePatch }) =>
      getClient()
        .platformPage.update({
          where: {
            id,
          },
          data: platformPagePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformPageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformPageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
