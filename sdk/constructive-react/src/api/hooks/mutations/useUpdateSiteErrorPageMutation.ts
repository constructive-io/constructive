/**
 * Custom error pages for a site surface, keyed by HTTP status code
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteErrorPageKeys } from '../query-keys';
import { siteErrorPageMutationKeys } from '../mutation-keys';
import type {
  SiteErrorPageSelect,
  SiteErrorPageWithRelations,
  SiteErrorPagePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteErrorPageSelect,
  SiteErrorPageWithRelations,
  SiteErrorPagePatch,
} from '../../orm/input-types';
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteErrorPageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteErrorPagePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteErrorPageMutation<S extends SiteErrorPageSelect>(
  params: {
    selection: {
      fields: S & SiteErrorPageSelect;
    } & HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteErrorPage: {
          siteErrorPage: InferSelectResult<SiteErrorPageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteErrorPagePatch: SiteErrorPagePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteErrorPage: {
      siteErrorPage: InferSelectResult<SiteErrorPageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteErrorPagePatch: SiteErrorPagePatch;
  }
>;
export function useUpdateSiteErrorPageMutation(
  params: {
    selection: SelectionConfig<SiteErrorPageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteErrorPagePatch: SiteErrorPagePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteErrorPageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteErrorPageMutationKeys.all,
    mutationFn: ({
      id,
      siteErrorPagePatch,
    }: {
      id: string;
      siteErrorPagePatch: SiteErrorPagePatch;
    }) =>
      getClient()
        .siteErrorPage.update({
          where: {
            id,
          },
          data: siteErrorPagePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteErrorPageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteErrorPageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
