/**
 * Create mutation hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { policyKeys } from '../query-keys';
import { policyMutationKeys } from '../mutation-keys';
import type { PolicySelect, PolicyWithRelations, CreatePolicyInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PolicySelect, PolicyWithRelations, CreatePolicyInput } from '../../orm/input-types';
/**
 * Mutation hook for creating a Policy
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePolicyMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePolicyMutation<S extends PolicySelect>(
  params: {
    selection: {
      fields: S & PolicySelect;
    } & HookStrictSelect<NoInfer<S>, PolicySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPolicy: {
          policy: InferSelectResult<PolicyWithRelations, S>;
        };
      },
      Error,
      CreatePolicyInput['policy']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPolicy: {
      policy: InferSelectResult<PolicyWithRelations, S>;
    };
  },
  Error,
  CreatePolicyInput['policy']
>;
export function useCreatePolicyMutation(
  params: {
    selection: SelectionConfig<PolicySelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePolicyInput['policy']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PolicySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: policyMutationKeys.create(),
    mutationFn: (data: CreatePolicyInput['policy']) =>
      getClient()
        .policy.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: policyKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
