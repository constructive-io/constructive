/**
 * Append-only audit trail of the domain verify->DNS->issue lifecycle, mirroring resource_events / cluster_events. One row per state transition emitted by the domain:* functions.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { domainEventKeys } from '../query-keys';
import { domainEventMutationKeys } from '../mutation-keys';
import type {
  DomainEventSelect,
  DomainEventWithRelations,
  DomainEventPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DomainEventSelect,
  DomainEventWithRelations,
  DomainEventPatch,
} from '../../orm/input-types';
/**
 * Append-only audit trail of the domain verify->DNS->issue lifecycle, mirroring resource_events / cluster_events. One row per state transition emitted by the domain:* functions.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDomainEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', domainEventPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateDomainEventMutation<S extends DomainEventSelect>(
  params: {
    selection: {
      fields: S & DomainEventSelect;
    } & HookStrictSelect<NoInfer<S>, DomainEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateDomainEvent: {
          domainEvent: InferSelectResult<DomainEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        domainEventPatch: DomainEventPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateDomainEvent: {
      domainEvent: InferSelectResult<DomainEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    domainEventPatch: DomainEventPatch;
  }
>;
export function useUpdateDomainEventMutation(
  params: {
    selection: SelectionConfig<DomainEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        domainEventPatch: DomainEventPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DomainEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: domainEventMutationKeys.all,
    mutationFn: ({ id, domainEventPatch }: { id: string; domainEventPatch: DomainEventPatch }) =>
      getClient()
        .domainEvent.update({
          where: {
            id,
          },
          data: domainEventPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: domainEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: domainEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
