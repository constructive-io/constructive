/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformEmailProviderAccountKeys } from '../query-keys';
import { platformEmailProviderAccountMutationKeys } from '../mutation-keys';
import type {
  PlatformEmailProviderAccountSelect,
  PlatformEmailProviderAccountWithRelations,
  PlatformEmailProviderAccountPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformEmailProviderAccountSelect,
  PlatformEmailProviderAccountWithRelations,
  PlatformEmailProviderAccountPatch,
} from '../../orm/input-types';
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformEmailProviderAccountMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformEmailProviderAccountPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformEmailProviderAccountMutation<
  S extends PlatformEmailProviderAccountSelect,
>(
  params: {
    selection: {
      fields: S & PlatformEmailProviderAccountSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformEmailProviderAccountSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformEmailProviderAccount: {
          platformEmailProviderAccount: InferSelectResult<
            PlatformEmailProviderAccountWithRelations,
            S
          >;
        };
      },
      Error,
      {
        id: string;
        platformEmailProviderAccountPatch: PlatformEmailProviderAccountPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformEmailProviderAccount: {
      platformEmailProviderAccount: InferSelectResult<PlatformEmailProviderAccountWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformEmailProviderAccountPatch: PlatformEmailProviderAccountPatch;
  }
>;
export function useUpdatePlatformEmailProviderAccountMutation(
  params: {
    selection: SelectionConfig<PlatformEmailProviderAccountSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformEmailProviderAccountPatch: PlatformEmailProviderAccountPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformEmailProviderAccountSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformEmailProviderAccountMutationKeys.all,
    mutationFn: ({
      id,
      platformEmailProviderAccountPatch,
    }: {
      id: string;
      platformEmailProviderAccountPatch: PlatformEmailProviderAccountPatch;
    }) =>
      getClient()
        .platformEmailProviderAccount.update({
          where: {
            id,
          },
          data: platformEmailProviderAccountPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformEmailProviderAccountKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformEmailProviderAccountKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
