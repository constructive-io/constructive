/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appComponentKeys } from '../query-keys';
import { appComponentMutationKeys } from '../mutation-keys';
import type {
  AppComponentSelect,
  AppComponentWithRelations,
  AppComponentPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppComponentSelect,
  AppComponentWithRelations,
  AppComponentPatch,
} from '../../orm/input-types';
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppComponentMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appComponentPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppComponentMutation<S extends AppComponentSelect>(
  params: {
    selection: {
      fields: S & AppComponentSelect;
    } & HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppComponent: {
          appComponent: InferSelectResult<AppComponentWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appComponentPatch: AppComponentPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppComponent: {
      appComponent: InferSelectResult<AppComponentWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appComponentPatch: AppComponentPatch;
  }
>;
export function useUpdateAppComponentMutation(
  params: {
    selection: SelectionConfig<AppComponentSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appComponentPatch: AppComponentPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppComponentSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appComponentMutationKeys.all,
    mutationFn: ({ id, appComponentPatch }: { id: string; appComponentPatch: AppComponentPatch }) =>
      getClient()
        .appComponent.update({
          where: {
            id,
          },
          data: appComponentPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appComponentKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appComponentKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
