/**
 * API surfaces exposed by this scope; publication makes a surface bindable from other scopes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { apisKeys } from '../query-keys';
import { apisMutationKeys } from '../mutation-keys';
import type { ApisSelect, ApisWithRelations, ApisPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ApisSelect, ApisWithRelations, ApisPatch } from '../../orm/input-types';
/**
 * API surfaces exposed by this scope; publication makes a surface bindable from other scopes
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateApisMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', apisPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateApisMutation<S extends ApisSelect>(
  params: {
    selection: {
      fields: S & ApisSelect;
    } & HookStrictSelect<NoInfer<S>, ApisSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateApis: {
          apis: InferSelectResult<ApisWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        apisPatch: ApisPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateApis: {
      apis: InferSelectResult<ApisWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    apisPatch: ApisPatch;
  }
>;
export function useUpdateApisMutation(
  params: {
    selection: SelectionConfig<ApisSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        apisPatch: ApisPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ApisSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: apisMutationKeys.all,
    mutationFn: ({ id, apisPatch }: { id: string; apisPatch: ApisPatch }) =>
      getClient()
        .apis.update({
          where: {
            id,
          },
          data: apisPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: apisKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: apisKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
