/**
 * Delete mutation hook for View
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { viewKeys } from '../query-keys';
import { viewMutationKeys } from '../mutation-keys';
import type { ViewSelect, ViewWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ViewSelect, ViewWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a View with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteViewMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteViewMutation<S extends ViewSelect>(
  params: {
    selection: {
      fields: S & ViewSelect;
    } & HookStrictSelect<NoInfer<S>, ViewSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteView: {
          view: InferSelectResult<ViewWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteView: {
      view: InferSelectResult<ViewWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteViewMutation(
  params: {
    selection: SelectionConfig<ViewSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ViewSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: viewMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .view.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: viewKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: viewKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
