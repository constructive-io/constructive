/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { pageKeys } from '../query-keys';
import { pageMutationKeys } from '../mutation-keys';
import type { PageSelect, PageWithRelations, PagePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PageSelect, PageWithRelations, PagePatch } from '../../orm/input-types';
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', pagePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePageMutation<S extends PageSelect>(
  params: {
    selection: {
      fields: S & PageSelect;
    } & HookStrictSelect<NoInfer<S>, PageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePage: {
          page: InferSelectResult<PageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        pagePatch: PagePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePage: {
      page: InferSelectResult<PageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    pagePatch: PagePatch;
  }
>;
export function useUpdatePageMutation(
  params: {
    selection: SelectionConfig<PageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        pagePatch: PagePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: pageMutationKeys.all,
    mutationFn: ({ id, pagePatch }: { id: string; pagePatch: PagePatch }) =>
      getClient()
        .page.update({
          where: {
            id,
          },
          data: pagePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: pageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: pageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
