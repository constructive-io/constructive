/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { httpRouteKeys } from '../query-keys';
import { httpRouteMutationKeys } from '../mutation-keys';
import type { HttpRouteSelect, HttpRouteWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { HttpRouteSelect, HttpRouteWithRelations } from '../../orm/input-types';
/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteHttpRouteMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteHttpRouteMutation<S extends HttpRouteSelect>(
  params: {
    selection: {
      fields: S & HttpRouteSelect;
    } & HookStrictSelect<NoInfer<S>, HttpRouteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteHttpRoute: {
          httpRoute: InferSelectResult<HttpRouteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteHttpRoute: {
      httpRoute: InferSelectResult<HttpRouteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteHttpRouteMutation(
  params: {
    selection: SelectionConfig<HttpRouteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<HttpRouteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: httpRouteMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .httpRoute.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: httpRouteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: httpRouteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
