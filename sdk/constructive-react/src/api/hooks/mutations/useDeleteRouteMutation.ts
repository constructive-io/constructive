/**
 * Routes binding a domain hostname and path to a typed catalog target
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from "@tanstack/react-query";
import type { UseMutationOptions, UseMutationResult } from "@tanstack/react-query";
import { getClient } from "../client";
import { buildSelectionArgs } from "../selection";
import type { SelectionConfig } from "../selection";
import { routeKeys } from "../query-keys";
import { routeMutationKeys } from "../mutation-keys";
import type { RouteSelect, RouteWithRelations } from "../../orm/input-types";
import type { InferSelectResult, HookStrictSelect } from "../../orm/select-types";
export type { RouteSelect, RouteWithRelations } from "../../orm/input-types";
/**
 * Routes binding a domain hostname and path to a typed catalog target
 * 
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRouteMutation({
 *   selection: { fields: { id: true } },
 * });
 * 
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRouteMutation<S extends RouteSelect>(params: {
  selection: ({
    fields: S & RouteSelect;
  } & HookStrictSelect<NoInfer<S>, RouteSelect>);
} & Omit<UseMutationOptions<{
  deleteRoute: {
    route: InferSelectResult<RouteWithRelations, S>;
  };
}, Error, {
  id: string;
}>, "mutationFn">): UseMutationResult<{
  deleteRoute: {
    route: InferSelectResult<RouteWithRelations, S>;
  };
}, Error, {
  id: string;
}>;
export function useDeleteRouteMutation(params: {
  selection: SelectionConfig<RouteSelect>;
} & Omit<UseMutationOptions<any, Error, {
  id: string;
}>, "mutationFn">) {
  const args = buildSelectionArgs<RouteSelect>(params.selection);
  const {
    selection: _selection,
    ...mutationOptions
  } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: routeMutationKeys.all,
    mutationFn: ({
      id
    }: {
      id: string;
    }) => getClient().route.delete({
      where: {
        id
      },
      select: args.select
    }).unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: routeKeys.detail(variables.id)
      });
      queryClient.invalidateQueries({
        queryKey: routeKeys.lists()
      });
    },
    ...mutationOptions
  });
}
