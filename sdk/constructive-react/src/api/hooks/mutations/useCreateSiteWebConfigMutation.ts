/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteWebConfigKeys } from '../query-keys';
import { siteWebConfigMutationKeys } from '../mutation-keys';
import type {
  SiteWebConfigSelect,
  SiteWebConfigWithRelations,
  CreateSiteWebConfigInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteWebConfigSelect,
  SiteWebConfigWithRelations,
  CreateSiteWebConfigInput,
} from '../../orm/input-types';
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteWebConfigMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateSiteWebConfigMutation<S extends SiteWebConfigSelect>(
  params: {
    selection: {
      fields: S & SiteWebConfigSelect;
    } & HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createSiteWebConfig: {
          siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S>;
        };
      },
      Error,
      CreateSiteWebConfigInput['siteWebConfig']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createSiteWebConfig: {
      siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S>;
    };
  },
  Error,
  CreateSiteWebConfigInput['siteWebConfig']
>;
export function useCreateSiteWebConfigMutation(
  params: {
    selection: SelectionConfig<SiteWebConfigSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateSiteWebConfigInput['siteWebConfig']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<SiteWebConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteWebConfigMutationKeys.create(),
    mutationFn: (data: CreateSiteWebConfigInput['siteWebConfig']) =>
      getClient()
        .siteWebConfig.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: siteWebConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
