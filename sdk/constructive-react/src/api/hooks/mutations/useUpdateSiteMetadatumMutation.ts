/**
 * SEO and social sharing metadata for a site: page title, description, and Open Graph image
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteMetadatumKeys } from '../query-keys';
import { siteMetadatumMutationKeys } from '../mutation-keys';
import type {
  SiteMetadatumSelect,
  SiteMetadatumWithRelations,
  SiteMetadatumPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteMetadatumSelect,
  SiteMetadatumWithRelations,
  SiteMetadatumPatch,
} from '../../orm/input-types';
/**
 * SEO and social sharing metadata for a site: page title, description, and Open Graph image
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteMetadatumMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteMetadatumPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteMetadatumMutation<S extends SiteMetadatumSelect>(
  params: {
    selection: {
      fields: S & SiteMetadatumSelect;
    } & HookStrictSelect<NoInfer<S>, SiteMetadatumSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteMetadatum: {
          siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteMetadatumPatch: SiteMetadatumPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteMetadatum: {
      siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteMetadatumPatch: SiteMetadatumPatch;
  }
>;
export function useUpdateSiteMetadatumMutation(
  params: {
    selection: SelectionConfig<SiteMetadatumSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteMetadatumPatch: SiteMetadatumPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteMetadatumSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteMetadatumMutationKeys.all,
    mutationFn: ({
      id,
      siteMetadatumPatch,
    }: {
      id: string;
      siteMetadatumPatch: SiteMetadatumPatch;
    }) =>
      getClient()
        .siteMetadatum.update({
          where: {
            id,
          },
          data: siteMetadatumPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteMetadatumKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteMetadatumKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
