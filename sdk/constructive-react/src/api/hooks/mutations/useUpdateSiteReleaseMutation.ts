/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteReleaseKeys } from '../query-keys';
import { siteReleaseMutationKeys } from '../mutation-keys';
import type {
  SiteReleaseSelect,
  SiteReleaseWithRelations,
  SiteReleasePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteReleaseSelect,
  SiteReleaseWithRelations,
  SiteReleasePatch,
} from '../../orm/input-types';
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteReleaseMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteReleasePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteReleaseMutation<S extends SiteReleaseSelect>(
  params: {
    selection: {
      fields: S & SiteReleaseSelect;
    } & HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteRelease: {
          siteRelease: InferSelectResult<SiteReleaseWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteReleasePatch: SiteReleasePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteRelease: {
      siteRelease: InferSelectResult<SiteReleaseWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteReleasePatch: SiteReleasePatch;
  }
>;
export function useUpdateSiteReleaseMutation(
  params: {
    selection: SelectionConfig<SiteReleaseSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteReleasePatch: SiteReleasePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteReleaseSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteReleaseMutationKeys.all,
    mutationFn: ({ id, siteReleasePatch }: { id: string; siteReleasePatch: SiteReleasePatch }) =>
      getClient()
        .siteRelease.update({
          where: {
            id,
          },
          data: siteReleasePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteReleaseKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteReleaseKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
