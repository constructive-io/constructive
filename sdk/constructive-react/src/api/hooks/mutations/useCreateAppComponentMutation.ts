/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appComponentKeys } from '../query-keys';
import { appComponentMutationKeys } from '../mutation-keys';
import type {
  AppComponentSelect,
  AppComponentWithRelations,
  CreateAppComponentInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppComponentSelect,
  AppComponentWithRelations,
  CreateAppComponentInput,
} from '../../orm/input-types';
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppComponentMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppComponentMutation<S extends AppComponentSelect>(
  params: {
    selection: {
      fields: S & AppComponentSelect;
    } & HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAppComponent: {
          appComponent: InferSelectResult<AppComponentWithRelations, S>;
        };
      },
      Error,
      CreateAppComponentInput['appComponent']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAppComponent: {
      appComponent: InferSelectResult<AppComponentWithRelations, S>;
    };
  },
  Error,
  CreateAppComponentInput['appComponent']
>;
export function useCreateAppComponentMutation(
  params: {
    selection: SelectionConfig<AppComponentSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppComponentInput['appComponent']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppComponentSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appComponentMutationKeys.create(),
    mutationFn: (data: CreateAppComponentInput['appComponent']) =>
      getClient()
        .appComponent.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appComponentKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
