/**
 * Update mutation hook for ViewBehavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { viewBehaviorKeys } from '../query-keys';
import { viewBehaviorMutationKeys } from '../mutation-keys';
import type {
  ViewBehaviorSelect,
  ViewBehaviorWithRelations,
  ViewBehaviorPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ViewBehaviorSelect,
  ViewBehaviorWithRelations,
  ViewBehaviorPatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a ViewBehavior
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateViewBehaviorMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', viewBehaviorPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateViewBehaviorMutation<S extends ViewBehaviorSelect>(
  params: {
    selection: {
      fields: S & ViewBehaviorSelect;
    } & HookStrictSelect<NoInfer<S>, ViewBehaviorSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateViewBehavior: {
          viewBehavior: InferSelectResult<ViewBehaviorWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        viewBehaviorPatch: ViewBehaviorPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateViewBehavior: {
      viewBehavior: InferSelectResult<ViewBehaviorWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    viewBehaviorPatch: ViewBehaviorPatch;
  }
>;
export function useUpdateViewBehaviorMutation(
  params: {
    selection: SelectionConfig<ViewBehaviorSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        viewBehaviorPatch: ViewBehaviorPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ViewBehaviorSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: viewBehaviorMutationKeys.all,
    mutationFn: ({ id, viewBehaviorPatch }: { id: string; viewBehaviorPatch: ViewBehaviorPatch }) =>
      getClient()
        .viewBehavior.update({
          where: {
            id,
          },
          data: viewBehaviorPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: viewBehaviorKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: viewBehaviorKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
