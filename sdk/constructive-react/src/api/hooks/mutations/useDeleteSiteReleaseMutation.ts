/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteReleaseKeys } from '../query-keys';
import { siteReleaseMutationKeys } from '../mutation-keys';
import type { SiteReleaseSelect, SiteReleaseWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteReleaseSelect, SiteReleaseWithRelations } from '../../orm/input-types';
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteReleaseMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteSiteReleaseMutation<S extends SiteReleaseSelect>(
  params: {
    selection: {
      fields: S & SiteReleaseSelect;
    } & HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteSiteRelease: {
          siteRelease: InferSelectResult<SiteReleaseWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteSiteRelease: {
      siteRelease: InferSelectResult<SiteReleaseWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteSiteReleaseMutation(
  params: {
    selection: SelectionConfig<SiteReleaseSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteReleaseSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteReleaseMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .siteRelease.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: siteReleaseKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteReleaseKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
