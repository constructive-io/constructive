/**
 * Update mutation hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { triggerKeys } from '../query-keys';
import { triggerMutationKeys } from '../mutation-keys';
import type { TriggerSelect, TriggerWithRelations, TriggerPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TriggerSelect, TriggerWithRelations, TriggerPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Trigger
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateTriggerMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', triggerPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateTriggerMutation<S extends TriggerSelect>(
  params: {
    selection: {
      fields: S & TriggerSelect;
    } & HookStrictSelect<NoInfer<S>, TriggerSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateTrigger: {
          trigger: InferSelectResult<TriggerWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        triggerPatch: TriggerPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateTrigger: {
      trigger: InferSelectResult<TriggerWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    triggerPatch: TriggerPatch;
  }
>;
export function useUpdateTriggerMutation(
  params: {
    selection: SelectionConfig<TriggerSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        triggerPatch: TriggerPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<TriggerSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: triggerMutationKeys.all,
    mutationFn: ({ id, triggerPatch }: { id: string; triggerPatch: TriggerPatch }) =>
      getClient()
        .trigger.update({
          where: {
            id,
          },
          data: triggerPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: triggerKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: triggerKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
