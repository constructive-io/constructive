/**
 * Update mutation hook for DefaultPrivilege
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { defaultPrivilegeKeys } from '../query-keys';
import { defaultPrivilegeMutationKeys } from '../mutation-keys';
import type {
  DefaultPrivilegeSelect,
  DefaultPrivilegeWithRelations,
  DefaultPrivilegePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DefaultPrivilegeSelect,
  DefaultPrivilegeWithRelations,
  DefaultPrivilegePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a DefaultPrivilege
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDefaultPrivilegeMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', defaultPrivilegePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateDefaultPrivilegeMutation<S extends DefaultPrivilegeSelect>(
  params: {
    selection: {
      fields: S & DefaultPrivilegeSelect;
    } & HookStrictSelect<NoInfer<S>, DefaultPrivilegeSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateDefaultPrivilege: {
          defaultPrivilege: InferSelectResult<DefaultPrivilegeWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        defaultPrivilegePatch: DefaultPrivilegePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateDefaultPrivilege: {
      defaultPrivilege: InferSelectResult<DefaultPrivilegeWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    defaultPrivilegePatch: DefaultPrivilegePatch;
  }
>;
export function useUpdateDefaultPrivilegeMutation(
  params: {
    selection: SelectionConfig<DefaultPrivilegeSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        defaultPrivilegePatch: DefaultPrivilegePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DefaultPrivilegeSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: defaultPrivilegeMutationKeys.all,
    mutationFn: ({
      id,
      defaultPrivilegePatch,
    }: {
      id: string;
      defaultPrivilegePatch: DefaultPrivilegePatch;
    }) =>
      getClient()
        .defaultPrivilege.update({
          where: {
            id,
          },
          data: defaultPrivilegePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: defaultPrivilegeKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: defaultPrivilegeKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
