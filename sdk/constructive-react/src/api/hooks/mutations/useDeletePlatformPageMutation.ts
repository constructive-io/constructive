/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformPageKeys } from '../query-keys';
import { platformPageMutationKeys } from '../mutation-keys';
import type { PlatformPageSelect, PlatformPageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformPageSelect, PlatformPageWithRelations } from '../../orm/input-types';
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformPageMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformPageMutation<S extends PlatformPageSelect>(
  params: {
    selection: {
      fields: S & PlatformPageSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformPage: {
          platformPage: InferSelectResult<PlatformPageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformPage: {
      platformPage: InferSelectResult<PlatformPageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformPageMutation(
  params: {
    selection: SelectionConfig<PlatformPageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformPageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformPageMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformPage.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformPageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformPageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
