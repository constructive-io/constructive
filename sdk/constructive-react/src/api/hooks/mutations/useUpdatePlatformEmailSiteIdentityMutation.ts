/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformEmailSiteIdentityKeys } from '../query-keys';
import { platformEmailSiteIdentityMutationKeys } from '../mutation-keys';
import type {
  PlatformEmailSiteIdentitySelect,
  PlatformEmailSiteIdentityWithRelations,
  PlatformEmailSiteIdentityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformEmailSiteIdentitySelect,
  PlatformEmailSiteIdentityWithRelations,
  PlatformEmailSiteIdentityPatch,
} from '../../orm/input-types';
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformEmailSiteIdentityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformEmailSiteIdentityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformEmailSiteIdentityMutation<
  S extends PlatformEmailSiteIdentitySelect,
>(
  params: {
    selection: {
      fields: S & PlatformEmailSiteIdentitySelect;
    } & HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformEmailSiteIdentity: {
          platformEmailSiteIdentity: InferSelectResult<PlatformEmailSiteIdentityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformEmailSiteIdentityPatch: PlatformEmailSiteIdentityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformEmailSiteIdentity: {
      platformEmailSiteIdentity: InferSelectResult<PlatformEmailSiteIdentityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformEmailSiteIdentityPatch: PlatformEmailSiteIdentityPatch;
  }
>;
export function useUpdatePlatformEmailSiteIdentityMutation(
  params: {
    selection: SelectionConfig<PlatformEmailSiteIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformEmailSiteIdentityPatch: PlatformEmailSiteIdentityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformEmailSiteIdentitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformEmailSiteIdentityMutationKeys.all,
    mutationFn: ({
      id,
      platformEmailSiteIdentityPatch,
    }: {
      id: string;
      platformEmailSiteIdentityPatch: PlatformEmailSiteIdentityPatch;
    }) =>
      getClient()
        .platformEmailSiteIdentity.update({
          where: {
            id,
          },
          data: platformEmailSiteIdentityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformEmailSiteIdentityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformEmailSiteIdentityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
