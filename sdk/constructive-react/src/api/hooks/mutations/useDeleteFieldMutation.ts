/**
 * Delete mutation hook for Field
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { fieldKeys } from '../query-keys';
import { fieldMutationKeys } from '../mutation-keys';
import type { FieldSelect, FieldWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FieldSelect, FieldWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Field with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFieldMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteFieldMutation<S extends FieldSelect>(
  params: {
    selection: {
      fields: S & FieldSelect;
    } & HookStrictSelect<NoInfer<S>, FieldSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteField: {
          field: InferSelectResult<FieldWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteField: {
      field: InferSelectResult<FieldWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteFieldMutation(
  params: {
    selection: SelectionConfig<FieldSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<FieldSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: fieldMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .field.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: fieldKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: fieldKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
