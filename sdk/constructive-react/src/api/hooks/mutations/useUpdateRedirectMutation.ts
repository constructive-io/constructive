/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { redirectKeys } from '../query-keys';
import { redirectMutationKeys } from '../mutation-keys';
import type { RedirectSelect, RedirectWithRelations, RedirectPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RedirectSelect, RedirectWithRelations, RedirectPatch } from '../../orm/input-types';
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRedirectMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', redirectPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRedirectMutation<S extends RedirectSelect>(
  params: {
    selection: {
      fields: S & RedirectSelect;
    } & HookStrictSelect<NoInfer<S>, RedirectSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRedirect: {
          redirect: InferSelectResult<RedirectWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        redirectPatch: RedirectPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRedirect: {
      redirect: InferSelectResult<RedirectWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    redirectPatch: RedirectPatch;
  }
>;
export function useUpdateRedirectMutation(
  params: {
    selection: SelectionConfig<RedirectSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        redirectPatch: RedirectPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RedirectSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: redirectMutationKeys.all,
    mutationFn: ({ id, redirectPatch }: { id: string; redirectPatch: RedirectPatch }) =>
      getClient()
        .redirect.update({
          where: {
            id,
          },
          data: redirectPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: redirectKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: redirectKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
