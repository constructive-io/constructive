/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { pageKeys } from '../query-keys';
import { pageMutationKeys } from '../mutation-keys';
import type { PageSelect, PageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PageSelect, PageWithRelations } from '../../orm/input-types';
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePageMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePageMutation<S extends PageSelect>(
  params: {
    selection: {
      fields: S & PageSelect;
    } & HookStrictSelect<NoInfer<S>, PageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePage: {
          page: InferSelectResult<PageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePage: {
      page: InferSelectResult<PageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePageMutation(
  params: {
    selection: SelectionConfig<PageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: pageMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .page.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: pageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: pageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
