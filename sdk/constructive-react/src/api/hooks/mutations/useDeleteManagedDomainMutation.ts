/**
 * One row per cert-bearing host or wildcard; tracks domain verification and TLS provisioning independently of services_public.domains. Reconcilers match a route's root domain to a row here by string (no FK/coupling in v1)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { managedDomainKeys } from '../query-keys';
import { managedDomainMutationKeys } from '../mutation-keys';
import type { ManagedDomainSelect, ManagedDomainWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ManagedDomainSelect, ManagedDomainWithRelations } from '../../orm/input-types';
/**
 * One row per cert-bearing host or wildcard; tracks domain verification and TLS provisioning independently of services_public.domains. Reconcilers match a route's root domain to a row here by string (no FK/coupling in v1)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteManagedDomainMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteManagedDomainMutation<S extends ManagedDomainSelect>(
  params: {
    selection: {
      fields: S & ManagedDomainSelect;
    } & HookStrictSelect<NoInfer<S>, ManagedDomainSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteManagedDomain: {
          managedDomain: InferSelectResult<ManagedDomainWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteManagedDomain: {
      managedDomain: InferSelectResult<ManagedDomainWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteManagedDomainMutation(
  params: {
    selection: SelectionConfig<ManagedDomainSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ManagedDomainSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: managedDomainMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .managedDomain.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: managedDomainKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: managedDomainKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
