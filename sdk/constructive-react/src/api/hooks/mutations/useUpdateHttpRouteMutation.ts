/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { httpRouteKeys } from '../query-keys';
import { httpRouteMutationKeys } from '../mutation-keys';
import type {
  HttpRouteSelect,
  HttpRouteWithRelations,
  HttpRoutePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  HttpRouteSelect,
  HttpRouteWithRelations,
  HttpRoutePatch,
} from '../../orm/input-types';
/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateHttpRouteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', httpRoutePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateHttpRouteMutation<S extends HttpRouteSelect>(
  params: {
    selection: {
      fields: S & HttpRouteSelect;
    } & HookStrictSelect<NoInfer<S>, HttpRouteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateHttpRoute: {
          httpRoute: InferSelectResult<HttpRouteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        httpRoutePatch: HttpRoutePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateHttpRoute: {
      httpRoute: InferSelectResult<HttpRouteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    httpRoutePatch: HttpRoutePatch;
  }
>;
export function useUpdateHttpRouteMutation(
  params: {
    selection: SelectionConfig<HttpRouteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        httpRoutePatch: HttpRoutePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<HttpRouteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: httpRouteMutationKeys.all,
    mutationFn: ({ id, httpRoutePatch }: { id: string; httpRoutePatch: HttpRoutePatch }) =>
      getClient()
        .httpRoute.update({
          where: {
            id,
          },
          data: httpRoutePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: httpRouteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: httpRouteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
