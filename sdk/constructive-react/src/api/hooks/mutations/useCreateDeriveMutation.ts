/**
 * Create mutation hook for Derive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { deriveKeys } from '../query-keys';
import { deriveMutationKeys } from '../mutation-keys';
import type { DeriveSelect, DeriveWithRelations, CreateDeriveInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DeriveSelect, DeriveWithRelations, CreateDeriveInput } from '../../orm/input-types';
/**
 * Mutation hook for creating a Derive
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDeriveMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateDeriveMutation<S extends DeriveSelect>(
  params: {
    selection: {
      fields: S & DeriveSelect;
    } & HookStrictSelect<NoInfer<S>, DeriveSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createDerive: {
          derive: InferSelectResult<DeriveWithRelations, S>;
        };
      },
      Error,
      CreateDeriveInput['derive']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createDerive: {
      derive: InferSelectResult<DeriveWithRelations, S>;
    };
  },
  Error,
  CreateDeriveInput['derive']
>;
export function useCreateDeriveMutation(
  params: {
    selection: SelectionConfig<DeriveSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateDeriveInput['derive']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<DeriveSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: deriveMutationKeys.create(),
    mutationFn: (data: CreateDeriveInput['derive']) =>
      getClient()
        .derive.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: deriveKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
