/**
 * Routes binding a domain hostname and path to a typed catalog target
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from "@tanstack/react-query";
import type { UseMutationOptions, UseMutationResult } from "@tanstack/react-query";
import { getClient } from "../client";
import { buildSelectionArgs } from "../selection";
import type { SelectionConfig } from "../selection";
import { routeKeys } from "../query-keys";
import { routeMutationKeys } from "../mutation-keys";
import type { RouteSelect, RouteWithRelations, RoutePatch } from "../../orm/input-types";
import type { InferSelectResult, HookStrictSelect } from "../../orm/select-types";
export type { RouteSelect, RouteWithRelations, RoutePatch } from "../../orm/input-types";
/**
 * Routes binding a domain hostname and path to a typed catalog target
 * 
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRouteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 * 
 * mutate({ id: 'value-here', routePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRouteMutation<S extends RouteSelect>(params: {
  selection: ({
    fields: S & RouteSelect;
  } & HookStrictSelect<NoInfer<S>, RouteSelect>);
} & Omit<UseMutationOptions<{
  updateRoute: {
    route: InferSelectResult<RouteWithRelations, S>;
  };
}, Error, {
  id: string;
  routePatch: RoutePatch;
}>, "mutationFn">): UseMutationResult<{
  updateRoute: {
    route: InferSelectResult<RouteWithRelations, S>;
  };
}, Error, {
  id: string;
  routePatch: RoutePatch;
}>;
export function useUpdateRouteMutation(params: {
  selection: SelectionConfig<RouteSelect>;
} & Omit<UseMutationOptions<any, Error, {
  id: string;
  routePatch: RoutePatch;
}>, "mutationFn">) {
  const args = buildSelectionArgs<RouteSelect>(params.selection);
  const {
    selection: _selection,
    ...mutationOptions
  } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: routeMutationKeys.all,
    mutationFn: ({
      id,
      routePatch
    }: {
      id: string;
      routePatch: RoutePatch;
    }) => getClient().route.update({
      where: {
        id
      },
      data: routePatch,
      select: args.select
    }).unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: routeKeys.detail(variables.id)
      });
      queryClient.invalidateQueries({
        queryKey: routeKeys.lists()
      });
    },
    ...mutationOptions
  });
}
