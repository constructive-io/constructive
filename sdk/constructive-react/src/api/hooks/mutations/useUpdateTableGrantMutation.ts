/**
 * Update mutation hook for TableGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { tableGrantKeys } from '../query-keys';
import { tableGrantMutationKeys } from '../mutation-keys';
import type {
  TableGrantSelect,
  TableGrantWithRelations,
  TableGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  TableGrantSelect,
  TableGrantWithRelations,
  TableGrantPatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a TableGrant
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateTableGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', tableGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateTableGrantMutation<S extends TableGrantSelect>(
  params: {
    selection: {
      fields: S & TableGrantSelect;
    } & HookStrictSelect<NoInfer<S>, TableGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateTableGrant: {
          tableGrant: InferSelectResult<TableGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        tableGrantPatch: TableGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateTableGrant: {
      tableGrant: InferSelectResult<TableGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    tableGrantPatch: TableGrantPatch;
  }
>;
export function useUpdateTableGrantMutation(
  params: {
    selection: SelectionConfig<TableGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        tableGrantPatch: TableGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<TableGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: tableGrantMutationKeys.all,
    mutationFn: ({ id, tableGrantPatch }: { id: string; tableGrantPatch: TableGrantPatch }) =>
      getClient()
        .tableGrant.update({
          where: {
            id,
          },
          data: tableGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: tableGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: tableGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
