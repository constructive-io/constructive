/**
 * Compiled route precedence index maintained by route sync triggers; carries typed target ids only, read through the resolver
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { routeBindingKeys } from '../query-keys';
import { routeBindingMutationKeys } from '../mutation-keys';
import type { RouteBindingSelect, RouteBindingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RouteBindingSelect, RouteBindingWithRelations } from '../../orm/input-types';
/**
 * Compiled route precedence index maintained by route sync triggers; carries typed target ids only, read through the resolver
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRouteBindingMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRouteBindingMutation<S extends RouteBindingSelect>(
  params: {
    selection: {
      fields: S & RouteBindingSelect;
    } & HookStrictSelect<NoInfer<S>, RouteBindingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRouteBinding: {
          routeBinding: InferSelectResult<RouteBindingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRouteBinding: {
      routeBinding: InferSelectResult<RouteBindingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRouteBindingMutation(
  params: {
    selection: SelectionConfig<RouteBindingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RouteBindingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: routeBindingMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .routeBinding.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: routeBindingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: routeBindingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
