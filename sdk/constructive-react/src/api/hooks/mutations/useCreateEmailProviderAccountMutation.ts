/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailProviderAccountKeys } from '../query-keys';
import { emailProviderAccountMutationKeys } from '../mutation-keys';
import type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
  CreateEmailProviderAccountInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
  CreateEmailProviderAccountInput,
} from '../../orm/input-types';
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateEmailProviderAccountMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateEmailProviderAccountMutation<S extends EmailProviderAccountSelect>(
  params: {
    selection: {
      fields: S & EmailProviderAccountSelect;
    } & HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createEmailProviderAccount: {
          emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S>;
        };
      },
      Error,
      CreateEmailProviderAccountInput['emailProviderAccount']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createEmailProviderAccount: {
      emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S>;
    };
  },
  Error,
  CreateEmailProviderAccountInput['emailProviderAccount']
>;
export function useCreateEmailProviderAccountMutation(
  params: {
    selection: SelectionConfig<EmailProviderAccountSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateEmailProviderAccountInput['emailProviderAccount']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailProviderAccountSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailProviderAccountMutationKeys.create(),
    mutationFn: (data: CreateEmailProviderAccountInput['emailProviderAccount']) =>
      getClient()
        .emailProviderAccount.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: emailProviderAccountKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
