/**
 * Update mutation hook for Partition
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { partitionKeys } from '../query-keys';
import { partitionMutationKeys } from '../mutation-keys';
import type {
  PartitionSelect,
  PartitionWithRelations,
  PartitionPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PartitionSelect,
  PartitionWithRelations,
  PartitionPatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a Partition
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePartitionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', partitionPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePartitionMutation<S extends PartitionSelect>(
  params: {
    selection: {
      fields: S & PartitionSelect;
    } & HookStrictSelect<NoInfer<S>, PartitionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePartition: {
          partition: InferSelectResult<PartitionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        partitionPatch: PartitionPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePartition: {
      partition: InferSelectResult<PartitionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    partitionPatch: PartitionPatch;
  }
>;
export function useUpdatePartitionMutation(
  params: {
    selection: SelectionConfig<PartitionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        partitionPatch: PartitionPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PartitionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: partitionMutationKeys.all,
    mutationFn: ({ id, partitionPatch }: { id: string; partitionPatch: PartitionPatch }) =>
      getClient()
        .partition.update({
          where: {
            id,
          },
          data: partitionPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: partitionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: partitionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
