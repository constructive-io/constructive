/**
 * Fully-qualified hostnames owned by this scope; each row claims its hostname globally through the catalog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformDomainKeys } from '../query-keys';
import { platformDomainMutationKeys } from '../mutation-keys';
import type { PlatformDomainSelect, PlatformDomainWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformDomainSelect, PlatformDomainWithRelations } from '../../orm/input-types';
/**
 * Fully-qualified hostnames owned by this scope; each row claims its hostname globally through the catalog
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformDomainMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformDomainMutation<S extends PlatformDomainSelect>(
  params: {
    selection: {
      fields: S & PlatformDomainSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformDomainSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformDomain: {
          platformDomain: InferSelectResult<PlatformDomainWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformDomain: {
      platformDomain: InferSelectResult<PlatformDomainWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformDomainMutation(
  params: {
    selection: SelectionConfig<PlatformDomainSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformDomainSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformDomainMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformDomain.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformDomainKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformDomainKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
