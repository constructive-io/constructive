/**
 * Top-level site configuration: branding assets, title, and description for a deployed application
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteKeys } from '../query-keys';
import { siteMutationKeys } from '../mutation-keys';
import type { SiteSelect, SiteWithRelations, SitePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteSelect, SiteWithRelations, SitePatch } from '../../orm/input-types';
/**
 * Top-level site configuration: branding assets, title, and description for a deployed application
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', sitePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteMutation<S extends SiteSelect>(
  params: {
    selection: {
      fields: S & SiteSelect;
    } & HookStrictSelect<NoInfer<S>, SiteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSite: {
          site: InferSelectResult<SiteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        sitePatch: SitePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSite: {
      site: InferSelectResult<SiteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    sitePatch: SitePatch;
  }
>;
export function useUpdateSiteMutation(
  params: {
    selection: SelectionConfig<SiteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        sitePatch: SitePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteMutationKeys.all,
    mutationFn: ({ id, sitePatch }: { id: string; sitePatch: SitePatch }) =>
      getClient()
        .site.update({
          where: {
            id,
          },
          data: sitePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
