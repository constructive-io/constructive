/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appComponentKeys } from '../query-keys';
import { appComponentMutationKeys } from '../mutation-keys';
import type { AppComponentSelect, AppComponentWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppComponentSelect, AppComponentWithRelations } from '../../orm/input-types';
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppComponentMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppComponentMutation<S extends AppComponentSelect>(
  params: {
    selection: {
      fields: S & AppComponentSelect;
    } & HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppComponent: {
          appComponent: InferSelectResult<AppComponentWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppComponent: {
      appComponent: InferSelectResult<AppComponentWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppComponentMutation(
  params: {
    selection: SelectionConfig<AppComponentSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppComponentSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appComponentMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appComponent.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appComponentKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appComponentKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
