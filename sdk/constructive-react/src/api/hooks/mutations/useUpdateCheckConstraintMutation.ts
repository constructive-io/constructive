/**
 * Update mutation hook for CheckConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { checkConstraintKeys } from '../query-keys';
import { checkConstraintMutationKeys } from '../mutation-keys';
import type {
  CheckConstraintSelect,
  CheckConstraintWithRelations,
  CheckConstraintPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  CheckConstraintSelect,
  CheckConstraintWithRelations,
  CheckConstraintPatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a CheckConstraint
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateCheckConstraintMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', checkConstraintPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateCheckConstraintMutation<S extends CheckConstraintSelect>(
  params: {
    selection: {
      fields: S & CheckConstraintSelect;
    } & HookStrictSelect<NoInfer<S>, CheckConstraintSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateCheckConstraint: {
          checkConstraint: InferSelectResult<CheckConstraintWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        checkConstraintPatch: CheckConstraintPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateCheckConstraint: {
      checkConstraint: InferSelectResult<CheckConstraintWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    checkConstraintPatch: CheckConstraintPatch;
  }
>;
export function useUpdateCheckConstraintMutation(
  params: {
    selection: SelectionConfig<CheckConstraintSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        checkConstraintPatch: CheckConstraintPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<CheckConstraintSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: checkConstraintMutationKeys.all,
    mutationFn: ({
      id,
      checkConstraintPatch,
    }: {
      id: string;
      checkConstraintPatch: CheckConstraintPatch;
    }) =>
      getClient()
        .checkConstraint.update({
          where: {
            id,
          },
          data: checkConstraintPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: checkConstraintKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: checkConstraintKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
