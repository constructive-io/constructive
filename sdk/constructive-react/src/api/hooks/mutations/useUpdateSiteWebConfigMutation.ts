/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteWebConfigKeys } from '../query-keys';
import { siteWebConfigMutationKeys } from '../mutation-keys';
import type {
  SiteWebConfigSelect,
  SiteWebConfigWithRelations,
  SiteWebConfigPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteWebConfigSelect,
  SiteWebConfigWithRelations,
  SiteWebConfigPatch,
} from '../../orm/input-types';
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteWebConfigMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteWebConfigPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteWebConfigMutation<S extends SiteWebConfigSelect>(
  params: {
    selection: {
      fields: S & SiteWebConfigSelect;
    } & HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteWebConfig: {
          siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteWebConfigPatch: SiteWebConfigPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteWebConfig: {
      siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteWebConfigPatch: SiteWebConfigPatch;
  }
>;
export function useUpdateSiteWebConfigMutation(
  params: {
    selection: SelectionConfig<SiteWebConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteWebConfigPatch: SiteWebConfigPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteWebConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteWebConfigMutationKeys.all,
    mutationFn: ({
      id,
      siteWebConfigPatch,
    }: {
      id: string;
      siteWebConfigPatch: SiteWebConfigPatch;
    }) =>
      getClient()
        .siteWebConfig.update({
          where: {
            id,
          },
          data: siteWebConfigPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteWebConfigKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteWebConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
