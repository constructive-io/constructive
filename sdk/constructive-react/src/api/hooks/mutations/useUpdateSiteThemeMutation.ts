/**
 * Theme configuration for a site; stores design tokens, colors, and typography as JSONB
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteThemeKeys } from '../query-keys';
import { siteThemeMutationKeys } from '../mutation-keys';
import type {
  SiteThemeSelect,
  SiteThemeWithRelations,
  SiteThemePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteThemeSelect,
  SiteThemeWithRelations,
  SiteThemePatch,
} from '../../orm/input-types';
/**
 * Theme configuration for a site; stores design tokens, colors, and typography as JSONB
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteThemeMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteThemePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteThemeMutation<S extends SiteThemeSelect>(
  params: {
    selection: {
      fields: S & SiteThemeSelect;
    } & HookStrictSelect<NoInfer<S>, SiteThemeSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteTheme: {
          siteTheme: InferSelectResult<SiteThemeWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteThemePatch: SiteThemePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteTheme: {
      siteTheme: InferSelectResult<SiteThemeWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteThemePatch: SiteThemePatch;
  }
>;
export function useUpdateSiteThemeMutation(
  params: {
    selection: SelectionConfig<SiteThemeSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteThemePatch: SiteThemePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteThemeSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteThemeMutationKeys.all,
    mutationFn: ({ id, siteThemePatch }: { id: string; siteThemePatch: SiteThemePatch }) =>
      getClient()
        .siteTheme.update({
          where: {
            id,
          },
          data: siteThemePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteThemeKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteThemeKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
