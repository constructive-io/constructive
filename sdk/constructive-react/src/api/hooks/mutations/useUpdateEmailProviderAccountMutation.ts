/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailProviderAccountKeys } from '../query-keys';
import { emailProviderAccountMutationKeys } from '../mutation-keys';
import type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
  EmailProviderAccountPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
  EmailProviderAccountPatch,
} from '../../orm/input-types';
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailProviderAccountMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', emailProviderAccountPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateEmailProviderAccountMutation<S extends EmailProviderAccountSelect>(
  params: {
    selection: {
      fields: S & EmailProviderAccountSelect;
    } & HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateEmailProviderAccount: {
          emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        emailProviderAccountPatch: EmailProviderAccountPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateEmailProviderAccount: {
      emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    emailProviderAccountPatch: EmailProviderAccountPatch;
  }
>;
export function useUpdateEmailProviderAccountMutation(
  params: {
    selection: SelectionConfig<EmailProviderAccountSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        emailProviderAccountPatch: EmailProviderAccountPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailProviderAccountSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailProviderAccountMutationKeys.all,
    mutationFn: ({
      id,
      emailProviderAccountPatch,
    }: {
      id: string;
      emailProviderAccountPatch: EmailProviderAccountPatch;
    }) =>
      getClient()
        .emailProviderAccount.update({
          where: {
            id,
          },
          data: emailProviderAccountPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: emailProviderAccountKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: emailProviderAccountKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
