/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailIdentityKeys } from '../query-keys';
import { emailIdentityMutationKeys } from '../mutation-keys';
import type {
  EmailIdentitySelect,
  EmailIdentityWithRelations,
  CreateEmailIdentityInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailIdentitySelect,
  EmailIdentityWithRelations,
  CreateEmailIdentityInput,
} from '../../orm/input-types';
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateEmailIdentityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateEmailIdentityMutation<S extends EmailIdentitySelect>(
  params: {
    selection: {
      fields: S & EmailIdentitySelect;
    } & HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createEmailIdentity: {
          emailIdentity: InferSelectResult<EmailIdentityWithRelations, S>;
        };
      },
      Error,
      CreateEmailIdentityInput['emailIdentity']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createEmailIdentity: {
      emailIdentity: InferSelectResult<EmailIdentityWithRelations, S>;
    };
  },
  Error,
  CreateEmailIdentityInput['emailIdentity']
>;
export function useCreateEmailIdentityMutation(
  params: {
    selection: SelectionConfig<EmailIdentitySelect>;
  } & Omit<UseMutationOptions<any, Error, CreateEmailIdentityInput['emailIdentity']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<EmailIdentitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailIdentityMutationKeys.create(),
    mutationFn: (data: CreateEmailIdentityInput['emailIdentity']) =>
      getClient()
        .emailIdentity.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: emailIdentityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
