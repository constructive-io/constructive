/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailIdentityKeys } from '../query-keys';
import { emailIdentityMutationKeys } from '../mutation-keys';
import type {
  EmailIdentitySelect,
  EmailIdentityWithRelations,
  EmailIdentityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailIdentitySelect,
  EmailIdentityWithRelations,
  EmailIdentityPatch,
} from '../../orm/input-types';
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateEmailIdentityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', emailIdentityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateEmailIdentityMutation<S extends EmailIdentitySelect>(
  params: {
    selection: {
      fields: S & EmailIdentitySelect;
    } & HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateEmailIdentity: {
          emailIdentity: InferSelectResult<EmailIdentityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        emailIdentityPatch: EmailIdentityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateEmailIdentity: {
      emailIdentity: InferSelectResult<EmailIdentityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    emailIdentityPatch: EmailIdentityPatch;
  }
>;
export function useUpdateEmailIdentityMutation(
  params: {
    selection: SelectionConfig<EmailIdentitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        emailIdentityPatch: EmailIdentityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<EmailIdentitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: emailIdentityMutationKeys.all,
    mutationFn: ({
      id,
      emailIdentityPatch,
    }: {
      id: string;
      emailIdentityPatch: EmailIdentityPatch;
    }) =>
      getClient()
        .emailIdentity.update({
          where: {
            id,
          },
          data: emailIdentityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: emailIdentityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: emailIdentityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
