/**
 * Delete mutation hook for UniqueConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { uniqueConstraintKeys } from '../query-keys';
import { uniqueConstraintMutationKeys } from '../mutation-keys';
import type { UniqueConstraintSelect, UniqueConstraintWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { UniqueConstraintSelect, UniqueConstraintWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a UniqueConstraint with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteUniqueConstraintMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteUniqueConstraintMutation<S extends UniqueConstraintSelect>(
  params: {
    selection: {
      fields: S & UniqueConstraintSelect;
    } & HookStrictSelect<NoInfer<S>, UniqueConstraintSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteUniqueConstraint: {
          uniqueConstraint: InferSelectResult<UniqueConstraintWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteUniqueConstraint: {
      uniqueConstraint: InferSelectResult<UniqueConstraintWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteUniqueConstraintMutation(
  params: {
    selection: SelectionConfig<UniqueConstraintSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<UniqueConstraintSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: uniqueConstraintMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .uniqueConstraint.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: uniqueConstraintKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: uniqueConstraintKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
