/**
 * Delete mutation hook for Function
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionKeys } from '../query-keys';
import { functionMutationKeys } from '../mutation-keys';
import type { FunctionSelect, FunctionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FunctionSelect, FunctionWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Function with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFunctionMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteFunctionMutation<S extends FunctionSelect>(
  params: {
    selection: {
      fields: S & FunctionSelect;
    } & HookStrictSelect<NoInfer<S>, FunctionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteFunction: {
          function: InferSelectResult<FunctionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteFunction: {
      function: InferSelectResult<FunctionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteFunctionMutation(
  params: {
    selection: SelectionConfig<FunctionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<FunctionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: functionMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .function.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: functionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: functionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
