/**
 * Single item query hook for TableBehavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { tableBehaviorKeys } from '../query-keys';
import type { TableBehaviorSelect, TableBehaviorWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TableBehaviorSelect, TableBehaviorWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const tableBehaviorQueryKey = tableBehaviorKeys.detail;
/**
 * Query hook for fetching a single TableBehavior
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTableBehaviorQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useTableBehaviorQuery<
  S extends TableBehaviorSelect,
  TData = {
    tableBehavior: InferSelectResult<TableBehaviorWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TableBehaviorSelect>;
  } & Omit<
    UseQueryOptions<
      {
        tableBehavior: InferSelectResult<TableBehaviorWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTableBehaviorQuery(
  params: {
    id: string;
    selection: SelectionConfig<TableBehaviorSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<TableBehaviorSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: tableBehaviorKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .tableBehavior.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single TableBehavior without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTableBehaviorQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchTableBehaviorQuery<S extends TableBehaviorSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, TableBehaviorSelect>;
}): Promise<{
  tableBehavior: InferSelectResult<TableBehaviorWithRelations, S> | null;
}>;
export async function fetchTableBehaviorQuery(params: {
  id: string;
  selection: SelectionConfig<TableBehaviorSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<TableBehaviorSelect>(params.selection);
  return getClient()
    .tableBehavior.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single TableBehavior for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTableBehaviorQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchTableBehaviorQuery<S extends TableBehaviorSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TableBehaviorSelect>;
  }
): Promise<void>;
export async function prefetchTableBehaviorQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<TableBehaviorSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<TableBehaviorSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: tableBehaviorKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .tableBehavior.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
