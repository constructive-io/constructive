/**
 * Custom error pages for a site surface, keyed by HTTP status code
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { siteErrorPageKeys } from '../query-keys';
import type {
  SiteErrorPageSelect,
  SiteErrorPageWithRelations,
  SiteErrorPageFilter,
  SiteErrorPageOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  SiteErrorPageSelect,
  SiteErrorPageWithRelations,
  SiteErrorPageFilter,
  SiteErrorPageOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteErrorPagesQueryKey = siteErrorPageKeys.list;
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteErrorPagesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useSiteErrorPagesQuery<
  S extends SiteErrorPageSelect,
  TData = {
    siteErrorPages: ConnectionResult<InferSelectResult<SiteErrorPageWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SiteErrorPageFilter, SiteErrorPageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteErrorPages: ConnectionResult<InferSelectResult<SiteErrorPageWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteErrorPagesQuery(
  params: {
    selection: ListSelectionConfig<SiteErrorPageSelect, SiteErrorPageFilter, SiteErrorPageOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    SiteErrorPageSelect,
    SiteErrorPageFilter,
    SiteErrorPageOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteErrorPageKeys.list(args),
    queryFn: () => getClient().siteErrorPage.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```ts
 * const data = await fetchSiteErrorPagesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchSiteErrorPagesQuery<S extends SiteErrorPageSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, SiteErrorPageFilter, SiteErrorPageOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
}): Promise<{
  siteErrorPages: ConnectionResult<InferSelectResult<SiteErrorPageWithRelations, S>>;
}>;
export async function fetchSiteErrorPagesQuery(params: {
  selection: ListSelectionConfig<SiteErrorPageSelect, SiteErrorPageFilter, SiteErrorPageOrderBy>;
}) {
  const args = buildListSelectionArgs<
    SiteErrorPageSelect,
    SiteErrorPageFilter,
    SiteErrorPageOrderBy
  >(params.selection);
  return getClient().siteErrorPage.findMany(args).unwrap();
}
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```ts
 * await prefetchSiteErrorPagesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchSiteErrorPagesQuery<S extends SiteErrorPageSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SiteErrorPageFilter, SiteErrorPageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
  }
): Promise<void>;
export async function prefetchSiteErrorPagesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<SiteErrorPageSelect, SiteErrorPageFilter, SiteErrorPageOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    SiteErrorPageSelect,
    SiteErrorPageFilter,
    SiteErrorPageOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteErrorPageKeys.list(args),
    queryFn: () => getClient().siteErrorPage.findMany(args).unwrap(),
  });
}
