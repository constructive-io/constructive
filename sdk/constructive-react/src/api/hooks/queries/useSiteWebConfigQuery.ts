/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteWebConfigKeys } from '../query-keys';
import type { SiteWebConfigSelect, SiteWebConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteWebConfigSelect, SiteWebConfigWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteWebConfigQueryKey = siteWebConfigKeys.detail;
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteWebConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSiteWebConfigQuery<
  S extends SiteWebConfigSelect,
  TData = {
    siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteWebConfigQuery(
  params: {
    id: string;
    selection: SelectionConfig<SiteWebConfigSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SiteWebConfigSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteWebConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteWebConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```ts
 * const data = await fetchSiteWebConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSiteWebConfigQuery<S extends SiteWebConfigSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
}): Promise<{
  siteWebConfig: InferSelectResult<SiteWebConfigWithRelations, S> | null;
}>;
export async function fetchSiteWebConfigQuery(params: {
  id: string;
  selection: SelectionConfig<SiteWebConfigSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SiteWebConfigSelect>(params.selection);
  return getClient()
    .siteWebConfig.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```ts
 * await prefetchSiteWebConfigQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSiteWebConfigQuery<S extends SiteWebConfigSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  }
): Promise<void>;
export async function prefetchSiteWebConfigQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SiteWebConfigSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SiteWebConfigSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteWebConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteWebConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
