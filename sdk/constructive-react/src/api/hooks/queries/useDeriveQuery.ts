/**
 * Single item query hook for Derive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { deriveKeys } from '../query-keys';
import type { DeriveSelect, DeriveWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DeriveSelect, DeriveWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const deriveQueryKey = deriveKeys.detail;
/**
 * Query hook for fetching a single Derive
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDeriveQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDeriveQuery<
  S extends DeriveSelect,
  TData = {
    derive: InferSelectResult<DeriveWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DeriveSelect>;
  } & Omit<
    UseQueryOptions<
      {
        derive: InferSelectResult<DeriveWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDeriveQuery(
  params: {
    id: string;
    selection: SelectionConfig<DeriveSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DeriveSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: deriveKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .derive.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single Derive without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDeriveQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDeriveQuery<S extends DeriveSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DeriveSelect>;
}): Promise<{
  derive: InferSelectResult<DeriveWithRelations, S> | null;
}>;
export async function fetchDeriveQuery(params: {
  id: string;
  selection: SelectionConfig<DeriveSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DeriveSelect>(params.selection);
  return getClient()
    .derive.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single Derive for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDeriveQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDeriveQuery<S extends DeriveSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DeriveSelect>;
  }
): Promise<void>;
export async function prefetchDeriveQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DeriveSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DeriveSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: deriveKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .derive.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
