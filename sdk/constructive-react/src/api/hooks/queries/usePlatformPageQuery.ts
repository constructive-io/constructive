/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformPageKeys } from '../query-keys';
import type { PlatformPageSelect, PlatformPageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformPageSelect, PlatformPageWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformPageQueryKey = platformPageKeys.detail;
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformPageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformPageQuery<
  S extends PlatformPageSelect,
  TData = {
    platformPage: InferSelectResult<PlatformPageWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformPage: InferSelectResult<PlatformPageWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformPageQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformPageSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformPageSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformPageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformPage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```ts
 * const data = await fetchPlatformPageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformPageQuery<S extends PlatformPageSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
}): Promise<{
  platformPage: InferSelectResult<PlatformPageWithRelations, S> | null;
}>;
export async function fetchPlatformPageQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformPageSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformPageSelect>(params.selection);
  return getClient()
    .platformPage.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```ts
 * await prefetchPlatformPageQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformPageQuery<S extends PlatformPageSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  }
): Promise<void>;
export async function prefetchPlatformPageQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformPageSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformPageSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformPageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformPage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
