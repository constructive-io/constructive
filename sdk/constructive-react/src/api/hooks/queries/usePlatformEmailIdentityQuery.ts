/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformEmailIdentityKeys } from '../query-keys';
import type {
  PlatformEmailIdentitySelect,
  PlatformEmailIdentityWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformEmailIdentitySelect,
  PlatformEmailIdentityWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformEmailIdentityQueryKey = platformEmailIdentityKeys.detail;
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformEmailIdentityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformEmailIdentityQuery<
  S extends PlatformEmailIdentitySelect,
  TData = {
    platformEmailIdentity: InferSelectResult<PlatformEmailIdentityWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformEmailIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformEmailIdentity: InferSelectResult<PlatformEmailIdentityWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformEmailIdentityQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformEmailIdentitySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformEmailIdentitySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformEmailIdentityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformEmailIdentity.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * const data = await fetchPlatformEmailIdentityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformEmailIdentityQuery<
  S extends PlatformEmailIdentitySelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformEmailIdentitySelect>;
}): Promise<{
  platformEmailIdentity: InferSelectResult<PlatformEmailIdentityWithRelations, S> | null;
}>;
export async function fetchPlatformEmailIdentityQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformEmailIdentitySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformEmailIdentitySelect>(params.selection);
  return getClient()
    .platformEmailIdentity.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * await prefetchPlatformEmailIdentityQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformEmailIdentityQuery<S extends PlatformEmailIdentitySelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformEmailIdentitySelect>;
  }
): Promise<void>;
export async function prefetchPlatformEmailIdentityQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformEmailIdentitySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformEmailIdentitySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformEmailIdentityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformEmailIdentity.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
