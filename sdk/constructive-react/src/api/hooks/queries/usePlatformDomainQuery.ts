/**
 * Fully-qualified hostnames owned by this scope; each row claims its hostname globally through the catalog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformDomainKeys } from '../query-keys';
import type { PlatformDomainSelect, PlatformDomainWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformDomainSelect, PlatformDomainWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformDomainQueryKey = platformDomainKeys.detail;
/**
 * Fully-qualified hostnames owned by this scope; each row claims its hostname globally through the catalog
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformDomainQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformDomainQuery<
  S extends PlatformDomainSelect,
  TData = {
    platformDomain: InferSelectResult<PlatformDomainWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformDomainSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformDomain: InferSelectResult<PlatformDomainWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformDomainQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformDomainSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformDomainSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformDomainKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformDomain.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fully-qualified hostnames owned by this scope; each row claims its hostname globally through the catalog
 *
 * @example
 * ```ts
 * const data = await fetchPlatformDomainQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformDomainQuery<S extends PlatformDomainSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformDomainSelect>;
}): Promise<{
  platformDomain: InferSelectResult<PlatformDomainWithRelations, S> | null;
}>;
export async function fetchPlatformDomainQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformDomainSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformDomainSelect>(params.selection);
  return getClient()
    .platformDomain.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Fully-qualified hostnames owned by this scope; each row claims its hostname globally through the catalog
 *
 * @example
 * ```ts
 * await prefetchPlatformDomainQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformDomainQuery<S extends PlatformDomainSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformDomainSelect>;
  }
): Promise<void>;
export async function prefetchPlatformDomainQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformDomainSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformDomainSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformDomainKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformDomain.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
