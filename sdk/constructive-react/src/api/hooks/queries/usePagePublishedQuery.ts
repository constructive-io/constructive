/**
 * Custom query hook for pagePublished
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
import type { PagePublishedVariables } from '../../orm/query';
export type { PagePublishedVariables } from '../../orm/query';
/** Query key factory - re-exported from query-keys.ts */
export const pagePublishedQueryKey = customQueryKeys.pagePublished;
/**
 * Query hook for pagePublished
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePagePublishedQuery({ variables: { pageSlug, targetSiteId } });
 *
 * if (data?.pagePublished) {
 *   console.log(data.pagePublished);
 * }
 * ```
 */
export function usePagePublishedQuery<
  TData = {
    pagePublished: unknown | null;
  },
>(
  params?: {
    variables?: PagePublishedVariables;
  } & Omit<
    UseQueryOptions<
      {
        pagePublished: unknown | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePagePublishedQuery<
  TData = {
    pagePublished: unknown | null;
  },
>(
  params?: {
    variables?: PagePublishedVariables;
  } & Omit<
    UseQueryOptions<
      {
        pagePublished: unknown | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const variables = params?.variables ?? {};
  const { variables: _variables, ...queryOptions } = params ?? {};
  void _variables;
  return useQuery({
    queryKey: pagePublishedQueryKey(variables),
    queryFn: () => getClient().query.pagePublished(variables).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch pagePublished without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchPagePublishedQuery({ variables: { pageSlug, targetSiteId } });
 * ```
 */
export async function fetchPagePublishedQuery(params?: { variables?: PagePublishedVariables }) {
  const variables = params?.variables ?? {};
  return getClient().query.pagePublished(variables).unwrap();
}
/**
 * Prefetch pagePublished for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPagePublishedQuery(queryClient, { variables: { pageSlug, targetSiteId } });
 * ```
 */
export async function prefetchPagePublishedQuery(
  queryClient: QueryClient,
  params?: {
    variables?: PagePublishedVariables;
  }
): Promise<void> {
  const variables = params?.variables ?? {};
  await queryClient.prefetchQuery({
    queryKey: pagePublishedQueryKey(variables),
    queryFn: () => getClient().query.pagePublished(variables).unwrap(),
  });
}
