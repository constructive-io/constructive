/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformEmailIdentityKeys } from '../query-keys';
import type {
  PlatformEmailIdentitySelect,
  PlatformEmailIdentityWithRelations,
  PlatformEmailIdentityFilter,
  PlatformEmailIdentityOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformEmailIdentitySelect,
  PlatformEmailIdentityWithRelations,
  PlatformEmailIdentityFilter,
  PlatformEmailIdentityOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformEmailIdentitiesQueryKey = platformEmailIdentityKeys.list;
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformEmailIdentitiesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformEmailIdentitiesQuery<
  S extends PlatformEmailIdentitySelect,
  TData = {
    platformEmailIdentities: ConnectionResult<
      InferSelectResult<PlatformEmailIdentityWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformEmailIdentityFilter, PlatformEmailIdentityOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformEmailIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformEmailIdentities: ConnectionResult<
          InferSelectResult<PlatformEmailIdentityWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformEmailIdentitiesQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformEmailIdentitySelect,
      PlatformEmailIdentityFilter,
      PlatformEmailIdentityOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformEmailIdentitySelect,
    PlatformEmailIdentityFilter,
    PlatformEmailIdentityOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformEmailIdentityKeys.list(args),
    queryFn: () => getClient().platformEmailIdentity.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * const data = await fetchPlatformEmailIdentitiesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformEmailIdentitiesQuery<
  S extends PlatformEmailIdentitySelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformEmailIdentityFilter, PlatformEmailIdentityOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformEmailIdentitySelect>;
}): Promise<{
  platformEmailIdentities: ConnectionResult<
    InferSelectResult<PlatformEmailIdentityWithRelations, S>
  >;
}>;
export async function fetchPlatformEmailIdentitiesQuery(params: {
  selection: ListSelectionConfig<
    PlatformEmailIdentitySelect,
    PlatformEmailIdentityFilter,
    PlatformEmailIdentityOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformEmailIdentitySelect,
    PlatformEmailIdentityFilter,
    PlatformEmailIdentityOrderBy
  >(params.selection);
  return getClient().platformEmailIdentity.findMany(args).unwrap();
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * await prefetchPlatformEmailIdentitiesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformEmailIdentitiesQuery<S extends PlatformEmailIdentitySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformEmailIdentityFilter, PlatformEmailIdentityOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformEmailIdentitySelect>;
  }
): Promise<void>;
export async function prefetchPlatformEmailIdentitiesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformEmailIdentitySelect,
      PlatformEmailIdentityFilter,
      PlatformEmailIdentityOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformEmailIdentitySelect,
    PlatformEmailIdentityFilter,
    PlatformEmailIdentityOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformEmailIdentityKeys.list(args),
    queryFn: () => getClient().platformEmailIdentity.findMany(args).unwrap(),
  });
}
