/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { emailIdentityKeys } from '../query-keys';
import type {
  EmailIdentitySelect,
  EmailIdentityWithRelations,
  EmailIdentityFilter,
  EmailIdentityOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  EmailIdentitySelect,
  EmailIdentityWithRelations,
  EmailIdentityFilter,
  EmailIdentityOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const emailIdentitiesQueryKey = emailIdentityKeys.list;
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailIdentitiesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useEmailIdentitiesQuery<
  S extends EmailIdentitySelect,
  TData = {
    emailIdentities: ConnectionResult<InferSelectResult<EmailIdentityWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, EmailIdentityFilter, EmailIdentityOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        emailIdentities: ConnectionResult<InferSelectResult<EmailIdentityWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEmailIdentitiesQuery(
  params: {
    selection: ListSelectionConfig<EmailIdentitySelect, EmailIdentityFilter, EmailIdentityOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    EmailIdentitySelect,
    EmailIdentityFilter,
    EmailIdentityOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: emailIdentityKeys.list(args),
    queryFn: () => getClient().emailIdentity.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * const data = await fetchEmailIdentitiesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchEmailIdentitiesQuery<S extends EmailIdentitySelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, EmailIdentityFilter, EmailIdentityOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
}): Promise<{
  emailIdentities: ConnectionResult<InferSelectResult<EmailIdentityWithRelations, S>>;
}>;
export async function fetchEmailIdentitiesQuery(params: {
  selection: ListSelectionConfig<EmailIdentitySelect, EmailIdentityFilter, EmailIdentityOrderBy>;
}) {
  const args = buildListSelectionArgs<
    EmailIdentitySelect,
    EmailIdentityFilter,
    EmailIdentityOrderBy
  >(params.selection);
  return getClient().emailIdentity.findMany(args).unwrap();
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * await prefetchEmailIdentitiesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchEmailIdentitiesQuery<S extends EmailIdentitySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, EmailIdentityFilter, EmailIdentityOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
  }
): Promise<void>;
export async function prefetchEmailIdentitiesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<EmailIdentitySelect, EmailIdentityFilter, EmailIdentityOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    EmailIdentitySelect,
    EmailIdentityFilter,
    EmailIdentityOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: emailIdentityKeys.list(args),
    queryFn: () => getClient().emailIdentity.findMany(args).unwrap(),
  });
}
