/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { siteWebConfigKeys } from '../query-keys';
import type {
  SiteWebConfigSelect,
  SiteWebConfigWithRelations,
  SiteWebConfigFilter,
  SiteWebConfigOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  SiteWebConfigSelect,
  SiteWebConfigWithRelations,
  SiteWebConfigFilter,
  SiteWebConfigOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteWebConfigsQueryKey = siteWebConfigKeys.list;
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteWebConfigsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useSiteWebConfigsQuery<
  S extends SiteWebConfigSelect,
  TData = {
    siteWebConfigs: ConnectionResult<InferSelectResult<SiteWebConfigWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SiteWebConfigFilter, SiteWebConfigOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteWebConfigs: ConnectionResult<InferSelectResult<SiteWebConfigWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteWebConfigsQuery(
  params: {
    selection: ListSelectionConfig<SiteWebConfigSelect, SiteWebConfigFilter, SiteWebConfigOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    SiteWebConfigSelect,
    SiteWebConfigFilter,
    SiteWebConfigOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteWebConfigKeys.list(args),
    queryFn: () => getClient().siteWebConfig.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```ts
 * const data = await fetchSiteWebConfigsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchSiteWebConfigsQuery<S extends SiteWebConfigSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, SiteWebConfigFilter, SiteWebConfigOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
}): Promise<{
  siteWebConfigs: ConnectionResult<InferSelectResult<SiteWebConfigWithRelations, S>>;
}>;
export async function fetchSiteWebConfigsQuery(params: {
  selection: ListSelectionConfig<SiteWebConfigSelect, SiteWebConfigFilter, SiteWebConfigOrderBy>;
}) {
  const args = buildListSelectionArgs<
    SiteWebConfigSelect,
    SiteWebConfigFilter,
    SiteWebConfigOrderBy
  >(params.selection);
  return getClient().siteWebConfig.findMany(args).unwrap();
}
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```ts
 * await prefetchSiteWebConfigsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchSiteWebConfigsQuery<S extends SiteWebConfigSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SiteWebConfigFilter, SiteWebConfigOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SiteWebConfigSelect>;
  }
): Promise<void>;
export async function prefetchSiteWebConfigsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<SiteWebConfigSelect, SiteWebConfigFilter, SiteWebConfigOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    SiteWebConfigSelect,
    SiteWebConfigFilter,
    SiteWebConfigOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteWebConfigKeys.list(args),
    queryFn: () => getClient().siteWebConfig.findMany(args).unwrap(),
  });
}
