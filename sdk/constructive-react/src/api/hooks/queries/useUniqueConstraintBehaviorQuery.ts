/**
 * Single item query hook for UniqueConstraintBehavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { uniqueConstraintBehaviorKeys } from '../query-keys';
import type {
  UniqueConstraintBehaviorSelect,
  UniqueConstraintBehaviorWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  UniqueConstraintBehaviorSelect,
  UniqueConstraintBehaviorWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const uniqueConstraintBehaviorQueryKey = uniqueConstraintBehaviorKeys.detail;
/**
 * Query hook for fetching a single UniqueConstraintBehavior
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUniqueConstraintBehaviorQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useUniqueConstraintBehaviorQuery<
  S extends UniqueConstraintBehaviorSelect,
  TData = {
    uniqueConstraintBehavior: InferSelectResult<UniqueConstraintBehaviorWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, UniqueConstraintBehaviorSelect>;
  } & Omit<
    UseQueryOptions<
      {
        uniqueConstraintBehavior: InferSelectResult<
          UniqueConstraintBehaviorWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useUniqueConstraintBehaviorQuery(
  params: {
    id: string;
    selection: SelectionConfig<UniqueConstraintBehaviorSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<UniqueConstraintBehaviorSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: uniqueConstraintBehaviorKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .uniqueConstraintBehavior.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single UniqueConstraintBehavior without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchUniqueConstraintBehaviorQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchUniqueConstraintBehaviorQuery<
  S extends UniqueConstraintBehaviorSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, UniqueConstraintBehaviorSelect>;
}): Promise<{
  uniqueConstraintBehavior: InferSelectResult<UniqueConstraintBehaviorWithRelations, S> | null;
}>;
export async function fetchUniqueConstraintBehaviorQuery(params: {
  id: string;
  selection: SelectionConfig<UniqueConstraintBehaviorSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<UniqueConstraintBehaviorSelect>(params.selection);
  return getClient()
    .uniqueConstraintBehavior.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single UniqueConstraintBehavior for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchUniqueConstraintBehaviorQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchUniqueConstraintBehaviorQuery<
  S extends UniqueConstraintBehaviorSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, UniqueConstraintBehaviorSelect>;
  }
): Promise<void>;
export async function prefetchUniqueConstraintBehaviorQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<UniqueConstraintBehaviorSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<UniqueConstraintBehaviorSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: uniqueConstraintBehaviorKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .uniqueConstraintBehavior.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
