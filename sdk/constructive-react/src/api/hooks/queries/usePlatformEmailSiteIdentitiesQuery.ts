/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformEmailSiteIdentityKeys } from '../query-keys';
import type {
  PlatformEmailSiteIdentitySelect,
  PlatformEmailSiteIdentityWithRelations,
  PlatformEmailSiteIdentityFilter,
  PlatformEmailSiteIdentityOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformEmailSiteIdentitySelect,
  PlatformEmailSiteIdentityWithRelations,
  PlatformEmailSiteIdentityFilter,
  PlatformEmailSiteIdentityOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformEmailSiteIdentitiesQueryKey = platformEmailSiteIdentityKeys.list;
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformEmailSiteIdentitiesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformEmailSiteIdentitiesQuery<
  S extends PlatformEmailSiteIdentitySelect,
  TData = {
    platformEmailSiteIdentities: ConnectionResult<
      InferSelectResult<PlatformEmailSiteIdentityWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformEmailSiteIdentityFilter, PlatformEmailSiteIdentityOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformEmailSiteIdentities: ConnectionResult<
          InferSelectResult<PlatformEmailSiteIdentityWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformEmailSiteIdentitiesQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformEmailSiteIdentitySelect,
      PlatformEmailSiteIdentityFilter,
      PlatformEmailSiteIdentityOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformEmailSiteIdentitySelect,
    PlatformEmailSiteIdentityFilter,
    PlatformEmailSiteIdentityOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformEmailSiteIdentityKeys.list(args),
    queryFn: () => getClient().platformEmailSiteIdentity.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```ts
 * const data = await fetchPlatformEmailSiteIdentitiesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformEmailSiteIdentitiesQuery<
  S extends PlatformEmailSiteIdentitySelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformEmailSiteIdentityFilter, PlatformEmailSiteIdentityOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
}): Promise<{
  platformEmailSiteIdentities: ConnectionResult<
    InferSelectResult<PlatformEmailSiteIdentityWithRelations, S>
  >;
}>;
export async function fetchPlatformEmailSiteIdentitiesQuery(params: {
  selection: ListSelectionConfig<
    PlatformEmailSiteIdentitySelect,
    PlatformEmailSiteIdentityFilter,
    PlatformEmailSiteIdentityOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformEmailSiteIdentitySelect,
    PlatformEmailSiteIdentityFilter,
    PlatformEmailSiteIdentityOrderBy
  >(params.selection);
  return getClient().platformEmailSiteIdentity.findMany(args).unwrap();
}
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```ts
 * await prefetchPlatformEmailSiteIdentitiesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformEmailSiteIdentitiesQuery<
  S extends PlatformEmailSiteIdentitySelect,
>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformEmailSiteIdentityFilter, PlatformEmailSiteIdentityOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
  }
): Promise<void>;
export async function prefetchPlatformEmailSiteIdentitiesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformEmailSiteIdentitySelect,
      PlatformEmailSiteIdentityFilter,
      PlatformEmailSiteIdentityOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformEmailSiteIdentitySelect,
    PlatformEmailSiteIdentityFilter,
    PlatformEmailSiteIdentityOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformEmailSiteIdentityKeys.list(args),
    queryFn: () => getClient().platformEmailSiteIdentity.findMany(args).unwrap(),
  });
}
