/**
 * Single item query hook for DomainType
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { domainTypeKeys } from '../query-keys';
import type { DomainTypeSelect, DomainTypeWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DomainTypeSelect, DomainTypeWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const domainTypeQueryKey = domainTypeKeys.detail;
/**
 * Query hook for fetching a single DomainType
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDomainTypeQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDomainTypeQuery<
  S extends DomainTypeSelect,
  TData = {
    domainType: InferSelectResult<DomainTypeWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DomainTypeSelect>;
  } & Omit<
    UseQueryOptions<
      {
        domainType: InferSelectResult<DomainTypeWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDomainTypeQuery(
  params: {
    id: string;
    selection: SelectionConfig<DomainTypeSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DomainTypeSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: domainTypeKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .domainType.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single DomainType without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDomainTypeQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDomainTypeQuery<S extends DomainTypeSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DomainTypeSelect>;
}): Promise<{
  domainType: InferSelectResult<DomainTypeWithRelations, S> | null;
}>;
export async function fetchDomainTypeQuery(params: {
  id: string;
  selection: SelectionConfig<DomainTypeSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DomainTypeSelect>(params.selection);
  return getClient()
    .domainType.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single DomainType for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDomainTypeQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDomainTypeQuery<S extends DomainTypeSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DomainTypeSelect>;
  }
): Promise<void>;
export async function prefetchDomainTypeQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DomainTypeSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DomainTypeSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: domainTypeKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .domainType.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
