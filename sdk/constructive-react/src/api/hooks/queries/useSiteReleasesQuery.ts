/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { siteReleaseKeys } from '../query-keys';
import type {
  SiteReleaseSelect,
  SiteReleaseWithRelations,
  SiteReleaseFilter,
  SiteReleaseOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  SiteReleaseSelect,
  SiteReleaseWithRelations,
  SiteReleaseFilter,
  SiteReleaseOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteReleasesQueryKey = siteReleaseKeys.list;
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteReleasesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useSiteReleasesQuery<
  S extends SiteReleaseSelect,
  TData = {
    siteReleases: ConnectionResult<InferSelectResult<SiteReleaseWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SiteReleaseFilter, SiteReleaseOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteReleases: ConnectionResult<InferSelectResult<SiteReleaseWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteReleasesQuery(
  params: {
    selection: ListSelectionConfig<SiteReleaseSelect, SiteReleaseFilter, SiteReleaseOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<SiteReleaseSelect, SiteReleaseFilter, SiteReleaseOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteReleaseKeys.list(args),
    queryFn: () => getClient().siteRelease.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```ts
 * const data = await fetchSiteReleasesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchSiteReleasesQuery<S extends SiteReleaseSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, SiteReleaseFilter, SiteReleaseOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
}): Promise<{
  siteReleases: ConnectionResult<InferSelectResult<SiteReleaseWithRelations, S>>;
}>;
export async function fetchSiteReleasesQuery(params: {
  selection: ListSelectionConfig<SiteReleaseSelect, SiteReleaseFilter, SiteReleaseOrderBy>;
}) {
  const args = buildListSelectionArgs<SiteReleaseSelect, SiteReleaseFilter, SiteReleaseOrderBy>(
    params.selection
  );
  return getClient().siteRelease.findMany(args).unwrap();
}
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```ts
 * await prefetchSiteReleasesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchSiteReleasesQuery<S extends SiteReleaseSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SiteReleaseFilter, SiteReleaseOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
  }
): Promise<void>;
export async function prefetchSiteReleasesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<SiteReleaseSelect, SiteReleaseFilter, SiteReleaseOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<SiteReleaseSelect, SiteReleaseFilter, SiteReleaseOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: siteReleaseKeys.list(args),
    queryFn: () => getClient().siteRelease.findMany(args).unwrap(),
  });
}
