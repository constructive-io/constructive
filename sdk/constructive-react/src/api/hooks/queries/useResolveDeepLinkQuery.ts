/**
 * Custom query hook for resolveDeepLink
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
import type { ResolveDeepLinkVariables } from '../../orm/query';
export type { ResolveDeepLinkVariables } from '../../orm/query';
/** Query key factory - re-exported from query-keys.ts */
export const resolveDeepLinkQueryKey = customQueryKeys.resolveDeepLink;
/**
 * Query hook for resolveDeepLink
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useResolveDeepLinkQuery({ variables: { linkSlug, targetSiteId } });
 *
 * if (data?.resolveDeepLink) {
 *   console.log(data.resolveDeepLink);
 * }
 * ```
 */
export function useResolveDeepLinkQuery<
  TData = {
    resolveDeepLink: unknown | null;
  },
>(
  params?: {
    variables?: ResolveDeepLinkVariables;
  } & Omit<
    UseQueryOptions<
      {
        resolveDeepLink: unknown | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useResolveDeepLinkQuery<
  TData = {
    resolveDeepLink: unknown | null;
  },
>(
  params?: {
    variables?: ResolveDeepLinkVariables;
  } & Omit<
    UseQueryOptions<
      {
        resolveDeepLink: unknown | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const variables = params?.variables ?? {};
  const { variables: _variables, ...queryOptions } = params ?? {};
  void _variables;
  return useQuery({
    queryKey: resolveDeepLinkQueryKey(variables),
    queryFn: () => getClient().query.resolveDeepLink(variables).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch resolveDeepLink without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchResolveDeepLinkQuery({ variables: { linkSlug, targetSiteId } });
 * ```
 */
export async function fetchResolveDeepLinkQuery(params?: { variables?: ResolveDeepLinkVariables }) {
  const variables = params?.variables ?? {};
  return getClient().query.resolveDeepLink(variables).unwrap();
}
/**
 * Prefetch resolveDeepLink for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchResolveDeepLinkQuery(queryClient, { variables: { linkSlug, targetSiteId } });
 * ```
 */
export async function prefetchResolveDeepLinkQuery(
  queryClient: QueryClient,
  params?: {
    variables?: ResolveDeepLinkVariables;
  }
): Promise<void> {
  const variables = params?.variables ?? {};
  await queryClient.prefetchQuery({
    queryKey: resolveDeepLinkQueryKey(variables),
    queryFn: () => getClient().query.resolveDeepLink(variables).unwrap(),
  });
}
