/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformSiteDeepLinkKeys } from '../query-keys';
import type {
  PlatformSiteDeepLinkSelect,
  PlatformSiteDeepLinkWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformSiteDeepLinkSelect,
  PlatformSiteDeepLinkWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformSiteDeepLinkQueryKey = platformSiteDeepLinkKeys.detail;
/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformSiteDeepLinkQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformSiteDeepLinkQuery<
  S extends PlatformSiteDeepLinkSelect,
  TData = {
    platformSiteDeepLink: InferSelectResult<PlatformSiteDeepLinkWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSiteDeepLinkSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformSiteDeepLink: InferSelectResult<PlatformSiteDeepLinkWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformSiteDeepLinkQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformSiteDeepLinkSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformSiteDeepLinkSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformSiteDeepLinkKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSiteDeepLink.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 *
 * @example
 * ```ts
 * const data = await fetchPlatformSiteDeepLinkQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformSiteDeepLinkQuery<S extends PlatformSiteDeepLinkSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformSiteDeepLinkSelect>;
}): Promise<{
  platformSiteDeepLink: InferSelectResult<PlatformSiteDeepLinkWithRelations, S> | null;
}>;
export async function fetchPlatformSiteDeepLinkQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformSiteDeepLinkSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformSiteDeepLinkSelect>(params.selection);
  return getClient()
    .platformSiteDeepLink.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 *
 * @example
 * ```ts
 * await prefetchPlatformSiteDeepLinkQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformSiteDeepLinkQuery<S extends PlatformSiteDeepLinkSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSiteDeepLinkSelect>;
  }
): Promise<void>;
export async function prefetchPlatformSiteDeepLinkQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformSiteDeepLinkSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformSiteDeepLinkSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformSiteDeepLinkKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSiteDeepLink.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
