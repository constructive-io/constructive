/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteReleaseKeys } from '../query-keys';
import type { SiteReleaseSelect, SiteReleaseWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteReleaseSelect, SiteReleaseWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteReleaseQueryKey = siteReleaseKeys.detail;
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteReleaseQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSiteReleaseQuery<
  S extends SiteReleaseSelect,
  TData = {
    siteRelease: InferSelectResult<SiteReleaseWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteRelease: InferSelectResult<SiteReleaseWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteReleaseQuery(
  params: {
    id: string;
    selection: SelectionConfig<SiteReleaseSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SiteReleaseSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteReleaseKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteRelease.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```ts
 * const data = await fetchSiteReleaseQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSiteReleaseQuery<S extends SiteReleaseSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
}): Promise<{
  siteRelease: InferSelectResult<SiteReleaseWithRelations, S> | null;
}>;
export async function fetchSiteReleaseQuery(params: {
  id: string;
  selection: SelectionConfig<SiteReleaseSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SiteReleaseSelect>(params.selection);
  return getClient()
    .siteRelease.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Immutable static-site release manifest head, versioned in the site-owned merkle store
 *
 * @example
 * ```ts
 * await prefetchSiteReleaseQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSiteReleaseQuery<S extends SiteReleaseSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteReleaseSelect>;
  }
): Promise<void>;
export async function prefetchSiteReleaseQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SiteReleaseSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SiteReleaseSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteReleaseKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteRelease.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
