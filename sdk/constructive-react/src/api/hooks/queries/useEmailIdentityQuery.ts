/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailIdentityKeys } from '../query-keys';
import type { EmailIdentitySelect, EmailIdentityWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { EmailIdentitySelect, EmailIdentityWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const emailIdentityQueryKey = emailIdentityKeys.detail;
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailIdentityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useEmailIdentityQuery<
  S extends EmailIdentitySelect,
  TData = {
    emailIdentity: InferSelectResult<EmailIdentityWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        emailIdentity: InferSelectResult<EmailIdentityWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEmailIdentityQuery(
  params: {
    id: string;
    selection: SelectionConfig<EmailIdentitySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<EmailIdentitySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: emailIdentityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .emailIdentity.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * const data = await fetchEmailIdentityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchEmailIdentityQuery<S extends EmailIdentitySelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
}): Promise<{
  emailIdentity: InferSelectResult<EmailIdentityWithRelations, S> | null;
}>;
export async function fetchEmailIdentityQuery(params: {
  id: string;
  selection: SelectionConfig<EmailIdentitySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<EmailIdentitySelect>(params.selection);
  return getClient()
    .emailIdentity.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Outbound sender identity: the from/reply-to/support addresses a tenant sends as, and the provider account the mail leaves through
 *
 * @example
 * ```ts
 * await prefetchEmailIdentityQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchEmailIdentityQuery<S extends EmailIdentitySelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, EmailIdentitySelect>;
  }
): Promise<void>;
export async function prefetchEmailIdentityQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<EmailIdentitySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<EmailIdentitySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: emailIdentityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .emailIdentity.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
