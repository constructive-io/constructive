/**
 * Custom error pages for a site surface, keyed by HTTP status code
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteErrorPageKeys } from '../query-keys';
import type { SiteErrorPageSelect, SiteErrorPageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteErrorPageSelect, SiteErrorPageWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteErrorPageQueryKey = siteErrorPageKeys.detail;
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteErrorPageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSiteErrorPageQuery<
  S extends SiteErrorPageSelect,
  TData = {
    siteErrorPage: InferSelectResult<SiteErrorPageWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteErrorPage: InferSelectResult<SiteErrorPageWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteErrorPageQuery(
  params: {
    id: string;
    selection: SelectionConfig<SiteErrorPageSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SiteErrorPageSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteErrorPageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteErrorPage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```ts
 * const data = await fetchSiteErrorPageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSiteErrorPageQuery<S extends SiteErrorPageSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
}): Promise<{
  siteErrorPage: InferSelectResult<SiteErrorPageWithRelations, S> | null;
}>;
export async function fetchSiteErrorPageQuery(params: {
  id: string;
  selection: SelectionConfig<SiteErrorPageSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SiteErrorPageSelect>(params.selection);
  return getClient()
    .siteErrorPage.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Custom error pages for a site surface, keyed by HTTP status code
 *
 * @example
 * ```ts
 * await prefetchSiteErrorPageQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSiteErrorPageQuery<S extends SiteErrorPageSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteErrorPageSelect>;
  }
): Promise<void>;
export async function prefetchSiteErrorPageQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SiteErrorPageSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SiteErrorPageSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteErrorPageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteErrorPage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
