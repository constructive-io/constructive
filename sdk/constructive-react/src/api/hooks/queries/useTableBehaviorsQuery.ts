/**
 * List query hook for TableBehavior
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { tableBehaviorKeys } from '../query-keys';
import type {
  TableBehaviorSelect,
  TableBehaviorWithRelations,
  TableBehaviorFilter,
  TableBehaviorOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  TableBehaviorSelect,
  TableBehaviorWithRelations,
  TableBehaviorFilter,
  TableBehaviorOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const tableBehaviorsQueryKey = tableBehaviorKeys.list;
/**
 * Query hook for fetching TableBehavior list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTableBehaviorsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useTableBehaviorsQuery<
  S extends TableBehaviorSelect,
  TData = {
    tableBehaviors: ConnectionResult<InferSelectResult<TableBehaviorWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, TableBehaviorFilter, TableBehaviorOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, TableBehaviorSelect>;
  } & Omit<
    UseQueryOptions<
      {
        tableBehaviors: ConnectionResult<InferSelectResult<TableBehaviorWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTableBehaviorsQuery(
  params: {
    selection: ListSelectionConfig<TableBehaviorSelect, TableBehaviorFilter, TableBehaviorOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    TableBehaviorSelect,
    TableBehaviorFilter,
    TableBehaviorOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: tableBehaviorKeys.list(args),
    queryFn: () => getClient().tableBehavior.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch TableBehavior list without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTableBehaviorsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchTableBehaviorsQuery<S extends TableBehaviorSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, TableBehaviorFilter, TableBehaviorOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, TableBehaviorSelect>;
}): Promise<{
  tableBehaviors: ConnectionResult<InferSelectResult<TableBehaviorWithRelations, S>>;
}>;
export async function fetchTableBehaviorsQuery(params: {
  selection: ListSelectionConfig<TableBehaviorSelect, TableBehaviorFilter, TableBehaviorOrderBy>;
}) {
  const args = buildListSelectionArgs<
    TableBehaviorSelect,
    TableBehaviorFilter,
    TableBehaviorOrderBy
  >(params.selection);
  return getClient().tableBehavior.findMany(args).unwrap();
}
/**
 * Prefetch TableBehavior list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTableBehaviorsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchTableBehaviorsQuery<S extends TableBehaviorSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, TableBehaviorFilter, TableBehaviorOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, TableBehaviorSelect>;
  }
): Promise<void>;
export async function prefetchTableBehaviorsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<TableBehaviorSelect, TableBehaviorFilter, TableBehaviorOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    TableBehaviorSelect,
    TableBehaviorFilter,
    TableBehaviorOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: tableBehaviorKeys.list(args),
    queryFn: () => getClient().tableBehavior.findMany(args).unwrap(),
  });
}
