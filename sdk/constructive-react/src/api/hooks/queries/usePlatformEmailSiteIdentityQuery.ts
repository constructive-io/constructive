/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformEmailSiteIdentityKeys } from '../query-keys';
import type {
  PlatformEmailSiteIdentitySelect,
  PlatformEmailSiteIdentityWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformEmailSiteIdentitySelect,
  PlatformEmailSiteIdentityWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformEmailSiteIdentityQueryKey = platformEmailSiteIdentityKeys.detail;
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformEmailSiteIdentityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformEmailSiteIdentityQuery<
  S extends PlatformEmailSiteIdentitySelect,
  TData = {
    platformEmailSiteIdentity: InferSelectResult<PlatformEmailSiteIdentityWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformEmailSiteIdentity: InferSelectResult<
          PlatformEmailSiteIdentityWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformEmailSiteIdentityQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformEmailSiteIdentitySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformEmailSiteIdentitySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformEmailSiteIdentityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformEmailSiteIdentity.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```ts
 * const data = await fetchPlatformEmailSiteIdentityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformEmailSiteIdentityQuery<
  S extends PlatformEmailSiteIdentitySelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
}): Promise<{
  platformEmailSiteIdentity: InferSelectResult<PlatformEmailSiteIdentityWithRelations, S> | null;
}>;
export async function fetchPlatformEmailSiteIdentityQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformEmailSiteIdentitySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformEmailSiteIdentitySelect>(params.selection);
  return getClient()
    .platformEmailSiteIdentity.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```ts
 * await prefetchPlatformEmailSiteIdentityQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformEmailSiteIdentityQuery<
  S extends PlatformEmailSiteIdentitySelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformEmailSiteIdentitySelect>;
  }
): Promise<void>;
export async function prefetchPlatformEmailSiteIdentityQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformEmailSiteIdentitySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformEmailSiteIdentitySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformEmailSiteIdentityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformEmailSiteIdentity.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
