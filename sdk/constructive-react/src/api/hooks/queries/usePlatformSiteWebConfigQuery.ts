/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformSiteWebConfigKeys } from '../query-keys';
import type {
  PlatformSiteWebConfigSelect,
  PlatformSiteWebConfigWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformSiteWebConfigSelect,
  PlatformSiteWebConfigWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformSiteWebConfigQueryKey = platformSiteWebConfigKeys.detail;
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformSiteWebConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformSiteWebConfigQuery<
  S extends PlatformSiteWebConfigSelect,
  TData = {
    platformSiteWebConfig: InferSelectResult<PlatformSiteWebConfigWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSiteWebConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformSiteWebConfig: InferSelectResult<PlatformSiteWebConfigWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformSiteWebConfigQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformSiteWebConfigSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformSiteWebConfigSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformSiteWebConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSiteWebConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```ts
 * const data = await fetchPlatformSiteWebConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformSiteWebConfigQuery<
  S extends PlatformSiteWebConfigSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformSiteWebConfigSelect>;
}): Promise<{
  platformSiteWebConfig: InferSelectResult<PlatformSiteWebConfigWithRelations, S> | null;
}>;
export async function fetchPlatformSiteWebConfigQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformSiteWebConfigSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformSiteWebConfigSelect>(params.selection);
  return getClient()
    .platformSiteWebConfig.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Static-site serving configuration for a site surface (index document, clean URLs, SPA fallback)
 *
 * @example
 * ```ts
 * await prefetchPlatformSiteWebConfigQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformSiteWebConfigQuery<S extends PlatformSiteWebConfigSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSiteWebConfigSelect>;
  }
): Promise<void>;
export async function prefetchPlatformSiteWebConfigQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformSiteWebConfigSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformSiteWebConfigSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformSiteWebConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSiteWebConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
