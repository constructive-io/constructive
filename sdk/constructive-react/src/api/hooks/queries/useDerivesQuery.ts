/**
 * List query hook for Derive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { deriveKeys } from '../query-keys';
import type {
  DeriveSelect,
  DeriveWithRelations,
  DeriveFilter,
  DeriveOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  DeriveSelect,
  DeriveWithRelations,
  DeriveFilter,
  DeriveOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const derivesQueryKey = deriveKeys.list;
/**
 * Query hook for fetching Derive list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDerivesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useDerivesQuery<
  S extends DeriveSelect,
  TData = {
    derives: ConnectionResult<InferSelectResult<DeriveWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DeriveFilter, DeriveOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DeriveSelect>;
  } & Omit<
    UseQueryOptions<
      {
        derives: ConnectionResult<InferSelectResult<DeriveWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDerivesQuery(
  params: {
    selection: ListSelectionConfig<DeriveSelect, DeriveFilter, DeriveOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<DeriveSelect, DeriveFilter, DeriveOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: deriveKeys.list(args),
    queryFn: () => getClient().derive.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch Derive list without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDerivesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchDerivesQuery<S extends DeriveSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, DeriveFilter, DeriveOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, DeriveSelect>;
}): Promise<{
  derives: ConnectionResult<InferSelectResult<DeriveWithRelations, S>>;
}>;
export async function fetchDerivesQuery(params: {
  selection: ListSelectionConfig<DeriveSelect, DeriveFilter, DeriveOrderBy>;
}) {
  const args = buildListSelectionArgs<DeriveSelect, DeriveFilter, DeriveOrderBy>(params.selection);
  return getClient().derive.findMany(args).unwrap();
}
/**
 * Prefetch Derive list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDerivesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchDerivesQuery<S extends DeriveSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DeriveFilter, DeriveOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DeriveSelect>;
  }
): Promise<void>;
export async function prefetchDerivesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<DeriveSelect, DeriveFilter, DeriveOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<DeriveSelect, DeriveFilter, DeriveOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: deriveKeys.list(args),
    queryFn: () => getClient().derive.findMany(args).unwrap(),
  });
}
