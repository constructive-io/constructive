/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { redirectKeys } from '../query-keys';
import type {
  RedirectSelect,
  RedirectWithRelations,
  RedirectFilter,
  RedirectOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  RedirectSelect,
  RedirectWithRelations,
  RedirectFilter,
  RedirectOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const redirectsQueryKey = redirectKeys.list;
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRedirectsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useRedirectsQuery<
  S extends RedirectSelect,
  TData = {
    redirects: ConnectionResult<InferSelectResult<RedirectWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RedirectFilter, RedirectOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RedirectSelect>;
  } & Omit<
    UseQueryOptions<
      {
        redirects: ConnectionResult<InferSelectResult<RedirectWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRedirectsQuery(
  params: {
    selection: ListSelectionConfig<RedirectSelect, RedirectFilter, RedirectOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<RedirectSelect, RedirectFilter, RedirectOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: redirectKeys.list(args),
    queryFn: () => getClient().redirect.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```ts
 * const data = await fetchRedirectsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchRedirectsQuery<S extends RedirectSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, RedirectFilter, RedirectOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, RedirectSelect>;
}): Promise<{
  redirects: ConnectionResult<InferSelectResult<RedirectWithRelations, S>>;
}>;
export async function fetchRedirectsQuery(params: {
  selection: ListSelectionConfig<RedirectSelect, RedirectFilter, RedirectOrderBy>;
}) {
  const args = buildListSelectionArgs<RedirectSelect, RedirectFilter, RedirectOrderBy>(
    params.selection
  );
  return getClient().redirect.findMany(args).unwrap();
}
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```ts
 * await prefetchRedirectsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchRedirectsQuery<S extends RedirectSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RedirectFilter, RedirectOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RedirectSelect>;
  }
): Promise<void>;
export async function prefetchRedirectsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<RedirectSelect, RedirectFilter, RedirectOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<RedirectSelect, RedirectFilter, RedirectOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: redirectKeys.list(args),
    queryFn: () => getClient().redirect.findMany(args).unwrap(),
  });
}
