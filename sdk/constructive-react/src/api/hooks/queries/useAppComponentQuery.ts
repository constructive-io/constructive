/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appComponentKeys } from '../query-keys';
import type { AppComponentSelect, AppComponentWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppComponentSelect, AppComponentWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appComponentQueryKey = appComponentKeys.detail;
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppComponentQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppComponentQuery<
  S extends AppComponentSelect,
  TData = {
    appComponent: InferSelectResult<AppComponentWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appComponent: InferSelectResult<AppComponentWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppComponentQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppComponentSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppComponentSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appComponentKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appComponent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```ts
 * const data = await fetchAppComponentQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppComponentQuery<S extends AppComponentSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppComponentSelect>;
}): Promise<{
  appComponent: InferSelectResult<AppComponentWithRelations, S> | null;
}>;
export async function fetchAppComponentQuery(params: {
  id: string;
  selection: SelectionConfig<AppComponentSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppComponentSelect>(params.selection);
  return getClient()
    .appComponent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```ts
 * await prefetchAppComponentQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppComponentQuery<S extends AppComponentSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  }
): Promise<void>;
export async function prefetchAppComponentQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppComponentSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppComponentSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appComponentKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appComponent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
