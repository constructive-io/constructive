/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteDeepLinkKeys } from '../query-keys';
import type { SiteDeepLinkSelect, SiteDeepLinkWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteDeepLinkSelect, SiteDeepLinkWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteDeepLinkQueryKey = siteDeepLinkKeys.detail;
/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteDeepLinkQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSiteDeepLinkQuery<
  S extends SiteDeepLinkSelect,
  TData = {
    siteDeepLink: InferSelectResult<SiteDeepLinkWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteDeepLinkSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteDeepLink: InferSelectResult<SiteDeepLinkWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteDeepLinkQuery(
  params: {
    id: string;
    selection: SelectionConfig<SiteDeepLinkSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SiteDeepLinkSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteDeepLinkKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteDeepLink.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 *
 * @example
 * ```ts
 * const data = await fetchSiteDeepLinkQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSiteDeepLinkQuery<S extends SiteDeepLinkSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SiteDeepLinkSelect>;
}): Promise<{
  siteDeepLink: InferSelectResult<SiteDeepLinkWithRelations, S> | null;
}>;
export async function fetchSiteDeepLinkQuery(params: {
  id: string;
  selection: SelectionConfig<SiteDeepLinkSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SiteDeepLinkSelect>(params.selection);
  return getClient()
    .siteDeepLink.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Named, retargetable deep links owned by a site surface (served at the deep-link path prefix; app interception via site_app_links)
 *
 * @example
 * ```ts
 * await prefetchSiteDeepLinkQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSiteDeepLinkQuery<S extends SiteDeepLinkSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteDeepLinkSelect>;
  }
): Promise<void>;
export async function prefetchSiteDeepLinkQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SiteDeepLinkSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SiteDeepLinkSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteDeepLinkKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteDeepLink.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
