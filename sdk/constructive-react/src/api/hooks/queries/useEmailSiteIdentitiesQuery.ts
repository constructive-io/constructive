/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { emailSiteIdentityKeys } from '../query-keys';
import type {
  EmailSiteIdentitySelect,
  EmailSiteIdentityWithRelations,
  EmailSiteIdentityFilter,
  EmailSiteIdentityOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  EmailSiteIdentitySelect,
  EmailSiteIdentityWithRelations,
  EmailSiteIdentityFilter,
  EmailSiteIdentityOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const emailSiteIdentitiesQueryKey = emailSiteIdentityKeys.list;
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailSiteIdentitiesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useEmailSiteIdentitiesQuery<
  S extends EmailSiteIdentitySelect,
  TData = {
    emailSiteIdentities: ConnectionResult<InferSelectResult<EmailSiteIdentityWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, EmailSiteIdentityFilter, EmailSiteIdentityOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, EmailSiteIdentitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        emailSiteIdentities: ConnectionResult<InferSelectResult<EmailSiteIdentityWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEmailSiteIdentitiesQuery(
  params: {
    selection: ListSelectionConfig<
      EmailSiteIdentitySelect,
      EmailSiteIdentityFilter,
      EmailSiteIdentityOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    EmailSiteIdentitySelect,
    EmailSiteIdentityFilter,
    EmailSiteIdentityOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: emailSiteIdentityKeys.list(args),
    queryFn: () => getClient().emailSiteIdentity.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```ts
 * const data = await fetchEmailSiteIdentitiesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchEmailSiteIdentitiesQuery<S extends EmailSiteIdentitySelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, EmailSiteIdentityFilter, EmailSiteIdentityOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, EmailSiteIdentitySelect>;
}): Promise<{
  emailSiteIdentities: ConnectionResult<InferSelectResult<EmailSiteIdentityWithRelations, S>>;
}>;
export async function fetchEmailSiteIdentitiesQuery(params: {
  selection: ListSelectionConfig<
    EmailSiteIdentitySelect,
    EmailSiteIdentityFilter,
    EmailSiteIdentityOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    EmailSiteIdentitySelect,
    EmailSiteIdentityFilter,
    EmailSiteIdentityOrderBy
  >(params.selection);
  return getClient().emailSiteIdentity.findMany(args).unwrap();
}
/**
 * Binds a site to the identity it sends as. Unique on site_id: one identity per site, but many sites may share an identity.
 *
 * @example
 * ```ts
 * await prefetchEmailSiteIdentitiesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchEmailSiteIdentitiesQuery<S extends EmailSiteIdentitySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, EmailSiteIdentityFilter, EmailSiteIdentityOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, EmailSiteIdentitySelect>;
  }
): Promise<void>;
export async function prefetchEmailSiteIdentitiesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      EmailSiteIdentitySelect,
      EmailSiteIdentityFilter,
      EmailSiteIdentityOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    EmailSiteIdentitySelect,
    EmailSiteIdentityFilter,
    EmailSiteIdentityOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: emailSiteIdentityKeys.list(args),
    queryFn: () => getClient().emailSiteIdentity.findMany(args).unwrap(),
  });
}
