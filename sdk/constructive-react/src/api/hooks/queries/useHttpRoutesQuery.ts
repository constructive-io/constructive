/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { httpRouteKeys } from '../query-keys';
import type {
  HttpRouteSelect,
  HttpRouteWithRelations,
  HttpRouteFilter,
  HttpRouteOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  HttpRouteSelect,
  HttpRouteWithRelations,
  HttpRouteFilter,
  HttpRouteOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const httpRoutesQueryKey = httpRouteKeys.list;
/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useHttpRoutesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useHttpRoutesQuery<
  S extends HttpRouteSelect,
  TData = {
    httpRoutes: ConnectionResult<InferSelectResult<HttpRouteWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, HttpRouteFilter, HttpRouteOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, HttpRouteSelect>;
  } & Omit<
    UseQueryOptions<
      {
        httpRoutes: ConnectionResult<InferSelectResult<HttpRouteWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useHttpRoutesQuery(
  params: {
    selection: ListSelectionConfig<HttpRouteSelect, HttpRouteFilter, HttpRouteOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<HttpRouteSelect, HttpRouteFilter, HttpRouteOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: httpRouteKeys.list(args),
    queryFn: () => getClient().httpRoute.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 *
 * @example
 * ```ts
 * const data = await fetchHttpRoutesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchHttpRoutesQuery<S extends HttpRouteSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, HttpRouteFilter, HttpRouteOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, HttpRouteSelect>;
}): Promise<{
  httpRoutes: ConnectionResult<InferSelectResult<HttpRouteWithRelations, S>>;
}>;
export async function fetchHttpRoutesQuery(params: {
  selection: ListSelectionConfig<HttpRouteSelect, HttpRouteFilter, HttpRouteOrderBy>;
}) {
  const args = buildListSelectionArgs<HttpRouteSelect, HttpRouteFilter, HttpRouteOrderBy>(
    params.selection
  );
  return getClient().httpRoute.findMany(args).unwrap();
}
/**
 * Request-time HTTP routing authority: registered domain plus path prefix and optional method to a typed target
 *
 * @example
 * ```ts
 * await prefetchHttpRoutesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchHttpRoutesQuery<S extends HttpRouteSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, HttpRouteFilter, HttpRouteOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, HttpRouteSelect>;
  }
): Promise<void>;
export async function prefetchHttpRoutesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<HttpRouteSelect, HttpRouteFilter, HttpRouteOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<HttpRouteSelect, HttpRouteFilter, HttpRouteOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: httpRouteKeys.list(args),
    queryFn: () => getClient().httpRoute.findMany(args).unwrap(),
  });
}
