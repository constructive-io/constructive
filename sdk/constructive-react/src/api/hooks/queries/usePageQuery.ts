/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { pageKeys } from '../query-keys';
import type { PageSelect, PageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PageSelect, PageWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const pageQueryKey = pageKeys.detail;
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePageQuery<
  S extends PageSelect,
  TData = {
    page: InferSelectResult<PageWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        page: InferSelectResult<PageWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePageQuery(
  params: {
    id: string;
    selection: SelectionConfig<PageSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PageSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: pageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .page.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```ts
 * const data = await fetchPageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPageQuery<S extends PageSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PageSelect>;
}): Promise<{
  page: InferSelectResult<PageWithRelations, S> | null;
}>;
export async function fetchPageQuery(params: {
  id: string;
  selection: SelectionConfig<PageSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PageSelect>(params.selection);
  return getClient()
    .page.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```ts
 * await prefetchPageQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPageQuery<S extends PageSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PageSelect>;
  }
): Promise<void>;
export async function prefetchPageQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PageSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PageSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: pageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .page.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
