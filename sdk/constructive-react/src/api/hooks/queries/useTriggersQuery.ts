/**
 * List query hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { triggerKeys } from '../query-keys';
import type {
  TriggerSelect,
  TriggerWithRelations,
  TriggerFilter,
  TriggerOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  TriggerSelect,
  TriggerWithRelations,
  TriggerFilter,
  TriggerOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const triggersQueryKey = triggerKeys.list;
/**
 * Query hook for fetching Trigger list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTriggersQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useTriggersQuery<
  S extends TriggerSelect,
  TData = {
    triggers: ConnectionResult<InferSelectResult<TriggerWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, TriggerFilter, TriggerOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, TriggerSelect>;
  } & Omit<
    UseQueryOptions<
      {
        triggers: ConnectionResult<InferSelectResult<TriggerWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTriggersQuery(
  params: {
    selection: ListSelectionConfig<TriggerSelect, TriggerFilter, TriggerOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<TriggerSelect, TriggerFilter, TriggerOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: triggerKeys.list(args),
    queryFn: () => getClient().trigger.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch Trigger list without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTriggersQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchTriggersQuery<S extends TriggerSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, TriggerFilter, TriggerOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, TriggerSelect>;
}): Promise<{
  triggers: ConnectionResult<InferSelectResult<TriggerWithRelations, S>>;
}>;
export async function fetchTriggersQuery(params: {
  selection: ListSelectionConfig<TriggerSelect, TriggerFilter, TriggerOrderBy>;
}) {
  const args = buildListSelectionArgs<TriggerSelect, TriggerFilter, TriggerOrderBy>(
    params.selection
  );
  return getClient().trigger.findMany(args).unwrap();
}
/**
 * Prefetch Trigger list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTriggersQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchTriggersQuery<S extends TriggerSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, TriggerFilter, TriggerOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, TriggerSelect>;
  }
): Promise<void>;
export async function prefetchTriggersQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<TriggerSelect, TriggerFilter, TriggerOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<TriggerSelect, TriggerFilter, TriggerOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: triggerKeys.list(args),
    queryFn: () => getClient().trigger.findMany(args).unwrap(),
  });
}
