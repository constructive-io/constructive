/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appComponentKeys } from '../query-keys';
import type {
  AppComponentSelect,
  AppComponentWithRelations,
  AppComponentFilter,
  AppComponentOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppComponentSelect,
  AppComponentWithRelations,
  AppComponentFilter,
  AppComponentOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appComponentsQueryKey = appComponentKeys.list;
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppComponentsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppComponentsQuery<
  S extends AppComponentSelect,
  TData = {
    appComponents: ConnectionResult<InferSelectResult<AppComponentWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppComponentFilter, AppComponentOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appComponents: ConnectionResult<InferSelectResult<AppComponentWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppComponentsQuery(
  params: {
    selection: ListSelectionConfig<AppComponentSelect, AppComponentFilter, AppComponentOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AppComponentSelect, AppComponentFilter, AppComponentOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appComponentKeys.list(args),
    queryFn: () => getClient().appComponent.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```ts
 * const data = await fetchAppComponentsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppComponentsQuery<S extends AppComponentSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppComponentFilter, AppComponentOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppComponentSelect>;
}): Promise<{
  appComponents: ConnectionResult<InferSelectResult<AppComponentWithRelations, S>>;
}>;
export async function fetchAppComponentsQuery(params: {
  selection: ListSelectionConfig<AppComponentSelect, AppComponentFilter, AppComponentOrderBy>;
}) {
  const args = buildListSelectionArgs<AppComponentSelect, AppComponentFilter, AppComponentOrderBy>(
    params.selection
  );
  return getClient().appComponent.findMany(args).unwrap();
}
/**
 * App component rows binding an app to typed catalog rows (exactly one typed component reference per row)
 *
 * @example
 * ```ts
 * await prefetchAppComponentsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppComponentsQuery<S extends AppComponentSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppComponentFilter, AppComponentOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppComponentSelect>;
  }
): Promise<void>;
export async function prefetchAppComponentsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AppComponentSelect, AppComponentFilter, AppComponentOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AppComponentSelect, AppComponentFilter, AppComponentOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: appComponentKeys.list(args),
    queryFn: () => getClient().appComponent.findMany(args).unwrap(),
  });
}
