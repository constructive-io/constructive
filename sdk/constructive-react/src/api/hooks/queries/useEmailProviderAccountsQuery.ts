/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { emailProviderAccountKeys } from '../query-keys';
import type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
  EmailProviderAccountFilter,
  EmailProviderAccountOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
  EmailProviderAccountFilter,
  EmailProviderAccountOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const emailProviderAccountsQueryKey = emailProviderAccountKeys.list;
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailProviderAccountsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useEmailProviderAccountsQuery<
  S extends EmailProviderAccountSelect,
  TData = {
    emailProviderAccounts: ConnectionResult<
      InferSelectResult<EmailProviderAccountWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, EmailProviderAccountFilter, EmailProviderAccountOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
  } & Omit<
    UseQueryOptions<
      {
        emailProviderAccounts: ConnectionResult<
          InferSelectResult<EmailProviderAccountWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEmailProviderAccountsQuery(
  params: {
    selection: ListSelectionConfig<
      EmailProviderAccountSelect,
      EmailProviderAccountFilter,
      EmailProviderAccountOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    EmailProviderAccountSelect,
    EmailProviderAccountFilter,
    EmailProviderAccountOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: emailProviderAccountKeys.list(args),
    queryFn: () => getClient().emailProviderAccount.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```ts
 * const data = await fetchEmailProviderAccountsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchEmailProviderAccountsQuery<
  S extends EmailProviderAccountSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, EmailProviderAccountFilter, EmailProviderAccountOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
}): Promise<{
  emailProviderAccounts: ConnectionResult<InferSelectResult<EmailProviderAccountWithRelations, S>>;
}>;
export async function fetchEmailProviderAccountsQuery(params: {
  selection: ListSelectionConfig<
    EmailProviderAccountSelect,
    EmailProviderAccountFilter,
    EmailProviderAccountOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    EmailProviderAccountSelect,
    EmailProviderAccountFilter,
    EmailProviderAccountOrderBy
  >(params.selection);
  return getClient().emailProviderAccount.findMany(args).unwrap();
}
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```ts
 * await prefetchEmailProviderAccountsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchEmailProviderAccountsQuery<S extends EmailProviderAccountSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, EmailProviderAccountFilter, EmailProviderAccountOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
  }
): Promise<void>;
export async function prefetchEmailProviderAccountsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      EmailProviderAccountSelect,
      EmailProviderAccountFilter,
      EmailProviderAccountOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    EmailProviderAccountSelect,
    EmailProviderAccountFilter,
    EmailProviderAccountOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: emailProviderAccountKeys.list(args),
    queryFn: () => getClient().emailProviderAccount.findMany(args).unwrap(),
  });
}
