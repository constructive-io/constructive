/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { emailProviderAccountKeys } from '../query-keys';
import type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  EmailProviderAccountSelect,
  EmailProviderAccountWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const emailProviderAccountQueryKey = emailProviderAccountKeys.detail;
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEmailProviderAccountQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useEmailProviderAccountQuery<
  S extends EmailProviderAccountSelect,
  TData = {
    emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
  } & Omit<
    UseQueryOptions<
      {
        emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEmailProviderAccountQuery(
  params: {
    id: string;
    selection: SelectionConfig<EmailProviderAccountSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<EmailProviderAccountSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: emailProviderAccountKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .emailProviderAccount.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```ts
 * const data = await fetchEmailProviderAccountQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchEmailProviderAccountQuery<S extends EmailProviderAccountSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
}): Promise<{
  emailProviderAccount: InferSelectResult<EmailProviderAccountWithRelations, S> | null;
}>;
export async function fetchEmailProviderAccountQuery(params: {
  id: string;
  selection: SelectionConfig<EmailProviderAccountSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<EmailProviderAccountSelect>(params.selection);
  return getClient()
    .emailProviderAccount.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * A tenant's configured account at an email provider: provider slug, endpoint coordinates, and the NAME of the secret holding its credentials (never the credential itself)
 *
 * @example
 * ```ts
 * await prefetchEmailProviderAccountQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchEmailProviderAccountQuery<S extends EmailProviderAccountSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, EmailProviderAccountSelect>;
  }
): Promise<void>;
export async function prefetchEmailProviderAccountQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<EmailProviderAccountSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<EmailProviderAccountSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: emailProviderAccountKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .emailProviderAccount.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
