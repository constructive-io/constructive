/**
 * Compiled hostname index maintained by domain sync triggers; read only through the resolver
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { hostnameBindingKeys } from '../query-keys';
import type { HostnameBindingSelect, HostnameBindingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { HostnameBindingSelect, HostnameBindingWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const hostnameBindingQueryKey = hostnameBindingKeys.detail;
/**
 * Compiled hostname index maintained by domain sync triggers; read only through the resolver
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useHostnameBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useHostnameBindingQuery<
  S extends HostnameBindingSelect,
  TData = {
    hostnameBinding: InferSelectResult<HostnameBindingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, HostnameBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        hostnameBinding: InferSelectResult<HostnameBindingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useHostnameBindingQuery(
  params: {
    id: string;
    selection: SelectionConfig<HostnameBindingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<HostnameBindingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: hostnameBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .hostnameBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Compiled hostname index maintained by domain sync triggers; read only through the resolver
 *
 * @example
 * ```ts
 * const data = await fetchHostnameBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchHostnameBindingQuery<S extends HostnameBindingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, HostnameBindingSelect>;
}): Promise<{
  hostnameBinding: InferSelectResult<HostnameBindingWithRelations, S> | null;
}>;
export async function fetchHostnameBindingQuery(params: {
  id: string;
  selection: SelectionConfig<HostnameBindingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<HostnameBindingSelect>(params.selection);
  return getClient()
    .hostnameBinding.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Compiled hostname index maintained by domain sync triggers; read only through the resolver
 *
 * @example
 * ```ts
 * await prefetchHostnameBindingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchHostnameBindingQuery<S extends HostnameBindingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, HostnameBindingSelect>;
  }
): Promise<void>;
export async function prefetchHostnameBindingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<HostnameBindingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<HostnameBindingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: hostnameBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .hostnameBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
