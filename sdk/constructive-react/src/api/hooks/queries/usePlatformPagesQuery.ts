/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformPageKeys } from '../query-keys';
import type {
  PlatformPageSelect,
  PlatformPageWithRelations,
  PlatformPageFilter,
  PlatformPageOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformPageSelect,
  PlatformPageWithRelations,
  PlatformPageFilter,
  PlatformPageOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformPagesQueryKey = platformPageKeys.list;
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformPagesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformPagesQuery<
  S extends PlatformPageSelect,
  TData = {
    platformPages: ConnectionResult<InferSelectResult<PlatformPageWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformPageFilter, PlatformPageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformPages: ConnectionResult<InferSelectResult<PlatformPageWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformPagesQuery(
  params: {
    selection: ListSelectionConfig<PlatformPageSelect, PlatformPageFilter, PlatformPageOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<PlatformPageSelect, PlatformPageFilter, PlatformPageOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformPageKeys.list(args),
    queryFn: () => getClient().platformPage.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```ts
 * const data = await fetchPlatformPagesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformPagesQuery<S extends PlatformPageSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlatformPageFilter, PlatformPageOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
}): Promise<{
  platformPages: ConnectionResult<InferSelectResult<PlatformPageWithRelations, S>>;
}>;
export async function fetchPlatformPagesQuery(params: {
  selection: ListSelectionConfig<PlatformPageSelect, PlatformPageFilter, PlatformPageOrderBy>;
}) {
  const args = buildListSelectionArgs<PlatformPageSelect, PlatformPageFilter, PlatformPageOrderBy>(
    params.selection
  );
  return getClient().platformPage.findMany(args).unwrap();
}
/**
 * Site-owned page content — merkle-versioned head over the infra store; never a routing surface
 *
 * @example
 * ```ts
 * await prefetchPlatformPagesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformPagesQuery<S extends PlatformPageSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformPageFilter, PlatformPageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformPageSelect>;
  }
): Promise<void>;
export async function prefetchPlatformPagesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<PlatformPageSelect, PlatformPageFilter, PlatformPageOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<PlatformPageSelect, PlatformPageFilter, PlatformPageOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: platformPageKeys.list(args),
    queryFn: () => getClient().platformPage.findMany(args).unwrap(),
  });
}
