/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { redirectKeys } from '../query-keys';
import type { RedirectSelect, RedirectWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RedirectSelect, RedirectWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const redirectQueryKey = redirectKeys.detail;
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRedirectQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRedirectQuery<
  S extends RedirectSelect,
  TData = {
    redirect: InferSelectResult<RedirectWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RedirectSelect>;
  } & Omit<
    UseQueryOptions<
      {
        redirect: InferSelectResult<RedirectWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRedirectQuery(
  params: {
    id: string;
    selection: SelectionConfig<RedirectSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RedirectSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: redirectKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .redirect.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```ts
 * const data = await fetchRedirectQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRedirectQuery<S extends RedirectSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RedirectSelect>;
}): Promise<{
  redirect: InferSelectResult<RedirectWithRelations, S> | null;
}>;
export async function fetchRedirectQuery(params: {
  id: string;
  selection: SelectionConfig<RedirectSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RedirectSelect>(params.selection);
  return getClient()
    .redirect.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Redirect targets a route can point at; the edge answers with a redirect status instead of a backend
 *
 * @example
 * ```ts
 * await prefetchRedirectQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRedirectQuery<S extends RedirectSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RedirectSelect>;
  }
): Promise<void>;
export async function prefetchRedirectQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RedirectSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RedirectSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: redirectKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .redirect.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
