/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { userSettingKeys } from '../query-keys';
import type {
  UserSettingSelect,
  UserSettingWithRelations,
  UserSettingFilter,
  UserSettingOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  UserSettingSelect,
  UserSettingWithRelations,
  UserSettingFilter,
  UserSettingOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const userSettingsQueryKey = userSettingKeys.list;
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserSettingsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useUserSettingsQuery<
  S extends UserSettingSelect,
  TData = {
    userSettings: ConnectionResult<InferSelectResult<UserSettingWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, UserSettingFilter, UserSettingOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, UserSettingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        userSettings: ConnectionResult<InferSelectResult<UserSettingWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useUserSettingsQuery(
  params: {
    selection: ListSelectionConfig<UserSettingSelect, UserSettingFilter, UserSettingOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<UserSettingSelect, UserSettingFilter, UserSettingOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: userSettingKeys.list(args),
    queryFn: () => getClient().userSetting.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```ts
 * const data = await fetchUserSettingsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchUserSettingsQuery<S extends UserSettingSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, UserSettingFilter, UserSettingOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, UserSettingSelect>;
}): Promise<{
  userSettings: ConnectionResult<InferSelectResult<UserSettingWithRelations, S>>;
}>;
export async function fetchUserSettingsQuery(params: {
  selection: ListSelectionConfig<UserSettingSelect, UserSettingFilter, UserSettingOrderBy>;
}) {
  const args = buildListSelectionArgs<UserSettingSelect, UserSettingFilter, UserSettingOrderBy>(
    params.selection
  );
  return getClient().userSetting.findMany(args).unwrap();
}
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```ts
 * await prefetchUserSettingsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchUserSettingsQuery<S extends UserSettingSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, UserSettingFilter, UserSettingOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, UserSettingSelect>;
  }
): Promise<void>;
export async function prefetchUserSettingsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<UserSettingSelect, UserSettingFilter, UserSettingOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<UserSettingSelect, UserSettingFilter, UserSettingOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: userSettingKeys.list(args),
    queryFn: () => getClient().userSetting.findMany(args).unwrap(),
  });
}
