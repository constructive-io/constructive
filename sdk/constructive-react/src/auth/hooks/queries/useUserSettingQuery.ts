/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { userSettingKeys } from '../query-keys';
import type { UserSettingSelect, UserSettingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { UserSettingSelect, UserSettingWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const userSettingQueryKey = userSettingKeys.detail;
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUserSettingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useUserSettingQuery<
  S extends UserSettingSelect,
  TData = {
    userSetting: InferSelectResult<UserSettingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, UserSettingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        userSetting: InferSelectResult<UserSettingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useUserSettingQuery(
  params: {
    id: string;
    selection: SelectionConfig<UserSettingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<UserSettingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: userSettingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .userSetting.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```ts
 * const data = await fetchUserSettingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchUserSettingQuery<S extends UserSettingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, UserSettingSelect>;
}): Promise<{
  userSetting: InferSelectResult<UserSettingWithRelations, S> | null;
}>;
export async function fetchUserSettingQuery(params: {
  id: string;
  selection: SelectionConfig<UserSettingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<UserSettingSelect>(params.selection);
  return getClient()
    .userSetting.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```ts
 * await prefetchUserSettingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchUserSettingQuery<S extends UserSettingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, UserSettingSelect>;
  }
): Promise<void>;
export async function prefetchUserSettingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<UserSettingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<UserSettingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: userSettingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .userSetting.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
