/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { userSettingKeys } from '../query-keys';
import { userSettingMutationKeys } from '../mutation-keys';
import type {
  UserSettingSelect,
  UserSettingWithRelations,
  UserSettingPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  UserSettingSelect,
  UserSettingWithRelations,
  UserSettingPatch,
} from '../../orm/input-types';
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateUserSettingMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', userSettingPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateUserSettingMutation<S extends UserSettingSelect>(
  params: {
    selection: {
      fields: S & UserSettingSelect;
    } & HookStrictSelect<NoInfer<S>, UserSettingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateUserSetting: {
          userSetting: InferSelectResult<UserSettingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        userSettingPatch: UserSettingPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateUserSetting: {
      userSetting: InferSelectResult<UserSettingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    userSettingPatch: UserSettingPatch;
  }
>;
export function useUpdateUserSettingMutation(
  params: {
    selection: SelectionConfig<UserSettingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        userSettingPatch: UserSettingPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<UserSettingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: userSettingMutationKeys.all,
    mutationFn: ({ id, userSettingPatch }: { id: string; userSettingPatch: UserSettingPatch }) =>
      getClient()
        .userSetting.update({
          where: {
            id,
          },
          data: userSettingPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: userSettingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: userSettingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
