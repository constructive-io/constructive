/**
 * Cryptocurrency wallet addresses owned by users, with network-specific validation and verification
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { cryptoAddressKeys } from '../query-keys';
import { cryptoAddressMutationKeys } from '../mutation-keys';
import type {
  CryptoAddressSelect,
  CryptoAddressWithRelations,
  CreateCryptoAddressInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  CryptoAddressSelect,
  CryptoAddressWithRelations,
  CreateCryptoAddressInput,
} from '../../orm/input-types';
/**
 * Cryptocurrency wallet addresses owned by users, with network-specific validation and verification
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCryptoAddressMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateCryptoAddressMutation<S extends CryptoAddressSelect>(
  params: {
    selection: {
      fields: S & CryptoAddressSelect;
    } & HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createCryptoAddress: {
          cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S>;
        };
      },
      Error,
      CreateCryptoAddressInput['cryptoAddress']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createCryptoAddress: {
      cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S>;
    };
  },
  Error,
  CreateCryptoAddressInput['cryptoAddress']
>;
export function useCreateCryptoAddressMutation(
  params: {
    selection: SelectionConfig<CryptoAddressSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateCryptoAddressInput['cryptoAddress']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<CryptoAddressSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: cryptoAddressMutationKeys.create(),
    mutationFn: (data: CreateCryptoAddressInput['cryptoAddress']) =>
      getClient()
        .cryptoAddress.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: cryptoAddressKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
