/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { userSettingKeys } from '../query-keys';
import { userSettingMutationKeys } from '../mutation-keys';
import type { UserSettingSelect, UserSettingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { UserSettingSelect, UserSettingWithRelations } from '../../orm/input-types';
/**
 * Per-user settings and preferences. Extended by other modules (i18n, notifications, MFA) via metaschema.create_field().
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteUserSettingMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteUserSettingMutation<S extends UserSettingSelect>(
  params: {
    selection: {
      fields: S & UserSettingSelect;
    } & HookStrictSelect<NoInfer<S>, UserSettingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteUserSetting: {
          userSetting: InferSelectResult<UserSettingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteUserSetting: {
      userSetting: InferSelectResult<UserSettingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteUserSettingMutation(
  params: {
    selection: SelectionConfig<UserSettingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<UserSettingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: userSettingMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .userSetting.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: userSettingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: userSettingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
