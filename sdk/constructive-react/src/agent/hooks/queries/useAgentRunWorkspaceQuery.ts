/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentRunWorkspaceKeys } from '../query-keys';
import type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentRunWorkspaceQueryKey = agentRunWorkspaceKeys.detail;
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentRunWorkspaceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAgentRunWorkspaceQuery<
  S extends AgentRunWorkspaceSelect,
  TData = {
    agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentRunWorkspaceQuery(
  params: {
    id: string;
    selection: SelectionConfig<AgentRunWorkspaceSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AgentRunWorkspaceSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentRunWorkspaceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentRunWorkspace.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```ts
 * const data = await fetchAgentRunWorkspaceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAgentRunWorkspaceQuery<S extends AgentRunWorkspaceSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
}): Promise<{
  agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S> | null;
}>;
export async function fetchAgentRunWorkspaceQuery(params: {
  id: string;
  selection: SelectionConfig<AgentRunWorkspaceSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AgentRunWorkspaceSelect>(params.selection);
  return getClient()
    .agentRunWorkspace.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```ts
 * await prefetchAgentRunWorkspaceQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAgentRunWorkspaceQuery<S extends AgentRunWorkspaceSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
  }
): Promise<void>;
export async function prefetchAgentRunWorkspaceQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AgentRunWorkspaceSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AgentRunWorkspaceSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: agentRunWorkspaceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentRunWorkspace.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
