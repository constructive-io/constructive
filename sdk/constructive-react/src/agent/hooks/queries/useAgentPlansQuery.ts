/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { agentPlanKeys } from '../query-keys';
import type {
  AgentPlanSelect,
  AgentPlanWithRelations,
  AgentPlanFilter,
  AgentPlanOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AgentPlanSelect,
  AgentPlanWithRelations,
  AgentPlanFilter,
  AgentPlanOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentPlansQueryKey = agentPlanKeys.list;
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentPlansQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAgentPlansQuery<
  S extends AgentPlanSelect,
  TData = {
    agentPlans: ConnectionResult<InferSelectResult<AgentPlanWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentPlanFilter, AgentPlanOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentPlanSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentPlans: ConnectionResult<InferSelectResult<AgentPlanWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentPlansQuery(
  params: {
    selection: ListSelectionConfig<AgentPlanSelect, AgentPlanFilter, AgentPlanOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AgentPlanSelect, AgentPlanFilter, AgentPlanOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentPlanKeys.list(args),
    queryFn: () => getClient().agentPlan.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```ts
 * const data = await fetchAgentPlansQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAgentPlansQuery<S extends AgentPlanSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AgentPlanFilter, AgentPlanOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AgentPlanSelect>;
}): Promise<{
  agentPlans: ConnectionResult<InferSelectResult<AgentPlanWithRelations, S>>;
}>;
export async function fetchAgentPlansQuery(params: {
  selection: ListSelectionConfig<AgentPlanSelect, AgentPlanFilter, AgentPlanOrderBy>;
}) {
  const args = buildListSelectionArgs<AgentPlanSelect, AgentPlanFilter, AgentPlanOrderBy>(
    params.selection
  );
  return getClient().agentPlan.findMany(args).unwrap();
}
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```ts
 * await prefetchAgentPlansQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAgentPlansQuery<S extends AgentPlanSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentPlanFilter, AgentPlanOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentPlanSelect>;
  }
): Promise<void>;
export async function prefetchAgentPlansQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AgentPlanSelect, AgentPlanFilter, AgentPlanOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AgentPlanSelect, AgentPlanFilter, AgentPlanOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: agentPlanKeys.list(args),
    queryFn: () => getClient().agentPlan.findMany(args).unwrap(),
  });
}
