/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentRunKeys } from '../query-keys';
import type { PlatformAgentRunSelect, PlatformAgentRunWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformAgentRunSelect, PlatformAgentRunWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentRunQueryKey = platformAgentRunKeys.detail;
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentRunQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentRunQuery<
  S extends PlatformAgentRunSelect,
  TData = {
    platformAgentRun: InferSelectResult<PlatformAgentRunWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentRunSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentRun: InferSelectResult<PlatformAgentRunWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentRunQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentRunSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentRunSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentRunKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentRun.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentRunQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentRunQuery<S extends PlatformAgentRunSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentRunSelect>;
}): Promise<{
  platformAgentRun: InferSelectResult<PlatformAgentRunWithRelations, S> | null;
}>;
export async function fetchPlatformAgentRunQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentRunSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentRunSelect>(params.selection);
  return getClient()
    .platformAgentRun.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentRunQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentRunQuery<S extends PlatformAgentRunSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentRunSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentRunQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentRunSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentRunSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentRunKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentRun.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
