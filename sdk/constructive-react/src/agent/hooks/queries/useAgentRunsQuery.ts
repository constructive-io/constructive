/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { agentRunKeys } from '../query-keys';
import type {
  AgentRunSelect,
  AgentRunWithRelations,
  AgentRunFilter,
  AgentRunOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AgentRunSelect,
  AgentRunWithRelations,
  AgentRunFilter,
  AgentRunOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentRunsQueryKey = agentRunKeys.list;
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentRunsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAgentRunsQuery<
  S extends AgentRunSelect,
  TData = {
    agentRuns: ConnectionResult<InferSelectResult<AgentRunWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentRunFilter, AgentRunOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentRunSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentRuns: ConnectionResult<InferSelectResult<AgentRunWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentRunsQuery(
  params: {
    selection: ListSelectionConfig<AgentRunSelect, AgentRunFilter, AgentRunOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AgentRunSelect, AgentRunFilter, AgentRunOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentRunKeys.list(args),
    queryFn: () => getClient().agentRun.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```ts
 * const data = await fetchAgentRunsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAgentRunsQuery<S extends AgentRunSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AgentRunFilter, AgentRunOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AgentRunSelect>;
}): Promise<{
  agentRuns: ConnectionResult<InferSelectResult<AgentRunWithRelations, S>>;
}>;
export async function fetchAgentRunsQuery(params: {
  selection: ListSelectionConfig<AgentRunSelect, AgentRunFilter, AgentRunOrderBy>;
}) {
  const args = buildListSelectionArgs<AgentRunSelect, AgentRunFilter, AgentRunOrderBy>(
    params.selection
  );
  return getClient().agentRun.findMany(args).unwrap();
}
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```ts
 * await prefetchAgentRunsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAgentRunsQuery<S extends AgentRunSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentRunFilter, AgentRunOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentRunSelect>;
  }
): Promise<void>;
export async function prefetchAgentRunsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AgentRunSelect, AgentRunFilter, AgentRunOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AgentRunSelect, AgentRunFilter, AgentRunOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: agentRunKeys.list(args),
    queryFn: () => getClient().agentRun.findMany(args).unwrap(),
  });
}
