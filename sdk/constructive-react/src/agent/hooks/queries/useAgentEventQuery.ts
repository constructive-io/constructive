/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentEventKeys } from '../query-keys';
import type { AgentEventSelect, AgentEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentEventSelect, AgentEventWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentEventQueryKey = agentEventKeys.detail;
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAgentEventQuery<
  S extends AgentEventSelect,
  TData = {
    agentEvent: InferSelectResult<AgentEventWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentEvent: InferSelectResult<AgentEventWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentEventQuery(
  params: {
    id: string;
    selection: SelectionConfig<AgentEventSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AgentEventSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```ts
 * const data = await fetchAgentEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAgentEventQuery<S extends AgentEventSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AgentEventSelect>;
}): Promise<{
  agentEvent: InferSelectResult<AgentEventWithRelations, S> | null;
}>;
export async function fetchAgentEventQuery(params: {
  id: string;
  selection: SelectionConfig<AgentEventSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AgentEventSelect>(params.selection);
  return getClient()
    .agentEvent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```ts
 * await prefetchAgentEventQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAgentEventQuery<S extends AgentEventSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentEventSelect>;
  }
): Promise<void>;
export async function prefetchAgentEventQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AgentEventSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AgentEventSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: agentEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
