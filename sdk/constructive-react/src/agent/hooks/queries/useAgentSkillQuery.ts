/**
 * Structured procedural instructions for agent workflows
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentSkillKeys } from '../query-keys';
import type { AgentSkillSelect, AgentSkillWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentSkillSelect, AgentSkillWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentSkillQueryKey = agentSkillKeys.detail;
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentSkillQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAgentSkillQuery<
  S extends AgentSkillSelect,
  TData = {
    agentSkill: InferSelectResult<AgentSkillWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentSkill: InferSelectResult<AgentSkillWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentSkillQuery(
  params: {
    id: string;
    selection: SelectionConfig<AgentSkillSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AgentSkillSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentSkillKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentSkill.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```ts
 * const data = await fetchAgentSkillQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAgentSkillQuery<S extends AgentSkillSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
}): Promise<{
  agentSkill: InferSelectResult<AgentSkillWithRelations, S> | null;
}>;
export async function fetchAgentSkillQuery(params: {
  id: string;
  selection: SelectionConfig<AgentSkillSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AgentSkillSelect>(params.selection);
  return getClient()
    .agentSkill.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```ts
 * await prefetchAgentSkillQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAgentSkillQuery<S extends AgentSkillSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
  }
): Promise<void>;
export async function prefetchAgentSkillQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AgentSkillSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AgentSkillSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: agentSkillKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentSkill.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
