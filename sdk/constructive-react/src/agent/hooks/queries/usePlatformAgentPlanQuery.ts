/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentPlanKeys } from '../query-keys';
import type {
  PlatformAgentPlanSelect,
  PlatformAgentPlanWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentPlanSelect,
  PlatformAgentPlanWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentPlanQueryKey = platformAgentPlanKeys.detail;
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentPlanQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentPlanQuery<
  S extends PlatformAgentPlanSelect,
  TData = {
    platformAgentPlan: InferSelectResult<PlatformAgentPlanWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentPlanSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentPlan: InferSelectResult<PlatformAgentPlanWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentPlanQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentPlanSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentPlanSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentPlanKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentPlan.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentPlanQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentPlanQuery<S extends PlatformAgentPlanSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentPlanSelect>;
}): Promise<{
  platformAgentPlan: InferSelectResult<PlatformAgentPlanWithRelations, S> | null;
}>;
export async function fetchPlatformAgentPlanQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentPlanSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentPlanSelect>(params.selection);
  return getClient()
    .platformAgentPlan.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentPlanQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentPlanQuery<S extends PlatformAgentPlanSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentPlanSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentPlanQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentPlanSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentPlanSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentPlanKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentPlan.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
