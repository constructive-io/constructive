/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentRunKeys } from '../query-keys';
import type { AgentRunSelect, AgentRunWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentRunSelect, AgentRunWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentRunQueryKey = agentRunKeys.detail;
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentRunQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAgentRunQuery<
  S extends AgentRunSelect,
  TData = {
    agentRun: InferSelectResult<AgentRunWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentRunSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentRun: InferSelectResult<AgentRunWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentRunQuery(
  params: {
    id: string;
    selection: SelectionConfig<AgentRunSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AgentRunSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentRunKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentRun.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```ts
 * const data = await fetchAgentRunQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAgentRunQuery<S extends AgentRunSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AgentRunSelect>;
}): Promise<{
  agentRun: InferSelectResult<AgentRunWithRelations, S> | null;
}>;
export async function fetchAgentRunQuery(params: {
  id: string;
  selection: SelectionConfig<AgentRunSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AgentRunSelect>(params.selection);
  return getClient()
    .agentRun.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * One supervised agent run of a thread: its placement, workspace, cursor and artifacts
 *
 * @example
 * ```ts
 * await prefetchAgentRunQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAgentRunQuery<S extends AgentRunSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentRunSelect>;
  }
): Promise<void>;
export async function prefetchAgentRunQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AgentRunSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AgentRunSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: agentRunKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentRun.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
