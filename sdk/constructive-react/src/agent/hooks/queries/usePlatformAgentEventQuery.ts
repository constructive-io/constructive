/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentEventKeys } from '../query-keys';
import type {
  PlatformAgentEventSelect,
  PlatformAgentEventWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentEventSelect,
  PlatformAgentEventWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentEventQueryKey = platformAgentEventKeys.detail;
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentEventQuery<
  S extends PlatformAgentEventSelect,
  TData = {
    platformAgentEvent: InferSelectResult<PlatformAgentEventWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentEvent: InferSelectResult<PlatformAgentEventWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentEventQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentEventSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentEventSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentEventQuery<S extends PlatformAgentEventSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentEventSelect>;
}): Promise<{
  platformAgentEvent: InferSelectResult<PlatformAgentEventWithRelations, S> | null;
}>;
export async function fetchPlatformAgentEventQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentEventSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentEventSelect>(params.selection);
  return getClient()
    .platformAgentEvent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentEventQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentEventQuery<S extends PlatformAgentEventSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentEventSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentEventQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentEventSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentEventSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
