/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentRunWorkspaceKeys } from '../query-keys';
import type {
  PlatformAgentRunWorkspaceSelect,
  PlatformAgentRunWorkspaceWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentRunWorkspaceSelect,
  PlatformAgentRunWorkspaceWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentRunWorkspaceQueryKey = platformAgentRunWorkspaceKeys.detail;
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentRunWorkspaceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentRunWorkspaceQuery<
  S extends PlatformAgentRunWorkspaceSelect,
  TData = {
    platformAgentRunWorkspace: InferSelectResult<PlatformAgentRunWorkspaceWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentRunWorkspaceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentRunWorkspace: InferSelectResult<
          PlatformAgentRunWorkspaceWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentRunWorkspaceQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentRunWorkspaceSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentRunWorkspaceSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentRunWorkspaceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentRunWorkspace.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentRunWorkspaceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentRunWorkspaceQuery<
  S extends PlatformAgentRunWorkspaceSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentRunWorkspaceSelect>;
}): Promise<{
  platformAgentRunWorkspace: InferSelectResult<PlatformAgentRunWorkspaceWithRelations, S> | null;
}>;
export async function fetchPlatformAgentRunWorkspaceQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentRunWorkspaceSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentRunWorkspaceSelect>(params.selection);
  return getClient()
    .platformAgentRunWorkspace.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentRunWorkspaceQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentRunWorkspaceQuery<
  S extends PlatformAgentRunWorkspaceSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentRunWorkspaceSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentRunWorkspaceQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentRunWorkspaceSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentRunWorkspaceSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentRunWorkspaceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentRunWorkspace.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
