/**
 * Task within a plan, with ordering and optional approval gates
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentTaskKeys } from '../query-keys';
import type {
  PlatformAgentTaskSelect,
  PlatformAgentTaskWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentTaskSelect,
  PlatformAgentTaskWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentTaskQueryKey = platformAgentTaskKeys.detail;
/**
 * Task within a plan, with ordering and optional approval gates
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentTaskQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentTaskQuery<
  S extends PlatformAgentTaskSelect,
  TData = {
    platformAgentTask: InferSelectResult<PlatformAgentTaskWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentTaskSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentTask: InferSelectResult<PlatformAgentTaskWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentTaskQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentTaskSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentTaskSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentTaskKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentTask.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Task within a plan, with ordering and optional approval gates
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentTaskQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentTaskQuery<S extends PlatformAgentTaskSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentTaskSelect>;
}): Promise<{
  platformAgentTask: InferSelectResult<PlatformAgentTaskWithRelations, S> | null;
}>;
export async function fetchPlatformAgentTaskQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentTaskSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentTaskSelect>(params.selection);
  return getClient()
    .platformAgentTask.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Task within a plan, with ordering and optional approval gates
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentTaskQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentTaskQuery<S extends PlatformAgentTaskSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentTaskSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentTaskQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentTaskSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentTaskSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentTaskKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentTask.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
