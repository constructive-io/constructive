/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { agentRunWorkspaceKeys } from '../query-keys';
import type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
  AgentRunWorkspaceFilter,
  AgentRunWorkspaceOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
  AgentRunWorkspaceFilter,
  AgentRunWorkspaceOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentRunWorkspacesQueryKey = agentRunWorkspaceKeys.list;
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentRunWorkspacesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAgentRunWorkspacesQuery<
  S extends AgentRunWorkspaceSelect,
  TData = {
    agentRunWorkspaces: ConnectionResult<InferSelectResult<AgentRunWorkspaceWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentRunWorkspaceFilter, AgentRunWorkspaceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentRunWorkspaces: ConnectionResult<InferSelectResult<AgentRunWorkspaceWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentRunWorkspacesQuery(
  params: {
    selection: ListSelectionConfig<
      AgentRunWorkspaceSelect,
      AgentRunWorkspaceFilter,
      AgentRunWorkspaceOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    AgentRunWorkspaceSelect,
    AgentRunWorkspaceFilter,
    AgentRunWorkspaceOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentRunWorkspaceKeys.list(args),
    queryFn: () => getClient().agentRunWorkspace.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```ts
 * const data = await fetchAgentRunWorkspacesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAgentRunWorkspacesQuery<S extends AgentRunWorkspaceSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AgentRunWorkspaceFilter, AgentRunWorkspaceOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
}): Promise<{
  agentRunWorkspaces: ConnectionResult<InferSelectResult<AgentRunWorkspaceWithRelations, S>>;
}>;
export async function fetchAgentRunWorkspacesQuery(params: {
  selection: ListSelectionConfig<
    AgentRunWorkspaceSelect,
    AgentRunWorkspaceFilter,
    AgentRunWorkspaceOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    AgentRunWorkspaceSelect,
    AgentRunWorkspaceFilter,
    AgentRunWorkspaceOrderBy
  >(params.selection);
  return getClient().agentRunWorkspace.findMany(args).unwrap();
}
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```ts
 * await prefetchAgentRunWorkspacesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAgentRunWorkspacesQuery<S extends AgentRunWorkspaceSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentRunWorkspaceFilter, AgentRunWorkspaceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
  }
): Promise<void>;
export async function prefetchAgentRunWorkspacesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      AgentRunWorkspaceSelect,
      AgentRunWorkspaceFilter,
      AgentRunWorkspaceOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    AgentRunWorkspaceSelect,
    AgentRunWorkspaceFilter,
    AgentRunWorkspaceOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: agentRunWorkspaceKeys.list(args),
    queryFn: () => getClient().agentRunWorkspace.findMany(args).unwrap(),
  });
}
