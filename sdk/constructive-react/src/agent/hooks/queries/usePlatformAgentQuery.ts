/**
 * Agent instance registry (human-managed or ephemeral sub-agents)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentKeys } from '../query-keys';
import type { PlatformAgentSelect, PlatformAgentWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformAgentSelect, PlatformAgentWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentQueryKey = platformAgentKeys.detail;
/**
 * Agent instance registry (human-managed or ephemeral sub-agents)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentQuery<
  S extends PlatformAgentSelect,
  TData = {
    platformAgent: InferSelectResult<PlatformAgentWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgent: InferSelectResult<PlatformAgentWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Agent instance registry (human-managed or ephemeral sub-agents)
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentQuery<S extends PlatformAgentSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentSelect>;
}): Promise<{
  platformAgent: InferSelectResult<PlatformAgentWithRelations, S> | null;
}>;
export async function fetchPlatformAgentQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentSelect>(params.selection);
  return getClient()
    .platformAgent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Agent instance registry (human-managed or ephemeral sub-agents)
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentQuery<S extends PlatformAgentSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
