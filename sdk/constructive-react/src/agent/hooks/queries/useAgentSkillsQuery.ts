/**
 * Structured procedural instructions for agent workflows
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { agentSkillKeys } from '../query-keys';
import type {
  AgentSkillSelect,
  AgentSkillWithRelations,
  AgentSkillFilter,
  AgentSkillOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AgentSkillSelect,
  AgentSkillWithRelations,
  AgentSkillFilter,
  AgentSkillOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentSkillsQueryKey = agentSkillKeys.list;
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentSkillsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAgentSkillsQuery<
  S extends AgentSkillSelect,
  TData = {
    agentSkills: ConnectionResult<InferSelectResult<AgentSkillWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentSkillFilter, AgentSkillOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentSkills: ConnectionResult<InferSelectResult<AgentSkillWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentSkillsQuery(
  params: {
    selection: ListSelectionConfig<AgentSkillSelect, AgentSkillFilter, AgentSkillOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AgentSkillSelect, AgentSkillFilter, AgentSkillOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentSkillKeys.list(args),
    queryFn: () => getClient().agentSkill.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```ts
 * const data = await fetchAgentSkillsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAgentSkillsQuery<S extends AgentSkillSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AgentSkillFilter, AgentSkillOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
}): Promise<{
  agentSkills: ConnectionResult<InferSelectResult<AgentSkillWithRelations, S>>;
}>;
export async function fetchAgentSkillsQuery(params: {
  selection: ListSelectionConfig<AgentSkillSelect, AgentSkillFilter, AgentSkillOrderBy>;
}) {
  const args = buildListSelectionArgs<AgentSkillSelect, AgentSkillFilter, AgentSkillOrderBy>(
    params.selection
  );
  return getClient().agentSkill.findMany(args).unwrap();
}
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```ts
 * await prefetchAgentSkillsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAgentSkillsQuery<S extends AgentSkillSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentSkillFilter, AgentSkillOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
  }
): Promise<void>;
export async function prefetchAgentSkillsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AgentSkillSelect, AgentSkillFilter, AgentSkillOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AgentSkillSelect, AgentSkillFilter, AgentSkillOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: agentSkillKeys.list(args),
    queryFn: () => getClient().agentSkill.findMany(args).unwrap(),
  });
}
