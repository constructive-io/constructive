/**
 * Unified skills and knowledge resources for agent retrieval
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentResourceKeys } from '../query-keys';
import type {
  PlatformAgentResourceSelect,
  PlatformAgentResourceWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentResourceSelect,
  PlatformAgentResourceWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentResourceQueryKey = platformAgentResourceKeys.detail;
/**
 * Unified skills and knowledge resources for agent retrieval
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentResourceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentResourceQuery<
  S extends PlatformAgentResourceSelect,
  TData = {
    platformAgentResource: InferSelectResult<PlatformAgentResourceWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentResourceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentResource: InferSelectResult<PlatformAgentResourceWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentResourceQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentResourceSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentResourceSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentResourceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentResource.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Unified skills and knowledge resources for agent retrieval
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentResourceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentResourceQuery<
  S extends PlatformAgentResourceSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentResourceSelect>;
}): Promise<{
  platformAgentResource: InferSelectResult<PlatformAgentResourceWithRelations, S> | null;
}>;
export async function fetchPlatformAgentResourceQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentResourceSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentResourceSelect>(params.selection);
  return getClient()
    .platformAgentResource.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Unified skills and knowledge resources for agent retrieval
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentResourceQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentResourceQuery<S extends PlatformAgentResourceSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentResourceSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentResourceQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentResourceSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentResourceSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentResourceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentResource.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
