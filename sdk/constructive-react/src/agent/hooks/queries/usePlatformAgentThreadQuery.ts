/**
 * Top-level AI/LLM conversation thread
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentThreadKeys } from '../query-keys';
import type {
  PlatformAgentThreadSelect,
  PlatformAgentThreadWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentThreadSelect,
  PlatformAgentThreadWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformAgentThreadQueryKey = platformAgentThreadKeys.detail;
/**
 * Top-level AI/LLM conversation thread
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformAgentThreadQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformAgentThreadQuery<
  S extends PlatformAgentThreadSelect,
  TData = {
    platformAgentThread: InferSelectResult<PlatformAgentThreadWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentThreadSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformAgentThread: InferSelectResult<PlatformAgentThreadWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformAgentThreadQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentThreadSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformAgentThreadSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformAgentThreadKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentThread.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Top-level AI/LLM conversation thread
 *
 * @example
 * ```ts
 * const data = await fetchPlatformAgentThreadQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformAgentThreadQuery<S extends PlatformAgentThreadSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformAgentThreadSelect>;
}): Promise<{
  platformAgentThread: InferSelectResult<PlatformAgentThreadWithRelations, S> | null;
}>;
export async function fetchPlatformAgentThreadQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformAgentThreadSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformAgentThreadSelect>(params.selection);
  return getClient()
    .platformAgentThread.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Top-level AI/LLM conversation thread
 *
 * @example
 * ```ts
 * await prefetchPlatformAgentThreadQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformAgentThreadQuery<S extends PlatformAgentThreadSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentThreadSelect>;
  }
): Promise<void>;
export async function prefetchPlatformAgentThreadQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformAgentThreadSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformAgentThreadSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformAgentThreadKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformAgentThread.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
