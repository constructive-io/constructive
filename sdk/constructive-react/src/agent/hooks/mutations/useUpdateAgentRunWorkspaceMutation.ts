/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentRunWorkspaceKeys } from '../query-keys';
import { agentRunWorkspaceMutationKeys } from '../mutation-keys';
import type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
  AgentRunWorkspacePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
  AgentRunWorkspacePatch,
} from '../../orm/input-types';
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAgentRunWorkspaceMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', agentRunWorkspacePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAgentRunWorkspaceMutation<S extends AgentRunWorkspaceSelect>(
  params: {
    selection: {
      fields: S & AgentRunWorkspaceSelect;
    } & HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAgentRunWorkspace: {
          agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        agentRunWorkspacePatch: AgentRunWorkspacePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAgentRunWorkspace: {
      agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    agentRunWorkspacePatch: AgentRunWorkspacePatch;
  }
>;
export function useUpdateAgentRunWorkspaceMutation(
  params: {
    selection: SelectionConfig<AgentRunWorkspaceSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        agentRunWorkspacePatch: AgentRunWorkspacePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentRunWorkspaceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentRunWorkspaceMutationKeys.all,
    mutationFn: ({
      id,
      agentRunWorkspacePatch,
    }: {
      id: string;
      agentRunWorkspacePatch: AgentRunWorkspacePatch;
    }) =>
      getClient()
        .agentRunWorkspace.update({
          where: {
            id,
          },
          data: agentRunWorkspacePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: agentRunWorkspaceKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentRunWorkspaceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
