/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentEventKeys } from '../query-keys';
import { agentEventMutationKeys } from '../mutation-keys';
import type { AgentEventSelect, AgentEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentEventSelect, AgentEventWithRelations } from '../../orm/input-types';
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAgentEventMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAgentEventMutation<S extends AgentEventSelect>(
  params: {
    selection: {
      fields: S & AgentEventSelect;
    } & HookStrictSelect<NoInfer<S>, AgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAgentEvent: {
          agentEvent: InferSelectResult<AgentEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAgentEvent: {
      agentEvent: InferSelectResult<AgentEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAgentEventMutation(
  params: {
    selection: SelectionConfig<AgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentEventMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .agentEvent.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: agentEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
