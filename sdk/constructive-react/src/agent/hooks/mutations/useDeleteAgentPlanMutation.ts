/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentPlanKeys } from '../query-keys';
import { agentPlanMutationKeys } from '../mutation-keys';
import type { AgentPlanSelect, AgentPlanWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentPlanSelect, AgentPlanWithRelations } from '../../orm/input-types';
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAgentPlanMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAgentPlanMutation<S extends AgentPlanSelect>(
  params: {
    selection: {
      fields: S & AgentPlanSelect;
    } & HookStrictSelect<NoInfer<S>, AgentPlanSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAgentPlan: {
          agentPlan: InferSelectResult<AgentPlanWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAgentPlan: {
      agentPlan: InferSelectResult<AgentPlanWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAgentPlanMutation(
  params: {
    selection: SelectionConfig<AgentPlanSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentPlanSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentPlanMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .agentPlan.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: agentPlanKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentPlanKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
