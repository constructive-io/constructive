/**
 * Shared system prompt templates for agent conversations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentPromptKeys } from '../query-keys';
import { agentPromptMutationKeys } from '../mutation-keys';
import type {
  AgentPromptSelect,
  AgentPromptWithRelations,
  AgentPromptPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentPromptSelect,
  AgentPromptWithRelations,
  AgentPromptPatch,
} from '../../orm/input-types';
/**
 * Shared system prompt templates for agent conversations
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAgentPromptMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', agentPromptPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAgentPromptMutation<S extends AgentPromptSelect>(
  params: {
    selection: {
      fields: S & AgentPromptSelect;
    } & HookStrictSelect<NoInfer<S>, AgentPromptSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAgentPrompt: {
          agentPrompt: InferSelectResult<AgentPromptWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        agentPromptPatch: AgentPromptPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAgentPrompt: {
      agentPrompt: InferSelectResult<AgentPromptWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    agentPromptPatch: AgentPromptPatch;
  }
>;
export function useUpdateAgentPromptMutation(
  params: {
    selection: SelectionConfig<AgentPromptSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        agentPromptPatch: AgentPromptPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentPromptSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentPromptMutationKeys.all,
    mutationFn: ({ id, agentPromptPatch }: { id: string; agentPromptPatch: AgentPromptPatch }) =>
      getClient()
        .agentPrompt.update({
          where: {
            id,
          },
          data: agentPromptPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: agentPromptKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentPromptKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
