/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformAgentEventKeys } from '../query-keys';
import { platformAgentEventMutationKeys } from '../mutation-keys';
import type {
  PlatformAgentEventSelect,
  PlatformAgentEventWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformAgentEventSelect,
  PlatformAgentEventWithRelations,
} from '../../orm/input-types';
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformAgentEventMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformAgentEventMutation<S extends PlatformAgentEventSelect>(
  params: {
    selection: {
      fields: S & PlatformAgentEventSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformAgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformAgentEvent: {
          platformAgentEvent: InferSelectResult<PlatformAgentEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformAgentEvent: {
      platformAgentEvent: InferSelectResult<PlatformAgentEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformAgentEventMutation(
  params: {
    selection: SelectionConfig<PlatformAgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformAgentEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformAgentEventMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformAgentEvent.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformAgentEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformAgentEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
