/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentRunWorkspaceKeys } from '../query-keys';
import { agentRunWorkspaceMutationKeys } from '../mutation-keys';
import type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentRunWorkspaceSelect,
  AgentRunWorkspaceWithRelations,
} from '../../orm/input-types';
/**
 * One repository an agent run works in: its remote, branch, commits and how the work is published
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAgentRunWorkspaceMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAgentRunWorkspaceMutation<S extends AgentRunWorkspaceSelect>(
  params: {
    selection: {
      fields: S & AgentRunWorkspaceSelect;
    } & HookStrictSelect<NoInfer<S>, AgentRunWorkspaceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAgentRunWorkspace: {
          agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAgentRunWorkspace: {
      agentRunWorkspace: InferSelectResult<AgentRunWorkspaceWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAgentRunWorkspaceMutation(
  params: {
    selection: SelectionConfig<AgentRunWorkspaceSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentRunWorkspaceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentRunWorkspaceMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .agentRunWorkspace.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: agentRunWorkspaceKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentRunWorkspaceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
