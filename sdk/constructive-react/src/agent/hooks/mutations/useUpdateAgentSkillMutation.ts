/**
 * Structured procedural instructions for agent workflows
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentSkillKeys } from '../query-keys';
import { agentSkillMutationKeys } from '../mutation-keys';
import type {
  AgentSkillSelect,
  AgentSkillWithRelations,
  AgentSkillPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentSkillSelect,
  AgentSkillWithRelations,
  AgentSkillPatch,
} from '../../orm/input-types';
/**
 * Structured procedural instructions for agent workflows
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAgentSkillMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', agentSkillPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAgentSkillMutation<S extends AgentSkillSelect>(
  params: {
    selection: {
      fields: S & AgentSkillSelect;
    } & HookStrictSelect<NoInfer<S>, AgentSkillSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAgentSkill: {
          agentSkill: InferSelectResult<AgentSkillWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        agentSkillPatch: AgentSkillPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAgentSkill: {
      agentSkill: InferSelectResult<AgentSkillWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    agentSkillPatch: AgentSkillPatch;
  }
>;
export function useUpdateAgentSkillMutation(
  params: {
    selection: SelectionConfig<AgentSkillSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        agentSkillPatch: AgentSkillPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentSkillSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentSkillMutationKeys.all,
    mutationFn: ({ id, agentSkillPatch }: { id: string; agentSkillPatch: AgentSkillPatch }) =>
      getClient()
        .agentSkill.update({
          where: {
            id,
          },
          data: agentSkillPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: agentSkillKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentSkillKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
