/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentPlanKeys } from '../query-keys';
import { agentPlanMutationKeys } from '../mutation-keys';
import type {
  AgentPlanSelect,
  AgentPlanWithRelations,
  AgentPlanPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentPlanSelect,
  AgentPlanWithRelations,
  AgentPlanPatch,
} from '../../orm/input-types';
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAgentPlanMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', agentPlanPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAgentPlanMutation<S extends AgentPlanSelect>(
  params: {
    selection: {
      fields: S & AgentPlanSelect;
    } & HookStrictSelect<NoInfer<S>, AgentPlanSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAgentPlan: {
          agentPlan: InferSelectResult<AgentPlanWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        agentPlanPatch: AgentPlanPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAgentPlan: {
      agentPlan: InferSelectResult<AgentPlanWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    agentPlanPatch: AgentPlanPatch;
  }
>;
export function useUpdateAgentPlanMutation(
  params: {
    selection: SelectionConfig<AgentPlanSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        agentPlanPatch: AgentPlanPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentPlanSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentPlanMutationKeys.all,
    mutationFn: ({ id, agentPlanPatch }: { id: string; agentPlanPatch: AgentPlanPatch }) =>
      getClient()
        .agentPlan.update({
          where: {
            id,
          },
          data: agentPlanPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: agentPlanKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentPlanKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
