/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentEventKeys } from '../query-keys';
import { agentEventMutationKeys } from '../mutation-keys';
import type {
  AgentEventSelect,
  AgentEventWithRelations,
  CreateAgentEventInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentEventSelect,
  AgentEventWithRelations,
  CreateAgentEventInput,
} from '../../orm/input-types';
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAgentEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAgentEventMutation<S extends AgentEventSelect>(
  params: {
    selection: {
      fields: S & AgentEventSelect;
    } & HookStrictSelect<NoInfer<S>, AgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAgentEvent: {
          agentEvent: InferSelectResult<AgentEventWithRelations, S>;
        };
      },
      Error,
      CreateAgentEventInput['agentEvent']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAgentEvent: {
      agentEvent: InferSelectResult<AgentEventWithRelations, S>;
    };
  },
  Error,
  CreateAgentEventInput['agentEvent']
>;
export function useCreateAgentEventMutation(
  params: {
    selection: SelectionConfig<AgentEventSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAgentEventInput['agentEvent']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AgentEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentEventMutationKeys.create(),
    mutationFn: (data: CreateAgentEventInput['agentEvent']) =>
      getClient()
        .agentEvent.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: agentEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
