/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentPlanKeys } from '../query-keys';
import { agentPlanMutationKeys } from '../mutation-keys';
import type {
  AgentPlanSelect,
  AgentPlanWithRelations,
  CreateAgentPlanInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentPlanSelect,
  AgentPlanWithRelations,
  CreateAgentPlanInput,
} from '../../orm/input-types';
/**
 * Workflow plan attached to an agent thread with ordered tasks and optional approval gates
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAgentPlanMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAgentPlanMutation<S extends AgentPlanSelect>(
  params: {
    selection: {
      fields: S & AgentPlanSelect;
    } & HookStrictSelect<NoInfer<S>, AgentPlanSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAgentPlan: {
          agentPlan: InferSelectResult<AgentPlanWithRelations, S>;
        };
      },
      Error,
      CreateAgentPlanInput['agentPlan']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAgentPlan: {
      agentPlan: InferSelectResult<AgentPlanWithRelations, S>;
    };
  },
  Error,
  CreateAgentPlanInput['agentPlan']
>;
export function useCreateAgentPlanMutation(
  params: {
    selection: SelectionConfig<AgentPlanSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAgentPlanInput['agentPlan']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AgentPlanSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentPlanMutationKeys.create(),
    mutationFn: (data: CreateAgentPlanInput['agentPlan']) =>
      getClient()
        .agentPlan.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: agentPlanKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
