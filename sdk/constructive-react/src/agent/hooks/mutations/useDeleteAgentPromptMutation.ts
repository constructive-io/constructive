/**
 * Shared system prompt templates for agent conversations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentPromptKeys } from '../query-keys';
import { agentPromptMutationKeys } from '../mutation-keys';
import type { AgentPromptSelect, AgentPromptWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentPromptSelect, AgentPromptWithRelations } from '../../orm/input-types';
/**
 * Shared system prompt templates for agent conversations
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAgentPromptMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAgentPromptMutation<S extends AgentPromptSelect>(
  params: {
    selection: {
      fields: S & AgentPromptSelect;
    } & HookStrictSelect<NoInfer<S>, AgentPromptSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAgentPrompt: {
          agentPrompt: InferSelectResult<AgentPromptWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAgentPrompt: {
      agentPrompt: InferSelectResult<AgentPromptWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAgentPromptMutation(
  params: {
    selection: SelectionConfig<AgentPromptSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentPromptSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentPromptMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .agentPrompt.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: agentPromptKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentPromptKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
