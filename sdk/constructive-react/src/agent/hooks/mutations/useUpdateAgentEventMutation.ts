/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentEventKeys } from '../query-keys';
import { agentEventMutationKeys } from '../mutation-keys';
import type {
  AgentEventSelect,
  AgentEventWithRelations,
  AgentEventPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentEventSelect,
  AgentEventWithRelations,
  AgentEventPatch,
} from '../../orm/input-types';
/**
 * Append-only transcript of an agent run: one agent session entry per row, stored verbatim
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAgentEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', agentEventPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAgentEventMutation<S extends AgentEventSelect>(
  params: {
    selection: {
      fields: S & AgentEventSelect;
    } & HookStrictSelect<NoInfer<S>, AgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAgentEvent: {
          agentEvent: InferSelectResult<AgentEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        agentEventPatch: AgentEventPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAgentEvent: {
      agentEvent: InferSelectResult<AgentEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    agentEventPatch: AgentEventPatch;
  }
>;
export function useUpdateAgentEventMutation(
  params: {
    selection: SelectionConfig<AgentEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        agentEventPatch: AgentEventPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentEventMutationKeys.all,
    mutationFn: ({ id, agentEventPatch }: { id: string; agentEventPatch: AgentEventPatch }) =>
      getClient()
        .agentEvent.update({
          where: {
            id,
          },
          data: agentEventPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: agentEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
