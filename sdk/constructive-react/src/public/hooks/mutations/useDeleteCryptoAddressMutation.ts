/**
 * Delete mutation hook for CryptoAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { cryptoAddressKeys } from '../query-keys';
import { cryptoAddressMutationKeys } from '../mutation-keys';
import type { CryptoAddressSelect, CryptoAddressWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { CryptoAddressSelect, CryptoAddressWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a CryptoAddress with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteCryptoAddressMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteCryptoAddressMutation<S extends CryptoAddressSelect>(
  params: {
    selection: {
      fields: S & CryptoAddressSelect;
    } & HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteCryptoAddress: {
          cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteCryptoAddress: {
      cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteCryptoAddressMutation(
  params: {
    selection: SelectionConfig<CryptoAddressSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<CryptoAddressSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: cryptoAddressMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .cryptoAddress.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: cryptoAddressKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: cryptoAddressKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
