/**
 * Create mutation hook for App
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appKeys } from '../query-keys';
import { appMutationKeys } from '../mutation-keys';
import type { AppSelect, AppWithRelations, CreateAppInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppSelect, AppWithRelations, CreateAppInput } from '../../orm/input-types';
/**
 * Mutation hook for creating a App
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAppMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAppMutation<S extends AppSelect>(
  params: {
    selection: {
      fields: S & AppSelect;
    } & HookStrictSelect<NoInfer<S>, AppSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createApp: {
          app: InferSelectResult<AppWithRelations, S>;
        };
      },
      Error,
      CreateAppInput['app']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createApp: {
      app: InferSelectResult<AppWithRelations, S>;
    };
  },
  Error,
  CreateAppInput['app']
>;
export function useCreateAppMutation(
  params: {
    selection: SelectionConfig<AppSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAppInput['app']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AppSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appMutationKeys.create(),
    mutationFn: (data: CreateAppInput['app']) =>
      getClient()
        .app.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: appKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
