/**
 * Top-level AI/LLM conversation. Owns the chat-mode + model + system-prompt snapshot for the lifetime of the conversation, and is the entity-scoping anchor — descendants (agent_message, agent_task) inherit entity_id from this row via DataInheritFromParent. RLS is owner-only (AuthzDirectOwner); entity_id is a grouping dimension, not a security dimension.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentThreadKeys } from '../query-keys';
import { agentThreadMutationKeys } from '../mutation-keys';
import type {
  AgentThreadSelect,
  AgentThreadWithRelations,
  CreateAgentThreadInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentThreadSelect,
  AgentThreadWithRelations,
  CreateAgentThreadInput,
} from '../../orm/input-types';
/**
 * Top-level AI/LLM conversation. Owns the chat-mode + model + system-prompt snapshot for the lifetime of the conversation, and is the entity-scoping anchor — descendants (agent_message, agent_task) inherit entity_id from this row via DataInheritFromParent. RLS is owner-only (AuthzDirectOwner); entity_id is a grouping dimension, not a security dimension.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAgentThreadMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAgentThreadMutation<S extends AgentThreadSelect>(
  params: {
    selection: {
      fields: S & AgentThreadSelect;
    } & HookStrictSelect<NoInfer<S>, AgentThreadSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAgentThread: {
          agentThread: InferSelectResult<AgentThreadWithRelations, S>;
        };
      },
      Error,
      CreateAgentThreadInput['agentThread']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAgentThread: {
      agentThread: InferSelectResult<AgentThreadWithRelations, S>;
    };
  },
  Error,
  CreateAgentThreadInput['agentThread']
>;
export function useCreateAgentThreadMutation(
  params: {
    selection: SelectionConfig<AgentThreadSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAgentThreadInput['agentThread']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AgentThreadSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentThreadMutationKeys.create(),
    mutationFn: (data: CreateAgentThreadInput['agentThread']) =>
      getClient()
        .agentThread.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: agentThreadKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
