/**
 * SEO and social sharing metadata for a site: page title, description, and Open Graph image
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteMetadatumKeys } from '../query-keys';
import { siteMetadatumMutationKeys } from '../mutation-keys';
import type {
  SiteMetadatumSelect,
  SiteMetadatumWithRelations,
  CreateSiteMetadatumInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteMetadatumSelect,
  SiteMetadatumWithRelations,
  CreateSiteMetadatumInput,
} from '../../orm/input-types';
/**
 * SEO and social sharing metadata for a site: page title, description, and Open Graph image
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteMetadatumMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateSiteMetadatumMutation<S extends SiteMetadatumSelect>(
  params: {
    selection: {
      fields: S & SiteMetadatumSelect;
    } & HookStrictSelect<NoInfer<S>, SiteMetadatumSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createSiteMetadatum: {
          siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S>;
        };
      },
      Error,
      CreateSiteMetadatumInput['siteMetadatum']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createSiteMetadatum: {
      siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S>;
    };
  },
  Error,
  CreateSiteMetadatumInput['siteMetadatum']
>;
export function useCreateSiteMetadatumMutation(
  params: {
    selection: SelectionConfig<SiteMetadatumSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateSiteMetadatumInput['siteMetadatum']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<SiteMetadatumSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteMetadatumMutationKeys.create(),
    mutationFn: (data: CreateSiteMetadatumInput['siteMetadatum']) =>
      getClient()
        .siteMetadatum.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: siteMetadatumKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
