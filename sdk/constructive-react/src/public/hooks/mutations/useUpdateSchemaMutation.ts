/**
 * Update mutation hook for Schema
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { schemaKeys } from '../query-keys';
import { schemaMutationKeys } from '../mutation-keys';
import type { SchemaSelect, SchemaWithRelations, SchemaPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SchemaSelect, SchemaWithRelations, SchemaPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Schema
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSchemaMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', schemaPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSchemaMutation<S extends SchemaSelect>(
  params: {
    selection: {
      fields: S & SchemaSelect;
    } & HookStrictSelect<NoInfer<S>, SchemaSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSchema: {
          schema: InferSelectResult<SchemaWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        schemaPatch: SchemaPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSchema: {
      schema: InferSelectResult<SchemaWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    schemaPatch: SchemaPatch;
  }
>;
export function useUpdateSchemaMutation(
  params: {
    selection: SelectionConfig<SchemaSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        schemaPatch: SchemaPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SchemaSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: schemaMutationKeys.all,
    mutationFn: ({ id, schemaPatch }: { id: string; schemaPatch: SchemaPatch }) =>
      getClient()
        .schema.update({
          where: {
            id,
          },
          data: schemaPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: schemaKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: schemaKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
