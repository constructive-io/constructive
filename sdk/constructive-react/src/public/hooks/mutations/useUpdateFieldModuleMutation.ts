/**
 * Update mutation hook for FieldModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { fieldModuleKeys } from '../query-keys';
import { fieldModuleMutationKeys } from '../mutation-keys';
import type {
  FieldModuleSelect,
  FieldModuleWithRelations,
  FieldModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  FieldModuleSelect,
  FieldModuleWithRelations,
  FieldModulePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a FieldModule
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateFieldModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', fieldModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateFieldModuleMutation<S extends FieldModuleSelect>(
  params: {
    selection: {
      fields: S & FieldModuleSelect;
    } & HookStrictSelect<NoInfer<S>, FieldModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateFieldModule: {
          fieldModule: InferSelectResult<FieldModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        fieldModulePatch: FieldModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateFieldModule: {
      fieldModule: InferSelectResult<FieldModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    fieldModulePatch: FieldModulePatch;
  }
>;
export function useUpdateFieldModuleMutation(
  params: {
    selection: SelectionConfig<FieldModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        fieldModulePatch: FieldModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<FieldModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: fieldModuleMutationKeys.all,
    mutationFn: ({ id, fieldModulePatch }: { id: string; fieldModulePatch: FieldModulePatch }) =>
      getClient()
        .fieldModule.update({
          where: {
            id,
          },
          data: fieldModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: fieldModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: fieldModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
