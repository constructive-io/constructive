/**
 * Server-side module configuration for an API endpoint; stores module name and JSON settings used by the application server
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { apiModuleKeys } from '../query-keys';
import { apiModuleMutationKeys } from '../mutation-keys';
import type { ApiModuleSelect, ApiModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ApiModuleSelect, ApiModuleWithRelations } from '../../orm/input-types';
/**
 * Server-side module configuration for an API endpoint; stores module name and JSON settings used by the application server
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteApiModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteApiModuleMutation<S extends ApiModuleSelect>(
  params: {
    selection: {
      fields: S & ApiModuleSelect;
    } & HookStrictSelect<NoInfer<S>, ApiModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteApiModule: {
          apiModule: InferSelectResult<ApiModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteApiModule: {
      apiModule: InferSelectResult<ApiModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteApiModuleMutation(
  params: {
    selection: SelectionConfig<ApiModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ApiModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: apiModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .apiModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: apiModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: apiModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
