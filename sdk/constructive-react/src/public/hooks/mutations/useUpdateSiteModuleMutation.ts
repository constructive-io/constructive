/**
 * Site-level module configuration; stores module name and JSON settings used by the frontend or server for each site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteModuleKeys } from '../query-keys';
import { siteModuleMutationKeys } from '../mutation-keys';
import type {
  SiteModuleSelect,
  SiteModuleWithRelations,
  SiteModulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SiteModuleSelect,
  SiteModuleWithRelations,
  SiteModulePatch,
} from '../../orm/input-types';
/**
 * Site-level module configuration; stores module name and JSON settings used by the frontend or server for each site
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateSiteModuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', siteModulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateSiteModuleMutation<S extends SiteModuleSelect>(
  params: {
    selection: {
      fields: S & SiteModuleSelect;
    } & HookStrictSelect<NoInfer<S>, SiteModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateSiteModule: {
          siteModule: InferSelectResult<SiteModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        siteModulePatch: SiteModulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateSiteModule: {
      siteModule: InferSelectResult<SiteModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    siteModulePatch: SiteModulePatch;
  }
>;
export function useUpdateSiteModuleMutation(
  params: {
    selection: SelectionConfig<SiteModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        siteModulePatch: SiteModulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteModuleMutationKeys.all,
    mutationFn: ({ id, siteModulePatch }: { id: string; siteModulePatch: SiteModulePatch }) =>
      getClient()
        .siteModule.update({
          where: {
            id,
          },
          data: siteModulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: siteModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
