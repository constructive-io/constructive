/**
 * Database-wide feature flags and settings; controls which platform features are available to all APIs in this database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseSettingKeys } from '../query-keys';
import { databaseSettingMutationKeys } from '../mutation-keys';
import type { DatabaseSettingSelect, DatabaseSettingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DatabaseSettingSelect, DatabaseSettingWithRelations } from '../../orm/input-types';
/**
 * Database-wide feature flags and settings; controls which platform features are available to all APIs in this database
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDatabaseSettingMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteDatabaseSettingMutation<S extends DatabaseSettingSelect>(
  params: {
    selection: {
      fields: S & DatabaseSettingSelect;
    } & HookStrictSelect<NoInfer<S>, DatabaseSettingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteDatabaseSetting: {
          databaseSetting: InferSelectResult<DatabaseSettingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteDatabaseSetting: {
      databaseSetting: InferSelectResult<DatabaseSettingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteDatabaseSettingMutation(
  params: {
    selection: SelectionConfig<DatabaseSettingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DatabaseSettingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: databaseSettingMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .databaseSetting.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: databaseSettingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: databaseSettingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
