/**
 * Delete mutation hook for InferenceLogModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { inferenceLogModuleKeys } from '../query-keys';
import { inferenceLogModuleMutationKeys } from '../mutation-keys';
import type {
  InferenceLogModuleSelect,
  InferenceLogModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  InferenceLogModuleSelect,
  InferenceLogModuleWithRelations,
} from '../../orm/input-types';
/**
 * Mutation hook for deleting a InferenceLogModule with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteInferenceLogModuleMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteInferenceLogModuleMutation<S extends InferenceLogModuleSelect>(
  params: {
    selection: {
      fields: S & InferenceLogModuleSelect;
    } & HookStrictSelect<NoInfer<S>, InferenceLogModuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteInferenceLogModule: {
          inferenceLogModule: InferSelectResult<InferenceLogModuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteInferenceLogModule: {
      inferenceLogModule: InferSelectResult<InferenceLogModuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteInferenceLogModuleMutation(
  params: {
    selection: SelectionConfig<InferenceLogModuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InferenceLogModuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: inferenceLogModuleMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .inferenceLogModule.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: inferenceLogModuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: inferenceLogModuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
