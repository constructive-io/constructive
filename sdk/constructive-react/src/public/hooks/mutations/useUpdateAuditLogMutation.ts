/**
 * Append-only audit log of authentication events (sign-in, sign-up, password changes, etc.)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { auditLogKeys } from '../query-keys';
import { auditLogMutationKeys } from '../mutation-keys';
import type { AuditLogSelect, AuditLogWithRelations, AuditLogPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AuditLogSelect, AuditLogWithRelations, AuditLogPatch } from '../../orm/input-types';
/**
 * Append-only audit log of authentication events (sign-in, sign-up, password changes, etc.)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAuditLogMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', auditLogPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAuditLogMutation<S extends AuditLogSelect>(
  params: {
    selection: {
      fields: S & AuditLogSelect;
    } & HookStrictSelect<NoInfer<S>, AuditLogSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAuditLog: {
          auditLog: InferSelectResult<AuditLogWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        auditLogPatch: AuditLogPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAuditLog: {
      auditLog: InferSelectResult<AuditLogWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    auditLogPatch: AuditLogPatch;
  }
>;
export function useUpdateAuditLogMutation(
  params: {
    selection: SelectionConfig<AuditLogSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        auditLogPatch: AuditLogPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AuditLogSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: auditLogMutationKeys.all,
    mutationFn: ({ id, auditLogPatch }: { id: string; auditLogPatch: AuditLogPatch }) =>
      getClient()
        .auditLog.update({
          where: {
            id,
          },
          data: auditLogPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: auditLogKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: auditLogKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
