/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentMessageKeys } from '../query-keys';
import { agentMessageMutationKeys } from '../mutation-keys';
import type {
  AgentMessageSelect,
  AgentMessageWithRelations,
  CreateAgentMessageInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentMessageSelect,
  AgentMessageWithRelations,
  CreateAgentMessageInput,
} from '../../orm/input-types';
/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAgentMessageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAgentMessageMutation<S extends AgentMessageSelect>(
  params: {
    selection: {
      fields: S & AgentMessageSelect;
    } & HookStrictSelect<NoInfer<S>, AgentMessageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAgentMessage: {
          agentMessage: InferSelectResult<AgentMessageWithRelations, S>;
        };
      },
      Error,
      CreateAgentMessageInput['agentMessage']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAgentMessage: {
      agentMessage: InferSelectResult<AgentMessageWithRelations, S>;
    };
  },
  Error,
  CreateAgentMessageInput['agentMessage']
>;
export function useCreateAgentMessageMutation(
  params: {
    selection: SelectionConfig<AgentMessageSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAgentMessageInput['agentMessage']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AgentMessageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentMessageMutationKeys.create(),
    mutationFn: (data: CreateAgentMessageInput['agentMessage']) =>
      getClient()
        .agentMessage.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: agentMessageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
