/**
 * Delete mutation hook for App
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appKeys } from '../query-keys';
import { appMutationKeys } from '../mutation-keys';
import type { AppSelect, AppWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppSelect, AppWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a App with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppMutation<S extends AppSelect>(
  params: {
    selection: {
      fields: S & AppSelect;
    } & HookStrictSelect<NoInfer<S>, AppSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteApp: {
          app: InferSelectResult<AppWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteApp: {
      app: InferSelectResult<AppWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppMutation(
  params: {
    selection: SelectionConfig<AppSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .app.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
