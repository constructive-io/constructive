/**
 * DNS domain and subdomain routing: maps hostnames to either an API endpoint or a site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { domainKeys } from '../query-keys';
import { domainMutationKeys } from '../mutation-keys';
import type { DomainSelect, DomainWithRelations, CreateDomainInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DomainSelect, DomainWithRelations, CreateDomainInput } from '../../orm/input-types';
/**
 * DNS domain and subdomain routing: maps hostnames to either an API endpoint or a site
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDomainMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateDomainMutation<S extends DomainSelect>(
  params: {
    selection: {
      fields: S & DomainSelect;
    } & HookStrictSelect<NoInfer<S>, DomainSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createDomain: {
          domain: InferSelectResult<DomainWithRelations, S>;
        };
      },
      Error,
      CreateDomainInput['domain']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createDomain: {
      domain: InferSelectResult<DomainWithRelations, S>;
    };
  },
  Error,
  CreateDomainInput['domain']
>;
export function useCreateDomainMutation(
  params: {
    selection: SelectionConfig<DomainSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateDomainInput['domain']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<DomainSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: domainMutationKeys.create(),
    mutationFn: (data: CreateDomainInput['domain']) =>
      getClient()
        .domain.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: domainKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
