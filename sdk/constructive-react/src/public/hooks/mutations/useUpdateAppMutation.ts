/**
 * Update mutation hook for App
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appKeys } from '../query-keys';
import { appMutationKeys } from '../mutation-keys';
import type { AppSelect, AppWithRelations, AppPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppSelect, AppWithRelations, AppPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a App
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppMutation<S extends AppSelect>(
  params: {
    selection: {
      fields: S & AppSelect;
    } & HookStrictSelect<NoInfer<S>, AppSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateApp: {
          app: InferSelectResult<AppWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appPatch: AppPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateApp: {
      app: InferSelectResult<AppWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appPatch: AppPatch;
  }
>;
export function useUpdateAppMutation(
  params: {
    selection: SelectionConfig<AppSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appPatch: AppPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appMutationKeys.all,
    mutationFn: ({ id, appPatch }: { id: string; appPatch: AppPatch }) =>
      getClient()
        .app.update({
          where: {
            id,
          },
          data: appPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
