/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentTaskKeys } from '../query-keys';
import { agentTaskMutationKeys } from '../mutation-keys';
import type {
  AgentTaskSelect,
  AgentTaskWithRelations,
  CreateAgentTaskInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AgentTaskSelect,
  AgentTaskWithRelations,
  CreateAgentTaskInput,
} from '../../orm/input-types';
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateAgentTaskMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateAgentTaskMutation<S extends AgentTaskSelect>(
  params: {
    selection: {
      fields: S & AgentTaskSelect;
    } & HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createAgentTask: {
          agentTask: InferSelectResult<AgentTaskWithRelations, S>;
        };
      },
      Error,
      CreateAgentTaskInput['agentTask']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createAgentTask: {
      agentTask: InferSelectResult<AgentTaskWithRelations, S>;
    };
  },
  Error,
  CreateAgentTaskInput['agentTask']
>;
export function useCreateAgentTaskMutation(
  params: {
    selection: SelectionConfig<AgentTaskSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateAgentTaskInput['agentTask']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<AgentTaskSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentTaskMutationKeys.create(),
    mutationFn: (data: CreateAgentTaskInput['agentTask']) =>
      getClient()
        .agentTask.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: agentTaskKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
