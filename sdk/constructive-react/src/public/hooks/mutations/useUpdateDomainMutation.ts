/**
 * Update mutation hook for Domain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { domainKeys } from '../query-keys';
import { domainMutationKeys } from '../mutation-keys';
import type { DomainSelect, DomainWithRelations, DomainPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DomainSelect, DomainWithRelations, DomainPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Domain
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDomainMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', domainPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateDomainMutation<S extends DomainSelect>(
  params: {
    selection: {
      fields: S & DomainSelect;
    } & HookStrictSelect<NoInfer<S>, DomainSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateDomain: {
          domain: InferSelectResult<DomainWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        domainPatch: DomainPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateDomain: {
      domain: InferSelectResult<DomainWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    domainPatch: DomainPatch;
  }
>;
export function useUpdateDomainMutation(
  params: {
    selection: SelectionConfig<DomainSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        domainPatch: DomainPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DomainSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: domainMutationKeys.all,
    mutationFn: ({ id, domainPatch }: { id: string; domainPatch: DomainPatch }) =>
      getClient()
        .domain.update({
          where: {
            id,
          },
          data: domainPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: domainKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: domainKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
