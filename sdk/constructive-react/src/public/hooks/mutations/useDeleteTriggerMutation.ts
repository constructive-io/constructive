/**
 * Delete mutation hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { triggerKeys } from '../query-keys';
import { triggerMutationKeys } from '../mutation-keys';
import type { TriggerSelect, TriggerWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TriggerSelect, TriggerWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Trigger with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteTriggerMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteTriggerMutation<S extends TriggerSelect>(
  params: {
    selection: {
      fields: S & TriggerSelect;
    } & HookStrictSelect<NoInfer<S>, TriggerSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteTrigger: {
          trigger: InferSelectResult<TriggerWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteTrigger: {
      trigger: InferSelectResult<TriggerWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteTriggerMutation(
  params: {
    selection: SelectionConfig<TriggerSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<TriggerSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: triggerMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .trigger.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: triggerKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: triggerKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
