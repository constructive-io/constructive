/**
 * Update mutation hook for Procedure
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { procedureKeys } from '../query-keys';
import { procedureMutationKeys } from '../mutation-keys';
import type {
  ProcedureSelect,
  ProcedureWithRelations,
  ProcedurePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ProcedureSelect,
  ProcedureWithRelations,
  ProcedurePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a Procedure
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateProcedureMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', procedurePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateProcedureMutation<S extends ProcedureSelect>(
  params: {
    selection: {
      fields: S & ProcedureSelect;
    } & HookStrictSelect<NoInfer<S>, ProcedureSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateProcedure: {
          procedure: InferSelectResult<ProcedureWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        procedurePatch: ProcedurePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateProcedure: {
      procedure: InferSelectResult<ProcedureWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    procedurePatch: ProcedurePatch;
  }
>;
export function useUpdateProcedureMutation(
  params: {
    selection: SelectionConfig<ProcedureSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        procedurePatch: ProcedurePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ProcedureSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: procedureMutationKeys.all,
    mutationFn: ({ id, procedurePatch }: { id: string; procedurePatch: ProcedurePatch }) =>
      getClient()
        .procedure.update({
          where: {
            id,
          },
          data: procedurePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: procedureKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: procedureKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
