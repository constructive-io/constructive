/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentMessageKeys } from '../query-keys';
import { agentMessageMutationKeys } from '../mutation-keys';
import type { AgentMessageSelect, AgentMessageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentMessageSelect, AgentMessageWithRelations } from '../../orm/input-types';
/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAgentMessageMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAgentMessageMutation<S extends AgentMessageSelect>(
  params: {
    selection: {
      fields: S & AgentMessageSelect;
    } & HookStrictSelect<NoInfer<S>, AgentMessageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAgentMessage: {
          agentMessage: InferSelectResult<AgentMessageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAgentMessage: {
      agentMessage: InferSelectResult<AgentMessageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAgentMessageMutation(
  params: {
    selection: SelectionConfig<AgentMessageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AgentMessageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: agentMessageMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .agentMessage.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: agentMessageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: agentMessageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
