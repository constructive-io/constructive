/**
 * Delete mutation hook for SiteMetadatum
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteMetadatumKeys } from '../query-keys';
import { siteMetadatumMutationKeys } from '../mutation-keys';
import type { SiteMetadatumSelect, SiteMetadatumWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteMetadatumSelect, SiteMetadatumWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a SiteMetadatum with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteMetadatumMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteSiteMetadatumMutation<S extends SiteMetadatumSelect>(
  params: {
    selection: {
      fields: S & SiteMetadatumSelect;
    } & HookStrictSelect<NoInfer<S>, SiteMetadatumSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteSiteMetadatum: {
          siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteSiteMetadatum: {
      siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteSiteMetadatumMutation(
  params: {
    selection: SelectionConfig<SiteMetadatumSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteMetadatumSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteMetadatumMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .siteMetadatum.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: siteMetadatumKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteMetadatumKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
