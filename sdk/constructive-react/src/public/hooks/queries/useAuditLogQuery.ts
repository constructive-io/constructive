/**
 * Append-only audit log of authentication events (sign-in, sign-up, password changes, etc.)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { auditLogKeys } from '../query-keys';
import type { AuditLogSelect, AuditLogWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AuditLogSelect, AuditLogWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const auditLogQueryKey = auditLogKeys.detail;
/**
 * Append-only audit log of authentication events (sign-in, sign-up, password changes, etc.)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAuditLogQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAuditLogQuery<
  S extends AuditLogSelect,
  TData = {
    auditLog: InferSelectResult<AuditLogWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AuditLogSelect>;
  } & Omit<
    UseQueryOptions<
      {
        auditLog: InferSelectResult<AuditLogWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAuditLogQuery(
  params: {
    id: string;
    selection: SelectionConfig<AuditLogSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AuditLogSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: auditLogKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .auditLog.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only audit log of authentication events (sign-in, sign-up, password changes, etc.)
 *
 * @example
 * ```ts
 * const data = await fetchAuditLogQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAuditLogQuery<S extends AuditLogSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AuditLogSelect>;
}): Promise<{
  auditLog: InferSelectResult<AuditLogWithRelations, S> | null;
}>;
export async function fetchAuditLogQuery(params: {
  id: string;
  selection: SelectionConfig<AuditLogSelect>;
}) {
  const args = buildSelectionArgs<AuditLogSelect>(params.selection);
  return getClient()
    .auditLog.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only audit log of authentication events (sign-in, sign-up, password changes, etc.)
 *
 * @example
 * ```ts
 * await prefetchAuditLogQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAuditLogQuery<S extends AuditLogSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AuditLogSelect>;
  }
): Promise<void>;
export async function prefetchAuditLogQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AuditLogSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AuditLogSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: auditLogKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .auditLog.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
