/**
 * Single item query hook for SessionsModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { sessionsModuleKeys } from '../query-keys';
import type { SessionsModuleSelect, SessionsModuleWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SessionsModuleSelect, SessionsModuleWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const sessionsModuleQueryKey = sessionsModuleKeys.detail;
/**
 * Query hook for fetching a single SessionsModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSessionsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSessionsModuleQuery<
  S extends SessionsModuleSelect,
  TData = {
    sessionsModule: InferSelectResult<SessionsModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SessionsModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        sessionsModule: InferSelectResult<SessionsModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSessionsModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<SessionsModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SessionsModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: sessionsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .sessionsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single SessionsModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSessionsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSessionsModuleQuery<S extends SessionsModuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SessionsModuleSelect>;
}): Promise<{
  sessionsModule: InferSelectResult<SessionsModuleWithRelations, S> | null;
}>;
export async function fetchSessionsModuleQuery(params: {
  id: string;
  selection: SelectionConfig<SessionsModuleSelect>;
}) {
  const args = buildSelectionArgs<SessionsModuleSelect>(params.selection);
  return getClient()
    .sessionsModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single SessionsModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSessionsModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSessionsModuleQuery<S extends SessionsModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SessionsModuleSelect>;
  }
): Promise<void>;
export async function prefetchSessionsModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SessionsModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SessionsModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: sessionsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .sessionsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
