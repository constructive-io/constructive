/**
 * Single item query hook for TriggerFunction
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { triggerFunctionKeys } from '../query-keys';
import type { TriggerFunctionSelect, TriggerFunctionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TriggerFunctionSelect, TriggerFunctionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const triggerFunctionQueryKey = triggerFunctionKeys.detail;
/**
 * Query hook for fetching a single TriggerFunction
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTriggerFunctionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useTriggerFunctionQuery<
  S extends TriggerFunctionSelect,
  TData = {
    triggerFunction: InferSelectResult<TriggerFunctionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TriggerFunctionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        triggerFunction: InferSelectResult<TriggerFunctionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTriggerFunctionQuery(
  params: {
    id: string;
    selection: SelectionConfig<TriggerFunctionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<TriggerFunctionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: triggerFunctionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .triggerFunction.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single TriggerFunction without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTriggerFunctionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchTriggerFunctionQuery<S extends TriggerFunctionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, TriggerFunctionSelect>;
}): Promise<{
  triggerFunction: InferSelectResult<TriggerFunctionWithRelations, S> | null;
}>;
export async function fetchTriggerFunctionQuery(params: {
  id: string;
  selection: SelectionConfig<TriggerFunctionSelect>;
}) {
  const args = buildSelectionArgs<TriggerFunctionSelect>(params.selection);
  return getClient()
    .triggerFunction.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single TriggerFunction for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTriggerFunctionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchTriggerFunctionQuery<S extends TriggerFunctionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TriggerFunctionSelect>;
  }
): Promise<void>;
export async function prefetchTriggerFunctionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<TriggerFunctionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<TriggerFunctionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: triggerFunctionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .triggerFunction.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
