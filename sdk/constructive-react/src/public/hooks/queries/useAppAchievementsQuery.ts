/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appAchievementKeys } from '../query-keys';
import type {
  AppAchievementSelect,
  AppAchievementWithRelations,
  AppAchievementFilter,
  AppAchievementOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppAchievementSelect,
  AppAchievementWithRelations,
  AppAchievementFilter,
  AppAchievementOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appAchievementsQueryKey = appAchievementKeys.list;
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppAchievementsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppAchievementsQuery<
  S extends AppAchievementSelect,
  TData = {
    appAchievements: ConnectionResult<InferSelectResult<AppAchievementWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppAchievementFilter, AppAchievementOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appAchievements: ConnectionResult<InferSelectResult<AppAchievementWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppAchievementsQuery(
  params: {
    selection: ListSelectionConfig<
      AppAchievementSelect,
      AppAchievementFilter,
      AppAchievementOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    AppAchievementSelect,
    AppAchievementFilter,
    AppAchievementOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appAchievementKeys.list(args),
    queryFn: () => getClient().appAchievement.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```ts
 * const data = await fetchAppAchievementsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppAchievementsQuery<S extends AppAchievementSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppAchievementFilter, AppAchievementOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
}): Promise<{
  appAchievements: ConnectionResult<InferSelectResult<AppAchievementWithRelations, S>>;
}>;
export async function fetchAppAchievementsQuery(params: {
  selection: ListSelectionConfig<AppAchievementSelect, AppAchievementFilter, AppAchievementOrderBy>;
}) {
  const args = buildListSelectionArgs<
    AppAchievementSelect,
    AppAchievementFilter,
    AppAchievementOrderBy
  >(params.selection);
  return getClient().appAchievement.findMany(args).unwrap();
}
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```ts
 * await prefetchAppAchievementsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppAchievementsQuery<S extends AppAchievementSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppAchievementFilter, AppAchievementOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
  }
): Promise<void>;
export async function prefetchAppAchievementsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      AppAchievementSelect,
      AppAchievementFilter,
      AppAchievementOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    AppAchievementSelect,
    AppAchievementFilter,
    AppAchievementOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appAchievementKeys.list(args),
    queryFn: () => getClient().appAchievement.findMany(args).unwrap(),
  });
}
