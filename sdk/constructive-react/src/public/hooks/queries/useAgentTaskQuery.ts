/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { agentTaskKeys } from '../query-keys';
import type { AgentTaskSelect, AgentTaskWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AgentTaskSelect, AgentTaskWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentTaskQueryKey = agentTaskKeys.detail;
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentTaskQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAgentTaskQuery<
  S extends AgentTaskSelect,
  TData = {
    agentTask: InferSelectResult<AgentTaskWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentTask: InferSelectResult<AgentTaskWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentTaskQuery(
  params: {
    id: string;
    selection: SelectionConfig<AgentTaskSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AgentTaskSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentTaskKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentTask.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```ts
 * const data = await fetchAgentTaskQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAgentTaskQuery<S extends AgentTaskSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
}): Promise<{
  agentTask: InferSelectResult<AgentTaskWithRelations, S> | null;
}>;
export async function fetchAgentTaskQuery(params: {
  id: string;
  selection: SelectionConfig<AgentTaskSelect>;
}) {
  const args = buildSelectionArgs<AgentTaskSelect>(params.selection);
  return getClient()
    .agentTask.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```ts
 * await prefetchAgentTaskQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAgentTaskQuery<S extends AgentTaskSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
  }
): Promise<void>;
export async function prefetchAgentTaskQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AgentTaskSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AgentTaskSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: agentTaskKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .agentTask.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
