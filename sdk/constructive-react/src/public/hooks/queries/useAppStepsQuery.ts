/**
 * The user achieving a requirement for a level. Log table that has every single step ever taken.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appStepKeys } from '../query-keys';
import type {
  AppStepSelect,
  AppStepWithRelations,
  AppStepFilter,
  AppStepOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppStepSelect,
  AppStepWithRelations,
  AppStepFilter,
  AppStepOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appStepsQueryKey = appStepKeys.list;
/**
 * The user achieving a requirement for a level. Log table that has every single step ever taken.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppStepsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppStepsQuery<
  S extends AppStepSelect,
  TData = {
    appSteps: ConnectionResult<InferSelectResult<AppStepWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppStepFilter, AppStepOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppStepSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appSteps: ConnectionResult<InferSelectResult<AppStepWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppStepsQuery(
  params: {
    selection: ListSelectionConfig<AppStepSelect, AppStepFilter, AppStepOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AppStepSelect, AppStepFilter, AppStepOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appStepKeys.list(args),
    queryFn: () => getClient().appStep.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * The user achieving a requirement for a level. Log table that has every single step ever taken.
 *
 * @example
 * ```ts
 * const data = await fetchAppStepsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppStepsQuery<S extends AppStepSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppStepFilter, AppStepOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppStepSelect>;
}): Promise<{
  appSteps: ConnectionResult<InferSelectResult<AppStepWithRelations, S>>;
}>;
export async function fetchAppStepsQuery(params: {
  selection: ListSelectionConfig<AppStepSelect, AppStepFilter, AppStepOrderBy>;
}) {
  const args = buildListSelectionArgs<AppStepSelect, AppStepFilter, AppStepOrderBy>(
    params.selection
  );
  return getClient().appStep.findMany(args).unwrap();
}
/**
 * The user achieving a requirement for a level. Log table that has every single step ever taken.
 *
 * @example
 * ```ts
 * await prefetchAppStepsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppStepsQuery<S extends AppStepSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppStepFilter, AppStepOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppStepSelect>;
  }
): Promise<void>;
export async function prefetchAppStepsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AppStepSelect, AppStepFilter, AppStepOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AppStepSelect, AppStepFilter, AppStepOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: appStepKeys.list(args),
    queryFn: () => getClient().appStep.findMany(args).unwrap(),
  });
}
