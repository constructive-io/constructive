/**
 * Single item query hook for TableGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { tableGrantKeys } from '../query-keys';
import type { TableGrantSelect, TableGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TableGrantSelect, TableGrantWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const tableGrantQueryKey = tableGrantKeys.detail;
/**
 * Query hook for fetching a single TableGrant
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTableGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useTableGrantQuery<
  S extends TableGrantSelect,
  TData = {
    tableGrant: InferSelectResult<TableGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TableGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        tableGrant: InferSelectResult<TableGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTableGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<TableGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<TableGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: tableGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .tableGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single TableGrant without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTableGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchTableGrantQuery<S extends TableGrantSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, TableGrantSelect>;
}): Promise<{
  tableGrant: InferSelectResult<TableGrantWithRelations, S> | null;
}>;
export async function fetchTableGrantQuery(params: {
  id: string;
  selection: SelectionConfig<TableGrantSelect>;
}) {
  const args = buildSelectionArgs<TableGrantSelect>(params.selection);
  return getClient()
    .tableGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single TableGrant for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTableGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchTableGrantQuery<S extends TableGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TableGrantSelect>;
  }
): Promise<void>;
export async function prefetchTableGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<TableGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<TableGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: tableGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .tableGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
