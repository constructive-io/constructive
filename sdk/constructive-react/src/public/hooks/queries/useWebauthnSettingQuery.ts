/**
 * Per-database WebAuthn/passkey runtime configuration; typed replacement for api_modules webauthn_challenge JSONB entries
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webauthnSettingKeys } from '../query-keys';
import type { WebauthnSettingSelect, WebauthnSettingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { WebauthnSettingSelect, WebauthnSettingWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const webauthnSettingQueryKey = webauthnSettingKeys.detail;
/**
 * Per-database WebAuthn/passkey runtime configuration; typed replacement for api_modules webauthn_challenge JSONB entries
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useWebauthnSettingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useWebauthnSettingQuery<
  S extends WebauthnSettingSelect,
  TData = {
    webauthnSetting: InferSelectResult<WebauthnSettingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, WebauthnSettingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        webauthnSetting: InferSelectResult<WebauthnSettingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useWebauthnSettingQuery(
  params: {
    id: string;
    selection: SelectionConfig<WebauthnSettingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<WebauthnSettingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: webauthnSettingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .webauthnSetting.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Per-database WebAuthn/passkey runtime configuration; typed replacement for api_modules webauthn_challenge JSONB entries
 *
 * @example
 * ```ts
 * const data = await fetchWebauthnSettingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchWebauthnSettingQuery<S extends WebauthnSettingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, WebauthnSettingSelect>;
}): Promise<{
  webauthnSetting: InferSelectResult<WebauthnSettingWithRelations, S> | null;
}>;
export async function fetchWebauthnSettingQuery(params: {
  id: string;
  selection: SelectionConfig<WebauthnSettingSelect>;
}) {
  const args = buildSelectionArgs<WebauthnSettingSelect>(params.selection);
  return getClient()
    .webauthnSetting.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Per-database WebAuthn/passkey runtime configuration; typed replacement for api_modules webauthn_challenge JSONB entries
 *
 * @example
 * ```ts
 * await prefetchWebauthnSettingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchWebauthnSettingQuery<S extends WebauthnSettingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, WebauthnSettingSelect>;
  }
): Promise<void>;
export async function prefetchWebauthnSettingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<WebauthnSettingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<WebauthnSettingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: webauthnSettingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .webauthnSetting.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
