/**
 * Cryptocurrency wallet addresses owned by users, with network-specific validation and verification
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { cryptoAddressKeys } from '../query-keys';
import type {
  CryptoAddressSelect,
  CryptoAddressWithRelations,
  CryptoAddressFilter,
  CryptoAddressOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  CryptoAddressSelect,
  CryptoAddressWithRelations,
  CryptoAddressFilter,
  CryptoAddressOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const cryptoAddressesQueryKey = cryptoAddressKeys.list;
/**
 * Cryptocurrency wallet addresses owned by users, with network-specific validation and verification
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCryptoAddressesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useCryptoAddressesQuery<
  S extends CryptoAddressSelect,
  TData = {
    cryptoAddresses: ConnectionResult<InferSelectResult<CryptoAddressWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, CryptoAddressFilter, CryptoAddressOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
  } & Omit<
    UseQueryOptions<
      {
        cryptoAddresses: ConnectionResult<InferSelectResult<CryptoAddressWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCryptoAddressesQuery(
  params: {
    selection: ListSelectionConfig<CryptoAddressSelect, CryptoAddressFilter, CryptoAddressOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    CryptoAddressSelect,
    CryptoAddressFilter,
    CryptoAddressOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: cryptoAddressKeys.list(args),
    queryFn: () => getClient().cryptoAddress.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Cryptocurrency wallet addresses owned by users, with network-specific validation and verification
 *
 * @example
 * ```ts
 * const data = await fetchCryptoAddressesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchCryptoAddressesQuery<S extends CryptoAddressSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, CryptoAddressFilter, CryptoAddressOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
}): Promise<{
  cryptoAddresses: ConnectionResult<InferSelectResult<CryptoAddressWithRelations, S>>;
}>;
export async function fetchCryptoAddressesQuery(params: {
  selection: ListSelectionConfig<CryptoAddressSelect, CryptoAddressFilter, CryptoAddressOrderBy>;
}) {
  const args = buildListSelectionArgs<
    CryptoAddressSelect,
    CryptoAddressFilter,
    CryptoAddressOrderBy
  >(params.selection);
  return getClient().cryptoAddress.findMany(args).unwrap();
}
/**
 * Cryptocurrency wallet addresses owned by users, with network-specific validation and verification
 *
 * @example
 * ```ts
 * await prefetchCryptoAddressesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchCryptoAddressesQuery<S extends CryptoAddressSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, CryptoAddressFilter, CryptoAddressOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
  }
): Promise<void>;
export async function prefetchCryptoAddressesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<CryptoAddressSelect, CryptoAddressFilter, CryptoAddressOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    CryptoAddressSelect,
    CryptoAddressFilter,
    CryptoAddressOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: cryptoAddressKeys.list(args),
    queryFn: () => getClient().cryptoAddress.findMany(args).unwrap(),
  });
}
