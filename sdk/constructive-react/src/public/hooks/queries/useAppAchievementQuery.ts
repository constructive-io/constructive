/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appAchievementKeys } from '../query-keys';
import type { AppAchievementSelect, AppAchievementWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppAchievementSelect, AppAchievementWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appAchievementQueryKey = appAchievementKeys.detail;
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppAchievementQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppAchievementQuery<
  S extends AppAchievementSelect,
  TData = {
    appAchievement: InferSelectResult<AppAchievementWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appAchievement: InferSelectResult<AppAchievementWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppAchievementQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppAchievementSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppAchievementSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appAchievementKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appAchievement.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```ts
 * const data = await fetchAppAchievementQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppAchievementQuery<S extends AppAchievementSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
}): Promise<{
  appAchievement: InferSelectResult<AppAchievementWithRelations, S> | null;
}>;
export async function fetchAppAchievementQuery(params: {
  id: string;
  selection: SelectionConfig<AppAchievementSelect>;
}) {
  const args = buildSelectionArgs<AppAchievementSelect>(params.selection);
  return getClient()
    .appAchievement.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```ts
 * await prefetchAppAchievementQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppAchievementQuery<S extends AppAchievementSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
  }
): Promise<void>;
export async function prefetchAppAchievementQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppAchievementSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppAchievementSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appAchievementKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appAchievement.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
