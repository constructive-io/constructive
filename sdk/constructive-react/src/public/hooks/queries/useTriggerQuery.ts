/**
 * Single item query hook for Trigger
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { triggerKeys } from '../query-keys';
import type { TriggerSelect, TriggerWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TriggerSelect, TriggerWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const triggerQueryKey = triggerKeys.detail;
/**
 * Query hook for fetching a single Trigger
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTriggerQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useTriggerQuery<
  S extends TriggerSelect,
  TData = {
    trigger: InferSelectResult<TriggerWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TriggerSelect>;
  } & Omit<
    UseQueryOptions<
      {
        trigger: InferSelectResult<TriggerWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTriggerQuery(
  params: {
    id: string;
    selection: SelectionConfig<TriggerSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<TriggerSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: triggerKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .trigger.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single Trigger without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTriggerQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchTriggerQuery<S extends TriggerSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, TriggerSelect>;
}): Promise<{
  trigger: InferSelectResult<TriggerWithRelations, S> | null;
}>;
export async function fetchTriggerQuery(params: {
  id: string;
  selection: SelectionConfig<TriggerSelect>;
}) {
  const args = buildSelectionArgs<TriggerSelect>(params.selection);
  return getClient()
    .trigger.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single Trigger for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTriggerQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchTriggerQuery<S extends TriggerSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TriggerSelect>;
  }
): Promise<void>;
export async function prefetchTriggerQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<TriggerSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<TriggerSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: triggerKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .trigger.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
