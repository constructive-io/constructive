/**
 * Tracks individual construction attempts of a blueprint. Each time construct_blueprint() is called, a new record is created here. This separates the editable blueprint definition from its build history, allowing blueprints to be re-executed, constructed into multiple databases, and maintain an audit trail of all construction attempts.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { blueprintConstructionKeys } from '../query-keys';
import type {
  BlueprintConstructionSelect,
  BlueprintConstructionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  BlueprintConstructionSelect,
  BlueprintConstructionWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const blueprintConstructionQueryKey = blueprintConstructionKeys.detail;
/**
 * Tracks individual construction attempts of a blueprint. Each time construct_blueprint() is called, a new record is created here. This separates the editable blueprint definition from its build history, allowing blueprints to be re-executed, constructed into multiple databases, and maintain an audit trail of all construction attempts.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBlueprintConstructionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useBlueprintConstructionQuery<
  S extends BlueprintConstructionSelect,
  TData = {
    blueprintConstruction: InferSelectResult<BlueprintConstructionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BlueprintConstructionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        blueprintConstruction: InferSelectResult<BlueprintConstructionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBlueprintConstructionQuery(
  params: {
    id: string;
    selection: SelectionConfig<BlueprintConstructionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<BlueprintConstructionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: blueprintConstructionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .blueprintConstruction.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Tracks individual construction attempts of a blueprint. Each time construct_blueprint() is called, a new record is created here. This separates the editable blueprint definition from its build history, allowing blueprints to be re-executed, constructed into multiple databases, and maintain an audit trail of all construction attempts.
 *
 * @example
 * ```ts
 * const data = await fetchBlueprintConstructionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchBlueprintConstructionQuery<
  S extends BlueprintConstructionSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, BlueprintConstructionSelect>;
}): Promise<{
  blueprintConstruction: InferSelectResult<BlueprintConstructionWithRelations, S> | null;
}>;
export async function fetchBlueprintConstructionQuery(params: {
  id: string;
  selection: SelectionConfig<BlueprintConstructionSelect>;
}) {
  const args = buildSelectionArgs<BlueprintConstructionSelect>(params.selection);
  return getClient()
    .blueprintConstruction.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Tracks individual construction attempts of a blueprint. Each time construct_blueprint() is called, a new record is created here. This separates the editable blueprint definition from its build history, allowing blueprints to be re-executed, constructed into multiple databases, and maintain an audit trail of all construction attempts.
 *
 * @example
 * ```ts
 * await prefetchBlueprintConstructionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchBlueprintConstructionQuery<S extends BlueprintConstructionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BlueprintConstructionSelect>;
  }
): Promise<void>;
export async function prefetchBlueprintConstructionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<BlueprintConstructionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<BlueprintConstructionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: blueprintConstructionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .blueprintConstruction.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
