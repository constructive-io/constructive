/**
 * Single item query hook for FullTextSearch
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { fullTextSearchKeys } from '../query-keys';
import type { FullTextSearchSelect, FullTextSearchWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FullTextSearchSelect, FullTextSearchWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const fullTextSearchQueryKey = fullTextSearchKeys.detail;
/**
 * Query hook for fetching a single FullTextSearch
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFullTextSearchQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFullTextSearchQuery<
  S extends FullTextSearchSelect,
  TData = {
    fullTextSearch: InferSelectResult<FullTextSearchWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FullTextSearchSelect>;
  } & Omit<
    UseQueryOptions<
      {
        fullTextSearch: InferSelectResult<FullTextSearchWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFullTextSearchQuery(
  params: {
    id: string;
    selection: SelectionConfig<FullTextSearchSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FullTextSearchSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: fullTextSearchKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .fullTextSearch.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single FullTextSearch without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchFullTextSearchQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFullTextSearchQuery<S extends FullTextSearchSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FullTextSearchSelect>;
}): Promise<{
  fullTextSearch: InferSelectResult<FullTextSearchWithRelations, S> | null;
}>;
export async function fetchFullTextSearchQuery(params: {
  id: string;
  selection: SelectionConfig<FullTextSearchSelect>;
}) {
  const args = buildSelectionArgs<FullTextSearchSelect>(params.selection);
  return getClient()
    .fullTextSearch.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single FullTextSearch for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchFullTextSearchQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFullTextSearchQuery<S extends FullTextSearchSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FullTextSearchSelect>;
  }
): Promise<void>;
export async function prefetchFullTextSearchQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FullTextSearchSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FullTextSearchSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: fullTextSearchKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .fullTextSearch.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
