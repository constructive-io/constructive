/**
 * Single item query hook for Partition
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { partitionKeys } from '../query-keys';
import type { PartitionSelect, PartitionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PartitionSelect, PartitionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const partitionQueryKey = partitionKeys.detail;
/**
 * Query hook for fetching a single Partition
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePartitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePartitionQuery<
  S extends PartitionSelect,
  TData = {
    partition: InferSelectResult<PartitionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PartitionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        partition: InferSelectResult<PartitionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePartitionQuery(
  params: {
    id: string;
    selection: SelectionConfig<PartitionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PartitionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: partitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .partition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single Partition without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchPartitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPartitionQuery<S extends PartitionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PartitionSelect>;
}): Promise<{
  partition: InferSelectResult<PartitionWithRelations, S> | null;
}>;
export async function fetchPartitionQuery(params: {
  id: string;
  selection: SelectionConfig<PartitionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PartitionSelect>(params.selection);
  return getClient()
    .partition.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single Partition for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPartitionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPartitionQuery<S extends PartitionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PartitionSelect>;
  }
): Promise<void>;
export async function prefetchPartitionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PartitionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PartitionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: partitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .partition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
