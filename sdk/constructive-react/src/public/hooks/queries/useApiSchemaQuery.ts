/**
 * Join table linking APIs to the database schemas they expose; controls which schemas are accessible through each API
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { apiSchemaKeys } from '../query-keys';
import type { ApiSchemaSelect, ApiSchemaWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ApiSchemaSelect, ApiSchemaWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const apiSchemaQueryKey = apiSchemaKeys.detail;
/**
 * Join table linking APIs to the database schemas they expose; controls which schemas are accessible through each API
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useApiSchemaQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useApiSchemaQuery<
  S extends ApiSchemaSelect,
  TData = {
    apiSchema: InferSelectResult<ApiSchemaWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ApiSchemaSelect>;
  } & Omit<
    UseQueryOptions<
      {
        apiSchema: InferSelectResult<ApiSchemaWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useApiSchemaQuery(
  params: {
    id: string;
    selection: SelectionConfig<ApiSchemaSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ApiSchemaSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: apiSchemaKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .apiSchema.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Join table linking APIs to the database schemas they expose; controls which schemas are accessible through each API
 *
 * @example
 * ```ts
 * const data = await fetchApiSchemaQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchApiSchemaQuery<S extends ApiSchemaSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ApiSchemaSelect>;
}): Promise<{
  apiSchema: InferSelectResult<ApiSchemaWithRelations, S> | null;
}>;
export async function fetchApiSchemaQuery(params: {
  id: string;
  selection: SelectionConfig<ApiSchemaSelect>;
}) {
  const args = buildSelectionArgs<ApiSchemaSelect>(params.selection);
  return getClient()
    .apiSchema.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Join table linking APIs to the database schemas they expose; controls which schemas are accessible through each API
 *
 * @example
 * ```ts
 * await prefetchApiSchemaQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchApiSchemaQuery<S extends ApiSchemaSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ApiSchemaSelect>;
  }
): Promise<void>;
export async function prefetchApiSchemaQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ApiSchemaSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ApiSchemaSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: apiSchemaKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .apiSchema.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
