/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webauthnCredentialsModuleKeys } from '../query-keys';
import type {
  WebauthnCredentialsModuleSelect,
  WebauthnCredentialsModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  WebauthnCredentialsModuleSelect,
  WebauthnCredentialsModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const webauthnCredentialsModuleQueryKey = webauthnCredentialsModuleKeys.detail;
/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useWebauthnCredentialsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useWebauthnCredentialsModuleQuery<
  S extends WebauthnCredentialsModuleSelect,
  TData = {
    webauthnCredentialsModule: InferSelectResult<WebauthnCredentialsModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, WebauthnCredentialsModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        webauthnCredentialsModule: InferSelectResult<
          WebauthnCredentialsModuleWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useWebauthnCredentialsModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<WebauthnCredentialsModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<WebauthnCredentialsModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: webauthnCredentialsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .webauthnCredentialsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 *
 * @example
 * ```ts
 * const data = await fetchWebauthnCredentialsModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchWebauthnCredentialsModuleQuery<
  S extends WebauthnCredentialsModuleSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, WebauthnCredentialsModuleSelect>;
}): Promise<{
  webauthnCredentialsModule: InferSelectResult<WebauthnCredentialsModuleWithRelations, S> | null;
}>;
export async function fetchWebauthnCredentialsModuleQuery(params: {
  id: string;
  selection: SelectionConfig<WebauthnCredentialsModuleSelect>;
}) {
  const args = buildSelectionArgs<WebauthnCredentialsModuleSelect>(params.selection);
  return getClient()
    .webauthnCredentialsModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 *
 * @example
 * ```ts
 * await prefetchWebauthnCredentialsModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchWebauthnCredentialsModuleQuery<
  S extends WebauthnCredentialsModuleSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, WebauthnCredentialsModuleSelect>;
  }
): Promise<void>;
export async function prefetchWebauthnCredentialsModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<WebauthnCredentialsModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<WebauthnCredentialsModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: webauthnCredentialsModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .webauthnCredentialsModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
