/**
 * Single item query hook for CryptoAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { cryptoAddressKeys } from '../query-keys';
import type { CryptoAddressSelect, CryptoAddressWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { CryptoAddressSelect, CryptoAddressWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const cryptoAddressQueryKey = cryptoAddressKeys.detail;
/**
 * Query hook for fetching a single CryptoAddress
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCryptoAddressQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useCryptoAddressQuery<
  S extends CryptoAddressSelect,
  TData = {
    cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
  } & Omit<
    UseQueryOptions<
      {
        cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCryptoAddressQuery(
  params: {
    id: string;
    selection: SelectionConfig<CryptoAddressSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<CryptoAddressSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: cryptoAddressKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .cryptoAddress.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single CryptoAddress without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCryptoAddressQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchCryptoAddressQuery<S extends CryptoAddressSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
}): Promise<{
  cryptoAddress: InferSelectResult<CryptoAddressWithRelations, S> | null;
}>;
export async function fetchCryptoAddressQuery(params: {
  id: string;
  selection: SelectionConfig<CryptoAddressSelect>;
}) {
  const args = buildSelectionArgs<CryptoAddressSelect>(params.selection);
  return getClient()
    .cryptoAddress.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single CryptoAddress for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCryptoAddressQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchCryptoAddressQuery<S extends CryptoAddressSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CryptoAddressSelect>;
  }
): Promise<void>;
export async function prefetchCryptoAddressQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<CryptoAddressSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<CryptoAddressSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: cryptoAddressKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .cryptoAddress.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
