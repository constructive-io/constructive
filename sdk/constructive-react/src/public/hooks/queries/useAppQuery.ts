/**
 * Mobile and native app configuration linked to a site, including store links and identifiers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appKeys } from '../query-keys';
import type { AppSelect, AppWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppSelect, AppWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appQueryKey = appKeys.detail;
/**
 * Mobile and native app configuration linked to a site, including store links and identifiers
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppQuery<
  S extends AppSelect,
  TData = {
    app: InferSelectResult<AppWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppSelect>;
  } & Omit<
    UseQueryOptions<
      {
        app: InferSelectResult<AppWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .app.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Mobile and native app configuration linked to a site, including store links and identifiers
 *
 * @example
 * ```ts
 * const data = await fetchAppQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppQuery<S extends AppSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppSelect>;
}): Promise<{
  app: InferSelectResult<AppWithRelations, S> | null;
}>;
export async function fetchAppQuery(params: { id: string; selection: SelectionConfig<AppSelect> }) {
  const args = buildSelectionArgs<AppSelect>(params.selection);
  return getClient()
    .app.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Mobile and native app configuration linked to a site, including store links and identifiers
 *
 * @example
 * ```ts
 * await prefetchAppQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppQuery<S extends AppSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppSelect>;
  }
): Promise<void>;
export async function prefetchAppQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .app.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
