/**
 * Custom query hook for revParse
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
import type { RevParseVariables } from '../../orm/query';
export type { RevParseVariables } from '../../orm/query';
/** Query key factory - re-exported from query-keys.ts */
export const revParseQueryKey = customQueryKeys.revParse;
/**
 * Query hook for revParse
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRevParseQuery({ variables: { dbId, storeId, refname } });
 *
 * if (data?.revParse) {
 *   console.log(data.revParse);
 * }
 * ```
 */
export function useRevParseQuery<
  TData = {
    revParse: string | null;
  },
>(
  params?: {
    variables?: RevParseVariables;
  } & Omit<
    UseQueryOptions<
      {
        revParse: string | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRevParseQuery<
  TData = {
    revParse: string | null;
  },
>(
  params?: {
    variables?: RevParseVariables;
  } & Omit<
    UseQueryOptions<
      {
        revParse: string | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const variables = params?.variables ?? {};
  const { variables: _variables, ...queryOptions } = params ?? {};
  void _variables;
  return useQuery({
    queryKey: revParseQueryKey(variables),
    queryFn: () => getClient().query.revParse(variables).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch revParse without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchRevParseQuery({ variables: { dbId, storeId, refname } });
 * ```
 */
export async function fetchRevParseQuery(params?: { variables?: RevParseVariables }) {
  const variables = params?.variables ?? {};
  return getClient().query.revParse(variables).unwrap();
}
/**
 * Prefetch revParse for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchRevParseQuery(queryClient, { variables: { dbId, storeId, refname } });
 * ```
 */
export async function prefetchRevParseQuery(
  queryClient: QueryClient,
  params?: {
    variables?: RevParseVariables;
  }
): Promise<void> {
  const variables = params?.variables ?? {};
  await queryClient.prefetchQuery({
    queryKey: revParseQueryKey(variables),
    queryFn: () => getClient().query.revParse(variables).unwrap(),
  });
}
