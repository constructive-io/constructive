/**
 * Periodic snapshot of a single metric for a database. Collected by the snapshot_usage() cron job in constructive-limits. Each row records one metric measurement (e.g. reads, writes, storage_bytes) at a point in time, with optional dimensions for sub-metric breakdowns.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { usageSnapshotKeys } from '../query-keys';
import type {
  UsageSnapshotSelect,
  UsageSnapshotWithRelations,
  UsageSnapshotFilter,
  UsageSnapshotOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  UsageSnapshotSelect,
  UsageSnapshotWithRelations,
  UsageSnapshotFilter,
  UsageSnapshotOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const usageSnapshotsQueryKey = usageSnapshotKeys.list;
/**
 * Periodic snapshot of a single metric for a database. Collected by the snapshot_usage() cron job in constructive-limits. Each row records one metric measurement (e.g. reads, writes, storage_bytes) at a point in time, with optional dimensions for sub-metric breakdowns.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useUsageSnapshotsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useUsageSnapshotsQuery<
  S extends UsageSnapshotSelect,
  TData = {
    usageSnapshots: ConnectionResult<InferSelectResult<UsageSnapshotWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, UsageSnapshotFilter, UsageSnapshotOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, UsageSnapshotSelect>;
  } & Omit<
    UseQueryOptions<
      {
        usageSnapshots: ConnectionResult<InferSelectResult<UsageSnapshotWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useUsageSnapshotsQuery(
  params: {
    selection: ListSelectionConfig<UsageSnapshotSelect, UsageSnapshotFilter, UsageSnapshotOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    UsageSnapshotSelect,
    UsageSnapshotFilter,
    UsageSnapshotOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: usageSnapshotKeys.list(args),
    queryFn: () => getClient().usageSnapshot.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Periodic snapshot of a single metric for a database. Collected by the snapshot_usage() cron job in constructive-limits. Each row records one metric measurement (e.g. reads, writes, storage_bytes) at a point in time, with optional dimensions for sub-metric breakdowns.
 *
 * @example
 * ```ts
 * const data = await fetchUsageSnapshotsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchUsageSnapshotsQuery<S extends UsageSnapshotSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, UsageSnapshotFilter, UsageSnapshotOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, UsageSnapshotSelect>;
}): Promise<{
  usageSnapshots: ConnectionResult<InferSelectResult<UsageSnapshotWithRelations, S>>;
}>;
export async function fetchUsageSnapshotsQuery(params: {
  selection: ListSelectionConfig<UsageSnapshotSelect, UsageSnapshotFilter, UsageSnapshotOrderBy>;
}) {
  const args = buildListSelectionArgs<
    UsageSnapshotSelect,
    UsageSnapshotFilter,
    UsageSnapshotOrderBy
  >(params.selection);
  return getClient().usageSnapshot.findMany(args).unwrap();
}
/**
 * Periodic snapshot of a single metric for a database. Collected by the snapshot_usage() cron job in constructive-limits. Each row records one metric measurement (e.g. reads, writes, storage_bytes) at a point in time, with optional dimensions for sub-metric breakdowns.
 *
 * @example
 * ```ts
 * await prefetchUsageSnapshotsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchUsageSnapshotsQuery<S extends UsageSnapshotSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, UsageSnapshotFilter, UsageSnapshotOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, UsageSnapshotSelect>;
  }
): Promise<void>;
export async function prefetchUsageSnapshotsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<UsageSnapshotSelect, UsageSnapshotFilter, UsageSnapshotOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    UsageSnapshotSelect,
    UsageSnapshotFilter,
    UsageSnapshotOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: usageSnapshotKeys.list(args),
    queryFn: () => getClient().usageSnapshot.findMany(args).unwrap(),
  });
}
