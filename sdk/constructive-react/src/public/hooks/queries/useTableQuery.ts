/**
 * Single item query hook for Table
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { tableKeys } from '../query-keys';
import type { TableSelect, TableWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { TableSelect, TableWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const tableQueryKey = tableKeys.detail;
/**
 * Query hook for fetching a single Table
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useTableQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useTableQuery<
  S extends TableSelect,
  TData = {
    table: InferSelectResult<TableWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TableSelect>;
  } & Omit<
    UseQueryOptions<
      {
        table: InferSelectResult<TableWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useTableQuery(
  params: {
    id: string;
    selection: SelectionConfig<TableSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<TableSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: tableKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .table.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single Table without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchTableQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchTableQuery<S extends TableSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, TableSelect>;
}): Promise<{
  table: InferSelectResult<TableWithRelations, S> | null;
}>;
export async function fetchTableQuery(params: {
  id: string;
  selection: SelectionConfig<TableSelect>;
}) {
  const args = buildSelectionArgs<TableSelect>(params.selection);
  return getClient()
    .table.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single Table for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchTableQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchTableQuery<S extends TableSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, TableSelect>;
  }
): Promise<void>;
export async function prefetchTableQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<TableSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<TableSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: tableKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .table.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
