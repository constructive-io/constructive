/**
 * Single item query hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { policyKeys } from '../query-keys';
import type { PolicySelect, PolicyWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PolicySelect, PolicyWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const policyQueryKey = policyKeys.detail;
/**
 * Query hook for fetching a single Policy
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePolicyQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePolicyQuery<
  S extends PolicySelect,
  TData = {
    policy: InferSelectResult<PolicyWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PolicySelect>;
  } & Omit<
    UseQueryOptions<
      {
        policy: InferSelectResult<PolicyWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePolicyQuery(
  params: {
    id: string;
    selection: SelectionConfig<PolicySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PolicySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: policyKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .policy.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single Policy without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchPolicyQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPolicyQuery<S extends PolicySelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PolicySelect>;
}): Promise<{
  policy: InferSelectResult<PolicyWithRelations, S> | null;
}>;
export async function fetchPolicyQuery(params: {
  id: string;
  selection: SelectionConfig<PolicySelect>;
}) {
  const args = buildSelectionArgs<PolicySelect>(params.selection);
  return getClient()
    .policy.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single Policy for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPolicyQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPolicyQuery<S extends PolicySelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PolicySelect>;
  }
): Promise<void>;
export async function prefetchPolicyQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PolicySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PolicySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: policyKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .policy.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
