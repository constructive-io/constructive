/**
 * Single item query hook for CryptoAddressesModule
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { cryptoAddressesModuleKeys } from '../query-keys';
import type {
  CryptoAddressesModuleSelect,
  CryptoAddressesModuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  CryptoAddressesModuleSelect,
  CryptoAddressesModuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const cryptoAddressesModuleQueryKey = cryptoAddressesModuleKeys.detail;
/**
 * Query hook for fetching a single CryptoAddressesModule
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCryptoAddressesModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useCryptoAddressesModuleQuery<
  S extends CryptoAddressesModuleSelect,
  TData = {
    cryptoAddressesModule: InferSelectResult<CryptoAddressesModuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CryptoAddressesModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        cryptoAddressesModule: InferSelectResult<CryptoAddressesModuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCryptoAddressesModuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<CryptoAddressesModuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<CryptoAddressesModuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: cryptoAddressesModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .cryptoAddressesModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single CryptoAddressesModule without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCryptoAddressesModuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchCryptoAddressesModuleQuery<
  S extends CryptoAddressesModuleSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, CryptoAddressesModuleSelect>;
}): Promise<{
  cryptoAddressesModule: InferSelectResult<CryptoAddressesModuleWithRelations, S> | null;
}>;
export async function fetchCryptoAddressesModuleQuery(params: {
  id: string;
  selection: SelectionConfig<CryptoAddressesModuleSelect>;
}) {
  const args = buildSelectionArgs<CryptoAddressesModuleSelect>(params.selection);
  return getClient()
    .cryptoAddressesModule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single CryptoAddressesModule for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCryptoAddressesModuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchCryptoAddressesModuleQuery<S extends CryptoAddressesModuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, CryptoAddressesModuleSelect>;
  }
): Promise<void>;
export async function prefetchCryptoAddressesModuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<CryptoAddressesModuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<CryptoAddressesModuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: cryptoAddressesModuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .cryptoAddressesModule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
