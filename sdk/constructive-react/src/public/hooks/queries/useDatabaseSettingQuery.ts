/**
 * Database-wide feature flags and settings; controls which platform features are available to all APIs in this database
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseSettingKeys } from '../query-keys';
import type { DatabaseSettingSelect, DatabaseSettingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DatabaseSettingSelect, DatabaseSettingWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseSettingQueryKey = databaseSettingKeys.detail;
/**
 * Database-wide feature flags and settings; controls which platform features are available to all APIs in this database
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseSettingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDatabaseSettingQuery<
  S extends DatabaseSettingSelect,
  TData = {
    databaseSetting: InferSelectResult<DatabaseSettingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseSettingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseSetting: InferSelectResult<DatabaseSettingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseSettingQuery(
  params: {
    id: string;
    selection: SelectionConfig<DatabaseSettingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DatabaseSettingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseSettingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseSetting.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Database-wide feature flags and settings; controls which platform features are available to all APIs in this database
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseSettingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDatabaseSettingQuery<S extends DatabaseSettingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DatabaseSettingSelect>;
}): Promise<{
  databaseSetting: InferSelectResult<DatabaseSettingWithRelations, S> | null;
}>;
export async function fetchDatabaseSettingQuery(params: {
  id: string;
  selection: SelectionConfig<DatabaseSettingSelect>;
}) {
  const args = buildSelectionArgs<DatabaseSettingSelect>(params.selection);
  return getClient()
    .databaseSetting.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Database-wide feature flags and settings; controls which platform features are available to all APIs in this database
 *
 * @example
 * ```ts
 * await prefetchDatabaseSettingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDatabaseSettingQuery<S extends DatabaseSettingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseSettingSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseSettingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DatabaseSettingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DatabaseSettingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseSettingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseSetting.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
