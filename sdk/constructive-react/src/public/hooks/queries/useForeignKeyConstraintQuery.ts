/**
 * Single item query hook for ForeignKeyConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { foreignKeyConstraintKeys } from '../query-keys';
import type {
  ForeignKeyConstraintSelect,
  ForeignKeyConstraintWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ForeignKeyConstraintSelect,
  ForeignKeyConstraintWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const foreignKeyConstraintQueryKey = foreignKeyConstraintKeys.detail;
/**
 * Query hook for fetching a single ForeignKeyConstraint
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useForeignKeyConstraintQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useForeignKeyConstraintQuery<
  S extends ForeignKeyConstraintSelect,
  TData = {
    foreignKeyConstraint: InferSelectResult<ForeignKeyConstraintWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ForeignKeyConstraintSelect>;
  } & Omit<
    UseQueryOptions<
      {
        foreignKeyConstraint: InferSelectResult<ForeignKeyConstraintWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useForeignKeyConstraintQuery(
  params: {
    id: string;
    selection: SelectionConfig<ForeignKeyConstraintSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ForeignKeyConstraintSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: foreignKeyConstraintKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .foreignKeyConstraint.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single ForeignKeyConstraint without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchForeignKeyConstraintQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchForeignKeyConstraintQuery<S extends ForeignKeyConstraintSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ForeignKeyConstraintSelect>;
}): Promise<{
  foreignKeyConstraint: InferSelectResult<ForeignKeyConstraintWithRelations, S> | null;
}>;
export async function fetchForeignKeyConstraintQuery(params: {
  id: string;
  selection: SelectionConfig<ForeignKeyConstraintSelect>;
}) {
  const args = buildSelectionArgs<ForeignKeyConstraintSelect>(params.selection);
  return getClient()
    .foreignKeyConstraint.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single ForeignKeyConstraint for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchForeignKeyConstraintQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchForeignKeyConstraintQuery<S extends ForeignKeyConstraintSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ForeignKeyConstraintSelect>;
  }
): Promise<void>;
export async function prefetchForeignKeyConstraintQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ForeignKeyConstraintSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ForeignKeyConstraintSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: foreignKeyConstraintKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .foreignKeyConstraint.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
