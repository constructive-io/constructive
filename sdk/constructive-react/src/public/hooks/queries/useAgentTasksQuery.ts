/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { agentTaskKeys } from '../query-keys';
import type {
  AgentTaskSelect,
  AgentTaskWithRelations,
  AgentTaskFilter,
  AgentTaskOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AgentTaskSelect,
  AgentTaskWithRelations,
  AgentTaskFilter,
  AgentTaskOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentTasksQueryKey = agentTaskKeys.list;
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentTasksQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAgentTasksQuery<
  S extends AgentTaskSelect,
  TData = {
    agentTasks: ConnectionResult<InferSelectResult<AgentTaskWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentTaskFilter, AgentTaskOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentTasks: ConnectionResult<InferSelectResult<AgentTaskWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentTasksQuery(
  params: {
    selection: ListSelectionConfig<AgentTaskSelect, AgentTaskFilter, AgentTaskOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AgentTaskSelect, AgentTaskFilter, AgentTaskOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentTaskKeys.list(args),
    queryFn: () => getClient().agentTask.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```ts
 * const data = await fetchAgentTasksQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAgentTasksQuery<S extends AgentTaskSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AgentTaskFilter, AgentTaskOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
}): Promise<{
  agentTasks: ConnectionResult<InferSelectResult<AgentTaskWithRelations, S>>;
}>;
export async function fetchAgentTasksQuery(params: {
  selection: ListSelectionConfig<AgentTaskSelect, AgentTaskFilter, AgentTaskOrderBy>;
}) {
  const args = buildListSelectionArgs<AgentTaskSelect, AgentTaskFilter, AgentTaskOrderBy>(
    params.selection
  );
  return getClient().agentTask.findMany(args).unwrap();
}
/**
 * An agent- or user-authored todo item attached to an agent_thread. Captures the planning surface for agent-mode conversations: each row is a single discrete unit of work with a status lifecycle (pending → in-progress → done|failed). Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```ts
 * await prefetchAgentTasksQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAgentTasksQuery<S extends AgentTaskSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentTaskFilter, AgentTaskOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentTaskSelect>;
  }
): Promise<void>;
export async function prefetchAgentTasksQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AgentTaskSelect, AgentTaskFilter, AgentTaskOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AgentTaskSelect, AgentTaskFilter, AgentTaskOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: agentTaskKeys.list(args),
    queryFn: () => getClient().agentTask.findMany(args).unwrap(),
  });
}
