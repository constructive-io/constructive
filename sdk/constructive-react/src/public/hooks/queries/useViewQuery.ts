/**
 * Single item query hook for View
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { viewKeys } from '../query-keys';
import type { ViewSelect, ViewWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ViewSelect, ViewWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const viewQueryKey = viewKeys.detail;
/**
 * Query hook for fetching a single View
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useViewQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useViewQuery<
  S extends ViewSelect,
  TData = {
    view: InferSelectResult<ViewWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ViewSelect>;
  } & Omit<
    UseQueryOptions<
      {
        view: InferSelectResult<ViewWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useViewQuery(
  params: {
    id: string;
    selection: SelectionConfig<ViewSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ViewSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: viewKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .view.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single View without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchViewQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchViewQuery<S extends ViewSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ViewSelect>;
}): Promise<{
  view: InferSelectResult<ViewWithRelations, S> | null;
}>;
export async function fetchViewQuery(params: {
  id: string;
  selection: SelectionConfig<ViewSelect>;
}) {
  const args = buildSelectionArgs<ViewSelect>(params.selection);
  return getClient()
    .view.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single View for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchViewQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchViewQuery<S extends ViewSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ViewSelect>;
  }
): Promise<void>;
export async function prefetchViewQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ViewSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ViewSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: viewKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .view.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
