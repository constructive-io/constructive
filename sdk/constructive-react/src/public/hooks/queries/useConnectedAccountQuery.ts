/**
 * OAuth and social login connections linking external service accounts to users
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { connectedAccountKeys } from '../query-keys';
import type { ConnectedAccountSelect, ConnectedAccountWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ConnectedAccountSelect, ConnectedAccountWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const connectedAccountQueryKey = connectedAccountKeys.detail;
/**
 * OAuth and social login connections linking external service accounts to users
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useConnectedAccountQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useConnectedAccountQuery<
  S extends ConnectedAccountSelect,
  TData = {
    connectedAccount: InferSelectResult<ConnectedAccountWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ConnectedAccountSelect>;
  } & Omit<
    UseQueryOptions<
      {
        connectedAccount: InferSelectResult<ConnectedAccountWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useConnectedAccountQuery(
  params: {
    id: string;
    selection: SelectionConfig<ConnectedAccountSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ConnectedAccountSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: connectedAccountKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .connectedAccount.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * OAuth and social login connections linking external service accounts to users
 *
 * @example
 * ```ts
 * const data = await fetchConnectedAccountQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchConnectedAccountQuery<S extends ConnectedAccountSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ConnectedAccountSelect>;
}): Promise<{
  connectedAccount: InferSelectResult<ConnectedAccountWithRelations, S> | null;
}>;
export async function fetchConnectedAccountQuery(params: {
  id: string;
  selection: SelectionConfig<ConnectedAccountSelect>;
}) {
  const args = buildSelectionArgs<ConnectedAccountSelect>(params.selection);
  return getClient()
    .connectedAccount.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * OAuth and social login connections linking external service accounts to users
 *
 * @example
 * ```ts
 * await prefetchConnectedAccountQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchConnectedAccountQuery<S extends ConnectedAccountSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ConnectedAccountSelect>;
  }
): Promise<void>;
export async function prefetchConnectedAccountQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ConnectedAccountSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ConnectedAccountSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: connectedAccountKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .connectedAccount.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
