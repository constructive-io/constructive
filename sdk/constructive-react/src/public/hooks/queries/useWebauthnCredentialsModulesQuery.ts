/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { webauthnCredentialsModuleKeys } from '../query-keys';
import type {
  WebauthnCredentialsModuleSelect,
  WebauthnCredentialsModuleWithRelations,
  WebauthnCredentialsModuleFilter,
  WebauthnCredentialsModuleOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  WebauthnCredentialsModuleSelect,
  WebauthnCredentialsModuleWithRelations,
  WebauthnCredentialsModuleFilter,
  WebauthnCredentialsModuleOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const webauthnCredentialsModulesQueryKey = webauthnCredentialsModuleKeys.list;
/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useWebauthnCredentialsModulesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useWebauthnCredentialsModulesQuery<
  S extends WebauthnCredentialsModuleSelect,
  TData = {
    webauthnCredentialsModules: ConnectionResult<
      InferSelectResult<WebauthnCredentialsModuleWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, WebauthnCredentialsModuleFilter, WebauthnCredentialsModuleOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, WebauthnCredentialsModuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        webauthnCredentialsModules: ConnectionResult<
          InferSelectResult<WebauthnCredentialsModuleWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useWebauthnCredentialsModulesQuery(
  params: {
    selection: ListSelectionConfig<
      WebauthnCredentialsModuleSelect,
      WebauthnCredentialsModuleFilter,
      WebauthnCredentialsModuleOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    WebauthnCredentialsModuleSelect,
    WebauthnCredentialsModuleFilter,
    WebauthnCredentialsModuleOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: webauthnCredentialsModuleKeys.list(args),
    queryFn: () => getClient().webauthnCredentialsModule.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 *
 * @example
 * ```ts
 * const data = await fetchWebauthnCredentialsModulesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchWebauthnCredentialsModulesQuery<
  S extends WebauthnCredentialsModuleSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, WebauthnCredentialsModuleFilter, WebauthnCredentialsModuleOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, WebauthnCredentialsModuleSelect>;
}): Promise<{
  webauthnCredentialsModules: ConnectionResult<
    InferSelectResult<WebauthnCredentialsModuleWithRelations, S>
  >;
}>;
export async function fetchWebauthnCredentialsModulesQuery(params: {
  selection: ListSelectionConfig<
    WebauthnCredentialsModuleSelect,
    WebauthnCredentialsModuleFilter,
    WebauthnCredentialsModuleOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    WebauthnCredentialsModuleSelect,
    WebauthnCredentialsModuleFilter,
    WebauthnCredentialsModuleOrderBy
  >(params.selection);
  return getClient().webauthnCredentialsModule.findMany(args).unwrap();
}
/**
 * Config row for the webauthn_credentials_module, which provisions the per-user WebAuthn/passkey credentials table (public key, counter, transports, device type, backup state) mirroring crypto_addresses_module. The sibling webauthn_auth_module (RP config, sign-in/sign-up function names) lands later; Phase 11a is credentials only.
 *
 * @example
 * ```ts
 * await prefetchWebauthnCredentialsModulesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchWebauthnCredentialsModulesQuery<
  S extends WebauthnCredentialsModuleSelect,
>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, WebauthnCredentialsModuleFilter, WebauthnCredentialsModuleOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, WebauthnCredentialsModuleSelect>;
  }
): Promise<void>;
export async function prefetchWebauthnCredentialsModulesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      WebauthnCredentialsModuleSelect,
      WebauthnCredentialsModuleFilter,
      WebauthnCredentialsModuleOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    WebauthnCredentialsModuleSelect,
    WebauthnCredentialsModuleFilter,
    WebauthnCredentialsModuleOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: webauthnCredentialsModuleKeys.list(args),
    queryFn: () => getClient().webauthnCredentialsModule.findMany(args).unwrap(),
  });
}
