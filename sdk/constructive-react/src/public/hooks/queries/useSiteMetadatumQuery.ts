/**
 * Single item query hook for SiteMetadatum
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteMetadatumKeys } from '../query-keys';
import type { SiteMetadatumSelect, SiteMetadatumWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteMetadatumSelect, SiteMetadatumWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const siteMetadatumQueryKey = siteMetadatumKeys.detail;
/**
 * Query hook for fetching a single SiteMetadatum
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSiteMetadatumQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSiteMetadatumQuery<
  S extends SiteMetadatumSelect,
  TData = {
    siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteMetadatumSelect>;
  } & Omit<
    UseQueryOptions<
      {
        siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSiteMetadatumQuery(
  params: {
    id: string;
    selection: SelectionConfig<SiteMetadatumSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SiteMetadatumSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: siteMetadatumKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteMetadatum.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single SiteMetadatum without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSiteMetadatumQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSiteMetadatumQuery<S extends SiteMetadatumSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SiteMetadatumSelect>;
}): Promise<{
  siteMetadatum: InferSelectResult<SiteMetadatumWithRelations, S> | null;
}>;
export async function fetchSiteMetadatumQuery(params: {
  id: string;
  selection: SelectionConfig<SiteMetadatumSelect>;
}) {
  const args = buildSelectionArgs<SiteMetadatumSelect>(params.selection);
  return getClient()
    .siteMetadatum.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single SiteMetadatum for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSiteMetadatumQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSiteMetadatumQuery<S extends SiteMetadatumSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SiteMetadatumSelect>;
  }
): Promise<void>;
export async function prefetchSiteMetadatumQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SiteMetadatumSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SiteMetadatumSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: siteMetadatumKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .siteMetadatum.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
