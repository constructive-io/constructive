/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { agentMessageKeys } from '../query-keys';
import type {
  AgentMessageSelect,
  AgentMessageWithRelations,
  AgentMessageFilter,
  AgentMessageOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AgentMessageSelect,
  AgentMessageWithRelations,
  AgentMessageFilter,
  AgentMessageOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const agentMessagesQueryKey = agentMessageKeys.list;
/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAgentMessagesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAgentMessagesQuery<
  S extends AgentMessageSelect,
  TData = {
    agentMessages: ConnectionResult<InferSelectResult<AgentMessageWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentMessageFilter, AgentMessageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentMessageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        agentMessages: ConnectionResult<InferSelectResult<AgentMessageWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAgentMessagesQuery(
  params: {
    selection: ListSelectionConfig<AgentMessageSelect, AgentMessageFilter, AgentMessageOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AgentMessageSelect, AgentMessageFilter, AgentMessageOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: agentMessageKeys.list(args),
    queryFn: () => getClient().agentMessage.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```ts
 * const data = await fetchAgentMessagesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAgentMessagesQuery<S extends AgentMessageSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AgentMessageFilter, AgentMessageOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AgentMessageSelect>;
}): Promise<{
  agentMessages: ConnectionResult<InferSelectResult<AgentMessageWithRelations, S>>;
}>;
export async function fetchAgentMessagesQuery(params: {
  selection: ListSelectionConfig<AgentMessageSelect, AgentMessageFilter, AgentMessageOrderBy>;
}) {
  const args = buildListSelectionArgs<AgentMessageSelect, AgentMessageFilter, AgentMessageOrderBy>(
    params.selection
  );
  return getClient().agentMessage.findMany(args).unwrap();
}
/**
 * A single message in an agent_thread. The full client-rendered content (TextPart and ToolPart, including the ToolPart state machine and inline approval object) lives in the `parts` jsonb column — there is no separate agent_tool_call or agent_tool_approval table in v1. Cascade-deleted with the parent thread; RLS is owner-only.
 *
 * @example
 * ```ts
 * await prefetchAgentMessagesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAgentMessagesQuery<S extends AgentMessageSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AgentMessageFilter, AgentMessageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AgentMessageSelect>;
  }
): Promise<void>;
export async function prefetchAgentMessagesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AgentMessageSelect, AgentMessageFilter, AgentMessageOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AgentMessageSelect, AgentMessageFilter, AgentMessageOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: agentMessageKeys.list(args),
    queryFn: () => getClient().agentMessage.findMany(args).unwrap(),
  });
}
