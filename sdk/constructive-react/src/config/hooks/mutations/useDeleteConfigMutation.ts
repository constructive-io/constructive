/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { configKeys } from '../query-keys';
import { configMutationKeys } from '../mutation-keys';
import type { ConfigSelect, ConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ConfigSelect, ConfigWithRelations } from '../../orm/input-types';
/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteConfigMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteConfigMutation<S extends ConfigSelect>(
  params: {
    selection: {
      fields: S & ConfigSelect;
    } & HookStrictSelect<NoInfer<S>, ConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteConfig: {
          config: InferSelectResult<ConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteConfig: {
      config: InferSelectResult<ConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteConfigMutation(
  params: {
    selection: SelectionConfig<ConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: configMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .config.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: configKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: configKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
