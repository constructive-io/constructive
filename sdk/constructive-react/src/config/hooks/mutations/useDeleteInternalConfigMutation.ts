/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { internalConfigKeys } from '../query-keys';
import { internalConfigMutationKeys } from '../mutation-keys';
import type { InternalConfigSelect, InternalConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InternalConfigSelect, InternalConfigWithRelations } from '../../orm/input-types';
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteInternalConfigMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteInternalConfigMutation<S extends InternalConfigSelect>(
  params: {
    selection: {
      fields: S & InternalConfigSelect;
    } & HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteInternalConfig: {
          internalConfig: InferSelectResult<InternalConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteInternalConfig: {
      internalConfig: InferSelectResult<InternalConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteInternalConfigMutation(
  params: {
    selection: SelectionConfig<InternalConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InternalConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: internalConfigMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .internalConfig.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: internalConfigKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: internalConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
