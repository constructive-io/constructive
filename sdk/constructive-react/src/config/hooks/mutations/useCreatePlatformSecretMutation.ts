/**
 * Create mutation hook for PlatformSecret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformSecretKeys } from '../query-keys';
import { platformSecretMutationKeys } from '../mutation-keys';
import type {
  PlatformSecretSelect,
  PlatformSecretWithRelations,
  CreatePlatformSecretInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformSecretSelect,
  PlatformSecretWithRelations,
  CreatePlatformSecretInput,
} from '../../orm/input-types';
/**
 * Mutation hook for creating a PlatformSecret
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlatformSecretMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlatformSecretMutation<S extends PlatformSecretSelect>(
  params: {
    selection: {
      fields: S & PlatformSecretSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformSecretSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlatformSecret: {
          platformSecret: InferSelectResult<PlatformSecretWithRelations, S>;
        };
      },
      Error,
      CreatePlatformSecretInput['platformSecret']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlatformSecret: {
      platformSecret: InferSelectResult<PlatformSecretWithRelations, S>;
    };
  },
  Error,
  CreatePlatformSecretInput['platformSecret']
>;
export function useCreatePlatformSecretMutation(
  params: {
    selection: SelectionConfig<PlatformSecretSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreatePlatformSecretInput['platformSecret']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformSecretSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformSecretMutationKeys.create(),
    mutationFn: (data: CreatePlatformSecretInput['platformSecret']) =>
      getClient()
        .platformSecret.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: platformSecretKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
