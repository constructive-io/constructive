/**
 * Create mutation hook for Secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { secretKeys } from '../query-keys';
import { secretMutationKeys } from '../mutation-keys';
import type { SecretSelect, SecretWithRelations, CreateSecretInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SecretSelect, SecretWithRelations, CreateSecretInput } from '../../orm/input-types';
/**
 * Mutation hook for creating a Secret
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSecretMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateSecretMutation<S extends SecretSelect>(
  params: {
    selection: {
      fields: S & SecretSelect;
    } & HookStrictSelect<NoInfer<S>, SecretSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createSecret: {
          secret: InferSelectResult<SecretWithRelations, S>;
        };
      },
      Error,
      CreateSecretInput['secret']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createSecret: {
      secret: InferSelectResult<SecretWithRelations, S>;
    };
  },
  Error,
  CreateSecretInput['secret']
>;
export function useCreateSecretMutation(
  params: {
    selection: SelectionConfig<SecretSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateSecretInput['secret']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<SecretSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: secretMutationKeys.create(),
    mutationFn: (data: CreateSecretInput['secret']) =>
      getClient()
        .secret.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: secretKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
