/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { configKeys } from '../query-keys';
import { configMutationKeys } from '../mutation-keys';
import type { ConfigSelect, ConfigWithRelations, ConfigPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ConfigSelect, ConfigWithRelations, ConfigPatch } from '../../orm/input-types';
/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateConfigMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', configPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateConfigMutation<S extends ConfigSelect>(
  params: {
    selection: {
      fields: S & ConfigSelect;
    } & HookStrictSelect<NoInfer<S>, ConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateConfig: {
          config: InferSelectResult<ConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        configPatch: ConfigPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateConfig: {
      config: InferSelectResult<ConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    configPatch: ConfigPatch;
  }
>;
export function useUpdateConfigMutation(
  params: {
    selection: SelectionConfig<ConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        configPatch: ConfigPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: configMutationKeys.all,
    mutationFn: ({ id, configPatch }: { id: string; configPatch: ConfigPatch }) =>
      getClient()
        .config.update({
          where: {
            id,
          },
          data: configPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: configKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: configKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
