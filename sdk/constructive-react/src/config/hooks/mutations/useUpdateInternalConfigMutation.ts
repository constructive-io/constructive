/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { internalConfigKeys } from '../query-keys';
import { internalConfigMutationKeys } from '../mutation-keys';
import type {
  InternalConfigSelect,
  InternalConfigWithRelations,
  InternalConfigPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  InternalConfigSelect,
  InternalConfigWithRelations,
  InternalConfigPatch,
} from '../../orm/input-types';
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateInternalConfigMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', internalConfigPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateInternalConfigMutation<S extends InternalConfigSelect>(
  params: {
    selection: {
      fields: S & InternalConfigSelect;
    } & HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateInternalConfig: {
          internalConfig: InferSelectResult<InternalConfigWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        internalConfigPatch: InternalConfigPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateInternalConfig: {
      internalConfig: InferSelectResult<InternalConfigWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    internalConfigPatch: InternalConfigPatch;
  }
>;
export function useUpdateInternalConfigMutation(
  params: {
    selection: SelectionConfig<InternalConfigSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        internalConfigPatch: InternalConfigPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InternalConfigSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: internalConfigMutationKeys.all,
    mutationFn: ({
      id,
      internalConfigPatch,
    }: {
      id: string;
      internalConfigPatch: InternalConfigPatch;
    }) =>
      getClient()
        .internalConfig.update({
          where: {
            id,
          },
          data: internalConfigPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: internalConfigKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: internalConfigKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
