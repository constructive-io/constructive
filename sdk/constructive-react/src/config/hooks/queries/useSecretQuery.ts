/**
 * Single item query hook for Secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { secretKeys } from '../query-keys';
import type { SecretSelect, SecretWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SecretSelect, SecretWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const secretQueryKey = secretKeys.detail;
/**
 * Query hook for fetching a single Secret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSecretQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSecretQuery<
  S extends SecretSelect,
  TData = {
    secret: InferSelectResult<SecretWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SecretSelect>;
  } & Omit<
    UseQueryOptions<
      {
        secret: InferSelectResult<SecretWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSecretQuery(
  params: {
    id: string;
    selection: SelectionConfig<SecretSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SecretSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: secretKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .secret.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single Secret without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchSecretQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSecretQuery<S extends SecretSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SecretSelect>;
}): Promise<{
  secret: InferSelectResult<SecretWithRelations, S> | null;
}>;
export async function fetchSecretQuery(params: {
  id: string;
  selection: SelectionConfig<SecretSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SecretSelect>(params.selection);
  return getClient()
    .secret.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single Secret for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchSecretQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSecretQuery<S extends SecretSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SecretSelect>;
  }
): Promise<void>;
export async function prefetchSecretQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SecretSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SecretSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: secretKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .secret.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
