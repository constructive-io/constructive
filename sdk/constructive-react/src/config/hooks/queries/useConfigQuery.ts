/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { configKeys } from '../query-keys';
import type { ConfigSelect, ConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ConfigSelect, ConfigWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const configQueryKey = configKeys.detail;
/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useConfigQuery<
  S extends ConfigSelect,
  TData = {
    config: InferSelectResult<ConfigWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        config: InferSelectResult<ConfigWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useConfigQuery(
  params: {
    id: string;
    selection: SelectionConfig<ConfigSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ConfigSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: configKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .config.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```ts
 * const data = await fetchConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchConfigQuery<S extends ConfigSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ConfigSelect>;
}): Promise<{
  config: InferSelectResult<ConfigWithRelations, S> | null;
}>;
export async function fetchConfigQuery(params: {
  id: string;
  selection: SelectionConfig<ConfigSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ConfigSelect>(params.selection);
  return getClient()
    .config.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Namespace-backed plaintext key-value config store (like a k8s ConfigMap); admin-only, fully CRUD-exposed
 *
 * @example
 * ```ts
 * await prefetchConfigQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchConfigQuery<S extends ConfigSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ConfigSelect>;
  }
): Promise<void>;
export async function prefetchConfigQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ConfigSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ConfigSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: configKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .config.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
