/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { internalConfigKeys } from '../query-keys';
import type { InternalConfigSelect, InternalConfigWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InternalConfigSelect, InternalConfigWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const internalConfigQueryKey = internalConfigKeys.detail;
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInternalConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useInternalConfigQuery<
  S extends InternalConfigSelect,
  TData = {
    internalConfig: InferSelectResult<InternalConfigWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        internalConfig: InferSelectResult<InternalConfigWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useInternalConfigQuery(
  params: {
    id: string;
    selection: SelectionConfig<InternalConfigSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<InternalConfigSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: internalConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .internalConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```ts
 * const data = await fetchInternalConfigQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchInternalConfigQuery<S extends InternalConfigSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
}): Promise<{
  internalConfig: InferSelectResult<InternalConfigWithRelations, S> | null;
}>;
export async function fetchInternalConfigQuery(params: {
  id: string;
  selection: SelectionConfig<InternalConfigSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<InternalConfigSelect>(params.selection);
  return getClient()
    .internalConfig.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```ts
 * await prefetchInternalConfigQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchInternalConfigQuery<S extends InternalConfigSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
  }
): Promise<void>;
export async function prefetchInternalConfigQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<InternalConfigSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<InternalConfigSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: internalConfigKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .internalConfig.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
