/**
 * Single item query hook for PlatformSecret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformSecretKeys } from '../query-keys';
import type { PlatformSecretSelect, PlatformSecretWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformSecretSelect, PlatformSecretWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformSecretQueryKey = platformSecretKeys.detail;
/**
 * Query hook for fetching a single PlatformSecret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformSecretQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformSecretQuery<
  S extends PlatformSecretSelect,
  TData = {
    platformSecret: InferSelectResult<PlatformSecretWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSecretSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformSecret: InferSelectResult<PlatformSecretWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformSecretQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformSecretSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformSecretSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformSecretKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSecret.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single PlatformSecret without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchPlatformSecretQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformSecretQuery<S extends PlatformSecretSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformSecretSelect>;
}): Promise<{
  platformSecret: InferSelectResult<PlatformSecretWithRelations, S> | null;
}>;
export async function fetchPlatformSecretQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformSecretSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformSecretSelect>(params.selection);
  return getClient()
    .platformSecret.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single PlatformSecret for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchPlatformSecretQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformSecretQuery<S extends PlatformSecretSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformSecretSelect>;
  }
): Promise<void>;
export async function prefetchPlatformSecretQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformSecretSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformSecretSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformSecretKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformSecret.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
