/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { internalConfigKeys } from '../query-keys';
import type {
  InternalConfigSelect,
  InternalConfigWithRelations,
  InternalConfigFilter,
  InternalConfigOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  InternalConfigSelect,
  InternalConfigWithRelations,
  InternalConfigFilter,
  InternalConfigOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const internalConfigsQueryKey = internalConfigKeys.list;
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInternalConfigsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useInternalConfigsQuery<
  S extends InternalConfigSelect,
  TData = {
    internalConfigs: ConnectionResult<InferSelectResult<InternalConfigWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, InternalConfigFilter, InternalConfigOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
  } & Omit<
    UseQueryOptions<
      {
        internalConfigs: ConnectionResult<InferSelectResult<InternalConfigWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useInternalConfigsQuery(
  params: {
    selection: ListSelectionConfig<
      InternalConfigSelect,
      InternalConfigFilter,
      InternalConfigOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    InternalConfigSelect,
    InternalConfigFilter,
    InternalConfigOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: internalConfigKeys.list(args),
    queryFn: () => getClient().internalConfig.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```ts
 * const data = await fetchInternalConfigsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchInternalConfigsQuery<S extends InternalConfigSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, InternalConfigFilter, InternalConfigOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
}): Promise<{
  internalConfigs: ConnectionResult<InferSelectResult<InternalConfigWithRelations, S>>;
}>;
export async function fetchInternalConfigsQuery(params: {
  selection: ListSelectionConfig<InternalConfigSelect, InternalConfigFilter, InternalConfigOrderBy>;
}) {
  const args = buildListSelectionArgs<
    InternalConfigSelect,
    InternalConfigFilter,
    InternalConfigOrderBy
  >(params.selection);
  return getClient().internalConfig.findMany(args).unwrap();
}
/**
 * -level plaintext key-value config store; database-resident, never projected into Kubernetes
 *
 * @example
 * ```ts
 * await prefetchInternalConfigsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchInternalConfigsQuery<S extends InternalConfigSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, InternalConfigFilter, InternalConfigOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, InternalConfigSelect>;
  }
): Promise<void>;
export async function prefetchInternalConfigsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      InternalConfigSelect,
      InternalConfigFilter,
      InternalConfigOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    InternalConfigSelect,
    InternalConfigFilter,
    InternalConfigOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: internalConfigKeys.list(args),
    queryFn: () => getClient().internalConfig.findMany(args).unwrap(),
  });
}
