/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appAchievementKeys } from '../query-keys';
import { appAchievementMutationKeys } from '../mutation-keys';
import type { AppAchievementSelect, AppAchievementWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppAchievementSelect, AppAchievementWithRelations } from '../../orm/input-types';
/**
 * This table represents the users progress for particular level requirements, tallying the total count. This table is updated via triggers and should not be updated maually.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppAchievementMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppAchievementMutation<S extends AppAchievementSelect>(
  params: {
    selection: {
      fields: S & AppAchievementSelect;
    } & HookStrictSelect<NoInfer<S>, AppAchievementSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppAchievement: {
          appAchievement: InferSelectResult<AppAchievementWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppAchievement: {
      appAchievement: InferSelectResult<AppAchievementWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppAchievementMutation(
  params: {
    selection: SelectionConfig<AppAchievementSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppAchievementSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appAchievementMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appAchievement.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appAchievementKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appAchievementKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
