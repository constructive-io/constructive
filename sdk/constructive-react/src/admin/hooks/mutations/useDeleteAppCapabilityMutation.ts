/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appCapabilityKeys } from '../query-keys';
import { appCapabilityMutationKeys } from '../mutation-keys';
import type { AppCapabilitySelect, AppCapabilityWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppCapabilitySelect, AppCapabilityWithRelations } from '../../orm/input-types';
/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppCapabilityMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppCapabilityMutation<S extends AppCapabilitySelect>(
  params: {
    selection: {
      fields: S & AppCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, AppCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppCapability: {
          appCapability: InferSelectResult<AppCapabilityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppCapability: {
      appCapability: InferSelectResult<AppCapabilityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppCapabilityMutation(
  params: {
    selection: SelectionConfig<AppCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appCapabilityMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appCapability.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
