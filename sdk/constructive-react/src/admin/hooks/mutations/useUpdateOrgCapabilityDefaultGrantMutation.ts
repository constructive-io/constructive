/**
 * Audit log of capability additions and removals from the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityDefaultGrantKeys } from '../query-keys';
import { orgCapabilityDefaultGrantMutationKeys } from '../mutation-keys';
import type {
  OrgCapabilityDefaultGrantSelect,
  OrgCapabilityDefaultGrantWithRelations,
  OrgCapabilityDefaultGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilityDefaultGrantSelect,
  OrgCapabilityDefaultGrantWithRelations,
  OrgCapabilityDefaultGrantPatch,
} from '../../orm/input-types';
/**
 * Audit log of capability additions and removals from the defaults bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgCapabilityDefaultGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgCapabilityDefaultGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgCapabilityDefaultGrantMutation<
  S extends OrgCapabilityDefaultGrantSelect,
>(
  params: {
    selection: {
      fields: S & OrgCapabilityDefaultGrantSelect;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgCapabilityDefaultGrant: {
          orgCapabilityDefaultGrant: InferSelectResult<OrgCapabilityDefaultGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgCapabilityDefaultGrantPatch: OrgCapabilityDefaultGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgCapabilityDefaultGrant: {
      orgCapabilityDefaultGrant: InferSelectResult<OrgCapabilityDefaultGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgCapabilityDefaultGrantPatch: OrgCapabilityDefaultGrantPatch;
  }
>;
export function useUpdateOrgCapabilityDefaultGrantMutation(
  params: {
    selection: SelectionConfig<OrgCapabilityDefaultGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgCapabilityDefaultGrantPatch: OrgCapabilityDefaultGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgCapabilityDefaultGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgCapabilityDefaultGrantMutationKeys.all,
    mutationFn: ({
      id,
      orgCapabilityDefaultGrantPatch,
    }: {
      id: string;
      orgCapabilityDefaultGrantPatch: OrgCapabilityDefaultGrantPatch;
    }) =>
      getClient()
        .orgCapabilityDefaultGrant.update({
          where: {
            id,
          },
          data: orgCapabilityDefaultGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgCapabilityDefaultGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgCapabilityDefaultGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
