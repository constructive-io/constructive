/**
 * Update mutation hook for Invite
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { inviteKeys } from '../query-keys';
import { inviteMutationKeys } from '../mutation-keys';
import type { InviteSelect, InviteWithRelations, InvitePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InviteSelect, InviteWithRelations, InvitePatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Invite
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateInviteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', invitePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateInviteMutation<S extends InviteSelect>(
  params: {
    selection: {
      fields: S & InviteSelect;
    } & HookStrictSelect<NoInfer<S>, InviteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateInvite: {
          invite: InferSelectResult<InviteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        invitePatch: InvitePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateInvite: {
      invite: InferSelectResult<InviteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    invitePatch: InvitePatch;
  }
>;
export function useUpdateInviteMutation(
  params: {
    selection: SelectionConfig<InviteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        invitePatch: InvitePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InviteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: inviteMutationKeys.all,
    mutationFn: ({ id, invitePatch }: { id: string; invitePatch: InvitePatch }) =>
      getClient()
        .invite.update({
          where: {
            id,
          },
          data: invitePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: inviteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: inviteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
