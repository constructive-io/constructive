/**
 * Join table linking profiles to individual capabilities they include
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileCapabilityKeys } from '../query-keys';
import { orgProfileCapabilityMutationKeys } from '../mutation-keys';
import type {
  OrgProfileCapabilitySelect,
  OrgProfileCapabilityWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileCapabilitySelect,
  OrgProfileCapabilityWithRelations,
} from '../../orm/input-types';
/**
 * Join table linking profiles to individual capabilities they include
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgProfileCapabilityMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgProfileCapabilityMutation<S extends OrgProfileCapabilitySelect>(
  params: {
    selection: {
      fields: S & OrgProfileCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgProfileCapability: {
          orgProfileCapability: InferSelectResult<OrgProfileCapabilityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgProfileCapability: {
      orgProfileCapability: InferSelectResult<OrgProfileCapabilityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgProfileCapabilityMutation(
  params: {
    selection: SelectionConfig<OrgProfileCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfileCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileCapabilityMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgProfileCapability.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgProfileCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgProfileCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
