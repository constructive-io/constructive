/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appCapabilityDefaultCapabilityKeys } from '../query-keys';
import { appCapabilityDefaultCapabilityMutationKeys } from '../mutation-keys';
import type {
  AppCapabilityDefaultCapabilitySelect,
  AppCapabilityDefaultCapabilityWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppCapabilityDefaultCapabilitySelect,
  AppCapabilityDefaultCapabilityWithRelations,
} from '../../orm/input-types';
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppCapabilityDefaultCapabilityMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppCapabilityDefaultCapabilityMutation<
  S extends AppCapabilityDefaultCapabilitySelect,
>(
  params: {
    selection: {
      fields: S & AppCapabilityDefaultCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppCapabilityDefaultCapability: {
          appCapabilityDefaultCapability: InferSelectResult<
            AppCapabilityDefaultCapabilityWithRelations,
            S
          >;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppCapabilityDefaultCapability: {
      appCapabilityDefaultCapability: InferSelectResult<
        AppCapabilityDefaultCapabilityWithRelations,
        S
      >;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppCapabilityDefaultCapabilityMutation(
  params: {
    selection: SelectionConfig<AppCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppCapabilityDefaultCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appCapabilityDefaultCapabilityMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appCapabilityDefaultCapability.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appCapabilityDefaultCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appCapabilityDefaultCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
