/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityDefaultCapabilityKeys } from '../query-keys';
import { orgCapabilityDefaultCapabilityMutationKeys } from '../mutation-keys';
import type {
  OrgCapabilityDefaultCapabilitySelect,
  OrgCapabilityDefaultCapabilityWithRelations,
  CreateOrgCapabilityDefaultCapabilityInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilityDefaultCapabilitySelect,
  OrgCapabilityDefaultCapabilityWithRelations,
  CreateOrgCapabilityDefaultCapabilityInput,
} from '../../orm/input-types';
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateOrgCapabilityDefaultCapabilityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateOrgCapabilityDefaultCapabilityMutation<
  S extends OrgCapabilityDefaultCapabilitySelect,
>(
  params: {
    selection: {
      fields: S & OrgCapabilityDefaultCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createOrgCapabilityDefaultCapability: {
          orgCapabilityDefaultCapability: InferSelectResult<
            OrgCapabilityDefaultCapabilityWithRelations,
            S
          >;
        };
      },
      Error,
      CreateOrgCapabilityDefaultCapabilityInput['orgCapabilityDefaultCapability']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createOrgCapabilityDefaultCapability: {
      orgCapabilityDefaultCapability: InferSelectResult<
        OrgCapabilityDefaultCapabilityWithRelations,
        S
      >;
    };
  },
  Error,
  CreateOrgCapabilityDefaultCapabilityInput['orgCapabilityDefaultCapability']
>;
export function useCreateOrgCapabilityDefaultCapabilityMutation(
  params: {
    selection: SelectionConfig<OrgCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      CreateOrgCapabilityDefaultCapabilityInput['orgCapabilityDefaultCapability']
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgCapabilityDefaultCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgCapabilityDefaultCapabilityMutationKeys.create(),
    mutationFn: (
      data: CreateOrgCapabilityDefaultCapabilityInput['orgCapabilityDefaultCapability']
    ) =>
      getClient()
        .orgCapabilityDefaultCapability.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: orgCapabilityDefaultCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
