/**
 * Stores the default capability bitmask assigned to new members upon joining
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appCapabilityDefaultKeys } from '../query-keys';
import { appCapabilityDefaultMutationKeys } from '../mutation-keys';
import type {
  AppCapabilityDefaultSelect,
  AppCapabilityDefaultWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppCapabilityDefaultSelect,
  AppCapabilityDefaultWithRelations,
} from '../../orm/input-types';
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppCapabilityDefaultMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppCapabilityDefaultMutation<S extends AppCapabilityDefaultSelect>(
  params: {
    selection: {
      fields: S & AppCapabilityDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppCapabilityDefault: {
          appCapabilityDefault: InferSelectResult<AppCapabilityDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppCapabilityDefault: {
      appCapabilityDefault: InferSelectResult<AppCapabilityDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppCapabilityDefaultMutation(
  params: {
    selection: SelectionConfig<AppCapabilityDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppCapabilityDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appCapabilityDefaultMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appCapabilityDefault.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appCapabilityDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appCapabilityDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
