/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityDefaultCapabilityKeys } from '../query-keys';
import { orgCapabilityDefaultCapabilityMutationKeys } from '../mutation-keys';
import type {
  OrgCapabilityDefaultCapabilitySelect,
  OrgCapabilityDefaultCapabilityWithRelations,
  OrgCapabilityDefaultCapabilityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilityDefaultCapabilitySelect,
  OrgCapabilityDefaultCapabilityWithRelations,
  OrgCapabilityDefaultCapabilityPatch,
} from '../../orm/input-types';
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgCapabilityDefaultCapabilityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgCapabilityDefaultCapabilityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgCapabilityDefaultCapabilityMutation<
  S extends OrgCapabilityDefaultCapabilitySelect,
>(
  params: {
    selection: {
      fields: S & OrgCapabilityDefaultCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgCapabilityDefaultCapability: {
          orgCapabilityDefaultCapability: InferSelectResult<
            OrgCapabilityDefaultCapabilityWithRelations,
            S
          >;
        };
      },
      Error,
      {
        id: string;
        orgCapabilityDefaultCapabilityPatch: OrgCapabilityDefaultCapabilityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgCapabilityDefaultCapability: {
      orgCapabilityDefaultCapability: InferSelectResult<
        OrgCapabilityDefaultCapabilityWithRelations,
        S
      >;
    };
  },
  Error,
  {
    id: string;
    orgCapabilityDefaultCapabilityPatch: OrgCapabilityDefaultCapabilityPatch;
  }
>;
export function useUpdateOrgCapabilityDefaultCapabilityMutation(
  params: {
    selection: SelectionConfig<OrgCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgCapabilityDefaultCapabilityPatch: OrgCapabilityDefaultCapabilityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgCapabilityDefaultCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgCapabilityDefaultCapabilityMutationKeys.all,
    mutationFn: ({
      id,
      orgCapabilityDefaultCapabilityPatch,
    }: {
      id: string;
      orgCapabilityDefaultCapabilityPatch: OrgCapabilityDefaultCapabilityPatch;
    }) =>
      getClient()
        .orgCapabilityDefaultCapability.update({
          where: {
            id,
          },
          data: orgCapabilityDefaultCapabilityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgCapabilityDefaultCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgCapabilityDefaultCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
