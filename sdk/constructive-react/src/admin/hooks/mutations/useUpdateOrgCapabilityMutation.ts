/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityKeys } from '../query-keys';
import { orgCapabilityMutationKeys } from '../mutation-keys';
import type {
  OrgCapabilitySelect,
  OrgCapabilityWithRelations,
  OrgCapabilityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilitySelect,
  OrgCapabilityWithRelations,
  OrgCapabilityPatch,
} from '../../orm/input-types';
/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgCapabilityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgCapabilityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgCapabilityMutation<S extends OrgCapabilitySelect>(
  params: {
    selection: {
      fields: S & OrgCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgCapability: {
          orgCapability: InferSelectResult<OrgCapabilityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgCapabilityPatch: OrgCapabilityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgCapability: {
      orgCapability: InferSelectResult<OrgCapabilityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgCapabilityPatch: OrgCapabilityPatch;
  }
>;
export function useUpdateOrgCapabilityMutation(
  params: {
    selection: SelectionConfig<OrgCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgCapabilityPatch: OrgCapabilityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgCapabilityMutationKeys.all,
    mutationFn: ({
      id,
      orgCapabilityPatch,
    }: {
      id: string;
      orgCapabilityPatch: OrgCapabilityPatch;
    }) =>
      getClient()
        .orgCapability.update({
          where: {
            id,
          },
          data: orgCapabilityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
