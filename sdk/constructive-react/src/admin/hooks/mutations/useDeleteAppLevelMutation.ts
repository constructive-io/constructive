/**
 * Levels for achievement
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLevelKeys } from '../query-keys';
import { appLevelMutationKeys } from '../mutation-keys';
import type { AppLevelSelect, AppLevelWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLevelSelect, AppLevelWithRelations } from '../../orm/input-types';
/**
 * Levels for achievement
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLevelMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppLevelMutation<S extends AppLevelSelect>(
  params: {
    selection: {
      fields: S & AppLevelSelect;
    } & HookStrictSelect<NoInfer<S>, AppLevelSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppLevel: {
          appLevel: InferSelectResult<AppLevelWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppLevel: {
      appLevel: InferSelectResult<AppLevelWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppLevelMutation(
  params: {
    selection: SelectionConfig<AppLevelSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLevelSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLevelMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appLevel.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appLevelKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLevelKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
