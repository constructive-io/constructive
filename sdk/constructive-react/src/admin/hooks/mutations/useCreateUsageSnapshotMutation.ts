/**
 * Periodic snapshot of a single metric for a database. Collected by the snapshot_usage() cron job in constructive-limits. Each row records one metric measurement (e.g. reads, writes, storage_bytes) at a point in time, with optional dimensions for sub-metric breakdowns.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { usageSnapshotKeys } from '../query-keys';
import { usageSnapshotMutationKeys } from '../mutation-keys';
import type {
  UsageSnapshotSelect,
  UsageSnapshotWithRelations,
  CreateUsageSnapshotInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  UsageSnapshotSelect,
  UsageSnapshotWithRelations,
  CreateUsageSnapshotInput,
} from '../../orm/input-types';
/**
 * Periodic snapshot of a single metric for a database. Collected by the snapshot_usage() cron job in constructive-limits. Each row records one metric measurement (e.g. reads, writes, storage_bytes) at a point in time, with optional dimensions for sub-metric breakdowns.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateUsageSnapshotMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateUsageSnapshotMutation<S extends UsageSnapshotSelect>(
  params: {
    selection: {
      fields: S & UsageSnapshotSelect;
    } & HookStrictSelect<NoInfer<S>, UsageSnapshotSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createUsageSnapshot: {
          usageSnapshot: InferSelectResult<UsageSnapshotWithRelations, S>;
        };
      },
      Error,
      CreateUsageSnapshotInput['usageSnapshot']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createUsageSnapshot: {
      usageSnapshot: InferSelectResult<UsageSnapshotWithRelations, S>;
    };
  },
  Error,
  CreateUsageSnapshotInput['usageSnapshot']
>;
export function useCreateUsageSnapshotMutation(
  params: {
    selection: SelectionConfig<UsageSnapshotSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateUsageSnapshotInput['usageSnapshot']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<UsageSnapshotSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: usageSnapshotMutationKeys.create(),
    mutationFn: (data: CreateUsageSnapshotInput['usageSnapshot']) =>
      getClient()
        .usageSnapshot.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: usageSnapshotKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
