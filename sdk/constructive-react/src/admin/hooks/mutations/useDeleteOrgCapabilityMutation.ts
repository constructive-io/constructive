/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityKeys } from '../query-keys';
import { orgCapabilityMutationKeys } from '../mutation-keys';
import type { OrgCapabilitySelect, OrgCapabilityWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { OrgCapabilitySelect, OrgCapabilityWithRelations } from '../../orm/input-types';
/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteOrgCapabilityMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteOrgCapabilityMutation<S extends OrgCapabilitySelect>(
  params: {
    selection: {
      fields: S & OrgCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteOrgCapability: {
          orgCapability: InferSelectResult<OrgCapabilityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteOrgCapability: {
      orgCapability: InferSelectResult<OrgCapabilityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteOrgCapabilityMutation(
  params: {
    selection: SelectionConfig<OrgCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgCapabilityMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .orgCapability.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: orgCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
