/**
 * Join table linking profiles to individual capabilities they include
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgProfileCapabilityKeys } from '../query-keys';
import { orgProfileCapabilityMutationKeys } from '../mutation-keys';
import type {
  OrgProfileCapabilitySelect,
  OrgProfileCapabilityWithRelations,
  OrgProfileCapabilityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgProfileCapabilitySelect,
  OrgProfileCapabilityWithRelations,
  OrgProfileCapabilityPatch,
} from '../../orm/input-types';
/**
 * Join table linking profiles to individual capabilities they include
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateOrgProfileCapabilityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', orgProfileCapabilityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateOrgProfileCapabilityMutation<S extends OrgProfileCapabilitySelect>(
  params: {
    selection: {
      fields: S & OrgProfileCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, OrgProfileCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateOrgProfileCapability: {
          orgProfileCapability: InferSelectResult<OrgProfileCapabilityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        orgProfileCapabilityPatch: OrgProfileCapabilityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateOrgProfileCapability: {
      orgProfileCapability: InferSelectResult<OrgProfileCapabilityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    orgProfileCapabilityPatch: OrgProfileCapabilityPatch;
  }
>;
export function useUpdateOrgProfileCapabilityMutation(
  params: {
    selection: SelectionConfig<OrgProfileCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        orgProfileCapabilityPatch: OrgProfileCapabilityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<OrgProfileCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: orgProfileCapabilityMutationKeys.all,
    mutationFn: ({
      id,
      orgProfileCapabilityPatch,
    }: {
      id: string;
      orgProfileCapabilityPatch: OrgProfileCapabilityPatch;
    }) =>
      getClient()
        .orgProfileCapability.update({
          where: {
            id,
          },
          data: orgProfileCapabilityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: orgProfileCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: orgProfileCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
