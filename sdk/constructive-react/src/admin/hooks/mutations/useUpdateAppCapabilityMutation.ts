/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appCapabilityKeys } from '../query-keys';
import { appCapabilityMutationKeys } from '../mutation-keys';
import type {
  AppCapabilitySelect,
  AppCapabilityWithRelations,
  AppCapabilityPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppCapabilitySelect,
  AppCapabilityWithRelations,
  AppCapabilityPatch,
} from '../../orm/input-types';
/**
 * Defines available capabilities as named bits within a bitmask, used by the RBAC system for access control
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppCapabilityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appCapabilityPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppCapabilityMutation<S extends AppCapabilitySelect>(
  params: {
    selection: {
      fields: S & AppCapabilitySelect;
    } & HookStrictSelect<NoInfer<S>, AppCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppCapability: {
          appCapability: InferSelectResult<AppCapabilityWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appCapabilityPatch: AppCapabilityPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppCapability: {
      appCapability: InferSelectResult<AppCapabilityWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appCapabilityPatch: AppCapabilityPatch;
  }
>;
export function useUpdateAppCapabilityMutation(
  params: {
    selection: SelectionConfig<AppCapabilitySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appCapabilityPatch: AppCapabilityPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppCapabilitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appCapabilityMutationKeys.all,
    mutationFn: ({
      id,
      appCapabilityPatch,
    }: {
      id: string;
      appCapabilityPatch: AppCapabilityPatch;
    }) =>
      getClient()
        .appCapability.update({
          where: {
            id,
          },
          data: appCapabilityPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appCapabilityKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appCapabilityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
