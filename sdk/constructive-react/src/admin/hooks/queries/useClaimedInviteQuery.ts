/**
 * Records of successfully claimed invitations, linking senders to receivers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { claimedInviteKeys } from '../query-keys';
import type { ClaimedInviteSelect, ClaimedInviteWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ClaimedInviteSelect, ClaimedInviteWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const claimedInviteQueryKey = claimedInviteKeys.detail;
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useClaimedInviteQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useClaimedInviteQuery<
  S extends ClaimedInviteSelect,
  TData = {
    claimedInvite: InferSelectResult<ClaimedInviteWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ClaimedInviteSelect>;
  } & Omit<
    UseQueryOptions<
      {
        claimedInvite: InferSelectResult<ClaimedInviteWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useClaimedInviteQuery(
  params: {
    id: string;
    selection: SelectionConfig<ClaimedInviteSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ClaimedInviteSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: claimedInviteKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .claimedInvite.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```ts
 * const data = await fetchClaimedInviteQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchClaimedInviteQuery<S extends ClaimedInviteSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ClaimedInviteSelect>;
}): Promise<{
  claimedInvite: InferSelectResult<ClaimedInviteWithRelations, S> | null;
}>;
export async function fetchClaimedInviteQuery(params: {
  id: string;
  selection: SelectionConfig<ClaimedInviteSelect>;
}) {
  const args = buildSelectionArgs<ClaimedInviteSelect>(params.selection);
  return getClient()
    .claimedInvite.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Records of successfully claimed invitations, linking senders to receivers
 *
 * @example
 * ```ts
 * await prefetchClaimedInviteQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchClaimedInviteQuery<S extends ClaimedInviteSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ClaimedInviteSelect>;
  }
): Promise<void>;
export async function prefetchClaimedInviteQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ClaimedInviteSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ClaimedInviteSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: claimedInviteKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .claimedInvite.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
