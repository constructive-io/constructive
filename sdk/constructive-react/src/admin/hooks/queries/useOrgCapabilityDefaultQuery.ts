/**
 * Stores the default capability bitmask assigned to new members upon joining
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityDefaultKeys } from '../query-keys';
import type {
  OrgCapabilityDefaultSelect,
  OrgCapabilityDefaultWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilityDefaultSelect,
  OrgCapabilityDefaultWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgCapabilityDefaultQueryKey = orgCapabilityDefaultKeys.detail;
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgCapabilityDefaultQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgCapabilityDefaultQuery<
  S extends OrgCapabilityDefaultSelect,
  TData = {
    orgCapabilityDefault: InferSelectResult<OrgCapabilityDefaultWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgCapabilityDefault: InferSelectResult<OrgCapabilityDefaultWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgCapabilityDefaultQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgCapabilityDefaultSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgCapabilityDefaultSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgCapabilityDefaultKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgCapabilityDefault.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```ts
 * const data = await fetchOrgCapabilityDefaultQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgCapabilityDefaultQuery<S extends OrgCapabilityDefaultSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultSelect>;
}): Promise<{
  orgCapabilityDefault: InferSelectResult<OrgCapabilityDefaultWithRelations, S> | null;
}>;
export async function fetchOrgCapabilityDefaultQuery(params: {
  id: string;
  selection: SelectionConfig<OrgCapabilityDefaultSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgCapabilityDefaultSelect>(params.selection);
  return getClient()
    .orgCapabilityDefault.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```ts
 * await prefetchOrgCapabilityDefaultQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgCapabilityDefaultQuery<S extends OrgCapabilityDefaultSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultSelect>;
  }
): Promise<void>;
export async function prefetchOrgCapabilityDefaultQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgCapabilityDefaultSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgCapabilityDefaultSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgCapabilityDefaultKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgCapabilityDefault.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
