/**
 * Stores the default capability bitmask assigned to new members upon joining
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appCapabilityDefaultKeys } from '../query-keys';
import type {
  AppCapabilityDefaultSelect,
  AppCapabilityDefaultWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppCapabilityDefaultSelect,
  AppCapabilityDefaultWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appCapabilityDefaultQueryKey = appCapabilityDefaultKeys.detail;
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppCapabilityDefaultQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppCapabilityDefaultQuery<
  S extends AppCapabilityDefaultSelect,
  TData = {
    appCapabilityDefault: InferSelectResult<AppCapabilityDefaultWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appCapabilityDefault: InferSelectResult<AppCapabilityDefaultWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppCapabilityDefaultQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppCapabilityDefaultSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppCapabilityDefaultSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appCapabilityDefaultKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appCapabilityDefault.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```ts
 * const data = await fetchAppCapabilityDefaultQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppCapabilityDefaultQuery<S extends AppCapabilityDefaultSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultSelect>;
}): Promise<{
  appCapabilityDefault: InferSelectResult<AppCapabilityDefaultWithRelations, S> | null;
}>;
export async function fetchAppCapabilityDefaultQuery(params: {
  id: string;
  selection: SelectionConfig<AppCapabilityDefaultSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppCapabilityDefaultSelect>(params.selection);
  return getClient()
    .appCapabilityDefault.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Stores the default capability bitmask assigned to new members upon joining
 *
 * @example
 * ```ts
 * await prefetchAppCapabilityDefaultQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppCapabilityDefaultQuery<S extends AppCapabilityDefaultSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultSelect>;
  }
): Promise<void>;
export async function prefetchAppCapabilityDefaultQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppCapabilityDefaultSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppCapabilityDefaultSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appCapabilityDefaultKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appCapabilityDefault.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
