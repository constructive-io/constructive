/**
 * Audit log of capability additions and removals from the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityDefaultGrantKeys } from '../query-keys';
import type {
  OrgCapabilityDefaultGrantSelect,
  OrgCapabilityDefaultGrantWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilityDefaultGrantSelect,
  OrgCapabilityDefaultGrantWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgCapabilityDefaultGrantQueryKey = orgCapabilityDefaultGrantKeys.detail;
/**
 * Audit log of capability additions and removals from the defaults bitmask
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgCapabilityDefaultGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgCapabilityDefaultGrantQuery<
  S extends OrgCapabilityDefaultGrantSelect,
  TData = {
    orgCapabilityDefaultGrant: InferSelectResult<OrgCapabilityDefaultGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgCapabilityDefaultGrant: InferSelectResult<
          OrgCapabilityDefaultGrantWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgCapabilityDefaultGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgCapabilityDefaultGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgCapabilityDefaultGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgCapabilityDefaultGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgCapabilityDefaultGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Audit log of capability additions and removals from the defaults bitmask
 *
 * @example
 * ```ts
 * const data = await fetchOrgCapabilityDefaultGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgCapabilityDefaultGrantQuery<
  S extends OrgCapabilityDefaultGrantSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultGrantSelect>;
}): Promise<{
  orgCapabilityDefaultGrant: InferSelectResult<OrgCapabilityDefaultGrantWithRelations, S> | null;
}>;
export async function fetchOrgCapabilityDefaultGrantQuery(params: {
  id: string;
  selection: SelectionConfig<OrgCapabilityDefaultGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgCapabilityDefaultGrantSelect>(params.selection);
  return getClient()
    .orgCapabilityDefaultGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Audit log of capability additions and removals from the defaults bitmask
 *
 * @example
 * ```ts
 * await prefetchOrgCapabilityDefaultGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgCapabilityDefaultGrantQuery<
  S extends OrgCapabilityDefaultGrantSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultGrantSelect>;
  }
): Promise<void>;
export async function prefetchOrgCapabilityDefaultGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgCapabilityDefaultGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgCapabilityDefaultGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgCapabilityDefaultGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgCapabilityDefaultGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
