/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { orgCapabilityDefaultCapabilityKeys } from '../query-keys';
import type {
  OrgCapabilityDefaultCapabilitySelect,
  OrgCapabilityDefaultCapabilityWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  OrgCapabilityDefaultCapabilitySelect,
  OrgCapabilityDefaultCapabilityWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const orgCapabilityDefaultCapabilityQueryKey = orgCapabilityDefaultCapabilityKeys.detail;
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useOrgCapabilityDefaultCapabilityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useOrgCapabilityDefaultCapabilityQuery<
  S extends OrgCapabilityDefaultCapabilitySelect,
  TData = {
    orgCapabilityDefaultCapability: InferSelectResult<
      OrgCapabilityDefaultCapabilityWithRelations,
      S
    > | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        orgCapabilityDefaultCapability: InferSelectResult<
          OrgCapabilityDefaultCapabilityWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useOrgCapabilityDefaultCapabilityQuery(
  params: {
    id: string;
    selection: SelectionConfig<OrgCapabilityDefaultCapabilitySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<OrgCapabilityDefaultCapabilitySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: orgCapabilityDefaultCapabilityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgCapabilityDefaultCapability.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```ts
 * const data = await fetchOrgCapabilityDefaultCapabilityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchOrgCapabilityDefaultCapabilityQuery<
  S extends OrgCapabilityDefaultCapabilitySelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultCapabilitySelect>;
}): Promise<{
  orgCapabilityDefaultCapability: InferSelectResult<
    OrgCapabilityDefaultCapabilityWithRelations,
    S
  > | null;
}>;
export async function fetchOrgCapabilityDefaultCapabilityQuery(params: {
  id: string;
  selection: SelectionConfig<OrgCapabilityDefaultCapabilitySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<OrgCapabilityDefaultCapabilitySelect>(params.selection);
  return getClient()
    .orgCapabilityDefaultCapability.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```ts
 * await prefetchOrgCapabilityDefaultCapabilityQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchOrgCapabilityDefaultCapabilityQuery<
  S extends OrgCapabilityDefaultCapabilitySelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, OrgCapabilityDefaultCapabilitySelect>;
  }
): Promise<void>;
export async function prefetchOrgCapabilityDefaultCapabilityQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<OrgCapabilityDefaultCapabilitySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<OrgCapabilityDefaultCapabilitySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: orgCapabilityDefaultCapabilityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .orgCapabilityDefaultCapability.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
