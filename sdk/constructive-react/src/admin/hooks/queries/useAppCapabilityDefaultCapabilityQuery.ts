/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appCapabilityDefaultCapabilityKeys } from '../query-keys';
import type {
  AppCapabilityDefaultCapabilitySelect,
  AppCapabilityDefaultCapabilityWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppCapabilityDefaultCapabilitySelect,
  AppCapabilityDefaultCapabilityWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appCapabilityDefaultCapabilityQueryKey = appCapabilityDefaultCapabilityKeys.detail;
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppCapabilityDefaultCapabilityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppCapabilityDefaultCapabilityQuery<
  S extends AppCapabilityDefaultCapabilitySelect,
  TData = {
    appCapabilityDefaultCapability: InferSelectResult<
      AppCapabilityDefaultCapabilityWithRelations,
      S
    > | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultCapabilitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        appCapabilityDefaultCapability: InferSelectResult<
          AppCapabilityDefaultCapabilityWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppCapabilityDefaultCapabilityQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppCapabilityDefaultCapabilitySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppCapabilityDefaultCapabilitySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appCapabilityDefaultCapabilityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appCapabilityDefaultCapability.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```ts
 * const data = await fetchAppCapabilityDefaultCapabilityQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppCapabilityDefaultCapabilityQuery<
  S extends AppCapabilityDefaultCapabilitySelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultCapabilitySelect>;
}): Promise<{
  appCapabilityDefaultCapability: InferSelectResult<
    AppCapabilityDefaultCapabilityWithRelations,
    S
  > | null;
}>;
export async function fetchAppCapabilityDefaultCapabilityQuery(params: {
  id: string;
  selection: SelectionConfig<AppCapabilityDefaultCapabilitySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppCapabilityDefaultCapabilitySelect>(params.selection);
  return getClient()
    .appCapabilityDefaultCapability.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Join table linking capability defaults to individual capabilities; recompute trigger rebuilds the defaults bitmask
 *
 * @example
 * ```ts
 * await prefetchAppCapabilityDefaultCapabilityQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppCapabilityDefaultCapabilityQuery<
  S extends AppCapabilityDefaultCapabilitySelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppCapabilityDefaultCapabilitySelect>;
  }
): Promise<void>;
export async function prefetchAppCapabilityDefaultCapabilityQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppCapabilityDefaultCapabilitySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppCapabilityDefaultCapabilitySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appCapabilityDefaultCapabilityKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appCapabilityDefaultCapability.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
