/**
 * Create mutation hook for DeclaredCapacity
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { declaredCapacityKeys } from '../query-keys';
import { declaredCapacityMutationKeys } from '../mutation-keys';
import type {
  DeclaredCapacitySelect,
  DeclaredCapacityWithRelations,
  CreateDeclaredCapacityInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DeclaredCapacitySelect,
  DeclaredCapacityWithRelations,
  CreateDeclaredCapacityInput,
} from '../../orm/input-types';
/**
 * Mutation hook for creating a DeclaredCapacity
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDeclaredCapacityMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateDeclaredCapacityMutation<S extends DeclaredCapacitySelect>(
  params: {
    selection: {
      fields: S & DeclaredCapacitySelect;
    } & HookStrictSelect<NoInfer<S>, DeclaredCapacitySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createDeclaredCapacity: {
          declaredCapacity: InferSelectResult<DeclaredCapacityWithRelations, S>;
        };
      },
      Error,
      CreateDeclaredCapacityInput['declaredCapacity']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createDeclaredCapacity: {
      declaredCapacity: InferSelectResult<DeclaredCapacityWithRelations, S>;
    };
  },
  Error,
  CreateDeclaredCapacityInput['declaredCapacity']
>;
export function useCreateDeclaredCapacityMutation(
  params: {
    selection: SelectionConfig<DeclaredCapacitySelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateDeclaredCapacityInput['declaredCapacity']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DeclaredCapacitySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: declaredCapacityMutationKeys.create(),
    mutationFn: (data: CreateDeclaredCapacityInput['declaredCapacity']) =>
      getClient()
        .declaredCapacity.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: declaredCapacityKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
