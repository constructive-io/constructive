/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceStatusCheckKeys } from '../query-keys';
import { resourceStatusCheckMutationKeys } from '../mutation-keys';
import type {
  ResourceStatusCheckSelect,
  ResourceStatusCheckWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceStatusCheckSelect,
  ResourceStatusCheckWithRelations,
} from '../../orm/input-types';
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteResourceStatusCheckMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteResourceStatusCheckMutation<S extends ResourceStatusCheckSelect>(
  params: {
    selection: {
      fields: S & ResourceStatusCheckSelect;
    } & HookStrictSelect<NoInfer<S>, ResourceStatusCheckSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteResourceStatusCheck: {
          resourceStatusCheck: InferSelectResult<ResourceStatusCheckWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteResourceStatusCheck: {
      resourceStatusCheck: InferSelectResult<ResourceStatusCheckWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteResourceStatusCheckMutation(
  params: {
    selection: SelectionConfig<ResourceStatusCheckSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceStatusCheckSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceStatusCheckMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .resourceStatusCheck.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: resourceStatusCheckKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: resourceStatusCheckKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
