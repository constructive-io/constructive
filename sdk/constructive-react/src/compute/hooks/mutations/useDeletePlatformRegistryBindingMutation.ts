/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRegistryBindingKeys } from '../query-keys';
import { platformRegistryBindingMutationKeys } from '../mutation-keys';
import type {
  PlatformRegistryBindingSelect,
  PlatformRegistryBindingWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformRegistryBindingSelect,
  PlatformRegistryBindingWithRelations,
} from '../../orm/input-types';
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformRegistryBindingMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformRegistryBindingMutation<S extends PlatformRegistryBindingSelect>(
  params: {
    selection: {
      fields: S & PlatformRegistryBindingSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformRegistryBinding: {
          platformRegistryBinding: InferSelectResult<PlatformRegistryBindingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformRegistryBinding: {
      platformRegistryBinding: InferSelectResult<PlatformRegistryBindingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformRegistryBindingMutation(
  params: {
    selection: SelectionConfig<PlatformRegistryBindingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformRegistryBindingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformRegistryBindingMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformRegistryBinding.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformRegistryBindingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformRegistryBindingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
