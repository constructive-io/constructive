/**
 * Source repositories, hosted locally or on an external provider
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryKeys } from '../query-keys';
import { repositoryMutationKeys } from '../mutation-keys';
import type {
  RepositorySelect,
  RepositoryWithRelations,
  RepositoryPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositorySelect,
  RepositoryWithRelations,
  RepositoryPatch,
} from '../../orm/input-types';
/**
 * Source repositories, hosted locally or on an external provider
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRepositoryMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', repositoryPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRepositoryMutation<S extends RepositorySelect>(
  params: {
    selection: {
      fields: S & RepositorySelect;
    } & HookStrictSelect<NoInfer<S>, RepositorySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRepository: {
          repository: InferSelectResult<RepositoryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        repositoryPatch: RepositoryPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRepository: {
      repository: InferSelectResult<RepositoryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    repositoryPatch: RepositoryPatch;
  }
>;
export function useUpdateRepositoryMutation(
  params: {
    selection: SelectionConfig<RepositorySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        repositoryPatch: RepositoryPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositorySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryMutationKeys.all,
    mutationFn: ({ id, repositoryPatch }: { id: string; repositoryPatch: RepositoryPatch }) =>
      getClient()
        .repository.update({
          where: {
            id,
          },
          data: repositoryPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: repositoryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
