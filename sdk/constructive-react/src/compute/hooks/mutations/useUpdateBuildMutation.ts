/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildKeys } from '../query-keys';
import { buildMutationKeys } from '../mutation-keys';
import type { BuildSelect, BuildWithRelations, BuildPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuildSelect, BuildWithRelations, BuildPatch } from '../../orm/input-types';
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateBuildMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', buildPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateBuildMutation<S extends BuildSelect>(
  params: {
    selection: {
      fields: S & BuildSelect;
    } & HookStrictSelect<NoInfer<S>, BuildSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateBuild: {
          build: InferSelectResult<BuildWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        buildPatch: BuildPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateBuild: {
      build: InferSelectResult<BuildWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    buildPatch: BuildPatch;
  }
>;
export function useUpdateBuildMutation(
  params: {
    selection: SelectionConfig<BuildSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        buildPatch: BuildPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BuildSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: buildMutationKeys.all,
    mutationFn: ({ id, buildPatch }: { id: string; buildPatch: BuildPatch }) =>
      getClient()
        .build.update({
          where: {
            id,
          },
          data: buildPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: buildKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: buildKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
