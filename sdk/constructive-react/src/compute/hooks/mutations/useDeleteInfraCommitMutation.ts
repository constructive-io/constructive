/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraCommitKeys } from '../query-keys';
import { infraCommitMutationKeys } from '../mutation-keys';
import type { InfraCommitSelect, InfraCommitWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InfraCommitSelect, InfraCommitWithRelations } from '../../orm/input-types';
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteInfraCommitMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteInfraCommitMutation<S extends InfraCommitSelect>(
  params: {
    selection: {
      fields: S & InfraCommitSelect;
    } & HookStrictSelect<NoInfer<S>, InfraCommitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteInfraCommit: {
          infraCommit: InferSelectResult<InfraCommitWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        databaseId: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteInfraCommit: {
      infraCommit: InferSelectResult<InfraCommitWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    databaseId: string;
  }
>;
export function useDeleteInfraCommitMutation(
  params: {
    selection: SelectionConfig<InfraCommitSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        databaseId: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InfraCommitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: infraCommitMutationKeys.all,
    mutationFn: ({ id, databaseId }: { id: string; databaseId: string }) =>
      getClient()
        .infraCommit.delete({
          where: {
            id,
            databaseId,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: infraCommitKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: infraCommitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
