/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webhookEventKeys } from '../query-keys';
import { webhookEventMutationKeys } from '../mutation-keys';
import type { WebhookEventSelect, WebhookEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { WebhookEventSelect, WebhookEventWithRelations } from '../../orm/input-types';
/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteWebhookEventMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteWebhookEventMutation<S extends WebhookEventSelect>(
  params: {
    selection: {
      fields: S & WebhookEventSelect;
    } & HookStrictSelect<NoInfer<S>, WebhookEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteWebhookEvent: {
          webhookEvent: InferSelectResult<WebhookEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteWebhookEvent: {
      webhookEvent: InferSelectResult<WebhookEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteWebhookEventMutation(
  params: {
    selection: SelectionConfig<WebhookEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<WebhookEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: webhookEventMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .webhookEvent.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: webhookEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: webhookEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
