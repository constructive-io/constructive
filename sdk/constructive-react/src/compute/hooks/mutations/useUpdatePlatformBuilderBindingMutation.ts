/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformBuilderBindingKeys } from '../query-keys';
import { platformBuilderBindingMutationKeys } from '../mutation-keys';
import type {
  PlatformBuilderBindingSelect,
  PlatformBuilderBindingWithRelations,
  PlatformBuilderBindingPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformBuilderBindingSelect,
  PlatformBuilderBindingWithRelations,
  PlatformBuilderBindingPatch,
} from '../../orm/input-types';
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformBuilderBindingMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformBuilderBindingPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformBuilderBindingMutation<S extends PlatformBuilderBindingSelect>(
  params: {
    selection: {
      fields: S & PlatformBuilderBindingSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformBuilderBinding: {
          platformBuilderBinding: InferSelectResult<PlatformBuilderBindingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformBuilderBindingPatch: PlatformBuilderBindingPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformBuilderBinding: {
      platformBuilderBinding: InferSelectResult<PlatformBuilderBindingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformBuilderBindingPatch: PlatformBuilderBindingPatch;
  }
>;
export function useUpdatePlatformBuilderBindingMutation(
  params: {
    selection: SelectionConfig<PlatformBuilderBindingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformBuilderBindingPatch: PlatformBuilderBindingPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformBuilderBindingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformBuilderBindingMutationKeys.all,
    mutationFn: ({
      id,
      platformBuilderBindingPatch,
    }: {
      id: string;
      platformBuilderBindingPatch: PlatformBuilderBindingPatch;
    }) =>
      getClient()
        .platformBuilderBinding.update({
          where: {
            id,
          },
          data: platformBuilderBindingPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformBuilderBindingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformBuilderBindingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
