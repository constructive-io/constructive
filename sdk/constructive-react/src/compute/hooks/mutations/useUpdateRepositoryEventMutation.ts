/**
 * Normalized repository events from local hooks and external providers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryEventKeys } from '../query-keys';
import { repositoryEventMutationKeys } from '../mutation-keys';
import type {
  RepositoryEventSelect,
  RepositoryEventWithRelations,
  RepositoryEventPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryEventSelect,
  RepositoryEventWithRelations,
  RepositoryEventPatch,
} from '../../orm/input-types';
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRepositoryEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', repositoryEventPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRepositoryEventMutation<S extends RepositoryEventSelect>(
  params: {
    selection: {
      fields: S & RepositoryEventSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRepositoryEvent: {
          repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        repositoryEventPatch: RepositoryEventPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRepositoryEvent: {
      repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    repositoryEventPatch: RepositoryEventPatch;
  }
>;
export function useUpdateRepositoryEventMutation(
  params: {
    selection: SelectionConfig<RepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        repositoryEventPatch: RepositoryEventPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryEventMutationKeys.all,
    mutationFn: ({
      id,
      repositoryEventPatch,
    }: {
      id: string;
      repositoryEventPatch: RepositoryEventPatch;
    }) =>
      getClient()
        .repositoryEvent.update({
          where: {
            id,
          },
          data: repositoryEventPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: repositoryEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
