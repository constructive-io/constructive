/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { proposalKeys } from '../query-keys';
import { proposalMutationKeys } from '../mutation-keys';
import type { ProposalSelect, ProposalWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ProposalSelect, ProposalWithRelations } from '../../orm/input-types';
/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteProposalMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteProposalMutation<S extends ProposalSelect>(
  params: {
    selection: {
      fields: S & ProposalSelect;
    } & HookStrictSelect<NoInfer<S>, ProposalSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteProposal: {
          proposal: InferSelectResult<ProposalWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteProposal: {
      proposal: InferSelectResult<ProposalWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteProposalMutation(
  params: {
    selection: SelectionConfig<ProposalSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ProposalSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: proposalMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .proposal.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: proposalKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: proposalKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
