/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildStepKeys } from '../query-keys';
import { buildStepMutationKeys } from '../mutation-keys';
import type {
  BuildStepSelect,
  BuildStepWithRelations,
  CreateBuildStepInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  BuildStepSelect,
  BuildStepWithRelations,
  CreateBuildStepInput,
} from '../../orm/input-types';
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateBuildStepMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateBuildStepMutation<S extends BuildStepSelect>(
  params: {
    selection: {
      fields: S & BuildStepSelect;
    } & HookStrictSelect<NoInfer<S>, BuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createBuildStep: {
          buildStep: InferSelectResult<BuildStepWithRelations, S>;
        };
      },
      Error,
      CreateBuildStepInput['buildStep']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createBuildStep: {
      buildStep: InferSelectResult<BuildStepWithRelations, S>;
    };
  },
  Error,
  CreateBuildStepInput['buildStep']
>;
export function useCreateBuildStepMutation(
  params: {
    selection: SelectionConfig<BuildStepSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateBuildStepInput['buildStep']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<BuildStepSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: buildStepMutationKeys.create(),
    mutationFn: (data: CreateBuildStepInput['buildStep']) =>
      getClient()
        .buildStep.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: buildStepKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
