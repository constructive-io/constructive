/**
 * Bindings from a repository event to the flow graph that should run
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryWorkflowKeys } from '../query-keys';
import { repositoryWorkflowMutationKeys } from '../mutation-keys';
import type {
  RepositoryWorkflowSelect,
  RepositoryWorkflowWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryWorkflowSelect,
  RepositoryWorkflowWithRelations,
} from '../../orm/input-types';
/**
 * Bindings from a repository event to the flow graph that should run
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRepositoryWorkflowMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRepositoryWorkflowMutation<S extends RepositoryWorkflowSelect>(
  params: {
    selection: {
      fields: S & RepositoryWorkflowSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryWorkflowSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRepositoryWorkflow: {
          repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRepositoryWorkflow: {
      repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRepositoryWorkflowMutation(
  params: {
    selection: SelectionConfig<RepositoryWorkflowSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryWorkflowSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryWorkflowMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .repositoryWorkflow.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: repositoryWorkflowKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryWorkflowKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
