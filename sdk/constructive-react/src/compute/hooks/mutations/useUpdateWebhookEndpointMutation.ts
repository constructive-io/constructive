/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webhookEndpointKeys } from '../query-keys';
import { webhookEndpointMutationKeys } from '../mutation-keys';
import type {
  WebhookEndpointSelect,
  WebhookEndpointWithRelations,
  WebhookEndpointPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  WebhookEndpointSelect,
  WebhookEndpointWithRelations,
  WebhookEndpointPatch,
} from '../../orm/input-types';
/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateWebhookEndpointMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', webhookEndpointPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateWebhookEndpointMutation<S extends WebhookEndpointSelect>(
  params: {
    selection: {
      fields: S & WebhookEndpointSelect;
    } & HookStrictSelect<NoInfer<S>, WebhookEndpointSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateWebhookEndpoint: {
          webhookEndpoint: InferSelectResult<WebhookEndpointWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        webhookEndpointPatch: WebhookEndpointPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateWebhookEndpoint: {
      webhookEndpoint: InferSelectResult<WebhookEndpointWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    webhookEndpointPatch: WebhookEndpointPatch;
  }
>;
export function useUpdateWebhookEndpointMutation(
  params: {
    selection: SelectionConfig<WebhookEndpointSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        webhookEndpointPatch: WebhookEndpointPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<WebhookEndpointSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: webhookEndpointMutationKeys.all,
    mutationFn: ({
      id,
      webhookEndpointPatch,
    }: {
      id: string;
      webhookEndpointPatch: WebhookEndpointPatch;
    }) =>
      getClient()
        .webhookEndpoint.update({
          where: {
            id,
          },
          data: webhookEndpointPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: webhookEndpointKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: webhookEndpointKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
