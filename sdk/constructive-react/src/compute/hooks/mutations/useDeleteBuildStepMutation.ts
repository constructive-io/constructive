/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildStepKeys } from '../query-keys';
import { buildStepMutationKeys } from '../mutation-keys';
import type { BuildStepSelect, BuildStepWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuildStepSelect, BuildStepWithRelations } from '../../orm/input-types';
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteBuildStepMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteBuildStepMutation<S extends BuildStepSelect>(
  params: {
    selection: {
      fields: S & BuildStepSelect;
    } & HookStrictSelect<NoInfer<S>, BuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteBuildStep: {
          buildStep: InferSelectResult<BuildStepWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        recordedAt: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteBuildStep: {
      buildStep: InferSelectResult<BuildStepWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    recordedAt: string;
  }
>;
export function useDeleteBuildStepMutation(
  params: {
    selection: SelectionConfig<BuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        recordedAt: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BuildStepSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: buildStepMutationKeys.all,
    mutationFn: ({ id, recordedAt }: { id: string; recordedAt: string }) =>
      getClient()
        .buildStep.delete({
          where: {
            id,
            recordedAt,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: buildStepKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: buildStepKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
