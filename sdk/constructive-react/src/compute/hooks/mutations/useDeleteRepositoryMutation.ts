/**
 * Source repositories, hosted locally or on an external provider
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryKeys } from '../query-keys';
import { repositoryMutationKeys } from '../mutation-keys';
import type { RepositorySelect, RepositoryWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RepositorySelect, RepositoryWithRelations } from '../../orm/input-types';
/**
 * Source repositories, hosted locally or on an external provider
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRepositoryMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRepositoryMutation<S extends RepositorySelect>(
  params: {
    selection: {
      fields: S & RepositorySelect;
    } & HookStrictSelect<NoInfer<S>, RepositorySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRepository: {
          repository: InferSelectResult<RepositoryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRepository: {
      repository: InferSelectResult<RepositoryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRepositoryMutation(
  params: {
    selection: SelectionConfig<RepositorySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositorySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .repository.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: repositoryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
