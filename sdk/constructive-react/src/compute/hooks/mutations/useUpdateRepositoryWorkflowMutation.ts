/**
 * Bindings from a repository event to the flow graph that should run
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryWorkflowKeys } from '../query-keys';
import { repositoryWorkflowMutationKeys } from '../mutation-keys';
import type {
  RepositoryWorkflowSelect,
  RepositoryWorkflowWithRelations,
  RepositoryWorkflowPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryWorkflowSelect,
  RepositoryWorkflowWithRelations,
  RepositoryWorkflowPatch,
} from '../../orm/input-types';
/**
 * Bindings from a repository event to the flow graph that should run
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRepositoryWorkflowMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', repositoryWorkflowPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRepositoryWorkflowMutation<S extends RepositoryWorkflowSelect>(
  params: {
    selection: {
      fields: S & RepositoryWorkflowSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryWorkflowSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRepositoryWorkflow: {
          repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        repositoryWorkflowPatch: RepositoryWorkflowPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRepositoryWorkflow: {
      repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    repositoryWorkflowPatch: RepositoryWorkflowPatch;
  }
>;
export function useUpdateRepositoryWorkflowMutation(
  params: {
    selection: SelectionConfig<RepositoryWorkflowSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        repositoryWorkflowPatch: RepositoryWorkflowPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryWorkflowSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryWorkflowMutationKeys.all,
    mutationFn: ({
      id,
      repositoryWorkflowPatch,
    }: {
      id: string;
      repositoryWorkflowPatch: RepositoryWorkflowPatch;
    }) =>
      getClient()
        .repositoryWorkflow.update({
          where: {
            id,
          },
          data: repositoryWorkflowPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: repositoryWorkflowKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryWorkflowKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
