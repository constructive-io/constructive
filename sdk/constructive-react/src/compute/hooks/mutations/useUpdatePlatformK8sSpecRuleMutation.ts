/**
 * Spec rulebook for DB-driven resources — merkle-versioned head over the infra store; enforced by the generated admission gate via infra_utils.check_resource_admission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformK8sSpecRuleKeys } from '../query-keys';
import { platformK8sSpecRuleMutationKeys } from '../mutation-keys';
import type {
  PlatformK8sSpecRuleSelect,
  PlatformK8sSpecRuleWithRelations,
  PlatformK8sSpecRulePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformK8sSpecRuleSelect,
  PlatformK8sSpecRuleWithRelations,
  PlatformK8sSpecRulePatch,
} from '../../orm/input-types';
/**
 * Spec rulebook for DB-driven resources — merkle-versioned head over the infra store; enforced by the generated admission gate via infra_utils.check_resource_admission
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformK8sSpecRuleMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformK8sSpecRulePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformK8sSpecRuleMutation<S extends PlatformK8sSpecRuleSelect>(
  params: {
    selection: {
      fields: S & PlatformK8sSpecRuleSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformK8sSpecRuleSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformK8sSpecRule: {
          platformK8sSpecRule: InferSelectResult<PlatformK8sSpecRuleWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformK8sSpecRulePatch: PlatformK8sSpecRulePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformK8sSpecRule: {
      platformK8sSpecRule: InferSelectResult<PlatformK8sSpecRuleWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformK8sSpecRulePatch: PlatformK8sSpecRulePatch;
  }
>;
export function useUpdatePlatformK8sSpecRuleMutation(
  params: {
    selection: SelectionConfig<PlatformK8sSpecRuleSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformK8sSpecRulePatch: PlatformK8sSpecRulePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformK8sSpecRuleSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformK8sSpecRuleMutationKeys.all,
    mutationFn: ({
      id,
      platformK8sSpecRulePatch,
    }: {
      id: string;
      platformK8sSpecRulePatch: PlatformK8sSpecRulePatch;
    }) =>
      getClient()
        .platformK8sSpecRule.update({
          where: {
            id,
          },
          data: platformK8sSpecRulePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformK8sSpecRuleKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformK8sSpecRuleKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
