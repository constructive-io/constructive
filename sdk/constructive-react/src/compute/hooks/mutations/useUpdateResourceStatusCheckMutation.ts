/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceStatusCheckKeys } from '../query-keys';
import { resourceStatusCheckMutationKeys } from '../mutation-keys';
import type {
  ResourceStatusCheckSelect,
  ResourceStatusCheckWithRelations,
  ResourceStatusCheckPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceStatusCheckSelect,
  ResourceStatusCheckWithRelations,
  ResourceStatusCheckPatch,
} from '../../orm/input-types';
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateResourceStatusCheckMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', resourceStatusCheckPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateResourceStatusCheckMutation<S extends ResourceStatusCheckSelect>(
  params: {
    selection: {
      fields: S & ResourceStatusCheckSelect;
    } & HookStrictSelect<NoInfer<S>, ResourceStatusCheckSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateResourceStatusCheck: {
          resourceStatusCheck: InferSelectResult<ResourceStatusCheckWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        resourceStatusCheckPatch: ResourceStatusCheckPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateResourceStatusCheck: {
      resourceStatusCheck: InferSelectResult<ResourceStatusCheckWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    resourceStatusCheckPatch: ResourceStatusCheckPatch;
  }
>;
export function useUpdateResourceStatusCheckMutation(
  params: {
    selection: SelectionConfig<ResourceStatusCheckSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        resourceStatusCheckPatch: ResourceStatusCheckPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceStatusCheckSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceStatusCheckMutationKeys.all,
    mutationFn: ({
      id,
      resourceStatusCheckPatch,
    }: {
      id: string;
      resourceStatusCheckPatch: ResourceStatusCheckPatch;
    }) =>
      getClient()
        .resourceStatusCheck.update({
          where: {
            id,
          },
          data: resourceStatusCheckPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: resourceStatusCheckKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: resourceStatusCheckKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
