/**
 * Flow graph definitions — FBP graphs stored in the dedicated graph Merkle store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionGraphKeys } from '../query-keys';
import { functionGraphMutationKeys } from '../mutation-keys';
import type { FunctionGraphSelect, FunctionGraphWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { FunctionGraphSelect, FunctionGraphWithRelations } from '../../orm/input-types';
/**
 * Flow graph definitions — FBP graphs stored in the dedicated graph Merkle store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFunctionGraphMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteFunctionGraphMutation<S extends FunctionGraphSelect>(
  params: {
    selection: {
      fields: S & FunctionGraphSelect;
    } & HookStrictSelect<NoInfer<S>, FunctionGraphSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteFunctionGraph: {
          functionGraph: InferSelectResult<FunctionGraphWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteFunctionGraph: {
      functionGraph: InferSelectResult<FunctionGraphWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteFunctionGraphMutation(
  params: {
    selection: SelectionConfig<FunctionGraphSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<FunctionGraphSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: functionGraphMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .functionGraph.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: functionGraphKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: functionGraphKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
