/**
 * Function definitions — registered cloud functions with routing, queue, and retry configuration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionDefinitionKeys } from '../query-keys';
import { functionDefinitionMutationKeys } from '../mutation-keys';
import type {
  FunctionDefinitionSelect,
  FunctionDefinitionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  FunctionDefinitionSelect,
  FunctionDefinitionWithRelations,
} from '../../orm/input-types';
/**
 * Function definitions — registered cloud functions with routing, queue, and retry configuration
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteFunctionDefinitionMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteFunctionDefinitionMutation<S extends FunctionDefinitionSelect>(
  params: {
    selection: {
      fields: S & FunctionDefinitionSelect;
    } & HookStrictSelect<NoInfer<S>, FunctionDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteFunctionDefinition: {
          functionDefinition: InferSelectResult<FunctionDefinitionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteFunctionDefinition: {
      functionDefinition: InferSelectResult<FunctionDefinitionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteFunctionDefinitionMutation(
  params: {
    selection: SelectionConfig<FunctionDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<FunctionDefinitionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: functionDefinitionMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .functionDefinition.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: functionDefinitionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: functionDefinitionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
