/**
 * Container image catalog: images available to run as functions, resources, and builds
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { imageKeys } from '../query-keys';
import { imageMutationKeys } from '../mutation-keys';
import type { ImageSelect, ImageWithRelations, ImagePatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ImageSelect, ImageWithRelations, ImagePatch } from '../../orm/input-types';
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateImageMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', imagePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateImageMutation<S extends ImageSelect>(
  params: {
    selection: {
      fields: S & ImageSelect;
    } & HookStrictSelect<NoInfer<S>, ImageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateImage: {
          image: InferSelectResult<ImageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        imagePatch: ImagePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateImage: {
      image: InferSelectResult<ImageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    imagePatch: ImagePatch;
  }
>;
export function useUpdateImageMutation(
  params: {
    selection: SelectionConfig<ImageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        imagePatch: ImagePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ImageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: imageMutationKeys.all,
    mutationFn: ({ id, imagePatch }: { id: string; imagePatch: ImagePatch }) =>
      getClient()
        .image.update({
          where: {
            id,
          },
          data: imagePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: imageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: imageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
