/**
 * Grants that make a catalog image usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { imageGrantKeys } from '../query-keys';
import { imageGrantMutationKeys } from '../mutation-keys';
import type { ImageGrantSelect, ImageGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ImageGrantSelect, ImageGrantWithRelations } from '../../orm/input-types';
/**
 * Grants that make a catalog image usable by one scope
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteImageGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteImageGrantMutation<S extends ImageGrantSelect>(
  params: {
    selection: {
      fields: S & ImageGrantSelect;
    } & HookStrictSelect<NoInfer<S>, ImageGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteImageGrant: {
          imageGrant: InferSelectResult<ImageGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteImageGrant: {
      imageGrant: InferSelectResult<ImageGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteImageGrantMutation(
  params: {
    selection: SelectionConfig<ImageGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ImageGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: imageGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .imageGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: imageGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: imageGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
