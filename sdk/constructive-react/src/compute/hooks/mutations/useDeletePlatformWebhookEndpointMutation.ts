/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformWebhookEndpointKeys } from '../query-keys';
import { platformWebhookEndpointMutationKeys } from '../mutation-keys';
import type {
  PlatformWebhookEndpointSelect,
  PlatformWebhookEndpointWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformWebhookEndpointSelect,
  PlatformWebhookEndpointWithRelations,
} from '../../orm/input-types';
/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformWebhookEndpointMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformWebhookEndpointMutation<S extends PlatformWebhookEndpointSelect>(
  params: {
    selection: {
      fields: S & PlatformWebhookEndpointSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformWebhookEndpointSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformWebhookEndpoint: {
          platformWebhookEndpoint: InferSelectResult<PlatformWebhookEndpointWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformWebhookEndpoint: {
      platformWebhookEndpoint: InferSelectResult<PlatformWebhookEndpointWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformWebhookEndpointMutation(
  params: {
    selection: SelectionConfig<PlatformWebhookEndpointSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformWebhookEndpointSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformWebhookEndpointMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformWebhookEndpoint.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformWebhookEndpointKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformWebhookEndpointKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
