/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceKeys } from '../query-keys';
import { resourceMutationKeys } from '../mutation-keys';
import type {
  ResourceSelect,
  ResourceWithRelations,
  CreateResourceInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceSelect,
  ResourceWithRelations,
  CreateResourceInput,
} from '../../orm/input-types';
/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateResourceMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateResourceMutation<S extends ResourceSelect>(
  params: {
    selection: {
      fields: S & ResourceSelect;
    } & HookStrictSelect<NoInfer<S>, ResourceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createResource: {
          resource: InferSelectResult<ResourceWithRelations, S>;
        };
      },
      Error,
      CreateResourceInput['resource']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createResource: {
      resource: InferSelectResult<ResourceWithRelations, S>;
    };
  },
  Error,
  CreateResourceInput['resource']
>;
export function useCreateResourceMutation(
  params: {
    selection: SelectionConfig<ResourceSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateResourceInput['resource']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<ResourceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceMutationKeys.create(),
    mutationFn: (data: CreateResourceInput['resource']) =>
      getClient()
        .resource.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: resourceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
