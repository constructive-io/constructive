/**
 * Normalized repository events from local hooks and external providers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryEventKeys } from '../query-keys';
import { repositoryEventMutationKeys } from '../mutation-keys';
import type { RepositoryEventSelect, RepositoryEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RepositoryEventSelect, RepositoryEventWithRelations } from '../../orm/input-types';
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRepositoryEventMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRepositoryEventMutation<S extends RepositoryEventSelect>(
  params: {
    selection: {
      fields: S & RepositoryEventSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRepositoryEvent: {
          repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRepositoryEvent: {
      repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRepositoryEventMutation(
  params: {
    selection: SelectionConfig<RepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryEventMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .repositoryEvent.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: repositoryEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
