/**
 * Normalized repository events from local hooks and external providers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryEventKeys } from '../query-keys';
import { repositoryEventMutationKeys } from '../mutation-keys';
import type {
  RepositoryEventSelect,
  RepositoryEventWithRelations,
  CreateRepositoryEventInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryEventSelect,
  RepositoryEventWithRelations,
  CreateRepositoryEventInput,
} from '../../orm/input-types';
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateRepositoryEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateRepositoryEventMutation<S extends RepositoryEventSelect>(
  params: {
    selection: {
      fields: S & RepositoryEventSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createRepositoryEvent: {
          repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S>;
        };
      },
      Error,
      CreateRepositoryEventInput['repositoryEvent']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createRepositoryEvent: {
      repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S>;
    };
  },
  Error,
  CreateRepositoryEventInput['repositoryEvent']
>;
export function useCreateRepositoryEventMutation(
  params: {
    selection: SelectionConfig<RepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateRepositoryEventInput['repositoryEvent']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryEventMutationKeys.create(),
    mutationFn: (data: CreateRepositoryEventInput['repositoryEvent']) =>
      getClient()
        .repositoryEvent.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: repositoryEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
