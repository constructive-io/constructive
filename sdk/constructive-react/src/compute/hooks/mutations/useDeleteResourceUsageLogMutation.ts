/**
 * Raw resource usage log — interval-accounting measurements from heartbeats (self), the reconciler (observer), and the namespace-grain collector (prometheus)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceUsageLogKeys } from '../query-keys';
import { resourceUsageLogMutationKeys } from '../mutation-keys';
import type { ResourceUsageLogSelect, ResourceUsageLogWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ResourceUsageLogSelect, ResourceUsageLogWithRelations } from '../../orm/input-types';
/**
 * Raw resource usage log — interval-accounting measurements from heartbeats (self), the reconciler (observer), and the namespace-grain collector (prometheus)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteResourceUsageLogMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteResourceUsageLogMutation<S extends ResourceUsageLogSelect>(
  params: {
    selection: {
      fields: S & ResourceUsageLogSelect;
    } & HookStrictSelect<NoInfer<S>, ResourceUsageLogSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteResourceUsageLog: {
          resourceUsageLog: InferSelectResult<ResourceUsageLogWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        sampledAt: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteResourceUsageLog: {
      resourceUsageLog: InferSelectResult<ResourceUsageLogWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    sampledAt: string;
  }
>;
export function useDeleteResourceUsageLogMutation(
  params: {
    selection: SelectionConfig<ResourceUsageLogSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        sampledAt: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceUsageLogSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceUsageLogMutationKeys.all,
    mutationFn: ({ id, sampledAt }: { id: string; sampledAt: string }) =>
      getClient()
        .resourceUsageLog.delete({
          where: {
            id,
            sampledAt,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: resourceUsageLogKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: resourceUsageLogKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
