/**
 * Branch heads — mutable pointers into the commit chain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraRefKeys } from '../query-keys';
import { infraRefMutationKeys } from '../mutation-keys';
import type { InfraRefSelect, InfraRefWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InfraRefSelect, InfraRefWithRelations } from '../../orm/input-types';
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteInfraRefMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteInfraRefMutation<S extends InfraRefSelect>(
  params: {
    selection: {
      fields: S & InfraRefSelect;
    } & HookStrictSelect<NoInfer<S>, InfraRefSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteInfraRef: {
          infraRef: InferSelectResult<InfraRefWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        databaseId: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteInfraRef: {
      infraRef: InferSelectResult<InfraRefWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    databaseId: string;
  }
>;
export function useDeleteInfraRefMutation(
  params: {
    selection: SelectionConfig<InfraRefSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        databaseId: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<InfraRefSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: infraRefMutationKeys.all,
    mutationFn: ({ id, databaseId }: { id: string; databaseId: string }) =>
      getClient()
        .infraRef.delete({
          where: {
            id,
            databaseId,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: infraRefKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: infraRefKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
