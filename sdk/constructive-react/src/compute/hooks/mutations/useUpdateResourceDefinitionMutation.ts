/**
 * Resource definitions — templates for resource kinds declaring default spec and secret/config requirements
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceDefinitionKeys } from '../query-keys';
import { resourceDefinitionMutationKeys } from '../mutation-keys';
import type {
  ResourceDefinitionSelect,
  ResourceDefinitionWithRelations,
  ResourceDefinitionPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceDefinitionSelect,
  ResourceDefinitionWithRelations,
  ResourceDefinitionPatch,
} from '../../orm/input-types';
/**
 * Resource definitions — templates for resource kinds declaring default spec and secret/config requirements
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateResourceDefinitionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', resourceDefinitionPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateResourceDefinitionMutation<S extends ResourceDefinitionSelect>(
  params: {
    selection: {
      fields: S & ResourceDefinitionSelect;
    } & HookStrictSelect<NoInfer<S>, ResourceDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateResourceDefinition: {
          resourceDefinition: InferSelectResult<ResourceDefinitionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        resourceDefinitionPatch: ResourceDefinitionPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateResourceDefinition: {
      resourceDefinition: InferSelectResult<ResourceDefinitionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    resourceDefinitionPatch: ResourceDefinitionPatch;
  }
>;
export function useUpdateResourceDefinitionMutation(
  params: {
    selection: SelectionConfig<ResourceDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        resourceDefinitionPatch: ResourceDefinitionPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceDefinitionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceDefinitionMutationKeys.all,
    mutationFn: ({
      id,
      resourceDefinitionPatch,
    }: {
      id: string;
      resourceDefinitionPatch: ResourceDefinitionPatch;
    }) =>
      getClient()
        .resourceDefinition.update({
          where: {
            id,
          },
          data: resourceDefinitionPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: resourceDefinitionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: resourceDefinitionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
