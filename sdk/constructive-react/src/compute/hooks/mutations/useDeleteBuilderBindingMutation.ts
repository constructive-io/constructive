/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { builderBindingKeys } from '../query-keys';
import { builderBindingMutationKeys } from '../mutation-keys';
import type { BuilderBindingSelect, BuilderBindingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuilderBindingSelect, BuilderBindingWithRelations } from '../../orm/input-types';
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteBuilderBindingMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteBuilderBindingMutation<S extends BuilderBindingSelect>(
  params: {
    selection: {
      fields: S & BuilderBindingSelect;
    } & HookStrictSelect<NoInfer<S>, BuilderBindingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteBuilderBinding: {
          builderBinding: InferSelectResult<BuilderBindingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteBuilderBinding: {
      builderBinding: InferSelectResult<BuilderBindingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteBuilderBindingMutation(
  params: {
    selection: SelectionConfig<BuilderBindingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BuilderBindingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: builderBindingMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .builderBinding.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: builderBindingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: builderBindingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
