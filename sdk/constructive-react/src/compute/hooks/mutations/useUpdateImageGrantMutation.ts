/**
 * Grants that make a catalog image usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { imageGrantKeys } from '../query-keys';
import { imageGrantMutationKeys } from '../mutation-keys';
import type {
  ImageGrantSelect,
  ImageGrantWithRelations,
  ImageGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ImageGrantSelect,
  ImageGrantWithRelations,
  ImageGrantPatch,
} from '../../orm/input-types';
/**
 * Grants that make a catalog image usable by one scope
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateImageGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', imageGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateImageGrantMutation<S extends ImageGrantSelect>(
  params: {
    selection: {
      fields: S & ImageGrantSelect;
    } & HookStrictSelect<NoInfer<S>, ImageGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateImageGrant: {
          imageGrant: InferSelectResult<ImageGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        imageGrantPatch: ImageGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateImageGrant: {
      imageGrant: InferSelectResult<ImageGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    imageGrantPatch: ImageGrantPatch;
  }
>;
export function useUpdateImageGrantMutation(
  params: {
    selection: SelectionConfig<ImageGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        imageGrantPatch: ImageGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ImageGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: imageGrantMutationKeys.all,
    mutationFn: ({ id, imageGrantPatch }: { id: string; imageGrantPatch: ImageGrantPatch }) =>
      getClient()
        .imageGrant.update({
          where: {
            id,
          },
          data: imageGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: imageGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: imageGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
