/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseGraphCommitKeys } from '../query-keys';
import { databaseGraphCommitMutationKeys } from '../mutation-keys';
import type {
  DatabaseGraphCommitSelect,
  DatabaseGraphCommitWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DatabaseGraphCommitSelect,
  DatabaseGraphCommitWithRelations,
} from '../../orm/input-types';
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDatabaseGraphCommitMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteDatabaseGraphCommitMutation<S extends DatabaseGraphCommitSelect>(
  params: {
    selection: {
      fields: S & DatabaseGraphCommitSelect;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphCommitSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteDatabaseGraphCommit: {
          databaseGraphCommit: InferSelectResult<DatabaseGraphCommitWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        databaseId: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteDatabaseGraphCommit: {
      databaseGraphCommit: InferSelectResult<DatabaseGraphCommitWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    databaseId: string;
  }
>;
export function useDeleteDatabaseGraphCommitMutation(
  params: {
    selection: SelectionConfig<DatabaseGraphCommitSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        databaseId: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DatabaseGraphCommitSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: databaseGraphCommitMutationKeys.all,
    mutationFn: ({ id, databaseId }: { id: string; databaseId: string }) =>
      getClient()
        .databaseGraphCommit.delete({
          where: {
            id,
            databaseId,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: databaseGraphCommitKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: databaseGraphCommitKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
