/**
 * Raw resource usage log — interval-accounting measurements from heartbeats (self), the reconciler (observer), and the namespace-grain collector (prometheus)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformResourceUsageLogKeys } from '../query-keys';
import { platformResourceUsageLogMutationKeys } from '../mutation-keys';
import type {
  PlatformResourceUsageLogSelect,
  PlatformResourceUsageLogWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformResourceUsageLogSelect,
  PlatformResourceUsageLogWithRelations,
} from '../../orm/input-types';
/**
 * Raw resource usage log — interval-accounting measurements from heartbeats (self), the reconciler (observer), and the namespace-grain collector (prometheus)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformResourceUsageLogMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformResourceUsageLogMutation<S extends PlatformResourceUsageLogSelect>(
  params: {
    selection: {
      fields: S & PlatformResourceUsageLogSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceUsageLogSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformResourceUsageLog: {
          platformResourceUsageLog: InferSelectResult<PlatformResourceUsageLogWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        sampledAt: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformResourceUsageLog: {
      platformResourceUsageLog: InferSelectResult<PlatformResourceUsageLogWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    sampledAt: string;
  }
>;
export function useDeletePlatformResourceUsageLogMutation(
  params: {
    selection: SelectionConfig<PlatformResourceUsageLogSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        sampledAt: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformResourceUsageLogSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformResourceUsageLogMutationKeys.all,
    mutationFn: ({ id, sampledAt }: { id: string; sampledAt: string }) =>
      getClient()
        .platformResourceUsageLog.delete({
          where: {
            id,
            sampledAt,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformResourceUsageLogKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformResourceUsageLogKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
