/**
 * Grants that make a registry usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryGrantKeys } from '../query-keys';
import { registryGrantMutationKeys } from '../mutation-keys';
import type { RegistryGrantSelect, RegistryGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistryGrantSelect, RegistryGrantWithRelations } from '../../orm/input-types';
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRegistryGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRegistryGrantMutation<S extends RegistryGrantSelect>(
  params: {
    selection: {
      fields: S & RegistryGrantSelect;
    } & HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRegistryGrant: {
          registryGrant: InferSelectResult<RegistryGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRegistryGrant: {
      registryGrant: InferSelectResult<RegistryGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRegistryGrantMutation(
  params: {
    selection: SelectionConfig<RegistryGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RegistryGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: registryGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .registryGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: registryGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: registryGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
