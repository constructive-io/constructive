/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webhookEventKeys } from '../query-keys';
import { webhookEventMutationKeys } from '../mutation-keys';
import type {
  WebhookEventSelect,
  WebhookEventWithRelations,
  CreateWebhookEventInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  WebhookEventSelect,
  WebhookEventWithRelations,
  CreateWebhookEventInput,
} from '../../orm/input-types';
/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateWebhookEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateWebhookEventMutation<S extends WebhookEventSelect>(
  params: {
    selection: {
      fields: S & WebhookEventSelect;
    } & HookStrictSelect<NoInfer<S>, WebhookEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createWebhookEvent: {
          webhookEvent: InferSelectResult<WebhookEventWithRelations, S>;
        };
      },
      Error,
      CreateWebhookEventInput['webhookEvent']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createWebhookEvent: {
      webhookEvent: InferSelectResult<WebhookEventWithRelations, S>;
    };
  },
  Error,
  CreateWebhookEventInput['webhookEvent']
>;
export function useCreateWebhookEventMutation(
  params: {
    selection: SelectionConfig<WebhookEventSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateWebhookEventInput['webhookEvent']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<WebhookEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: webhookEventMutationKeys.create(),
    mutationFn: (data: CreateWebhookEventInput['webhookEvent']) =>
      getClient()
        .webhookEvent.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: webhookEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
