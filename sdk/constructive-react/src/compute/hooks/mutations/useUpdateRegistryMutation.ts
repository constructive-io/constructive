/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryKeys } from '../query-keys';
import { registryMutationKeys } from '../mutation-keys';
import type { RegistrySelect, RegistryWithRelations, RegistryPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistrySelect, RegistryWithRelations, RegistryPatch } from '../../orm/input-types';
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateRegistryMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', registryPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateRegistryMutation<S extends RegistrySelect>(
  params: {
    selection: {
      fields: S & RegistrySelect;
    } & HookStrictSelect<NoInfer<S>, RegistrySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateRegistry: {
          registry: InferSelectResult<RegistryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        registryPatch: RegistryPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateRegistry: {
      registry: InferSelectResult<RegistryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    registryPatch: RegistryPatch;
  }
>;
export function useUpdateRegistryMutation(
  params: {
    selection: SelectionConfig<RegistrySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        registryPatch: RegistryPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RegistrySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: registryMutationKeys.all,
    mutationFn: ({ id, registryPatch }: { id: string; registryPatch: RegistryPatch }) =>
      getClient()
        .registry.update({
          where: {
            id,
          },
          data: registryPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: registryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: registryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
