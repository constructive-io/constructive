/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { proposalKeys } from '../query-keys';
import { proposalMutationKeys } from '../mutation-keys';
import type { ProposalSelect, ProposalWithRelations, ProposalPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ProposalSelect, ProposalWithRelations, ProposalPatch } from '../../orm/input-types';
/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateProposalMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', proposalPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateProposalMutation<S extends ProposalSelect>(
  params: {
    selection: {
      fields: S & ProposalSelect;
    } & HookStrictSelect<NoInfer<S>, ProposalSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateProposal: {
          proposal: InferSelectResult<ProposalWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        proposalPatch: ProposalPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateProposal: {
      proposal: InferSelectResult<ProposalWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    proposalPatch: ProposalPatch;
  }
>;
export function useUpdateProposalMutation(
  params: {
    selection: SelectionConfig<ProposalSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        proposalPatch: ProposalPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ProposalSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: proposalMutationKeys.all,
    mutationFn: ({ id, proposalPatch }: { id: string; proposalPatch: ProposalPatch }) =>
      getClient()
        .proposal.update({
          where: {
            id,
          },
          data: proposalPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: proposalKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: proposalKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
