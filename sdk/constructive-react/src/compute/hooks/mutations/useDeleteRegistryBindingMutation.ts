/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryBindingKeys } from '../query-keys';
import { registryBindingMutationKeys } from '../mutation-keys';
import type { RegistryBindingSelect, RegistryBindingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistryBindingSelect, RegistryBindingWithRelations } from '../../orm/input-types';
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRegistryBindingMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRegistryBindingMutation<S extends RegistryBindingSelect>(
  params: {
    selection: {
      fields: S & RegistryBindingSelect;
    } & HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRegistryBinding: {
          registryBinding: InferSelectResult<RegistryBindingWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRegistryBinding: {
      registryBinding: InferSelectResult<RegistryBindingWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRegistryBindingMutation(
  params: {
    selection: SelectionConfig<RegistryBindingSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RegistryBindingSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: registryBindingMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .registryBinding.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: registryBindingKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: registryBindingKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
