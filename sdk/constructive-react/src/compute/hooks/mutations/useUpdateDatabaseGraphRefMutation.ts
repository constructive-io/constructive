/**
 * Branch heads — mutable pointers into the commit chain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseGraphRefKeys } from '../query-keys';
import { databaseGraphRefMutationKeys } from '../mutation-keys';
import type {
  DatabaseGraphRefSelect,
  DatabaseGraphRefWithRelations,
  DatabaseGraphRefPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DatabaseGraphRefSelect,
  DatabaseGraphRefWithRelations,
  DatabaseGraphRefPatch,
} from '../../orm/input-types';
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateDatabaseGraphRefMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', databaseGraphRefPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateDatabaseGraphRefMutation<S extends DatabaseGraphRefSelect>(
  params: {
    selection: {
      fields: S & DatabaseGraphRefSelect;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateDatabaseGraphRef: {
          databaseGraphRef: InferSelectResult<DatabaseGraphRefWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        databaseId: string;
        databaseGraphRefPatch: DatabaseGraphRefPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateDatabaseGraphRef: {
      databaseGraphRef: InferSelectResult<DatabaseGraphRefWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    databaseId: string;
    databaseGraphRefPatch: DatabaseGraphRefPatch;
  }
>;
export function useUpdateDatabaseGraphRefMutation(
  params: {
    selection: SelectionConfig<DatabaseGraphRefSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        databaseId: string;
        databaseGraphRefPatch: DatabaseGraphRefPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DatabaseGraphRefSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: databaseGraphRefMutationKeys.all,
    mutationFn: ({
      id,
      databaseId,
      databaseGraphRefPatch,
    }: {
      id: string;
      databaseId: string;
      databaseGraphRefPatch: DatabaseGraphRefPatch;
    }) =>
      getClient()
        .databaseGraphRef.update({
          where: {
            id,
            databaseId,
          },
          data: databaseGraphRefPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: databaseGraphRefKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: databaseGraphRefKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
