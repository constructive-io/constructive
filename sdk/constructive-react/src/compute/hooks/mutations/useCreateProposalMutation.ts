/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { proposalKeys } from '../query-keys';
import { proposalMutationKeys } from '../mutation-keys';
import type {
  ProposalSelect,
  ProposalWithRelations,
  CreateProposalInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ProposalSelect,
  ProposalWithRelations,
  CreateProposalInput,
} from '../../orm/input-types';
/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateProposalMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateProposalMutation<S extends ProposalSelect>(
  params: {
    selection: {
      fields: S & ProposalSelect;
    } & HookStrictSelect<NoInfer<S>, ProposalSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createProposal: {
          proposal: InferSelectResult<ProposalWithRelations, S>;
        };
      },
      Error,
      CreateProposalInput['proposal']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createProposal: {
      proposal: InferSelectResult<ProposalWithRelations, S>;
    };
  },
  Error,
  CreateProposalInput['proposal']
>;
export function useCreateProposalMutation(
  params: {
    selection: SelectionConfig<ProposalSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateProposalInput['proposal']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<ProposalSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: proposalMutationKeys.create(),
    mutationFn: (data: CreateProposalInput['proposal']) =>
      getClient()
        .proposal.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: proposalKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
