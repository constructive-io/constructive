/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformBuildStepKeys } from '../query-keys';
import { platformBuildStepMutationKeys } from '../mutation-keys';
import type {
  PlatformBuildStepSelect,
  PlatformBuildStepWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformBuildStepSelect,
  PlatformBuildStepWithRelations,
} from '../../orm/input-types';
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformBuildStepMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformBuildStepMutation<S extends PlatformBuildStepSelect>(
  params: {
    selection: {
      fields: S & PlatformBuildStepSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformBuildStep: {
          platformBuildStep: InferSelectResult<PlatformBuildStepWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        recordedAt: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformBuildStep: {
      platformBuildStep: InferSelectResult<PlatformBuildStepWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    recordedAt: string;
  }
>;
export function useDeletePlatformBuildStepMutation(
  params: {
    selection: SelectionConfig<PlatformBuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        recordedAt: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformBuildStepSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformBuildStepMutationKeys.all,
    mutationFn: ({ id, recordedAt }: { id: string; recordedAt: string }) =>
      getClient()
        .platformBuildStep.delete({
          where: {
            id,
            recordedAt,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformBuildStepKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformBuildStepKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
