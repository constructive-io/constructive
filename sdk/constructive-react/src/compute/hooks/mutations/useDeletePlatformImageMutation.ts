/**
 * Container image catalog: images available to run as functions, resources, and builds
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformImageKeys } from '../query-keys';
import { platformImageMutationKeys } from '../mutation-keys';
import type { PlatformImageSelect, PlatformImageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformImageSelect, PlatformImageWithRelations } from '../../orm/input-types';
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformImageMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformImageMutation<S extends PlatformImageSelect>(
  params: {
    selection: {
      fields: S & PlatformImageSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformImage: {
          platformImage: InferSelectResult<PlatformImageWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformImage: {
      platformImage: InferSelectResult<PlatformImageWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformImageMutation(
  params: {
    selection: SelectionConfig<PlatformImageSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformImageSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformImageMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformImage.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformImageKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformImageKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
