/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformResourceStatusCheckKeys } from '../query-keys';
import { platformResourceStatusCheckMutationKeys } from '../mutation-keys';
import type {
  PlatformResourceStatusCheckSelect,
  PlatformResourceStatusCheckWithRelations,
  PlatformResourceStatusCheckPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformResourceStatusCheckSelect,
  PlatformResourceStatusCheckWithRelations,
  PlatformResourceStatusCheckPatch,
} from '../../orm/input-types';
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformResourceStatusCheckMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformResourceStatusCheckPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformResourceStatusCheckMutation<
  S extends PlatformResourceStatusCheckSelect,
>(
  params: {
    selection: {
      fields: S & PlatformResourceStatusCheckSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceStatusCheckSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformResourceStatusCheck: {
          platformResourceStatusCheck: InferSelectResult<
            PlatformResourceStatusCheckWithRelations,
            S
          >;
        };
      },
      Error,
      {
        id: string;
        platformResourceStatusCheckPatch: PlatformResourceStatusCheckPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformResourceStatusCheck: {
      platformResourceStatusCheck: InferSelectResult<PlatformResourceStatusCheckWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformResourceStatusCheckPatch: PlatformResourceStatusCheckPatch;
  }
>;
export function useUpdatePlatformResourceStatusCheckMutation(
  params: {
    selection: SelectionConfig<PlatformResourceStatusCheckSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformResourceStatusCheckPatch: PlatformResourceStatusCheckPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformResourceStatusCheckSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformResourceStatusCheckMutationKeys.all,
    mutationFn: ({
      id,
      platformResourceStatusCheckPatch,
    }: {
      id: string;
      platformResourceStatusCheckPatch: PlatformResourceStatusCheckPatch;
    }) =>
      getClient()
        .platformResourceStatusCheck.update({
          where: {
            id,
          },
          data: platformResourceStatusCheckPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformResourceStatusCheckKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformResourceStatusCheckKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
