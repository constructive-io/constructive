/**
 * Grants that make a registry usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRegistryGrantKeys } from '../query-keys';
import { platformRegistryGrantMutationKeys } from '../mutation-keys';
import type {
  PlatformRegistryGrantSelect,
  PlatformRegistryGrantWithRelations,
  PlatformRegistryGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformRegistryGrantSelect,
  PlatformRegistryGrantWithRelations,
  PlatformRegistryGrantPatch,
} from '../../orm/input-types';
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformRegistryGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformRegistryGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformRegistryGrantMutation<S extends PlatformRegistryGrantSelect>(
  params: {
    selection: {
      fields: S & PlatformRegistryGrantSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistryGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformRegistryGrant: {
          platformRegistryGrant: InferSelectResult<PlatformRegistryGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformRegistryGrantPatch: PlatformRegistryGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformRegistryGrant: {
      platformRegistryGrant: InferSelectResult<PlatformRegistryGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformRegistryGrantPatch: PlatformRegistryGrantPatch;
  }
>;
export function useUpdatePlatformRegistryGrantMutation(
  params: {
    selection: SelectionConfig<PlatformRegistryGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformRegistryGrantPatch: PlatformRegistryGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformRegistryGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformRegistryGrantMutationKeys.all,
    mutationFn: ({
      id,
      platformRegistryGrantPatch,
    }: {
      id: string;
      platformRegistryGrantPatch: PlatformRegistryGrantPatch;
    }) =>
      getClient()
        .platformRegistryGrant.update({
          where: {
            id,
          },
          data: platformRegistryGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformRegistryGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformRegistryGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
