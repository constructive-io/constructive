/**
 * Resource usage summaries — runtime seconds, GB-seconds, and max gauges per (resource, namespace, day); resource_id-NULL rows are namespace-grain totals
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceUsageSummaryKeys } from '../query-keys';
import { resourceUsageSummaryMutationKeys } from '../mutation-keys';
import type {
  ResourceUsageSummarySelect,
  ResourceUsageSummaryWithRelations,
  CreateResourceUsageSummaryInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceUsageSummarySelect,
  ResourceUsageSummaryWithRelations,
  CreateResourceUsageSummaryInput,
} from '../../orm/input-types';
/**
 * Resource usage summaries — runtime seconds, GB-seconds, and max gauges per (resource, namespace, day); resource_id-NULL rows are namespace-grain totals
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateResourceUsageSummaryMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateResourceUsageSummaryMutation<S extends ResourceUsageSummarySelect>(
  params: {
    selection: {
      fields: S & ResourceUsageSummarySelect;
    } & HookStrictSelect<NoInfer<S>, ResourceUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createResourceUsageSummary: {
          resourceUsageSummary: InferSelectResult<ResourceUsageSummaryWithRelations, S>;
        };
      },
      Error,
      CreateResourceUsageSummaryInput['resourceUsageSummary']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createResourceUsageSummary: {
      resourceUsageSummary: InferSelectResult<ResourceUsageSummaryWithRelations, S>;
    };
  },
  Error,
  CreateResourceUsageSummaryInput['resourceUsageSummary']
>;
export function useCreateResourceUsageSummaryMutation(
  params: {
    selection: SelectionConfig<ResourceUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateResourceUsageSummaryInput['resourceUsageSummary']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceUsageSummarySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceUsageSummaryMutationKeys.create(),
    mutationFn: (data: CreateResourceUsageSummaryInput['resourceUsageSummary']) =>
      getClient()
        .resourceUsageSummary.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: resourceUsageSummaryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
