/**
 * Function deployment bindings — ties a handler image to a namespace for Knative provisioning and routing (one row per handler image per namespace)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformFunctionDeploymentKeys } from '../query-keys';
import { platformFunctionDeploymentMutationKeys } from '../mutation-keys';
import type {
  PlatformFunctionDeploymentSelect,
  PlatformFunctionDeploymentWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformFunctionDeploymentSelect,
  PlatformFunctionDeploymentWithRelations,
} from '../../orm/input-types';
/**
 * Function deployment bindings — ties a handler image to a namespace for Knative provisioning and routing (one row per handler image per namespace)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformFunctionDeploymentMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformFunctionDeploymentMutation<
  S extends PlatformFunctionDeploymentSelect,
>(
  params: {
    selection: {
      fields: S & PlatformFunctionDeploymentSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformFunctionDeploymentSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformFunctionDeployment: {
          platformFunctionDeployment: InferSelectResult<PlatformFunctionDeploymentWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformFunctionDeployment: {
      platformFunctionDeployment: InferSelectResult<PlatformFunctionDeploymentWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformFunctionDeploymentMutation(
  params: {
    selection: SelectionConfig<PlatformFunctionDeploymentSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformFunctionDeploymentSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformFunctionDeploymentMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformFunctionDeployment.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformFunctionDeploymentKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformFunctionDeploymentKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
