/**
 * Namespace lifecycle events — audit log of creation, activation, deactivation, label changes
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { namespaceEventKeys } from '../query-keys';
import { namespaceEventMutationKeys } from '../mutation-keys';
import type {
  NamespaceEventSelect,
  NamespaceEventWithRelations,
  NamespaceEventPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  NamespaceEventSelect,
  NamespaceEventWithRelations,
  NamespaceEventPatch,
} from '../../orm/input-types';
/**
 * Namespace lifecycle events — audit log of creation, activation, deactivation, label changes
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateNamespaceEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', namespaceEventPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateNamespaceEventMutation<S extends NamespaceEventSelect>(
  params: {
    selection: {
      fields: S & NamespaceEventSelect;
    } & HookStrictSelect<NoInfer<S>, NamespaceEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateNamespaceEvent: {
          namespaceEvent: InferSelectResult<NamespaceEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        createdAt: string;
        namespaceEventPatch: NamespaceEventPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateNamespaceEvent: {
      namespaceEvent: InferSelectResult<NamespaceEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    createdAt: string;
    namespaceEventPatch: NamespaceEventPatch;
  }
>;
export function useUpdateNamespaceEventMutation(
  params: {
    selection: SelectionConfig<NamespaceEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        createdAt: string;
        namespaceEventPatch: NamespaceEventPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<NamespaceEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: namespaceEventMutationKeys.all,
    mutationFn: ({
      id,
      createdAt,
      namespaceEventPatch,
    }: {
      id: string;
      createdAt: string;
      namespaceEventPatch: NamespaceEventPatch;
    }) =>
      getClient()
        .namespaceEvent.update({
          where: {
            id,
            createdAt,
          },
          data: namespaceEventPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: namespaceEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: namespaceEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
