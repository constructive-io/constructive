/**
 * Normalized repository events from local hooks and external providers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRepositoryEventKeys } from '../query-keys';
import { platformRepositoryEventMutationKeys } from '../mutation-keys';
import type {
  PlatformRepositoryEventSelect,
  PlatformRepositoryEventWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformRepositoryEventSelect,
  PlatformRepositoryEventWithRelations,
} from '../../orm/input-types';
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformRepositoryEventMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformRepositoryEventMutation<S extends PlatformRepositoryEventSelect>(
  params: {
    selection: {
      fields: S & PlatformRepositoryEventSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformRepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformRepositoryEvent: {
          platformRepositoryEvent: InferSelectResult<PlatformRepositoryEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformRepositoryEvent: {
      platformRepositoryEvent: InferSelectResult<PlatformRepositoryEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformRepositoryEventMutation(
  params: {
    selection: SelectionConfig<PlatformRepositoryEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformRepositoryEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformRepositoryEventMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformRepositoryEvent.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformRepositoryEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformRepositoryEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
