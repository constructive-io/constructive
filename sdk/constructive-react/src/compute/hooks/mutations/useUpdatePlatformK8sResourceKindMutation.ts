/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformK8sResourceKindKeys } from '../query-keys';
import { platformK8sResourceKindMutationKeys } from '../mutation-keys';
import type {
  PlatformK8sResourceKindSelect,
  PlatformK8sResourceKindWithRelations,
  PlatformK8sResourceKindPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformK8sResourceKindSelect,
  PlatformK8sResourceKindWithRelations,
  PlatformK8sResourceKindPatch,
} from '../../orm/input-types';
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlatformK8sResourceKindMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', platformK8sResourceKindPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlatformK8sResourceKindMutation<S extends PlatformK8sResourceKindSelect>(
  params: {
    selection: {
      fields: S & PlatformK8sResourceKindSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlatformK8sResourceKind: {
          platformK8sResourceKind: InferSelectResult<PlatformK8sResourceKindWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        platformK8sResourceKindPatch: PlatformK8sResourceKindPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlatformK8sResourceKind: {
      platformK8sResourceKind: InferSelectResult<PlatformK8sResourceKindWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    platformK8sResourceKindPatch: PlatformK8sResourceKindPatch;
  }
>;
export function useUpdatePlatformK8sResourceKindMutation(
  params: {
    selection: SelectionConfig<PlatformK8sResourceKindSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        platformK8sResourceKindPatch: PlatformK8sResourceKindPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformK8sResourceKindSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformK8sResourceKindMutationKeys.all,
    mutationFn: ({
      id,
      platformK8sResourceKindPatch,
    }: {
      id: string;
      platformK8sResourceKindPatch: PlatformK8sResourceKindPatch;
    }) =>
      getClient()
        .platformK8sResourceKind.update({
          where: {
            id,
          },
          data: platformK8sResourceKindPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: platformK8sResourceKindKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformK8sResourceKindKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
