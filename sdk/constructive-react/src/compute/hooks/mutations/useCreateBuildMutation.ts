/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildKeys } from '../query-keys';
import { buildMutationKeys } from '../mutation-keys';
import type { BuildSelect, BuildWithRelations, CreateBuildInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuildSelect, BuildWithRelations, CreateBuildInput } from '../../orm/input-types';
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateBuildMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateBuildMutation<S extends BuildSelect>(
  params: {
    selection: {
      fields: S & BuildSelect;
    } & HookStrictSelect<NoInfer<S>, BuildSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createBuild: {
          build: InferSelectResult<BuildWithRelations, S>;
        };
      },
      Error,
      CreateBuildInput['build']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createBuild: {
      build: InferSelectResult<BuildWithRelations, S>;
    };
  },
  Error,
  CreateBuildInput['build']
>;
export function useCreateBuildMutation(
  params: {
    selection: SelectionConfig<BuildSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateBuildInput['build']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<BuildSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: buildMutationKeys.create(),
    mutationFn: (data: CreateBuildInput['build']) =>
      getClient()
        .build.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: buildKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
