/**
 * Workflows required to pass before a repository proposal merges
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryRequiredCheckKeys } from '../query-keys';
import { repositoryRequiredCheckMutationKeys } from '../mutation-keys';
import type {
  RepositoryRequiredCheckSelect,
  RepositoryRequiredCheckWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryRequiredCheckSelect,
  RepositoryRequiredCheckWithRelations,
} from '../../orm/input-types';
/**
 * Workflows required to pass before a repository proposal merges
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRepositoryRequiredCheckMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRepositoryRequiredCheckMutation<S extends RepositoryRequiredCheckSelect>(
  params: {
    selection: {
      fields: S & RepositoryRequiredCheckSelect;
    } & HookStrictSelect<NoInfer<S>, RepositoryRequiredCheckSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRepositoryRequiredCheck: {
          repositoryRequiredCheck: InferSelectResult<RepositoryRequiredCheckWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRepositoryRequiredCheck: {
      repositoryRequiredCheck: InferSelectResult<RepositoryRequiredCheckWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRepositoryRequiredCheckMutation(
  params: {
    selection: SelectionConfig<RepositoryRequiredCheckSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RepositoryRequiredCheckSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: repositoryRequiredCheckMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .repositoryRequiredCheck.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: repositoryRequiredCheckKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: repositoryRequiredCheckKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
