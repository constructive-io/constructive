/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildKeys } from '../query-keys';
import { buildMutationKeys } from '../mutation-keys';
import type { BuildSelect, BuildWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuildSelect, BuildWithRelations } from '../../orm/input-types';
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteBuildMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteBuildMutation<S extends BuildSelect>(
  params: {
    selection: {
      fields: S & BuildSelect;
    } & HookStrictSelect<NoInfer<S>, BuildSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteBuild: {
          build: InferSelectResult<BuildWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteBuild: {
      build: InferSelectResult<BuildWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteBuildMutation(
  params: {
    selection: SelectionConfig<BuildSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BuildSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: buildMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .build.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: buildKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: buildKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
