/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildStepKeys } from '../query-keys';
import { buildStepMutationKeys } from '../mutation-keys';
import type {
  BuildStepSelect,
  BuildStepWithRelations,
  BuildStepPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  BuildStepSelect,
  BuildStepWithRelations,
  BuildStepPatch,
} from '../../orm/input-types';
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateBuildStepMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', buildStepPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateBuildStepMutation<S extends BuildStepSelect>(
  params: {
    selection: {
      fields: S & BuildStepSelect;
    } & HookStrictSelect<NoInfer<S>, BuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateBuildStep: {
          buildStep: InferSelectResult<BuildStepWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        recordedAt: string;
        buildStepPatch: BuildStepPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateBuildStep: {
      buildStep: InferSelectResult<BuildStepWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    recordedAt: string;
    buildStepPatch: BuildStepPatch;
  }
>;
export function useUpdateBuildStepMutation(
  params: {
    selection: SelectionConfig<BuildStepSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        recordedAt: string;
        buildStepPatch: BuildStepPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BuildStepSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: buildStepMutationKeys.all,
    mutationFn: ({
      id,
      recordedAt,
      buildStepPatch,
    }: {
      id: string;
      recordedAt: string;
      buildStepPatch: BuildStepPatch;
    }) =>
      getClient()
        .buildStep.update({
          where: {
            id,
            recordedAt,
          },
          data: buildStepPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: buildStepKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: buildStepKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
