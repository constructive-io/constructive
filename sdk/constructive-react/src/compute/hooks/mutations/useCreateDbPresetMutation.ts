/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPresetKeys } from '../query-keys';
import { dbPresetMutationKeys } from '../mutation-keys';
import type {
  DbPresetSelect,
  DbPresetWithRelations,
  CreateDbPresetInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DbPresetSelect,
  DbPresetWithRelations,
  CreateDbPresetInput,
} from '../../orm/input-types';
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateDbPresetMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateDbPresetMutation<S extends DbPresetSelect>(
  params: {
    selection: {
      fields: S & DbPresetSelect;
    } & HookStrictSelect<NoInfer<S>, DbPresetSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createDbPreset: {
          dbPreset: InferSelectResult<DbPresetWithRelations, S>;
        };
      },
      Error,
      CreateDbPresetInput['dbPreset']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createDbPreset: {
      dbPreset: InferSelectResult<DbPresetWithRelations, S>;
    };
  },
  Error,
  CreateDbPresetInput['dbPreset']
>;
export function useCreateDbPresetMutation(
  params: {
    selection: SelectionConfig<DbPresetSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateDbPresetInput['dbPreset']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<DbPresetSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: dbPresetMutationKeys.create(),
    mutationFn: (data: CreateDbPresetInput['dbPreset']) =>
      getClient()
        .dbPreset.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: dbPresetKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
