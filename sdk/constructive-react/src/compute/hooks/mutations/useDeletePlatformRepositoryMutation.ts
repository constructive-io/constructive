/**
 * Source repositories, hosted locally or on an external provider
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRepositoryKeys } from '../query-keys';
import { platformRepositoryMutationKeys } from '../mutation-keys';
import type {
  PlatformRepositorySelect,
  PlatformRepositoryWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformRepositorySelect,
  PlatformRepositoryWithRelations,
} from '../../orm/input-types';
/**
 * Source repositories, hosted locally or on an external provider
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlatformRepositoryMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlatformRepositoryMutation<S extends PlatformRepositorySelect>(
  params: {
    selection: {
      fields: S & PlatformRepositorySelect;
    } & HookStrictSelect<NoInfer<S>, PlatformRepositorySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlatformRepository: {
          platformRepository: InferSelectResult<PlatformRepositoryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlatformRepository: {
      platformRepository: InferSelectResult<PlatformRepositoryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlatformRepositoryMutation(
  params: {
    selection: SelectionConfig<PlatformRepositorySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformRepositorySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformRepositoryMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .platformRepository.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: platformRepositoryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: platformRepositoryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
