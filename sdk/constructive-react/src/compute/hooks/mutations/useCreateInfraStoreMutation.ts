/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraStoreKeys } from '../query-keys';
import { infraStoreMutationKeys } from '../mutation-keys';
import type {
  InfraStoreSelect,
  InfraStoreWithRelations,
  CreateInfraStoreInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  InfraStoreSelect,
  InfraStoreWithRelations,
  CreateInfraStoreInput,
} from '../../orm/input-types';
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateInfraStoreMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateInfraStoreMutation<S extends InfraStoreSelect>(
  params: {
    selection: {
      fields: S & InfraStoreSelect;
    } & HookStrictSelect<NoInfer<S>, InfraStoreSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createInfraStore: {
          infraStore: InferSelectResult<InfraStoreWithRelations, S>;
        };
      },
      Error,
      CreateInfraStoreInput['infraStore']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createInfraStore: {
      infraStore: InferSelectResult<InfraStoreWithRelations, S>;
    };
  },
  Error,
  CreateInfraStoreInput['infraStore']
>;
export function useCreateInfraStoreMutation(
  params: {
    selection: SelectionConfig<InfraStoreSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateInfraStoreInput['infraStore']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<InfraStoreSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: infraStoreMutationKeys.create(),
    mutationFn: (data: CreateInfraStoreInput['infraStore']) =>
      getClient()
        .infraStore.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: infraStoreKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
