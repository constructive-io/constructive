/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryKeys } from '../query-keys';
import { registryMutationKeys } from '../mutation-keys';
import type { RegistrySelect, RegistryWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistrySelect, RegistryWithRelations } from '../../orm/input-types';
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteRegistryMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteRegistryMutation<S extends RegistrySelect>(
  params: {
    selection: {
      fields: S & RegistrySelect;
    } & HookStrictSelect<NoInfer<S>, RegistrySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteRegistry: {
          registry: InferSelectResult<RegistryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteRegistry: {
      registry: InferSelectResult<RegistryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteRegistryMutation(
  params: {
    selection: SelectionConfig<RegistrySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<RegistrySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: registryMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .registry.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: registryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: registryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
