/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { secretDefinitionKeys } from '../query-keys';
import { secretDefinitionMutationKeys } from '../mutation-keys';
import type {
  SecretDefinitionSelect,
  SecretDefinitionWithRelations,
  CreateSecretDefinitionInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  SecretDefinitionSelect,
  SecretDefinitionWithRelations,
  CreateSecretDefinitionInput,
} from '../../orm/input-types';
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSecretDefinitionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateSecretDefinitionMutation<S extends SecretDefinitionSelect>(
  params: {
    selection: {
      fields: S & SecretDefinitionSelect;
    } & HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createSecretDefinition: {
          secretDefinition: InferSelectResult<SecretDefinitionWithRelations, S>;
        };
      },
      Error,
      CreateSecretDefinitionInput['secretDefinition']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createSecretDefinition: {
      secretDefinition: InferSelectResult<SecretDefinitionWithRelations, S>;
    };
  },
  Error,
  CreateSecretDefinitionInput['secretDefinition']
>;
export function useCreateSecretDefinitionMutation(
  params: {
    selection: SelectionConfig<SecretDefinitionSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateSecretDefinitionInput['secretDefinition']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SecretDefinitionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: secretDefinitionMutationKeys.create(),
    mutationFn: (data: CreateSecretDefinitionInput['secretDefinition']) =>
      getClient()
        .secretDefinition.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: secretDefinitionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
