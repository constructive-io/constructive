/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryKeys } from '../query-keys';
import { registryMutationKeys } from '../mutation-keys';
import type {
  RegistrySelect,
  RegistryWithRelations,
  CreateRegistryInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RegistrySelect,
  RegistryWithRelations,
  CreateRegistryInput,
} from '../../orm/input-types';
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateRegistryMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateRegistryMutation<S extends RegistrySelect>(
  params: {
    selection: {
      fields: S & RegistrySelect;
    } & HookStrictSelect<NoInfer<S>, RegistrySelect>;
  } & Omit<
    UseMutationOptions<
      {
        createRegistry: {
          registry: InferSelectResult<RegistryWithRelations, S>;
        };
      },
      Error,
      CreateRegistryInput['registry']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createRegistry: {
      registry: InferSelectResult<RegistryWithRelations, S>;
    };
  },
  Error,
  CreateRegistryInput['registry']
>;
export function useCreateRegistryMutation(
  params: {
    selection: SelectionConfig<RegistrySelect>;
  } & Omit<UseMutationOptions<any, Error, CreateRegistryInput['registry']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<RegistrySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: registryMutationKeys.create(),
    mutationFn: (data: CreateRegistryInput['registry']) =>
      getClient()
        .registry.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: registryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
