/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { namespaceKeys } from '../query-keys';
import { namespaceMutationKeys } from '../mutation-keys';
import type { NamespaceSelect, NamespaceWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { NamespaceSelect, NamespaceWithRelations } from '../../orm/input-types';
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteNamespaceMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteNamespaceMutation<S extends NamespaceSelect>(
  params: {
    selection: {
      fields: S & NamespaceSelect;
    } & HookStrictSelect<NoInfer<S>, NamespaceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteNamespace: {
          namespace: InferSelectResult<NamespaceWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteNamespace: {
      namespace: InferSelectResult<NamespaceWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteNamespaceMutation(
  params: {
    selection: SelectionConfig<NamespaceSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<NamespaceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: namespaceMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .namespace.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: namespaceKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: namespaceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
