/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { webhookEndpointKeys } from '../query-keys';
import { webhookEndpointMutationKeys } from '../mutation-keys';
import type {
  WebhookEndpointSelect,
  WebhookEndpointWithRelations,
  CreateWebhookEndpointInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  WebhookEndpointSelect,
  WebhookEndpointWithRelations,
  CreateWebhookEndpointInput,
} from '../../orm/input-types';
/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateWebhookEndpointMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateWebhookEndpointMutation<S extends WebhookEndpointSelect>(
  params: {
    selection: {
      fields: S & WebhookEndpointSelect;
    } & HookStrictSelect<NoInfer<S>, WebhookEndpointSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createWebhookEndpoint: {
          webhookEndpoint: InferSelectResult<WebhookEndpointWithRelations, S>;
        };
      },
      Error,
      CreateWebhookEndpointInput['webhookEndpoint']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createWebhookEndpoint: {
      webhookEndpoint: InferSelectResult<WebhookEndpointWithRelations, S>;
    };
  },
  Error,
  CreateWebhookEndpointInput['webhookEndpoint']
>;
export function useCreateWebhookEndpointMutation(
  params: {
    selection: SelectionConfig<WebhookEndpointSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateWebhookEndpointInput['webhookEndpoint']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<WebhookEndpointSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: webhookEndpointMutationKeys.create(),
    mutationFn: (data: CreateWebhookEndpointInput['webhookEndpoint']) =>
      getClient()
        .webhookEndpoint.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: webhookEndpointKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
