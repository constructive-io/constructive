/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformResourceKeys } from '../query-keys';
import { platformResourceMutationKeys } from '../mutation-keys';
import type {
  PlatformResourceSelect,
  PlatformResourceWithRelations,
  CreatePlatformResourceInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformResourceSelect,
  PlatformResourceWithRelations,
  CreatePlatformResourceInput,
} from '../../orm/input-types';
/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlatformResourceMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlatformResourceMutation<S extends PlatformResourceSelect>(
  params: {
    selection: {
      fields: S & PlatformResourceSelect;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlatformResource: {
          platformResource: InferSelectResult<PlatformResourceWithRelations, S>;
        };
      },
      Error,
      CreatePlatformResourceInput['platformResource']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlatformResource: {
      platformResource: InferSelectResult<PlatformResourceWithRelations, S>;
    };
  },
  Error,
  CreatePlatformResourceInput['platformResource']
>;
export function useCreatePlatformResourceMutation(
  params: {
    selection: SelectionConfig<PlatformResourceSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreatePlatformResourceInput['platformResource']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlatformResourceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: platformResourceMutationKeys.create(),
    mutationFn: (data: CreatePlatformResourceInput['platformResource']) =>
      getClient()
        .platformResource.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: platformResourceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
