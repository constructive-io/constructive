/**
 * Installed resource bundle ("release") — groups a set of resources; params are merkle-versioned in the scope's shared infra store for rollback
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceInstallationKeys } from '../query-keys';
import { resourceInstallationMutationKeys } from '../mutation-keys';
import type {
  ResourceInstallationSelect,
  ResourceInstallationWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceInstallationSelect,
  ResourceInstallationWithRelations,
} from '../../orm/input-types';
/**
 * Installed resource bundle ("release") — groups a set of resources; params are merkle-versioned in the scope's shared infra store for rollback
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteResourceInstallationMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteResourceInstallationMutation<S extends ResourceInstallationSelect>(
  params: {
    selection: {
      fields: S & ResourceInstallationSelect;
    } & HookStrictSelect<NoInfer<S>, ResourceInstallationSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteResourceInstallation: {
          resourceInstallation: InferSelectResult<ResourceInstallationWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteResourceInstallation: {
      resourceInstallation: InferSelectResult<ResourceInstallationWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteResourceInstallationMutation(
  params: {
    selection: SelectionConfig<ResourceInstallationSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceInstallationSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceInstallationMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .resourceInstallation.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: resourceInstallationKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: resourceInstallationKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
