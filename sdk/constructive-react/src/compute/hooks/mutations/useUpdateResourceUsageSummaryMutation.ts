/**
 * Resource usage summaries — runtime seconds, GB-seconds, and max gauges per (resource, namespace, day); resource_id-NULL rows are namespace-grain totals
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourceUsageSummaryKeys } from '../query-keys';
import { resourceUsageSummaryMutationKeys } from '../mutation-keys';
import type {
  ResourceUsageSummarySelect,
  ResourceUsageSummaryWithRelations,
  ResourceUsageSummaryPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ResourceUsageSummarySelect,
  ResourceUsageSummaryWithRelations,
  ResourceUsageSummaryPatch,
} from '../../orm/input-types';
/**
 * Resource usage summaries — runtime seconds, GB-seconds, and max gauges per (resource, namespace, day); resource_id-NULL rows are namespace-grain totals
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateResourceUsageSummaryMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', resourceUsageSummaryPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateResourceUsageSummaryMutation<S extends ResourceUsageSummarySelect>(
  params: {
    selection: {
      fields: S & ResourceUsageSummarySelect;
    } & HookStrictSelect<NoInfer<S>, ResourceUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateResourceUsageSummary: {
          resourceUsageSummary: InferSelectResult<ResourceUsageSummaryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        date: string;
        resourceUsageSummaryPatch: ResourceUsageSummaryPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateResourceUsageSummary: {
      resourceUsageSummary: InferSelectResult<ResourceUsageSummaryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    date: string;
    resourceUsageSummaryPatch: ResourceUsageSummaryPatch;
  }
>;
export function useUpdateResourceUsageSummaryMutation(
  params: {
    selection: SelectionConfig<ResourceUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        date: string;
        resourceUsageSummaryPatch: ResourceUsageSummaryPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ResourceUsageSummarySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: resourceUsageSummaryMutationKeys.all,
    mutationFn: ({
      id,
      date,
      resourceUsageSummaryPatch,
    }: {
      id: string;
      date: string;
      resourceUsageSummaryPatch: ResourceUsageSummaryPatch;
    }) =>
      getClient()
        .resourceUsageSummary.update({
          where: {
            id,
            date,
          },
          data: resourceUsageSummaryPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: resourceUsageSummaryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: resourceUsageSummaryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
