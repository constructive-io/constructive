/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { proposalKeys } from '../query-keys';
import type { ProposalSelect, ProposalWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ProposalSelect, ProposalWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const proposalQueryKey = proposalKeys.detail;
/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useProposalQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useProposalQuery<
  S extends ProposalSelect,
  TData = {
    proposal: InferSelectResult<ProposalWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ProposalSelect>;
  } & Omit<
    UseQueryOptions<
      {
        proposal: InferSelectResult<ProposalWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useProposalQuery(
  params: {
    id: string;
    selection: SelectionConfig<ProposalSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ProposalSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: proposalKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .proposal.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 *
 * @example
 * ```ts
 * const data = await fetchProposalQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchProposalQuery<S extends ProposalSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ProposalSelect>;
}): Promise<{
  proposal: InferSelectResult<ProposalWithRelations, S> | null;
}>;
export async function fetchProposalQuery(params: {
  id: string;
  selection: SelectionConfig<ProposalSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ProposalSelect>(params.selection);
  return getClient()
    .proposal.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Proposals against a repository: issues, changes to merge, discussions and decisions
 *
 * @example
 * ```ts
 * await prefetchProposalQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchProposalQuery<S extends ProposalSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ProposalSelect>;
  }
): Promise<void>;
export async function prefetchProposalQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ProposalSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ProposalSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: proposalKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .proposal.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
