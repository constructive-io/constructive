/**
 * Container image catalog: images available to run as functions, resources, and builds
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformImageKeys } from '../query-keys';
import type { PlatformImageSelect, PlatformImageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformImageSelect, PlatformImageWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformImageQueryKey = platformImageKeys.detail;
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformImageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformImageQuery<
  S extends PlatformImageSelect,
  TData = {
    platformImage: InferSelectResult<PlatformImageWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformImage: InferSelectResult<PlatformImageWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformImageQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformImageSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformImageSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformImageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformImage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * const data = await fetchPlatformImageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformImageQuery<S extends PlatformImageSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
}): Promise<{
  platformImage: InferSelectResult<PlatformImageWithRelations, S> | null;
}>;
export async function fetchPlatformImageQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformImageSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformImageSelect>(params.selection);
  return getClient()
    .platformImage.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * await prefetchPlatformImageQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformImageQuery<S extends PlatformImageSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
  }
): Promise<void>;
export async function prefetchPlatformImageQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformImageSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformImageSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformImageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformImage.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
