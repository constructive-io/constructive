/**
 * Join table binding function definitions to API endpoints with per-binding alias and config
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionApiBindingKeys } from '../query-keys';
import type {
  FunctionApiBindingSelect,
  FunctionApiBindingWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  FunctionApiBindingSelect,
  FunctionApiBindingWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const functionApiBindingQueryKey = functionApiBindingKeys.detail;
/**
 * Join table binding function definitions to API endpoints with per-binding alias and config
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFunctionApiBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFunctionApiBindingQuery<
  S extends FunctionApiBindingSelect,
  TData = {
    functionApiBinding: InferSelectResult<FunctionApiBindingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionApiBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        functionApiBinding: InferSelectResult<FunctionApiBindingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFunctionApiBindingQuery(
  params: {
    id: string;
    selection: SelectionConfig<FunctionApiBindingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FunctionApiBindingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: functionApiBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionApiBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Join table binding function definitions to API endpoints with per-binding alias and config
 *
 * @example
 * ```ts
 * const data = await fetchFunctionApiBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFunctionApiBindingQuery<S extends FunctionApiBindingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FunctionApiBindingSelect>;
}): Promise<{
  functionApiBinding: InferSelectResult<FunctionApiBindingWithRelations, S> | null;
}>;
export async function fetchFunctionApiBindingQuery(params: {
  id: string;
  selection: SelectionConfig<FunctionApiBindingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<FunctionApiBindingSelect>(params.selection);
  return getClient()
    .functionApiBinding.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Join table binding function definitions to API endpoints with per-binding alias and config
 *
 * @example
 * ```ts
 * await prefetchFunctionApiBindingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFunctionApiBindingQuery<S extends FunctionApiBindingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionApiBindingSelect>;
  }
): Promise<void>;
export async function prefetchFunctionApiBindingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FunctionApiBindingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FunctionApiBindingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: functionApiBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionApiBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
