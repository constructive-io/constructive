/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformResourceStatusCheckKeys } from '../query-keys';
import type {
  PlatformResourceStatusCheckSelect,
  PlatformResourceStatusCheckWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformResourceStatusCheckSelect,
  PlatformResourceStatusCheckWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformResourceStatusCheckQueryKey = platformResourceStatusCheckKeys.detail;
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformResourceStatusCheckQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformResourceStatusCheckQuery<
  S extends PlatformResourceStatusCheckSelect,
  TData = {
    platformResourceStatusCheck: InferSelectResult<
      PlatformResourceStatusCheckWithRelations,
      S
    > | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceStatusCheckSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformResourceStatusCheck: InferSelectResult<
          PlatformResourceStatusCheckWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformResourceStatusCheckQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformResourceStatusCheckSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformResourceStatusCheckSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformResourceStatusCheckKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformResourceStatusCheck.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```ts
 * const data = await fetchPlatformResourceStatusCheckQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformResourceStatusCheckQuery<
  S extends PlatformResourceStatusCheckSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformResourceStatusCheckSelect>;
}): Promise<{
  platformResourceStatusCheck: InferSelectResult<
    PlatformResourceStatusCheckWithRelations,
    S
  > | null;
}>;
export async function fetchPlatformResourceStatusCheckQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformResourceStatusCheckSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformResourceStatusCheckSelect>(params.selection);
  return getClient()
    .platformResourceStatusCheck.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```ts
 * await prefetchPlatformResourceStatusCheckQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformResourceStatusCheckQuery<
  S extends PlatformResourceStatusCheckSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceStatusCheckSelect>;
  }
): Promise<void>;
export async function prefetchPlatformResourceStatusCheckQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformResourceStatusCheckSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformResourceStatusCheckSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformResourceStatusCheckKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformResourceStatusCheck.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
