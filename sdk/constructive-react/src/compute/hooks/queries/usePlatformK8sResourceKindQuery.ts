/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformK8sResourceKindKeys } from '../query-keys';
import type {
  PlatformK8sResourceKindSelect,
  PlatformK8sResourceKindWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformK8sResourceKindSelect,
  PlatformK8sResourceKindWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformK8sResourceKindQueryKey = platformK8sResourceKindKeys.detail;
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformK8sResourceKindQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformK8sResourceKindQuery<
  S extends PlatformK8sResourceKindSelect,
  TData = {
    platformK8sResourceKind: InferSelectResult<PlatformK8sResourceKindWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformK8sResourceKind: InferSelectResult<PlatformK8sResourceKindWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformK8sResourceKindQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformK8sResourceKindSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformK8sResourceKindSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformK8sResourceKindKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformK8sResourceKind.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```ts
 * const data = await fetchPlatformK8sResourceKindQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformK8sResourceKindQuery<
  S extends PlatformK8sResourceKindSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
}): Promise<{
  platformK8sResourceKind: InferSelectResult<PlatformK8sResourceKindWithRelations, S> | null;
}>;
export async function fetchPlatformK8sResourceKindQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformK8sResourceKindSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformK8sResourceKindSelect>(params.selection);
  return getClient()
    .platformK8sResourceKind.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```ts
 * await prefetchPlatformK8sResourceKindQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformK8sResourceKindQuery<S extends PlatformK8sResourceKindSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
  }
): Promise<void>;
export async function prefetchPlatformK8sResourceKindQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformK8sResourceKindSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformK8sResourceKindSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformK8sResourceKindKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformK8sResourceKind.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
