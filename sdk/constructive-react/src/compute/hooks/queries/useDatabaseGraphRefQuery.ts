/**
 * Branch heads — mutable pointers into the commit chain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseGraphRefKeys } from '../query-keys';
import type { DatabaseGraphRefSelect, DatabaseGraphRefWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DatabaseGraphRefSelect, DatabaseGraphRefWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseGraphRefQueryKey = databaseGraphRefKeys.detail;
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseGraphRefQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDatabaseGraphRefQuery<
  S extends DatabaseGraphRefSelect,
  TData = {
    databaseGraphRef: InferSelectResult<DatabaseGraphRefWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseGraphRef: InferSelectResult<DatabaseGraphRefWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseGraphRefQuery(
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphRefSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DatabaseGraphRefSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseGraphRefKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphRef.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseGraphRefQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDatabaseGraphRefQuery<S extends DatabaseGraphRefSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
}): Promise<{
  databaseGraphRef: InferSelectResult<DatabaseGraphRefWithRelations, S> | null;
}>;
export async function fetchDatabaseGraphRefQuery(params: {
  id: string;
  selection: SelectionConfig<DatabaseGraphRefSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DatabaseGraphRefSelect>(params.selection);
  return getClient()
    .databaseGraphRef.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```ts
 * await prefetchDatabaseGraphRefQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDatabaseGraphRefQuery<S extends DatabaseGraphRefSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseGraphRefQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphRefSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DatabaseGraphRefSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseGraphRefKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphRef.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
