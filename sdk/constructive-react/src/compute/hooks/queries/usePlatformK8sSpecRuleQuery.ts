/**
 * Spec rulebook for DB-driven resources — merkle-versioned head over the infra store; enforced by the generated admission gate via infra_utils.check_resource_admission
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformK8sSpecRuleKeys } from '../query-keys';
import type {
  PlatformK8sSpecRuleSelect,
  PlatformK8sSpecRuleWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformK8sSpecRuleSelect,
  PlatformK8sSpecRuleWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformK8sSpecRuleQueryKey = platformK8sSpecRuleKeys.detail;
/**
 * Spec rulebook for DB-driven resources — merkle-versioned head over the infra store; enforced by the generated admission gate via infra_utils.check_resource_admission
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformK8sSpecRuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformK8sSpecRuleQuery<
  S extends PlatformK8sSpecRuleSelect,
  TData = {
    platformK8sSpecRule: InferSelectResult<PlatformK8sSpecRuleWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformK8sSpecRuleSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformK8sSpecRule: InferSelectResult<PlatformK8sSpecRuleWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformK8sSpecRuleQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformK8sSpecRuleSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformK8sSpecRuleSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformK8sSpecRuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformK8sSpecRule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Spec rulebook for DB-driven resources — merkle-versioned head over the infra store; enforced by the generated admission gate via infra_utils.check_resource_admission
 *
 * @example
 * ```ts
 * const data = await fetchPlatformK8sSpecRuleQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformK8sSpecRuleQuery<S extends PlatformK8sSpecRuleSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformK8sSpecRuleSelect>;
}): Promise<{
  platformK8sSpecRule: InferSelectResult<PlatformK8sSpecRuleWithRelations, S> | null;
}>;
export async function fetchPlatformK8sSpecRuleQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformK8sSpecRuleSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformK8sSpecRuleSelect>(params.selection);
  return getClient()
    .platformK8sSpecRule.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Spec rulebook for DB-driven resources — merkle-versioned head over the infra store; enforced by the generated admission gate via infra_utils.check_resource_admission
 *
 * @example
 * ```ts
 * await prefetchPlatformK8sSpecRuleQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformK8sSpecRuleQuery<S extends PlatformK8sSpecRuleSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformK8sSpecRuleSelect>;
  }
): Promise<void>;
export async function prefetchPlatformK8sSpecRuleQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformK8sSpecRuleSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformK8sSpecRuleSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformK8sSpecRuleKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformK8sSpecRule.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
