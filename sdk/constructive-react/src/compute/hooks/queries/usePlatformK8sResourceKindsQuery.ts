/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformK8sResourceKindKeys } from '../query-keys';
import type {
  PlatformK8sResourceKindSelect,
  PlatformK8sResourceKindWithRelations,
  PlatformK8sResourceKindFilter,
  PlatformK8sResourceKindOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformK8sResourceKindSelect,
  PlatformK8sResourceKindWithRelations,
  PlatformK8sResourceKindFilter,
  PlatformK8sResourceKindOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformK8sResourceKindsQueryKey = platformK8sResourceKindKeys.list;
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformK8sResourceKindsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformK8sResourceKindsQuery<
  S extends PlatformK8sResourceKindSelect,
  TData = {
    platformK8sResourceKinds: ConnectionResult<
      InferSelectResult<PlatformK8sResourceKindWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformK8sResourceKindFilter, PlatformK8sResourceKindOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformK8sResourceKinds: ConnectionResult<
          InferSelectResult<PlatformK8sResourceKindWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformK8sResourceKindsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformK8sResourceKindSelect,
      PlatformK8sResourceKindFilter,
      PlatformK8sResourceKindOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformK8sResourceKindSelect,
    PlatformK8sResourceKindFilter,
    PlatformK8sResourceKindOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformK8sResourceKindKeys.list(args),
    queryFn: () => getClient().platformK8sResourceKind.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```ts
 * const data = await fetchPlatformK8sResourceKindsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformK8sResourceKindsQuery<
  S extends PlatformK8sResourceKindSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformK8sResourceKindFilter, PlatformK8sResourceKindOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
}): Promise<{
  platformK8sResourceKinds: ConnectionResult<
    InferSelectResult<PlatformK8sResourceKindWithRelations, S>
  >;
}>;
export async function fetchPlatformK8sResourceKindsQuery(params: {
  selection: ListSelectionConfig<
    PlatformK8sResourceKindSelect,
    PlatformK8sResourceKindFilter,
    PlatformK8sResourceKindOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformK8sResourceKindSelect,
    PlatformK8sResourceKindFilter,
    PlatformK8sResourceKindOrderBy
  >(params.selection);
  return getClient().platformK8sResourceKind.findMany(args).unwrap();
}
/**
 * Kubernetes kind allow-list for DB-driven resources — merkle-versioned head over the infra store; the admission gate fails closed on kinds without an active row
 *
 * @example
 * ```ts
 * await prefetchPlatformK8sResourceKindsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformK8sResourceKindsQuery<
  S extends PlatformK8sResourceKindSelect,
>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformK8sResourceKindFilter, PlatformK8sResourceKindOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformK8sResourceKindSelect>;
  }
): Promise<void>;
export async function prefetchPlatformK8sResourceKindsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformK8sResourceKindSelect,
      PlatformK8sResourceKindFilter,
      PlatformK8sResourceKindOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformK8sResourceKindSelect,
    PlatformK8sResourceKindFilter,
    PlatformK8sResourceKindOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformK8sResourceKindKeys.list(args),
    queryFn: () => getClient().platformK8sResourceKind.findMany(args).unwrap(),
  });
}
