/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformRegistryBindingKeys } from '../query-keys';
import type {
  PlatformRegistryBindingSelect,
  PlatformRegistryBindingWithRelations,
  PlatformRegistryBindingFilter,
  PlatformRegistryBindingOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformRegistryBindingSelect,
  PlatformRegistryBindingWithRelations,
  PlatformRegistryBindingFilter,
  PlatformRegistryBindingOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformRegistryBindingsQueryKey = platformRegistryBindingKeys.list;
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformRegistryBindingsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformRegistryBindingsQuery<
  S extends PlatformRegistryBindingSelect,
  TData = {
    platformRegistryBindings: ConnectionResult<
      InferSelectResult<PlatformRegistryBindingWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformRegistryBindingFilter, PlatformRegistryBindingOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformRegistryBindings: ConnectionResult<
          InferSelectResult<PlatformRegistryBindingWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformRegistryBindingsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformRegistryBindingSelect,
      PlatformRegistryBindingFilter,
      PlatformRegistryBindingOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformRegistryBindingSelect,
    PlatformRegistryBindingFilter,
    PlatformRegistryBindingOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformRegistryBindingKeys.list(args),
    queryFn: () => getClient().platformRegistryBinding.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * const data = await fetchPlatformRegistryBindingsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformRegistryBindingsQuery<
  S extends PlatformRegistryBindingSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformRegistryBindingFilter, PlatformRegistryBindingOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
}): Promise<{
  platformRegistryBindings: ConnectionResult<
    InferSelectResult<PlatformRegistryBindingWithRelations, S>
  >;
}>;
export async function fetchPlatformRegistryBindingsQuery(params: {
  selection: ListSelectionConfig<
    PlatformRegistryBindingSelect,
    PlatformRegistryBindingFilter,
    PlatformRegistryBindingOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformRegistryBindingSelect,
    PlatformRegistryBindingFilter,
    PlatformRegistryBindingOrderBy
  >(params.selection);
  return getClient().platformRegistryBinding.findMany(args).unwrap();
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * await prefetchPlatformRegistryBindingsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformRegistryBindingsQuery<
  S extends PlatformRegistryBindingSelect,
>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformRegistryBindingFilter, PlatformRegistryBindingOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
  }
): Promise<void>;
export async function prefetchPlatformRegistryBindingsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformRegistryBindingSelect,
      PlatformRegistryBindingFilter,
      PlatformRegistryBindingOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformRegistryBindingSelect,
    PlatformRegistryBindingFilter,
    PlatformRegistryBindingOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformRegistryBindingKeys.list(args),
    queryFn: () => getClient().platformRegistryBinding.findMany(args).unwrap(),
  });
}
