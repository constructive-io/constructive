/**
 * Grants that make a registry usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryGrantKeys } from '../query-keys';
import type { RegistryGrantSelect, RegistryGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistryGrantSelect, RegistryGrantWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const registryGrantQueryKey = registryGrantKeys.detail;
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRegistryGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRegistryGrantQuery<
  S extends RegistryGrantSelect,
  TData = {
    registryGrant: InferSelectResult<RegistryGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        registryGrant: InferSelectResult<RegistryGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRegistryGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<RegistryGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RegistryGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: registryGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .registryGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```ts
 * const data = await fetchRegistryGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRegistryGrantQuery<S extends RegistryGrantSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
}): Promise<{
  registryGrant: InferSelectResult<RegistryGrantWithRelations, S> | null;
}>;
export async function fetchRegistryGrantQuery(params: {
  id: string;
  selection: SelectionConfig<RegistryGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RegistryGrantSelect>(params.selection);
  return getClient()
    .registryGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```ts
 * await prefetchRegistryGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRegistryGrantQuery<S extends RegistryGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
  }
): Promise<void>;
export async function prefetchRegistryGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RegistryGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RegistryGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: registryGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .registryGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
