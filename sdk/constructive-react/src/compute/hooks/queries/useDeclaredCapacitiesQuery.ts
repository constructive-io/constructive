/**
 * List query hook for DeclaredCapacity
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { declaredCapacityKeys } from '../query-keys';
import type {
  DeclaredCapacitySelect,
  DeclaredCapacityWithRelations,
  DeclaredCapacityFilter,
  DeclaredCapacityOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  DeclaredCapacitySelect,
  DeclaredCapacityWithRelations,
  DeclaredCapacityFilter,
  DeclaredCapacityOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const declaredCapacitiesQueryKey = declaredCapacityKeys.list;
/**
 * Query hook for fetching DeclaredCapacity list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDeclaredCapacitiesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useDeclaredCapacitiesQuery<
  S extends DeclaredCapacitySelect,
  TData = {
    declaredCapacities: ConnectionResult<InferSelectResult<DeclaredCapacityWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DeclaredCapacityFilter, DeclaredCapacityOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DeclaredCapacitySelect>;
  } & Omit<
    UseQueryOptions<
      {
        declaredCapacities: ConnectionResult<InferSelectResult<DeclaredCapacityWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDeclaredCapacitiesQuery(
  params: {
    selection: ListSelectionConfig<
      DeclaredCapacitySelect,
      DeclaredCapacityFilter,
      DeclaredCapacityOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    DeclaredCapacitySelect,
    DeclaredCapacityFilter,
    DeclaredCapacityOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: declaredCapacityKeys.list(args),
    queryFn: () => getClient().declaredCapacity.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch DeclaredCapacity list without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDeclaredCapacitiesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchDeclaredCapacitiesQuery<S extends DeclaredCapacitySelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, DeclaredCapacityFilter, DeclaredCapacityOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, DeclaredCapacitySelect>;
}): Promise<{
  declaredCapacities: ConnectionResult<InferSelectResult<DeclaredCapacityWithRelations, S>>;
}>;
export async function fetchDeclaredCapacitiesQuery(params: {
  selection: ListSelectionConfig<
    DeclaredCapacitySelect,
    DeclaredCapacityFilter,
    DeclaredCapacityOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    DeclaredCapacitySelect,
    DeclaredCapacityFilter,
    DeclaredCapacityOrderBy
  >(params.selection);
  return getClient().declaredCapacity.findMany(args).unwrap();
}
/**
 * Prefetch DeclaredCapacity list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDeclaredCapacitiesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchDeclaredCapacitiesQuery<S extends DeclaredCapacitySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DeclaredCapacityFilter, DeclaredCapacityOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DeclaredCapacitySelect>;
  }
): Promise<void>;
export async function prefetchDeclaredCapacitiesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      DeclaredCapacitySelect,
      DeclaredCapacityFilter,
      DeclaredCapacityOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    DeclaredCapacitySelect,
    DeclaredCapacityFilter,
    DeclaredCapacityOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: declaredCapacityKeys.list(args),
    queryFn: () => getClient().declaredCapacity.findMany(args).unwrap(),
  });
}
