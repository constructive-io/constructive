/**
 * Content-addressed Merkle tree objects keyed by UUID v5 hash of data + children
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseGraphObjectKeys } from '../query-keys';
import type {
  DatabaseGraphObjectSelect,
  DatabaseGraphObjectWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DatabaseGraphObjectSelect,
  DatabaseGraphObjectWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseGraphGetNodeAtPathQueryKey = databaseGraphObjectKeys.detail;
/**
 * Content-addressed Merkle tree objects keyed by UUID v5 hash of data + children
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseGraphObjectQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDatabaseGraphObjectQuery<
  S extends DatabaseGraphObjectSelect,
  TData = {
    databaseGraphGetNodeAtPath: InferSelectResult<DatabaseGraphObjectWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphObjectSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseGraphGetNodeAtPath: InferSelectResult<DatabaseGraphObjectWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseGraphObjectQuery(
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphObjectSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DatabaseGraphObjectSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseGraphObjectKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphObject.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Content-addressed Merkle tree objects keyed by UUID v5 hash of data + children
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseGraphObjectQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDatabaseGraphObjectQuery<S extends DatabaseGraphObjectSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DatabaseGraphObjectSelect>;
}): Promise<{
  databaseGraphGetNodeAtPath: InferSelectResult<DatabaseGraphObjectWithRelations, S> | null;
}>;
export async function fetchDatabaseGraphObjectQuery(params: {
  id: string;
  selection: SelectionConfig<DatabaseGraphObjectSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DatabaseGraphObjectSelect>(params.selection);
  return getClient()
    .databaseGraphObject.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Content-addressed Merkle tree objects keyed by UUID v5 hash of data + children
 *
 * @example
 * ```ts
 * await prefetchDatabaseGraphObjectQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDatabaseGraphObjectQuery<S extends DatabaseGraphObjectSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphObjectSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseGraphObjectQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphObjectSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DatabaseGraphObjectSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseGraphObjectKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphObject.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
