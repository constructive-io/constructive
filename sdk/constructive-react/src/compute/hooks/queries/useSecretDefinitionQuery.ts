/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { secretDefinitionKeys } from '../query-keys';
import type { SecretDefinitionSelect, SecretDefinitionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SecretDefinitionSelect, SecretDefinitionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const secretDefinitionQueryKey = secretDefinitionKeys.detail;
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSecretDefinitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useSecretDefinitionQuery<
  S extends SecretDefinitionSelect,
  TData = {
    secretDefinition: InferSelectResult<SecretDefinitionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        secretDefinition: InferSelectResult<SecretDefinitionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSecretDefinitionQuery(
  params: {
    id: string;
    selection: SelectionConfig<SecretDefinitionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<SecretDefinitionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: secretDefinitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .secretDefinition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```ts
 * const data = await fetchSecretDefinitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchSecretDefinitionQuery<S extends SecretDefinitionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
}): Promise<{
  secretDefinition: InferSelectResult<SecretDefinitionWithRelations, S> | null;
}>;
export async function fetchSecretDefinitionQuery(params: {
  id: string;
  selection: SelectionConfig<SecretDefinitionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<SecretDefinitionSelect>(params.selection);
  return getClient()
    .secretDefinition.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```ts
 * await prefetchSecretDefinitionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchSecretDefinitionQuery<S extends SecretDefinitionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
  }
): Promise<void>;
export async function prefetchSecretDefinitionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<SecretDefinitionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<SecretDefinitionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: secretDefinitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .secretDefinition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
