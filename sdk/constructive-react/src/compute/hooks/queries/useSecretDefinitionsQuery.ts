/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { secretDefinitionKeys } from '../query-keys';
import type {
  SecretDefinitionSelect,
  SecretDefinitionWithRelations,
  SecretDefinitionFilter,
  SecretDefinitionOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  SecretDefinitionSelect,
  SecretDefinitionWithRelations,
  SecretDefinitionFilter,
  SecretDefinitionOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const secretDefinitionsQueryKey = secretDefinitionKeys.list;
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useSecretDefinitionsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useSecretDefinitionsQuery<
  S extends SecretDefinitionSelect,
  TData = {
    secretDefinitions: ConnectionResult<InferSelectResult<SecretDefinitionWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SecretDefinitionFilter, SecretDefinitionOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        secretDefinitions: ConnectionResult<InferSelectResult<SecretDefinitionWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useSecretDefinitionsQuery(
  params: {
    selection: ListSelectionConfig<
      SecretDefinitionSelect,
      SecretDefinitionFilter,
      SecretDefinitionOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    SecretDefinitionSelect,
    SecretDefinitionFilter,
    SecretDefinitionOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: secretDefinitionKeys.list(args),
    queryFn: () => getClient().secretDefinition.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```ts
 * const data = await fetchSecretDefinitionsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchSecretDefinitionsQuery<S extends SecretDefinitionSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, SecretDefinitionFilter, SecretDefinitionOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
}): Promise<{
  secretDefinitions: ConnectionResult<InferSelectResult<SecretDefinitionWithRelations, S>>;
}>;
export async function fetchSecretDefinitionsQuery(params: {
  selection: ListSelectionConfig<
    SecretDefinitionSelect,
    SecretDefinitionFilter,
    SecretDefinitionOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    SecretDefinitionSelect,
    SecretDefinitionFilter,
    SecretDefinitionOrderBy
  >(params.selection);
  return getClient().secretDefinition.findMany(args).unwrap();
}
/**
 * Global secret name registry — declares which secrets the platform recognizes. Actual values live in app_secrets.
 *
 * @example
 * ```ts
 * await prefetchSecretDefinitionsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchSecretDefinitionsQuery<S extends SecretDefinitionSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, SecretDefinitionFilter, SecretDefinitionOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, SecretDefinitionSelect>;
  }
): Promise<void>;
export async function prefetchSecretDefinitionsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      SecretDefinitionSelect,
      SecretDefinitionFilter,
      SecretDefinitionOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    SecretDefinitionSelect,
    SecretDefinitionFilter,
    SecretDefinitionOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: secretDefinitionKeys.list(args),
    queryFn: () => getClient().secretDefinition.findMany(args).unwrap(),
  });
}
