/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformBuildKeys } from '../query-keys';
import type { PlatformBuildSelect, PlatformBuildWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformBuildSelect, PlatformBuildWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBuildQueryKey = platformBuildKeys.detail;
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBuildQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformBuildQuery<
  S extends PlatformBuildSelect,
  TData = {
    platformBuild: InferSelectResult<PlatformBuildWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBuildSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBuild: InferSelectResult<PlatformBuildWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBuildQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformBuildSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformBuildSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBuildKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBuild.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBuildQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformBuildQuery<S extends PlatformBuildSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformBuildSelect>;
}): Promise<{
  platformBuild: InferSelectResult<PlatformBuildWithRelations, S> | null;
}>;
export async function fetchPlatformBuildQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformBuildSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformBuildSelect>(params.selection);
  return getClient()
    .platformBuild.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * await prefetchPlatformBuildQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformBuildQuery<S extends PlatformBuildSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBuildSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBuildQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformBuildSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformBuildSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBuildKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBuild.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
