/**
 * Emoji reactions to a local proposal or one of its comments
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { proposalReactionKeys } from '../query-keys';
import type { ProposalReactionSelect, ProposalReactionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ProposalReactionSelect, ProposalReactionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const proposalReactionQueryKey = proposalReactionKeys.detail;
/**
 * Emoji reactions to a local proposal or one of its comments
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useProposalReactionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useProposalReactionQuery<
  S extends ProposalReactionSelect,
  TData = {
    proposalReaction: InferSelectResult<ProposalReactionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ProposalReactionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        proposalReaction: InferSelectResult<ProposalReactionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useProposalReactionQuery(
  params: {
    id: string;
    selection: SelectionConfig<ProposalReactionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ProposalReactionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: proposalReactionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .proposalReaction.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Emoji reactions to a local proposal or one of its comments
 *
 * @example
 * ```ts
 * const data = await fetchProposalReactionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchProposalReactionQuery<S extends ProposalReactionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ProposalReactionSelect>;
}): Promise<{
  proposalReaction: InferSelectResult<ProposalReactionWithRelations, S> | null;
}>;
export async function fetchProposalReactionQuery(params: {
  id: string;
  selection: SelectionConfig<ProposalReactionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ProposalReactionSelect>(params.selection);
  return getClient()
    .proposalReaction.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Emoji reactions to a local proposal or one of its comments
 *
 * @example
 * ```ts
 * await prefetchProposalReactionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchProposalReactionQuery<S extends ProposalReactionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ProposalReactionSelect>;
  }
): Promise<void>;
export async function prefetchProposalReactionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ProposalReactionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ProposalReactionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: proposalReactionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .proposalReaction.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
