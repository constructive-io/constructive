/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { registryKeys } from '../query-keys';
import type {
  RegistrySelect,
  RegistryWithRelations,
  RegistryFilter,
  RegistryOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  RegistrySelect,
  RegistryWithRelations,
  RegistryFilter,
  RegistryOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const registriesQueryKey = registryKeys.list;
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRegistriesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useRegistriesQuery<
  S extends RegistrySelect,
  TData = {
    registries: ConnectionResult<InferSelectResult<RegistryWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RegistryFilter, RegistryOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RegistrySelect>;
  } & Omit<
    UseQueryOptions<
      {
        registries: ConnectionResult<InferSelectResult<RegistryWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRegistriesQuery(
  params: {
    selection: ListSelectionConfig<RegistrySelect, RegistryFilter, RegistryOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<RegistrySelect, RegistryFilter, RegistryOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: registryKeys.list(args),
    queryFn: () => getClient().registry.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```ts
 * const data = await fetchRegistriesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchRegistriesQuery<S extends RegistrySelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, RegistryFilter, RegistryOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, RegistrySelect>;
}): Promise<{
  registries: ConnectionResult<InferSelectResult<RegistryWithRelations, S>>;
}>;
export async function fetchRegistriesQuery(params: {
  selection: ListSelectionConfig<RegistrySelect, RegistryFilter, RegistryOrderBy>;
}) {
  const args = buildListSelectionArgs<RegistrySelect, RegistryFilter, RegistryOrderBy>(
    params.selection
  );
  return getClient().registry.findMany(args).unwrap();
}
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```ts
 * await prefetchRegistriesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchRegistriesQuery<S extends RegistrySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RegistryFilter, RegistryOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RegistrySelect>;
  }
): Promise<void>;
export async function prefetchRegistriesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<RegistrySelect, RegistryFilter, RegistryOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<RegistrySelect, RegistryFilter, RegistryOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: registryKeys.list(args),
    queryFn: () => getClient().registry.findMany(args).unwrap(),
  });
}
