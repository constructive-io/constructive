/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformWebhookEndpointKeys } from '../query-keys';
import type {
  PlatformWebhookEndpointSelect,
  PlatformWebhookEndpointWithRelations,
  PlatformWebhookEndpointFilter,
  PlatformWebhookEndpointOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformWebhookEndpointSelect,
  PlatformWebhookEndpointWithRelations,
  PlatformWebhookEndpointFilter,
  PlatformWebhookEndpointOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformWebhookEndpointsQueryKey = platformWebhookEndpointKeys.list;
/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformWebhookEndpointsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformWebhookEndpointsQuery<
  S extends PlatformWebhookEndpointSelect,
  TData = {
    platformWebhookEndpoints: ConnectionResult<
      InferSelectResult<PlatformWebhookEndpointWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformWebhookEndpointFilter, PlatformWebhookEndpointOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformWebhookEndpointSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformWebhookEndpoints: ConnectionResult<
          InferSelectResult<PlatformWebhookEndpointWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformWebhookEndpointsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformWebhookEndpointSelect,
      PlatformWebhookEndpointFilter,
      PlatformWebhookEndpointOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformWebhookEndpointSelect,
    PlatformWebhookEndpointFilter,
    PlatformWebhookEndpointOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformWebhookEndpointKeys.list(args),
    queryFn: () => getClient().platformWebhookEndpoint.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 *
 * @example
 * ```ts
 * const data = await fetchPlatformWebhookEndpointsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformWebhookEndpointsQuery<
  S extends PlatformWebhookEndpointSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformWebhookEndpointFilter, PlatformWebhookEndpointOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformWebhookEndpointSelect>;
}): Promise<{
  platformWebhookEndpoints: ConnectionResult<
    InferSelectResult<PlatformWebhookEndpointWithRelations, S>
  >;
}>;
export async function fetchPlatformWebhookEndpointsQuery(params: {
  selection: ListSelectionConfig<
    PlatformWebhookEndpointSelect,
    PlatformWebhookEndpointFilter,
    PlatformWebhookEndpointOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformWebhookEndpointSelect,
    PlatformWebhookEndpointFilter,
    PlatformWebhookEndpointOrderBy
  >(params.selection);
  return getClient().platformWebhookEndpoint.findMany(args).unwrap();
}
/**
 * Webhook route authority: (host, path) -> function task_identifier invoked through the webhook channel, with provider, signing-secret reference, and replay window
 *
 * @example
 * ```ts
 * await prefetchPlatformWebhookEndpointsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformWebhookEndpointsQuery<
  S extends PlatformWebhookEndpointSelect,
>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformWebhookEndpointFilter, PlatformWebhookEndpointOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformWebhookEndpointSelect>;
  }
): Promise<void>;
export async function prefetchPlatformWebhookEndpointsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformWebhookEndpointSelect,
      PlatformWebhookEndpointFilter,
      PlatformWebhookEndpointOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformWebhookEndpointSelect,
    PlatformWebhookEndpointFilter,
    PlatformWebhookEndpointOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformWebhookEndpointKeys.list(args),
    queryFn: () => getClient().platformWebhookEndpoint.findMany(args).unwrap(),
  });
}
