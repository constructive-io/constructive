/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryKeys } from '../query-keys';
import type { RegistrySelect, RegistryWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistrySelect, RegistryWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const registryQueryKey = registryKeys.detail;
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRegistryQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRegistryQuery<
  S extends RegistrySelect,
  TData = {
    registry: InferSelectResult<RegistryWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RegistrySelect>;
  } & Omit<
    UseQueryOptions<
      {
        registry: InferSelectResult<RegistryWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRegistryQuery(
  params: {
    id: string;
    selection: SelectionConfig<RegistrySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RegistrySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: registryKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .registry.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```ts
 * const data = await fetchRegistryQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRegistryQuery<S extends RegistrySelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RegistrySelect>;
}): Promise<{
  registry: InferSelectResult<RegistryWithRelations, S> | null;
}>;
export async function fetchRegistryQuery(params: {
  id: string;
  selection: SelectionConfig<RegistrySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RegistrySelect>(params.selection);
  return getClient()
    .registry.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```ts
 * await prefetchRegistryQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRegistryQuery<S extends RegistrySelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RegistrySelect>;
  }
): Promise<void>;
export async function prefetchRegistryQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RegistrySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RegistrySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: registryKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .registry.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
