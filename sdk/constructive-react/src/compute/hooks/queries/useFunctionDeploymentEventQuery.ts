/**
 * Deployment lifecycle events — audit log of provisioning, scaling, and failure events
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionDeploymentEventKeys } from '../query-keys';
import type {
  FunctionDeploymentEventSelect,
  FunctionDeploymentEventWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  FunctionDeploymentEventSelect,
  FunctionDeploymentEventWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const functionDeploymentEventQueryKey = functionDeploymentEventKeys.detail;
/**
 * Deployment lifecycle events — audit log of provisioning, scaling, and failure events
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFunctionDeploymentEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFunctionDeploymentEventQuery<
  S extends FunctionDeploymentEventSelect,
  TData = {
    functionDeploymentEvent: InferSelectResult<FunctionDeploymentEventWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionDeploymentEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        functionDeploymentEvent: InferSelectResult<FunctionDeploymentEventWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFunctionDeploymentEventQuery(
  params: {
    id: string;
    selection: SelectionConfig<FunctionDeploymentEventSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FunctionDeploymentEventSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: functionDeploymentEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionDeploymentEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Deployment lifecycle events — audit log of provisioning, scaling, and failure events
 *
 * @example
 * ```ts
 * const data = await fetchFunctionDeploymentEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFunctionDeploymentEventQuery<
  S extends FunctionDeploymentEventSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FunctionDeploymentEventSelect>;
}): Promise<{
  functionDeploymentEvent: InferSelectResult<FunctionDeploymentEventWithRelations, S> | null;
}>;
export async function fetchFunctionDeploymentEventQuery(params: {
  id: string;
  selection: SelectionConfig<FunctionDeploymentEventSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<FunctionDeploymentEventSelect>(params.selection);
  return getClient()
    .functionDeploymentEvent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Deployment lifecycle events — audit log of provisioning, scaling, and failure events
 *
 * @example
 * ```ts
 * await prefetchFunctionDeploymentEventQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFunctionDeploymentEventQuery<S extends FunctionDeploymentEventSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionDeploymentEventSelect>;
  }
): Promise<void>;
export async function prefetchFunctionDeploymentEventQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FunctionDeploymentEventSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FunctionDeploymentEventSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: functionDeploymentEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionDeploymentEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
