/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformBuilderBindingKeys } from '../query-keys';
import type {
  PlatformBuilderBindingSelect,
  PlatformBuilderBindingWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformBuilderBindingSelect,
  PlatformBuilderBindingWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBuilderBindingQueryKey = platformBuilderBindingKeys.detail;
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBuilderBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformBuilderBindingQuery<
  S extends PlatformBuilderBindingSelect,
  TData = {
    platformBuilderBinding: InferSelectResult<PlatformBuilderBindingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBuilderBinding: InferSelectResult<PlatformBuilderBindingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBuilderBindingQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformBuilderBindingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformBuilderBindingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBuilderBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBuilderBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBuilderBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformBuilderBindingQuery<
  S extends PlatformBuilderBindingSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
}): Promise<{
  platformBuilderBinding: InferSelectResult<PlatformBuilderBindingWithRelations, S> | null;
}>;
export async function fetchPlatformBuilderBindingQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformBuilderBindingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformBuilderBindingSelect>(params.selection);
  return getClient()
    .platformBuilderBinding.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```ts
 * await prefetchPlatformBuilderBindingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformBuilderBindingQuery<S extends PlatformBuilderBindingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBuilderBindingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformBuilderBindingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformBuilderBindingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBuilderBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBuilderBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
