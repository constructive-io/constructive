/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { namespaceKeys } from '../query-keys';
import type {
  NamespaceSelect,
  NamespaceWithRelations,
  NamespaceFilter,
  NamespaceOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  NamespaceSelect,
  NamespaceWithRelations,
  NamespaceFilter,
  NamespaceOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const namespacesQueryKey = namespaceKeys.list;
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useNamespacesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useNamespacesQuery<
  S extends NamespaceSelect,
  TData = {
    namespaces: ConnectionResult<InferSelectResult<NamespaceWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, NamespaceFilter, NamespaceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, NamespaceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        namespaces: ConnectionResult<InferSelectResult<NamespaceWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useNamespacesQuery(
  params: {
    selection: ListSelectionConfig<NamespaceSelect, NamespaceFilter, NamespaceOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<NamespaceSelect, NamespaceFilter, NamespaceOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: namespaceKeys.list(args),
    queryFn: () => getClient().namespace.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```ts
 * const data = await fetchNamespacesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchNamespacesQuery<S extends NamespaceSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, NamespaceFilter, NamespaceOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, NamespaceSelect>;
}): Promise<{
  namespaces: ConnectionResult<InferSelectResult<NamespaceWithRelations, S>>;
}>;
export async function fetchNamespacesQuery(params: {
  selection: ListSelectionConfig<NamespaceSelect, NamespaceFilter, NamespaceOrderBy>;
}) {
  const args = buildListSelectionArgs<NamespaceSelect, NamespaceFilter, NamespaceOrderBy>(
    params.selection
  );
  return getClient().namespace.findMany(args).unwrap();
}
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```ts
 * await prefetchNamespacesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchNamespacesQuery<S extends NamespaceSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, NamespaceFilter, NamespaceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, NamespaceSelect>;
  }
): Promise<void>;
export async function prefetchNamespacesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<NamespaceSelect, NamespaceFilter, NamespaceOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<NamespaceSelect, NamespaceFilter, NamespaceOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: namespaceKeys.list(args),
    queryFn: () => getClient().namespace.findMany(args).unwrap(),
  });
}
