/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { databaseGraphStoreKeys } from '../query-keys';
import type {
  DatabaseGraphStoreSelect,
  DatabaseGraphStoreWithRelations,
  DatabaseGraphStoreFilter,
  DatabaseGraphStoreOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  DatabaseGraphStoreSelect,
  DatabaseGraphStoreWithRelations,
  DatabaseGraphStoreFilter,
  DatabaseGraphStoreOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseGraphStoresQueryKey = databaseGraphStoreKeys.list;
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseGraphStoresQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useDatabaseGraphStoresQuery<
  S extends DatabaseGraphStoreSelect,
  TData = {
    databaseGraphStores: ConnectionResult<InferSelectResult<DatabaseGraphStoreWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, DatabaseGraphStoreFilter, DatabaseGraphStoreOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, DatabaseGraphStoreSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseGraphStores: ConnectionResult<
          InferSelectResult<DatabaseGraphStoreWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseGraphStoresQuery(
  params: {
    selection: ListSelectionConfig<
      DatabaseGraphStoreSelect,
      DatabaseGraphStoreFilter,
      DatabaseGraphStoreOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    DatabaseGraphStoreSelect,
    DatabaseGraphStoreFilter,
    DatabaseGraphStoreOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseGraphStoreKeys.list(args),
    queryFn: () => getClient().databaseGraphStore.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseGraphStoresQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchDatabaseGraphStoresQuery<S extends DatabaseGraphStoreSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, DatabaseGraphStoreFilter, DatabaseGraphStoreOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, DatabaseGraphStoreSelect>;
}): Promise<{
  databaseGraphStores: ConnectionResult<InferSelectResult<DatabaseGraphStoreWithRelations, S>>;
}>;
export async function fetchDatabaseGraphStoresQuery(params: {
  selection: ListSelectionConfig<
    DatabaseGraphStoreSelect,
    DatabaseGraphStoreFilter,
    DatabaseGraphStoreOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    DatabaseGraphStoreSelect,
    DatabaseGraphStoreFilter,
    DatabaseGraphStoreOrderBy
  >(params.selection);
  return getClient().databaseGraphStore.findMany(args).unwrap();
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * await prefetchDatabaseGraphStoresQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchDatabaseGraphStoresQuery<S extends DatabaseGraphStoreSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, DatabaseGraphStoreFilter, DatabaseGraphStoreOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, DatabaseGraphStoreSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseGraphStoresQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      DatabaseGraphStoreSelect,
      DatabaseGraphStoreFilter,
      DatabaseGraphStoreOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    DatabaseGraphStoreSelect,
    DatabaseGraphStoreFilter,
    DatabaseGraphStoreOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseGraphStoreKeys.list(args),
    queryFn: () => getClient().databaseGraphStore.findMany(args).unwrap(),
  });
}
