/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { registryBindingKeys } from '../query-keys';
import type {
  RegistryBindingSelect,
  RegistryBindingWithRelations,
  RegistryBindingFilter,
  RegistryBindingOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  RegistryBindingSelect,
  RegistryBindingWithRelations,
  RegistryBindingFilter,
  RegistryBindingOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const registryBindingsQueryKey = registryBindingKeys.list;
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRegistryBindingsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useRegistryBindingsQuery<
  S extends RegistryBindingSelect,
  TData = {
    registryBindings: ConnectionResult<InferSelectResult<RegistryBindingWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RegistryBindingFilter, RegistryBindingOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        registryBindings: ConnectionResult<InferSelectResult<RegistryBindingWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRegistryBindingsQuery(
  params: {
    selection: ListSelectionConfig<
      RegistryBindingSelect,
      RegistryBindingFilter,
      RegistryBindingOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    RegistryBindingSelect,
    RegistryBindingFilter,
    RegistryBindingOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: registryBindingKeys.list(args),
    queryFn: () => getClient().registryBinding.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * const data = await fetchRegistryBindingsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchRegistryBindingsQuery<S extends RegistryBindingSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, RegistryBindingFilter, RegistryBindingOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
}): Promise<{
  registryBindings: ConnectionResult<InferSelectResult<RegistryBindingWithRelations, S>>;
}>;
export async function fetchRegistryBindingsQuery(params: {
  selection: ListSelectionConfig<
    RegistryBindingSelect,
    RegistryBindingFilter,
    RegistryBindingOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    RegistryBindingSelect,
    RegistryBindingFilter,
    RegistryBindingOrderBy
  >(params.selection);
  return getClient().registryBinding.findMany(args).unwrap();
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * await prefetchRegistryBindingsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchRegistryBindingsQuery<S extends RegistryBindingSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RegistryBindingFilter, RegistryBindingOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
  }
): Promise<void>;
export async function prefetchRegistryBindingsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      RegistryBindingSelect,
      RegistryBindingFilter,
      RegistryBindingOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    RegistryBindingSelect,
    RegistryBindingFilter,
    RegistryBindingOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: registryBindingKeys.list(args),
    queryFn: () => getClient().registryBinding.findMany(args).unwrap(),
  });
}
