/**
 * Container image catalog: images available to run as functions, resources, and builds
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformImageKeys } from '../query-keys';
import type {
  PlatformImageSelect,
  PlatformImageWithRelations,
  PlatformImageFilter,
  PlatformImageOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformImageSelect,
  PlatformImageWithRelations,
  PlatformImageFilter,
  PlatformImageOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformImagesQueryKey = platformImageKeys.list;
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformImagesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformImagesQuery<
  S extends PlatformImageSelect,
  TData = {
    platformImages: ConnectionResult<InferSelectResult<PlatformImageWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformImageFilter, PlatformImageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformImages: ConnectionResult<InferSelectResult<PlatformImageWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformImagesQuery(
  params: {
    selection: ListSelectionConfig<PlatformImageSelect, PlatformImageFilter, PlatformImageOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformImageSelect,
    PlatformImageFilter,
    PlatformImageOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformImageKeys.list(args),
    queryFn: () => getClient().platformImage.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * const data = await fetchPlatformImagesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformImagesQuery<S extends PlatformImageSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlatformImageFilter, PlatformImageOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
}): Promise<{
  platformImages: ConnectionResult<InferSelectResult<PlatformImageWithRelations, S>>;
}>;
export async function fetchPlatformImagesQuery(params: {
  selection: ListSelectionConfig<PlatformImageSelect, PlatformImageFilter, PlatformImageOrderBy>;
}) {
  const args = buildListSelectionArgs<
    PlatformImageSelect,
    PlatformImageFilter,
    PlatformImageOrderBy
  >(params.selection);
  return getClient().platformImage.findMany(args).unwrap();
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * await prefetchPlatformImagesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformImagesQuery<S extends PlatformImageSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformImageFilter, PlatformImageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformImageSelect>;
  }
): Promise<void>;
export async function prefetchPlatformImagesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<PlatformImageSelect, PlatformImageFilter, PlatformImageOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformImageSelect,
    PlatformImageFilter,
    PlatformImageOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformImageKeys.list(args),
    queryFn: () => getClient().platformImage.findMany(args).unwrap(),
  });
}
