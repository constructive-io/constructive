/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { buildKeys } from '../query-keys';
import type { BuildSelect, BuildWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuildSelect, BuildWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const buildQueryKey = buildKeys.detail;
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBuildQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useBuildQuery<
  S extends BuildSelect,
  TData = {
    build: InferSelectResult<BuildWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BuildSelect>;
  } & Omit<
    UseQueryOptions<
      {
        build: InferSelectResult<BuildWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBuildQuery(
  params: {
    id: string;
    selection: SelectionConfig<BuildSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<BuildSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: buildKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .build.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * const data = await fetchBuildQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchBuildQuery<S extends BuildSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, BuildSelect>;
}): Promise<{
  build: InferSelectResult<BuildWithRelations, S> | null;
}>;
export async function fetchBuildQuery(params: {
  id: string;
  selection: SelectionConfig<BuildSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<BuildSelect>(params.selection);
  return getClient()
    .build.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * await prefetchBuildQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchBuildQuery<S extends BuildSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BuildSelect>;
  }
): Promise<void>;
export async function prefetchBuildQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<BuildSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<BuildSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: buildKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .build.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
