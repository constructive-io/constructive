/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformWebhookEventKeys } from '../query-keys';
import type {
  PlatformWebhookEventSelect,
  PlatformWebhookEventWithRelations,
  PlatformWebhookEventFilter,
  PlatformWebhookEventOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformWebhookEventSelect,
  PlatformWebhookEventWithRelations,
  PlatformWebhookEventFilter,
  PlatformWebhookEventOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformWebhookEventsQueryKey = platformWebhookEventKeys.list;
/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformWebhookEventsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformWebhookEventsQuery<
  S extends PlatformWebhookEventSelect,
  TData = {
    platformWebhookEvents: ConnectionResult<
      InferSelectResult<PlatformWebhookEventWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformWebhookEventFilter, PlatformWebhookEventOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformWebhookEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformWebhookEvents: ConnectionResult<
          InferSelectResult<PlatformWebhookEventWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformWebhookEventsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformWebhookEventSelect,
      PlatformWebhookEventFilter,
      PlatformWebhookEventOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformWebhookEventSelect,
    PlatformWebhookEventFilter,
    PlatformWebhookEventOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformWebhookEventKeys.list(args),
    queryFn: () => getClient().platformWebhookEvent.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 *
 * @example
 * ```ts
 * const data = await fetchPlatformWebhookEventsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformWebhookEventsQuery<
  S extends PlatformWebhookEventSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformWebhookEventFilter, PlatformWebhookEventOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformWebhookEventSelect>;
}): Promise<{
  platformWebhookEvents: ConnectionResult<InferSelectResult<PlatformWebhookEventWithRelations, S>>;
}>;
export async function fetchPlatformWebhookEventsQuery(params: {
  selection: ListSelectionConfig<
    PlatformWebhookEventSelect,
    PlatformWebhookEventFilter,
    PlatformWebhookEventOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformWebhookEventSelect,
    PlatformWebhookEventFilter,
    PlatformWebhookEventOrderBy
  >(params.selection);
  return getClient().platformWebhookEvent.findMany(args).unwrap();
}
/**
 * Durable webhook acceptance log — one row per accepted delivery, deduplicated on (endpoint_id, external_event_id), linked to the pending function invocation it enqueued
 *
 * @example
 * ```ts
 * await prefetchPlatformWebhookEventsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformWebhookEventsQuery<S extends PlatformWebhookEventSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformWebhookEventFilter, PlatformWebhookEventOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformWebhookEventSelect>;
  }
): Promise<void>;
export async function prefetchPlatformWebhookEventsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformWebhookEventSelect,
      PlatformWebhookEventFilter,
      PlatformWebhookEventOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformWebhookEventSelect,
    PlatformWebhookEventFilter,
    PlatformWebhookEventOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformWebhookEventKeys.list(args),
    queryFn: () => getClient().platformWebhookEvent.findMany(args).unwrap(),
  });
}
