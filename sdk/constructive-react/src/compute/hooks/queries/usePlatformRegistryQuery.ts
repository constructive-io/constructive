/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRegistryKeys } from '../query-keys';
import type { PlatformRegistrySelect, PlatformRegistryWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlatformRegistrySelect, PlatformRegistryWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformRegistryQueryKey = platformRegistryKeys.detail;
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformRegistryQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformRegistryQuery<
  S extends PlatformRegistrySelect,
  TData = {
    platformRegistry: InferSelectResult<PlatformRegistryWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistrySelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformRegistry: InferSelectResult<PlatformRegistryWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformRegistryQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformRegistrySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformRegistrySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformRegistryKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformRegistry.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```ts
 * const data = await fetchPlatformRegistryQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformRegistryQuery<S extends PlatformRegistrySelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformRegistrySelect>;
}): Promise<{
  platformRegistry: InferSelectResult<PlatformRegistryWithRelations, S> | null;
}>;
export async function fetchPlatformRegistryQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformRegistrySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformRegistrySelect>(params.selection);
  return getClient()
    .platformRegistry.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Artifact registries this scope pulls from or pushes to (OCI images, npm packages), platform-run or external
 *
 * @example
 * ```ts
 * await prefetchPlatformRegistryQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformRegistryQuery<S extends PlatformRegistrySelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistrySelect>;
  }
): Promise<void>;
export async function prefetchPlatformRegistryQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformRegistrySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformRegistrySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformRegistryKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformRegistry.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
