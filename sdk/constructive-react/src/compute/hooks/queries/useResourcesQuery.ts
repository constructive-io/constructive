/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { resourceKeys } from '../query-keys';
import type {
  ResourceSelect,
  ResourceWithRelations,
  ResourceFilter,
  ResourceOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  ResourceSelect,
  ResourceWithRelations,
  ResourceFilter,
  ResourceOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const resourcesQueryKey = resourceKeys.list;
/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useResourcesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useResourcesQuery<
  S extends ResourceSelect,
  TData = {
    resources: ConnectionResult<InferSelectResult<ResourceWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ResourceFilter, ResourceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ResourceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        resources: ConnectionResult<InferSelectResult<ResourceWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useResourcesQuery(
  params: {
    selection: ListSelectionConfig<ResourceSelect, ResourceFilter, ResourceOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<ResourceSelect, ResourceFilter, ResourceOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: resourceKeys.list(args),
    queryFn: () => getClient().resource.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 *
 * @example
 * ```ts
 * const data = await fetchResourcesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchResourcesQuery<S extends ResourceSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, ResourceFilter, ResourceOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, ResourceSelect>;
}): Promise<{
  resources: ConnectionResult<InferSelectResult<ResourceWithRelations, S>>;
}>;
export async function fetchResourcesQuery(params: {
  selection: ListSelectionConfig<ResourceSelect, ResourceFilter, ResourceOrderBy>;
}) {
  const args = buildListSelectionArgs<ResourceSelect, ResourceFilter, ResourceOrderBy>(
    params.selection
  );
  return getClient().resource.findMany(args).unwrap();
}
/**
 * Unified K8s resource declarations — stores desired state (spec) and observed state (status) for all resource kinds within a namespace
 *
 * @example
 * ```ts
 * await prefetchResourcesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchResourcesQuery<S extends ResourceSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ResourceFilter, ResourceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ResourceSelect>;
  }
): Promise<void>;
export async function prefetchResourcesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<ResourceSelect, ResourceFilter, ResourceOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<ResourceSelect, ResourceFilter, ResourceOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: resourceKeys.list(args),
    queryFn: () => getClient().resource.findMany(args).unwrap(),
  });
}
