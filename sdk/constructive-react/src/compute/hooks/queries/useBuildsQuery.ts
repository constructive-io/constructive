/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { buildKeys } from '../query-keys';
import type {
  BuildSelect,
  BuildWithRelations,
  BuildFilter,
  BuildOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  BuildSelect,
  BuildWithRelations,
  BuildFilter,
  BuildOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const buildsQueryKey = buildKeys.list;
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBuildsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useBuildsQuery<
  S extends BuildSelect,
  TData = {
    builds: ConnectionResult<InferSelectResult<BuildWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, BuildFilter, BuildOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, BuildSelect>;
  } & Omit<
    UseQueryOptions<
      {
        builds: ConnectionResult<InferSelectResult<BuildWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBuildsQuery(
  params: {
    selection: ListSelectionConfig<BuildSelect, BuildFilter, BuildOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<BuildSelect, BuildFilter, BuildOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: buildKeys.list(args),
    queryFn: () => getClient().build.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * const data = await fetchBuildsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchBuildsQuery<S extends BuildSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, BuildFilter, BuildOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, BuildSelect>;
}): Promise<{
  builds: ConnectionResult<InferSelectResult<BuildWithRelations, S>>;
}>;
export async function fetchBuildsQuery(params: {
  selection: ListSelectionConfig<BuildSelect, BuildFilter, BuildOrderBy>;
}) {
  const args = buildListSelectionArgs<BuildSelect, BuildFilter, BuildOrderBy>(params.selection);
  return getClient().build.findMany(args).unwrap();
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * await prefetchBuildsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchBuildsQuery<S extends BuildSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, BuildFilter, BuildOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, BuildSelect>;
  }
): Promise<void>;
export async function prefetchBuildsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<BuildSelect, BuildFilter, BuildOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<BuildSelect, BuildFilter, BuildOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: buildKeys.list(args),
    queryFn: () => getClient().build.findMany(args).unwrap(),
  });
}
