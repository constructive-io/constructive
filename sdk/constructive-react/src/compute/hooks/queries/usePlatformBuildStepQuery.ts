/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformBuildStepKeys } from '../query-keys';
import type {
  PlatformBuildStepSelect,
  PlatformBuildStepWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformBuildStepSelect,
  PlatformBuildStepWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBuildStepQueryKey = platformBuildStepKeys.detail;
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBuildStepQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformBuildStepQuery<
  S extends PlatformBuildStepSelect,
  TData = {
    platformBuildStep: InferSelectResult<PlatformBuildStepWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBuildStep: InferSelectResult<PlatformBuildStepWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBuildStepQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformBuildStepSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformBuildStepSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBuildStepKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBuildStep.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBuildStepQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformBuildStepQuery<S extends PlatformBuildStepSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
}): Promise<{
  platformBuildStep: InferSelectResult<PlatformBuildStepWithRelations, S> | null;
}>;
export async function fetchPlatformBuildStepQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformBuildStepSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformBuildStepSelect>(params.selection);
  return getClient()
    .platformBuildStep.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```ts
 * await prefetchPlatformBuildStepQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformBuildStepQuery<S extends PlatformBuildStepSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBuildStepQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformBuildStepSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformBuildStepSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBuildStepKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformBuildStep.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
