/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { infraCommitKeys } from '../query-keys';
import type { InfraCommitSelect, InfraCommitWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { InfraCommitSelect, InfraCommitWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const infraCommitQueryKey = infraCommitKeys.detail;
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useInfraCommitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useInfraCommitQuery<
  S extends InfraCommitSelect,
  TData = {
    infraCommit: InferSelectResult<InfraCommitWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InfraCommitSelect>;
  } & Omit<
    UseQueryOptions<
      {
        infraCommit: InferSelectResult<InfraCommitWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useInfraCommitQuery(
  params: {
    id: string;
    selection: SelectionConfig<InfraCommitSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<InfraCommitSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: infraCommitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .infraCommit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```ts
 * const data = await fetchInfraCommitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchInfraCommitQuery<S extends InfraCommitSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, InfraCommitSelect>;
}): Promise<{
  infraCommit: InferSelectResult<InfraCommitWithRelations, S> | null;
}>;
export async function fetchInfraCommitQuery(params: {
  id: string;
  selection: SelectionConfig<InfraCommitSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<InfraCommitSelect>(params.selection);
  return getClient()
    .infraCommit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```ts
 * await prefetchInfraCommitQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchInfraCommitQuery<S extends InfraCommitSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, InfraCommitSelect>;
  }
): Promise<void>;
export async function prefetchInfraCommitQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<InfraCommitSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<InfraCommitSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: infraCommitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .infraCommit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
