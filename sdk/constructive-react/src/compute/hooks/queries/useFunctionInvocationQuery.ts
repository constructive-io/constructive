/**
 * Function invocation log — INSERT to call a function (business-layer, metered). Linked to definitions via function_definition_id FK, with task_identifier as the denormalized routing/audit slug.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionInvocationKeys } from '../query-keys';
import type {
  FunctionInvocationSelect,
  FunctionInvocationWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  FunctionInvocationSelect,
  FunctionInvocationWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const functionInvocationQueryKey = functionInvocationKeys.detail;
/**
 * Function invocation log — INSERT to call a function (business-layer, metered). Linked to definitions via function_definition_id FK, with task_identifier as the denormalized routing/audit slug.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFunctionInvocationQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFunctionInvocationQuery<
  S extends FunctionInvocationSelect,
  TData = {
    functionInvocation: InferSelectResult<FunctionInvocationWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionInvocationSelect>;
  } & Omit<
    UseQueryOptions<
      {
        functionInvocation: InferSelectResult<FunctionInvocationWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFunctionInvocationQuery(
  params: {
    id: string;
    selection: SelectionConfig<FunctionInvocationSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FunctionInvocationSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: functionInvocationKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionInvocation.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Function invocation log — INSERT to call a function (business-layer, metered). Linked to definitions via function_definition_id FK, with task_identifier as the denormalized routing/audit slug.
 *
 * @example
 * ```ts
 * const data = await fetchFunctionInvocationQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFunctionInvocationQuery<S extends FunctionInvocationSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FunctionInvocationSelect>;
}): Promise<{
  functionInvocation: InferSelectResult<FunctionInvocationWithRelations, S> | null;
}>;
export async function fetchFunctionInvocationQuery(params: {
  id: string;
  selection: SelectionConfig<FunctionInvocationSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<FunctionInvocationSelect>(params.selection);
  return getClient()
    .functionInvocation.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Function invocation log — INSERT to call a function (business-layer, metered). Linked to definitions via function_definition_id FK, with task_identifier as the denormalized routing/audit slug.
 *
 * @example
 * ```ts
 * await prefetchFunctionInvocationQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFunctionInvocationQuery<S extends FunctionInvocationSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionInvocationSelect>;
  }
): Promise<void>;
export async function prefetchFunctionInvocationQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FunctionInvocationSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FunctionInvocationSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: functionInvocationKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionInvocation.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
