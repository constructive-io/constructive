/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { builderBindingKeys } from '../query-keys';
import type { BuilderBindingSelect, BuilderBindingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { BuilderBindingSelect, BuilderBindingWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const builderBindingQueryKey = builderBindingKeys.detail;
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBuilderBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useBuilderBindingQuery<
  S extends BuilderBindingSelect,
  TData = {
    builderBinding: InferSelectResult<BuilderBindingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BuilderBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        builderBinding: InferSelectResult<BuilderBindingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBuilderBindingQuery(
  params: {
    id: string;
    selection: SelectionConfig<BuilderBindingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<BuilderBindingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: builderBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .builderBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```ts
 * const data = await fetchBuilderBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchBuilderBindingQuery<S extends BuilderBindingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, BuilderBindingSelect>;
}): Promise<{
  builderBinding: InferSelectResult<BuilderBindingWithRelations, S> | null;
}>;
export async function fetchBuilderBindingQuery(params: {
  id: string;
  selection: SelectionConfig<BuilderBindingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<BuilderBindingSelect>(params.selection);
  return getClient()
    .builderBinding.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```ts
 * await prefetchBuilderBindingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchBuilderBindingQuery<S extends BuilderBindingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BuilderBindingSelect>;
  }
): Promise<void>;
export async function prefetchBuilderBindingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<BuilderBindingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<BuilderBindingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: builderBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .builderBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
