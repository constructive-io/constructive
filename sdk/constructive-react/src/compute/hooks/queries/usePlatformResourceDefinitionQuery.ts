/**
 * Resource definitions — templates for resource kinds declaring default spec and secret/config requirements
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformResourceDefinitionKeys } from '../query-keys';
import type {
  PlatformResourceDefinitionSelect,
  PlatformResourceDefinitionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformResourceDefinitionSelect,
  PlatformResourceDefinitionWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformResourceDefinitionQueryKey = platformResourceDefinitionKeys.detail;
/**
 * Resource definitions — templates for resource kinds declaring default spec and secret/config requirements
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformResourceDefinitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformResourceDefinitionQuery<
  S extends PlatformResourceDefinitionSelect,
  TData = {
    platformResourceDefinition: InferSelectResult<
      PlatformResourceDefinitionWithRelations,
      S
    > | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceDefinitionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformResourceDefinition: InferSelectResult<
          PlatformResourceDefinitionWithRelations,
          S
        > | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformResourceDefinitionQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformResourceDefinitionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformResourceDefinitionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformResourceDefinitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformResourceDefinition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Resource definitions — templates for resource kinds declaring default spec and secret/config requirements
 *
 * @example
 * ```ts
 * const data = await fetchPlatformResourceDefinitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformResourceDefinitionQuery<
  S extends PlatformResourceDefinitionSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformResourceDefinitionSelect>;
}): Promise<{
  platformResourceDefinition: InferSelectResult<PlatformResourceDefinitionWithRelations, S> | null;
}>;
export async function fetchPlatformResourceDefinitionQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformResourceDefinitionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformResourceDefinitionSelect>(params.selection);
  return getClient()
    .platformResourceDefinition.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Resource definitions — templates for resource kinds declaring default spec and secret/config requirements
 *
 * @example
 * ```ts
 * await prefetchPlatformResourceDefinitionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformResourceDefinitionQuery<
  S extends PlatformResourceDefinitionSelect,
>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformResourceDefinitionSelect>;
  }
): Promise<void>;
export async function prefetchPlatformResourceDefinitionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformResourceDefinitionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformResourceDefinitionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformResourceDefinitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformResourceDefinition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
