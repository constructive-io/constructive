/**
 * Grants that make a catalog image usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformImageGrantKeys } from '../query-keys';
import type {
  PlatformImageGrantSelect,
  PlatformImageGrantWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformImageGrantSelect,
  PlatformImageGrantWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformImageGrantQueryKey = platformImageGrantKeys.detail;
/**
 * Grants that make a catalog image usable by one scope
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformImageGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformImageGrantQuery<
  S extends PlatformImageGrantSelect,
  TData = {
    platformImageGrant: InferSelectResult<PlatformImageGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformImageGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformImageGrant: InferSelectResult<PlatformImageGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformImageGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformImageGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformImageGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformImageGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformImageGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Grants that make a catalog image usable by one scope
 *
 * @example
 * ```ts
 * const data = await fetchPlatformImageGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformImageGrantQuery<S extends PlatformImageGrantSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformImageGrantSelect>;
}): Promise<{
  platformImageGrant: InferSelectResult<PlatformImageGrantWithRelations, S> | null;
}>;
export async function fetchPlatformImageGrantQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformImageGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformImageGrantSelect>(params.selection);
  return getClient()
    .platformImageGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Grants that make a catalog image usable by one scope
 *
 * @example
 * ```ts
 * await prefetchPlatformImageGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformImageGrantQuery<S extends PlatformImageGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformImageGrantSelect>;
  }
): Promise<void>;
export async function prefetchPlatformImageGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformImageGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformImageGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformImageGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformImageGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
