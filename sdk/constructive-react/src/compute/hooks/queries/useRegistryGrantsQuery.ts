/**
 * Grants that make a registry usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { registryGrantKeys } from '../query-keys';
import type {
  RegistryGrantSelect,
  RegistryGrantWithRelations,
  RegistryGrantFilter,
  RegistryGrantOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  RegistryGrantSelect,
  RegistryGrantWithRelations,
  RegistryGrantFilter,
  RegistryGrantOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const registryGrantsQueryKey = registryGrantKeys.list;
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRegistryGrantsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useRegistryGrantsQuery<
  S extends RegistryGrantSelect,
  TData = {
    registryGrants: ConnectionResult<InferSelectResult<RegistryGrantWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RegistryGrantFilter, RegistryGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        registryGrants: ConnectionResult<InferSelectResult<RegistryGrantWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRegistryGrantsQuery(
  params: {
    selection: ListSelectionConfig<RegistryGrantSelect, RegistryGrantFilter, RegistryGrantOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    RegistryGrantSelect,
    RegistryGrantFilter,
    RegistryGrantOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: registryGrantKeys.list(args),
    queryFn: () => getClient().registryGrant.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```ts
 * const data = await fetchRegistryGrantsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchRegistryGrantsQuery<S extends RegistryGrantSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, RegistryGrantFilter, RegistryGrantOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
}): Promise<{
  registryGrants: ConnectionResult<InferSelectResult<RegistryGrantWithRelations, S>>;
}>;
export async function fetchRegistryGrantsQuery(params: {
  selection: ListSelectionConfig<RegistryGrantSelect, RegistryGrantFilter, RegistryGrantOrderBy>;
}) {
  const args = buildListSelectionArgs<
    RegistryGrantSelect,
    RegistryGrantFilter,
    RegistryGrantOrderBy
  >(params.selection);
  return getClient().registryGrant.findMany(args).unwrap();
}
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```ts
 * await prefetchRegistryGrantsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchRegistryGrantsQuery<S extends RegistryGrantSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, RegistryGrantFilter, RegistryGrantOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, RegistryGrantSelect>;
  }
): Promise<void>;
export async function prefetchRegistryGrantsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<RegistryGrantSelect, RegistryGrantFilter, RegistryGrantOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    RegistryGrantSelect,
    RegistryGrantFilter,
    RegistryGrantOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: registryGrantKeys.list(args),
    queryFn: () => getClient().registryGrant.findMany(args).unwrap(),
  });
}
