/**
 * Commit history — each commit snapshots a tree root for a store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseGraphCommitKeys } from '../query-keys';
import type {
  DatabaseGraphCommitSelect,
  DatabaseGraphCommitWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DatabaseGraphCommitSelect,
  DatabaseGraphCommitWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseGraphCommitQueryKey = databaseGraphCommitKeys.detail;
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseGraphCommitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDatabaseGraphCommitQuery<
  S extends DatabaseGraphCommitSelect,
  TData = {
    databaseGraphCommit: InferSelectResult<DatabaseGraphCommitWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphCommitSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseGraphCommit: InferSelectResult<DatabaseGraphCommitWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseGraphCommitQuery(
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphCommitSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DatabaseGraphCommitSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseGraphCommitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphCommit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseGraphCommitQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDatabaseGraphCommitQuery<S extends DatabaseGraphCommitSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DatabaseGraphCommitSelect>;
}): Promise<{
  databaseGraphCommit: InferSelectResult<DatabaseGraphCommitWithRelations, S> | null;
}>;
export async function fetchDatabaseGraphCommitQuery(params: {
  id: string;
  selection: SelectionConfig<DatabaseGraphCommitSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DatabaseGraphCommitSelect>(params.selection);
  return getClient()
    .databaseGraphCommit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Commit history — each commit snapshots a tree root for a store
 *
 * @example
 * ```ts
 * await prefetchDatabaseGraphCommitQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDatabaseGraphCommitQuery<S extends DatabaseGraphCommitSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphCommitSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseGraphCommitQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphCommitSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DatabaseGraphCommitSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseGraphCommitKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphCommit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
