/**
 * Container image catalog: images available to run as functions, resources, and builds
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { imageKeys } from '../query-keys';
import type { ImageSelect, ImageWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ImageSelect, ImageWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const imageQueryKey = imageKeys.detail;
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useImageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useImageQuery<
  S extends ImageSelect,
  TData = {
    image: InferSelectResult<ImageWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ImageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        image: InferSelectResult<ImageWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useImageQuery(
  params: {
    id: string;
    selection: SelectionConfig<ImageSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ImageSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: imageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .image.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * const data = await fetchImageQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchImageQuery<S extends ImageSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ImageSelect>;
}): Promise<{
  image: InferSelectResult<ImageWithRelations, S> | null;
}>;
export async function fetchImageQuery(params: {
  id: string;
  selection: SelectionConfig<ImageSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ImageSelect>(params.selection);
  return getClient()
    .image.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * await prefetchImageQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchImageQuery<S extends ImageSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ImageSelect>;
  }
): Promise<void>;
export async function prefetchImageQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ImageSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ImageSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: imageKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .image.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
