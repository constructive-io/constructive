/**
 * One run of a repository workflow: its commit, its job, and what it produced
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformBuildKeys } from '../query-keys';
import type {
  PlatformBuildSelect,
  PlatformBuildWithRelations,
  PlatformBuildFilter,
  PlatformBuildOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformBuildSelect,
  PlatformBuildWithRelations,
  PlatformBuildFilter,
  PlatformBuildOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBuildsQueryKey = platformBuildKeys.list;
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBuildsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformBuildsQuery<
  S extends PlatformBuildSelect,
  TData = {
    platformBuilds: ConnectionResult<InferSelectResult<PlatformBuildWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformBuildFilter, PlatformBuildOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformBuildSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBuilds: ConnectionResult<InferSelectResult<PlatformBuildWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBuildsQuery(
  params: {
    selection: ListSelectionConfig<PlatformBuildSelect, PlatformBuildFilter, PlatformBuildOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformBuildSelect,
    PlatformBuildFilter,
    PlatformBuildOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBuildKeys.list(args),
    queryFn: () => getClient().platformBuild.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBuildsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformBuildsQuery<S extends PlatformBuildSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlatformBuildFilter, PlatformBuildOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlatformBuildSelect>;
}): Promise<{
  platformBuilds: ConnectionResult<InferSelectResult<PlatformBuildWithRelations, S>>;
}>;
export async function fetchPlatformBuildsQuery(params: {
  selection: ListSelectionConfig<PlatformBuildSelect, PlatformBuildFilter, PlatformBuildOrderBy>;
}) {
  const args = buildListSelectionArgs<
    PlatformBuildSelect,
    PlatformBuildFilter,
    PlatformBuildOrderBy
  >(params.selection);
  return getClient().platformBuild.findMany(args).unwrap();
}
/**
 * One run of a repository workflow: its commit, its job, and what it produced
 *
 * @example
 * ```ts
 * await prefetchPlatformBuildsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformBuildsQuery<S extends PlatformBuildSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformBuildFilter, PlatformBuildOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformBuildSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBuildsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<PlatformBuildSelect, PlatformBuildFilter, PlatformBuildOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformBuildSelect,
    PlatformBuildFilter,
    PlatformBuildOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBuildKeys.list(args),
    queryFn: () => getClient().platformBuild.findMany(args).unwrap(),
  });
}
