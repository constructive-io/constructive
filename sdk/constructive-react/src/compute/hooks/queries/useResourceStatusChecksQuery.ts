/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { resourceStatusCheckKeys } from '../query-keys';
import type {
  ResourceStatusCheckSelect,
  ResourceStatusCheckWithRelations,
  ResourceStatusCheckFilter,
  ResourceStatusCheckOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  ResourceStatusCheckSelect,
  ResourceStatusCheckWithRelations,
  ResourceStatusCheckFilter,
  ResourceStatusCheckOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const resourceStatusChecksQueryKey = resourceStatusCheckKeys.list;
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useResourceStatusChecksQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useResourceStatusChecksQuery<
  S extends ResourceStatusCheckSelect,
  TData = {
    resourceStatusChecks: ConnectionResult<InferSelectResult<ResourceStatusCheckWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, ResourceStatusCheckFilter, ResourceStatusCheckOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, ResourceStatusCheckSelect>;
  } & Omit<
    UseQueryOptions<
      {
        resourceStatusChecks: ConnectionResult<
          InferSelectResult<ResourceStatusCheckWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useResourceStatusChecksQuery(
  params: {
    selection: ListSelectionConfig<
      ResourceStatusCheckSelect,
      ResourceStatusCheckFilter,
      ResourceStatusCheckOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    ResourceStatusCheckSelect,
    ResourceStatusCheckFilter,
    ResourceStatusCheckOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: resourceStatusCheckKeys.list(args),
    queryFn: () => getClient().resourceStatusCheck.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```ts
 * const data = await fetchResourceStatusChecksQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchResourceStatusChecksQuery<S extends ResourceStatusCheckSelect>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, ResourceStatusCheckFilter, ResourceStatusCheckOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, ResourceStatusCheckSelect>;
}): Promise<{
  resourceStatusChecks: ConnectionResult<InferSelectResult<ResourceStatusCheckWithRelations, S>>;
}>;
export async function fetchResourceStatusChecksQuery(params: {
  selection: ListSelectionConfig<
    ResourceStatusCheckSelect,
    ResourceStatusCheckFilter,
    ResourceStatusCheckOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    ResourceStatusCheckSelect,
    ResourceStatusCheckFilter,
    ResourceStatusCheckOrderBy
  >(params.selection);
  return getClient().resourceStatusCheck.findMany(args).unwrap();
}
/**
 * On-demand resource status checks — diagnostic snapshots from the runtime (K8s status, conditions, log tails)
 *
 * @example
 * ```ts
 * await prefetchResourceStatusChecksQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchResourceStatusChecksQuery<S extends ResourceStatusCheckSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, ResourceStatusCheckFilter, ResourceStatusCheckOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, ResourceStatusCheckSelect>;
  }
): Promise<void>;
export async function prefetchResourceStatusChecksQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      ResourceStatusCheckSelect,
      ResourceStatusCheckFilter,
      ResourceStatusCheckOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    ResourceStatusCheckSelect,
    ResourceStatusCheckFilter,
    ResourceStatusCheckOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: resourceStatusCheckKeys.list(args),
    queryFn: () => getClient().resourceStatusCheck.findMany(args).unwrap(),
  });
}
