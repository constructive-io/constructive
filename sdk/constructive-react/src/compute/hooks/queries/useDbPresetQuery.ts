/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { dbPresetKeys } from '../query-keys';
import type { DbPresetSelect, DbPresetWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DbPresetSelect, DbPresetWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const dbPresetQueryKey = dbPresetKeys.detail;
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDbPresetQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDbPresetQuery<
  S extends DbPresetSelect,
  TData = {
    dbPreset: InferSelectResult<DbPresetWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DbPresetSelect>;
  } & Omit<
    UseQueryOptions<
      {
        dbPreset: InferSelectResult<DbPresetWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDbPresetQuery(
  params: {
    id: string;
    selection: SelectionConfig<DbPresetSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DbPresetSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: dbPresetKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .dbPreset.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * const data = await fetchDbPresetQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDbPresetQuery<S extends DbPresetSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DbPresetSelect>;
}): Promise<{
  dbPreset: InferSelectResult<DbPresetWithRelations, S> | null;
}>;
export async function fetchDbPresetQuery(params: {
  id: string;
  selection: SelectionConfig<DbPresetSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DbPresetSelect>(params.selection);
  return getClient()
    .dbPreset.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Database provisioning preset catalog — merkle-versioned head over the infra store
 *
 * @example
 * ```ts
 * await prefetchDbPresetQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDbPresetQuery<S extends DbPresetSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DbPresetSelect>;
  }
): Promise<void>;
export async function prefetchDbPresetQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DbPresetSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DbPresetSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: dbPresetKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .dbPreset.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
