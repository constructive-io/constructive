/**
 * Comments on a local proposal, optionally anchored to a line
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { proposalCommentKeys } from '../query-keys';
import type { ProposalCommentSelect, ProposalCommentWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ProposalCommentSelect, ProposalCommentWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const proposalCommentQueryKey = proposalCommentKeys.detail;
/**
 * Comments on a local proposal, optionally anchored to a line
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useProposalCommentQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useProposalCommentQuery<
  S extends ProposalCommentSelect,
  TData = {
    proposalComment: InferSelectResult<ProposalCommentWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ProposalCommentSelect>;
  } & Omit<
    UseQueryOptions<
      {
        proposalComment: InferSelectResult<ProposalCommentWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useProposalCommentQuery(
  params: {
    id: string;
    selection: SelectionConfig<ProposalCommentSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ProposalCommentSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: proposalCommentKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .proposalComment.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Comments on a local proposal, optionally anchored to a line
 *
 * @example
 * ```ts
 * const data = await fetchProposalCommentQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchProposalCommentQuery<S extends ProposalCommentSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ProposalCommentSelect>;
}): Promise<{
  proposalComment: InferSelectResult<ProposalCommentWithRelations, S> | null;
}>;
export async function fetchProposalCommentQuery(params: {
  id: string;
  selection: SelectionConfig<ProposalCommentSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ProposalCommentSelect>(params.selection);
  return getClient()
    .proposalComment.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Comments on a local proposal, optionally anchored to a line
 *
 * @example
 * ```ts
 * await prefetchProposalCommentQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchProposalCommentQuery<S extends ProposalCommentSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ProposalCommentSelect>;
  }
): Promise<void>;
export async function prefetchProposalCommentQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ProposalCommentSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ProposalCommentSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: proposalCommentKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .proposalComment.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
