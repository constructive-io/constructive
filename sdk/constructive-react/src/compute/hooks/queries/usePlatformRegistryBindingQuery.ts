/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRegistryBindingKeys } from '../query-keys';
import type {
  PlatformRegistryBindingSelect,
  PlatformRegistryBindingWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformRegistryBindingSelect,
  PlatformRegistryBindingWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformRegistryBindingQueryKey = platformRegistryBindingKeys.detail;
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformRegistryBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformRegistryBindingQuery<
  S extends PlatformRegistryBindingSelect,
  TData = {
    platformRegistryBinding: InferSelectResult<PlatformRegistryBindingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformRegistryBinding: InferSelectResult<PlatformRegistryBindingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformRegistryBindingQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformRegistryBindingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformRegistryBindingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformRegistryBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformRegistryBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * const data = await fetchPlatformRegistryBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformRegistryBindingQuery<
  S extends PlatformRegistryBindingSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
}): Promise<{
  platformRegistryBinding: InferSelectResult<PlatformRegistryBindingWithRelations, S> | null;
}>;
export async function fetchPlatformRegistryBindingQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformRegistryBindingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformRegistryBindingSelect>(params.selection);
  return getClient()
    .platformRegistryBinding.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * await prefetchPlatformRegistryBindingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformRegistryBindingQuery<S extends PlatformRegistryBindingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistryBindingSelect>;
  }
): Promise<void>;
export async function prefetchPlatformRegistryBindingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformRegistryBindingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformRegistryBindingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformRegistryBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformRegistryBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
