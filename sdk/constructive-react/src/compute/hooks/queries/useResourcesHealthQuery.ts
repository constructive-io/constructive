/**
 * Single item query hook for ResourcesHealth
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { resourcesHealthKeys } from '../query-keys';
import type { ResourcesHealthSelect, ResourcesHealthWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ResourcesHealthSelect, ResourcesHealthWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const resourcesHealthQueryKey = resourcesHealthKeys.detail;
/**
 * Query hook for fetching a single ResourcesHealth
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useResourcesHealthQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useResourcesHealthQuery<
  S extends ResourcesHealthSelect,
  TData = {
    resourcesHealth: InferSelectResult<ResourcesHealthWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ResourcesHealthSelect>;
  } & Omit<
    UseQueryOptions<
      {
        resourcesHealth: InferSelectResult<ResourcesHealthWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useResourcesHealthQuery(
  params: {
    id: string;
    selection: SelectionConfig<ResourcesHealthSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<ResourcesHealthSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: resourcesHealthKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .resourcesHealth.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch a single ResourcesHealth without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchResourcesHealthQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchResourcesHealthQuery<S extends ResourcesHealthSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, ResourcesHealthSelect>;
}): Promise<{
  resourcesHealth: InferSelectResult<ResourcesHealthWithRelations, S> | null;
}>;
export async function fetchResourcesHealthQuery(params: {
  id: string;
  selection: SelectionConfig<ResourcesHealthSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<ResourcesHealthSelect>(params.selection);
  return getClient()
    .resourcesHealth.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Prefetch a single ResourcesHealth for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchResourcesHealthQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchResourcesHealthQuery<S extends ResourcesHealthSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, ResourcesHealthSelect>;
  }
): Promise<void>;
export async function prefetchResourcesHealthQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<ResourcesHealthSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<ResourcesHealthSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: resourcesHealthKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .resourcesHealth.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
