/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { namespaceKeys } from '../query-keys';
import type { NamespaceSelect, NamespaceWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { NamespaceSelect, NamespaceWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const namespaceQueryKey = namespaceKeys.detail;
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useNamespaceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useNamespaceQuery<
  S extends NamespaceSelect,
  TData = {
    namespace: InferSelectResult<NamespaceWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, NamespaceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        namespace: InferSelectResult<NamespaceWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useNamespaceQuery(
  params: {
    id: string;
    selection: SelectionConfig<NamespaceSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<NamespaceSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: namespaceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .namespace.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```ts
 * const data = await fetchNamespaceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchNamespaceQuery<S extends NamespaceSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, NamespaceSelect>;
}): Promise<{
  namespace: InferSelectResult<NamespaceWithRelations, S> | null;
}>;
export async function fetchNamespaceQuery(params: {
  id: string;
  selection: SelectionConfig<NamespaceSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<NamespaceSelect>(params.selection);
  return getClient()
    .namespace.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Logical namespace containers for grouping secrets, config, functions, and other resources
 *
 * @example
 * ```ts
 * await prefetchNamespaceQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchNamespaceQuery<S extends NamespaceSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, NamespaceSelect>;
  }
): Promise<void>;
export async function prefetchNamespaceQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<NamespaceSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<NamespaceSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: namespaceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .namespace.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
