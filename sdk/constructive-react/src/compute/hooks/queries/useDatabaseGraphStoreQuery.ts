/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { databaseGraphStoreKeys } from '../query-keys';
import type {
  DatabaseGraphStoreSelect,
  DatabaseGraphStoreWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  DatabaseGraphStoreSelect,
  DatabaseGraphStoreWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseGraphStoreQueryKey = databaseGraphStoreKeys.detail;
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseGraphStoreQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useDatabaseGraphStoreQuery<
  S extends DatabaseGraphStoreSelect,
  TData = {
    databaseGraphStore: InferSelectResult<DatabaseGraphStoreWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphStoreSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseGraphStore: InferSelectResult<DatabaseGraphStoreWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseGraphStoreQuery(
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphStoreSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<DatabaseGraphStoreSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseGraphStoreKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphStore.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseGraphStoreQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchDatabaseGraphStoreQuery<S extends DatabaseGraphStoreSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, DatabaseGraphStoreSelect>;
}): Promise<{
  databaseGraphStore: InferSelectResult<DatabaseGraphStoreWithRelations, S> | null;
}>;
export async function fetchDatabaseGraphStoreQuery(params: {
  id: string;
  selection: SelectionConfig<DatabaseGraphStoreSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<DatabaseGraphStoreSelect>(params.selection);
  return getClient()
    .databaseGraphStore.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Named stores — one per version-controlled tree (e.g. one graph, one definition set)
 *
 * @example
 * ```ts
 * await prefetchDatabaseGraphStoreQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchDatabaseGraphStoreQuery<S extends DatabaseGraphStoreSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, DatabaseGraphStoreSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseGraphStoreQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<DatabaseGraphStoreSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<DatabaseGraphStoreSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseGraphStoreKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .databaseGraphStore.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
