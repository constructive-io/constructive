/**
 * Branch heads — mutable pointers into the commit chain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { databaseGraphRefKeys } from '../query-keys';
import type {
  DatabaseGraphRefSelect,
  DatabaseGraphRefWithRelations,
  DatabaseGraphRefFilter,
  DatabaseGraphRefOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  DatabaseGraphRefSelect,
  DatabaseGraphRefWithRelations,
  DatabaseGraphRefFilter,
  DatabaseGraphRefOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const databaseGraphRefsQueryKey = databaseGraphRefKeys.list;
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDatabaseGraphRefsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useDatabaseGraphRefsQuery<
  S extends DatabaseGraphRefSelect,
  TData = {
    databaseGraphRefs: ConnectionResult<InferSelectResult<DatabaseGraphRefWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DatabaseGraphRefFilter, DatabaseGraphRefOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
  } & Omit<
    UseQueryOptions<
      {
        databaseGraphRefs: ConnectionResult<InferSelectResult<DatabaseGraphRefWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDatabaseGraphRefsQuery(
  params: {
    selection: ListSelectionConfig<
      DatabaseGraphRefSelect,
      DatabaseGraphRefFilter,
      DatabaseGraphRefOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    DatabaseGraphRefSelect,
    DatabaseGraphRefFilter,
    DatabaseGraphRefOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: databaseGraphRefKeys.list(args),
    queryFn: () => getClient().databaseGraphRef.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```ts
 * const data = await fetchDatabaseGraphRefsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchDatabaseGraphRefsQuery<S extends DatabaseGraphRefSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, DatabaseGraphRefFilter, DatabaseGraphRefOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
}): Promise<{
  databaseGraphRefs: ConnectionResult<InferSelectResult<DatabaseGraphRefWithRelations, S>>;
}>;
export async function fetchDatabaseGraphRefsQuery(params: {
  selection: ListSelectionConfig<
    DatabaseGraphRefSelect,
    DatabaseGraphRefFilter,
    DatabaseGraphRefOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    DatabaseGraphRefSelect,
    DatabaseGraphRefFilter,
    DatabaseGraphRefOrderBy
  >(params.selection);
  return getClient().databaseGraphRef.findMany(args).unwrap();
}
/**
 * Branch heads — mutable pointers into the commit chain
 *
 * @example
 * ```ts
 * await prefetchDatabaseGraphRefsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchDatabaseGraphRefsQuery<S extends DatabaseGraphRefSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DatabaseGraphRefFilter, DatabaseGraphRefOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DatabaseGraphRefSelect>;
  }
): Promise<void>;
export async function prefetchDatabaseGraphRefsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      DatabaseGraphRefSelect,
      DatabaseGraphRefFilter,
      DatabaseGraphRefOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    DatabaseGraphRefSelect,
    DatabaseGraphRefFilter,
    DatabaseGraphRefOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: databaseGraphRefKeys.list(args),
    queryFn: () => getClient().databaseGraphRef.findMany(args).unwrap(),
  });
}
