/**
 * Bindings from a repository event to the flow graph that should run
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryWorkflowKeys } from '../query-keys';
import type {
  RepositoryWorkflowSelect,
  RepositoryWorkflowWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  RepositoryWorkflowSelect,
  RepositoryWorkflowWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const repositoryWorkflowQueryKey = repositoryWorkflowKeys.detail;
/**
 * Bindings from a repository event to the flow graph that should run
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRepositoryWorkflowQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRepositoryWorkflowQuery<
  S extends RepositoryWorkflowSelect,
  TData = {
    repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RepositoryWorkflowSelect>;
  } & Omit<
    UseQueryOptions<
      {
        repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRepositoryWorkflowQuery(
  params: {
    id: string;
    selection: SelectionConfig<RepositoryWorkflowSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RepositoryWorkflowSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: repositoryWorkflowKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .repositoryWorkflow.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Bindings from a repository event to the flow graph that should run
 *
 * @example
 * ```ts
 * const data = await fetchRepositoryWorkflowQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRepositoryWorkflowQuery<S extends RepositoryWorkflowSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RepositoryWorkflowSelect>;
}): Promise<{
  repositoryWorkflow: InferSelectResult<RepositoryWorkflowWithRelations, S> | null;
}>;
export async function fetchRepositoryWorkflowQuery(params: {
  id: string;
  selection: SelectionConfig<RepositoryWorkflowSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RepositoryWorkflowSelect>(params.selection);
  return getClient()
    .repositoryWorkflow.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Bindings from a repository event to the flow graph that should run
 *
 * @example
 * ```ts
 * await prefetchRepositoryWorkflowQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRepositoryWorkflowQuery<S extends RepositoryWorkflowSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RepositoryWorkflowSelect>;
  }
): Promise<void>;
export async function prefetchRepositoryWorkflowQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RepositoryWorkflowSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RepositoryWorkflowSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: repositoryWorkflowKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .repositoryWorkflow.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
