/**
 * Normalized repository events from local hooks and external providers
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { repositoryEventKeys } from '../query-keys';
import type { RepositoryEventSelect, RepositoryEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RepositoryEventSelect, RepositoryEventWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const repositoryEventQueryKey = repositoryEventKeys.detail;
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRepositoryEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRepositoryEventQuery<
  S extends RepositoryEventSelect,
  TData = {
    repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RepositoryEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRepositoryEventQuery(
  params: {
    id: string;
    selection: SelectionConfig<RepositoryEventSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RepositoryEventSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: repositoryEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .repositoryEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```ts
 * const data = await fetchRepositoryEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRepositoryEventQuery<S extends RepositoryEventSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RepositoryEventSelect>;
}): Promise<{
  repositoryEvent: InferSelectResult<RepositoryEventWithRelations, S> | null;
}>;
export async function fetchRepositoryEventQuery(params: {
  id: string;
  selection: SelectionConfig<RepositoryEventSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RepositoryEventSelect>(params.selection);
  return getClient()
    .repositoryEvent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Normalized repository events from local hooks and external providers
 *
 * @example
 * ```ts
 * await prefetchRepositoryEventQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRepositoryEventQuery<S extends RepositoryEventSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RepositoryEventSelect>;
  }
): Promise<void>;
export async function prefetchRepositoryEventQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RepositoryEventSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RepositoryEventSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: repositoryEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .repositoryEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
