/**
 * Function deployment bindings — ties a handler image to a namespace for Knative provisioning and routing (one row per handler image per namespace)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { functionDeploymentKeys } from '../query-keys';
import type {
  FunctionDeploymentSelect,
  FunctionDeploymentWithRelations,
  FunctionDeploymentFilter,
  FunctionDeploymentOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  FunctionDeploymentSelect,
  FunctionDeploymentWithRelations,
  FunctionDeploymentFilter,
  FunctionDeploymentOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const functionDeploymentsQueryKey = functionDeploymentKeys.list;
/**
 * Function deployment bindings — ties a handler image to a namespace for Knative provisioning and routing (one row per handler image per namespace)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFunctionDeploymentsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useFunctionDeploymentsQuery<
  S extends FunctionDeploymentSelect,
  TData = {
    functionDeployments: ConnectionResult<InferSelectResult<FunctionDeploymentWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, FunctionDeploymentFilter, FunctionDeploymentOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, FunctionDeploymentSelect>;
  } & Omit<
    UseQueryOptions<
      {
        functionDeployments: ConnectionResult<
          InferSelectResult<FunctionDeploymentWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFunctionDeploymentsQuery(
  params: {
    selection: ListSelectionConfig<
      FunctionDeploymentSelect,
      FunctionDeploymentFilter,
      FunctionDeploymentOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    FunctionDeploymentSelect,
    FunctionDeploymentFilter,
    FunctionDeploymentOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: functionDeploymentKeys.list(args),
    queryFn: () => getClient().functionDeployment.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Function deployment bindings — ties a handler image to a namespace for Knative provisioning and routing (one row per handler image per namespace)
 *
 * @example
 * ```ts
 * const data = await fetchFunctionDeploymentsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchFunctionDeploymentsQuery<S extends FunctionDeploymentSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, FunctionDeploymentFilter, FunctionDeploymentOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, FunctionDeploymentSelect>;
}): Promise<{
  functionDeployments: ConnectionResult<InferSelectResult<FunctionDeploymentWithRelations, S>>;
}>;
export async function fetchFunctionDeploymentsQuery(params: {
  selection: ListSelectionConfig<
    FunctionDeploymentSelect,
    FunctionDeploymentFilter,
    FunctionDeploymentOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    FunctionDeploymentSelect,
    FunctionDeploymentFilter,
    FunctionDeploymentOrderBy
  >(params.selection);
  return getClient().functionDeployment.findMany(args).unwrap();
}
/**
 * Function deployment bindings — ties a handler image to a namespace for Knative provisioning and routing (one row per handler image per namespace)
 *
 * @example
 * ```ts
 * await prefetchFunctionDeploymentsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchFunctionDeploymentsQuery<S extends FunctionDeploymentSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, FunctionDeploymentFilter, FunctionDeploymentOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, FunctionDeploymentSelect>;
  }
): Promise<void>;
export async function prefetchFunctionDeploymentsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      FunctionDeploymentSelect,
      FunctionDeploymentFilter,
      FunctionDeploymentOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    FunctionDeploymentSelect,
    FunctionDeploymentFilter,
    FunctionDeploymentOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: functionDeploymentKeys.list(args),
    queryFn: () => getClient().functionDeployment.findMany(args).unwrap(),
  });
}
