/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformBuilderBindingKeys } from '../query-keys';
import type {
  PlatformBuilderBindingSelect,
  PlatformBuilderBindingWithRelations,
  PlatformBuilderBindingFilter,
  PlatformBuilderBindingOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformBuilderBindingSelect,
  PlatformBuilderBindingWithRelations,
  PlatformBuilderBindingFilter,
  PlatformBuilderBindingOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBuilderBindingsQueryKey = platformBuilderBindingKeys.list;
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBuilderBindingsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformBuilderBindingsQuery<
  S extends PlatformBuilderBindingSelect,
  TData = {
    platformBuilderBindings: ConnectionResult<
      InferSelectResult<PlatformBuilderBindingWithRelations, S>
    >;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformBuilderBindingFilter, PlatformBuilderBindingOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBuilderBindings: ConnectionResult<
          InferSelectResult<PlatformBuilderBindingWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBuilderBindingsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformBuilderBindingSelect,
      PlatformBuilderBindingFilter,
      PlatformBuilderBindingOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformBuilderBindingSelect,
    PlatformBuilderBindingFilter,
    PlatformBuilderBindingOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBuilderBindingKeys.list(args),
    queryFn: () => getClient().platformBuilderBinding.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBuilderBindingsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformBuilderBindingsQuery<
  S extends PlatformBuilderBindingSelect,
>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, PlatformBuilderBindingFilter, PlatformBuilderBindingOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
}): Promise<{
  platformBuilderBindings: ConnectionResult<
    InferSelectResult<PlatformBuilderBindingWithRelations, S>
  >;
}>;
export async function fetchPlatformBuilderBindingsQuery(params: {
  selection: ListSelectionConfig<
    PlatformBuilderBindingSelect,
    PlatformBuilderBindingFilter,
    PlatformBuilderBindingOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformBuilderBindingSelect,
    PlatformBuilderBindingFilter,
    PlatformBuilderBindingOrderBy
  >(params.selection);
  return getClient().platformBuilderBinding.findMany(args).unwrap();
}
/**
 * Binds a namespace to an installed BuildKit resource for a lane (realm) — the reconciler projects its in-cluster Service address as BUILDKIT_HOST
 *
 * @example
 * ```ts
 * await prefetchPlatformBuilderBindingsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformBuilderBindingsQuery<S extends PlatformBuilderBindingSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, PlatformBuilderBindingFilter, PlatformBuilderBindingOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, PlatformBuilderBindingSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBuilderBindingsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformBuilderBindingSelect,
      PlatformBuilderBindingFilter,
      PlatformBuilderBindingOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformBuilderBindingSelect,
    PlatformBuilderBindingFilter,
    PlatformBuilderBindingOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBuilderBindingKeys.list(args),
    queryFn: () => getClient().platformBuilderBinding.findMany(args).unwrap(),
  });
}
