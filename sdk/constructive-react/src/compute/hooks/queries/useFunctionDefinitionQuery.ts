/**
 * Function definitions — registered cloud functions with routing, queue, and retry configuration
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { functionDefinitionKeys } from '../query-keys';
import type {
  FunctionDefinitionSelect,
  FunctionDefinitionWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  FunctionDefinitionSelect,
  FunctionDefinitionWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const functionDefinitionQueryKey = functionDefinitionKeys.detail;
/**
 * Function definitions — registered cloud functions with routing, queue, and retry configuration
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useFunctionDefinitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useFunctionDefinitionQuery<
  S extends FunctionDefinitionSelect,
  TData = {
    functionDefinition: InferSelectResult<FunctionDefinitionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionDefinitionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        functionDefinition: InferSelectResult<FunctionDefinitionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useFunctionDefinitionQuery(
  params: {
    id: string;
    selection: SelectionConfig<FunctionDefinitionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<FunctionDefinitionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: functionDefinitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionDefinition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Function definitions — registered cloud functions with routing, queue, and retry configuration
 *
 * @example
 * ```ts
 * const data = await fetchFunctionDefinitionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchFunctionDefinitionQuery<S extends FunctionDefinitionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, FunctionDefinitionSelect>;
}): Promise<{
  functionDefinition: InferSelectResult<FunctionDefinitionWithRelations, S> | null;
}>;
export async function fetchFunctionDefinitionQuery(params: {
  id: string;
  selection: SelectionConfig<FunctionDefinitionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<FunctionDefinitionSelect>(params.selection);
  return getClient()
    .functionDefinition.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Function definitions — registered cloud functions with routing, queue, and retry configuration
 *
 * @example
 * ```ts
 * await prefetchFunctionDefinitionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchFunctionDefinitionQuery<S extends FunctionDefinitionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, FunctionDefinitionSelect>;
  }
): Promise<void>;
export async function prefetchFunctionDefinitionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<FunctionDefinitionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<FunctionDefinitionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: functionDefinitionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .functionDefinition.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
