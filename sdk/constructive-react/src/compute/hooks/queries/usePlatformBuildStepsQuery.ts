/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { platformBuildStepKeys } from '../query-keys';
import type {
  PlatformBuildStepSelect,
  PlatformBuildStepWithRelations,
  PlatformBuildStepFilter,
  PlatformBuildStepOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlatformBuildStepSelect,
  PlatformBuildStepWithRelations,
  PlatformBuildStepFilter,
  PlatformBuildStepOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformBuildStepsQueryKey = platformBuildStepKeys.list;
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformBuildStepsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlatformBuildStepsQuery<
  S extends PlatformBuildStepSelect,
  TData = {
    platformBuildSteps: ConnectionResult<InferSelectResult<PlatformBuildStepWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformBuildStepFilter, PlatformBuildStepOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformBuildSteps: ConnectionResult<InferSelectResult<PlatformBuildStepWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformBuildStepsQuery(
  params: {
    selection: ListSelectionConfig<
      PlatformBuildStepSelect,
      PlatformBuildStepFilter,
      PlatformBuildStepOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    PlatformBuildStepSelect,
    PlatformBuildStepFilter,
    PlatformBuildStepOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformBuildStepKeys.list(args),
    queryFn: () => getClient().platformBuildStep.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```ts
 * const data = await fetchPlatformBuildStepsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlatformBuildStepsQuery<S extends PlatformBuildStepSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlatformBuildStepFilter, PlatformBuildStepOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
}): Promise<{
  platformBuildSteps: ConnectionResult<InferSelectResult<PlatformBuildStepWithRelations, S>>;
}>;
export async function fetchPlatformBuildStepsQuery(params: {
  selection: ListSelectionConfig<
    PlatformBuildStepSelect,
    PlatformBuildStepFilter,
    PlatformBuildStepOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    PlatformBuildStepSelect,
    PlatformBuildStepFilter,
    PlatformBuildStepOrderBy
  >(params.selection);
  return getClient().platformBuildStep.findMany(args).unwrap();
}
/**
 * Partitioned append-only step and test results of a build, keyed into its log object
 *
 * @example
 * ```ts
 * await prefetchPlatformBuildStepsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlatformBuildStepsQuery<S extends PlatformBuildStepSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlatformBuildStepFilter, PlatformBuildStepOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlatformBuildStepSelect>;
  }
): Promise<void>;
export async function prefetchPlatformBuildStepsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      PlatformBuildStepSelect,
      PlatformBuildStepFilter,
      PlatformBuildStepOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    PlatformBuildStepSelect,
    PlatformBuildStepFilter,
    PlatformBuildStepOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformBuildStepKeys.list(args),
    queryFn: () => getClient().platformBuildStep.findMany(args).unwrap(),
  });
}
