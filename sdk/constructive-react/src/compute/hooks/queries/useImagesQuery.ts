/**
 * Container image catalog: images available to run as functions, resources, and builds
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { imageKeys } from '../query-keys';
import type {
  ImageSelect,
  ImageWithRelations,
  ImageFilter,
  ImageOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  ImageSelect,
  ImageWithRelations,
  ImageFilter,
  ImageOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const imagesQueryKey = imageKeys.list;
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useImagesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useImagesQuery<
  S extends ImageSelect,
  TData = {
    images: ConnectionResult<InferSelectResult<ImageWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ImageFilter, ImageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ImageSelect>;
  } & Omit<
    UseQueryOptions<
      {
        images: ConnectionResult<InferSelectResult<ImageWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useImagesQuery(
  params: {
    selection: ListSelectionConfig<ImageSelect, ImageFilter, ImageOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<ImageSelect, ImageFilter, ImageOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: imageKeys.list(args),
    queryFn: () => getClient().image.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * const data = await fetchImagesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchImagesQuery<S extends ImageSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, ImageFilter, ImageOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, ImageSelect>;
}): Promise<{
  images: ConnectionResult<InferSelectResult<ImageWithRelations, S>>;
}>;
export async function fetchImagesQuery(params: {
  selection: ListSelectionConfig<ImageSelect, ImageFilter, ImageOrderBy>;
}) {
  const args = buildListSelectionArgs<ImageSelect, ImageFilter, ImageOrderBy>(params.selection);
  return getClient().image.findMany(args).unwrap();
}
/**
 * Container image catalog: images available to run as functions, resources, and builds
 *
 * @example
 * ```ts
 * await prefetchImagesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchImagesQuery<S extends ImageSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, ImageFilter, ImageOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, ImageSelect>;
  }
): Promise<void>;
export async function prefetchImagesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<ImageSelect, ImageFilter, ImageOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<ImageSelect, ImageFilter, ImageOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: imageKeys.list(args),
    queryFn: () => getClient().image.findMany(args).unwrap(),
  });
}
