/**
 * Grants that make a registry usable by one scope
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { platformRegistryGrantKeys } from '../query-keys';
import type {
  PlatformRegistryGrantSelect,
  PlatformRegistryGrantWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlatformRegistryGrantSelect,
  PlatformRegistryGrantWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const platformRegistryGrantQueryKey = platformRegistryGrantKeys.detail;
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlatformRegistryGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlatformRegistryGrantQuery<
  S extends PlatformRegistryGrantSelect,
  TData = {
    platformRegistryGrant: InferSelectResult<PlatformRegistryGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistryGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        platformRegistryGrant: InferSelectResult<PlatformRegistryGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlatformRegistryGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlatformRegistryGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlatformRegistryGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: platformRegistryGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformRegistryGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```ts
 * const data = await fetchPlatformRegistryGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlatformRegistryGrantQuery<
  S extends PlatformRegistryGrantSelect,
>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlatformRegistryGrantSelect>;
}): Promise<{
  platformRegistryGrant: InferSelectResult<PlatformRegistryGrantWithRelations, S> | null;
}>;
export async function fetchPlatformRegistryGrantQuery(params: {
  id: string;
  selection: SelectionConfig<PlatformRegistryGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlatformRegistryGrantSelect>(params.selection);
  return getClient()
    .platformRegistryGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Grants that make a registry usable by one scope
 *
 * @example
 * ```ts
 * await prefetchPlatformRegistryGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlatformRegistryGrantQuery<S extends PlatformRegistryGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlatformRegistryGrantSelect>;
  }
): Promise<void>;
export async function prefetchPlatformRegistryGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlatformRegistryGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlatformRegistryGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: platformRegistryGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .platformRegistryGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
