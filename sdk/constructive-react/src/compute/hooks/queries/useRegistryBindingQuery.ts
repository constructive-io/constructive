/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { registryBindingKeys } from '../query-keys';
import type { RegistryBindingSelect, RegistryBindingWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { RegistryBindingSelect, RegistryBindingWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const registryBindingQueryKey = registryBindingKeys.detail;
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useRegistryBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useRegistryBindingQuery<
  S extends RegistryBindingSelect,
  TData = {
    registryBinding: InferSelectResult<RegistryBindingWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
  } & Omit<
    UseQueryOptions<
      {
        registryBinding: InferSelectResult<RegistryBindingWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useRegistryBindingQuery(
  params: {
    id: string;
    selection: SelectionConfig<RegistryBindingSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<RegistryBindingSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: registryBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .registryBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * const data = await fetchRegistryBindingQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchRegistryBindingQuery<S extends RegistryBindingSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
}): Promise<{
  registryBinding: InferSelectResult<RegistryBindingWithRelations, S> | null;
}>;
export async function fetchRegistryBindingQuery(params: {
  id: string;
  selection: SelectionConfig<RegistryBindingSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<RegistryBindingSelect>(params.selection);
  return getClient()
    .registryBinding.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Binds a namespace to an installed container registry for a lane (realm) — the reconciler projects that registry's credentials into the namespace as an image pull secret
 *
 * @example
 * ```ts
 * await prefetchRegistryBindingQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchRegistryBindingQuery<S extends RegistryBindingSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, RegistryBindingSelect>;
  }
): Promise<void>;
export async function prefetchRegistryBindingQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<RegistryBindingSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<RegistryBindingSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: registryBindingKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .registryBinding.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
