/**
 * Records when a user achieves a level; prevents duplicate reward grants
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLevelGrantKeys } from '../query-keys';
import type { AppLevelGrantSelect, AppLevelGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLevelGrantSelect, AppLevelGrantWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appLevelGrantQueryKey = appLevelGrantKeys.detail;
/**
 * Records when a user achieves a level; prevents duplicate reward grants
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppLevelGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppLevelGrantQuery<
  S extends AppLevelGrantSelect,
  TData = {
    appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppLevelGrantSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppLevelGrantQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppLevelGrantSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppLevelGrantSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appLevelGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appLevelGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Records when a user achieves a level; prevents duplicate reward grants
 *
 * @example
 * ```ts
 * const data = await fetchAppLevelGrantQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppLevelGrantQuery<S extends AppLevelGrantSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppLevelGrantSelect>;
}): Promise<{
  appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S> | null;
}>;
export async function fetchAppLevelGrantQuery(params: {
  id: string;
  selection: SelectionConfig<AppLevelGrantSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppLevelGrantSelect>(params.selection);
  return getClient()
    .appLevelGrant.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Records when a user achieves a level; prevents duplicate reward grants
 *
 * @example
 * ```ts
 * await prefetchAppLevelGrantQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppLevelGrantQuery<S extends AppLevelGrantSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppLevelGrantSelect>;
  }
): Promise<void>;
export async function prefetchAppLevelGrantQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppLevelGrantSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppLevelGrantSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appLevelGrantKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appLevelGrant.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
