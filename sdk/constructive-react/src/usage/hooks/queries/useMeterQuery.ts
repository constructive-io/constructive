/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterKeys } from '../query-keys';
import type { MeterSelect, MeterWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterSelect, MeterWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterQueryKey = meterKeys.detail;
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMeterQuery<
  S extends MeterSelect,
  TData = {
    meter: InferSelectResult<MeterWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meter: InferSelectResult<MeterWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterQuery(
  params: {
    id: string;
    selection: SelectionConfig<MeterSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MeterSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meter.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```ts
 * const data = await fetchMeterQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMeterQuery<S extends MeterSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MeterSelect>;
}): Promise<{
  meter: InferSelectResult<MeterWithRelations, S> | null;
}>;
export async function fetchMeterQuery(params: {
  id: string;
  selection: SelectionConfig<MeterSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MeterSelect>(params.selection);
  return getClient()
    .meter.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```ts
 * await prefetchMeterQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMeterQuery<S extends MeterSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterSelect>;
  }
): Promise<void>;
export async function prefetchMeterQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MeterSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MeterSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: meterKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meter.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
