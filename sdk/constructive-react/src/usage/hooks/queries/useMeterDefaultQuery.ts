/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterDefaultKeys } from '../query-keys';
import type { MeterDefaultSelect, MeterDefaultWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterDefaultSelect, MeterDefaultWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterDefaultQueryKey = meterDefaultKeys.detail;
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterDefaultQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMeterDefaultQuery<
  S extends MeterDefaultSelect,
  TData = {
    meterDefault: InferSelectResult<MeterDefaultWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meterDefault: InferSelectResult<MeterDefaultWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterDefaultQuery(
  params: {
    id: string;
    selection: SelectionConfig<MeterDefaultSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MeterDefaultSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterDefaultKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meterDefault.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```ts
 * const data = await fetchMeterDefaultQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMeterDefaultQuery<S extends MeterDefaultSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
}): Promise<{
  meterDefault: InferSelectResult<MeterDefaultWithRelations, S> | null;
}>;
export async function fetchMeterDefaultQuery(params: {
  id: string;
  selection: SelectionConfig<MeterDefaultSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MeterDefaultSelect>(params.selection);
  return getClient()
    .meterDefault.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```ts
 * await prefetchMeterDefaultQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMeterDefaultQuery<S extends MeterDefaultSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  }
): Promise<void>;
export async function prefetchMeterDefaultQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MeterDefaultSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MeterDefaultSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: meterDefaultKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meterDefault.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
