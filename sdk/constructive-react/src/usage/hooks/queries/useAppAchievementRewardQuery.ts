/**
 * Defines rewards granted when a level is achieved; supports limit_credits and meter_credits
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appAchievementRewardKeys } from '../query-keys';
import type {
  AppAchievementRewardSelect,
  AppAchievementRewardWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppAchievementRewardSelect,
  AppAchievementRewardWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appAchievementRewardQueryKey = appAchievementRewardKeys.detail;
/**
 * Defines rewards granted when a level is achieved; supports limit_credits and meter_credits
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppAchievementRewardQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppAchievementRewardQuery<
  S extends AppAchievementRewardSelect,
  TData = {
    appAchievementReward: InferSelectResult<AppAchievementRewardWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppAchievementRewardSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appAchievementReward: InferSelectResult<AppAchievementRewardWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppAchievementRewardQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppAchievementRewardSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppAchievementRewardSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appAchievementRewardKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appAchievementReward.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines rewards granted when a level is achieved; supports limit_credits and meter_credits
 *
 * @example
 * ```ts
 * const data = await fetchAppAchievementRewardQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppAchievementRewardQuery<S extends AppAchievementRewardSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppAchievementRewardSelect>;
}): Promise<{
  appAchievementReward: InferSelectResult<AppAchievementRewardWithRelations, S> | null;
}>;
export async function fetchAppAchievementRewardQuery(params: {
  id: string;
  selection: SelectionConfig<AppAchievementRewardSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppAchievementRewardSelect>(params.selection);
  return getClient()
    .appAchievementReward.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Defines rewards granted when a level is achieved; supports limit_credits and meter_credits
 *
 * @example
 * ```ts
 * await prefetchAppAchievementRewardQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppAchievementRewardQuery<S extends AppAchievementRewardSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppAchievementRewardSelect>;
  }
): Promise<void>;
export async function prefetchAppAchievementRewardQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppAchievementRewardSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppAchievementRewardSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appAchievementRewardKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appAchievementReward.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
