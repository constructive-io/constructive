/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { meterDefaultKeys } from '../query-keys';
import type {
  MeterDefaultSelect,
  MeterDefaultWithRelations,
  MeterDefaultFilter,
  MeterDefaultOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  MeterDefaultSelect,
  MeterDefaultWithRelations,
  MeterDefaultFilter,
  MeterDefaultOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterDefaultsQueryKey = meterDefaultKeys.list;
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterDefaultsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useMeterDefaultsQuery<
  S extends MeterDefaultSelect,
  TData = {
    meterDefaults: ConnectionResult<InferSelectResult<MeterDefaultWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterDefaultFilter, MeterDefaultOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meterDefaults: ConnectionResult<InferSelectResult<MeterDefaultWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterDefaultsQuery(
  params: {
    selection: ListSelectionConfig<MeterDefaultSelect, MeterDefaultFilter, MeterDefaultOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<MeterDefaultSelect, MeterDefaultFilter, MeterDefaultOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterDefaultKeys.list(args),
    queryFn: () => getClient().meterDefault.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```ts
 * const data = await fetchMeterDefaultsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchMeterDefaultsQuery<S extends MeterDefaultSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, MeterDefaultFilter, MeterDefaultOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
}): Promise<{
  meterDefaults: ConnectionResult<InferSelectResult<MeterDefaultWithRelations, S>>;
}>;
export async function fetchMeterDefaultsQuery(params: {
  selection: ListSelectionConfig<MeterDefaultSelect, MeterDefaultFilter, MeterDefaultOrderBy>;
}) {
  const args = buildListSelectionArgs<MeterDefaultSelect, MeterDefaultFilter, MeterDefaultOrderBy>(
    params.selection
  );
  return getClient().meterDefault.findMany(args).unwrap();
}
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```ts
 * await prefetchMeterDefaultsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchMeterDefaultsQuery<S extends MeterDefaultSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterDefaultFilter, MeterDefaultOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  }
): Promise<void>;
export async function prefetchMeterDefaultsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<MeterDefaultSelect, MeterDefaultFilter, MeterDefaultOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<MeterDefaultSelect, MeterDefaultFilter, MeterDefaultOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: meterDefaultKeys.list(args),
    queryFn: () => getClient().meterDefault.findMany(args).unwrap(),
  });
}
