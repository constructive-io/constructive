/**
 * Defines plan tiers with named limit configurations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planKeys } from '../query-keys';
import type { PlanSelect, PlanWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanSelect, PlanWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const planQueryKey = planKeys.detail;
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlanQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlanQuery<
  S extends PlanSelect,
  TData = {
    plan: InferSelectResult<PlanWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanSelect>;
  } & Omit<
    UseQueryOptions<
      {
        plan: InferSelectResult<PlanWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlanQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlanSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlanSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .plan.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```ts
 * const data = await fetchPlanQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlanQuery<S extends PlanSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlanSelect>;
}): Promise<{
  plan: InferSelectResult<PlanWithRelations, S> | null;
}>;
export async function fetchPlanQuery(params: {
  id: string;
  selection: SelectionConfig<PlanSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlanSelect>(params.selection);
  return getClient()
    .plan.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```ts
 * await prefetchPlanQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlanQuery<S extends PlanSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanSelect>;
  }
): Promise<void>;
export async function prefetchPlanQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlanSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlanSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: planKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .plan.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
