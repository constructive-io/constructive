/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { meterKeys } from '../query-keys';
import type {
  MeterSelect,
  MeterWithRelations,
  MeterFilter,
  MeterOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  MeterSelect,
  MeterWithRelations,
  MeterFilter,
  MeterOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const metersQueryKey = meterKeys.list;
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMetersQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useMetersQuery<
  S extends MeterSelect,
  TData = {
    meters: ConnectionResult<InferSelectResult<MeterWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterFilter, MeterOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meters: ConnectionResult<InferSelectResult<MeterWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMetersQuery(
  params: {
    selection: ListSelectionConfig<MeterSelect, MeterFilter, MeterOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<MeterSelect, MeterFilter, MeterOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterKeys.list(args),
    queryFn: () => getClient().meter.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```ts
 * const data = await fetchMetersQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchMetersQuery<S extends MeterSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, MeterFilter, MeterOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, MeterSelect>;
}): Promise<{
  meters: ConnectionResult<InferSelectResult<MeterWithRelations, S>>;
}>;
export async function fetchMetersQuery(params: {
  selection: ListSelectionConfig<MeterSelect, MeterFilter, MeterOrderBy>;
}) {
  const args = buildListSelectionArgs<MeterSelect, MeterFilter, MeterOrderBy>(params.selection);
  return getClient().meter.findMany(args).unwrap();
}
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```ts
 * await prefetchMetersQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchMetersQuery<S extends MeterSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterFilter, MeterOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterSelect>;
  }
): Promise<void>;
export async function prefetchMetersQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<MeterSelect, MeterFilter, MeterOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<MeterSelect, MeterFilter, MeterOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: meterKeys.list(args),
    queryFn: () => getClient().meter.findMany(args).unwrap(),
  });
}
