/**
 * Custom query hook for captureTrustLadder
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
/** Query key factory - re-exported from query-keys.ts */
export const captureTrustLadderQueryKey = customQueryKeys.captureTrustLadder;
/**
 * Query hook for captureTrustLadder
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useCaptureTrustLadderQuery();
 *
 * if (data?.captureTrustLadder) {
 *   console.log(data.captureTrustLadder);
 * }
 * ```
 */
export function useCaptureTrustLadderQuery<
  TData = {
    captureTrustLadder: unknown | null;
  },
>(
  params?: Omit<
    UseQueryOptions<
      {
        captureTrustLadder: unknown | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useCaptureTrustLadderQuery<
  TData = {
    captureTrustLadder: unknown | null;
  },
>(
  params?: Omit<
    UseQueryOptions<
      {
        captureTrustLadder: unknown | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const queryOptions = params ?? {};
  return useQuery({
    queryKey: captureTrustLadderQueryKey(),
    queryFn: () => getClient().query.captureTrustLadder().unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch captureTrustLadder without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchCaptureTrustLadderQuery();
 * ```
 */
export async function fetchCaptureTrustLadderQuery() {
  return getClient().query.captureTrustLadder().unwrap();
}
/**
 * Prefetch captureTrustLadder for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchCaptureTrustLadderQuery(queryClient);
 * ```
 */
export async function prefetchCaptureTrustLadderQuery(queryClient: QueryClient): Promise<void> {
  await queryClient.prefetchQuery({
    queryKey: captureTrustLadderQueryKey(),
    queryFn: () => getClient().query.captureTrustLadder().unwrap(),
  });
}
