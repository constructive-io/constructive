/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { billingUsageSummaryKeys } from '../query-keys';
import type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const billingUsageSummaryQueryKey = billingUsageSummaryKeys.detail;
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBillingUsageSummaryQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useBillingUsageSummaryQuery<
  S extends BillingUsageSummarySelect,
  TData = {
    billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
  } & Omit<
    UseQueryOptions<
      {
        billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBillingUsageSummaryQuery(
  params: {
    id: string;
    selection: SelectionConfig<BillingUsageSummarySelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<BillingUsageSummarySelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: billingUsageSummaryKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .billingUsageSummary.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```ts
 * const data = await fetchBillingUsageSummaryQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchBillingUsageSummaryQuery<S extends BillingUsageSummarySelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
}): Promise<{
  billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S> | null;
}>;
export async function fetchBillingUsageSummaryQuery(params: {
  id: string;
  selection: SelectionConfig<BillingUsageSummarySelect>;
}): Promise<any> {
  const args = buildSelectionArgs<BillingUsageSummarySelect>(params.selection);
  return getClient()
    .billingUsageSummary.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```ts
 * await prefetchBillingUsageSummaryQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchBillingUsageSummaryQuery<S extends BillingUsageSummarySelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
  }
): Promise<void>;
export async function prefetchBillingUsageSummaryQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<BillingUsageSummarySelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<BillingUsageSummarySelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: billingUsageSummaryKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .billingUsageSummary.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
