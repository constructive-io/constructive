/**
 * Per-entity limit overrides that take precedence over plan defaults
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planOverrideKeys } from '../query-keys';
import type { PlanOverrideSelect, PlanOverrideWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanOverrideSelect, PlanOverrideWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const planOverrideQueryKey = planOverrideKeys.detail;
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlanOverrideQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlanOverrideQuery<
  S extends PlanOverrideSelect,
  TData = {
    planOverride: InferSelectResult<PlanOverrideWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
  } & Omit<
    UseQueryOptions<
      {
        planOverride: InferSelectResult<PlanOverrideWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlanOverrideQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlanOverrideSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlanOverrideSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planOverrideKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .planOverride.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```ts
 * const data = await fetchPlanOverrideQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlanOverrideQuery<S extends PlanOverrideSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
}): Promise<{
  planOverride: InferSelectResult<PlanOverrideWithRelations, S> | null;
}>;
export async function fetchPlanOverrideQuery(params: {
  id: string;
  selection: SelectionConfig<PlanOverrideSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlanOverrideSelect>(params.selection);
  return getClient()
    .planOverride.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```ts
 * await prefetchPlanOverrideQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlanOverrideQuery<S extends PlanOverrideSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
  }
): Promise<void>;
export async function prefetchPlanOverrideQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlanOverrideSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlanOverrideSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: planOverrideKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .planOverride.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
