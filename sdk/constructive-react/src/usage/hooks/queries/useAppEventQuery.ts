/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appEventKeys } from '../query-keys';
import type { AppEventSelect, AppEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppEventSelect, AppEventWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appEventQueryKey = appEventKeys.detail;
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useAppEventQuery<
  S extends AppEventSelect,
  TData = {
    appEvent: InferSelectResult<AppEventWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appEvent: InferSelectResult<AppEventWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppEventQuery(
  params: {
    id: string;
    selection: SelectionConfig<AppEventSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<AppEventSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```ts
 * const data = await fetchAppEventQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchAppEventQuery<S extends AppEventSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, AppEventSelect>;
}): Promise<{
  appEvent: InferSelectResult<AppEventWithRelations, S> | null;
}>;
export async function fetchAppEventQuery(params: {
  id: string;
  selection: SelectionConfig<AppEventSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<AppEventSelect>(params.selection);
  return getClient()
    .appEvent.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```ts
 * await prefetchAppEventQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchAppEventQuery<S extends AppEventSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, AppEventSelect>;
  }
): Promise<void>;
export async function prefetchAppEventQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<AppEventSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<AppEventSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: appEventKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .appEvent.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
