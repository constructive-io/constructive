/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { meterSourceKeys } from '../query-keys';
import type {
  MeterSourceSelect,
  MeterSourceWithRelations,
  MeterSourceFilter,
  MeterSourceOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  MeterSourceSelect,
  MeterSourceWithRelations,
  MeterSourceFilter,
  MeterSourceOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterSourcesQueryKey = meterSourceKeys.list;
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterSourcesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useMeterSourcesQuery<
  S extends MeterSourceSelect,
  TData = {
    meterSources: ConnectionResult<InferSelectResult<MeterSourceWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterSourceFilter, MeterSourceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meterSources: ConnectionResult<InferSelectResult<MeterSourceWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterSourcesQuery(
  params: {
    selection: ListSelectionConfig<MeterSourceSelect, MeterSourceFilter, MeterSourceOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<MeterSourceSelect, MeterSourceFilter, MeterSourceOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterSourceKeys.list(args),
    queryFn: () => getClient().meterSource.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```ts
 * const data = await fetchMeterSourcesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchMeterSourcesQuery<S extends MeterSourceSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, MeterSourceFilter, MeterSourceOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
}): Promise<{
  meterSources: ConnectionResult<InferSelectResult<MeterSourceWithRelations, S>>;
}>;
export async function fetchMeterSourcesQuery(params: {
  selection: ListSelectionConfig<MeterSourceSelect, MeterSourceFilter, MeterSourceOrderBy>;
}) {
  const args = buildListSelectionArgs<MeterSourceSelect, MeterSourceFilter, MeterSourceOrderBy>(
    params.selection
  );
  return getClient().meterSource.findMany(args).unwrap();
}
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```ts
 * await prefetchMeterSourcesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchMeterSourcesQuery<S extends MeterSourceSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterSourceFilter, MeterSourceOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
  }
): Promise<void>;
export async function prefetchMeterSourcesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<MeterSourceSelect, MeterSourceFilter, MeterSourceOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<MeterSourceSelect, MeterSourceFilter, MeterSourceOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: meterSourceKeys.list(args),
    queryFn: () => getClient().meterSource.findMany(args).unwrap(),
  });
}
