/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { planCapKeys } from '../query-keys';
import type {
  PlanCapSelect,
  PlanCapWithRelations,
  PlanCapFilter,
  PlanCapOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlanCapSelect,
  PlanCapWithRelations,
  PlanCapFilter,
  PlanCapOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const planCapsQueryKey = planCapKeys.list;
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlanCapsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlanCapsQuery<
  S extends PlanCapSelect,
  TData = {
    planCaps: ConnectionResult<InferSelectResult<PlanCapWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlanCapFilter, PlanCapOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlanCapSelect>;
  } & Omit<
    UseQueryOptions<
      {
        planCaps: ConnectionResult<InferSelectResult<PlanCapWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlanCapsQuery(
  params: {
    selection: ListSelectionConfig<PlanCapSelect, PlanCapFilter, PlanCapOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<PlanCapSelect, PlanCapFilter, PlanCapOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planCapKeys.list(args),
    queryFn: () => getClient().planCap.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```ts
 * const data = await fetchPlanCapsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlanCapsQuery<S extends PlanCapSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlanCapFilter, PlanCapOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlanCapSelect>;
}): Promise<{
  planCaps: ConnectionResult<InferSelectResult<PlanCapWithRelations, S>>;
}>;
export async function fetchPlanCapsQuery(params: {
  selection: ListSelectionConfig<PlanCapSelect, PlanCapFilter, PlanCapOrderBy>;
}) {
  const args = buildListSelectionArgs<PlanCapSelect, PlanCapFilter, PlanCapOrderBy>(
    params.selection
  );
  return getClient().planCap.findMany(args).unwrap();
}
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```ts
 * await prefetchPlanCapsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlanCapsQuery<S extends PlanCapSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlanCapFilter, PlanCapOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlanCapSelect>;
  }
): Promise<void>;
export async function prefetchPlanCapsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<PlanCapSelect, PlanCapFilter, PlanCapOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<PlanCapSelect, PlanCapFilter, PlanCapOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: planCapKeys.list(args),
    queryFn: () => getClient().planCap.findMany(args).unwrap(),
  });
}
