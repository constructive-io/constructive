/**
 * Defines plan tiers with named limit configurations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { planKeys } from '../query-keys';
import type { PlanSelect, PlanWithRelations, PlanFilter, PlanOrderBy } from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type { PlanSelect, PlanWithRelations, PlanFilter, PlanOrderBy } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const plansQueryKey = planKeys.list;
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlansQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlansQuery<
  S extends PlanSelect,
  TData = {
    plans: ConnectionResult<InferSelectResult<PlanWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlanFilter, PlanOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlanSelect>;
  } & Omit<
    UseQueryOptions<
      {
        plans: ConnectionResult<InferSelectResult<PlanWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlansQuery(
  params: {
    selection: ListSelectionConfig<PlanSelect, PlanFilter, PlanOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<PlanSelect, PlanFilter, PlanOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planKeys.list(args),
    queryFn: () => getClient().plan.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```ts
 * const data = await fetchPlansQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlansQuery<S extends PlanSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlanFilter, PlanOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlanSelect>;
}): Promise<{
  plans: ConnectionResult<InferSelectResult<PlanWithRelations, S>>;
}>;
export async function fetchPlansQuery(params: {
  selection: ListSelectionConfig<PlanSelect, PlanFilter, PlanOrderBy>;
}) {
  const args = buildListSelectionArgs<PlanSelect, PlanFilter, PlanOrderBy>(params.selection);
  return getClient().plan.findMany(args).unwrap();
}
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```ts
 * await prefetchPlansQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlansQuery<S extends PlanSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlanFilter, PlanOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlanSelect>;
  }
): Promise<void>;
export async function prefetchPlansQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<PlanSelect, PlanFilter, PlanOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<PlanSelect, PlanFilter, PlanOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: planKeys.list(args),
    queryFn: () => getClient().plan.findMany(args).unwrap(),
  });
}
