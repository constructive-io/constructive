/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planCapKeys } from '../query-keys';
import type { PlanCapSelect, PlanCapWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanCapSelect, PlanCapWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const planCapQueryKey = planCapKeys.detail;
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlanCapQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlanCapQuery<
  S extends PlanCapSelect,
  TData = {
    planCap: InferSelectResult<PlanCapWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanCapSelect>;
  } & Omit<
    UseQueryOptions<
      {
        planCap: InferSelectResult<PlanCapWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlanCapQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlanCapSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlanCapSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planCapKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .planCap.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```ts
 * const data = await fetchPlanCapQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlanCapQuery<S extends PlanCapSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlanCapSelect>;
}): Promise<{
  planCap: InferSelectResult<PlanCapWithRelations, S> | null;
}>;
export async function fetchPlanCapQuery(params: {
  id: string;
  selection: SelectionConfig<PlanCapSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlanCapSelect>(params.selection);
  return getClient()
    .planCap.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```ts
 * await prefetchPlanCapQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlanCapQuery<S extends PlanCapSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanCapSelect>;
  }
): Promise<void>;
export async function prefetchPlanCapQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlanCapSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlanCapSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: planCapKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .planCap.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
