/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { appEventKeys } from '../query-keys';
import type {
  AppEventSelect,
  AppEventWithRelations,
  AppEventFilter,
  AppEventOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  AppEventSelect,
  AppEventWithRelations,
  AppEventFilter,
  AppEventOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const appEventsQueryKey = appEventKeys.list;
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useAppEventsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useAppEventsQuery<
  S extends AppEventSelect,
  TData = {
    appEvents: ConnectionResult<InferSelectResult<AppEventWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppEventFilter, AppEventOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppEventSelect>;
  } & Omit<
    UseQueryOptions<
      {
        appEvents: ConnectionResult<InferSelectResult<AppEventWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useAppEventsQuery(
  params: {
    selection: ListSelectionConfig<AppEventSelect, AppEventFilter, AppEventOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<AppEventSelect, AppEventFilter, AppEventOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: appEventKeys.list(args),
    queryFn: () => getClient().appEvent.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```ts
 * const data = await fetchAppEventsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchAppEventsQuery<S extends AppEventSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, AppEventFilter, AppEventOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, AppEventSelect>;
}): Promise<{
  appEvents: ConnectionResult<InferSelectResult<AppEventWithRelations, S>>;
}>;
export async function fetchAppEventsQuery(params: {
  selection: ListSelectionConfig<AppEventSelect, AppEventFilter, AppEventOrderBy>;
}) {
  const args = buildListSelectionArgs<AppEventSelect, AppEventFilter, AppEventOrderBy>(
    params.selection
  );
  return getClient().appEvent.findMany(args).unwrap();
}
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```ts
 * await prefetchAppEventsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchAppEventsQuery<S extends AppEventSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, AppEventFilter, AppEventOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, AppEventSelect>;
  }
): Promise<void>;
export async function prefetchAppEventsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<AppEventSelect, AppEventFilter, AppEventOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<AppEventSelect, AppEventFilter, AppEventOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: appEventKeys.list(args),
    queryFn: () => getClient().appEvent.findMany(args).unwrap(),
  });
}
