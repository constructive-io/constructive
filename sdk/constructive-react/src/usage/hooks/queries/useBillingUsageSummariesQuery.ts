/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { billingUsageSummaryKeys } from '../query-keys';
import type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
  BillingUsageSummaryFilter,
  BillingUsageSummaryOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
  BillingUsageSummaryFilter,
  BillingUsageSummaryOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const billingUsageSummariesQueryKey = billingUsageSummaryKeys.list;
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useBillingUsageSummariesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useBillingUsageSummariesQuery<
  S extends BillingUsageSummarySelect,
  TData = {
    billingUsageSummaries: ConnectionResult<InferSelectResult<BillingUsageSummaryWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, BillingUsageSummaryFilter, BillingUsageSummaryOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
  } & Omit<
    UseQueryOptions<
      {
        billingUsageSummaries: ConnectionResult<
          InferSelectResult<BillingUsageSummaryWithRelations, S>
        >;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useBillingUsageSummariesQuery(
  params: {
    selection: ListSelectionConfig<
      BillingUsageSummarySelect,
      BillingUsageSummaryFilter,
      BillingUsageSummaryOrderBy
    >;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<
    BillingUsageSummarySelect,
    BillingUsageSummaryFilter,
    BillingUsageSummaryOrderBy
  >(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: billingUsageSummaryKeys.list(args),
    queryFn: () => getClient().billingUsageSummary.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```ts
 * const data = await fetchBillingUsageSummariesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchBillingUsageSummariesQuery<S extends BillingUsageSummarySelect>(params: {
  selection: {
    fields: S;
  } & Omit<
    ListSelectionConfig<S, BillingUsageSummaryFilter, BillingUsageSummaryOrderBy>,
    'fields'
  > &
    HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
}): Promise<{
  billingUsageSummaries: ConnectionResult<InferSelectResult<BillingUsageSummaryWithRelations, S>>;
}>;
export async function fetchBillingUsageSummariesQuery(params: {
  selection: ListSelectionConfig<
    BillingUsageSummarySelect,
    BillingUsageSummaryFilter,
    BillingUsageSummaryOrderBy
  >;
}) {
  const args = buildListSelectionArgs<
    BillingUsageSummarySelect,
    BillingUsageSummaryFilter,
    BillingUsageSummaryOrderBy
  >(params.selection);
  return getClient().billingUsageSummary.findMany(args).unwrap();
}
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```ts
 * await prefetchBillingUsageSummariesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchBillingUsageSummariesQuery<S extends BillingUsageSummarySelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<
      ListSelectionConfig<S, BillingUsageSummaryFilter, BillingUsageSummaryOrderBy>,
      'fields'
    > &
      HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
  }
): Promise<void>;
export async function prefetchBillingUsageSummariesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<
      BillingUsageSummarySelect,
      BillingUsageSummaryFilter,
      BillingUsageSummaryOrderBy
    >;
  }
): Promise<void> {
  const args = buildListSelectionArgs<
    BillingUsageSummarySelect,
    BillingUsageSummaryFilter,
    BillingUsageSummaryOrderBy
  >(params.selection);
  await queryClient.prefetchQuery({
    queryKey: billingUsageSummaryKeys.list(args),
    queryFn: () => getClient().billingUsageSummary.findMany(args).unwrap(),
  });
}
