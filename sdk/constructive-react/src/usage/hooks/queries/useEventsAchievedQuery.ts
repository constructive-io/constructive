/**
 * Custom query hook for eventsAchieved
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
import type { EventsAchievedVariables } from '../../orm/query';
export type { EventsAchievedVariables } from '../../orm/query';
/** Query key factory - re-exported from query-keys.ts */
export const eventsAchievedQueryKey = customQueryKeys.eventsAchieved;
/**
 * Query hook for eventsAchieved
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEventsAchievedQuery({ variables: { level, roleId } });
 *
 * if (data?.eventsAchieved) {
 *   console.log(data.eventsAchieved);
 * }
 * ```
 */
export function useEventsAchievedQuery<
  TData = {
    eventsAchieved: boolean | null;
  },
>(
  params?: {
    variables?: EventsAchievedVariables;
  } & Omit<
    UseQueryOptions<
      {
        eventsAchieved: boolean | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEventsAchievedQuery<
  TData = {
    eventsAchieved: boolean | null;
  },
>(
  params?: {
    variables?: EventsAchievedVariables;
  } & Omit<
    UseQueryOptions<
      {
        eventsAchieved: boolean | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const variables = params?.variables ?? {};
  const { variables: _variables, ...queryOptions } = params ?? {};
  void _variables;
  return useQuery({
    queryKey: eventsAchievedQueryKey(variables),
    queryFn: () => getClient().query.eventsAchieved(variables).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch eventsAchieved without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchEventsAchievedQuery({ variables: { level, roleId } });
 * ```
 */
export async function fetchEventsAchievedQuery(params?: { variables?: EventsAchievedVariables }) {
  const variables = params?.variables ?? {};
  return getClient().query.eventsAchieved(variables).unwrap();
}
/**
 * Prefetch eventsAchieved for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchEventsAchievedQuery(queryClient, { variables: { level, roleId } });
 * ```
 */
export async function prefetchEventsAchievedQuery(
  queryClient: QueryClient,
  params?: {
    variables?: EventsAchievedVariables;
  }
): Promise<void> {
  const variables = params?.variables ?? {};
  await queryClient.prefetchQuery({
    queryKey: eventsAchievedQueryKey(variables),
    queryFn: () => getClient().query.eventsAchieved(variables).unwrap(),
  });
}
