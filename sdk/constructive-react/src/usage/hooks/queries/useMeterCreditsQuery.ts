/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { meterCreditKeys } from '../query-keys';
import type {
  MeterCreditSelect,
  MeterCreditWithRelations,
  MeterCreditFilter,
  MeterCreditOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  MeterCreditSelect,
  MeterCreditWithRelations,
  MeterCreditFilter,
  MeterCreditOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterCreditsQueryKey = meterCreditKeys.list;
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterCreditsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useMeterCreditsQuery<
  S extends MeterCreditSelect,
  TData = {
    meterCredits: ConnectionResult<InferSelectResult<MeterCreditWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterCreditFilter, MeterCreditOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meterCredits: ConnectionResult<InferSelectResult<MeterCreditWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterCreditsQuery(
  params: {
    selection: ListSelectionConfig<MeterCreditSelect, MeterCreditFilter, MeterCreditOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<MeterCreditSelect, MeterCreditFilter, MeterCreditOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterCreditKeys.list(args),
    queryFn: () => getClient().meterCredit.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```ts
 * const data = await fetchMeterCreditsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchMeterCreditsQuery<S extends MeterCreditSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, MeterCreditFilter, MeterCreditOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
}): Promise<{
  meterCredits: ConnectionResult<InferSelectResult<MeterCreditWithRelations, S>>;
}>;
export async function fetchMeterCreditsQuery(params: {
  selection: ListSelectionConfig<MeterCreditSelect, MeterCreditFilter, MeterCreditOrderBy>;
}) {
  const args = buildListSelectionArgs<MeterCreditSelect, MeterCreditFilter, MeterCreditOrderBy>(
    params.selection
  );
  return getClient().meterCredit.findMany(args).unwrap();
}
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```ts
 * await prefetchMeterCreditsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchMeterCreditsQuery<S extends MeterCreditSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, MeterCreditFilter, MeterCreditOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  }
): Promise<void>;
export async function prefetchMeterCreditsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<MeterCreditSelect, MeterCreditFilter, MeterCreditOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<MeterCreditSelect, MeterCreditFilter, MeterCreditOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: meterCreditKeys.list(args),
    queryFn: () => getClient().meterCredit.findMany(args).unwrap(),
  });
}
