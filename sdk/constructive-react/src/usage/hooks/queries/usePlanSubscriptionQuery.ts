/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planSubscriptionKeys } from '../query-keys';
import type { PlanSubscriptionSelect, PlanSubscriptionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanSubscriptionSelect, PlanSubscriptionWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const planSubscriptionQueryKey = planSubscriptionKeys.detail;
/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlanSubscriptionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function usePlanSubscriptionQuery<
  S extends PlanSubscriptionSelect,
  TData = {
    planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanSubscriptionSelect>;
  } & Omit<
    UseQueryOptions<
      {
        planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlanSubscriptionQuery(
  params: {
    id: string;
    selection: SelectionConfig<PlanSubscriptionSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<PlanSubscriptionSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planSubscriptionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .planSubscription.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 *
 * @example
 * ```ts
 * const data = await fetchPlanSubscriptionQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchPlanSubscriptionQuery<S extends PlanSubscriptionSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, PlanSubscriptionSelect>;
}): Promise<{
  planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S> | null;
}>;
export async function fetchPlanSubscriptionQuery(params: {
  id: string;
  selection: SelectionConfig<PlanSubscriptionSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<PlanSubscriptionSelect>(params.selection);
  return getClient()
    .planSubscription.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 *
 * @example
 * ```ts
 * await prefetchPlanSubscriptionQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchPlanSubscriptionQuery<S extends PlanSubscriptionSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, PlanSubscriptionSelect>;
  }
): Promise<void>;
export async function prefetchPlanSubscriptionQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<PlanSubscriptionSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<PlanSubscriptionSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: planSubscriptionKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .planSubscription.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
