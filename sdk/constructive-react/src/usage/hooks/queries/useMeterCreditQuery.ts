/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterCreditKeys } from '../query-keys';
import type { MeterCreditSelect, MeterCreditWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterCreditSelect, MeterCreditWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterCreditQueryKey = meterCreditKeys.detail;
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterCreditQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMeterCreditQuery<
  S extends MeterCreditSelect,
  TData = {
    meterCredit: InferSelectResult<MeterCreditWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meterCredit: InferSelectResult<MeterCreditWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterCreditQuery(
  params: {
    id: string;
    selection: SelectionConfig<MeterCreditSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MeterCreditSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterCreditKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meterCredit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```ts
 * const data = await fetchMeterCreditQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMeterCreditQuery<S extends MeterCreditSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
}): Promise<{
  meterCredit: InferSelectResult<MeterCreditWithRelations, S> | null;
}>;
export async function fetchMeterCreditQuery(params: {
  id: string;
  selection: SelectionConfig<MeterCreditSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MeterCreditSelect>(params.selection);
  return getClient()
    .meterCredit.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```ts
 * await prefetchMeterCreditQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMeterCreditQuery<S extends MeterCreditSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  }
): Promise<void>;
export async function prefetchMeterCreditQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MeterCreditSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MeterCreditSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: meterCreditKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meterCredit.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
