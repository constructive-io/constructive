/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { ledgerKeys } from '../query-keys';
import type {
  LedgerSelect,
  LedgerWithRelations,
  LedgerFilter,
  LedgerOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  LedgerSelect,
  LedgerWithRelations,
  LedgerFilter,
  LedgerOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const ledgersQueryKey = ledgerKeys.list;
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useLedgersQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useLedgersQuery<
  S extends LedgerSelect,
  TData = {
    ledgers: ConnectionResult<InferSelectResult<LedgerWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, LedgerFilter, LedgerOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, LedgerSelect>;
  } & Omit<
    UseQueryOptions<
      {
        ledgers: ConnectionResult<InferSelectResult<LedgerWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useLedgersQuery(
  params: {
    selection: ListSelectionConfig<LedgerSelect, LedgerFilter, LedgerOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<LedgerSelect, LedgerFilter, LedgerOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: ledgerKeys.list(args),
    queryFn: () => getClient().ledger.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```ts
 * const data = await fetchLedgersQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchLedgersQuery<S extends LedgerSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, LedgerFilter, LedgerOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, LedgerSelect>;
}): Promise<{
  ledgers: ConnectionResult<InferSelectResult<LedgerWithRelations, S>>;
}>;
export async function fetchLedgersQuery(params: {
  selection: ListSelectionConfig<LedgerSelect, LedgerFilter, LedgerOrderBy>;
}) {
  const args = buildListSelectionArgs<LedgerSelect, LedgerFilter, LedgerOrderBy>(params.selection);
  return getClient().ledger.findMany(args).unwrap();
}
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```ts
 * await prefetchLedgersQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchLedgersQuery<S extends LedgerSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, LedgerFilter, LedgerOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, LedgerSelect>;
  }
): Promise<void>;
export async function prefetchLedgersQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<LedgerSelect, LedgerFilter, LedgerOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<LedgerSelect, LedgerFilter, LedgerOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: ledgerKeys.list(args),
    queryFn: () => getClient().ledger.findMany(args).unwrap(),
  });
}
