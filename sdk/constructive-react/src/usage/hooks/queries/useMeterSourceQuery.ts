/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterSourceKeys } from '../query-keys';
import type { MeterSourceSelect, MeterSourceWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterSourceSelect, MeterSourceWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const meterSourceQueryKey = meterSourceKeys.detail;
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useMeterSourceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useMeterSourceQuery<
  S extends MeterSourceSelect,
  TData = {
    meterSource: InferSelectResult<MeterSourceWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
  } & Omit<
    UseQueryOptions<
      {
        meterSource: InferSelectResult<MeterSourceWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useMeterSourceQuery(
  params: {
    id: string;
    selection: SelectionConfig<MeterSourceSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<MeterSourceSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: meterSourceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meterSource.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```ts
 * const data = await fetchMeterSourceQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchMeterSourceQuery<S extends MeterSourceSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
}): Promise<{
  meterSource: InferSelectResult<MeterSourceWithRelations, S> | null;
}>;
export async function fetchMeterSourceQuery(params: {
  id: string;
  selection: SelectionConfig<MeterSourceSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<MeterSourceSelect>(params.selection);
  return getClient()
    .meterSource.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```ts
 * await prefetchMeterSourceQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchMeterSourceQuery<S extends MeterSourceSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
  }
): Promise<void>;
export async function prefetchMeterSourceQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<MeterSourceSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<MeterSourceSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: meterSourceKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .meterSource.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
