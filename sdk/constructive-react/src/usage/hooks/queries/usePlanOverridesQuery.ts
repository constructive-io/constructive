/**
 * Per-entity limit overrides that take precedence over plan defaults
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { planOverrideKeys } from '../query-keys';
import type {
  PlanOverrideSelect,
  PlanOverrideWithRelations,
  PlanOverrideFilter,
  PlanOverrideOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  PlanOverrideSelect,
  PlanOverrideWithRelations,
  PlanOverrideFilter,
  PlanOverrideOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const planOverridesQueryKey = planOverrideKeys.list;
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```tsx
 * const { data, isLoading } = usePlanOverridesQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function usePlanOverridesQuery<
  S extends PlanOverrideSelect,
  TData = {
    planOverrides: ConnectionResult<InferSelectResult<PlanOverrideWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlanOverrideFilter, PlanOverrideOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
  } & Omit<
    UseQueryOptions<
      {
        planOverrides: ConnectionResult<InferSelectResult<PlanOverrideWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function usePlanOverridesQuery(
  params: {
    selection: ListSelectionConfig<PlanOverrideSelect, PlanOverrideFilter, PlanOverrideOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<PlanOverrideSelect, PlanOverrideFilter, PlanOverrideOrderBy>(
    params.selection
  );
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: planOverrideKeys.list(args),
    queryFn: () => getClient().planOverride.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```ts
 * const data = await fetchPlanOverridesQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchPlanOverridesQuery<S extends PlanOverrideSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, PlanOverrideFilter, PlanOverrideOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
}): Promise<{
  planOverrides: ConnectionResult<InferSelectResult<PlanOverrideWithRelations, S>>;
}>;
export async function fetchPlanOverridesQuery(params: {
  selection: ListSelectionConfig<PlanOverrideSelect, PlanOverrideFilter, PlanOverrideOrderBy>;
}) {
  const args = buildListSelectionArgs<PlanOverrideSelect, PlanOverrideFilter, PlanOverrideOrderBy>(
    params.selection
  );
  return getClient().planOverride.findMany(args).unwrap();
}
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```ts
 * await prefetchPlanOverridesQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchPlanOverridesQuery<S extends PlanOverrideSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, PlanOverrideFilter, PlanOverrideOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
  }
): Promise<void>;
export async function prefetchPlanOverridesQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<PlanOverrideSelect, PlanOverrideFilter, PlanOverrideOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<PlanOverrideSelect, PlanOverrideFilter, PlanOverrideOrderBy>(
    params.selection
  );
  await queryClient.prefetchQuery({
    queryKey: planOverrideKeys.list(args),
    queryFn: () => getClient().planOverride.findMany(args).unwrap(),
  });
}
