/**
 * Custom query hook for eventsRequired
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { customQueryKeys } from '../query-keys';
import type { EventsRequiredVariables } from '../../orm/query';
import type { AppLevelRequirementConnection } from '../../orm/input-types';
export type { EventsRequiredVariables } from '../../orm/query';
/** Query key factory - re-exported from query-keys.ts */
export const eventsRequiredQueryKey = customQueryKeys.eventsRequired;
/**
 * Reads and enables pagination through a set of `AppLevelRequirement`.
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useEventsRequiredQuery({ variables: { after, first, level, offset, roleId } });
 *
 * if (data?.eventsRequired) {
 *   console.log(data.eventsRequired);
 * }
 * ```
 */
export function useEventsRequiredQuery<
  TData = {
    eventsRequired: AppLevelRequirementConnection | null;
  },
>(
  params?: {
    variables?: EventsRequiredVariables;
  } & Omit<
    UseQueryOptions<
      {
        eventsRequired: AppLevelRequirementConnection | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useEventsRequiredQuery<
  TData = {
    eventsRequired: AppLevelRequirementConnection | null;
  },
>(
  params?: {
    variables?: EventsRequiredVariables;
  } & Omit<
    UseQueryOptions<
      {
        eventsRequired: AppLevelRequirementConnection | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData> {
  const variables = params?.variables ?? {};
  const { variables: _variables, ...queryOptions } = params ?? {};
  void _variables;
  return useQuery({
    queryKey: eventsRequiredQueryKey(variables),
    queryFn: () => getClient().query.eventsRequired(variables).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch eventsRequired without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchEventsRequiredQuery({ variables: { after, first, level, offset, roleId } });
 * ```
 */
export async function fetchEventsRequiredQuery(params?: { variables?: EventsRequiredVariables }) {
  const variables = params?.variables ?? {};
  return getClient().query.eventsRequired(variables).unwrap();
}
/**
 * Prefetch eventsRequired for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchEventsRequiredQuery(queryClient, { variables: { after, first, level, offset, roleId } });
 * ```
 */
export async function prefetchEventsRequiredQuery(
  queryClient: QueryClient,
  params?: {
    variables?: EventsRequiredVariables;
  }
): Promise<void> {
  const variables = params?.variables ?? {};
  await queryClient.prefetchQuery({
    queryKey: eventsRequiredQueryKey(variables),
    queryFn: () => getClient().query.eventsRequired(variables).unwrap(),
  });
}
