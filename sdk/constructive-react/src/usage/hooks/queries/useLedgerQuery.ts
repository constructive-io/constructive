/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { ledgerKeys } from '../query-keys';
import type { LedgerSelect, LedgerWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { LedgerSelect, LedgerWithRelations } from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const ledgerQueryKey = ledgerKeys.detail;
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useLedgerQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true, name: true } },
 * });
 * ```
 */
export function useLedgerQuery<
  S extends LedgerSelect,
  TData = {
    ledger: InferSelectResult<LedgerWithRelations, S> | null;
  },
>(
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, LedgerSelect>;
  } & Omit<
    UseQueryOptions<
      {
        ledger: InferSelectResult<LedgerWithRelations, S> | null;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useLedgerQuery(
  params: {
    id: string;
    selection: SelectionConfig<LedgerSelect>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildSelectionArgs<LedgerSelect>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: ledgerKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .ledger.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
    ...queryOptions,
  });
}
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```ts
 * const data = await fetchLedgerQuery({
 *   id: 'some-id',
 *   selection: { fields: { id: true } },
 * });
 * ```
 */
export async function fetchLedgerQuery<S extends LedgerSelect>(params: {
  id: string;
  selection: {
    fields: S;
  } & HookStrictSelect<NoInfer<S>, LedgerSelect>;
}): Promise<{
  ledger: InferSelectResult<LedgerWithRelations, S> | null;
}>;
export async function fetchLedgerQuery(params: {
  id: string;
  selection: SelectionConfig<LedgerSelect>;
}): Promise<any> {
  const args = buildSelectionArgs<LedgerSelect>(params.selection);
  return getClient()
    .ledger.findOne({
      id: params.id,
      select: args.select,
    })
    .unwrap();
}
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```ts
 * await prefetchLedgerQuery(queryClient, { id: 'some-id', selection: { fields: { id: true } } });
 * ```
 */
export async function prefetchLedgerQuery<S extends LedgerSelect>(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: {
      fields: S;
    } & HookStrictSelect<NoInfer<S>, LedgerSelect>;
  }
): Promise<void>;
export async function prefetchLedgerQuery(
  queryClient: QueryClient,
  params: {
    id: string;
    selection: SelectionConfig<LedgerSelect>;
  }
): Promise<void> {
  const args = buildSelectionArgs<LedgerSelect>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: ledgerKeys.detail(params.id),
    queryFn: () =>
      getClient()
        .ledger.findOne({
          id: params.id,
          select: args.select,
        })
        .unwrap(),
  });
}
