/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planSubscriptionKeys } from '../query-keys';
import { planSubscriptionMutationKeys } from '../mutation-keys';
import type {
  PlanSubscriptionSelect,
  PlanSubscriptionWithRelations,
  CreatePlanSubscriptionInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlanSubscriptionSelect,
  PlanSubscriptionWithRelations,
  CreatePlanSubscriptionInput,
} from '../../orm/input-types';
/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlanSubscriptionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlanSubscriptionMutation<S extends PlanSubscriptionSelect>(
  params: {
    selection: {
      fields: S & PlanSubscriptionSelect;
    } & HookStrictSelect<NoInfer<S>, PlanSubscriptionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlanSubscription: {
          planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S>;
        };
      },
      Error,
      CreatePlanSubscriptionInput['planSubscription']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlanSubscription: {
      planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S>;
    };
  },
  Error,
  CreatePlanSubscriptionInput['planSubscription']
>;
export function useCreatePlanSubscriptionMutation(
  params: {
    selection: SelectionConfig<PlanSubscriptionSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreatePlanSubscriptionInput['planSubscription']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlanSubscriptionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planSubscriptionMutationKeys.create(),
    mutationFn: (data: CreatePlanSubscriptionInput['planSubscription']) =>
      getClient()
        .planSubscription.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: planSubscriptionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
