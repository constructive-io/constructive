/**
 * Records when a user achieves a level; prevents duplicate reward grants
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLevelGrantKeys } from '../query-keys';
import { appLevelGrantMutationKeys } from '../mutation-keys';
import type {
  AppLevelGrantSelect,
  AppLevelGrantWithRelations,
  AppLevelGrantPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  AppLevelGrantSelect,
  AppLevelGrantWithRelations,
  AppLevelGrantPatch,
} from '../../orm/input-types';
/**
 * Records when a user achieves a level; prevents duplicate reward grants
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppLevelGrantMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appLevelGrantPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppLevelGrantMutation<S extends AppLevelGrantSelect>(
  params: {
    selection: {
      fields: S & AppLevelGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppLevelGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppLevelGrant: {
          appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        appLevelGrantPatch: AppLevelGrantPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppLevelGrant: {
      appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    appLevelGrantPatch: AppLevelGrantPatch;
  }
>;
export function useUpdateAppLevelGrantMutation(
  params: {
    selection: SelectionConfig<AppLevelGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        appLevelGrantPatch: AppLevelGrantPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLevelGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLevelGrantMutationKeys.all,
    mutationFn: ({
      id,
      appLevelGrantPatch,
    }: {
      id: string;
      appLevelGrantPatch: AppLevelGrantPatch;
    }) =>
      getClient()
        .appLevelGrant.update({
          where: {
            id,
          },
          data: appLevelGrantPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appLevelGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLevelGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
