/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterDefaultKeys } from '../query-keys';
import { meterDefaultMutationKeys } from '../mutation-keys';
import type {
  MeterDefaultSelect,
  MeterDefaultWithRelations,
  CreateMeterDefaultInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  MeterDefaultSelect,
  MeterDefaultWithRelations,
  CreateMeterDefaultInput,
} from '../../orm/input-types';
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateMeterDefaultMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateMeterDefaultMutation<S extends MeterDefaultSelect>(
  params: {
    selection: {
      fields: S & MeterDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createMeterDefault: {
          meterDefault: InferSelectResult<MeterDefaultWithRelations, S>;
        };
      },
      Error,
      CreateMeterDefaultInput['meterDefault']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createMeterDefault: {
      meterDefault: InferSelectResult<MeterDefaultWithRelations, S>;
    };
  },
  Error,
  CreateMeterDefaultInput['meterDefault']
>;
export function useCreateMeterDefaultMutation(
  params: {
    selection: SelectionConfig<MeterDefaultSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateMeterDefaultInput['meterDefault']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<MeterDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterDefaultMutationKeys.create(),
    mutationFn: (data: CreateMeterDefaultInput['meterDefault']) =>
      getClient()
        .meterDefault.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: meterDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
