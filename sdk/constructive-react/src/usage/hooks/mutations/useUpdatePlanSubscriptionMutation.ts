/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planSubscriptionKeys } from '../query-keys';
import { planSubscriptionMutationKeys } from '../mutation-keys';
import type {
  PlanSubscriptionSelect,
  PlanSubscriptionWithRelations,
  PlanSubscriptionPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlanSubscriptionSelect,
  PlanSubscriptionWithRelations,
  PlanSubscriptionPatch,
} from '../../orm/input-types';
/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlanSubscriptionMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', planSubscriptionPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlanSubscriptionMutation<S extends PlanSubscriptionSelect>(
  params: {
    selection: {
      fields: S & PlanSubscriptionSelect;
    } & HookStrictSelect<NoInfer<S>, PlanSubscriptionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlanSubscription: {
          planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        planSubscriptionPatch: PlanSubscriptionPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlanSubscription: {
      planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    planSubscriptionPatch: PlanSubscriptionPatch;
  }
>;
export function useUpdatePlanSubscriptionMutation(
  params: {
    selection: SelectionConfig<PlanSubscriptionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        planSubscriptionPatch: PlanSubscriptionPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlanSubscriptionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planSubscriptionMutationKeys.all,
    mutationFn: ({
      id,
      planSubscriptionPatch,
    }: {
      id: string;
      planSubscriptionPatch: PlanSubscriptionPatch;
    }) =>
      getClient()
        .planSubscription.update({
          where: {
            id,
          },
          data: planSubscriptionPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: planSubscriptionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: planSubscriptionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
