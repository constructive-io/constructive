/**
 * Per-entity limit overrides that take precedence over plan defaults
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planOverrideKeys } from '../query-keys';
import { planOverrideMutationKeys } from '../mutation-keys';
import type {
  PlanOverrideSelect,
  PlanOverrideWithRelations,
  CreatePlanOverrideInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  PlanOverrideSelect,
  PlanOverrideWithRelations,
  CreatePlanOverrideInput,
} from '../../orm/input-types';
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlanOverrideMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlanOverrideMutation<S extends PlanOverrideSelect>(
  params: {
    selection: {
      fields: S & PlanOverrideSelect;
    } & HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlanOverride: {
          planOverride: InferSelectResult<PlanOverrideWithRelations, S>;
        };
      },
      Error,
      CreatePlanOverrideInput['planOverride']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlanOverride: {
      planOverride: InferSelectResult<PlanOverrideWithRelations, S>;
    };
  },
  Error,
  CreatePlanOverrideInput['planOverride']
>;
export function useCreatePlanOverrideMutation(
  params: {
    selection: SelectionConfig<PlanOverrideSelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePlanOverrideInput['planOverride']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PlanOverrideSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planOverrideMutationKeys.create(),
    mutationFn: (data: CreatePlanOverrideInput['planOverride']) =>
      getClient()
        .planOverride.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: planOverrideKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
