/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterKeys } from '../query-keys';
import { meterMutationKeys } from '../mutation-keys';
import type { MeterSelect, MeterWithRelations, MeterPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterSelect, MeterWithRelations, MeterPatch } from '../../orm/input-types';
/**
 * Defines billable meters (what to track: quotas, feature flags, credit pools)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateMeterMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', meterPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateMeterMutation<S extends MeterSelect>(
  params: {
    selection: {
      fields: S & MeterSelect;
    } & HookStrictSelect<NoInfer<S>, MeterSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateMeter: {
          meter: InferSelectResult<MeterWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        meterPatch: MeterPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateMeter: {
      meter: InferSelectResult<MeterWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    meterPatch: MeterPatch;
  }
>;
export function useUpdateMeterMutation(
  params: {
    selection: SelectionConfig<MeterSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        meterPatch: MeterPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MeterSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterMutationKeys.all,
    mutationFn: ({ id, meterPatch }: { id: string; meterPatch: MeterPatch }) =>
      getClient()
        .meter.update({
          where: {
            id,
          },
          data: meterPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: meterKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: meterKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
