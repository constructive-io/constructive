/**
 * Per-entity limit overrides that take precedence over plan defaults
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planOverrideKeys } from '../query-keys';
import { planOverrideMutationKeys } from '../mutation-keys';
import type { PlanOverrideSelect, PlanOverrideWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanOverrideSelect, PlanOverrideWithRelations } from '../../orm/input-types';
/**
 * Per-entity limit overrides that take precedence over plan defaults
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlanOverrideMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlanOverrideMutation<S extends PlanOverrideSelect>(
  params: {
    selection: {
      fields: S & PlanOverrideSelect;
    } & HookStrictSelect<NoInfer<S>, PlanOverrideSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlanOverride: {
          planOverride: InferSelectResult<PlanOverrideWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlanOverride: {
      planOverride: InferSelectResult<PlanOverrideWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlanOverrideMutation(
  params: {
    selection: SelectionConfig<PlanOverrideSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlanOverrideSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planOverrideMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .planOverride.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: planOverrideKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: planOverrideKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
