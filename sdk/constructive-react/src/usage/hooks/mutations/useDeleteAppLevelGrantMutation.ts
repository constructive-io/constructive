/**
 * Records when a user achieves a level; prevents duplicate reward grants
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appLevelGrantKeys } from '../query-keys';
import { appLevelGrantMutationKeys } from '../mutation-keys';
import type { AppLevelGrantSelect, AppLevelGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppLevelGrantSelect, AppLevelGrantWithRelations } from '../../orm/input-types';
/**
 * Records when a user achieves a level; prevents duplicate reward grants
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppLevelGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppLevelGrantMutation<S extends AppLevelGrantSelect>(
  params: {
    selection: {
      fields: S & AppLevelGrantSelect;
    } & HookStrictSelect<NoInfer<S>, AppLevelGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppLevelGrant: {
          appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppLevelGrant: {
      appLevelGrant: InferSelectResult<AppLevelGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppLevelGrantMutation(
  params: {
    selection: SelectionConfig<AppLevelGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppLevelGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appLevelGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appLevelGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appLevelGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appLevelGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
