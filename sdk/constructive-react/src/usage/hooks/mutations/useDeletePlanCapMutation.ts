/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planCapKeys } from '../query-keys';
import { planCapMutationKeys } from '../mutation-keys';
import type { PlanCapSelect, PlanCapWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanCapSelect, PlanCapWithRelations } from '../../orm/input-types';
/**
 * Maps each plan to feature flag cap values (written to limit_caps when plan is applied)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlanCapMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlanCapMutation<S extends PlanCapSelect>(
  params: {
    selection: {
      fields: S & PlanCapSelect;
    } & HookStrictSelect<NoInfer<S>, PlanCapSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlanCap: {
          planCap: InferSelectResult<PlanCapWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlanCap: {
      planCap: InferSelectResult<PlanCapWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlanCapMutation(
  params: {
    selection: SelectionConfig<PlanCapSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlanCapSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planCapMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .planCap.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: planCapKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: planCapKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
