/**
 * Catalog of known event types with per-type configuration for aggregation, retention, and level participation
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appEventTypeKeys } from '../query-keys';
import { appEventTypeMutationKeys } from '../mutation-keys';
import type { AppEventTypeSelect, AppEventTypeWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppEventTypeSelect, AppEventTypeWithRelations } from '../../orm/input-types';
/**
 * Catalog of known event types with per-type configuration for aggregation, retention, and level participation
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppEventTypeMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppEventTypeMutation<S extends AppEventTypeSelect>(
  params: {
    selection: {
      fields: S & AppEventTypeSelect;
    } & HookStrictSelect<NoInfer<S>, AppEventTypeSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppEventType: {
          appEventType: InferSelectResult<AppEventTypeWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppEventType: {
      appEventType: InferSelectResult<AppEventTypeWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteAppEventTypeMutation(
  params: {
    selection: SelectionConfig<AppEventTypeSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppEventTypeSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appEventTypeMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .appEventType.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appEventTypeKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appEventTypeKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
