/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterSourceKeys } from '../query-keys';
import { meterSourceMutationKeys } from '../mutation-keys';
import type {
  MeterSourceSelect,
  MeterSourceWithRelations,
  CreateMeterSourceInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  MeterSourceSelect,
  MeterSourceWithRelations,
  CreateMeterSourceInput,
} from '../../orm/input-types';
/**
 * Maps billing meters to typed usage summary table columns for automated usage reconciliation. Each row tells reconcile_typed_usage() which column to aggregate and how.
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateMeterSourceMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateMeterSourceMutation<S extends MeterSourceSelect>(
  params: {
    selection: {
      fields: S & MeterSourceSelect;
    } & HookStrictSelect<NoInfer<S>, MeterSourceSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createMeterSource: {
          meterSource: InferSelectResult<MeterSourceWithRelations, S>;
        };
      },
      Error,
      CreateMeterSourceInput['meterSource']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createMeterSource: {
      meterSource: InferSelectResult<MeterSourceWithRelations, S>;
    };
  },
  Error,
  CreateMeterSourceInput['meterSource']
>;
export function useCreateMeterSourceMutation(
  params: {
    selection: SelectionConfig<MeterSourceSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateMeterSourceInput['meterSource']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<MeterSourceSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterSourceMutationKeys.create(),
    mutationFn: (data: CreateMeterSourceInput['meterSource']) =>
      getClient()
        .meterSource.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: meterSourceKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
