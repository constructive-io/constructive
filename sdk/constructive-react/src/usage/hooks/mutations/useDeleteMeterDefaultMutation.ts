/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterDefaultKeys } from '../query-keys';
import { meterDefaultMutationKeys } from '../mutation-keys';
import type { MeterDefaultSelect, MeterDefaultWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterDefaultSelect, MeterDefaultWithRelations } from '../../orm/input-types';
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteMeterDefaultMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteMeterDefaultMutation<S extends MeterDefaultSelect>(
  params: {
    selection: {
      fields: S & MeterDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteMeterDefault: {
          meterDefault: InferSelectResult<MeterDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteMeterDefault: {
      meterDefault: InferSelectResult<MeterDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteMeterDefaultMutation(
  params: {
    selection: SelectionConfig<MeterDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MeterDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterDefaultMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .meterDefault.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: meterDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: meterDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
