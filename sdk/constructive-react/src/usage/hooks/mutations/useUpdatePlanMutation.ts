/**
 * Defines plan tiers with named limit configurations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planKeys } from '../query-keys';
import { planMutationKeys } from '../mutation-keys';
import type { PlanSelect, PlanWithRelations, PlanPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanSelect, PlanWithRelations, PlanPatch } from '../../orm/input-types';
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePlanMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', planPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePlanMutation<S extends PlanSelect>(
  params: {
    selection: {
      fields: S & PlanSelect;
    } & HookStrictSelect<NoInfer<S>, PlanSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePlan: {
          plan: InferSelectResult<PlanWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        planPatch: PlanPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePlan: {
      plan: InferSelectResult<PlanWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    planPatch: PlanPatch;
  }
>;
export function useUpdatePlanMutation(
  params: {
    selection: SelectionConfig<PlanSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        planPatch: PlanPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlanSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planMutationKeys.all,
    mutationFn: ({ id, planPatch }: { id: string; planPatch: PlanPatch }) =>
      getClient()
        .plan.update({
          where: {
            id,
          },
          data: planPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: planKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: planKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
