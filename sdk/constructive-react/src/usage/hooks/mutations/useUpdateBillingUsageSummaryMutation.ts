/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { billingUsageSummaryKeys } from '../query-keys';
import { billingUsageSummaryMutationKeys } from '../mutation-keys';
import type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
  BillingUsageSummaryPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
  BillingUsageSummaryPatch,
} from '../../orm/input-types';
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateBillingUsageSummaryMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', billingUsageSummaryPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateBillingUsageSummaryMutation<S extends BillingUsageSummarySelect>(
  params: {
    selection: {
      fields: S & BillingUsageSummarySelect;
    } & HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateBillingUsageSummary: {
          billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        billingUsageSummaryPatch: BillingUsageSummaryPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateBillingUsageSummary: {
      billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    billingUsageSummaryPatch: BillingUsageSummaryPatch;
  }
>;
export function useUpdateBillingUsageSummaryMutation(
  params: {
    selection: SelectionConfig<BillingUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        billingUsageSummaryPatch: BillingUsageSummaryPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BillingUsageSummarySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: billingUsageSummaryMutationKeys.all,
    mutationFn: ({
      id,
      billingUsageSummaryPatch,
    }: {
      id: string;
      billingUsageSummaryPatch: BillingUsageSummaryPatch;
    }) =>
      getClient()
        .billingUsageSummary.update({
          where: {
            id,
          },
          data: billingUsageSummaryPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: billingUsageSummaryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: billingUsageSummaryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
