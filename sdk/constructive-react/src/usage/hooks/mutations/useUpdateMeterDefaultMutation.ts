/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterDefaultKeys } from '../query-keys';
import { meterDefaultMutationKeys } from '../mutation-keys';
import type {
  MeterDefaultSelect,
  MeterDefaultWithRelations,
  MeterDefaultPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  MeterDefaultSelect,
  MeterDefaultWithRelations,
  MeterDefaultPatch,
} from '../../orm/input-types';
/**
 * Default meter catalog: defines which meters are available and their default plan_limit values for new entities
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateMeterDefaultMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', meterDefaultPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateMeterDefaultMutation<S extends MeterDefaultSelect>(
  params: {
    selection: {
      fields: S & MeterDefaultSelect;
    } & HookStrictSelect<NoInfer<S>, MeterDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateMeterDefault: {
          meterDefault: InferSelectResult<MeterDefaultWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        meterDefaultPatch: MeterDefaultPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateMeterDefault: {
      meterDefault: InferSelectResult<MeterDefaultWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    meterDefaultPatch: MeterDefaultPatch;
  }
>;
export function useUpdateMeterDefaultMutation(
  params: {
    selection: SelectionConfig<MeterDefaultSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        meterDefaultPatch: MeterDefaultPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MeterDefaultSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterDefaultMutationKeys.all,
    mutationFn: ({ id, meterDefaultPatch }: { id: string; meterDefaultPatch: MeterDefaultPatch }) =>
      getClient()
        .meterDefault.update({
          where: {
            id,
          },
          data: meterDefaultPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: meterDefaultKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: meterDefaultKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
