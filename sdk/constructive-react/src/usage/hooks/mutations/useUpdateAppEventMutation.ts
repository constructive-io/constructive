/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appEventKeys } from '../query-keys';
import { appEventMutationKeys } from '../mutation-keys';
import type { AppEventSelect, AppEventWithRelations, AppEventPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppEventSelect, AppEventWithRelations, AppEventPatch } from '../../orm/input-types';
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateAppEventMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', appEventPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateAppEventMutation<S extends AppEventSelect>(
  params: {
    selection: {
      fields: S & AppEventSelect;
    } & HookStrictSelect<NoInfer<S>, AppEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateAppEvent: {
          appEvent: InferSelectResult<AppEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        createdAt: string;
        appEventPatch: AppEventPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateAppEvent: {
      appEvent: InferSelectResult<AppEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    createdAt: string;
    appEventPatch: AppEventPatch;
  }
>;
export function useUpdateAppEventMutation(
  params: {
    selection: SelectionConfig<AppEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        createdAt: string;
        appEventPatch: AppEventPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appEventMutationKeys.all,
    mutationFn: ({
      id,
      createdAt,
      appEventPatch,
    }: {
      id: string;
      createdAt: string;
      appEventPatch: AppEventPatch;
    }) =>
      getClient()
        .appEvent.update({
          where: {
            id,
            createdAt,
          },
          data: appEventPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: appEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
