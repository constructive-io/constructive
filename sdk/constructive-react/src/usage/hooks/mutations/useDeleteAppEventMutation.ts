/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { appEventKeys } from '../query-keys';
import { appEventMutationKeys } from '../mutation-keys';
import type { AppEventSelect, AppEventWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { AppEventSelect, AppEventWithRelations } from '../../orm/input-types';
/**
 * Partitioned append-only log of individual user actions; every single event ever recorded
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteAppEventMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteAppEventMutation<S extends AppEventSelect>(
  params: {
    selection: {
      fields: S & AppEventSelect;
    } & HookStrictSelect<NoInfer<S>, AppEventSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteAppEvent: {
          appEvent: InferSelectResult<AppEventWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        createdAt: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteAppEvent: {
      appEvent: InferSelectResult<AppEventWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    createdAt: string;
  }
>;
export function useDeleteAppEventMutation(
  params: {
    selection: SelectionConfig<AppEventSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        createdAt: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<AppEventSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: appEventMutationKeys.all,
    mutationFn: ({ id, createdAt }: { id: string; createdAt: string }) =>
      getClient()
        .appEvent.delete({
          where: {
            id,
            createdAt,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: appEventKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: appEventKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
