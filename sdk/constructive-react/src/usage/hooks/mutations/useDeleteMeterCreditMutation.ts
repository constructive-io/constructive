/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterCreditKeys } from '../query-keys';
import { meterCreditMutationKeys } from '../mutation-keys';
import type { MeterCreditSelect, MeterCreditWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { MeterCreditSelect, MeterCreditWithRelations } from '../../orm/input-types';
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteMeterCreditMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteMeterCreditMutation<S extends MeterCreditSelect>(
  params: {
    selection: {
      fields: S & MeterCreditSelect;
    } & HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteMeterCredit: {
          meterCredit: InferSelectResult<MeterCreditWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteMeterCredit: {
      meterCredit: InferSelectResult<MeterCreditWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteMeterCreditMutation(
  params: {
    selection: SelectionConfig<MeterCreditSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MeterCreditSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterCreditMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .meterCredit.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: meterCreditKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: meterCreditKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
