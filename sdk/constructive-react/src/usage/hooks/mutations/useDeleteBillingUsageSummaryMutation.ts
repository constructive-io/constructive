/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { billingUsageSummaryKeys } from '../query-keys';
import { billingUsageSummaryMutationKeys } from '../mutation-keys';
import type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  BillingUsageSummarySelect,
  BillingUsageSummaryWithRelations,
} from '../../orm/input-types';
/**
 * Permanent monthly usage summary per entity per meter (user-facing billing dashboard)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteBillingUsageSummaryMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteBillingUsageSummaryMutation<S extends BillingUsageSummarySelect>(
  params: {
    selection: {
      fields: S & BillingUsageSummarySelect;
    } & HookStrictSelect<NoInfer<S>, BillingUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteBillingUsageSummary: {
          billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteBillingUsageSummary: {
      billingUsageSummary: InferSelectResult<BillingUsageSummaryWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteBillingUsageSummaryMutation(
  params: {
    selection: SelectionConfig<BillingUsageSummarySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<BillingUsageSummarySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: billingUsageSummaryMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .billingUsageSummary.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: billingUsageSummaryKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: billingUsageSummaryKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
