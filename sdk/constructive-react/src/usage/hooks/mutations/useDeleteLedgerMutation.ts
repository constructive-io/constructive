/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { ledgerKeys } from '../query-keys';
import { ledgerMutationKeys } from '../mutation-keys';
import type { LedgerSelect, LedgerWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { LedgerSelect, LedgerWithRelations } from '../../orm/input-types';
/**
 * Append-only event log for all billing events (usage, grants, adjustments)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteLedgerMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteLedgerMutation<S extends LedgerSelect>(
  params: {
    selection: {
      fields: S & LedgerSelect;
    } & HookStrictSelect<NoInfer<S>, LedgerSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteLedger: {
          ledger: InferSelectResult<LedgerWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        createdAt: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteLedger: {
      ledger: InferSelectResult<LedgerWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    createdAt: string;
  }
>;
export function useDeleteLedgerMutation(
  params: {
    selection: SelectionConfig<LedgerSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        createdAt: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<LedgerSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: ledgerMutationKeys.all,
    mutationFn: ({ id, createdAt }: { id: string; createdAt: string }) =>
      getClient()
        .ledger.delete({
          where: {
            id,
            createdAt,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: ledgerKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: ledgerKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
