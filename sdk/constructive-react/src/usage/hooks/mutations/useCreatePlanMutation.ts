/**
 * Defines plan tiers with named limit configurations
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planKeys } from '../query-keys';
import { planMutationKeys } from '../mutation-keys';
import type { PlanSelect, PlanWithRelations, CreatePlanInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanSelect, PlanWithRelations, CreatePlanInput } from '../../orm/input-types';
/**
 * Defines plan tiers with named limit configurations
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreatePlanMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreatePlanMutation<S extends PlanSelect>(
  params: {
    selection: {
      fields: S & PlanSelect;
    } & HookStrictSelect<NoInfer<S>, PlanSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createPlan: {
          plan: InferSelectResult<PlanWithRelations, S>;
        };
      },
      Error,
      CreatePlanInput['plan']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createPlan: {
      plan: InferSelectResult<PlanWithRelations, S>;
    };
  },
  Error,
  CreatePlanInput['plan']
>;
export function useCreatePlanMutation(
  params: {
    selection: SelectionConfig<PlanSelect>;
  } & Omit<UseMutationOptions<any, Error, CreatePlanInput['plan']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<PlanSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planMutationKeys.create(),
    mutationFn: (data: CreatePlanInput['plan']) =>
      getClient()
        .plan.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: planKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
