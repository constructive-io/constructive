/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { planSubscriptionKeys } from '../query-keys';
import { planSubscriptionMutationKeys } from '../mutation-keys';
import type { PlanSubscriptionSelect, PlanSubscriptionWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PlanSubscriptionSelect, PlanSubscriptionWithRelations } from '../../orm/input-types';
/**
 * Assigns a plan to an entity with subscription lifecycle (start, end, active state)
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeletePlanSubscriptionMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeletePlanSubscriptionMutation<S extends PlanSubscriptionSelect>(
  params: {
    selection: {
      fields: S & PlanSubscriptionSelect;
    } & HookStrictSelect<NoInfer<S>, PlanSubscriptionSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deletePlanSubscription: {
          planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deletePlanSubscription: {
      planSubscription: InferSelectResult<PlanSubscriptionWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeletePlanSubscriptionMutation(
  params: {
    selection: SelectionConfig<PlanSubscriptionSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PlanSubscriptionSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: planSubscriptionMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .planSubscription.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: planSubscriptionKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: planSubscriptionKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
