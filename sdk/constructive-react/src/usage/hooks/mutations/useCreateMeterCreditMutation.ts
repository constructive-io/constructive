/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterCreditKeys } from '../query-keys';
import { meterCreditMutationKeys } from '../mutation-keys';
import type {
  MeterCreditSelect,
  MeterCreditWithRelations,
  CreateMeterCreditInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  MeterCreditSelect,
  MeterCreditWithRelations,
  CreateMeterCreditInput,
} from '../../orm/input-types';
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateMeterCreditMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateMeterCreditMutation<S extends MeterCreditSelect>(
  params: {
    selection: {
      fields: S & MeterCreditSelect;
    } & HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createMeterCredit: {
          meterCredit: InferSelectResult<MeterCreditWithRelations, S>;
        };
      },
      Error,
      CreateMeterCreditInput['meterCredit']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createMeterCredit: {
      meterCredit: InferSelectResult<MeterCreditWithRelations, S>;
    };
  },
  Error,
  CreateMeterCreditInput['meterCredit']
>;
export function useCreateMeterCreditMutation(
  params: {
    selection: SelectionConfig<MeterCreditSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateMeterCreditInput['meterCredit']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<MeterCreditSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterCreditMutationKeys.create(),
    mutationFn: (data: CreateMeterCreditInput['meterCredit']) =>
      getClient()
        .meterCredit.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: meterCreditKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
