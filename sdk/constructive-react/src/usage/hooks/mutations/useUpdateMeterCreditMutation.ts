/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { meterCreditKeys } from '../query-keys';
import { meterCreditMutationKeys } from '../mutation-keys';
import type {
  MeterCreditSelect,
  MeterCreditWithRelations,
  MeterCreditPatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  MeterCreditSelect,
  MeterCreditWithRelations,
  MeterCreditPatch,
} from '../../orm/input-types';
/**
 * Append-only ledger of credit grants for billing meters that automatically update balances
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateMeterCreditMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', meterCreditPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateMeterCreditMutation<S extends MeterCreditSelect>(
  params: {
    selection: {
      fields: S & MeterCreditSelect;
    } & HookStrictSelect<NoInfer<S>, MeterCreditSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateMeterCredit: {
          meterCredit: InferSelectResult<MeterCreditWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        meterCreditPatch: MeterCreditPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateMeterCredit: {
      meterCredit: InferSelectResult<MeterCreditWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    meterCreditPatch: MeterCreditPatch;
  }
>;
export function useUpdateMeterCreditMutation(
  params: {
    selection: SelectionConfig<MeterCreditSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        meterCreditPatch: MeterCreditPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<MeterCreditSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: meterCreditMutationKeys.all,
    mutationFn: ({ id, meterCreditPatch }: { id: string; meterCreditPatch: MeterCreditPatch }) =>
      getClient()
        .meterCredit.update({
          where: {
            id,
          },
          data: meterCreditPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: meterCreditKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: meterCreditKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
