# ORM Client - Agent Reference

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT
> This document is structured for LLM/agent consumption.

## OVERVIEW

Prisma-like ORM client for interacting with a GraphQL API.
All methods return a query builder. Call `.execute()` to run the query.

## SETUP

```typescript
import { createClient } from './orm';

const db = createClient({
  endpoint: 'https://api.example.com/graphql',
  headers: { Authorization: 'Bearer <token>' },
});
```

## MODELS

### MODEL: appPermission

Access: `db.appPermission`

```
METHODS:
  db.appPermission.findMany({ select, where?, orderBy?, first?, offset? })
  db.appPermission.findOne({ where: { id }, select })
  db.appPermission.create({ data: { name, bitnum, bitstr, description }, select })
  db.appPermission.update({ where: { id }, data, select })
  db.appPermission.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  bitnum: number
  bitstr: string
  description: string

EDITABLE FIELDS:
  name: string
  bitnum: number
  bitstr: string
  description: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, bitnum, bitstr, description }]
  findOne:  { id, name, bitnum, bitstr, description }
  create:   { id, name, bitnum, bitstr, description }
  update:   { id, name, bitnum, bitstr, description }
  delete:   { id }
```

### MODEL: orgPermission

Access: `db.orgPermission`

```
METHODS:
  db.orgPermission.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgPermission.findOne({ where: { id }, select })
  db.orgPermission.create({ data: { name, bitnum, bitstr, description }, select })
  db.orgPermission.update({ where: { id }, data, select })
  db.orgPermission.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  bitnum: number
  bitstr: string
  description: string

EDITABLE FIELDS:
  name: string
  bitnum: number
  bitstr: string
  description: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, bitnum, bitstr, description }]
  findOne:  { id, name, bitnum, bitstr, description }
  create:   { id, name, bitnum, bitstr, description }
  update:   { id, name, bitnum, bitstr, description }
  delete:   { id }
```

### MODEL: appLevelRequirement

Access: `db.appLevelRequirement`

```
METHODS:
  db.appLevelRequirement.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLevelRequirement.findOne({ where: { id }, select })
  db.appLevelRequirement.create({ data: { name, level, description, requiredCount, priority }, select })
  db.appLevelRequirement.update({ where: { id }, data, select })
  db.appLevelRequirement.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  level: string
  description: string
  requiredCount: number
  priority: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  name: string
  level: string
  description: string
  requiredCount: number
  priority: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, level, description, requiredCount, priority, createdAt, updatedAt }]
  findOne:  { id, name, level, description, requiredCount, priority, createdAt, updatedAt }
  create:   { id, name, level, description, requiredCount, priority, createdAt, updatedAt }
  update:   { id, name, level, description, requiredCount, priority, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgMember

Access: `db.orgMember`

```
METHODS:
  db.orgMember.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgMember.findOne({ where: { id }, select })
  db.orgMember.create({ data: { isAdmin, actorId, entityId }, select })
  db.orgMember.update({ where: { id }, data, select })
  db.orgMember.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isAdmin: boolean
  actorId: string
  entityId: string

EDITABLE FIELDS:
  isAdmin: boolean
  actorId: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isAdmin, actorId, entityId }]
  findOne:  { id, isAdmin, actorId, entityId }
  create:   { id, isAdmin, actorId, entityId }
  update:   { id, isAdmin, actorId, entityId }
  delete:   { id }
```

### MODEL: appPermissionDefault

Access: `db.appPermissionDefault`

```
METHODS:
  db.appPermissionDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.appPermissionDefault.findOne({ where: { id }, select })
  db.appPermissionDefault.create({ data: { permissions }, select })
  db.appPermissionDefault.update({ where: { id }, data, select })
  db.appPermissionDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string

EDITABLE FIELDS:
  permissions: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions }]
  findOne:  { id, permissions }
  create:   { id, permissions }
  update:   { id, permissions }
  delete:   { id }
```

### MODEL: orgPermissionDefault

Access: `db.orgPermissionDefault`

```
METHODS:
  db.orgPermissionDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgPermissionDefault.findOne({ where: { id }, select })
  db.orgPermissionDefault.create({ data: { permissions, entityId }, select })
  db.orgPermissionDefault.update({ where: { id }, data, select })
  db.orgPermissionDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string
  entityId: string

EDITABLE FIELDS:
  permissions: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions, entityId }]
  findOne:  { id, permissions, entityId }
  create:   { id, permissions, entityId }
  update:   { id, permissions, entityId }
  delete:   { id }
```

### MODEL: appAdminGrant

Access: `db.appAdminGrant`

```
METHODS:
  db.appAdminGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.appAdminGrant.findOne({ where: { id }, select })
  db.appAdminGrant.create({ data: { isGrant, actorId, grantorId }, select })
  db.appAdminGrant.update({ where: { id }, data, select })
  db.appAdminGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appOwnerGrant

Access: `db.appOwnerGrant`

```
METHODS:
  db.appOwnerGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.appOwnerGrant.findOne({ where: { id }, select })
  db.appOwnerGrant.create({ data: { isGrant, actorId, grantorId }, select })
  db.appOwnerGrant.update({ where: { id }, data, select })
  db.appOwnerGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appLimitDefault

Access: `db.appLimitDefault`

```
METHODS:
  db.appLimitDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLimitDefault.findOne({ where: { id }, select })
  db.appLimitDefault.create({ data: { name, max }, select })
  db.appLimitDefault.update({ where: { id }, data, select })
  db.appLimitDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  max: number

EDITABLE FIELDS:
  name: string
  max: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, max }]
  findOne:  { id, name, max }
  create:   { id, name, max }
  update:   { id, name, max }
  delete:   { id }
```

### MODEL: orgLimitDefault

Access: `db.orgLimitDefault`

```
METHODS:
  db.orgLimitDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgLimitDefault.findOne({ where: { id }, select })
  db.orgLimitDefault.create({ data: { name, max }, select })
  db.orgLimitDefault.update({ where: { id }, data, select })
  db.orgLimitDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  max: number

EDITABLE FIELDS:
  name: string
  max: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, max }]
  findOne:  { id, name, max }
  create:   { id, name, max }
  update:   { id, name, max }
  delete:   { id }
```

### MODEL: orgAdminGrant

Access: `db.orgAdminGrant`

```
METHODS:
  db.orgAdminGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgAdminGrant.findOne({ where: { id }, select })
  db.orgAdminGrant.create({ data: { isGrant, actorId, entityId, grantorId }, select })
  db.orgAdminGrant.update({ where: { id }, data, select })
  db.orgAdminGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgOwnerGrant

Access: `db.orgOwnerGrant`

```
METHODS:
  db.orgOwnerGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgOwnerGrant.findOne({ where: { id }, select })
  db.orgOwnerGrant.create({ data: { isGrant, actorId, entityId, grantorId }, select })
  db.orgOwnerGrant.update({ where: { id }, data, select })
  db.orgOwnerGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: membershipType

Access: `db.membershipType`

```
METHODS:
  db.membershipType.findMany({ select, where?, orderBy?, first?, offset? })
  db.membershipType.findOne({ where: { id }, select })
  db.membershipType.create({ data: { name, description, prefix }, select })
  db.membershipType.update({ where: { id }, data, select })
  db.membershipType.delete({ where: { id } })

FIELDS:
  id: number (primary key)
  name: string
  description: string
  prefix: string

EDITABLE FIELDS:
  name: string
  description: string
  prefix: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, description, prefix }]
  findOne:  { id, name, description, prefix }
  create:   { id, name, description, prefix }
  update:   { id, name, description, prefix }
  delete:   { id }
```

### MODEL: appLimit

Access: `db.appLimit`

```
METHODS:
  db.appLimit.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLimit.findOne({ where: { id }, select })
  db.appLimit.create({ data: { name, actorId, num, max }, select })
  db.appLimit.update({ where: { id }, data, select })
  db.appLimit.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  actorId: string
  num: number
  max: number

EDITABLE FIELDS:
  name: string
  actorId: string
  num: number
  max: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, actorId, num, max }]
  findOne:  { id, name, actorId, num, max }
  create:   { id, name, actorId, num, max }
  update:   { id, name, actorId, num, max }
  delete:   { id }
```

### MODEL: appAchievement

Access: `db.appAchievement`

```
METHODS:
  db.appAchievement.findMany({ select, where?, orderBy?, first?, offset? })
  db.appAchievement.findOne({ where: { id }, select })
  db.appAchievement.create({ data: { actorId, name, count }, select })
  db.appAchievement.update({ where: { id }, data, select })
  db.appAchievement.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  actorId: string
  name: string
  count: number

OUTPUT: Promise<JSON>
  findMany: [{ id, actorId, name, count, createdAt, updatedAt }]
  findOne:  { id, actorId, name, count, createdAt, updatedAt }
  create:   { id, actorId, name, count, createdAt, updatedAt }
  update:   { id, actorId, name, count, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appStep

Access: `db.appStep`

```
METHODS:
  db.appStep.findMany({ select, where?, orderBy?, first?, offset? })
  db.appStep.findOne({ where: { id }, select })
  db.appStep.create({ data: { actorId, name, count }, select })
  db.appStep.update({ where: { id }, data, select })
  db.appStep.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  actorId: string
  name: string
  count: number

OUTPUT: Promise<JSON>
  findMany: [{ id, actorId, name, count, createdAt, updatedAt }]
  findOne:  { id, actorId, name, count, createdAt, updatedAt }
  create:   { id, actorId, name, count, createdAt, updatedAt }
  update:   { id, actorId, name, count, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: claimedInvite

Access: `db.claimedInvite`

```
METHODS:
  db.claimedInvite.findMany({ select, where?, orderBy?, first?, offset? })
  db.claimedInvite.findOne({ where: { id }, select })
  db.claimedInvite.create({ data: { data, senderId, receiverId }, select })
  db.claimedInvite.update({ where: { id }, data, select })
  db.claimedInvite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  data: unknown
  senderId: string
  receiverId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, data, senderId, receiverId, createdAt, updatedAt }]
  findOne:  { id, data, senderId, receiverId, createdAt, updatedAt }
  create:   { id, data, senderId, receiverId, createdAt, updatedAt }
  update:   { id, data, senderId, receiverId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appGrant

Access: `db.appGrant`

```
METHODS:
  db.appGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.appGrant.findOne({ where: { id }, select })
  db.appGrant.create({ data: { permissions, isGrant, actorId, grantorId }, select })
  db.appGrant.update({ where: { id }, data, select })
  db.appGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  permissions: string
  isGrant: boolean
  actorId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }]
  findOne:  { id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }
  create:   { id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }
  update:   { id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appMembershipDefault

Access: `db.appMembershipDefault`

```
METHODS:
  db.appMembershipDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.appMembershipDefault.findOne({ where: { id }, select })
  db.appMembershipDefault.create({ data: { createdBy, updatedBy, isApproved, isVerified }, select })
  db.appMembershipDefault.update({ where: { id }, data, select })
  db.appMembershipDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isVerified: boolean

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isVerified: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }
  delete:   { id }
```

### MODEL: orgLimit

Access: `db.orgLimit`

```
METHODS:
  db.orgLimit.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgLimit.findOne({ where: { id }, select })
  db.orgLimit.create({ data: { name, actorId, num, max, entityId }, select })
  db.orgLimit.update({ where: { id }, data, select })
  db.orgLimit.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  actorId: string
  num: number
  max: number
  entityId: string

EDITABLE FIELDS:
  name: string
  actorId: string
  num: number
  max: number
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, actorId, num, max, entityId }]
  findOne:  { id, name, actorId, num, max, entityId }
  create:   { id, name, actorId, num, max, entityId }
  update:   { id, name, actorId, num, max, entityId }
  delete:   { id }
```

### MODEL: orgClaimedInvite

Access: `db.orgClaimedInvite`

```
METHODS:
  db.orgClaimedInvite.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgClaimedInvite.findOne({ where: { id }, select })
  db.orgClaimedInvite.create({ data: { data, senderId, receiverId, entityId }, select })
  db.orgClaimedInvite.update({ where: { id }, data, select })
  db.orgClaimedInvite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string
  entityId: string

EDITABLE FIELDS:
  data: unknown
  senderId: string
  receiverId: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, data, senderId, receiverId, createdAt, updatedAt, entityId }]
  findOne:  { id, data, senderId, receiverId, createdAt, updatedAt, entityId }
  create:   { id, data, senderId, receiverId, createdAt, updatedAt, entityId }
  update:   { id, data, senderId, receiverId, createdAt, updatedAt, entityId }
  delete:   { id }
```

### MODEL: orgGrant

Access: `db.orgGrant`

```
METHODS:
  db.orgGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgGrant.findOne({ where: { id }, select })
  db.orgGrant.create({ data: { permissions, isGrant, actorId, entityId, grantorId }, select })
  db.orgGrant.update({ where: { id }, data, select })
  db.orgGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  permissions: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }]
  findOne:  { id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  create:   { id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  update:   { id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgMembershipDefault

Access: `db.orgMembershipDefault`

```
METHODS:
  db.orgMembershipDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgMembershipDefault.findOne({ where: { id }, select })
  db.orgMembershipDefault.create({ data: { createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }, select })
  db.orgMembershipDefault.update({ where: { id }, data, select })
  db.orgMembershipDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  entityId: string
  deleteMemberCascadeGroups: boolean
  createGroupsCascadeMembers: boolean

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  entityId: string
  deleteMemberCascadeGroups: boolean
  createGroupsCascadeMembers: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }
  delete:   { id }
```

### MODEL: appLevel

Access: `db.appLevel`

```
METHODS:
  db.appLevel.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLevel.findOne({ where: { id }, select })
  db.appLevel.create({ data: { name, description, image, ownerId }, select })
  db.appLevel.update({ where: { id }, data, select })
  db.appLevel.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  description: string
  image: ConstructiveInternalTypeImage
  ownerId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  name: string
  description: string
  image: ConstructiveInternalTypeImage
  ownerId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, description, image, ownerId, createdAt, updatedAt }]
  findOne:  { id, name, description, image, ownerId, createdAt, updatedAt }
  create:   { id, name, description, image, ownerId, createdAt, updatedAt }
  update:   { id, name, description, image, ownerId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: invite

Access: `db.invite`

```
METHODS:
  db.invite.findMany({ select, where?, orderBy?, first?, offset? })
  db.invite.findOne({ where: { id }, select })
  db.invite.create({ data: { email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt }, select })
  db.invite.update({ where: { id }, data, select })
  db.invite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  email: ConstructiveInternalTypeEmail
  senderId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  email: ConstructiveInternalTypeEmail
  senderId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string

OUTPUT: Promise<JSON>
  findMany: [{ id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }]
  findOne:  { id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }
  create:   { id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }
  update:   { id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appMembership

Access: `db.appMembership`

```
METHODS:
  db.appMembership.findMany({ select, where?, orderBy?, first?, offset? })
  db.appMembership.findOne({ where: { id }, select })
  db.appMembership.create({ data: { createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }, select })
  db.appMembership.update({ where: { id }, data, select })
  db.appMembership.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isVerified: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isVerified: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }
  delete:   { id }
```

### MODEL: orgMembership

Access: `db.orgMembership`

```
METHODS:
  db.orgMembership.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgMembership.findOne({ where: { id }, select })
  db.orgMembership.create({ data: { createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }, select })
  db.orgMembership.update({ where: { id }, data, select })
  db.orgMembership.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
  entityId: string

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }
  delete:   { id }
```

### MODEL: orgInvite

Access: `db.orgInvite`

```
METHODS:
  db.orgInvite.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgInvite.findOne({ where: { id }, select })
  db.orgInvite.create({ data: { email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, entityId }, select })
  db.orgInvite.update({ where: { id }, data, select })
  db.orgInvite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  email: ConstructiveInternalTypeEmail
  senderId: string
  receiverId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string
  entityId: string

EDITABLE FIELDS:
  email: ConstructiveInternalTypeEmail
  senderId: string
  receiverId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }]
  findOne:  { id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }
  create:   { id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }
  update:   { id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }
  delete:   { id }
```

## CUSTOM OPERATIONS

### OPERATION: appPermissionsGetPaddedMask

appPermissionsGetPaddedMask

```
TYPE: query
ACCESS: db.query.appPermissionsGetPaddedMask
USAGE: db.query.appPermissionsGetPaddedMask({ mask: <value> }).execute()

INPUT:
  mask: BitString

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetPaddedMask

orgPermissionsGetPaddedMask

```
TYPE: query
ACCESS: db.query.orgPermissionsGetPaddedMask
USAGE: db.query.orgPermissionsGetPaddedMask({ mask: <value> }).execute()

INPUT:
  mask: BitString

OUTPUT: Promise<JSON>
```

### OPERATION: stepsAchieved

stepsAchieved

```
TYPE: query
ACCESS: db.query.stepsAchieved
USAGE: db.query.stepsAchieved({ vlevel: <value>, vroleId: <value> }).execute()

INPUT:
  vlevel: String
  vroleId: UUID

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetMask

appPermissionsGetMask

```
TYPE: query
ACCESS: db.query.appPermissionsGetMask
USAGE: db.query.appPermissionsGetMask({ ids: <value> }).execute()

INPUT:
  ids: [UUID]

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetMask

orgPermissionsGetMask

```
TYPE: query
ACCESS: db.query.orgPermissionsGetMask
USAGE: db.query.orgPermissionsGetMask({ ids: <value> }).execute()

INPUT:
  ids: [UUID]

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetMaskByNames

appPermissionsGetMaskByNames

```
TYPE: query
ACCESS: db.query.appPermissionsGetMaskByNames
USAGE: db.query.appPermissionsGetMaskByNames({ names: <value> }).execute()

INPUT:
  names: [String]

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetMaskByNames

orgPermissionsGetMaskByNames

```
TYPE: query
ACCESS: db.query.orgPermissionsGetMaskByNames
USAGE: db.query.orgPermissionsGetMaskByNames({ names: <value> }).execute()

INPUT:
  names: [String]

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetByMask

Reads and enables pagination through a set of `AppPermission`.

```
TYPE: query
ACCESS: db.query.appPermissionsGetByMask
USAGE: db.query.appPermissionsGetByMask({ mask: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  mask: BitString
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetByMask

Reads and enables pagination through a set of `OrgPermission`.

```
TYPE: query
ACCESS: db.query.orgPermissionsGetByMask
USAGE: db.query.orgPermissionsGetByMask({ mask: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  mask: BitString
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: stepsRequired

Reads and enables pagination through a set of `AppLevelRequirement`.

```
TYPE: query
ACCESS: db.query.stepsRequired
USAGE: db.query.stepsRequired({ vlevel: <value>, vroleId: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  vlevel: String
  vroleId: UUID
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: submitInviteCode

submitInviteCode

```
TYPE: mutation
ACCESS: db.mutation.submitInviteCode
USAGE: db.mutation.submitInviteCode({ input: <value> }).execute()

INPUT:
  input: SubmitInviteCodeInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: submitOrgInviteCode

submitOrgInviteCode

```
TYPE: mutation
ACCESS: db.mutation.submitOrgInviteCode
USAGE: db.mutation.submitOrgInviteCode({ input: <value> }).execute()

INPUT:
  input: SubmitOrgInviteCodeInput (required)

OUTPUT: Promise<JSON>
```

## PATTERNS

```typescript
// All methods require .execute() to run
const result = await db.modelName.findMany({ select: { id: true } }).execute();

// Select specific fields
const partial = await db.modelName.findMany({ select: { id: true, name: true } }).execute();

// Filter with where clause
const filtered = await db.modelName.findMany({ select: { id: true }, where: { name: 'test' } }).execute();
```
