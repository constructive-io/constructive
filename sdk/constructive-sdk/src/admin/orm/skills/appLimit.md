# orm-appLimit

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT

ORM operations for AppLimit records

## Usage

```typescript
db.appLimit.findMany({ select: { id: true } }).execute()
db.appLimit.findOne({ where: { id: '<value>' }, select: { id: true } }).execute()
db.appLimit.create({ data: { name: '<value>', actorId: '<value>', num: '<value>', max: '<value>' }, select: { id: true } }).execute()
db.appLimit.update({ where: { id: '<value>' }, data: { name: '<new>' }, select: { id: true } }).execute()
db.appLimit.delete({ where: { id: '<value>' } }).execute()
```

## Examples

### List all appLimit records

```typescript
const items = await db.appLimit.findMany({
  select: { id: true, name: true }
}).execute();
```

### Create a appLimit

```typescript
const item = await db.appLimit.create({
  data: { name: 'value', actorId: 'value', num: 'value', max: 'value' },
  select: { id: true }
}).execute();
```
