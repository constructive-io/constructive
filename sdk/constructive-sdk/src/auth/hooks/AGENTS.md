# React Query Hooks - Agent Reference

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT
> This document is structured for LLM/agent consumption.

## OVERVIEW

React Query hooks wrapping ORM operations for data fetching and mutations.
All query hooks return `UseQueryResult`. All mutation hooks return `UseMutationResult`.

## SETUP

```typescript
import { configure } from './hooks';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

configure({ endpoint: 'https://api.example.com/graphql' });
const queryClient = new QueryClient();
// Wrap app in <QueryClientProvider client={queryClient}>
```

## HOOKS

### HOOK: useRoleTypesQuery

List all roleTypes.

```
TYPE: query
USAGE: useRoleTypesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: number
  name: string
}>>
```

### HOOK: useRoleTypeQuery

Get a single roleType by id.

```
TYPE: query
USAGE: useRoleTypeQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: number (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: number
  name: string
}>
```

### HOOK: useCreateRoleTypeMutation

Create a new roleType.

```
TYPE: mutation
USAGE: const { mutate } = useCreateRoleTypeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateRoleTypeMutation

Update an existing roleType.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateRoleTypeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteRoleTypeMutation

Delete a roleType.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteRoleTypeMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useCryptoAddressesQuery

List all cryptoAddresses.

```
TYPE: query
USAGE: useCryptoAddressesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  address: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useCryptoAddressQuery

Get a single cryptoAddress by id.

```
TYPE: query
USAGE: useCryptoAddressQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  address: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateCryptoAddressMutation

Create a new cryptoAddress.

```
TYPE: mutation
USAGE: const { mutate } = useCreateCryptoAddressMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateCryptoAddressMutation

Update an existing cryptoAddress.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateCryptoAddressMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteCryptoAddressMutation

Delete a cryptoAddress.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteCryptoAddressMutation({})

OUTPUT: UseMutationResult
```

### HOOK: usePhoneNumbersQuery

List all phoneNumbers.

```
TYPE: query
USAGE: usePhoneNumbersQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  cc: string
  number: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: usePhoneNumberQuery

Get a single phoneNumber by id.

```
TYPE: query
USAGE: usePhoneNumberQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  cc: string
  number: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreatePhoneNumberMutation

Create a new phoneNumber.

```
TYPE: mutation
USAGE: const { mutate } = useCreatePhoneNumberMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdatePhoneNumberMutation

Update an existing phoneNumber.

```
TYPE: mutation
USAGE: const { mutate } = useUpdatePhoneNumberMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeletePhoneNumberMutation

Delete a phoneNumber.

```
TYPE: mutation
USAGE: const { mutate } = useDeletePhoneNumberMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useConnectedAccountsQuery

List all connectedAccounts.

```
TYPE: query
USAGE: useConnectedAccountsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  service: string
  identifier: string
  details: unknown
  isVerified: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useConnectedAccountQuery

Get a single connectedAccount by id.

```
TYPE: query
USAGE: useConnectedAccountQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  service: string
  identifier: string
  details: unknown
  isVerified: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateConnectedAccountMutation

Create a new connectedAccount.

```
TYPE: mutation
USAGE: const { mutate } = useCreateConnectedAccountMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateConnectedAccountMutation

Update an existing connectedAccount.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateConnectedAccountMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteConnectedAccountMutation

Delete a connectedAccount.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteConnectedAccountMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useEmailsQuery

List all emails.

```
TYPE: query
USAGE: useEmailsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  email: ConstructiveInternalTypeEmail
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useEmailQuery

Get a single email by id.

```
TYPE: query
USAGE: useEmailQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  email: ConstructiveInternalTypeEmail
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateEmailMutation

Create a new email.

```
TYPE: mutation
USAGE: const { mutate } = useCreateEmailMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateEmailMutation

Update an existing email.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateEmailMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteEmailMutation

Delete a email.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteEmailMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAuditLogsQuery

List all auditLogs.

```
TYPE: query
USAGE: useAuditLogsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  event: string
  actorId: string
  origin: ConstructiveInternalTypeOrigin
  userAgent: string
  ipAddress: string
  success: boolean
  createdAt: string
}>>
```

### HOOK: useAuditLogQuery

Get a single auditLog by id.

```
TYPE: query
USAGE: useAuditLogQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  event: string
  actorId: string
  origin: ConstructiveInternalTypeOrigin
  userAgent: string
  ipAddress: string
  success: boolean
  createdAt: string
}>
```

### HOOK: useCreateAuditLogMutation

Create a new auditLog.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAuditLogMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAuditLogMutation

Update an existing auditLog.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAuditLogMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAuditLogMutation

Delete a auditLog.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAuditLogMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useUsersQuery

List all users.

```
TYPE: query
USAGE: useUsersQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  username: string
  displayName: string
  profilePicture: ConstructiveInternalTypeImage
  searchTsv: string
  type: number
  createdAt: string
  updatedAt: string
  searchTsvRank: number
}>>
```

### HOOK: useUserQuery

Get a single user by id.

```
TYPE: query
USAGE: useUserQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  username: string
  displayName: string
  profilePicture: ConstructiveInternalTypeImage
  searchTsv: string
  type: number
  createdAt: string
  updatedAt: string
  searchTsvRank: number
}>
```

### HOOK: useCreateUserMutation

Create a new user.

```
TYPE: mutation
USAGE: const { mutate } = useCreateUserMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateUserMutation

Update an existing user.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateUserMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteUserMutation

Delete a user.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteUserMutation({})

OUTPUT: UseMutationResult
```

## CUSTOM OPERATION HOOKS

### HOOK: useCurrentIpAddressQuery

currentIpAddress

```
TYPE: query
USAGE: useCurrentIpAddressQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useCurrentUserAgentQuery

currentUserAgent

```
TYPE: query
USAGE: useCurrentUserAgentQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useCurrentUserIdQuery

currentUserId

```
TYPE: query
USAGE: useCurrentUserIdQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useCurrentUserQuery

currentUser

```
TYPE: query
USAGE: useCurrentUserQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useSignOutMutation

signOut

```
TYPE: mutation
USAGE: const { mutate } = useSignOutMutation()
  mutate({ input: <value> })

INPUT:
  input: SignOutInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSendAccountDeletionEmailMutation

sendAccountDeletionEmail

```
TYPE: mutation
USAGE: const { mutate } = useSendAccountDeletionEmailMutation()
  mutate({ input: <value> })

INPUT:
  input: SendAccountDeletionEmailInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useCheckPasswordMutation

checkPassword

```
TYPE: mutation
USAGE: const { mutate } = useCheckPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: CheckPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useConfirmDeleteAccountMutation

confirmDeleteAccount

```
TYPE: mutation
USAGE: const { mutate } = useConfirmDeleteAccountMutation()
  mutate({ input: <value> })

INPUT:
  input: ConfirmDeleteAccountInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSetPasswordMutation

setPassword

```
TYPE: mutation
USAGE: const { mutate } = useSetPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: SetPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useVerifyEmailMutation

verifyEmail

```
TYPE: mutation
USAGE: const { mutate } = useVerifyEmailMutation()
  mutate({ input: <value> })

INPUT:
  input: VerifyEmailInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useResetPasswordMutation

resetPassword

```
TYPE: mutation
USAGE: const { mutate } = useResetPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: ResetPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSignInOneTimeTokenMutation

signInOneTimeToken

```
TYPE: mutation
USAGE: const { mutate } = useSignInOneTimeTokenMutation()
  mutate({ input: <value> })

INPUT:
  input: SignInOneTimeTokenInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSignInMutation

signIn

```
TYPE: mutation
USAGE: const { mutate } = useSignInMutation()
  mutate({ input: <value> })

INPUT:
  input: SignInInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSignUpMutation

signUp

```
TYPE: mutation
USAGE: const { mutate } = useSignUpMutation()
  mutate({ input: <value> })

INPUT:
  input: SignUpInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useOneTimeTokenMutation

oneTimeToken

```
TYPE: mutation
USAGE: const { mutate } = useOneTimeTokenMutation()
  mutate({ input: <value> })

INPUT:
  input: OneTimeTokenInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useExtendTokenExpiresMutation

extendTokenExpires

```
TYPE: mutation
USAGE: const { mutate } = useExtendTokenExpiresMutation()
  mutate({ input: <value> })

INPUT:
  input: ExtendTokenExpiresInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useForgotPasswordMutation

forgotPassword

```
TYPE: mutation
USAGE: const { mutate } = useForgotPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: ForgotPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSendVerificationEmailMutation

sendVerificationEmail

```
TYPE: mutation
USAGE: const { mutate } = useSendVerificationEmailMutation()
  mutate({ input: <value> })

INPUT:
  input: SendVerificationEmailInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useVerifyPasswordMutation

verifyPassword

```
TYPE: mutation
USAGE: const { mutate } = useVerifyPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: VerifyPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useVerifyTotpMutation

verifyTotp

```
TYPE: mutation
USAGE: const { mutate } = useVerifyTotpMutation()
  mutate({ input: <value> })

INPUT:
  input: VerifyTotpInput (required)

OUTPUT: UseMutationResult
```
