# ORM Client - Agent Reference

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT
> This document is structured for LLM/agent consumption.

## OVERVIEW

Prisma-like ORM client for interacting with a GraphQL API.
All methods return a query builder. Call `.execute()` to run the query.

## SETUP

```typescript
import { createClient } from './orm';

const db = createClient({
  endpoint: 'https://api.example.com/graphql',
  headers: { Authorization: 'Bearer <token>' },
});
```

## MODELS

### MODEL: getAllRecord

Access: `db.getAllRecord`

```
METHODS:
  db.getAllRecord.findMany({ select, where?, orderBy?, first?, offset? })
  db.getAllRecord.findOne({ where: { id }, select })
  db.getAllRecord.create({ data: { path, data }, select })
  db.getAllRecord.update({ where: { id }, data, select })
  db.getAllRecord.delete({ where: { id } })

FIELDS:
  path: string[]
  data: unknown

EDITABLE FIELDS:
  path: string[]
  data: unknown

OUTPUT: Promise<JSON>
  findMany: [{ path, data }]
  findOne:  { path, data }
  create:   { path, data }
  update:   { path, data }
  delete:   { id }
```

### MODEL: appPermission

Access: `db.appPermission`

```
METHODS:
  db.appPermission.findMany({ select, where?, orderBy?, first?, offset? })
  db.appPermission.findOne({ where: { id }, select })
  db.appPermission.create({ data: { name, bitnum, bitstr, description }, select })
  db.appPermission.update({ where: { id }, data, select })
  db.appPermission.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  bitnum: number
  bitstr: string
  description: string

EDITABLE FIELDS:
  name: string
  bitnum: number
  bitstr: string
  description: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, bitnum, bitstr, description }]
  findOne:  { id, name, bitnum, bitstr, description }
  create:   { id, name, bitnum, bitstr, description }
  update:   { id, name, bitnum, bitstr, description }
  delete:   { id }
```

### MODEL: orgPermission

Access: `db.orgPermission`

```
METHODS:
  db.orgPermission.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgPermission.findOne({ where: { id }, select })
  db.orgPermission.create({ data: { name, bitnum, bitstr, description }, select })
  db.orgPermission.update({ where: { id }, data, select })
  db.orgPermission.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  bitnum: number
  bitstr: string
  description: string

EDITABLE FIELDS:
  name: string
  bitnum: number
  bitstr: string
  description: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, bitnum, bitstr, description }]
  findOne:  { id, name, bitnum, bitstr, description }
  create:   { id, name, bitnum, bitstr, description }
  update:   { id, name, bitnum, bitstr, description }
  delete:   { id }
```

### MODEL: object

Access: `db.object`

```
METHODS:
  db.object.findMany({ select, where?, orderBy?, first?, offset? })
  db.object.findOne({ where: { id }, select })
  db.object.create({ data: { hashUuid, databaseId, kids, ktree, data, frzn }, select })
  db.object.update({ where: { id }, data, select })
  db.object.delete({ where: { id } })

FIELDS:
  hashUuid: string
  id: string (primary key)
  databaseId: string
  kids: string[]
  ktree: string[]
  data: unknown
  frzn: boolean
  createdAt: string

EDITABLE FIELDS:
  hashUuid: string
  databaseId: string
  kids: string[]
  ktree: string[]
  data: unknown
  frzn: boolean

OUTPUT: Promise<JSON>
  findMany: [{ hashUuid, id, databaseId, kids, ktree, data, frzn, createdAt }]
  findOne:  { hashUuid, id, databaseId, kids, ktree, data, frzn, createdAt }
  create:   { hashUuid, id, databaseId, kids, ktree, data, frzn, createdAt }
  update:   { hashUuid, id, databaseId, kids, ktree, data, frzn, createdAt }
  delete:   { id }
```

### MODEL: appLevelRequirement

Access: `db.appLevelRequirement`

```
METHODS:
  db.appLevelRequirement.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLevelRequirement.findOne({ where: { id }, select })
  db.appLevelRequirement.create({ data: { name, level, description, requiredCount, priority }, select })
  db.appLevelRequirement.update({ where: { id }, data, select })
  db.appLevelRequirement.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  level: string
  description: string
  requiredCount: number
  priority: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  name: string
  level: string
  description: string
  requiredCount: number
  priority: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, level, description, requiredCount, priority, createdAt, updatedAt }]
  findOne:  { id, name, level, description, requiredCount, priority, createdAt, updatedAt }
  create:   { id, name, level, description, requiredCount, priority, createdAt, updatedAt }
  update:   { id, name, level, description, requiredCount, priority, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: database

Access: `db.database`

```
METHODS:
  db.database.findMany({ select, where?, orderBy?, first?, offset? })
  db.database.findOne({ where: { id }, select })
  db.database.create({ data: { ownerId, schemaHash, name, label, hash }, select })
  db.database.update({ where: { id }, data, select })
  db.database.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  ownerId: string
  schemaHash: string
  name: string
  label: string
  hash: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  ownerId: string
  schemaHash: string
  name: string
  label: string
  hash: string

OUTPUT: Promise<JSON>
  findMany: [{ id, ownerId, schemaHash, name, label, hash, createdAt, updatedAt }]
  findOne:  { id, ownerId, schemaHash, name, label, hash, createdAt, updatedAt }
  create:   { id, ownerId, schemaHash, name, label, hash, createdAt, updatedAt }
  update:   { id, ownerId, schemaHash, name, label, hash, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: schema

Access: `db.schema`

```
METHODS:
  db.schema.findMany({ select, where?, orderBy?, first?, offset? })
  db.schema.findOne({ where: { id }, select })
  db.schema.create({ data: { databaseId, name, schemaName, label, description, smartTags, category, module, scope, tags, isPublic }, select })
  db.schema.update({ where: { id }, data, select })
  db.schema.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  name: string
  schemaName: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  isPublic: boolean
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  name: string
  schemaName: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  isPublic: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, name, schemaName, label, description, smartTags, category, module, scope, tags, isPublic, createdAt, updatedAt }]
  findOne:  { id, databaseId, name, schemaName, label, description, smartTags, category, module, scope, tags, isPublic, createdAt, updatedAt }
  create:   { id, databaseId, name, schemaName, label, description, smartTags, category, module, scope, tags, isPublic, createdAt, updatedAt }
  update:   { id, databaseId, name, schemaName, label, description, smartTags, category, module, scope, tags, isPublic, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: table

Access: `db.table`

```
METHODS:
  db.table.findMany({ select, where?, orderBy?, first?, offset? })
  db.table.findOne({ where: { id }, select })
  db.table.create({ data: { databaseId, schemaId, name, label, description, smartTags, category, module, scope, useRls, timestamps, peoplestamps, pluralName, singularName, tags, inheritsId }, select })
  db.table.update({ where: { id }, data, select })
  db.table.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  useRls: boolean
  timestamps: boolean
  peoplestamps: boolean
  pluralName: string
  singularName: string
  tags: string[]
  inheritsId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  useRls: boolean
  timestamps: boolean
  peoplestamps: boolean
  pluralName: string
  singularName: string
  tags: string[]
  inheritsId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, name, label, description, smartTags, category, module, scope, useRls, timestamps, peoplestamps, pluralName, singularName, tags, inheritsId, createdAt, updatedAt }]
  findOne:  { id, databaseId, schemaId, name, label, description, smartTags, category, module, scope, useRls, timestamps, peoplestamps, pluralName, singularName, tags, inheritsId, createdAt, updatedAt }
  create:   { id, databaseId, schemaId, name, label, description, smartTags, category, module, scope, useRls, timestamps, peoplestamps, pluralName, singularName, tags, inheritsId, createdAt, updatedAt }
  update:   { id, databaseId, schemaId, name, label, description, smartTags, category, module, scope, useRls, timestamps, peoplestamps, pluralName, singularName, tags, inheritsId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: checkConstraint

Access: `db.checkConstraint`

```
METHODS:
  db.checkConstraint.findMany({ select, where?, orderBy?, first?, offset? })
  db.checkConstraint.findOne({ where: { id }, select })
  db.checkConstraint.create({ data: { databaseId, tableId, name, type, fieldIds, expr, smartTags, category, module, scope, tags }, select })
  db.checkConstraint.update({ where: { id }, data, select })
  db.checkConstraint.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  expr: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  expr: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, type, fieldIds, expr, smartTags, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, type, fieldIds, expr, smartTags, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, type, fieldIds, expr, smartTags, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, type, fieldIds, expr, smartTags, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: field

Access: `db.field`

```
METHODS:
  db.field.findMany({ select, where?, orderBy?, first?, offset? })
  db.field.findOne({ where: { id }, select })
  db.field.create({ data: { databaseId, tableId, name, label, description, smartTags, isRequired, defaultValue, defaultValueAst, isHidden, type, fieldOrder, regexp, chk, chkExpr, min, max, tags, category, module, scope }, select })
  db.field.update({ where: { id }, data, select })
  db.field.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  isRequired: boolean
  defaultValue: string
  defaultValueAst: unknown
  isHidden: boolean
  type: string
  fieldOrder: number
  regexp: string
  chk: unknown
  chkExpr: unknown
  min: number
  max: number
  tags: string[]
  category: ObjectCategory
  module: string
  scope: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  isRequired: boolean
  defaultValue: string
  defaultValueAst: unknown
  isHidden: boolean
  type: string
  fieldOrder: number
  regexp: string
  chk: unknown
  chkExpr: unknown
  min: number
  max: number
  tags: string[]
  category: ObjectCategory
  module: string
  scope: number

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, label, description, smartTags, isRequired, defaultValue, defaultValueAst, isHidden, type, fieldOrder, regexp, chk, chkExpr, min, max, tags, category, module, scope, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, label, description, smartTags, isRequired, defaultValue, defaultValueAst, isHidden, type, fieldOrder, regexp, chk, chkExpr, min, max, tags, category, module, scope, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, label, description, smartTags, isRequired, defaultValue, defaultValueAst, isHidden, type, fieldOrder, regexp, chk, chkExpr, min, max, tags, category, module, scope, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, label, description, smartTags, isRequired, defaultValue, defaultValueAst, isHidden, type, fieldOrder, regexp, chk, chkExpr, min, max, tags, category, module, scope, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: foreignKeyConstraint

Access: `db.foreignKeyConstraint`

```
METHODS:
  db.foreignKeyConstraint.findMany({ select, where?, orderBy?, first?, offset? })
  db.foreignKeyConstraint.findOne({ where: { id }, select })
  db.foreignKeyConstraint.create({ data: { databaseId, tableId, name, description, smartTags, type, fieldIds, refTableId, refFieldIds, deleteAction, updateAction, category, module, scope, tags }, select })
  db.foreignKeyConstraint.update({ where: { id }, data, select })
  db.foreignKeyConstraint.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  refTableId: string
  refFieldIds: string[]
  deleteAction: string
  updateAction: string
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  refTableId: string
  refFieldIds: string[]
  deleteAction: string
  updateAction: string
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, description, smartTags, type, fieldIds, refTableId, refFieldIds, deleteAction, updateAction, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, description, smartTags, type, fieldIds, refTableId, refFieldIds, deleteAction, updateAction, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, description, smartTags, type, fieldIds, refTableId, refFieldIds, deleteAction, updateAction, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, description, smartTags, type, fieldIds, refTableId, refFieldIds, deleteAction, updateAction, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: fullTextSearch

Access: `db.fullTextSearch`

```
METHODS:
  db.fullTextSearch.findMany({ select, where?, orderBy?, first?, offset? })
  db.fullTextSearch.findOne({ where: { id }, select })
  db.fullTextSearch.create({ data: { databaseId, tableId, fieldId, fieldIds, weights, langs }, select })
  db.fullTextSearch.update({ where: { id }, data, select })
  db.fullTextSearch.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  fieldId: string
  fieldIds: string[]
  weights: string[]
  langs: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  fieldId: string
  fieldIds: string[]
  weights: string[]
  langs: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, fieldId, fieldIds, weights, langs, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, fieldId, fieldIds, weights, langs, createdAt, updatedAt }
  create:   { id, databaseId, tableId, fieldId, fieldIds, weights, langs, createdAt, updatedAt }
  update:   { id, databaseId, tableId, fieldId, fieldIds, weights, langs, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: index

Access: `db.index`

```
METHODS:
  db.index.findMany({ select, where?, orderBy?, first?, offset? })
  db.index.findOne({ where: { id }, select })
  db.index.create({ data: { databaseId, tableId, name, fieldIds, includeFieldIds, accessMethod, indexParams, whereClause, isUnique, smartTags, category, module, scope, tags }, select })
  db.index.update({ where: { id }, data, select })
  db.index.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  fieldIds: string[]
  includeFieldIds: string[]
  accessMethod: string
  indexParams: unknown
  whereClause: unknown
  isUnique: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  fieldIds: string[]
  includeFieldIds: string[]
  accessMethod: string
  indexParams: unknown
  whereClause: unknown
  isUnique: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, fieldIds, includeFieldIds, accessMethod, indexParams, whereClause, isUnique, smartTags, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, fieldIds, includeFieldIds, accessMethod, indexParams, whereClause, isUnique, smartTags, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, fieldIds, includeFieldIds, accessMethod, indexParams, whereClause, isUnique, smartTags, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, fieldIds, includeFieldIds, accessMethod, indexParams, whereClause, isUnique, smartTags, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: limitFunction

Access: `db.limitFunction`

```
METHODS:
  db.limitFunction.findMany({ select, where?, orderBy?, first?, offset? })
  db.limitFunction.findOne({ where: { id }, select })
  db.limitFunction.create({ data: { databaseId, tableId, name, label, description, data, security }, select })
  db.limitFunction.update({ where: { id }, data, select })
  db.limitFunction.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  data: unknown
  security: number

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  data: unknown
  security: number

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, label, description, data, security }]
  findOne:  { id, databaseId, tableId, name, label, description, data, security }
  create:   { id, databaseId, tableId, name, label, description, data, security }
  update:   { id, databaseId, tableId, name, label, description, data, security }
  delete:   { id }
```

### MODEL: policy

Access: `db.policy`

```
METHODS:
  db.policy.findMany({ select, where?, orderBy?, first?, offset? })
  db.policy.findOne({ where: { id }, select })
  db.policy.create({ data: { databaseId, tableId, name, roleName, privilege, permissive, disabled, policyType, data, smartTags, category, module, scope, tags }, select })
  db.policy.update({ where: { id }, data, select })
  db.policy.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  roleName: string
  privilege: string
  permissive: boolean
  disabled: boolean
  policyType: string
  data: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  roleName: string
  privilege: string
  permissive: boolean
  disabled: boolean
  policyType: string
  data: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, roleName, privilege, permissive, disabled, policyType, data, smartTags, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, roleName, privilege, permissive, disabled, policyType, data, smartTags, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, roleName, privilege, permissive, disabled, policyType, data, smartTags, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, roleName, privilege, permissive, disabled, policyType, data, smartTags, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: primaryKeyConstraint

Access: `db.primaryKeyConstraint`

```
METHODS:
  db.primaryKeyConstraint.findMany({ select, where?, orderBy?, first?, offset? })
  db.primaryKeyConstraint.findOne({ where: { id }, select })
  db.primaryKeyConstraint.create({ data: { databaseId, tableId, name, type, fieldIds, smartTags, category, module, scope, tags }, select })
  db.primaryKeyConstraint.update({ where: { id }, data, select })
  db.primaryKeyConstraint.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, type, fieldIds, smartTags, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, type, fieldIds, smartTags, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, type, fieldIds, smartTags, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, type, fieldIds, smartTags, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: tableGrant

Access: `db.tableGrant`

```
METHODS:
  db.tableGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.tableGrant.findOne({ where: { id }, select })
  db.tableGrant.create({ data: { databaseId, tableId, privilege, roleName, fieldIds }, select })
  db.tableGrant.update({ where: { id }, data, select })
  db.tableGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  privilege: string
  roleName: string
  fieldIds: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  privilege: string
  roleName: string
  fieldIds: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, privilege, roleName, fieldIds, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, privilege, roleName, fieldIds, createdAt, updatedAt }
  create:   { id, databaseId, tableId, privilege, roleName, fieldIds, createdAt, updatedAt }
  update:   { id, databaseId, tableId, privilege, roleName, fieldIds, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: trigger

Access: `db.trigger`

```
METHODS:
  db.trigger.findMany({ select, where?, orderBy?, first?, offset? })
  db.trigger.findOne({ where: { id }, select })
  db.trigger.create({ data: { databaseId, tableId, name, event, functionName, smartTags, category, module, scope, tags }, select })
  db.trigger.update({ where: { id }, data, select })
  db.trigger.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  event: string
  functionName: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  event: string
  functionName: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, event, functionName, smartTags, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, event, functionName, smartTags, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, event, functionName, smartTags, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, event, functionName, smartTags, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: uniqueConstraint

Access: `db.uniqueConstraint`

```
METHODS:
  db.uniqueConstraint.findMany({ select, where?, orderBy?, first?, offset? })
  db.uniqueConstraint.findOne({ where: { id }, select })
  db.uniqueConstraint.create({ data: { databaseId, tableId, name, description, smartTags, type, fieldIds, category, module, scope, tags }, select })
  db.uniqueConstraint.update({ where: { id }, data, select })
  db.uniqueConstraint.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, name, description, smartTags, type, fieldIds, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, tableId, name, description, smartTags, type, fieldIds, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, tableId, name, description, smartTags, type, fieldIds, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, tableId, name, description, smartTags, type, fieldIds, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: view

Access: `db.view`

```
METHODS:
  db.view.findMany({ select, where?, orderBy?, first?, offset? })
  db.view.findOne({ where: { id }, select })
  db.view.create({ data: { databaseId, schemaId, name, tableId, viewType, data, filterType, filterData, securityInvoker, isReadOnly, smartTags, category, module, scope, tags }, select })
  db.view.update({ where: { id }, data, select })
  db.view.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  name: string
  tableId: string
  viewType: string
  data: unknown
  filterType: string
  filterData: unknown
  securityInvoker: boolean
  isReadOnly: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  name: string
  tableId: string
  viewType: string
  data: unknown
  filterType: string
  filterData: unknown
  securityInvoker: boolean
  isReadOnly: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, name, tableId, viewType, data, filterType, filterData, securityInvoker, isReadOnly, smartTags, category, module, scope, tags }]
  findOne:  { id, databaseId, schemaId, name, tableId, viewType, data, filterType, filterData, securityInvoker, isReadOnly, smartTags, category, module, scope, tags }
  create:   { id, databaseId, schemaId, name, tableId, viewType, data, filterType, filterData, securityInvoker, isReadOnly, smartTags, category, module, scope, tags }
  update:   { id, databaseId, schemaId, name, tableId, viewType, data, filterType, filterData, securityInvoker, isReadOnly, smartTags, category, module, scope, tags }
  delete:   { id }
```

### MODEL: viewTable

Access: `db.viewTable`

```
METHODS:
  db.viewTable.findMany({ select, where?, orderBy?, first?, offset? })
  db.viewTable.findOne({ where: { id }, select })
  db.viewTable.create({ data: { viewId, tableId, joinOrder }, select })
  db.viewTable.update({ where: { id }, data, select })
  db.viewTable.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  viewId: string
  tableId: string
  joinOrder: number

EDITABLE FIELDS:
  viewId: string
  tableId: string
  joinOrder: number

OUTPUT: Promise<JSON>
  findMany: [{ id, viewId, tableId, joinOrder }]
  findOne:  { id, viewId, tableId, joinOrder }
  create:   { id, viewId, tableId, joinOrder }
  update:   { id, viewId, tableId, joinOrder }
  delete:   { id }
```

### MODEL: viewGrant

Access: `db.viewGrant`

```
METHODS:
  db.viewGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.viewGrant.findOne({ where: { id }, select })
  db.viewGrant.create({ data: { databaseId, viewId, roleName, privilege, withGrantOption }, select })
  db.viewGrant.update({ where: { id }, data, select })
  db.viewGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  viewId: string
  roleName: string
  privilege: string
  withGrantOption: boolean

EDITABLE FIELDS:
  databaseId: string
  viewId: string
  roleName: string
  privilege: string
  withGrantOption: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, viewId, roleName, privilege, withGrantOption }]
  findOne:  { id, databaseId, viewId, roleName, privilege, withGrantOption }
  create:   { id, databaseId, viewId, roleName, privilege, withGrantOption }
  update:   { id, databaseId, viewId, roleName, privilege, withGrantOption }
  delete:   { id }
```

### MODEL: viewRule

Access: `db.viewRule`

```
METHODS:
  db.viewRule.findMany({ select, where?, orderBy?, first?, offset? })
  db.viewRule.findOne({ where: { id }, select })
  db.viewRule.create({ data: { databaseId, viewId, name, event, action }, select })
  db.viewRule.update({ where: { id }, data, select })
  db.viewRule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  viewId: string
  name: string
  event: string
  action: string

EDITABLE FIELDS:
  databaseId: string
  viewId: string
  name: string
  event: string
  action: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, viewId, name, event, action }]
  findOne:  { id, databaseId, viewId, name, event, action }
  create:   { id, databaseId, viewId, name, event, action }
  update:   { id, databaseId, viewId, name, event, action }
  delete:   { id }
```

### MODEL: tableModule

Access: `db.tableModule`

```
METHODS:
  db.tableModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.tableModule.findOne({ where: { id }, select })
  db.tableModule.create({ data: { databaseId, privateSchemaId, tableId, nodeType, data, fields }, select })
  db.tableModule.update({ where: { id }, data, select })
  db.tableModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  privateSchemaId: string
  tableId: string
  nodeType: string
  data: unknown
  fields: string[]

EDITABLE FIELDS:
  databaseId: string
  privateSchemaId: string
  tableId: string
  nodeType: string
  data: unknown
  fields: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, privateSchemaId, tableId, nodeType, data, fields }]
  findOne:  { id, databaseId, privateSchemaId, tableId, nodeType, data, fields }
  create:   { id, databaseId, privateSchemaId, tableId, nodeType, data, fields }
  update:   { id, databaseId, privateSchemaId, tableId, nodeType, data, fields }
  delete:   { id }
```

### MODEL: tableTemplateModule

Access: `db.tableTemplateModule`

```
METHODS:
  db.tableTemplateModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.tableTemplateModule.findOne({ where: { id }, select })
  db.tableTemplateModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, nodeType, data }, select })
  db.tableTemplateModule.update({ where: { id }, data, select })
  db.tableTemplateModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  nodeType: string
  data: unknown

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  nodeType: string
  data: unknown

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, nodeType, data }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, nodeType, data }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, nodeType, data }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, nodeType, data }
  delete:   { id }
```

### MODEL: schemaGrant

Access: `db.schemaGrant`

```
METHODS:
  db.schemaGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.schemaGrant.findOne({ where: { id }, select })
  db.schemaGrant.create({ data: { databaseId, schemaId, granteeName }, select })
  db.schemaGrant.update({ where: { id }, data, select })
  db.schemaGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  granteeName: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  granteeName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, granteeName, createdAt, updatedAt }]
  findOne:  { id, databaseId, schemaId, granteeName, createdAt, updatedAt }
  create:   { id, databaseId, schemaId, granteeName, createdAt, updatedAt }
  update:   { id, databaseId, schemaId, granteeName, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: apiSchema

Access: `db.apiSchema`

```
METHODS:
  db.apiSchema.findMany({ select, where?, orderBy?, first?, offset? })
  db.apiSchema.findOne({ where: { id }, select })
  db.apiSchema.create({ data: { databaseId, schemaId, apiId }, select })
  db.apiSchema.update({ where: { id }, data, select })
  db.apiSchema.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  apiId: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  apiId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, apiId }]
  findOne:  { id, databaseId, schemaId, apiId }
  create:   { id, databaseId, schemaId, apiId }
  update:   { id, databaseId, schemaId, apiId }
  delete:   { id }
```

### MODEL: apiModule

Access: `db.apiModule`

```
METHODS:
  db.apiModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.apiModule.findOne({ where: { id }, select })
  db.apiModule.create({ data: { databaseId, apiId, name, data }, select })
  db.apiModule.update({ where: { id }, data, select })
  db.apiModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  apiId: string
  name: string
  data: unknown

EDITABLE FIELDS:
  databaseId: string
  apiId: string
  name: string
  data: unknown

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, apiId, name, data }]
  findOne:  { id, databaseId, apiId, name, data }
  create:   { id, databaseId, apiId, name, data }
  update:   { id, databaseId, apiId, name, data }
  delete:   { id }
```

### MODEL: domain

Access: `db.domain`

```
METHODS:
  db.domain.findMany({ select, where?, orderBy?, first?, offset? })
  db.domain.findOne({ where: { id }, select })
  db.domain.create({ data: { databaseId, apiId, siteId, subdomain, domain }, select })
  db.domain.update({ where: { id }, data, select })
  db.domain.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  apiId: string
  siteId: string
  subdomain: ConstructiveInternalTypeHostname
  domain: ConstructiveInternalTypeHostname

EDITABLE FIELDS:
  databaseId: string
  apiId: string
  siteId: string
  subdomain: ConstructiveInternalTypeHostname
  domain: ConstructiveInternalTypeHostname

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, apiId, siteId, subdomain, domain }]
  findOne:  { id, databaseId, apiId, siteId, subdomain, domain }
  create:   { id, databaseId, apiId, siteId, subdomain, domain }
  update:   { id, databaseId, apiId, siteId, subdomain, domain }
  delete:   { id }
```

### MODEL: siteMetadatum

Access: `db.siteMetadatum`

```
METHODS:
  db.siteMetadatum.findMany({ select, where?, orderBy?, first?, offset? })
  db.siteMetadatum.findOne({ where: { id }, select })
  db.siteMetadatum.create({ data: { databaseId, siteId, title, description, ogImage }, select })
  db.siteMetadatum.update({ where: { id }, data, select })
  db.siteMetadatum.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  siteId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage

EDITABLE FIELDS:
  databaseId: string
  siteId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, siteId, title, description, ogImage }]
  findOne:  { id, databaseId, siteId, title, description, ogImage }
  create:   { id, databaseId, siteId, title, description, ogImage }
  update:   { id, databaseId, siteId, title, description, ogImage }
  delete:   { id }
```

### MODEL: siteModule

Access: `db.siteModule`

```
METHODS:
  db.siteModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.siteModule.findOne({ where: { id }, select })
  db.siteModule.create({ data: { databaseId, siteId, name, data }, select })
  db.siteModule.update({ where: { id }, data, select })
  db.siteModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  siteId: string
  name: string
  data: unknown

EDITABLE FIELDS:
  databaseId: string
  siteId: string
  name: string
  data: unknown

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, siteId, name, data }]
  findOne:  { id, databaseId, siteId, name, data }
  create:   { id, databaseId, siteId, name, data }
  update:   { id, databaseId, siteId, name, data }
  delete:   { id }
```

### MODEL: siteTheme

Access: `db.siteTheme`

```
METHODS:
  db.siteTheme.findMany({ select, where?, orderBy?, first?, offset? })
  db.siteTheme.findOne({ where: { id }, select })
  db.siteTheme.create({ data: { databaseId, siteId, theme }, select })
  db.siteTheme.update({ where: { id }, data, select })
  db.siteTheme.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  siteId: string
  theme: unknown

EDITABLE FIELDS:
  databaseId: string
  siteId: string
  theme: unknown

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, siteId, theme }]
  findOne:  { id, databaseId, siteId, theme }
  create:   { id, databaseId, siteId, theme }
  update:   { id, databaseId, siteId, theme }
  delete:   { id }
```

### MODEL: procedure

Access: `db.procedure`

```
METHODS:
  db.procedure.findMany({ select, where?, orderBy?, first?, offset? })
  db.procedure.findOne({ where: { id }, select })
  db.procedure.create({ data: { databaseId, name, argnames, argtypes, argdefaults, langName, definition, smartTags, category, module, scope, tags }, select })
  db.procedure.update({ where: { id }, data, select })
  db.procedure.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  name: string
  argnames: string[]
  argtypes: string[]
  argdefaults: string[]
  langName: string
  definition: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  name: string
  argnames: string[]
  argtypes: string[]
  argdefaults: string[]
  langName: string
  definition: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, name, argnames, argtypes, argdefaults, langName, definition, smartTags, category, module, scope, tags, createdAt, updatedAt }]
  findOne:  { id, databaseId, name, argnames, argtypes, argdefaults, langName, definition, smartTags, category, module, scope, tags, createdAt, updatedAt }
  create:   { id, databaseId, name, argnames, argtypes, argdefaults, langName, definition, smartTags, category, module, scope, tags, createdAt, updatedAt }
  update:   { id, databaseId, name, argnames, argtypes, argdefaults, langName, definition, smartTags, category, module, scope, tags, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: triggerFunction

Access: `db.triggerFunction`

```
METHODS:
  db.triggerFunction.findMany({ select, where?, orderBy?, first?, offset? })
  db.triggerFunction.findOne({ where: { id }, select })
  db.triggerFunction.create({ data: { databaseId, name, code }, select })
  db.triggerFunction.update({ where: { id }, data, select })
  db.triggerFunction.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  name: string
  code: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  databaseId: string
  name: string
  code: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, name, code, createdAt, updatedAt }]
  findOne:  { id, databaseId, name, code, createdAt, updatedAt }
  create:   { id, databaseId, name, code, createdAt, updatedAt }
  update:   { id, databaseId, name, code, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: api

Access: `db.api`

```
METHODS:
  db.api.findMany({ select, where?, orderBy?, first?, offset? })
  db.api.findOne({ where: { id }, select })
  db.api.create({ data: { databaseId, name, dbname, roleName, anonRole, isPublic }, select })
  db.api.update({ where: { id }, data, select })
  db.api.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  name: string
  dbname: string
  roleName: string
  anonRole: string
  isPublic: boolean

EDITABLE FIELDS:
  databaseId: string
  name: string
  dbname: string
  roleName: string
  anonRole: string
  isPublic: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, name, dbname, roleName, anonRole, isPublic }]
  findOne:  { id, databaseId, name, dbname, roleName, anonRole, isPublic }
  create:   { id, databaseId, name, dbname, roleName, anonRole, isPublic }
  update:   { id, databaseId, name, dbname, roleName, anonRole, isPublic }
  delete:   { id }
```

### MODEL: site

Access: `db.site`

```
METHODS:
  db.site.findMany({ select, where?, orderBy?, first?, offset? })
  db.site.findOne({ where: { id }, select })
  db.site.create({ data: { databaseId, title, description, ogImage, favicon, appleTouchIcon, logo, dbname }, select })
  db.site.update({ where: { id }, data, select })
  db.site.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage
  favicon: ConstructiveInternalTypeAttachment
  appleTouchIcon: ConstructiveInternalTypeImage
  logo: ConstructiveInternalTypeImage
  dbname: string

EDITABLE FIELDS:
  databaseId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage
  favicon: ConstructiveInternalTypeAttachment
  appleTouchIcon: ConstructiveInternalTypeImage
  logo: ConstructiveInternalTypeImage
  dbname: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, title, description, ogImage, favicon, appleTouchIcon, logo, dbname }]
  findOne:  { id, databaseId, title, description, ogImage, favicon, appleTouchIcon, logo, dbname }
  create:   { id, databaseId, title, description, ogImage, favicon, appleTouchIcon, logo, dbname }
  update:   { id, databaseId, title, description, ogImage, favicon, appleTouchIcon, logo, dbname }
  delete:   { id }
```

### MODEL: app

Access: `db.app`

```
METHODS:
  db.app.findMany({ select, where?, orderBy?, first?, offset? })
  db.app.findOne({ where: { id }, select })
  db.app.create({ data: { databaseId, siteId, name, appImage, appStoreLink, appStoreId, appIdPrefix, playStoreLink }, select })
  db.app.update({ where: { id }, data, select })
  db.app.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  siteId: string
  name: string
  appImage: ConstructiveInternalTypeImage
  appStoreLink: ConstructiveInternalTypeUrl
  appStoreId: string
  appIdPrefix: string
  playStoreLink: ConstructiveInternalTypeUrl

EDITABLE FIELDS:
  databaseId: string
  siteId: string
  name: string
  appImage: ConstructiveInternalTypeImage
  appStoreLink: ConstructiveInternalTypeUrl
  appStoreId: string
  appIdPrefix: string
  playStoreLink: ConstructiveInternalTypeUrl

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, siteId, name, appImage, appStoreLink, appStoreId, appIdPrefix, playStoreLink }]
  findOne:  { id, databaseId, siteId, name, appImage, appStoreLink, appStoreId, appIdPrefix, playStoreLink }
  create:   { id, databaseId, siteId, name, appImage, appStoreLink, appStoreId, appIdPrefix, playStoreLink }
  update:   { id, databaseId, siteId, name, appImage, appStoreLink, appStoreId, appIdPrefix, playStoreLink }
  delete:   { id }
```

### MODEL: connectedAccountsModule

Access: `db.connectedAccountsModule`

```
METHODS:
  db.connectedAccountsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.connectedAccountsModule.findOne({ where: { id }, select })
  db.connectedAccountsModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }, select })
  db.connectedAccountsModule.update({ where: { id }, data, select })
  db.connectedAccountsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  delete:   { id }
```

### MODEL: cryptoAddressesModule

Access: `db.cryptoAddressesModule`

```
METHODS:
  db.cryptoAddressesModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.cryptoAddressesModule.findOne({ where: { id }, select })
  db.cryptoAddressesModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, cryptoNetwork }, select })
  db.cryptoAddressesModule.update({ where: { id }, data, select })
  db.cryptoAddressesModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  cryptoNetwork: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  cryptoNetwork: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, cryptoNetwork }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, cryptoNetwork }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, cryptoNetwork }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName, cryptoNetwork }
  delete:   { id }
```

### MODEL: cryptoAuthModule

Access: `db.cryptoAuthModule`

```
METHODS:
  db.cryptoAuthModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.cryptoAuthModule.findOne({ where: { id }, select })
  db.cryptoAuthModule.create({ data: { databaseId, schemaId, usersTableId, secretsTableId, sessionsTableId, sessionCredentialsTableId, addressesTableId, userField, cryptoNetwork, signInRequestChallenge, signInRecordFailure, signUpWithKey, signInWithChallenge }, select })
  db.cryptoAuthModule.update({ where: { id }, data, select })
  db.cryptoAuthModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  usersTableId: string
  secretsTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  addressesTableId: string
  userField: string
  cryptoNetwork: string
  signInRequestChallenge: string
  signInRecordFailure: string
  signUpWithKey: string
  signInWithChallenge: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  usersTableId: string
  secretsTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  addressesTableId: string
  userField: string
  cryptoNetwork: string
  signInRequestChallenge: string
  signInRecordFailure: string
  signUpWithKey: string
  signInWithChallenge: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, usersTableId, secretsTableId, sessionsTableId, sessionCredentialsTableId, addressesTableId, userField, cryptoNetwork, signInRequestChallenge, signInRecordFailure, signUpWithKey, signInWithChallenge }]
  findOne:  { id, databaseId, schemaId, usersTableId, secretsTableId, sessionsTableId, sessionCredentialsTableId, addressesTableId, userField, cryptoNetwork, signInRequestChallenge, signInRecordFailure, signUpWithKey, signInWithChallenge }
  create:   { id, databaseId, schemaId, usersTableId, secretsTableId, sessionsTableId, sessionCredentialsTableId, addressesTableId, userField, cryptoNetwork, signInRequestChallenge, signInRecordFailure, signUpWithKey, signInWithChallenge }
  update:   { id, databaseId, schemaId, usersTableId, secretsTableId, sessionsTableId, sessionCredentialsTableId, addressesTableId, userField, cryptoNetwork, signInRequestChallenge, signInRecordFailure, signUpWithKey, signInWithChallenge }
  delete:   { id }
```

### MODEL: defaultIdsModule

Access: `db.defaultIdsModule`

```
METHODS:
  db.defaultIdsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.defaultIdsModule.findOne({ where: { id }, select })
  db.defaultIdsModule.create({ data: { databaseId }, select })
  db.defaultIdsModule.update({ where: { id }, data, select })
  db.defaultIdsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string

EDITABLE FIELDS:
  databaseId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId }]
  findOne:  { id, databaseId }
  create:   { id, databaseId }
  update:   { id, databaseId }
  delete:   { id }
```

### MODEL: denormalizedTableField

Access: `db.denormalizedTableField`

```
METHODS:
  db.denormalizedTableField.findMany({ select, where?, orderBy?, first?, offset? })
  db.denormalizedTableField.findOne({ where: { id }, select })
  db.denormalizedTableField.create({ data: { databaseId, tableId, fieldId, setIds, refTableId, refFieldId, refIds, useUpdates, updateDefaults, funcName, funcOrder }, select })
  db.denormalizedTableField.update({ where: { id }, data, select })
  db.denormalizedTableField.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  tableId: string
  fieldId: string
  setIds: string[]
  refTableId: string
  refFieldId: string
  refIds: string[]
  useUpdates: boolean
  updateDefaults: boolean
  funcName: string
  funcOrder: number

EDITABLE FIELDS:
  databaseId: string
  tableId: string
  fieldId: string
  setIds: string[]
  refTableId: string
  refFieldId: string
  refIds: string[]
  useUpdates: boolean
  updateDefaults: boolean
  funcName: string
  funcOrder: number

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, tableId, fieldId, setIds, refTableId, refFieldId, refIds, useUpdates, updateDefaults, funcName, funcOrder }]
  findOne:  { id, databaseId, tableId, fieldId, setIds, refTableId, refFieldId, refIds, useUpdates, updateDefaults, funcName, funcOrder }
  create:   { id, databaseId, tableId, fieldId, setIds, refTableId, refFieldId, refIds, useUpdates, updateDefaults, funcName, funcOrder }
  update:   { id, databaseId, tableId, fieldId, setIds, refTableId, refFieldId, refIds, useUpdates, updateDefaults, funcName, funcOrder }
  delete:   { id }
```

### MODEL: emailsModule

Access: `db.emailsModule`

```
METHODS:
  db.emailsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.emailsModule.findOne({ where: { id }, select })
  db.emailsModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }, select })
  db.emailsModule.update({ where: { id }, data, select })
  db.emailsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  delete:   { id }
```

### MODEL: encryptedSecretsModule

Access: `db.encryptedSecretsModule`

```
METHODS:
  db.encryptedSecretsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.encryptedSecretsModule.findOne({ where: { id }, select })
  db.encryptedSecretsModule.create({ data: { databaseId, schemaId, tableId, tableName }, select })
  db.encryptedSecretsModule.update({ where: { id }, data, select })
  db.encryptedSecretsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, tableId, tableName }]
  findOne:  { id, databaseId, schemaId, tableId, tableName }
  create:   { id, databaseId, schemaId, tableId, tableName }
  update:   { id, databaseId, schemaId, tableId, tableName }
  delete:   { id }
```

### MODEL: fieldModule

Access: `db.fieldModule`

```
METHODS:
  db.fieldModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.fieldModule.findOne({ where: { id }, select })
  db.fieldModule.create({ data: { databaseId, privateSchemaId, tableId, fieldId, nodeType, data, triggers, functions }, select })
  db.fieldModule.update({ where: { id }, data, select })
  db.fieldModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  privateSchemaId: string
  tableId: string
  fieldId: string
  nodeType: string
  data: unknown
  triggers: string[]
  functions: string[]

EDITABLE FIELDS:
  databaseId: string
  privateSchemaId: string
  tableId: string
  fieldId: string
  nodeType: string
  data: unknown
  triggers: string[]
  functions: string[]

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, privateSchemaId, tableId, fieldId, nodeType, data, triggers, functions }]
  findOne:  { id, databaseId, privateSchemaId, tableId, fieldId, nodeType, data, triggers, functions }
  create:   { id, databaseId, privateSchemaId, tableId, fieldId, nodeType, data, triggers, functions }
  update:   { id, databaseId, privateSchemaId, tableId, fieldId, nodeType, data, triggers, functions }
  delete:   { id }
```

### MODEL: invitesModule

Access: `db.invitesModule`

```
METHODS:
  db.invitesModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.invitesModule.findOne({ where: { id }, select })
  db.invitesModule.create({ data: { databaseId, schemaId, privateSchemaId, emailsTableId, usersTableId, invitesTableId, claimedInvitesTableId, invitesTableName, claimedInvitesTableName, submitInviteCodeFunction, prefix, membershipType, entityTableId }, select })
  db.invitesModule.update({ where: { id }, data, select })
  db.invitesModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  emailsTableId: string
  usersTableId: string
  invitesTableId: string
  claimedInvitesTableId: string
  invitesTableName: string
  claimedInvitesTableName: string
  submitInviteCodeFunction: string
  prefix: string
  membershipType: number
  entityTableId: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  emailsTableId: string
  usersTableId: string
  invitesTableId: string
  claimedInvitesTableId: string
  invitesTableName: string
  claimedInvitesTableName: string
  submitInviteCodeFunction: string
  prefix: string
  membershipType: number
  entityTableId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, emailsTableId, usersTableId, invitesTableId, claimedInvitesTableId, invitesTableName, claimedInvitesTableName, submitInviteCodeFunction, prefix, membershipType, entityTableId }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, emailsTableId, usersTableId, invitesTableId, claimedInvitesTableId, invitesTableName, claimedInvitesTableName, submitInviteCodeFunction, prefix, membershipType, entityTableId }
  create:   { id, databaseId, schemaId, privateSchemaId, emailsTableId, usersTableId, invitesTableId, claimedInvitesTableId, invitesTableName, claimedInvitesTableName, submitInviteCodeFunction, prefix, membershipType, entityTableId }
  update:   { id, databaseId, schemaId, privateSchemaId, emailsTableId, usersTableId, invitesTableId, claimedInvitesTableId, invitesTableName, claimedInvitesTableName, submitInviteCodeFunction, prefix, membershipType, entityTableId }
  delete:   { id }
```

### MODEL: levelsModule

Access: `db.levelsModule`

```
METHODS:
  db.levelsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.levelsModule.findOne({ where: { id }, select })
  db.levelsModule.create({ data: { databaseId, schemaId, privateSchemaId, stepsTableId, stepsTableName, achievementsTableId, achievementsTableName, levelsTableId, levelsTableName, levelRequirementsTableId, levelRequirementsTableName, completedStep, incompletedStep, tgAchievement, tgAchievementToggle, tgAchievementToggleBoolean, tgAchievementBoolean, upsertAchievement, tgUpdateAchievements, stepsRequired, levelAchieved, prefix, membershipType, entityTableId, actorTableId }, select })
  db.levelsModule.update({ where: { id }, data, select })
  db.levelsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  stepsTableId: string
  stepsTableName: string
  achievementsTableId: string
  achievementsTableName: string
  levelsTableId: string
  levelsTableName: string
  levelRequirementsTableId: string
  levelRequirementsTableName: string
  completedStep: string
  incompletedStep: string
  tgAchievement: string
  tgAchievementToggle: string
  tgAchievementToggleBoolean: string
  tgAchievementBoolean: string
  upsertAchievement: string
  tgUpdateAchievements: string
  stepsRequired: string
  levelAchieved: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  stepsTableId: string
  stepsTableName: string
  achievementsTableId: string
  achievementsTableName: string
  levelsTableId: string
  levelsTableName: string
  levelRequirementsTableId: string
  levelRequirementsTableName: string
  completedStep: string
  incompletedStep: string
  tgAchievement: string
  tgAchievementToggle: string
  tgAchievementToggleBoolean: string
  tgAchievementBoolean: string
  upsertAchievement: string
  tgUpdateAchievements: string
  stepsRequired: string
  levelAchieved: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, stepsTableId, stepsTableName, achievementsTableId, achievementsTableName, levelsTableId, levelsTableName, levelRequirementsTableId, levelRequirementsTableName, completedStep, incompletedStep, tgAchievement, tgAchievementToggle, tgAchievementToggleBoolean, tgAchievementBoolean, upsertAchievement, tgUpdateAchievements, stepsRequired, levelAchieved, prefix, membershipType, entityTableId, actorTableId }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, stepsTableId, stepsTableName, achievementsTableId, achievementsTableName, levelsTableId, levelsTableName, levelRequirementsTableId, levelRequirementsTableName, completedStep, incompletedStep, tgAchievement, tgAchievementToggle, tgAchievementToggleBoolean, tgAchievementBoolean, upsertAchievement, tgUpdateAchievements, stepsRequired, levelAchieved, prefix, membershipType, entityTableId, actorTableId }
  create:   { id, databaseId, schemaId, privateSchemaId, stepsTableId, stepsTableName, achievementsTableId, achievementsTableName, levelsTableId, levelsTableName, levelRequirementsTableId, levelRequirementsTableName, completedStep, incompletedStep, tgAchievement, tgAchievementToggle, tgAchievementToggleBoolean, tgAchievementBoolean, upsertAchievement, tgUpdateAchievements, stepsRequired, levelAchieved, prefix, membershipType, entityTableId, actorTableId }
  update:   { id, databaseId, schemaId, privateSchemaId, stepsTableId, stepsTableName, achievementsTableId, achievementsTableName, levelsTableId, levelsTableName, levelRequirementsTableId, levelRequirementsTableName, completedStep, incompletedStep, tgAchievement, tgAchievementToggle, tgAchievementToggleBoolean, tgAchievementBoolean, upsertAchievement, tgUpdateAchievements, stepsRequired, levelAchieved, prefix, membershipType, entityTableId, actorTableId }
  delete:   { id }
```

### MODEL: limitsModule

Access: `db.limitsModule`

```
METHODS:
  db.limitsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.limitsModule.findOne({ where: { id }, select })
  db.limitsModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, limitIncrementFunction, limitDecrementFunction, limitIncrementTrigger, limitDecrementTrigger, limitUpdateTrigger, limitCheckFunction, prefix, membershipType, entityTableId, actorTableId }, select })
  db.limitsModule.update({ where: { id }, data, select })
  db.limitsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  limitIncrementFunction: string
  limitDecrementFunction: string
  limitIncrementTrigger: string
  limitDecrementTrigger: string
  limitUpdateTrigger: string
  limitCheckFunction: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  limitIncrementFunction: string
  limitDecrementFunction: string
  limitIncrementTrigger: string
  limitDecrementTrigger: string
  limitUpdateTrigger: string
  limitCheckFunction: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, limitIncrementFunction, limitDecrementFunction, limitIncrementTrigger, limitDecrementTrigger, limitUpdateTrigger, limitCheckFunction, prefix, membershipType, entityTableId, actorTableId }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, limitIncrementFunction, limitDecrementFunction, limitIncrementTrigger, limitDecrementTrigger, limitUpdateTrigger, limitCheckFunction, prefix, membershipType, entityTableId, actorTableId }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, limitIncrementFunction, limitDecrementFunction, limitIncrementTrigger, limitDecrementTrigger, limitUpdateTrigger, limitCheckFunction, prefix, membershipType, entityTableId, actorTableId }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, limitIncrementFunction, limitDecrementFunction, limitIncrementTrigger, limitDecrementTrigger, limitUpdateTrigger, limitCheckFunction, prefix, membershipType, entityTableId, actorTableId }
  delete:   { id }
```

### MODEL: membershipTypesModule

Access: `db.membershipTypesModule`

```
METHODS:
  db.membershipTypesModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.membershipTypesModule.findOne({ where: { id }, select })
  db.membershipTypesModule.create({ data: { databaseId, schemaId, tableId, tableName }, select })
  db.membershipTypesModule.update({ where: { id }, data, select })
  db.membershipTypesModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, tableId, tableName }]
  findOne:  { id, databaseId, schemaId, tableId, tableName }
  create:   { id, databaseId, schemaId, tableId, tableName }
  update:   { id, databaseId, schemaId, tableId, tableName }
  delete:   { id }
```

### MODEL: membershipsModule

Access: `db.membershipsModule`

```
METHODS:
  db.membershipsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.membershipsModule.findOne({ where: { id }, select })
  db.membershipsModule.create({ data: { databaseId, schemaId, privateSchemaId, membershipsTableId, membershipsTableName, membersTableId, membersTableName, membershipDefaultsTableId, membershipDefaultsTableName, grantsTableId, grantsTableName, actorTableId, limitsTableId, defaultLimitsTableId, permissionsTableId, defaultPermissionsTableId, sprtTableId, adminGrantsTableId, adminGrantsTableName, ownerGrantsTableId, ownerGrantsTableName, membershipType, entityTableId, entityTableOwnerId, prefix, actorMaskCheck, actorPermCheck, entityIdsByMask, entityIdsByPerm, entityIdsFunction }, select })
  db.membershipsModule.update({ where: { id }, data, select })
  db.membershipsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  membershipsTableId: string
  membershipsTableName: string
  membersTableId: string
  membersTableName: string
  membershipDefaultsTableId: string
  membershipDefaultsTableName: string
  grantsTableId: string
  grantsTableName: string
  actorTableId: string
  limitsTableId: string
  defaultLimitsTableId: string
  permissionsTableId: string
  defaultPermissionsTableId: string
  sprtTableId: string
  adminGrantsTableId: string
  adminGrantsTableName: string
  ownerGrantsTableId: string
  ownerGrantsTableName: string
  membershipType: number
  entityTableId: string
  entityTableOwnerId: string
  prefix: string
  actorMaskCheck: string
  actorPermCheck: string
  entityIdsByMask: string
  entityIdsByPerm: string
  entityIdsFunction: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  membershipsTableId: string
  membershipsTableName: string
  membersTableId: string
  membersTableName: string
  membershipDefaultsTableId: string
  membershipDefaultsTableName: string
  grantsTableId: string
  grantsTableName: string
  actorTableId: string
  limitsTableId: string
  defaultLimitsTableId: string
  permissionsTableId: string
  defaultPermissionsTableId: string
  sprtTableId: string
  adminGrantsTableId: string
  adminGrantsTableName: string
  ownerGrantsTableId: string
  ownerGrantsTableName: string
  membershipType: number
  entityTableId: string
  entityTableOwnerId: string
  prefix: string
  actorMaskCheck: string
  actorPermCheck: string
  entityIdsByMask: string
  entityIdsByPerm: string
  entityIdsFunction: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, membershipsTableId, membershipsTableName, membersTableId, membersTableName, membershipDefaultsTableId, membershipDefaultsTableName, grantsTableId, grantsTableName, actorTableId, limitsTableId, defaultLimitsTableId, permissionsTableId, defaultPermissionsTableId, sprtTableId, adminGrantsTableId, adminGrantsTableName, ownerGrantsTableId, ownerGrantsTableName, membershipType, entityTableId, entityTableOwnerId, prefix, actorMaskCheck, actorPermCheck, entityIdsByMask, entityIdsByPerm, entityIdsFunction }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, membershipsTableId, membershipsTableName, membersTableId, membersTableName, membershipDefaultsTableId, membershipDefaultsTableName, grantsTableId, grantsTableName, actorTableId, limitsTableId, defaultLimitsTableId, permissionsTableId, defaultPermissionsTableId, sprtTableId, adminGrantsTableId, adminGrantsTableName, ownerGrantsTableId, ownerGrantsTableName, membershipType, entityTableId, entityTableOwnerId, prefix, actorMaskCheck, actorPermCheck, entityIdsByMask, entityIdsByPerm, entityIdsFunction }
  create:   { id, databaseId, schemaId, privateSchemaId, membershipsTableId, membershipsTableName, membersTableId, membersTableName, membershipDefaultsTableId, membershipDefaultsTableName, grantsTableId, grantsTableName, actorTableId, limitsTableId, defaultLimitsTableId, permissionsTableId, defaultPermissionsTableId, sprtTableId, adminGrantsTableId, adminGrantsTableName, ownerGrantsTableId, ownerGrantsTableName, membershipType, entityTableId, entityTableOwnerId, prefix, actorMaskCheck, actorPermCheck, entityIdsByMask, entityIdsByPerm, entityIdsFunction }
  update:   { id, databaseId, schemaId, privateSchemaId, membershipsTableId, membershipsTableName, membersTableId, membersTableName, membershipDefaultsTableId, membershipDefaultsTableName, grantsTableId, grantsTableName, actorTableId, limitsTableId, defaultLimitsTableId, permissionsTableId, defaultPermissionsTableId, sprtTableId, adminGrantsTableId, adminGrantsTableName, ownerGrantsTableId, ownerGrantsTableName, membershipType, entityTableId, entityTableOwnerId, prefix, actorMaskCheck, actorPermCheck, entityIdsByMask, entityIdsByPerm, entityIdsFunction }
  delete:   { id }
```

### MODEL: permissionsModule

Access: `db.permissionsModule`

```
METHODS:
  db.permissionsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.permissionsModule.findOne({ where: { id }, select })
  db.permissionsModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, bitlen, membershipType, entityTableId, actorTableId, prefix, getPaddedMask, getMask, getByMask, getMaskByName }, select })
  db.permissionsModule.update({ where: { id }, data, select })
  db.permissionsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  prefix: string
  getPaddedMask: string
  getMask: string
  getByMask: string
  getMaskByName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  prefix: string
  getPaddedMask: string
  getMask: string
  getByMask: string
  getMaskByName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, bitlen, membershipType, entityTableId, actorTableId, prefix, getPaddedMask, getMask, getByMask, getMaskByName }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, bitlen, membershipType, entityTableId, actorTableId, prefix, getPaddedMask, getMask, getByMask, getMaskByName }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, bitlen, membershipType, entityTableId, actorTableId, prefix, getPaddedMask, getMask, getByMask, getMaskByName }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, tableName, defaultTableId, defaultTableName, bitlen, membershipType, entityTableId, actorTableId, prefix, getPaddedMask, getMask, getByMask, getMaskByName }
  delete:   { id }
```

### MODEL: phoneNumbersModule

Access: `db.phoneNumbersModule`

```
METHODS:
  db.phoneNumbersModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.phoneNumbersModule.findOne({ where: { id }, select })
  db.phoneNumbersModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }, select })
  db.phoneNumbersModule.update({ where: { id }, data, select })
  db.phoneNumbersModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, ownerTableId, tableName }
  delete:   { id }
```

### MODEL: profilesModule

Access: `db.profilesModule`

```
METHODS:
  db.profilesModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.profilesModule.findOne({ where: { id }, select })
  db.profilesModule.create({ data: { databaseId, schemaId, privateSchemaId, tableId, tableName, profilePermissionsTableId, profilePermissionsTableName, profileGrantsTableId, profileGrantsTableName, profileDefinitionGrantsTableId, profileDefinitionGrantsTableName, bitlen, membershipType, entityTableId, actorTableId, permissionsTableId, membershipsTableId, prefix }, select })
  db.profilesModule.update({ where: { id }, data, select })
  db.profilesModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  profilePermissionsTableId: string
  profilePermissionsTableName: string
  profileGrantsTableId: string
  profileGrantsTableName: string
  profileDefinitionGrantsTableId: string
  profileDefinitionGrantsTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  permissionsTableId: string
  membershipsTableId: string
  prefix: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  profilePermissionsTableId: string
  profilePermissionsTableName: string
  profileGrantsTableId: string
  profileGrantsTableName: string
  profileDefinitionGrantsTableId: string
  profileDefinitionGrantsTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  permissionsTableId: string
  membershipsTableId: string
  prefix: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, tableId, tableName, profilePermissionsTableId, profilePermissionsTableName, profileGrantsTableId, profileGrantsTableName, profileDefinitionGrantsTableId, profileDefinitionGrantsTableName, bitlen, membershipType, entityTableId, actorTableId, permissionsTableId, membershipsTableId, prefix }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, tableId, tableName, profilePermissionsTableId, profilePermissionsTableName, profileGrantsTableId, profileGrantsTableName, profileDefinitionGrantsTableId, profileDefinitionGrantsTableName, bitlen, membershipType, entityTableId, actorTableId, permissionsTableId, membershipsTableId, prefix }
  create:   { id, databaseId, schemaId, privateSchemaId, tableId, tableName, profilePermissionsTableId, profilePermissionsTableName, profileGrantsTableId, profileGrantsTableName, profileDefinitionGrantsTableId, profileDefinitionGrantsTableName, bitlen, membershipType, entityTableId, actorTableId, permissionsTableId, membershipsTableId, prefix }
  update:   { id, databaseId, schemaId, privateSchemaId, tableId, tableName, profilePermissionsTableId, profilePermissionsTableName, profileGrantsTableId, profileGrantsTableName, profileDefinitionGrantsTableId, profileDefinitionGrantsTableName, bitlen, membershipType, entityTableId, actorTableId, permissionsTableId, membershipsTableId, prefix }
  delete:   { id }
```

### MODEL: rlsModule

Access: `db.rlsModule`

```
METHODS:
  db.rlsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.rlsModule.findOne({ where: { id }, select })
  db.rlsModule.create({ data: { databaseId, apiId, schemaId, privateSchemaId, sessionCredentialsTableId, sessionsTableId, usersTableId, authenticate, authenticateStrict, currentRole, currentRoleId }, select })
  db.rlsModule.update({ where: { id }, data, select })
  db.rlsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  apiId: string
  schemaId: string
  privateSchemaId: string
  sessionCredentialsTableId: string
  sessionsTableId: string
  usersTableId: string
  authenticate: string
  authenticateStrict: string
  currentRole: string
  currentRoleId: string

EDITABLE FIELDS:
  databaseId: string
  apiId: string
  schemaId: string
  privateSchemaId: string
  sessionCredentialsTableId: string
  sessionsTableId: string
  usersTableId: string
  authenticate: string
  authenticateStrict: string
  currentRole: string
  currentRoleId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, apiId, schemaId, privateSchemaId, sessionCredentialsTableId, sessionsTableId, usersTableId, authenticate, authenticateStrict, currentRole, currentRoleId }]
  findOne:  { id, databaseId, apiId, schemaId, privateSchemaId, sessionCredentialsTableId, sessionsTableId, usersTableId, authenticate, authenticateStrict, currentRole, currentRoleId }
  create:   { id, databaseId, apiId, schemaId, privateSchemaId, sessionCredentialsTableId, sessionsTableId, usersTableId, authenticate, authenticateStrict, currentRole, currentRoleId }
  update:   { id, databaseId, apiId, schemaId, privateSchemaId, sessionCredentialsTableId, sessionsTableId, usersTableId, authenticate, authenticateStrict, currentRole, currentRoleId }
  delete:   { id }
```

### MODEL: secretsModule

Access: `db.secretsModule`

```
METHODS:
  db.secretsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.secretsModule.findOne({ where: { id }, select })
  db.secretsModule.create({ data: { databaseId, schemaId, tableId, tableName }, select })
  db.secretsModule.update({ where: { id }, data, select })
  db.secretsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, tableId, tableName }]
  findOne:  { id, databaseId, schemaId, tableId, tableName }
  create:   { id, databaseId, schemaId, tableId, tableName }
  update:   { id, databaseId, schemaId, tableId, tableName }
  delete:   { id }
```

### MODEL: sessionsModule

Access: `db.sessionsModule`

```
METHODS:
  db.sessionsModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.sessionsModule.findOne({ where: { id }, select })
  db.sessionsModule.create({ data: { databaseId, schemaId, sessionsTableId, sessionCredentialsTableId, authSettingsTableId, usersTableId, sessionsDefaultExpiration, sessionsTable, sessionCredentialsTable, authSettingsTable }, select })
  db.sessionsModule.update({ where: { id }, data, select })
  db.sessionsModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  authSettingsTableId: string
  usersTableId: string
  sessionsDefaultExpiration: string
  sessionsTable: string
  sessionCredentialsTable: string
  authSettingsTable: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  authSettingsTableId: string
  usersTableId: string
  sessionsDefaultExpiration: string
  sessionsTable: string
  sessionCredentialsTable: string
  authSettingsTable: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, sessionsTableId, sessionCredentialsTableId, authSettingsTableId, usersTableId, sessionsDefaultExpiration, sessionsTable, sessionCredentialsTable, authSettingsTable }]
  findOne:  { id, databaseId, schemaId, sessionsTableId, sessionCredentialsTableId, authSettingsTableId, usersTableId, sessionsDefaultExpiration, sessionsTable, sessionCredentialsTable, authSettingsTable }
  create:   { id, databaseId, schemaId, sessionsTableId, sessionCredentialsTableId, authSettingsTableId, usersTableId, sessionsDefaultExpiration, sessionsTable, sessionCredentialsTable, authSettingsTable }
  update:   { id, databaseId, schemaId, sessionsTableId, sessionCredentialsTableId, authSettingsTableId, usersTableId, sessionsDefaultExpiration, sessionsTable, sessionCredentialsTable, authSettingsTable }
  delete:   { id }
```

### MODEL: userAuthModule

Access: `db.userAuthModule`

```
METHODS:
  db.userAuthModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.userAuthModule.findOne({ where: { id }, select })
  db.userAuthModule.create({ data: { databaseId, schemaId, emailsTableId, usersTableId, secretsTableId, encryptedTableId, sessionsTableId, sessionCredentialsTableId, auditsTableId, auditsTableName, signInFunction, signUpFunction, signOutFunction, setPasswordFunction, resetPasswordFunction, forgotPasswordFunction, sendVerificationEmailFunction, verifyEmailFunction, verifyPasswordFunction, checkPasswordFunction, sendAccountDeletionEmailFunction, deleteAccountFunction, signInOneTimeTokenFunction, oneTimeTokenFunction, extendTokenExpires }, select })
  db.userAuthModule.update({ where: { id }, data, select })
  db.userAuthModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  emailsTableId: string
  usersTableId: string
  secretsTableId: string
  encryptedTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  auditsTableId: string
  auditsTableName: string
  signInFunction: string
  signUpFunction: string
  signOutFunction: string
  setPasswordFunction: string
  resetPasswordFunction: string
  forgotPasswordFunction: string
  sendVerificationEmailFunction: string
  verifyEmailFunction: string
  verifyPasswordFunction: string
  checkPasswordFunction: string
  sendAccountDeletionEmailFunction: string
  deleteAccountFunction: string
  signInOneTimeTokenFunction: string
  oneTimeTokenFunction: string
  extendTokenExpires: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  emailsTableId: string
  usersTableId: string
  secretsTableId: string
  encryptedTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  auditsTableId: string
  auditsTableName: string
  signInFunction: string
  signUpFunction: string
  signOutFunction: string
  setPasswordFunction: string
  resetPasswordFunction: string
  forgotPasswordFunction: string
  sendVerificationEmailFunction: string
  verifyEmailFunction: string
  verifyPasswordFunction: string
  checkPasswordFunction: string
  sendAccountDeletionEmailFunction: string
  deleteAccountFunction: string
  signInOneTimeTokenFunction: string
  oneTimeTokenFunction: string
  extendTokenExpires: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, emailsTableId, usersTableId, secretsTableId, encryptedTableId, sessionsTableId, sessionCredentialsTableId, auditsTableId, auditsTableName, signInFunction, signUpFunction, signOutFunction, setPasswordFunction, resetPasswordFunction, forgotPasswordFunction, sendVerificationEmailFunction, verifyEmailFunction, verifyPasswordFunction, checkPasswordFunction, sendAccountDeletionEmailFunction, deleteAccountFunction, signInOneTimeTokenFunction, oneTimeTokenFunction, extendTokenExpires }]
  findOne:  { id, databaseId, schemaId, emailsTableId, usersTableId, secretsTableId, encryptedTableId, sessionsTableId, sessionCredentialsTableId, auditsTableId, auditsTableName, signInFunction, signUpFunction, signOutFunction, setPasswordFunction, resetPasswordFunction, forgotPasswordFunction, sendVerificationEmailFunction, verifyEmailFunction, verifyPasswordFunction, checkPasswordFunction, sendAccountDeletionEmailFunction, deleteAccountFunction, signInOneTimeTokenFunction, oneTimeTokenFunction, extendTokenExpires }
  create:   { id, databaseId, schemaId, emailsTableId, usersTableId, secretsTableId, encryptedTableId, sessionsTableId, sessionCredentialsTableId, auditsTableId, auditsTableName, signInFunction, signUpFunction, signOutFunction, setPasswordFunction, resetPasswordFunction, forgotPasswordFunction, sendVerificationEmailFunction, verifyEmailFunction, verifyPasswordFunction, checkPasswordFunction, sendAccountDeletionEmailFunction, deleteAccountFunction, signInOneTimeTokenFunction, oneTimeTokenFunction, extendTokenExpires }
  update:   { id, databaseId, schemaId, emailsTableId, usersTableId, secretsTableId, encryptedTableId, sessionsTableId, sessionCredentialsTableId, auditsTableId, auditsTableName, signInFunction, signUpFunction, signOutFunction, setPasswordFunction, resetPasswordFunction, forgotPasswordFunction, sendVerificationEmailFunction, verifyEmailFunction, verifyPasswordFunction, checkPasswordFunction, sendAccountDeletionEmailFunction, deleteAccountFunction, signInOneTimeTokenFunction, oneTimeTokenFunction, extendTokenExpires }
  delete:   { id }
```

### MODEL: usersModule

Access: `db.usersModule`

```
METHODS:
  db.usersModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.usersModule.findOne({ where: { id }, select })
  db.usersModule.create({ data: { databaseId, schemaId, tableId, tableName, typeTableId, typeTableName }, select })
  db.usersModule.update({ where: { id }, data, select })
  db.usersModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
  typeTableId: string
  typeTableName: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
  typeTableId: string
  typeTableName: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, tableId, tableName, typeTableId, typeTableName }]
  findOne:  { id, databaseId, schemaId, tableId, tableName, typeTableId, typeTableName }
  create:   { id, databaseId, schemaId, tableId, tableName, typeTableId, typeTableName }
  update:   { id, databaseId, schemaId, tableId, tableName, typeTableId, typeTableName }
  delete:   { id }
```

### MODEL: uuidModule

Access: `db.uuidModule`

```
METHODS:
  db.uuidModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.uuidModule.findOne({ where: { id }, select })
  db.uuidModule.create({ data: { databaseId, schemaId, uuidFunction, uuidSeed }, select })
  db.uuidModule.update({ where: { id }, data, select })
  db.uuidModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  uuidFunction: string
  uuidSeed: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  uuidFunction: string
  uuidSeed: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, uuidFunction, uuidSeed }]
  findOne:  { id, databaseId, schemaId, uuidFunction, uuidSeed }
  create:   { id, databaseId, schemaId, uuidFunction, uuidSeed }
  update:   { id, databaseId, schemaId, uuidFunction, uuidSeed }
  delete:   { id }
```

### MODEL: databaseProvisionModule

Access: `db.databaseProvisionModule`

```
METHODS:
  db.databaseProvisionModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.databaseProvisionModule.findOne({ where: { id }, select })
  db.databaseProvisionModule.create({ data: { databaseName, ownerId, subdomain, domain, modules, options, bootstrapUser, status, errorMessage, databaseId, completedAt }, select })
  db.databaseProvisionModule.update({ where: { id }, data, select })
  db.databaseProvisionModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseName: string
  ownerId: string
  subdomain: string
  domain: string
  modules: string[]
  options: unknown
  bootstrapUser: boolean
  status: string
  errorMessage: string
  databaseId: string
  createdAt: string
  updatedAt: string
  completedAt: string

EDITABLE FIELDS:
  databaseName: string
  ownerId: string
  subdomain: string
  domain: string
  modules: string[]
  options: unknown
  bootstrapUser: boolean
  status: string
  errorMessage: string
  databaseId: string
  completedAt: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseName, ownerId, subdomain, domain, modules, options, bootstrapUser, status, errorMessage, databaseId, createdAt, updatedAt, completedAt }]
  findOne:  { id, databaseName, ownerId, subdomain, domain, modules, options, bootstrapUser, status, errorMessage, databaseId, createdAt, updatedAt, completedAt }
  create:   { id, databaseName, ownerId, subdomain, domain, modules, options, bootstrapUser, status, errorMessage, databaseId, createdAt, updatedAt, completedAt }
  update:   { id, databaseName, ownerId, subdomain, domain, modules, options, bootstrapUser, status, errorMessage, databaseId, createdAt, updatedAt, completedAt }
  delete:   { id }
```

### MODEL: appAdminGrant

Access: `db.appAdminGrant`

```
METHODS:
  db.appAdminGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.appAdminGrant.findOne({ where: { id }, select })
  db.appAdminGrant.create({ data: { isGrant, actorId, grantorId }, select })
  db.appAdminGrant.update({ where: { id }, data, select })
  db.appAdminGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appOwnerGrant

Access: `db.appOwnerGrant`

```
METHODS:
  db.appOwnerGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.appOwnerGrant.findOne({ where: { id }, select })
  db.appOwnerGrant.create({ data: { isGrant, actorId, grantorId }, select })
  db.appOwnerGrant.update({ where: { id }, data, select })
  db.appOwnerGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appGrant

Access: `db.appGrant`

```
METHODS:
  db.appGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.appGrant.findOne({ where: { id }, select })
  db.appGrant.create({ data: { permissions, isGrant, actorId, grantorId }, select })
  db.appGrant.update({ where: { id }, data, select })
  db.appGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  permissions: string
  isGrant: boolean
  actorId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }]
  findOne:  { id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }
  create:   { id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }
  update:   { id, permissions, isGrant, actorId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgMembership

Access: `db.orgMembership`

```
METHODS:
  db.orgMembership.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgMembership.findOne({ where: { id }, select })
  db.orgMembership.create({ data: { createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }, select })
  db.orgMembership.update({ where: { id }, data, select })
  db.orgMembership.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
  entityId: string

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isActive, isOwner, isAdmin, permissions, granted, actorId, entityId }
  delete:   { id }
```

### MODEL: orgMember

Access: `db.orgMember`

```
METHODS:
  db.orgMember.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgMember.findOne({ where: { id }, select })
  db.orgMember.create({ data: { isAdmin, actorId, entityId }, select })
  db.orgMember.update({ where: { id }, data, select })
  db.orgMember.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isAdmin: boolean
  actorId: string
  entityId: string

EDITABLE FIELDS:
  isAdmin: boolean
  actorId: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isAdmin, actorId, entityId }]
  findOne:  { id, isAdmin, actorId, entityId }
  create:   { id, isAdmin, actorId, entityId }
  update:   { id, isAdmin, actorId, entityId }
  delete:   { id }
```

### MODEL: orgAdminGrant

Access: `db.orgAdminGrant`

```
METHODS:
  db.orgAdminGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgAdminGrant.findOne({ where: { id }, select })
  db.orgAdminGrant.create({ data: { isGrant, actorId, entityId, grantorId }, select })
  db.orgAdminGrant.update({ where: { id }, data, select })
  db.orgAdminGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgOwnerGrant

Access: `db.orgOwnerGrant`

```
METHODS:
  db.orgOwnerGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgOwnerGrant.findOne({ where: { id }, select })
  db.orgOwnerGrant.create({ data: { isGrant, actorId, entityId, grantorId }, select })
  db.orgOwnerGrant.update({ where: { id }, data, select })
  db.orgOwnerGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }]
  findOne:  { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  create:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  update:   { id, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgGrant

Access: `db.orgGrant`

```
METHODS:
  db.orgGrant.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgGrant.findOne({ where: { id }, select })
  db.orgGrant.create({ data: { permissions, isGrant, actorId, entityId, grantorId }, select })
  db.orgGrant.update({ where: { id }, data, select })
  db.orgGrant.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  permissions: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }]
  findOne:  { id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  create:   { id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  update:   { id, permissions, isGrant, actorId, entityId, grantorId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appLimit

Access: `db.appLimit`

```
METHODS:
  db.appLimit.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLimit.findOne({ where: { id }, select })
  db.appLimit.create({ data: { name, actorId, num, max }, select })
  db.appLimit.update({ where: { id }, data, select })
  db.appLimit.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  actorId: string
  num: number
  max: number

EDITABLE FIELDS:
  name: string
  actorId: string
  num: number
  max: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, actorId, num, max }]
  findOne:  { id, name, actorId, num, max }
  create:   { id, name, actorId, num, max }
  update:   { id, name, actorId, num, max }
  delete:   { id }
```

### MODEL: orgLimit

Access: `db.orgLimit`

```
METHODS:
  db.orgLimit.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgLimit.findOne({ where: { id }, select })
  db.orgLimit.create({ data: { name, actorId, num, max, entityId }, select })
  db.orgLimit.update({ where: { id }, data, select })
  db.orgLimit.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  actorId: string
  num: number
  max: number
  entityId: string

EDITABLE FIELDS:
  name: string
  actorId: string
  num: number
  max: number
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, actorId, num, max, entityId }]
  findOne:  { id, name, actorId, num, max, entityId }
  create:   { id, name, actorId, num, max, entityId }
  update:   { id, name, actorId, num, max, entityId }
  delete:   { id }
```

### MODEL: appStep

Access: `db.appStep`

```
METHODS:
  db.appStep.findMany({ select, where?, orderBy?, first?, offset? })
  db.appStep.findOne({ where: { id }, select })
  db.appStep.create({ data: { actorId, name, count }, select })
  db.appStep.update({ where: { id }, data, select })
  db.appStep.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  actorId: string
  name: string
  count: number

OUTPUT: Promise<JSON>
  findMany: [{ id, actorId, name, count, createdAt, updatedAt }]
  findOne:  { id, actorId, name, count, createdAt, updatedAt }
  create:   { id, actorId, name, count, createdAt, updatedAt }
  update:   { id, actorId, name, count, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appAchievement

Access: `db.appAchievement`

```
METHODS:
  db.appAchievement.findMany({ select, where?, orderBy?, first?, offset? })
  db.appAchievement.findOne({ where: { id }, select })
  db.appAchievement.create({ data: { actorId, name, count }, select })
  db.appAchievement.update({ where: { id }, data, select })
  db.appAchievement.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  actorId: string
  name: string
  count: number

OUTPUT: Promise<JSON>
  findMany: [{ id, actorId, name, count, createdAt, updatedAt }]
  findOne:  { id, actorId, name, count, createdAt, updatedAt }
  create:   { id, actorId, name, count, createdAt, updatedAt }
  update:   { id, actorId, name, count, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: invite

Access: `db.invite`

```
METHODS:
  db.invite.findMany({ select, where?, orderBy?, first?, offset? })
  db.invite.findOne({ where: { id }, select })
  db.invite.create({ data: { email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt }, select })
  db.invite.update({ where: { id }, data, select })
  db.invite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  email: ConstructiveInternalTypeEmail
  senderId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  email: ConstructiveInternalTypeEmail
  senderId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string

OUTPUT: Promise<JSON>
  findMany: [{ id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }]
  findOne:  { id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }
  create:   { id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }
  update:   { id, email, senderId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: claimedInvite

Access: `db.claimedInvite`

```
METHODS:
  db.claimedInvite.findMany({ select, where?, orderBy?, first?, offset? })
  db.claimedInvite.findOne({ where: { id }, select })
  db.claimedInvite.create({ data: { data, senderId, receiverId }, select })
  db.claimedInvite.update({ where: { id }, data, select })
  db.claimedInvite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  data: unknown
  senderId: string
  receiverId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, data, senderId, receiverId, createdAt, updatedAt }]
  findOne:  { id, data, senderId, receiverId, createdAt, updatedAt }
  create:   { id, data, senderId, receiverId, createdAt, updatedAt }
  update:   { id, data, senderId, receiverId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: orgInvite

Access: `db.orgInvite`

```
METHODS:
  db.orgInvite.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgInvite.findOne({ where: { id }, select })
  db.orgInvite.create({ data: { email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, entityId }, select })
  db.orgInvite.update({ where: { id }, data, select })
  db.orgInvite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  email: ConstructiveInternalTypeEmail
  senderId: string
  receiverId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string
  entityId: string

EDITABLE FIELDS:
  email: ConstructiveInternalTypeEmail
  senderId: string
  receiverId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }]
  findOne:  { id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }
  create:   { id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }
  update:   { id, email, senderId, receiverId, inviteToken, inviteValid, inviteLimit, inviteCount, multiple, data, expiresAt, createdAt, updatedAt, entityId }
  delete:   { id }
```

### MODEL: orgClaimedInvite

Access: `db.orgClaimedInvite`

```
METHODS:
  db.orgClaimedInvite.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgClaimedInvite.findOne({ where: { id }, select })
  db.orgClaimedInvite.create({ data: { data, senderId, receiverId, entityId }, select })
  db.orgClaimedInvite.update({ where: { id }, data, select })
  db.orgClaimedInvite.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string
  entityId: string

EDITABLE FIELDS:
  data: unknown
  senderId: string
  receiverId: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, data, senderId, receiverId, createdAt, updatedAt, entityId }]
  findOne:  { id, data, senderId, receiverId, createdAt, updatedAt, entityId }
  create:   { id, data, senderId, receiverId, createdAt, updatedAt, entityId }
  update:   { id, data, senderId, receiverId, createdAt, updatedAt, entityId }
  delete:   { id }
```

### MODEL: appPermissionDefault

Access: `db.appPermissionDefault`

```
METHODS:
  db.appPermissionDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.appPermissionDefault.findOne({ where: { id }, select })
  db.appPermissionDefault.create({ data: { permissions }, select })
  db.appPermissionDefault.update({ where: { id }, data, select })
  db.appPermissionDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string

EDITABLE FIELDS:
  permissions: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions }]
  findOne:  { id, permissions }
  create:   { id, permissions }
  update:   { id, permissions }
  delete:   { id }
```

### MODEL: ref

Access: `db.ref`

```
METHODS:
  db.ref.findMany({ select, where?, orderBy?, first?, offset? })
  db.ref.findOne({ where: { id }, select })
  db.ref.create({ data: { name, databaseId, storeId, commitId }, select })
  db.ref.update({ where: { id }, data, select })
  db.ref.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  databaseId: string
  storeId: string
  commitId: string

EDITABLE FIELDS:
  name: string
  databaseId: string
  storeId: string
  commitId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, databaseId, storeId, commitId }]
  findOne:  { id, name, databaseId, storeId, commitId }
  create:   { id, name, databaseId, storeId, commitId }
  update:   { id, name, databaseId, storeId, commitId }
  delete:   { id }
```

### MODEL: store

Access: `db.store`

```
METHODS:
  db.store.findMany({ select, where?, orderBy?, first?, offset? })
  db.store.findOne({ where: { id }, select })
  db.store.create({ data: { name, databaseId, hash }, select })
  db.store.update({ where: { id }, data, select })
  db.store.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  databaseId: string
  hash: string
  createdAt: string

EDITABLE FIELDS:
  name: string
  databaseId: string
  hash: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, databaseId, hash, createdAt }]
  findOne:  { id, name, databaseId, hash, createdAt }
  create:   { id, name, databaseId, hash, createdAt }
  update:   { id, name, databaseId, hash, createdAt }
  delete:   { id }
```

### MODEL: roleType

Access: `db.roleType`

```
METHODS:
  db.roleType.findMany({ select, where?, orderBy?, first?, offset? })
  db.roleType.findOne({ where: { id }, select })
  db.roleType.create({ data: { name }, select })
  db.roleType.update({ where: { id }, data, select })
  db.roleType.delete({ where: { id } })

FIELDS:
  id: number (primary key)
  name: string

EDITABLE FIELDS:
  name: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name }]
  findOne:  { id, name }
  create:   { id, name }
  update:   { id, name }
  delete:   { id }
```

### MODEL: orgPermissionDefault

Access: `db.orgPermissionDefault`

```
METHODS:
  db.orgPermissionDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgPermissionDefault.findOne({ where: { id }, select })
  db.orgPermissionDefault.create({ data: { permissions, entityId }, select })
  db.orgPermissionDefault.update({ where: { id }, data, select })
  db.orgPermissionDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  permissions: string
  entityId: string

EDITABLE FIELDS:
  permissions: string
  entityId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, permissions, entityId }]
  findOne:  { id, permissions, entityId }
  create:   { id, permissions, entityId }
  update:   { id, permissions, entityId }
  delete:   { id }
```

### MODEL: appLimitDefault

Access: `db.appLimitDefault`

```
METHODS:
  db.appLimitDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLimitDefault.findOne({ where: { id }, select })
  db.appLimitDefault.create({ data: { name, max }, select })
  db.appLimitDefault.update({ where: { id }, data, select })
  db.appLimitDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  max: number

EDITABLE FIELDS:
  name: string
  max: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, max }]
  findOne:  { id, name, max }
  create:   { id, name, max }
  update:   { id, name, max }
  delete:   { id }
```

### MODEL: orgLimitDefault

Access: `db.orgLimitDefault`

```
METHODS:
  db.orgLimitDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgLimitDefault.findOne({ where: { id }, select })
  db.orgLimitDefault.create({ data: { name, max }, select })
  db.orgLimitDefault.update({ where: { id }, data, select })
  db.orgLimitDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  max: number

EDITABLE FIELDS:
  name: string
  max: number

OUTPUT: Promise<JSON>
  findMany: [{ id, name, max }]
  findOne:  { id, name, max }
  create:   { id, name, max }
  update:   { id, name, max }
  delete:   { id }
```

### MODEL: cryptoAddress

Access: `db.cryptoAddress`

```
METHODS:
  db.cryptoAddress.findMany({ select, where?, orderBy?, first?, offset? })
  db.cryptoAddress.findOne({ where: { id }, select })
  db.cryptoAddress.create({ data: { ownerId, address, isVerified, isPrimary }, select })
  db.cryptoAddress.update({ where: { id }, data, select })
  db.cryptoAddress.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  ownerId: string
  address: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  ownerId: string
  address: string
  isVerified: boolean
  isPrimary: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, ownerId, address, isVerified, isPrimary, createdAt, updatedAt }]
  findOne:  { id, ownerId, address, isVerified, isPrimary, createdAt, updatedAt }
  create:   { id, ownerId, address, isVerified, isPrimary, createdAt, updatedAt }
  update:   { id, ownerId, address, isVerified, isPrimary, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: membershipType

Access: `db.membershipType`

```
METHODS:
  db.membershipType.findMany({ select, where?, orderBy?, first?, offset? })
  db.membershipType.findOne({ where: { id }, select })
  db.membershipType.create({ data: { name, description, prefix }, select })
  db.membershipType.update({ where: { id }, data, select })
  db.membershipType.delete({ where: { id } })

FIELDS:
  id: number (primary key)
  name: string
  description: string
  prefix: string

EDITABLE FIELDS:
  name: string
  description: string
  prefix: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, description, prefix }]
  findOne:  { id, name, description, prefix }
  create:   { id, name, description, prefix }
  update:   { id, name, description, prefix }
  delete:   { id }
```

### MODEL: connectedAccount

Access: `db.connectedAccount`

```
METHODS:
  db.connectedAccount.findMany({ select, where?, orderBy?, first?, offset? })
  db.connectedAccount.findOne({ where: { id }, select })
  db.connectedAccount.create({ data: { ownerId, service, identifier, details, isVerified }, select })
  db.connectedAccount.update({ where: { id }, data, select })
  db.connectedAccount.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  ownerId: string
  service: string
  identifier: string
  details: unknown
  isVerified: boolean
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  ownerId: string
  service: string
  identifier: string
  details: unknown
  isVerified: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, ownerId, service, identifier, details, isVerified, createdAt, updatedAt }]
  findOne:  { id, ownerId, service, identifier, details, isVerified, createdAt, updatedAt }
  create:   { id, ownerId, service, identifier, details, isVerified, createdAt, updatedAt }
  update:   { id, ownerId, service, identifier, details, isVerified, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: phoneNumber

Access: `db.phoneNumber`

```
METHODS:
  db.phoneNumber.findMany({ select, where?, orderBy?, first?, offset? })
  db.phoneNumber.findOne({ where: { id }, select })
  db.phoneNumber.create({ data: { ownerId, cc, number, isVerified, isPrimary }, select })
  db.phoneNumber.update({ where: { id }, data, select })
  db.phoneNumber.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  ownerId: string
  cc: string
  number: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  ownerId: string
  cc: string
  number: string
  isVerified: boolean
  isPrimary: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, ownerId, cc, number, isVerified, isPrimary, createdAt, updatedAt }]
  findOne:  { id, ownerId, cc, number, isVerified, isPrimary, createdAt, updatedAt }
  create:   { id, ownerId, cc, number, isVerified, isPrimary, createdAt, updatedAt }
  update:   { id, ownerId, cc, number, isVerified, isPrimary, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: appMembershipDefault

Access: `db.appMembershipDefault`

```
METHODS:
  db.appMembershipDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.appMembershipDefault.findOne({ where: { id }, select })
  db.appMembershipDefault.create({ data: { createdBy, updatedBy, isApproved, isVerified }, select })
  db.appMembershipDefault.update({ where: { id }, data, select })
  db.appMembershipDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isVerified: boolean

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isVerified: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isVerified }
  delete:   { id }
```

### MODEL: nodeTypeRegistry

Access: `db.nodeTypeRegistry`

```
METHODS:
  db.nodeTypeRegistry.findMany({ select, where?, orderBy?, first?, offset? })
  db.nodeTypeRegistry.findOne({ where: { name }, select })
  db.nodeTypeRegistry.create({ data: { slug, category, displayName, description, parameterSchema, tags }, select })
  db.nodeTypeRegistry.update({ where: { name }, data, select })
  db.nodeTypeRegistry.delete({ where: { name } })

FIELDS:
  name: string (primary key)
  slug: string
  category: string
  displayName: string
  description: string
  parameterSchema: unknown
  tags: string[]
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  slug: string
  category: string
  displayName: string
  description: string
  parameterSchema: unknown
  tags: string[]

OUTPUT: Promise<JSON>
  findMany: [{ name, slug, category, displayName, description, parameterSchema, tags, createdAt, updatedAt }]
  findOne:  { name, slug, category, displayName, description, parameterSchema, tags, createdAt, updatedAt }
  create:   { name, slug, category, displayName, description, parameterSchema, tags, createdAt, updatedAt }
  update:   { name, slug, category, displayName, description, parameterSchema, tags, createdAt, updatedAt }
  delete:   { name }
```

### MODEL: commit

Access: `db.commit`

```
METHODS:
  db.commit.findMany({ select, where?, orderBy?, first?, offset? })
  db.commit.findOne({ where: { id }, select })
  db.commit.create({ data: { message, databaseId, storeId, parentIds, authorId, committerId, treeId, date }, select })
  db.commit.update({ where: { id }, data, select })
  db.commit.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  message: string
  databaseId: string
  storeId: string
  parentIds: string[]
  authorId: string
  committerId: string
  treeId: string
  date: string

EDITABLE FIELDS:
  message: string
  databaseId: string
  storeId: string
  parentIds: string[]
  authorId: string
  committerId: string
  treeId: string
  date: string

OUTPUT: Promise<JSON>
  findMany: [{ id, message, databaseId, storeId, parentIds, authorId, committerId, treeId, date }]
  findOne:  { id, message, databaseId, storeId, parentIds, authorId, committerId, treeId, date }
  create:   { id, message, databaseId, storeId, parentIds, authorId, committerId, treeId, date }
  update:   { id, message, databaseId, storeId, parentIds, authorId, committerId, treeId, date }
  delete:   { id }
```

### MODEL: orgMembershipDefault

Access: `db.orgMembershipDefault`

```
METHODS:
  db.orgMembershipDefault.findMany({ select, where?, orderBy?, first?, offset? })
  db.orgMembershipDefault.findOne({ where: { id }, select })
  db.orgMembershipDefault.create({ data: { createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }, select })
  db.orgMembershipDefault.update({ where: { id }, data, select })
  db.orgMembershipDefault.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  entityId: string
  deleteMemberCascadeGroups: boolean
  createGroupsCascadeMembers: boolean

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  entityId: string
  deleteMemberCascadeGroups: boolean
  createGroupsCascadeMembers: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, entityId, deleteMemberCascadeGroups, createGroupsCascadeMembers }
  delete:   { id }
```

### MODEL: email

Access: `db.email`

```
METHODS:
  db.email.findMany({ select, where?, orderBy?, first?, offset? })
  db.email.findOne({ where: { id }, select })
  db.email.create({ data: { ownerId, email, isVerified, isPrimary }, select })
  db.email.update({ where: { id }, data, select })
  db.email.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  ownerId: string
  email: ConstructiveInternalTypeEmail
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  ownerId: string
  email: ConstructiveInternalTypeEmail
  isVerified: boolean
  isPrimary: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, ownerId, email, isVerified, isPrimary, createdAt, updatedAt }]
  findOne:  { id, ownerId, email, isVerified, isPrimary, createdAt, updatedAt }
  create:   { id, ownerId, email, isVerified, isPrimary, createdAt, updatedAt }
  update:   { id, ownerId, email, isVerified, isPrimary, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: auditLog

Access: `db.auditLog`

```
METHODS:
  db.auditLog.findMany({ select, where?, orderBy?, first?, offset? })
  db.auditLog.findOne({ where: { id }, select })
  db.auditLog.create({ data: { event, actorId, origin, userAgent, ipAddress, success }, select })
  db.auditLog.update({ where: { id }, data, select })
  db.auditLog.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  event: string
  actorId: string
  origin: ConstructiveInternalTypeOrigin
  userAgent: string
  ipAddress: string
  success: boolean
  createdAt: string

EDITABLE FIELDS:
  event: string
  actorId: string
  origin: ConstructiveInternalTypeOrigin
  userAgent: string
  ipAddress: string
  success: boolean

OUTPUT: Promise<JSON>
  findMany: [{ id, event, actorId, origin, userAgent, ipAddress, success, createdAt }]
  findOne:  { id, event, actorId, origin, userAgent, ipAddress, success, createdAt }
  create:   { id, event, actorId, origin, userAgent, ipAddress, success, createdAt }
  update:   { id, event, actorId, origin, userAgent, ipAddress, success, createdAt }
  delete:   { id }
```

### MODEL: appLevel

Access: `db.appLevel`

```
METHODS:
  db.appLevel.findMany({ select, where?, orderBy?, first?, offset? })
  db.appLevel.findOne({ where: { id }, select })
  db.appLevel.create({ data: { name, description, image, ownerId }, select })
  db.appLevel.update({ where: { id }, data, select })
  db.appLevel.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  name: string
  description: string
  image: ConstructiveInternalTypeImage
  ownerId: string
  createdAt: string
  updatedAt: string

EDITABLE FIELDS:
  name: string
  description: string
  image: ConstructiveInternalTypeImage
  ownerId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, description, image, ownerId, createdAt, updatedAt }]
  findOne:  { id, name, description, image, ownerId, createdAt, updatedAt }
  create:   { id, name, description, image, ownerId, createdAt, updatedAt }
  update:   { id, name, description, image, ownerId, createdAt, updatedAt }
  delete:   { id }
```

### MODEL: sqlMigration

Access: `db.sqlMigration`

```
METHODS:
  db.sqlMigration.findMany({ select, where?, orderBy?, first?, offset? })
  db.sqlMigration.findOne({ where: { id }, select })
  db.sqlMigration.create({ data: { name, databaseId, deploy, deps, payload, content, revert, verify, action, actionId, actorId }, select })
  db.sqlMigration.update({ where: { id }, data, select })
  db.sqlMigration.delete({ where: { id } })

FIELDS:
  id: number (primary key)
  name: string
  databaseId: string
  deploy: string
  deps: string[]
  payload: unknown
  content: string
  revert: string
  verify: string
  createdAt: string
  action: string
  actionId: string
  actorId: string

EDITABLE FIELDS:
  name: string
  databaseId: string
  deploy: string
  deps: string[]
  payload: unknown
  content: string
  revert: string
  verify: string
  action: string
  actionId: string
  actorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, name, databaseId, deploy, deps, payload, content, revert, verify, createdAt, action, actionId, actorId }]
  findOne:  { id, name, databaseId, deploy, deps, payload, content, revert, verify, createdAt, action, actionId, actorId }
  create:   { id, name, databaseId, deploy, deps, payload, content, revert, verify, createdAt, action, actionId, actorId }
  update:   { id, name, databaseId, deploy, deps, payload, content, revert, verify, createdAt, action, actionId, actorId }
  delete:   { id }
```

### MODEL: astMigration

Access: `db.astMigration`

```
METHODS:
  db.astMigration.findMany({ select, where?, orderBy?, first?, offset? })
  db.astMigration.findOne({ where: { id }, select })
  db.astMigration.create({ data: { databaseId, name, requires, payload, deploys, deploy, revert, verify, action, actionId, actorId }, select })
  db.astMigration.update({ where: { id }, data, select })
  db.astMigration.delete({ where: { id } })

FIELDS:
  id: number (primary key)
  databaseId: string
  name: string
  requires: string[]
  payload: unknown
  deploys: string
  deploy: unknown
  revert: unknown
  verify: unknown
  createdAt: string
  action: string
  actionId: string
  actorId: string

EDITABLE FIELDS:
  databaseId: string
  name: string
  requires: string[]
  payload: unknown
  deploys: string
  deploy: unknown
  revert: unknown
  verify: unknown
  action: string
  actionId: string
  actorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, name, requires, payload, deploys, deploy, revert, verify, createdAt, action, actionId, actorId }]
  findOne:  { id, databaseId, name, requires, payload, deploys, deploy, revert, verify, createdAt, action, actionId, actorId }
  create:   { id, databaseId, name, requires, payload, deploys, deploy, revert, verify, createdAt, action, actionId, actorId }
  update:   { id, databaseId, name, requires, payload, deploys, deploy, revert, verify, createdAt, action, actionId, actorId }
  delete:   { id }
```

### MODEL: appMembership

Access: `db.appMembership`

```
METHODS:
  db.appMembership.findMany({ select, where?, orderBy?, first?, offset? })
  db.appMembership.findOne({ where: { id }, select })
  db.appMembership.create({ data: { createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }, select })
  db.appMembership.update({ where: { id }, data, select })
  db.appMembership.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isVerified: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string

EDITABLE FIELDS:
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isVerified: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string

OUTPUT: Promise<JSON>
  findMany: [{ id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }]
  findOne:  { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }
  create:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }
  update:   { id, createdAt, updatedAt, createdBy, updatedBy, isApproved, isBanned, isDisabled, isVerified, isActive, isOwner, isAdmin, permissions, granted, actorId }
  delete:   { id }
```

### MODEL: user

Access: `db.user`

```
METHODS:
  db.user.findMany({ select, where?, orderBy?, first?, offset? })
  db.user.findOne({ where: { id }, select })
  db.user.create({ data: { username, displayName, profilePicture, searchTsv, type, searchTsvRank }, select })
  db.user.update({ where: { id }, data, select })
  db.user.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  username: string
  displayName: string
  profilePicture: ConstructiveInternalTypeImage
  searchTsv: string
  type: number
  createdAt: string
  updatedAt: string
  searchTsvRank: number

EDITABLE FIELDS:
  username: string
  displayName: string
  profilePicture: ConstructiveInternalTypeImage
  searchTsv: string
  type: number
  searchTsvRank: number

OUTPUT: Promise<JSON>
  findMany: [{ id, username, displayName, profilePicture, searchTsv, type, createdAt, updatedAt, searchTsvRank }]
  findOne:  { id, username, displayName, profilePicture, searchTsv, type, createdAt, updatedAt, searchTsvRank }
  create:   { id, username, displayName, profilePicture, searchTsv, type, createdAt, updatedAt, searchTsvRank }
  update:   { id, username, displayName, profilePicture, searchTsv, type, createdAt, updatedAt, searchTsvRank }
  delete:   { id }
```

### MODEL: hierarchyModule

Access: `db.hierarchyModule`

```
METHODS:
  db.hierarchyModule.findMany({ select, where?, orderBy?, first?, offset? })
  db.hierarchyModule.findOne({ where: { id }, select })
  db.hierarchyModule.create({ data: { databaseId, schemaId, privateSchemaId, chartEdgesTableId, chartEdgesTableName, hierarchySprtTableId, hierarchySprtTableName, chartEdgeGrantsTableId, chartEdgeGrantsTableName, entityTableId, usersTableId, prefix, privateSchemaName, sprtTableName, rebuildHierarchyFunction, getSubordinatesFunction, getManagersFunction, isManagerOfFunction }, select })
  db.hierarchyModule.update({ where: { id }, data, select })
  db.hierarchyModule.delete({ where: { id } })

FIELDS:
  id: string (primary key)
  databaseId: string
  schemaId: string
  privateSchemaId: string
  chartEdgesTableId: string
  chartEdgesTableName: string
  hierarchySprtTableId: string
  hierarchySprtTableName: string
  chartEdgeGrantsTableId: string
  chartEdgeGrantsTableName: string
  entityTableId: string
  usersTableId: string
  prefix: string
  privateSchemaName: string
  sprtTableName: string
  rebuildHierarchyFunction: string
  getSubordinatesFunction: string
  getManagersFunction: string
  isManagerOfFunction: string
  createdAt: string

EDITABLE FIELDS:
  databaseId: string
  schemaId: string
  privateSchemaId: string
  chartEdgesTableId: string
  chartEdgesTableName: string
  hierarchySprtTableId: string
  hierarchySprtTableName: string
  chartEdgeGrantsTableId: string
  chartEdgeGrantsTableName: string
  entityTableId: string
  usersTableId: string
  prefix: string
  privateSchemaName: string
  sprtTableName: string
  rebuildHierarchyFunction: string
  getSubordinatesFunction: string
  getManagersFunction: string
  isManagerOfFunction: string

OUTPUT: Promise<JSON>
  findMany: [{ id, databaseId, schemaId, privateSchemaId, chartEdgesTableId, chartEdgesTableName, hierarchySprtTableId, hierarchySprtTableName, chartEdgeGrantsTableId, chartEdgeGrantsTableName, entityTableId, usersTableId, prefix, privateSchemaName, sprtTableName, rebuildHierarchyFunction, getSubordinatesFunction, getManagersFunction, isManagerOfFunction, createdAt }]
  findOne:  { id, databaseId, schemaId, privateSchemaId, chartEdgesTableId, chartEdgesTableName, hierarchySprtTableId, hierarchySprtTableName, chartEdgeGrantsTableId, chartEdgeGrantsTableName, entityTableId, usersTableId, prefix, privateSchemaName, sprtTableName, rebuildHierarchyFunction, getSubordinatesFunction, getManagersFunction, isManagerOfFunction, createdAt }
  create:   { id, databaseId, schemaId, privateSchemaId, chartEdgesTableId, chartEdgesTableName, hierarchySprtTableId, hierarchySprtTableName, chartEdgeGrantsTableId, chartEdgeGrantsTableName, entityTableId, usersTableId, prefix, privateSchemaName, sprtTableName, rebuildHierarchyFunction, getSubordinatesFunction, getManagersFunction, isManagerOfFunction, createdAt }
  update:   { id, databaseId, schemaId, privateSchemaId, chartEdgesTableId, chartEdgesTableName, hierarchySprtTableId, hierarchySprtTableName, chartEdgeGrantsTableId, chartEdgeGrantsTableName, entityTableId, usersTableId, prefix, privateSchemaName, sprtTableName, rebuildHierarchyFunction, getSubordinatesFunction, getManagersFunction, isManagerOfFunction, createdAt }
  delete:   { id }
```

## CUSTOM OPERATIONS

### OPERATION: currentUserId

currentUserId

```
TYPE: query
ACCESS: db.query.currentUserId
USAGE: db.query.currentUserId().execute()

INPUT: none

OUTPUT: Promise<JSON>
```

### OPERATION: currentIpAddress

currentIpAddress

```
TYPE: query
ACCESS: db.query.currentIpAddress
USAGE: db.query.currentIpAddress().execute()

INPUT: none

OUTPUT: Promise<JSON>
```

### OPERATION: currentUserAgent

currentUserAgent

```
TYPE: query
ACCESS: db.query.currentUserAgent
USAGE: db.query.currentUserAgent().execute()

INPUT: none

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetPaddedMask

appPermissionsGetPaddedMask

```
TYPE: query
ACCESS: db.query.appPermissionsGetPaddedMask
USAGE: db.query.appPermissionsGetPaddedMask({ mask: <value> }).execute()

INPUT:
  mask: BitString

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetPaddedMask

orgPermissionsGetPaddedMask

```
TYPE: query
ACCESS: db.query.orgPermissionsGetPaddedMask
USAGE: db.query.orgPermissionsGetPaddedMask({ mask: <value> }).execute()

INPUT:
  mask: BitString

OUTPUT: Promise<JSON>
```

### OPERATION: stepsAchieved

stepsAchieved

```
TYPE: query
ACCESS: db.query.stepsAchieved
USAGE: db.query.stepsAchieved({ vlevel: <value>, vroleId: <value> }).execute()

INPUT:
  vlevel: String
  vroleId: UUID

OUTPUT: Promise<JSON>
```

### OPERATION: revParse

revParse

```
TYPE: query
ACCESS: db.query.revParse
USAGE: db.query.revParse({ dbId: <value>, storeId: <value>, refname: <value> }).execute()

INPUT:
  dbId: UUID
  storeId: UUID
  refname: String

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetMask

appPermissionsGetMask

```
TYPE: query
ACCESS: db.query.appPermissionsGetMask
USAGE: db.query.appPermissionsGetMask({ ids: <value> }).execute()

INPUT:
  ids: [UUID]

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetMask

orgPermissionsGetMask

```
TYPE: query
ACCESS: db.query.orgPermissionsGetMask
USAGE: db.query.orgPermissionsGetMask({ ids: <value> }).execute()

INPUT:
  ids: [UUID]

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetMaskByNames

appPermissionsGetMaskByNames

```
TYPE: query
ACCESS: db.query.appPermissionsGetMaskByNames
USAGE: db.query.appPermissionsGetMaskByNames({ names: <value> }).execute()

INPUT:
  names: [String]

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetMaskByNames

orgPermissionsGetMaskByNames

```
TYPE: query
ACCESS: db.query.orgPermissionsGetMaskByNames
USAGE: db.query.orgPermissionsGetMaskByNames({ names: <value> }).execute()

INPUT:
  names: [String]

OUTPUT: Promise<JSON>
```

### OPERATION: appPermissionsGetByMask

Reads and enables pagination through a set of `AppPermission`.

```
TYPE: query
ACCESS: db.query.appPermissionsGetByMask
USAGE: db.query.appPermissionsGetByMask({ mask: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  mask: BitString
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: orgPermissionsGetByMask

Reads and enables pagination through a set of `OrgPermission`.

```
TYPE: query
ACCESS: db.query.orgPermissionsGetByMask
USAGE: db.query.orgPermissionsGetByMask({ mask: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  mask: BitString
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: getAllObjectsFromRoot

Reads and enables pagination through a set of `Object`.

```
TYPE: query
ACCESS: db.query.getAllObjectsFromRoot
USAGE: db.query.getAllObjectsFromRoot({ databaseId: <value>, id: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  databaseId: UUID
  id: UUID
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: getPathObjectsFromRoot

Reads and enables pagination through a set of `Object`.

```
TYPE: query
ACCESS: db.query.getPathObjectsFromRoot
USAGE: db.query.getPathObjectsFromRoot({ databaseId: <value>, id: <value>, path: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  databaseId: UUID
  id: UUID
  path: [String]
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: getObjectAtPath

getObjectAtPath

```
TYPE: query
ACCESS: db.query.getObjectAtPath
USAGE: db.query.getObjectAtPath({ dbId: <value>, storeId: <value>, path: <value>, refname: <value> }).execute()

INPUT:
  dbId: UUID
  storeId: UUID
  path: [String]
  refname: String

OUTPUT: Promise<JSON>
```

### OPERATION: stepsRequired

Reads and enables pagination through a set of `AppLevelRequirement`.

```
TYPE: query
ACCESS: db.query.stepsRequired
USAGE: db.query.stepsRequired({ vlevel: <value>, vroleId: <value>, first: <value>, offset: <value>, after: <value> }).execute()

INPUT:
  vlevel: String
  vroleId: UUID
  first: Int
  offset: Int
  after: Cursor

OUTPUT: Promise<JSON>
```

### OPERATION: currentUser

currentUser

```
TYPE: query
ACCESS: db.query.currentUser
USAGE: db.query.currentUser().execute()

INPUT: none

OUTPUT: Promise<JSON>
```

### OPERATION: signOut

signOut

```
TYPE: mutation
ACCESS: db.mutation.signOut
USAGE: db.mutation.signOut({ input: <value> }).execute()

INPUT:
  input: SignOutInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: sendAccountDeletionEmail

sendAccountDeletionEmail

```
TYPE: mutation
ACCESS: db.mutation.sendAccountDeletionEmail
USAGE: db.mutation.sendAccountDeletionEmail({ input: <value> }).execute()

INPUT:
  input: SendAccountDeletionEmailInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: checkPassword

checkPassword

```
TYPE: mutation
ACCESS: db.mutation.checkPassword
USAGE: db.mutation.checkPassword({ input: <value> }).execute()

INPUT:
  input: CheckPasswordInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: submitInviteCode

submitInviteCode

```
TYPE: mutation
ACCESS: db.mutation.submitInviteCode
USAGE: db.mutation.submitInviteCode({ input: <value> }).execute()

INPUT:
  input: SubmitInviteCodeInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: submitOrgInviteCode

submitOrgInviteCode

```
TYPE: mutation
ACCESS: db.mutation.submitOrgInviteCode
USAGE: db.mutation.submitOrgInviteCode({ input: <value> }).execute()

INPUT:
  input: SubmitOrgInviteCodeInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: freezeObjects

freezeObjects

```
TYPE: mutation
ACCESS: db.mutation.freezeObjects
USAGE: db.mutation.freezeObjects({ input: <value> }).execute()

INPUT:
  input: FreezeObjectsInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: initEmptyRepo

initEmptyRepo

```
TYPE: mutation
ACCESS: db.mutation.initEmptyRepo
USAGE: db.mutation.initEmptyRepo({ input: <value> }).execute()

INPUT:
  input: InitEmptyRepoInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: confirmDeleteAccount

confirmDeleteAccount

```
TYPE: mutation
ACCESS: db.mutation.confirmDeleteAccount
USAGE: db.mutation.confirmDeleteAccount({ input: <value> }).execute()

INPUT:
  input: ConfirmDeleteAccountInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: setPassword

setPassword

```
TYPE: mutation
ACCESS: db.mutation.setPassword
USAGE: db.mutation.setPassword({ input: <value> }).execute()

INPUT:
  input: SetPasswordInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: verifyEmail

verifyEmail

```
TYPE: mutation
ACCESS: db.mutation.verifyEmail
USAGE: db.mutation.verifyEmail({ input: <value> }).execute()

INPUT:
  input: VerifyEmailInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: resetPassword

resetPassword

```
TYPE: mutation
ACCESS: db.mutation.resetPassword
USAGE: db.mutation.resetPassword({ input: <value> }).execute()

INPUT:
  input: ResetPasswordInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: removeNodeAtPath

removeNodeAtPath

```
TYPE: mutation
ACCESS: db.mutation.removeNodeAtPath
USAGE: db.mutation.removeNodeAtPath({ input: <value> }).execute()

INPUT:
  input: RemoveNodeAtPathInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: bootstrapUser

bootstrapUser

```
TYPE: mutation
ACCESS: db.mutation.bootstrapUser
USAGE: db.mutation.bootstrapUser({ input: <value> }).execute()

INPUT:
  input: BootstrapUserInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: setDataAtPath

setDataAtPath

```
TYPE: mutation
ACCESS: db.mutation.setDataAtPath
USAGE: db.mutation.setDataAtPath({ input: <value> }).execute()

INPUT:
  input: SetDataAtPathInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: setPropsAndCommit

setPropsAndCommit

```
TYPE: mutation
ACCESS: db.mutation.setPropsAndCommit
USAGE: db.mutation.setPropsAndCommit({ input: <value> }).execute()

INPUT:
  input: SetPropsAndCommitInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: provisionDatabaseWithUser

provisionDatabaseWithUser

```
TYPE: mutation
ACCESS: db.mutation.provisionDatabaseWithUser
USAGE: db.mutation.provisionDatabaseWithUser({ input: <value> }).execute()

INPUT:
  input: ProvisionDatabaseWithUserInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: signInOneTimeToken

signInOneTimeToken

```
TYPE: mutation
ACCESS: db.mutation.signInOneTimeToken
USAGE: db.mutation.signInOneTimeToken({ input: <value> }).execute()

INPUT:
  input: SignInOneTimeTokenInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: createUserDatabase

Creates a new user database with all required modules, permissions, and RLS policies.

Parameters:
  - database_name: Name for the new database (required)
  - owner_id: UUID of the owner user (required)
  - include_invites: Include invite system (default: true)
  - include_groups: Include group-level memberships (default: false)
  - include_levels: Include levels/achievements (default: false)
  - bitlen: Bit length for permission masks (default: 64)
  - tokens_expiration: Token expiration interval (default: 30 days)

Returns the database_id UUID of the newly created database.

Example usage:
  SELECT metaschema_public.create_user_database('my_app', 'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid);
  SELECT metaschema_public.create_user_database('my_app', 'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid, true, true);  -- with invites and groups


```
TYPE: mutation
ACCESS: db.mutation.createUserDatabase
USAGE: db.mutation.createUserDatabase({ input: <value> }).execute()

INPUT:
  input: CreateUserDatabaseInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: extendTokenExpires

extendTokenExpires

```
TYPE: mutation
ACCESS: db.mutation.extendTokenExpires
USAGE: db.mutation.extendTokenExpires({ input: <value> }).execute()

INPUT:
  input: ExtendTokenExpiresInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: signIn

signIn

```
TYPE: mutation
ACCESS: db.mutation.signIn
USAGE: db.mutation.signIn({ input: <value> }).execute()

INPUT:
  input: SignInInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: signUp

signUp

```
TYPE: mutation
ACCESS: db.mutation.signUp
USAGE: db.mutation.signUp({ input: <value> }).execute()

INPUT:
  input: SignUpInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: setFieldOrder

setFieldOrder

```
TYPE: mutation
ACCESS: db.mutation.setFieldOrder
USAGE: db.mutation.setFieldOrder({ input: <value> }).execute()

INPUT:
  input: SetFieldOrderInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: oneTimeToken

oneTimeToken

```
TYPE: mutation
ACCESS: db.mutation.oneTimeToken
USAGE: db.mutation.oneTimeToken({ input: <value> }).execute()

INPUT:
  input: OneTimeTokenInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: insertNodeAtPath

insertNodeAtPath

```
TYPE: mutation
ACCESS: db.mutation.insertNodeAtPath
USAGE: db.mutation.insertNodeAtPath({ input: <value> }).execute()

INPUT:
  input: InsertNodeAtPathInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: updateNodeAtPath

updateNodeAtPath

```
TYPE: mutation
ACCESS: db.mutation.updateNodeAtPath
USAGE: db.mutation.updateNodeAtPath({ input: <value> }).execute()

INPUT:
  input: UpdateNodeAtPathInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: setAndCommit

setAndCommit

```
TYPE: mutation
ACCESS: db.mutation.setAndCommit
USAGE: db.mutation.setAndCommit({ input: <value> }).execute()

INPUT:
  input: SetAndCommitInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: applyRls

applyRls

```
TYPE: mutation
ACCESS: db.mutation.applyRls
USAGE: db.mutation.applyRls({ input: <value> }).execute()

INPUT:
  input: ApplyRlsInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: forgotPassword

forgotPassword

```
TYPE: mutation
ACCESS: db.mutation.forgotPassword
USAGE: db.mutation.forgotPassword({ input: <value> }).execute()

INPUT:
  input: ForgotPasswordInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: sendVerificationEmail

sendVerificationEmail

```
TYPE: mutation
ACCESS: db.mutation.sendVerificationEmail
USAGE: db.mutation.sendVerificationEmail({ input: <value> }).execute()

INPUT:
  input: SendVerificationEmailInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: verifyPassword

verifyPassword

```
TYPE: mutation
ACCESS: db.mutation.verifyPassword
USAGE: db.mutation.verifyPassword({ input: <value> }).execute()

INPUT:
  input: VerifyPasswordInput (required)

OUTPUT: Promise<JSON>
```

### OPERATION: verifyTotp

verifyTotp

```
TYPE: mutation
ACCESS: db.mutation.verifyTotp
USAGE: db.mutation.verifyTotp({ input: <value> }).execute()

INPUT:
  input: VerifyTotpInput (required)

OUTPUT: Promise<JSON>
```

## PATTERNS

```typescript
// All methods require .execute() to run
const result = await db.modelName.findMany({ select: { id: true } }).execute();

// Select specific fields
const partial = await db.modelName.findMany({ select: { id: true, name: true } }).execute();

// Filter with where clause
const filtered = await db.modelName.findMany({ select: { id: true }, where: { name: 'test' } }).execute();
```
