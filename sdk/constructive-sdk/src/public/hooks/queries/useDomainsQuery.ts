/**
 * List query hook for Domain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useQuery } from '@tanstack/react-query';
import type { UseQueryOptions, UseQueryResult, QueryClient } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildListSelectionArgs } from '../selection';
import type { ListSelectionConfig } from '../selection';
import { domainKeys } from '../query-keys';
import type {
  DomainSelect,
  DomainWithRelations,
  DomainFilter,
  DomainOrderBy,
} from '../../orm/input-types';
import type {
  FindManyArgs,
  InferSelectResult,
  ConnectionResult,
  HookStrictSelect,
} from '../../orm/select-types';
export type {
  DomainSelect,
  DomainWithRelations,
  DomainFilter,
  DomainOrderBy,
} from '../../orm/input-types';
/** Query key factory - re-exported from query-keys.ts */
export const domainsQueryKey = domainKeys.list;
/**
 * Query hook for fetching Domain list
 *
 * @example
 * ```tsx
 * const { data, isLoading } = useDomainsQuery({
 *   selection: {
 *     fields: { id: true, name: true },
 *     where: { name: { equalTo: "example" } },
 *     orderBy: ['CREATED_AT_DESC'],
 *     first: 10,
 *   },
 * });
 * ```
 */
export function useDomainsQuery<
  S extends DomainSelect,
  TData = {
    domains: ConnectionResult<InferSelectResult<DomainWithRelations, S>>;
  },
>(
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DomainFilter, DomainOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DomainSelect>;
  } & Omit<
    UseQueryOptions<
      {
        domains: ConnectionResult<InferSelectResult<DomainWithRelations, S>>;
      },
      Error,
      TData
    >,
    'queryKey' | 'queryFn'
  >
): UseQueryResult<TData>;
export function useDomainsQuery(
  params: {
    selection: ListSelectionConfig<DomainSelect, DomainFilter, DomainOrderBy>;
  } & Omit<UseQueryOptions<any, Error, any, any>, 'queryKey' | 'queryFn'>
) {
  const args = buildListSelectionArgs<DomainSelect, DomainFilter, DomainOrderBy>(params.selection);
  const { selection: _selection, ...queryOptions } = params ?? {};
  void _selection;
  return useQuery({
    queryKey: domainKeys.list(args),
    queryFn: () => getClient().domain.findMany(args).unwrap(),
    ...queryOptions,
  });
}
/**
 * Fetch Domain list without React hooks
 *
 * @example
 * ```ts
 * const data = await fetchDomainsQuery({
 *   selection: {
 *     fields: { id: true },
 *     first: 10,
 *   },
 * });
 * ```
 */
export async function fetchDomainsQuery<S extends DomainSelect>(params: {
  selection: {
    fields: S;
  } & Omit<ListSelectionConfig<S, DomainFilter, DomainOrderBy>, 'fields'> &
    HookStrictSelect<NoInfer<S>, DomainSelect>;
}): Promise<{
  domains: ConnectionResult<InferSelectResult<DomainWithRelations, S>>;
}>;
export async function fetchDomainsQuery(params: {
  selection: ListSelectionConfig<DomainSelect, DomainFilter, DomainOrderBy>;
}) {
  const args = buildListSelectionArgs<DomainSelect, DomainFilter, DomainOrderBy>(params.selection);
  return getClient().domain.findMany(args).unwrap();
}
/**
 * Prefetch Domain list for SSR or cache warming
 *
 * @example
 * ```ts
 * await prefetchDomainsQuery(queryClient, { selection: { fields: { id: true }, first: 10 } });
 * ```
 */
export async function prefetchDomainsQuery<S extends DomainSelect>(
  queryClient: QueryClient,
  params: {
    selection: {
      fields: S;
    } & Omit<ListSelectionConfig<S, DomainFilter, DomainOrderBy>, 'fields'> &
      HookStrictSelect<NoInfer<S>, DomainSelect>;
  }
): Promise<void>;
export async function prefetchDomainsQuery(
  queryClient: QueryClient,
  params: {
    selection: ListSelectionConfig<DomainSelect, DomainFilter, DomainOrderBy>;
  }
): Promise<void> {
  const args = buildListSelectionArgs<DomainSelect, DomainFilter, DomainOrderBy>(params.selection);
  await queryClient.prefetchQuery({
    queryKey: domainKeys.list(args),
    queryFn: () => getClient().domain.findMany(args).unwrap(),
  });
}
