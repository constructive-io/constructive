/**
 * Create mutation hook for Site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteKeys } from '../query-keys';
import { siteMutationKeys } from '../mutation-keys';
import type { SiteSelect, SiteWithRelations, CreateSiteInput } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteSelect, SiteWithRelations, CreateSiteInput } from '../../orm/input-types';
/**
 * Mutation hook for creating a Site
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateSiteMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateSiteMutation<S extends SiteSelect>(
  params: {
    selection: {
      fields: S & SiteSelect;
    } & HookStrictSelect<NoInfer<S>, SiteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createSite: {
          site: InferSelectResult<SiteWithRelations, S>;
        };
      },
      Error,
      CreateSiteInput['site']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createSite: {
      site: InferSelectResult<SiteWithRelations, S>;
    };
  },
  Error,
  CreateSiteInput['site']
>;
export function useCreateSiteMutation(
  params: {
    selection: SelectionConfig<SiteSelect>;
  } & Omit<UseMutationOptions<any, Error, CreateSiteInput['site']>, 'mutationFn'>
) {
  const args = buildSelectionArgs<SiteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteMutationKeys.create(),
    mutationFn: (data: CreateSiteInput['site']) =>
      getClient()
        .site.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: siteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
