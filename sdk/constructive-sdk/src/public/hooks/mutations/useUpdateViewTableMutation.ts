/**
 * Update mutation hook for ViewTable
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { viewTableKeys } from '../query-keys';
import { viewTableMutationKeys } from '../mutation-keys';
import type {
  ViewTableSelect,
  ViewTableWithRelations,
  ViewTablePatch,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  ViewTableSelect,
  ViewTableWithRelations,
  ViewTablePatch,
} from '../../orm/input-types';
/**
 * Mutation hook for updating a ViewTable
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdateViewTableMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', viewTablePatch: { name: 'Updated' } });
 * ```
 */
export function useUpdateViewTableMutation<S extends ViewTableSelect>(
  params: {
    selection: {
      fields: S & ViewTableSelect;
    } & HookStrictSelect<NoInfer<S>, ViewTableSelect>;
  } & Omit<
    UseMutationOptions<
      {
        updateViewTable: {
          viewTable: InferSelectResult<ViewTableWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        viewTablePatch: ViewTablePatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updateViewTable: {
      viewTable: InferSelectResult<ViewTableWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    viewTablePatch: ViewTablePatch;
  }
>;
export function useUpdateViewTableMutation(
  params: {
    selection: SelectionConfig<ViewTableSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        viewTablePatch: ViewTablePatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ViewTableSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: viewTableMutationKeys.all,
    mutationFn: ({ id, viewTablePatch }: { id: string; viewTablePatch: ViewTablePatch }) =>
      getClient()
        .viewTable.update({
          where: {
            id,
          },
          data: viewTablePatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: viewTableKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: viewTableKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
