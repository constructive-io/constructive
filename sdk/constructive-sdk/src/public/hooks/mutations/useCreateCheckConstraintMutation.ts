/**
 * Create mutation hook for CheckConstraint
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { checkConstraintKeys } from '../query-keys';
import { checkConstraintMutationKeys } from '../mutation-keys';
import type {
  CheckConstraintSelect,
  CheckConstraintWithRelations,
  CreateCheckConstraintInput,
} from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type {
  CheckConstraintSelect,
  CheckConstraintWithRelations,
  CreateCheckConstraintInput,
} from '../../orm/input-types';
/**
 * Mutation hook for creating a CheckConstraint
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useCreateCheckConstraintMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ name: 'New item' });
 * ```
 */
export function useCreateCheckConstraintMutation<S extends CheckConstraintSelect>(
  params: {
    selection: {
      fields: S & CheckConstraintSelect;
    } & HookStrictSelect<NoInfer<S>, CheckConstraintSelect>;
  } & Omit<
    UseMutationOptions<
      {
        createCheckConstraint: {
          checkConstraint: InferSelectResult<CheckConstraintWithRelations, S>;
        };
      },
      Error,
      CreateCheckConstraintInput['checkConstraint']
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    createCheckConstraint: {
      checkConstraint: InferSelectResult<CheckConstraintWithRelations, S>;
    };
  },
  Error,
  CreateCheckConstraintInput['checkConstraint']
>;
export function useCreateCheckConstraintMutation(
  params: {
    selection: SelectionConfig<CheckConstraintSelect>;
  } & Omit<
    UseMutationOptions<any, Error, CreateCheckConstraintInput['checkConstraint']>,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<CheckConstraintSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: checkConstraintMutationKeys.create(),
    mutationFn: (data: CreateCheckConstraintInput['checkConstraint']) =>
      getClient()
        .checkConstraint.create({
          data,
          select: args.select,
        })
        .unwrap(),
    onSuccess: () => {
      queryClient.invalidateQueries({
        queryKey: checkConstraintKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
