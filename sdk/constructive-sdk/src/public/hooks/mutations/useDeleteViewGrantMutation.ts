/**
 * Delete mutation hook for ViewGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { viewGrantKeys } from '../query-keys';
import { viewGrantMutationKeys } from '../mutation-keys';
import type { ViewGrantSelect, ViewGrantWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { ViewGrantSelect, ViewGrantWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a ViewGrant with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteViewGrantMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteViewGrantMutation<S extends ViewGrantSelect>(
  params: {
    selection: {
      fields: S & ViewGrantSelect;
    } & HookStrictSelect<NoInfer<S>, ViewGrantSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteViewGrant: {
          viewGrant: InferSelectResult<ViewGrantWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteViewGrant: {
      viewGrant: InferSelectResult<ViewGrantWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteViewGrantMutation(
  params: {
    selection: SelectionConfig<ViewGrantSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<ViewGrantSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: viewGrantMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .viewGrant.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: viewGrantKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: viewGrantKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
