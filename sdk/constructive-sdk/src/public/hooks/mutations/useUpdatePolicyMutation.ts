/**
 * Update mutation hook for Policy
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { policyKeys } from '../query-keys';
import { policyMutationKeys } from '../mutation-keys';
import type { PolicySelect, PolicyWithRelations, PolicyPatch } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { PolicySelect, PolicyWithRelations, PolicyPatch } from '../../orm/input-types';
/**
 * Mutation hook for updating a Policy
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useUpdatePolicyMutation({
 *   selection: { fields: { id: true, name: true } },
 * });
 *
 * mutate({ id: 'value-here', policyPatch: { name: 'Updated' } });
 * ```
 */
export function useUpdatePolicyMutation<S extends PolicySelect>(
  params: {
    selection: {
      fields: S & PolicySelect;
    } & HookStrictSelect<NoInfer<S>, PolicySelect>;
  } & Omit<
    UseMutationOptions<
      {
        updatePolicy: {
          policy: InferSelectResult<PolicyWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
        policyPatch: PolicyPatch;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    updatePolicy: {
      policy: InferSelectResult<PolicyWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
    policyPatch: PolicyPatch;
  }
>;
export function useUpdatePolicyMutation(
  params: {
    selection: SelectionConfig<PolicySelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
        policyPatch: PolicyPatch;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<PolicySelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: policyMutationKeys.all,
    mutationFn: ({ id, policyPatch }: { id: string; policyPatch: PolicyPatch }) =>
      getClient()
        .policy.update({
          where: {
            id,
          },
          data: policyPatch,
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({
        queryKey: policyKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: policyKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
