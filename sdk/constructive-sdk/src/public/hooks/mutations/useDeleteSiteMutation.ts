/**
 * Delete mutation hook for Site
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { siteKeys } from '../query-keys';
import { siteMutationKeys } from '../mutation-keys';
import type { SiteSelect, SiteWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { SiteSelect, SiteWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Site with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteSiteMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteSiteMutation<S extends SiteSelect>(
  params: {
    selection: {
      fields: S & SiteSelect;
    } & HookStrictSelect<NoInfer<S>, SiteSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteSite: {
          site: InferSelectResult<SiteWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteSite: {
      site: InferSelectResult<SiteWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteSiteMutation(
  params: {
    selection: SelectionConfig<SiteSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<SiteSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: siteMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .site.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: siteKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: siteKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
