/**
 * Delete mutation hook for Domain
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */

import { useMutation, useQueryClient } from '@tanstack/react-query';
import type { UseMutationOptions, UseMutationResult } from '@tanstack/react-query';
import { getClient } from '../client';
import { buildSelectionArgs } from '../selection';
import type { SelectionConfig } from '../selection';
import { domainKeys } from '../query-keys';
import { domainMutationKeys } from '../mutation-keys';
import type { DomainSelect, DomainWithRelations } from '../../orm/input-types';
import type { InferSelectResult, HookStrictSelect } from '../../orm/select-types';
export type { DomainSelect, DomainWithRelations } from '../../orm/input-types';
/**
 * Mutation hook for deleting a Domain with typed selection
 *
 * @example
 * ```tsx
 * const { mutate, isPending } = useDeleteDomainMutation({
 *   selection: { fields: { id: true } },
 * });
 *
 * mutate({ id: 'value-to-delete' });
 * ```
 */
export function useDeleteDomainMutation<S extends DomainSelect>(
  params: {
    selection: {
      fields: S & DomainSelect;
    } & HookStrictSelect<NoInfer<S>, DomainSelect>;
  } & Omit<
    UseMutationOptions<
      {
        deleteDomain: {
          domain: InferSelectResult<DomainWithRelations, S>;
        };
      },
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
): UseMutationResult<
  {
    deleteDomain: {
      domain: InferSelectResult<DomainWithRelations, S>;
    };
  },
  Error,
  {
    id: string;
  }
>;
export function useDeleteDomainMutation(
  params: {
    selection: SelectionConfig<DomainSelect>;
  } & Omit<
    UseMutationOptions<
      any,
      Error,
      {
        id: string;
      }
    >,
    'mutationFn'
  >
) {
  const args = buildSelectionArgs<DomainSelect>(params.selection);
  const { selection: _selection, ...mutationOptions } = params ?? {};
  void _selection;
  const queryClient = useQueryClient();
  return useMutation({
    mutationKey: domainMutationKeys.all,
    mutationFn: ({ id }: { id: string }) =>
      getClient()
        .domain.delete({
          where: {
            id,
          },
          select: args.select,
        })
        .unwrap(),
    onSuccess: (_, variables) => {
      queryClient.removeQueries({
        queryKey: domainKeys.detail(variables.id),
      });
      queryClient.invalidateQueries({
        queryKey: domainKeys.lists(),
      });
    },
    ...mutationOptions,
  });
}
