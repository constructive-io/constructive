# React Query Hooks - Agent Reference

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT
> This document is structured for LLM/agent consumption.

## OVERVIEW

React Query hooks wrapping ORM operations for data fetching and mutations.
All query hooks return `UseQueryResult`. All mutation hooks return `UseMutationResult`.

## SETUP

```typescript
import { configure } from './hooks';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

configure({ endpoint: 'https://api.example.com/graphql' });
const queryClient = new QueryClient();
// Wrap app in <QueryClientProvider client={queryClient}>
```

## HOOKS

### HOOK: useGetAllQuery

List all getAll.

```
TYPE: query
USAGE: useGetAllQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  path: string[]
  data: unknown
}>>
```

### HOOK: useCreateGetAllRecordMutation

Create a new getAllRecord.

```
TYPE: mutation
USAGE: const { mutate } = useCreateGetAllRecordMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useAppPermissionsQuery

List all appPermissions.

```
TYPE: query
USAGE: useAppPermissionsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  bitnum: number
  bitstr: string
  description: string
}>>
```

### HOOK: useAppPermissionQuery

Get a single appPermission by id.

```
TYPE: query
USAGE: useAppPermissionQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  bitnum: number
  bitstr: string
  description: string
}>
```

### HOOK: useCreateAppPermissionMutation

Create a new appPermission.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppPermissionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppPermissionMutation

Update an existing appPermission.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppPermissionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppPermissionMutation

Delete a appPermission.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppPermissionMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgPermissionsQuery

List all orgPermissions.

```
TYPE: query
USAGE: useOrgPermissionsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  bitnum: number
  bitstr: string
  description: string
}>>
```

### HOOK: useOrgPermissionQuery

Get a single orgPermission by id.

```
TYPE: query
USAGE: useOrgPermissionQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  bitnum: number
  bitstr: string
  description: string
}>
```

### HOOK: useCreateOrgPermissionMutation

Create a new orgPermission.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgPermissionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgPermissionMutation

Update an existing orgPermission.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgPermissionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgPermissionMutation

Delete a orgPermission.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgPermissionMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useObjectsQuery

List all objects.

```
TYPE: query
USAGE: useObjectsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  hashUuid: string
  id: string
  databaseId: string
  kids: string[]
  ktree: string[]
  data: unknown
  frzn: boolean
  createdAt: string
}>>
```

### HOOK: useObjectQuery

Get a single object by id.

```
TYPE: query
USAGE: useObjectQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  hashUuid: string
  id: string
  databaseId: string
  kids: string[]
  ktree: string[]
  data: unknown
  frzn: boolean
  createdAt: string
}>
```

### HOOK: useCreateObjectMutation

Create a new object.

```
TYPE: mutation
USAGE: const { mutate } = useCreateObjectMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateObjectMutation

Update an existing object.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateObjectMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteObjectMutation

Delete a object.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteObjectMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppLevelRequirementsQuery

List all appLevelRequirements.

```
TYPE: query
USAGE: useAppLevelRequirementsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  level: string
  description: string
  requiredCount: number
  priority: number
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppLevelRequirementQuery

Get a single appLevelRequirement by id.

```
TYPE: query
USAGE: useAppLevelRequirementQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  level: string
  description: string
  requiredCount: number
  priority: number
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppLevelRequirementMutation

Create a new appLevelRequirement.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppLevelRequirementMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppLevelRequirementMutation

Update an existing appLevelRequirement.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppLevelRequirementMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppLevelRequirementMutation

Delete a appLevelRequirement.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppLevelRequirementMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useDatabasesQuery

List all databases.

```
TYPE: query
USAGE: useDatabasesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  schemaHash: string
  name: string
  label: string
  hash: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useDatabaseQuery

Get a single database by id.

```
TYPE: query
USAGE: useDatabaseQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  schemaHash: string
  name: string
  label: string
  hash: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateDatabaseMutation

Create a new database.

```
TYPE: mutation
USAGE: const { mutate } = useCreateDatabaseMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateDatabaseMutation

Update an existing database.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateDatabaseMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteDatabaseMutation

Delete a database.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteDatabaseMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSchemasQuery

List all schemas.

```
TYPE: query
USAGE: useSchemasQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  name: string
  schemaName: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  isPublic: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useSchemaQuery

Get a single schema by id.

```
TYPE: query
USAGE: useSchemaQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  name: string
  schemaName: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  isPublic: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateSchemaMutation

Create a new schema.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSchemaMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSchemaMutation

Update an existing schema.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSchemaMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSchemaMutation

Delete a schema.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSchemaMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useTablesQuery

List all tables.

```
TYPE: query
USAGE: useTablesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  useRls: boolean
  timestamps: boolean
  peoplestamps: boolean
  pluralName: string
  singularName: string
  tags: string[]
  inheritsId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useTableQuery

Get a single table by id.

```
TYPE: query
USAGE: useTableQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  useRls: boolean
  timestamps: boolean
  peoplestamps: boolean
  pluralName: string
  singularName: string
  tags: string[]
  inheritsId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateTableMutation

Create a new table.

```
TYPE: mutation
USAGE: const { mutate } = useCreateTableMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateTableMutation

Update an existing table.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateTableMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteTableMutation

Delete a table.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteTableMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useCheckConstraintsQuery

List all checkConstraints.

```
TYPE: query
USAGE: useCheckConstraintsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  expr: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useCheckConstraintQuery

Get a single checkConstraint by id.

```
TYPE: query
USAGE: useCheckConstraintQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  expr: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateCheckConstraintMutation

Create a new checkConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useCreateCheckConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateCheckConstraintMutation

Update an existing checkConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateCheckConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteCheckConstraintMutation

Delete a checkConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteCheckConstraintMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useFieldsQuery

List all fields.

```
TYPE: query
USAGE: useFieldsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  isRequired: boolean
  defaultValue: string
  defaultValueAst: unknown
  isHidden: boolean
  type: string
  fieldOrder: number
  regexp: string
  chk: unknown
  chkExpr: unknown
  min: number
  max: number
  tags: string[]
  category: ObjectCategory
  module: string
  scope: number
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useFieldQuery

Get a single field by id.

```
TYPE: query
USAGE: useFieldQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  smartTags: unknown
  isRequired: boolean
  defaultValue: string
  defaultValueAst: unknown
  isHidden: boolean
  type: string
  fieldOrder: number
  regexp: string
  chk: unknown
  chkExpr: unknown
  min: number
  max: number
  tags: string[]
  category: ObjectCategory
  module: string
  scope: number
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateFieldMutation

Create a new field.

```
TYPE: mutation
USAGE: const { mutate } = useCreateFieldMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateFieldMutation

Update an existing field.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateFieldMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteFieldMutation

Delete a field.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteFieldMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useForeignKeyConstraintsQuery

List all foreignKeyConstraints.

```
TYPE: query
USAGE: useForeignKeyConstraintsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  refTableId: string
  refFieldIds: string[]
  deleteAction: string
  updateAction: string
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useForeignKeyConstraintQuery

Get a single foreignKeyConstraint by id.

```
TYPE: query
USAGE: useForeignKeyConstraintQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  refTableId: string
  refFieldIds: string[]
  deleteAction: string
  updateAction: string
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateForeignKeyConstraintMutation

Create a new foreignKeyConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useCreateForeignKeyConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateForeignKeyConstraintMutation

Update an existing foreignKeyConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateForeignKeyConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteForeignKeyConstraintMutation

Delete a foreignKeyConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteForeignKeyConstraintMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useFullTextSearchesQuery

List all fullTextSearches.

```
TYPE: query
USAGE: useFullTextSearchesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  fieldId: string
  fieldIds: string[]
  weights: string[]
  langs: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useFullTextSearchQuery

Get a single fullTextSearch by id.

```
TYPE: query
USAGE: useFullTextSearchQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  fieldId: string
  fieldIds: string[]
  weights: string[]
  langs: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateFullTextSearchMutation

Create a new fullTextSearch.

```
TYPE: mutation
USAGE: const { mutate } = useCreateFullTextSearchMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateFullTextSearchMutation

Update an existing fullTextSearch.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateFullTextSearchMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteFullTextSearchMutation

Delete a fullTextSearch.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteFullTextSearchMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useIndicesQuery

List all indices.

```
TYPE: query
USAGE: useIndicesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  fieldIds: string[]
  includeFieldIds: string[]
  accessMethod: string
  indexParams: unknown
  whereClause: unknown
  isUnique: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useIndexQuery

Get a single index by id.

```
TYPE: query
USAGE: useIndexQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  fieldIds: string[]
  includeFieldIds: string[]
  accessMethod: string
  indexParams: unknown
  whereClause: unknown
  isUnique: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateIndexMutation

Create a new index.

```
TYPE: mutation
USAGE: const { mutate } = useCreateIndexMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateIndexMutation

Update an existing index.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateIndexMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteIndexMutation

Delete a index.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteIndexMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useLimitFunctionsQuery

List all limitFunctions.

```
TYPE: query
USAGE: useLimitFunctionsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  data: unknown
  security: number
}>>
```

### HOOK: useLimitFunctionQuery

Get a single limitFunction by id.

```
TYPE: query
USAGE: useLimitFunctionQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  label: string
  description: string
  data: unknown
  security: number
}>
```

### HOOK: useCreateLimitFunctionMutation

Create a new limitFunction.

```
TYPE: mutation
USAGE: const { mutate } = useCreateLimitFunctionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateLimitFunctionMutation

Update an existing limitFunction.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateLimitFunctionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteLimitFunctionMutation

Delete a limitFunction.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteLimitFunctionMutation({})

OUTPUT: UseMutationResult
```

### HOOK: usePoliciesQuery

List all policies.

```
TYPE: query
USAGE: usePoliciesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  roleName: string
  privilege: string
  permissive: boolean
  disabled: boolean
  policyType: string
  data: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: usePolicyQuery

Get a single policy by id.

```
TYPE: query
USAGE: usePolicyQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  roleName: string
  privilege: string
  permissive: boolean
  disabled: boolean
  policyType: string
  data: unknown
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreatePolicyMutation

Create a new policy.

```
TYPE: mutation
USAGE: const { mutate } = useCreatePolicyMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdatePolicyMutation

Update an existing policy.

```
TYPE: mutation
USAGE: const { mutate } = useUpdatePolicyMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeletePolicyMutation

Delete a policy.

```
TYPE: mutation
USAGE: const { mutate } = useDeletePolicyMutation({})

OUTPUT: UseMutationResult
```

### HOOK: usePrimaryKeyConstraintsQuery

List all primaryKeyConstraints.

```
TYPE: query
USAGE: usePrimaryKeyConstraintsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: usePrimaryKeyConstraintQuery

Get a single primaryKeyConstraint by id.

```
TYPE: query
USAGE: usePrimaryKeyConstraintQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  type: string
  fieldIds: string[]
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreatePrimaryKeyConstraintMutation

Create a new primaryKeyConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useCreatePrimaryKeyConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdatePrimaryKeyConstraintMutation

Update an existing primaryKeyConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useUpdatePrimaryKeyConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeletePrimaryKeyConstraintMutation

Delete a primaryKeyConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useDeletePrimaryKeyConstraintMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useTableGrantsQuery

List all tableGrants.

```
TYPE: query
USAGE: useTableGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  privilege: string
  roleName: string
  fieldIds: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useTableGrantQuery

Get a single tableGrant by id.

```
TYPE: query
USAGE: useTableGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  privilege: string
  roleName: string
  fieldIds: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateTableGrantMutation

Create a new tableGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateTableGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateTableGrantMutation

Update an existing tableGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateTableGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteTableGrantMutation

Delete a tableGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteTableGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useTriggersQuery

List all triggers.

```
TYPE: query
USAGE: useTriggersQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  event: string
  functionName: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useTriggerQuery

Get a single trigger by id.

```
TYPE: query
USAGE: useTriggerQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  event: string
  functionName: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateTriggerMutation

Create a new trigger.

```
TYPE: mutation
USAGE: const { mutate } = useCreateTriggerMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateTriggerMutation

Update an existing trigger.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateTriggerMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteTriggerMutation

Delete a trigger.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteTriggerMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useUniqueConstraintsQuery

List all uniqueConstraints.

```
TYPE: query
USAGE: useUniqueConstraintsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useUniqueConstraintQuery

Get a single uniqueConstraint by id.

```
TYPE: query
USAGE: useUniqueConstraintQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  name: string
  description: string
  smartTags: unknown
  type: string
  fieldIds: string[]
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateUniqueConstraintMutation

Create a new uniqueConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useCreateUniqueConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateUniqueConstraintMutation

Update an existing uniqueConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateUniqueConstraintMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteUniqueConstraintMutation

Delete a uniqueConstraint.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteUniqueConstraintMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useViewsQuery

List all views.

```
TYPE: query
USAGE: useViewsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  name: string
  tableId: string
  viewType: string
  data: unknown
  filterType: string
  filterData: unknown
  securityInvoker: boolean
  isReadOnly: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
}>>
```

### HOOK: useViewQuery

Get a single view by id.

```
TYPE: query
USAGE: useViewQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  name: string
  tableId: string
  viewType: string
  data: unknown
  filterType: string
  filterData: unknown
  securityInvoker: boolean
  isReadOnly: boolean
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
}>
```

### HOOK: useCreateViewMutation

Create a new view.

```
TYPE: mutation
USAGE: const { mutate } = useCreateViewMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateViewMutation

Update an existing view.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateViewMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteViewMutation

Delete a view.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteViewMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useViewTablesQuery

List all viewTables.

```
TYPE: query
USAGE: useViewTablesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  viewId: string
  tableId: string
  joinOrder: number
}>>
```

### HOOK: useViewTableQuery

Get a single viewTable by id.

```
TYPE: query
USAGE: useViewTableQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  viewId: string
  tableId: string
  joinOrder: number
}>
```

### HOOK: useCreateViewTableMutation

Create a new viewTable.

```
TYPE: mutation
USAGE: const { mutate } = useCreateViewTableMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateViewTableMutation

Update an existing viewTable.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateViewTableMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteViewTableMutation

Delete a viewTable.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteViewTableMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useViewGrantsQuery

List all viewGrants.

```
TYPE: query
USAGE: useViewGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  viewId: string
  roleName: string
  privilege: string
  withGrantOption: boolean
}>>
```

### HOOK: useViewGrantQuery

Get a single viewGrant by id.

```
TYPE: query
USAGE: useViewGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  viewId: string
  roleName: string
  privilege: string
  withGrantOption: boolean
}>
```

### HOOK: useCreateViewGrantMutation

Create a new viewGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateViewGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateViewGrantMutation

Update an existing viewGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateViewGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteViewGrantMutation

Delete a viewGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteViewGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useViewRulesQuery

List all viewRules.

```
TYPE: query
USAGE: useViewRulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  viewId: string
  name: string
  event: string
  action: string
}>>
```

### HOOK: useViewRuleQuery

Get a single viewRule by id.

```
TYPE: query
USAGE: useViewRuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  viewId: string
  name: string
  event: string
  action: string
}>
```

### HOOK: useCreateViewRuleMutation

Create a new viewRule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateViewRuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateViewRuleMutation

Update an existing viewRule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateViewRuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteViewRuleMutation

Delete a viewRule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteViewRuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useTableModulesQuery

List all tableModules.

```
TYPE: query
USAGE: useTableModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  privateSchemaId: string
  tableId: string
  nodeType: string
  data: unknown
  fields: string[]
}>>
```

### HOOK: useTableModuleQuery

Get a single tableModule by id.

```
TYPE: query
USAGE: useTableModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  privateSchemaId: string
  tableId: string
  nodeType: string
  data: unknown
  fields: string[]
}>
```

### HOOK: useCreateTableModuleMutation

Create a new tableModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateTableModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateTableModuleMutation

Update an existing tableModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateTableModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteTableModuleMutation

Delete a tableModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteTableModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useTableTemplateModulesQuery

List all tableTemplateModules.

```
TYPE: query
USAGE: useTableTemplateModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  nodeType: string
  data: unknown
}>>
```

### HOOK: useTableTemplateModuleQuery

Get a single tableTemplateModule by id.

```
TYPE: query
USAGE: useTableTemplateModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  nodeType: string
  data: unknown
}>
```

### HOOK: useCreateTableTemplateModuleMutation

Create a new tableTemplateModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateTableTemplateModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateTableTemplateModuleMutation

Update an existing tableTemplateModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateTableTemplateModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteTableTemplateModuleMutation

Delete a tableTemplateModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteTableTemplateModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSchemaGrantsQuery

List all schemaGrants.

```
TYPE: query
USAGE: useSchemaGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  granteeName: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useSchemaGrantQuery

Get a single schemaGrant by id.

```
TYPE: query
USAGE: useSchemaGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  granteeName: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateSchemaGrantMutation

Create a new schemaGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSchemaGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSchemaGrantMutation

Update an existing schemaGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSchemaGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSchemaGrantMutation

Delete a schemaGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSchemaGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useApiSchemasQuery

List all apiSchemas.

```
TYPE: query
USAGE: useApiSchemasQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  apiId: string
}>>
```

### HOOK: useApiSchemaQuery

Get a single apiSchema by id.

```
TYPE: query
USAGE: useApiSchemaQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  apiId: string
}>
```

### HOOK: useCreateApiSchemaMutation

Create a new apiSchema.

```
TYPE: mutation
USAGE: const { mutate } = useCreateApiSchemaMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateApiSchemaMutation

Update an existing apiSchema.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateApiSchemaMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteApiSchemaMutation

Delete a apiSchema.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteApiSchemaMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useApiModulesQuery

List all apiModules.

```
TYPE: query
USAGE: useApiModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  apiId: string
  name: string
  data: unknown
}>>
```

### HOOK: useApiModuleQuery

Get a single apiModule by id.

```
TYPE: query
USAGE: useApiModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  apiId: string
  name: string
  data: unknown
}>
```

### HOOK: useCreateApiModuleMutation

Create a new apiModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateApiModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateApiModuleMutation

Update an existing apiModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateApiModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteApiModuleMutation

Delete a apiModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteApiModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useDomainsQuery

List all domains.

```
TYPE: query
USAGE: useDomainsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  apiId: string
  siteId: string
  subdomain: ConstructiveInternalTypeHostname
  domain: ConstructiveInternalTypeHostname
}>>
```

### HOOK: useDomainQuery

Get a single domain by id.

```
TYPE: query
USAGE: useDomainQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  apiId: string
  siteId: string
  subdomain: ConstructiveInternalTypeHostname
  domain: ConstructiveInternalTypeHostname
}>
```

### HOOK: useCreateDomainMutation

Create a new domain.

```
TYPE: mutation
USAGE: const { mutate } = useCreateDomainMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateDomainMutation

Update an existing domain.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateDomainMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteDomainMutation

Delete a domain.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteDomainMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSiteMetadataQuery

List all siteMetadata.

```
TYPE: query
USAGE: useSiteMetadataQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  siteId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage
}>>
```

### HOOK: useSiteMetadatumQuery

Get a single siteMetadatum by id.

```
TYPE: query
USAGE: useSiteMetadatumQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  siteId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage
}>
```

### HOOK: useCreateSiteMetadatumMutation

Create a new siteMetadatum.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSiteMetadatumMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSiteMetadatumMutation

Update an existing siteMetadatum.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSiteMetadatumMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSiteMetadatumMutation

Delete a siteMetadatum.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSiteMetadatumMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSiteModulesQuery

List all siteModules.

```
TYPE: query
USAGE: useSiteModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  siteId: string
  name: string
  data: unknown
}>>
```

### HOOK: useSiteModuleQuery

Get a single siteModule by id.

```
TYPE: query
USAGE: useSiteModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  siteId: string
  name: string
  data: unknown
}>
```

### HOOK: useCreateSiteModuleMutation

Create a new siteModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSiteModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSiteModuleMutation

Update an existing siteModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSiteModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSiteModuleMutation

Delete a siteModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSiteModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSiteThemesQuery

List all siteThemes.

```
TYPE: query
USAGE: useSiteThemesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  siteId: string
  theme: unknown
}>>
```

### HOOK: useSiteThemeQuery

Get a single siteTheme by id.

```
TYPE: query
USAGE: useSiteThemeQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  siteId: string
  theme: unknown
}>
```

### HOOK: useCreateSiteThemeMutation

Create a new siteTheme.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSiteThemeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSiteThemeMutation

Update an existing siteTheme.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSiteThemeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSiteThemeMutation

Delete a siteTheme.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSiteThemeMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useProceduresQuery

List all procedures.

```
TYPE: query
USAGE: useProceduresQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  name: string
  argnames: string[]
  argtypes: string[]
  argdefaults: string[]
  langName: string
  definition: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useProcedureQuery

Get a single procedure by id.

```
TYPE: query
USAGE: useProcedureQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  name: string
  argnames: string[]
  argtypes: string[]
  argdefaults: string[]
  langName: string
  definition: string
  smartTags: unknown
  category: ObjectCategory
  module: string
  scope: number
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateProcedureMutation

Create a new procedure.

```
TYPE: mutation
USAGE: const { mutate } = useCreateProcedureMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateProcedureMutation

Update an existing procedure.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateProcedureMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteProcedureMutation

Delete a procedure.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteProcedureMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useTriggerFunctionsQuery

List all triggerFunctions.

```
TYPE: query
USAGE: useTriggerFunctionsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  name: string
  code: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useTriggerFunctionQuery

Get a single triggerFunction by id.

```
TYPE: query
USAGE: useTriggerFunctionQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  name: string
  code: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateTriggerFunctionMutation

Create a new triggerFunction.

```
TYPE: mutation
USAGE: const { mutate } = useCreateTriggerFunctionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateTriggerFunctionMutation

Update an existing triggerFunction.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateTriggerFunctionMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteTriggerFunctionMutation

Delete a triggerFunction.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteTriggerFunctionMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useApisQuery

List all apis.

```
TYPE: query
USAGE: useApisQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  name: string
  dbname: string
  roleName: string
  anonRole: string
  isPublic: boolean
}>>
```

### HOOK: useApiQuery

Get a single api by id.

```
TYPE: query
USAGE: useApiQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  name: string
  dbname: string
  roleName: string
  anonRole: string
  isPublic: boolean
}>
```

### HOOK: useCreateApiMutation

Create a new api.

```
TYPE: mutation
USAGE: const { mutate } = useCreateApiMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateApiMutation

Update an existing api.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateApiMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteApiMutation

Delete a api.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteApiMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSitesQuery

List all sites.

```
TYPE: query
USAGE: useSitesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage
  favicon: ConstructiveInternalTypeAttachment
  appleTouchIcon: ConstructiveInternalTypeImage
  logo: ConstructiveInternalTypeImage
  dbname: string
}>>
```

### HOOK: useSiteQuery

Get a single site by id.

```
TYPE: query
USAGE: useSiteQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  title: string
  description: string
  ogImage: ConstructiveInternalTypeImage
  favicon: ConstructiveInternalTypeAttachment
  appleTouchIcon: ConstructiveInternalTypeImage
  logo: ConstructiveInternalTypeImage
  dbname: string
}>
```

### HOOK: useCreateSiteMutation

Create a new site.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSiteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSiteMutation

Update an existing site.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSiteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSiteMutation

Delete a site.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSiteMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppsQuery

List all apps.

```
TYPE: query
USAGE: useAppsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  siteId: string
  name: string
  appImage: ConstructiveInternalTypeImage
  appStoreLink: ConstructiveInternalTypeUrl
  appStoreId: string
  appIdPrefix: string
  playStoreLink: ConstructiveInternalTypeUrl
}>>
```

### HOOK: useAppQuery

Get a single app by id.

```
TYPE: query
USAGE: useAppQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  siteId: string
  name: string
  appImage: ConstructiveInternalTypeImage
  appStoreLink: ConstructiveInternalTypeUrl
  appStoreId: string
  appIdPrefix: string
  playStoreLink: ConstructiveInternalTypeUrl
}>
```

### HOOK: useCreateAppMutation

Create a new app.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppMutation

Update an existing app.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppMutation

Delete a app.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useConnectedAccountsModulesQuery

List all connectedAccountsModules.

```
TYPE: query
USAGE: useConnectedAccountsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
}>>
```

### HOOK: useConnectedAccountsModuleQuery

Get a single connectedAccountsModule by id.

```
TYPE: query
USAGE: useConnectedAccountsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
}>
```

### HOOK: useCreateConnectedAccountsModuleMutation

Create a new connectedAccountsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateConnectedAccountsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateConnectedAccountsModuleMutation

Update an existing connectedAccountsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateConnectedAccountsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteConnectedAccountsModuleMutation

Delete a connectedAccountsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteConnectedAccountsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useCryptoAddressesModulesQuery

List all cryptoAddressesModules.

```
TYPE: query
USAGE: useCryptoAddressesModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  cryptoNetwork: string
}>>
```

### HOOK: useCryptoAddressesModuleQuery

Get a single cryptoAddressesModule by id.

```
TYPE: query
USAGE: useCryptoAddressesModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
  cryptoNetwork: string
}>
```

### HOOK: useCreateCryptoAddressesModuleMutation

Create a new cryptoAddressesModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateCryptoAddressesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateCryptoAddressesModuleMutation

Update an existing cryptoAddressesModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateCryptoAddressesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteCryptoAddressesModuleMutation

Delete a cryptoAddressesModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteCryptoAddressesModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useCryptoAuthModulesQuery

List all cryptoAuthModules.

```
TYPE: query
USAGE: useCryptoAuthModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  usersTableId: string
  secretsTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  addressesTableId: string
  userField: string
  cryptoNetwork: string
  signInRequestChallenge: string
  signInRecordFailure: string
  signUpWithKey: string
  signInWithChallenge: string
}>>
```

### HOOK: useCryptoAuthModuleQuery

Get a single cryptoAuthModule by id.

```
TYPE: query
USAGE: useCryptoAuthModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  usersTableId: string
  secretsTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  addressesTableId: string
  userField: string
  cryptoNetwork: string
  signInRequestChallenge: string
  signInRecordFailure: string
  signUpWithKey: string
  signInWithChallenge: string
}>
```

### HOOK: useCreateCryptoAuthModuleMutation

Create a new cryptoAuthModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateCryptoAuthModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateCryptoAuthModuleMutation

Update an existing cryptoAuthModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateCryptoAuthModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteCryptoAuthModuleMutation

Delete a cryptoAuthModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteCryptoAuthModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useDefaultIdsModulesQuery

List all defaultIdsModules.

```
TYPE: query
USAGE: useDefaultIdsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
}>>
```

### HOOK: useDefaultIdsModuleQuery

Get a single defaultIdsModule by id.

```
TYPE: query
USAGE: useDefaultIdsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
}>
```

### HOOK: useCreateDefaultIdsModuleMutation

Create a new defaultIdsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateDefaultIdsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateDefaultIdsModuleMutation

Update an existing defaultIdsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateDefaultIdsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteDefaultIdsModuleMutation

Delete a defaultIdsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteDefaultIdsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useDenormalizedTableFieldsQuery

List all denormalizedTableFields.

```
TYPE: query
USAGE: useDenormalizedTableFieldsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  tableId: string
  fieldId: string
  setIds: string[]
  refTableId: string
  refFieldId: string
  refIds: string[]
  useUpdates: boolean
  updateDefaults: boolean
  funcName: string
  funcOrder: number
}>>
```

### HOOK: useDenormalizedTableFieldQuery

Get a single denormalizedTableField by id.

```
TYPE: query
USAGE: useDenormalizedTableFieldQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  tableId: string
  fieldId: string
  setIds: string[]
  refTableId: string
  refFieldId: string
  refIds: string[]
  useUpdates: boolean
  updateDefaults: boolean
  funcName: string
  funcOrder: number
}>
```

### HOOK: useCreateDenormalizedTableFieldMutation

Create a new denormalizedTableField.

```
TYPE: mutation
USAGE: const { mutate } = useCreateDenormalizedTableFieldMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateDenormalizedTableFieldMutation

Update an existing denormalizedTableField.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateDenormalizedTableFieldMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteDenormalizedTableFieldMutation

Delete a denormalizedTableField.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteDenormalizedTableFieldMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useEmailsModulesQuery

List all emailsModules.

```
TYPE: query
USAGE: useEmailsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
}>>
```

### HOOK: useEmailsModuleQuery

Get a single emailsModule by id.

```
TYPE: query
USAGE: useEmailsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
}>
```

### HOOK: useCreateEmailsModuleMutation

Create a new emailsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateEmailsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateEmailsModuleMutation

Update an existing emailsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateEmailsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteEmailsModuleMutation

Delete a emailsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteEmailsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useEncryptedSecretsModulesQuery

List all encryptedSecretsModules.

```
TYPE: query
USAGE: useEncryptedSecretsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
}>>
```

### HOOK: useEncryptedSecretsModuleQuery

Get a single encryptedSecretsModule by id.

```
TYPE: query
USAGE: useEncryptedSecretsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
}>
```

### HOOK: useCreateEncryptedSecretsModuleMutation

Create a new encryptedSecretsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateEncryptedSecretsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateEncryptedSecretsModuleMutation

Update an existing encryptedSecretsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateEncryptedSecretsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteEncryptedSecretsModuleMutation

Delete a encryptedSecretsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteEncryptedSecretsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useFieldModulesQuery

List all fieldModules.

```
TYPE: query
USAGE: useFieldModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  privateSchemaId: string
  tableId: string
  fieldId: string
  nodeType: string
  data: unknown
  triggers: string[]
  functions: string[]
}>>
```

### HOOK: useFieldModuleQuery

Get a single fieldModule by id.

```
TYPE: query
USAGE: useFieldModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  privateSchemaId: string
  tableId: string
  fieldId: string
  nodeType: string
  data: unknown
  triggers: string[]
  functions: string[]
}>
```

### HOOK: useCreateFieldModuleMutation

Create a new fieldModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateFieldModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateFieldModuleMutation

Update an existing fieldModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateFieldModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteFieldModuleMutation

Delete a fieldModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteFieldModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useInvitesModulesQuery

List all invitesModules.

```
TYPE: query
USAGE: useInvitesModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  emailsTableId: string
  usersTableId: string
  invitesTableId: string
  claimedInvitesTableId: string
  invitesTableName: string
  claimedInvitesTableName: string
  submitInviteCodeFunction: string
  prefix: string
  membershipType: number
  entityTableId: string
}>>
```

### HOOK: useInvitesModuleQuery

Get a single invitesModule by id.

```
TYPE: query
USAGE: useInvitesModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  emailsTableId: string
  usersTableId: string
  invitesTableId: string
  claimedInvitesTableId: string
  invitesTableName: string
  claimedInvitesTableName: string
  submitInviteCodeFunction: string
  prefix: string
  membershipType: number
  entityTableId: string
}>
```

### HOOK: useCreateInvitesModuleMutation

Create a new invitesModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateInvitesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateInvitesModuleMutation

Update an existing invitesModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateInvitesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteInvitesModuleMutation

Delete a invitesModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteInvitesModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useLevelsModulesQuery

List all levelsModules.

```
TYPE: query
USAGE: useLevelsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  stepsTableId: string
  stepsTableName: string
  achievementsTableId: string
  achievementsTableName: string
  levelsTableId: string
  levelsTableName: string
  levelRequirementsTableId: string
  levelRequirementsTableName: string
  completedStep: string
  incompletedStep: string
  tgAchievement: string
  tgAchievementToggle: string
  tgAchievementToggleBoolean: string
  tgAchievementBoolean: string
  upsertAchievement: string
  tgUpdateAchievements: string
  stepsRequired: string
  levelAchieved: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string
}>>
```

### HOOK: useLevelsModuleQuery

Get a single levelsModule by id.

```
TYPE: query
USAGE: useLevelsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  stepsTableId: string
  stepsTableName: string
  achievementsTableId: string
  achievementsTableName: string
  levelsTableId: string
  levelsTableName: string
  levelRequirementsTableId: string
  levelRequirementsTableName: string
  completedStep: string
  incompletedStep: string
  tgAchievement: string
  tgAchievementToggle: string
  tgAchievementToggleBoolean: string
  tgAchievementBoolean: string
  upsertAchievement: string
  tgUpdateAchievements: string
  stepsRequired: string
  levelAchieved: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string
}>
```

### HOOK: useCreateLevelsModuleMutation

Create a new levelsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateLevelsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateLevelsModuleMutation

Update an existing levelsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateLevelsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteLevelsModuleMutation

Delete a levelsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteLevelsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useLimitsModulesQuery

List all limitsModules.

```
TYPE: query
USAGE: useLimitsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  limitIncrementFunction: string
  limitDecrementFunction: string
  limitIncrementTrigger: string
  limitDecrementTrigger: string
  limitUpdateTrigger: string
  limitCheckFunction: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string
}>>
```

### HOOK: useLimitsModuleQuery

Get a single limitsModule by id.

```
TYPE: query
USAGE: useLimitsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  limitIncrementFunction: string
  limitDecrementFunction: string
  limitIncrementTrigger: string
  limitDecrementTrigger: string
  limitUpdateTrigger: string
  limitCheckFunction: string
  prefix: string
  membershipType: number
  entityTableId: string
  actorTableId: string
}>
```

### HOOK: useCreateLimitsModuleMutation

Create a new limitsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateLimitsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateLimitsModuleMutation

Update an existing limitsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateLimitsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteLimitsModuleMutation

Delete a limitsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteLimitsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useMembershipTypesModulesQuery

List all membershipTypesModules.

```
TYPE: query
USAGE: useMembershipTypesModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
}>>
```

### HOOK: useMembershipTypesModuleQuery

Get a single membershipTypesModule by id.

```
TYPE: query
USAGE: useMembershipTypesModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
}>
```

### HOOK: useCreateMembershipTypesModuleMutation

Create a new membershipTypesModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateMembershipTypesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateMembershipTypesModuleMutation

Update an existing membershipTypesModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateMembershipTypesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteMembershipTypesModuleMutation

Delete a membershipTypesModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteMembershipTypesModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useMembershipsModulesQuery

List all membershipsModules.

```
TYPE: query
USAGE: useMembershipsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  membershipsTableId: string
  membershipsTableName: string
  membersTableId: string
  membersTableName: string
  membershipDefaultsTableId: string
  membershipDefaultsTableName: string
  grantsTableId: string
  grantsTableName: string
  actorTableId: string
  limitsTableId: string
  defaultLimitsTableId: string
  permissionsTableId: string
  defaultPermissionsTableId: string
  sprtTableId: string
  adminGrantsTableId: string
  adminGrantsTableName: string
  ownerGrantsTableId: string
  ownerGrantsTableName: string
  membershipType: number
  entityTableId: string
  entityTableOwnerId: string
  prefix: string
  actorMaskCheck: string
  actorPermCheck: string
  entityIdsByMask: string
  entityIdsByPerm: string
  entityIdsFunction: string
}>>
```

### HOOK: useMembershipsModuleQuery

Get a single membershipsModule by id.

```
TYPE: query
USAGE: useMembershipsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  membershipsTableId: string
  membershipsTableName: string
  membersTableId: string
  membersTableName: string
  membershipDefaultsTableId: string
  membershipDefaultsTableName: string
  grantsTableId: string
  grantsTableName: string
  actorTableId: string
  limitsTableId: string
  defaultLimitsTableId: string
  permissionsTableId: string
  defaultPermissionsTableId: string
  sprtTableId: string
  adminGrantsTableId: string
  adminGrantsTableName: string
  ownerGrantsTableId: string
  ownerGrantsTableName: string
  membershipType: number
  entityTableId: string
  entityTableOwnerId: string
  prefix: string
  actorMaskCheck: string
  actorPermCheck: string
  entityIdsByMask: string
  entityIdsByPerm: string
  entityIdsFunction: string
}>
```

### HOOK: useCreateMembershipsModuleMutation

Create a new membershipsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateMembershipsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateMembershipsModuleMutation

Update an existing membershipsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateMembershipsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteMembershipsModuleMutation

Delete a membershipsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteMembershipsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: usePermissionsModulesQuery

List all permissionsModules.

```
TYPE: query
USAGE: usePermissionsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  prefix: string
  getPaddedMask: string
  getMask: string
  getByMask: string
  getMaskByName: string
}>>
```

### HOOK: usePermissionsModuleQuery

Get a single permissionsModule by id.

```
TYPE: query
USAGE: usePermissionsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  defaultTableId: string
  defaultTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  prefix: string
  getPaddedMask: string
  getMask: string
  getByMask: string
  getMaskByName: string
}>
```

### HOOK: useCreatePermissionsModuleMutation

Create a new permissionsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreatePermissionsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdatePermissionsModuleMutation

Update an existing permissionsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdatePermissionsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeletePermissionsModuleMutation

Delete a permissionsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeletePermissionsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: usePhoneNumbersModulesQuery

List all phoneNumbersModules.

```
TYPE: query
USAGE: usePhoneNumbersModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
}>>
```

### HOOK: usePhoneNumbersModuleQuery

Get a single phoneNumbersModule by id.

```
TYPE: query
USAGE: usePhoneNumbersModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  ownerTableId: string
  tableName: string
}>
```

### HOOK: useCreatePhoneNumbersModuleMutation

Create a new phoneNumbersModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreatePhoneNumbersModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdatePhoneNumbersModuleMutation

Update an existing phoneNumbersModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdatePhoneNumbersModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeletePhoneNumbersModuleMutation

Delete a phoneNumbersModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeletePhoneNumbersModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useProfilesModulesQuery

List all profilesModules.

```
TYPE: query
USAGE: useProfilesModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  profilePermissionsTableId: string
  profilePermissionsTableName: string
  profileGrantsTableId: string
  profileGrantsTableName: string
  profileDefinitionGrantsTableId: string
  profileDefinitionGrantsTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  permissionsTableId: string
  membershipsTableId: string
  prefix: string
}>>
```

### HOOK: useProfilesModuleQuery

Get a single profilesModule by id.

```
TYPE: query
USAGE: useProfilesModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  tableId: string
  tableName: string
  profilePermissionsTableId: string
  profilePermissionsTableName: string
  profileGrantsTableId: string
  profileGrantsTableName: string
  profileDefinitionGrantsTableId: string
  profileDefinitionGrantsTableName: string
  bitlen: number
  membershipType: number
  entityTableId: string
  actorTableId: string
  permissionsTableId: string
  membershipsTableId: string
  prefix: string
}>
```

### HOOK: useCreateProfilesModuleMutation

Create a new profilesModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateProfilesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateProfilesModuleMutation

Update an existing profilesModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateProfilesModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteProfilesModuleMutation

Delete a profilesModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteProfilesModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useRlsModulesQuery

List all rlsModules.

```
TYPE: query
USAGE: useRlsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  apiId: string
  schemaId: string
  privateSchemaId: string
  sessionCredentialsTableId: string
  sessionsTableId: string
  usersTableId: string
  authenticate: string
  authenticateStrict: string
  currentRole: string
  currentRoleId: string
}>>
```

### HOOK: useRlsModuleQuery

Get a single rlsModule by id.

```
TYPE: query
USAGE: useRlsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  apiId: string
  schemaId: string
  privateSchemaId: string
  sessionCredentialsTableId: string
  sessionsTableId: string
  usersTableId: string
  authenticate: string
  authenticateStrict: string
  currentRole: string
  currentRoleId: string
}>
```

### HOOK: useCreateRlsModuleMutation

Create a new rlsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateRlsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateRlsModuleMutation

Update an existing rlsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateRlsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteRlsModuleMutation

Delete a rlsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteRlsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSecretsModulesQuery

List all secretsModules.

```
TYPE: query
USAGE: useSecretsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
}>>
```

### HOOK: useSecretsModuleQuery

Get a single secretsModule by id.

```
TYPE: query
USAGE: useSecretsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
}>
```

### HOOK: useCreateSecretsModuleMutation

Create a new secretsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSecretsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSecretsModuleMutation

Update an existing secretsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSecretsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSecretsModuleMutation

Delete a secretsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSecretsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSessionsModulesQuery

List all sessionsModules.

```
TYPE: query
USAGE: useSessionsModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  authSettingsTableId: string
  usersTableId: string
  sessionsDefaultExpiration: string
  sessionsTable: string
  sessionCredentialsTable: string
  authSettingsTable: string
}>>
```

### HOOK: useSessionsModuleQuery

Get a single sessionsModule by id.

```
TYPE: query
USAGE: useSessionsModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  authSettingsTableId: string
  usersTableId: string
  sessionsDefaultExpiration: string
  sessionsTable: string
  sessionCredentialsTable: string
  authSettingsTable: string
}>
```

### HOOK: useCreateSessionsModuleMutation

Create a new sessionsModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSessionsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSessionsModuleMutation

Update an existing sessionsModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSessionsModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSessionsModuleMutation

Delete a sessionsModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSessionsModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useUserAuthModulesQuery

List all userAuthModules.

```
TYPE: query
USAGE: useUserAuthModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  emailsTableId: string
  usersTableId: string
  secretsTableId: string
  encryptedTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  auditsTableId: string
  auditsTableName: string
  signInFunction: string
  signUpFunction: string
  signOutFunction: string
  setPasswordFunction: string
  resetPasswordFunction: string
  forgotPasswordFunction: string
  sendVerificationEmailFunction: string
  verifyEmailFunction: string
  verifyPasswordFunction: string
  checkPasswordFunction: string
  sendAccountDeletionEmailFunction: string
  deleteAccountFunction: string
  signInOneTimeTokenFunction: string
  oneTimeTokenFunction: string
  extendTokenExpires: string
}>>
```

### HOOK: useUserAuthModuleQuery

Get a single userAuthModule by id.

```
TYPE: query
USAGE: useUserAuthModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  emailsTableId: string
  usersTableId: string
  secretsTableId: string
  encryptedTableId: string
  sessionsTableId: string
  sessionCredentialsTableId: string
  auditsTableId: string
  auditsTableName: string
  signInFunction: string
  signUpFunction: string
  signOutFunction: string
  setPasswordFunction: string
  resetPasswordFunction: string
  forgotPasswordFunction: string
  sendVerificationEmailFunction: string
  verifyEmailFunction: string
  verifyPasswordFunction: string
  checkPasswordFunction: string
  sendAccountDeletionEmailFunction: string
  deleteAccountFunction: string
  signInOneTimeTokenFunction: string
  oneTimeTokenFunction: string
  extendTokenExpires: string
}>
```

### HOOK: useCreateUserAuthModuleMutation

Create a new userAuthModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateUserAuthModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateUserAuthModuleMutation

Update an existing userAuthModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateUserAuthModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteUserAuthModuleMutation

Delete a userAuthModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteUserAuthModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useUsersModulesQuery

List all usersModules.

```
TYPE: query
USAGE: useUsersModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
  typeTableId: string
  typeTableName: string
}>>
```

### HOOK: useUsersModuleQuery

Get a single usersModule by id.

```
TYPE: query
USAGE: useUsersModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  tableId: string
  tableName: string
  typeTableId: string
  typeTableName: string
}>
```

### HOOK: useCreateUsersModuleMutation

Create a new usersModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateUsersModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateUsersModuleMutation

Update an existing usersModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateUsersModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteUsersModuleMutation

Delete a usersModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteUsersModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useUuidModulesQuery

List all uuidModules.

```
TYPE: query
USAGE: useUuidModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  uuidFunction: string
  uuidSeed: string
}>>
```

### HOOK: useUuidModuleQuery

Get a single uuidModule by id.

```
TYPE: query
USAGE: useUuidModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  uuidFunction: string
  uuidSeed: string
}>
```

### HOOK: useCreateUuidModuleMutation

Create a new uuidModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateUuidModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateUuidModuleMutation

Update an existing uuidModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateUuidModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteUuidModuleMutation

Delete a uuidModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteUuidModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useDatabaseProvisionModulesQuery

List all databaseProvisionModules.

```
TYPE: query
USAGE: useDatabaseProvisionModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseName: string
  ownerId: string
  subdomain: string
  domain: string
  modules: string[]
  options: unknown
  bootstrapUser: boolean
  status: string
  errorMessage: string
  databaseId: string
  createdAt: string
  updatedAt: string
  completedAt: string
}>>
```

### HOOK: useDatabaseProvisionModuleQuery

Get a single databaseProvisionModule by id.

```
TYPE: query
USAGE: useDatabaseProvisionModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseName: string
  ownerId: string
  subdomain: string
  domain: string
  modules: string[]
  options: unknown
  bootstrapUser: boolean
  status: string
  errorMessage: string
  databaseId: string
  createdAt: string
  updatedAt: string
  completedAt: string
}>
```

### HOOK: useCreateDatabaseProvisionModuleMutation

Create a new databaseProvisionModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateDatabaseProvisionModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateDatabaseProvisionModuleMutation

Update an existing databaseProvisionModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateDatabaseProvisionModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteDatabaseProvisionModuleMutation

Delete a databaseProvisionModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteDatabaseProvisionModuleMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppAdminGrantsQuery

List all appAdminGrants.

```
TYPE: query
USAGE: useAppAdminGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppAdminGrantQuery

Get a single appAdminGrant by id.

```
TYPE: query
USAGE: useAppAdminGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppAdminGrantMutation

Create a new appAdminGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppAdminGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppAdminGrantMutation

Update an existing appAdminGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppAdminGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppAdminGrantMutation

Delete a appAdminGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppAdminGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppOwnerGrantsQuery

List all appOwnerGrants.

```
TYPE: query
USAGE: useAppOwnerGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppOwnerGrantQuery

Get a single appOwnerGrant by id.

```
TYPE: query
USAGE: useAppOwnerGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppOwnerGrantMutation

Create a new appOwnerGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppOwnerGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppOwnerGrantMutation

Update an existing appOwnerGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppOwnerGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppOwnerGrantMutation

Delete a appOwnerGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppOwnerGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppGrantsQuery

List all appGrants.

```
TYPE: query
USAGE: useAppGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  permissions: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppGrantQuery

Get a single appGrant by id.

```
TYPE: query
USAGE: useAppGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  permissions: string
  isGrant: boolean
  actorId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppGrantMutation

Create a new appGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppGrantMutation

Update an existing appGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppGrantMutation

Delete a appGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgMembershipsQuery

List all orgMemberships.

```
TYPE: query
USAGE: useOrgMembershipsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
  entityId: string
}>>
```

### HOOK: useOrgMembershipQuery

Get a single orgMembership by id.

```
TYPE: query
USAGE: useOrgMembershipQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
  entityId: string
}>
```

### HOOK: useCreateOrgMembershipMutation

Create a new orgMembership.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgMembershipMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgMembershipMutation

Update an existing orgMembership.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgMembershipMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgMembershipMutation

Delete a orgMembership.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgMembershipMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgMembersQuery

List all orgMembers.

```
TYPE: query
USAGE: useOrgMembersQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  isAdmin: boolean
  actorId: string
  entityId: string
}>>
```

### HOOK: useOrgMemberQuery

Get a single orgMember by id.

```
TYPE: query
USAGE: useOrgMemberQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  isAdmin: boolean
  actorId: string
  entityId: string
}>
```

### HOOK: useCreateOrgMemberMutation

Create a new orgMember.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgMemberMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgMemberMutation

Update an existing orgMember.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgMemberMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgMemberMutation

Delete a orgMember.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgMemberMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgAdminGrantsQuery

List all orgAdminGrants.

```
TYPE: query
USAGE: useOrgAdminGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useOrgAdminGrantQuery

Get a single orgAdminGrant by id.

```
TYPE: query
USAGE: useOrgAdminGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateOrgAdminGrantMutation

Create a new orgAdminGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgAdminGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgAdminGrantMutation

Update an existing orgAdminGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgAdminGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgAdminGrantMutation

Delete a orgAdminGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgAdminGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgOwnerGrantsQuery

List all orgOwnerGrants.

```
TYPE: query
USAGE: useOrgOwnerGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useOrgOwnerGrantQuery

Get a single orgOwnerGrant by id.

```
TYPE: query
USAGE: useOrgOwnerGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateOrgOwnerGrantMutation

Create a new orgOwnerGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgOwnerGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgOwnerGrantMutation

Update an existing orgOwnerGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgOwnerGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgOwnerGrantMutation

Delete a orgOwnerGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgOwnerGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgGrantsQuery

List all orgGrants.

```
TYPE: query
USAGE: useOrgGrantsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  permissions: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useOrgGrantQuery

Get a single orgGrant by id.

```
TYPE: query
USAGE: useOrgGrantQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  permissions: string
  isGrant: boolean
  actorId: string
  entityId: string
  grantorId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateOrgGrantMutation

Create a new orgGrant.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgGrantMutation

Update an existing orgGrant.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgGrantMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgGrantMutation

Delete a orgGrant.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgGrantMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppLimitsQuery

List all appLimits.

```
TYPE: query
USAGE: useAppLimitsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  actorId: string
  num: number
  max: number
}>>
```

### HOOK: useAppLimitQuery

Get a single appLimit by id.

```
TYPE: query
USAGE: useAppLimitQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  actorId: string
  num: number
  max: number
}>
```

### HOOK: useCreateAppLimitMutation

Create a new appLimit.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppLimitMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppLimitMutation

Update an existing appLimit.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppLimitMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppLimitMutation

Delete a appLimit.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppLimitMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgLimitsQuery

List all orgLimits.

```
TYPE: query
USAGE: useOrgLimitsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  actorId: string
  num: number
  max: number
  entityId: string
}>>
```

### HOOK: useOrgLimitQuery

Get a single orgLimit by id.

```
TYPE: query
USAGE: useOrgLimitQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  actorId: string
  num: number
  max: number
  entityId: string
}>
```

### HOOK: useCreateOrgLimitMutation

Create a new orgLimit.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgLimitMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgLimitMutation

Update an existing orgLimit.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgLimitMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgLimitMutation

Delete a orgLimit.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgLimitMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppStepsQuery

List all appSteps.

```
TYPE: query
USAGE: useAppStepsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppStepQuery

Get a single appStep by id.

```
TYPE: query
USAGE: useAppStepQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppStepMutation

Create a new appStep.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppStepMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppStepMutation

Update an existing appStep.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppStepMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppStepMutation

Delete a appStep.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppStepMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppAchievementsQuery

List all appAchievements.

```
TYPE: query
USAGE: useAppAchievementsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppAchievementQuery

Get a single appAchievement by id.

```
TYPE: query
USAGE: useAppAchievementQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  actorId: string
  name: string
  count: number
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppAchievementMutation

Create a new appAchievement.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppAchievementMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppAchievementMutation

Update an existing appAchievement.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppAchievementMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppAchievementMutation

Delete a appAchievement.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppAchievementMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useInvitesQuery

List all invites.

```
TYPE: query
USAGE: useInvitesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  email: ConstructiveInternalTypeEmail
  senderId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useInviteQuery

Get a single invite by id.

```
TYPE: query
USAGE: useInviteQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  email: ConstructiveInternalTypeEmail
  senderId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateInviteMutation

Create a new invite.

```
TYPE: mutation
USAGE: const { mutate } = useCreateInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateInviteMutation

Update an existing invite.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteInviteMutation

Delete a invite.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteInviteMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useClaimedInvitesQuery

List all claimedInvites.

```
TYPE: query
USAGE: useClaimedInvitesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useClaimedInviteQuery

Get a single claimedInvite by id.

```
TYPE: query
USAGE: useClaimedInviteQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateClaimedInviteMutation

Create a new claimedInvite.

```
TYPE: mutation
USAGE: const { mutate } = useCreateClaimedInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateClaimedInviteMutation

Update an existing claimedInvite.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateClaimedInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteClaimedInviteMutation

Delete a claimedInvite.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteClaimedInviteMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgInvitesQuery

List all orgInvites.

```
TYPE: query
USAGE: useOrgInvitesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  email: ConstructiveInternalTypeEmail
  senderId: string
  receiverId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string
  entityId: string
}>>
```

### HOOK: useOrgInviteQuery

Get a single orgInvite by id.

```
TYPE: query
USAGE: useOrgInviteQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  email: ConstructiveInternalTypeEmail
  senderId: string
  receiverId: string
  inviteToken: string
  inviteValid: boolean
  inviteLimit: number
  inviteCount: number
  multiple: boolean
  data: unknown
  expiresAt: string
  createdAt: string
  updatedAt: string
  entityId: string
}>
```

### HOOK: useCreateOrgInviteMutation

Create a new orgInvite.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgInviteMutation

Update an existing orgInvite.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgInviteMutation

Delete a orgInvite.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgInviteMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgClaimedInvitesQuery

List all orgClaimedInvites.

```
TYPE: query
USAGE: useOrgClaimedInvitesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string
  entityId: string
}>>
```

### HOOK: useOrgClaimedInviteQuery

Get a single orgClaimedInvite by id.

```
TYPE: query
USAGE: useOrgClaimedInviteQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  data: unknown
  senderId: string
  receiverId: string
  createdAt: string
  updatedAt: string
  entityId: string
}>
```

### HOOK: useCreateOrgClaimedInviteMutation

Create a new orgClaimedInvite.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgClaimedInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgClaimedInviteMutation

Update an existing orgClaimedInvite.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgClaimedInviteMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgClaimedInviteMutation

Delete a orgClaimedInvite.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgClaimedInviteMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppPermissionDefaultsQuery

List all appPermissionDefaults.

```
TYPE: query
USAGE: useAppPermissionDefaultsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  permissions: string
}>>
```

### HOOK: useAppPermissionDefaultQuery

Get a single appPermissionDefault by id.

```
TYPE: query
USAGE: useAppPermissionDefaultQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  permissions: string
}>
```

### HOOK: useCreateAppPermissionDefaultMutation

Create a new appPermissionDefault.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppPermissionDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppPermissionDefaultMutation

Update an existing appPermissionDefault.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppPermissionDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppPermissionDefaultMutation

Delete a appPermissionDefault.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppPermissionDefaultMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useRefsQuery

List all refs.

```
TYPE: query
USAGE: useRefsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  databaseId: string
  storeId: string
  commitId: string
}>>
```

### HOOK: useRefQuery

Get a single ref by id.

```
TYPE: query
USAGE: useRefQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  databaseId: string
  storeId: string
  commitId: string
}>
```

### HOOK: useCreateRefMutation

Create a new ref.

```
TYPE: mutation
USAGE: const { mutate } = useCreateRefMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateRefMutation

Update an existing ref.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateRefMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteRefMutation

Delete a ref.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteRefMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useStoresQuery

List all stores.

```
TYPE: query
USAGE: useStoresQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  databaseId: string
  hash: string
  createdAt: string
}>>
```

### HOOK: useStoreQuery

Get a single store by id.

```
TYPE: query
USAGE: useStoreQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  databaseId: string
  hash: string
  createdAt: string
}>
```

### HOOK: useCreateStoreMutation

Create a new store.

```
TYPE: mutation
USAGE: const { mutate } = useCreateStoreMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateStoreMutation

Update an existing store.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateStoreMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteStoreMutation

Delete a store.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteStoreMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useRoleTypesQuery

List all roleTypes.

```
TYPE: query
USAGE: useRoleTypesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: number
  name: string
}>>
```

### HOOK: useRoleTypeQuery

Get a single roleType by id.

```
TYPE: query
USAGE: useRoleTypeQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: number (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: number
  name: string
}>
```

### HOOK: useCreateRoleTypeMutation

Create a new roleType.

```
TYPE: mutation
USAGE: const { mutate } = useCreateRoleTypeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateRoleTypeMutation

Update an existing roleType.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateRoleTypeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteRoleTypeMutation

Delete a roleType.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteRoleTypeMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgPermissionDefaultsQuery

List all orgPermissionDefaults.

```
TYPE: query
USAGE: useOrgPermissionDefaultsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  permissions: string
  entityId: string
}>>
```

### HOOK: useOrgPermissionDefaultQuery

Get a single orgPermissionDefault by id.

```
TYPE: query
USAGE: useOrgPermissionDefaultQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  permissions: string
  entityId: string
}>
```

### HOOK: useCreateOrgPermissionDefaultMutation

Create a new orgPermissionDefault.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgPermissionDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgPermissionDefaultMutation

Update an existing orgPermissionDefault.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgPermissionDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgPermissionDefaultMutation

Delete a orgPermissionDefault.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgPermissionDefaultMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppLimitDefaultsQuery

List all appLimitDefaults.

```
TYPE: query
USAGE: useAppLimitDefaultsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  max: number
}>>
```

### HOOK: useAppLimitDefaultQuery

Get a single appLimitDefault by id.

```
TYPE: query
USAGE: useAppLimitDefaultQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  max: number
}>
```

### HOOK: useCreateAppLimitDefaultMutation

Create a new appLimitDefault.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppLimitDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppLimitDefaultMutation

Update an existing appLimitDefault.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppLimitDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppLimitDefaultMutation

Delete a appLimitDefault.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppLimitDefaultMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgLimitDefaultsQuery

List all orgLimitDefaults.

```
TYPE: query
USAGE: useOrgLimitDefaultsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  max: number
}>>
```

### HOOK: useOrgLimitDefaultQuery

Get a single orgLimitDefault by id.

```
TYPE: query
USAGE: useOrgLimitDefaultQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  max: number
}>
```

### HOOK: useCreateOrgLimitDefaultMutation

Create a new orgLimitDefault.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgLimitDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgLimitDefaultMutation

Update an existing orgLimitDefault.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgLimitDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgLimitDefaultMutation

Delete a orgLimitDefault.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgLimitDefaultMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useCryptoAddressesQuery

List all cryptoAddresses.

```
TYPE: query
USAGE: useCryptoAddressesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  address: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useCryptoAddressQuery

Get a single cryptoAddress by id.

```
TYPE: query
USAGE: useCryptoAddressQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  address: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateCryptoAddressMutation

Create a new cryptoAddress.

```
TYPE: mutation
USAGE: const { mutate } = useCreateCryptoAddressMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateCryptoAddressMutation

Update an existing cryptoAddress.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateCryptoAddressMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteCryptoAddressMutation

Delete a cryptoAddress.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteCryptoAddressMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useMembershipTypesQuery

List all membershipTypes.

```
TYPE: query
USAGE: useMembershipTypesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: number
  name: string
  description: string
  prefix: string
}>>
```

### HOOK: useMembershipTypeQuery

Get a single membershipType by id.

```
TYPE: query
USAGE: useMembershipTypeQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: number (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: number
  name: string
  description: string
  prefix: string
}>
```

### HOOK: useCreateMembershipTypeMutation

Create a new membershipType.

```
TYPE: mutation
USAGE: const { mutate } = useCreateMembershipTypeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateMembershipTypeMutation

Update an existing membershipType.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateMembershipTypeMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteMembershipTypeMutation

Delete a membershipType.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteMembershipTypeMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useConnectedAccountsQuery

List all connectedAccounts.

```
TYPE: query
USAGE: useConnectedAccountsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  service: string
  identifier: string
  details: unknown
  isVerified: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useConnectedAccountQuery

Get a single connectedAccount by id.

```
TYPE: query
USAGE: useConnectedAccountQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  service: string
  identifier: string
  details: unknown
  isVerified: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateConnectedAccountMutation

Create a new connectedAccount.

```
TYPE: mutation
USAGE: const { mutate } = useCreateConnectedAccountMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateConnectedAccountMutation

Update an existing connectedAccount.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateConnectedAccountMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteConnectedAccountMutation

Delete a connectedAccount.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteConnectedAccountMutation({})

OUTPUT: UseMutationResult
```

### HOOK: usePhoneNumbersQuery

List all phoneNumbers.

```
TYPE: query
USAGE: usePhoneNumbersQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  cc: string
  number: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: usePhoneNumberQuery

Get a single phoneNumber by id.

```
TYPE: query
USAGE: usePhoneNumberQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  cc: string
  number: string
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreatePhoneNumberMutation

Create a new phoneNumber.

```
TYPE: mutation
USAGE: const { mutate } = useCreatePhoneNumberMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdatePhoneNumberMutation

Update an existing phoneNumber.

```
TYPE: mutation
USAGE: const { mutate } = useUpdatePhoneNumberMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeletePhoneNumberMutation

Delete a phoneNumber.

```
TYPE: mutation
USAGE: const { mutate } = useDeletePhoneNumberMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppMembershipDefaultsQuery

List all appMembershipDefaults.

```
TYPE: query
USAGE: useAppMembershipDefaultsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isVerified: boolean
}>>
```

### HOOK: useAppMembershipDefaultQuery

Get a single appMembershipDefault by id.

```
TYPE: query
USAGE: useAppMembershipDefaultQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isVerified: boolean
}>
```

### HOOK: useCreateAppMembershipDefaultMutation

Create a new appMembershipDefault.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppMembershipDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppMembershipDefaultMutation

Update an existing appMembershipDefault.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppMembershipDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppMembershipDefaultMutation

Delete a appMembershipDefault.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppMembershipDefaultMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useNodeTypeRegistriesQuery

List all nodeTypeRegistries.

```
TYPE: query
USAGE: useNodeTypeRegistriesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  name: string
  slug: string
  category: string
  displayName: string
  description: string
  parameterSchema: unknown
  tags: string[]
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useNodeTypeRegistryQuery

Get a single nodeTypeRegistry by name.

```
TYPE: query
USAGE: useNodeTypeRegistryQuery({ name: '<value>', selection: { fields: { ... } } })

INPUT:
  name: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  name: string
  slug: string
  category: string
  displayName: string
  description: string
  parameterSchema: unknown
  tags: string[]
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateNodeTypeRegistryMutation

Create a new nodeTypeRegistry.

```
TYPE: mutation
USAGE: const { mutate } = useCreateNodeTypeRegistryMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateNodeTypeRegistryMutation

Update an existing nodeTypeRegistry.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateNodeTypeRegistryMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteNodeTypeRegistryMutation

Delete a nodeTypeRegistry.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteNodeTypeRegistryMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useCommitsQuery

List all commits.

```
TYPE: query
USAGE: useCommitsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  message: string
  databaseId: string
  storeId: string
  parentIds: string[]
  authorId: string
  committerId: string
  treeId: string
  date: string
}>>
```

### HOOK: useCommitQuery

Get a single commit by id.

```
TYPE: query
USAGE: useCommitQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  message: string
  databaseId: string
  storeId: string
  parentIds: string[]
  authorId: string
  committerId: string
  treeId: string
  date: string
}>
```

### HOOK: useCreateCommitMutation

Create a new commit.

```
TYPE: mutation
USAGE: const { mutate } = useCreateCommitMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateCommitMutation

Update an existing commit.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateCommitMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteCommitMutation

Delete a commit.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteCommitMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useOrgMembershipDefaultsQuery

List all orgMembershipDefaults.

```
TYPE: query
USAGE: useOrgMembershipDefaultsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  entityId: string
  deleteMemberCascadeGroups: boolean
  createGroupsCascadeMembers: boolean
}>>
```

### HOOK: useOrgMembershipDefaultQuery

Get a single orgMembershipDefault by id.

```
TYPE: query
USAGE: useOrgMembershipDefaultQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  entityId: string
  deleteMemberCascadeGroups: boolean
  createGroupsCascadeMembers: boolean
}>
```

### HOOK: useCreateOrgMembershipDefaultMutation

Create a new orgMembershipDefault.

```
TYPE: mutation
USAGE: const { mutate } = useCreateOrgMembershipDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateOrgMembershipDefaultMutation

Update an existing orgMembershipDefault.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateOrgMembershipDefaultMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteOrgMembershipDefaultMutation

Delete a orgMembershipDefault.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteOrgMembershipDefaultMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useEmailsQuery

List all emails.

```
TYPE: query
USAGE: useEmailsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  ownerId: string
  email: ConstructiveInternalTypeEmail
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useEmailQuery

Get a single email by id.

```
TYPE: query
USAGE: useEmailQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  ownerId: string
  email: ConstructiveInternalTypeEmail
  isVerified: boolean
  isPrimary: boolean
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateEmailMutation

Create a new email.

```
TYPE: mutation
USAGE: const { mutate } = useCreateEmailMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateEmailMutation

Update an existing email.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateEmailMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteEmailMutation

Delete a email.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteEmailMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAuditLogsQuery

List all auditLogs.

```
TYPE: query
USAGE: useAuditLogsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  event: string
  actorId: string
  origin: ConstructiveInternalTypeOrigin
  userAgent: string
  ipAddress: string
  success: boolean
  createdAt: string
}>>
```

### HOOK: useAuditLogQuery

Get a single auditLog by id.

```
TYPE: query
USAGE: useAuditLogQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  event: string
  actorId: string
  origin: ConstructiveInternalTypeOrigin
  userAgent: string
  ipAddress: string
  success: boolean
  createdAt: string
}>
```

### HOOK: useCreateAuditLogMutation

Create a new auditLog.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAuditLogMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAuditLogMutation

Update an existing auditLog.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAuditLogMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAuditLogMutation

Delete a auditLog.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAuditLogMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppLevelsQuery

List all appLevels.

```
TYPE: query
USAGE: useAppLevelsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  name: string
  description: string
  image: ConstructiveInternalTypeImage
  ownerId: string
  createdAt: string
  updatedAt: string
}>>
```

### HOOK: useAppLevelQuery

Get a single appLevel by id.

```
TYPE: query
USAGE: useAppLevelQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  name: string
  description: string
  image: ConstructiveInternalTypeImage
  ownerId: string
  createdAt: string
  updatedAt: string
}>
```

### HOOK: useCreateAppLevelMutation

Create a new appLevel.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppLevelMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppLevelMutation

Update an existing appLevel.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppLevelMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppLevelMutation

Delete a appLevel.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppLevelMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useSqlMigrationsQuery

List all sqlMigrations.

```
TYPE: query
USAGE: useSqlMigrationsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: number
  name: string
  databaseId: string
  deploy: string
  deps: string[]
  payload: unknown
  content: string
  revert: string
  verify: string
  createdAt: string
  action: string
  actionId: string
  actorId: string
}>>
```

### HOOK: useSqlMigrationQuery

Get a single sqlMigration by id.

```
TYPE: query
USAGE: useSqlMigrationQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: number (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: number
  name: string
  databaseId: string
  deploy: string
  deps: string[]
  payload: unknown
  content: string
  revert: string
  verify: string
  createdAt: string
  action: string
  actionId: string
  actorId: string
}>
```

### HOOK: useCreateSqlMigrationMutation

Create a new sqlMigration.

```
TYPE: mutation
USAGE: const { mutate } = useCreateSqlMigrationMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateSqlMigrationMutation

Update an existing sqlMigration.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateSqlMigrationMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteSqlMigrationMutation

Delete a sqlMigration.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteSqlMigrationMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAstMigrationsQuery

List all astMigrations.

```
TYPE: query
USAGE: useAstMigrationsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: number
  databaseId: string
  name: string
  requires: string[]
  payload: unknown
  deploys: string
  deploy: unknown
  revert: unknown
  verify: unknown
  createdAt: string
  action: string
  actionId: string
  actorId: string
}>>
```

### HOOK: useAstMigrationQuery

Get a single astMigration by id.

```
TYPE: query
USAGE: useAstMigrationQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: number (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: number
  databaseId: string
  name: string
  requires: string[]
  payload: unknown
  deploys: string
  deploy: unknown
  revert: unknown
  verify: unknown
  createdAt: string
  action: string
  actionId: string
  actorId: string
}>
```

### HOOK: useCreateAstMigrationMutation

Create a new astMigration.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAstMigrationMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAstMigrationMutation

Update an existing astMigration.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAstMigrationMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAstMigrationMutation

Delete a astMigration.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAstMigrationMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useAppMembershipsQuery

List all appMemberships.

```
TYPE: query
USAGE: useAppMembershipsQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isVerified: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
}>>
```

### HOOK: useAppMembershipQuery

Get a single appMembership by id.

```
TYPE: query
USAGE: useAppMembershipQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  createdAt: string
  updatedAt: string
  createdBy: string
  updatedBy: string
  isApproved: boolean
  isBanned: boolean
  isDisabled: boolean
  isVerified: boolean
  isActive: boolean
  isOwner: boolean
  isAdmin: boolean
  permissions: string
  granted: string
  actorId: string
}>
```

### HOOK: useCreateAppMembershipMutation

Create a new appMembership.

```
TYPE: mutation
USAGE: const { mutate } = useCreateAppMembershipMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateAppMembershipMutation

Update an existing appMembership.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateAppMembershipMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteAppMembershipMutation

Delete a appMembership.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteAppMembershipMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useUsersQuery

List all users.

```
TYPE: query
USAGE: useUsersQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  username: string
  displayName: string
  profilePicture: ConstructiveInternalTypeImage
  searchTsv: string
  type: number
  createdAt: string
  updatedAt: string
  searchTsvRank: number
}>>
```

### HOOK: useUserQuery

Get a single user by id.

```
TYPE: query
USAGE: useUserQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  username: string
  displayName: string
  profilePicture: ConstructiveInternalTypeImage
  searchTsv: string
  type: number
  createdAt: string
  updatedAt: string
  searchTsvRank: number
}>
```

### HOOK: useCreateUserMutation

Create a new user.

```
TYPE: mutation
USAGE: const { mutate } = useCreateUserMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateUserMutation

Update an existing user.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateUserMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteUserMutation

Delete a user.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteUserMutation({})

OUTPUT: UseMutationResult
```

### HOOK: useHierarchyModulesQuery

List all hierarchyModules.

```
TYPE: query
USAGE: useHierarchyModulesQuery({ selection: { fields: { ... } } })

INPUT:
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<Array<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  chartEdgesTableId: string
  chartEdgesTableName: string
  hierarchySprtTableId: string
  hierarchySprtTableName: string
  chartEdgeGrantsTableId: string
  chartEdgeGrantsTableName: string
  entityTableId: string
  usersTableId: string
  prefix: string
  privateSchemaName: string
  sprtTableName: string
  rebuildHierarchyFunction: string
  getSubordinatesFunction: string
  getManagersFunction: string
  isManagerOfFunction: string
  createdAt: string
}>>
```

### HOOK: useHierarchyModuleQuery

Get a single hierarchyModule by id.

```
TYPE: query
USAGE: useHierarchyModuleQuery({ id: '<value>', selection: { fields: { ... } } })

INPUT:
  id: string (required)
  selection: { fields: Record<string, boolean> } - Fields to select

OUTPUT: UseQueryResult<{
  id: string
  databaseId: string
  schemaId: string
  privateSchemaId: string
  chartEdgesTableId: string
  chartEdgesTableName: string
  hierarchySprtTableId: string
  hierarchySprtTableName: string
  chartEdgeGrantsTableId: string
  chartEdgeGrantsTableName: string
  entityTableId: string
  usersTableId: string
  prefix: string
  privateSchemaName: string
  sprtTableName: string
  rebuildHierarchyFunction: string
  getSubordinatesFunction: string
  getManagersFunction: string
  isManagerOfFunction: string
  createdAt: string
}>
```

### HOOK: useCreateHierarchyModuleMutation

Create a new hierarchyModule.

```
TYPE: mutation
USAGE: const { mutate } = useCreateHierarchyModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useUpdateHierarchyModuleMutation

Update an existing hierarchyModule.

```
TYPE: mutation
USAGE: const { mutate } = useUpdateHierarchyModuleMutation({ selection: { fields: { ... } } })

OUTPUT: UseMutationResult
```

### HOOK: useDeleteHierarchyModuleMutation

Delete a hierarchyModule.

```
TYPE: mutation
USAGE: const { mutate } = useDeleteHierarchyModuleMutation({})

OUTPUT: UseMutationResult
```

## CUSTOM OPERATION HOOKS

### HOOK: useCurrentUserIdQuery

currentUserId

```
TYPE: query
USAGE: useCurrentUserIdQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useCurrentIpAddressQuery

currentIpAddress

```
TYPE: query
USAGE: useCurrentIpAddressQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useCurrentUserAgentQuery

currentUserAgent

```
TYPE: query
USAGE: useCurrentUserAgentQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useAppPermissionsGetPaddedMaskQuery

appPermissionsGetPaddedMask

```
TYPE: query
USAGE: useAppPermissionsGetPaddedMaskQuery({ mask: <value> })

INPUT:
  mask: BitString

OUTPUT: UseQueryResult
```

### HOOK: useOrgPermissionsGetPaddedMaskQuery

orgPermissionsGetPaddedMask

```
TYPE: query
USAGE: useOrgPermissionsGetPaddedMaskQuery({ mask: <value> })

INPUT:
  mask: BitString

OUTPUT: UseQueryResult
```

### HOOK: useStepsAchievedQuery

stepsAchieved

```
TYPE: query
USAGE: useStepsAchievedQuery({ vlevel: <value>, vroleId: <value> })

INPUT:
  vlevel: String
  vroleId: UUID

OUTPUT: UseQueryResult
```

### HOOK: useRevParseQuery

revParse

```
TYPE: query
USAGE: useRevParseQuery({ dbId: <value>, storeId: <value>, refname: <value> })

INPUT:
  dbId: UUID
  storeId: UUID
  refname: String

OUTPUT: UseQueryResult
```

### HOOK: useAppPermissionsGetMaskQuery

appPermissionsGetMask

```
TYPE: query
USAGE: useAppPermissionsGetMaskQuery({ ids: <value> })

INPUT:
  ids: [UUID]

OUTPUT: UseQueryResult
```

### HOOK: useOrgPermissionsGetMaskQuery

orgPermissionsGetMask

```
TYPE: query
USAGE: useOrgPermissionsGetMaskQuery({ ids: <value> })

INPUT:
  ids: [UUID]

OUTPUT: UseQueryResult
```

### HOOK: useAppPermissionsGetMaskByNamesQuery

appPermissionsGetMaskByNames

```
TYPE: query
USAGE: useAppPermissionsGetMaskByNamesQuery({ names: <value> })

INPUT:
  names: [String]

OUTPUT: UseQueryResult
```

### HOOK: useOrgPermissionsGetMaskByNamesQuery

orgPermissionsGetMaskByNames

```
TYPE: query
USAGE: useOrgPermissionsGetMaskByNamesQuery({ names: <value> })

INPUT:
  names: [String]

OUTPUT: UseQueryResult
```

### HOOK: useAppPermissionsGetByMaskQuery

Reads and enables pagination through a set of `AppPermission`.

```
TYPE: query
USAGE: useAppPermissionsGetByMaskQuery({ mask: <value>, first: <value>, offset: <value>, after: <value> })

INPUT:
  mask: BitString
  first: Int
  offset: Int
  after: Cursor

OUTPUT: UseQueryResult
```

### HOOK: useOrgPermissionsGetByMaskQuery

Reads and enables pagination through a set of `OrgPermission`.

```
TYPE: query
USAGE: useOrgPermissionsGetByMaskQuery({ mask: <value>, first: <value>, offset: <value>, after: <value> })

INPUT:
  mask: BitString
  first: Int
  offset: Int
  after: Cursor

OUTPUT: UseQueryResult
```

### HOOK: useGetAllObjectsFromRootQuery

Reads and enables pagination through a set of `Object`.

```
TYPE: query
USAGE: useGetAllObjectsFromRootQuery({ databaseId: <value>, id: <value>, first: <value>, offset: <value>, after: <value> })

INPUT:
  databaseId: UUID
  id: UUID
  first: Int
  offset: Int
  after: Cursor

OUTPUT: UseQueryResult
```

### HOOK: useGetPathObjectsFromRootQuery

Reads and enables pagination through a set of `Object`.

```
TYPE: query
USAGE: useGetPathObjectsFromRootQuery({ databaseId: <value>, id: <value>, path: <value>, first: <value>, offset: <value>, after: <value> })

INPUT:
  databaseId: UUID
  id: UUID
  path: [String]
  first: Int
  offset: Int
  after: Cursor

OUTPUT: UseQueryResult
```

### HOOK: useGetObjectAtPathQuery

getObjectAtPath

```
TYPE: query
USAGE: useGetObjectAtPathQuery({ dbId: <value>, storeId: <value>, path: <value>, refname: <value> })

INPUT:
  dbId: UUID
  storeId: UUID
  path: [String]
  refname: String

OUTPUT: UseQueryResult
```

### HOOK: useStepsRequiredQuery

Reads and enables pagination through a set of `AppLevelRequirement`.

```
TYPE: query
USAGE: useStepsRequiredQuery({ vlevel: <value>, vroleId: <value>, first: <value>, offset: <value>, after: <value> })

INPUT:
  vlevel: String
  vroleId: UUID
  first: Int
  offset: Int
  after: Cursor

OUTPUT: UseQueryResult
```

### HOOK: useCurrentUserQuery

currentUser

```
TYPE: query
USAGE: useCurrentUserQuery()

INPUT: none

OUTPUT: UseQueryResult
```

### HOOK: useSignOutMutation

signOut

```
TYPE: mutation
USAGE: const { mutate } = useSignOutMutation()
  mutate({ input: <value> })

INPUT:
  input: SignOutInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSendAccountDeletionEmailMutation

sendAccountDeletionEmail

```
TYPE: mutation
USAGE: const { mutate } = useSendAccountDeletionEmailMutation()
  mutate({ input: <value> })

INPUT:
  input: SendAccountDeletionEmailInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useCheckPasswordMutation

checkPassword

```
TYPE: mutation
USAGE: const { mutate } = useCheckPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: CheckPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSubmitInviteCodeMutation

submitInviteCode

```
TYPE: mutation
USAGE: const { mutate } = useSubmitInviteCodeMutation()
  mutate({ input: <value> })

INPUT:
  input: SubmitInviteCodeInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSubmitOrgInviteCodeMutation

submitOrgInviteCode

```
TYPE: mutation
USAGE: const { mutate } = useSubmitOrgInviteCodeMutation()
  mutate({ input: <value> })

INPUT:
  input: SubmitOrgInviteCodeInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useFreezeObjectsMutation

freezeObjects

```
TYPE: mutation
USAGE: const { mutate } = useFreezeObjectsMutation()
  mutate({ input: <value> })

INPUT:
  input: FreezeObjectsInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useInitEmptyRepoMutation

initEmptyRepo

```
TYPE: mutation
USAGE: const { mutate } = useInitEmptyRepoMutation()
  mutate({ input: <value> })

INPUT:
  input: InitEmptyRepoInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useConfirmDeleteAccountMutation

confirmDeleteAccount

```
TYPE: mutation
USAGE: const { mutate } = useConfirmDeleteAccountMutation()
  mutate({ input: <value> })

INPUT:
  input: ConfirmDeleteAccountInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSetPasswordMutation

setPassword

```
TYPE: mutation
USAGE: const { mutate } = useSetPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: SetPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useVerifyEmailMutation

verifyEmail

```
TYPE: mutation
USAGE: const { mutate } = useVerifyEmailMutation()
  mutate({ input: <value> })

INPUT:
  input: VerifyEmailInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useResetPasswordMutation

resetPassword

```
TYPE: mutation
USAGE: const { mutate } = useResetPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: ResetPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useRemoveNodeAtPathMutation

removeNodeAtPath

```
TYPE: mutation
USAGE: const { mutate } = useRemoveNodeAtPathMutation()
  mutate({ input: <value> })

INPUT:
  input: RemoveNodeAtPathInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useBootstrapUserMutation

bootstrapUser

```
TYPE: mutation
USAGE: const { mutate } = useBootstrapUserMutation()
  mutate({ input: <value> })

INPUT:
  input: BootstrapUserInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSetDataAtPathMutation

setDataAtPath

```
TYPE: mutation
USAGE: const { mutate } = useSetDataAtPathMutation()
  mutate({ input: <value> })

INPUT:
  input: SetDataAtPathInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSetPropsAndCommitMutation

setPropsAndCommit

```
TYPE: mutation
USAGE: const { mutate } = useSetPropsAndCommitMutation()
  mutate({ input: <value> })

INPUT:
  input: SetPropsAndCommitInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useProvisionDatabaseWithUserMutation

provisionDatabaseWithUser

```
TYPE: mutation
USAGE: const { mutate } = useProvisionDatabaseWithUserMutation()
  mutate({ input: <value> })

INPUT:
  input: ProvisionDatabaseWithUserInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSignInOneTimeTokenMutation

signInOneTimeToken

```
TYPE: mutation
USAGE: const { mutate } = useSignInOneTimeTokenMutation()
  mutate({ input: <value> })

INPUT:
  input: SignInOneTimeTokenInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useCreateUserDatabaseMutation

Creates a new user database with all required modules, permissions, and RLS policies.

Parameters:
  - database_name: Name for the new database (required)
  - owner_id: UUID of the owner user (required)
  - include_invites: Include invite system (default: true)
  - include_groups: Include group-level memberships (default: false)
  - include_levels: Include levels/achievements (default: false)
  - bitlen: Bit length for permission masks (default: 64)
  - tokens_expiration: Token expiration interval (default: 30 days)

Returns the database_id UUID of the newly created database.

Example usage:
  SELECT metaschema_public.create_user_database('my_app', 'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid);
  SELECT metaschema_public.create_user_database('my_app', 'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid, true, true);  -- with invites and groups


```
TYPE: mutation
USAGE: const { mutate } = useCreateUserDatabaseMutation()
  mutate({ input: <value> })

INPUT:
  input: CreateUserDatabaseInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useExtendTokenExpiresMutation

extendTokenExpires

```
TYPE: mutation
USAGE: const { mutate } = useExtendTokenExpiresMutation()
  mutate({ input: <value> })

INPUT:
  input: ExtendTokenExpiresInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSignInMutation

signIn

```
TYPE: mutation
USAGE: const { mutate } = useSignInMutation()
  mutate({ input: <value> })

INPUT:
  input: SignInInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSignUpMutation

signUp

```
TYPE: mutation
USAGE: const { mutate } = useSignUpMutation()
  mutate({ input: <value> })

INPUT:
  input: SignUpInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSetFieldOrderMutation

setFieldOrder

```
TYPE: mutation
USAGE: const { mutate } = useSetFieldOrderMutation()
  mutate({ input: <value> })

INPUT:
  input: SetFieldOrderInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useOneTimeTokenMutation

oneTimeToken

```
TYPE: mutation
USAGE: const { mutate } = useOneTimeTokenMutation()
  mutate({ input: <value> })

INPUT:
  input: OneTimeTokenInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useInsertNodeAtPathMutation

insertNodeAtPath

```
TYPE: mutation
USAGE: const { mutate } = useInsertNodeAtPathMutation()
  mutate({ input: <value> })

INPUT:
  input: InsertNodeAtPathInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useUpdateNodeAtPathMutation

updateNodeAtPath

```
TYPE: mutation
USAGE: const { mutate } = useUpdateNodeAtPathMutation()
  mutate({ input: <value> })

INPUT:
  input: UpdateNodeAtPathInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSetAndCommitMutation

setAndCommit

```
TYPE: mutation
USAGE: const { mutate } = useSetAndCommitMutation()
  mutate({ input: <value> })

INPUT:
  input: SetAndCommitInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useApplyRlsMutation

applyRls

```
TYPE: mutation
USAGE: const { mutate } = useApplyRlsMutation()
  mutate({ input: <value> })

INPUT:
  input: ApplyRlsInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useForgotPasswordMutation

forgotPassword

```
TYPE: mutation
USAGE: const { mutate } = useForgotPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: ForgotPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useSendVerificationEmailMutation

sendVerificationEmail

```
TYPE: mutation
USAGE: const { mutate } = useSendVerificationEmailMutation()
  mutate({ input: <value> })

INPUT:
  input: SendVerificationEmailInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useVerifyPasswordMutation

verifyPassword

```
TYPE: mutation
USAGE: const { mutate } = useVerifyPasswordMutation()
  mutate({ input: <value> })

INPUT:
  input: VerifyPasswordInput (required)

OUTPUT: UseMutationResult
```

### HOOK: useVerifyTotpMutation

verifyTotp

```
TYPE: mutation
USAGE: const { mutate } = useVerifyTotpMutation()
  mutate({ input: <value> })

INPUT:
  input: VerifyTotpInput (required)

OUTPUT: UseMutationResult
```
