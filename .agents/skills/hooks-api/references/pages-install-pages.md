# pagesInstallPages

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

React Query mutation hook for pagesInstallPages

## Usage

```typescript
const { mutate } = usePagesInstallPagesMutation(); mutate({ input: { entityId: '<UUID>', pagesPreset: '<String>', siteId: '<UUID>' } });
```

## Examples

### Use usePagesInstallPagesMutation

```typescript
const { mutate, isLoading } = usePagesInstallPagesMutation();
mutate({ input: { entityId: '<UUID>', pagesPreset: '<String>', siteId: '<UUID>' } });
```
