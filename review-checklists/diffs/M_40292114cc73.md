# Diff — `M` `packages/cli/__tests__/deploy.test.ts`

## Navigation
- Prev: [M_b0cbf04212b2.md](M_b0cbf04212b2.md) | Up: [packages/cli.md](../packages/cli.md) | Index: [README.md](../README.md) | Diffs: [diffs/README.md](README.md) | Snapshots: [SNAPSHOTS.md](../SNAPSHOTS.md) | Next: [M_ae8271207e9c.md](M_ae8271207e9c.md)

## Context
- Diff base (merge base for `main...HEAD`): `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Main tip: `2492d840ab18e5c30cc7a24a7b4959abf5d6b21e`
- Head: `refactor/ensure-new-name-mappings` @ `27ba20cfc1e66cb921f229963ca63a201fcf6952`
- Module: `packages/cli`
- Numstat: `+8/-8`
- Reproduce: `git diff main...HEAD -- packages/cli/__tests__/deploy.test.ts`

## Changes (line-aligned)
- ``    const commands = `cnc deploy --database ${testDb.name} --package my-first --to schema_myfirstapp --yes`;``
  - `lql` → `cnc`
- ``    const commands = `cnc deploy --database ${testDb.name} --package my-first --yes`;``
  - `lql` → `cnc`
- `      cnc deploy --database ${testDb.name} --package my-first --yes`
  - `lql` → `cnc`
- `      cnc deploy --database ${testDb.name} --package my-second --yes`
  - `lql` → `cnc`
- `      cnc deploy --database ${testDb.name} --package my-third --yes`
  - `lql` → `cnc`
- ``    const deployCommands = `cnc deploy --database ${testDb.name} --package my-first --yes`;``
  - `lql` → `cnc`
- ``    const revertCommands = `cnc revert --database $database  --package my-first --yes`;``
  - `lql` → `cnc`
- ``    const commands = `cnc deploy --database ${testDb.name} --package my-first --no-recursive --yes`;``
  - `lql` → `cnc`

## Full diff (side-by-side, changed lines only)
<details>
<summary>Show</summary>

- Left = diff base, Right = `HEAD`
- Legend: `|` changed, `<` only in base, `>` only in `HEAD`

```text
    const commands = `lql deploy --database ${testDb.name} --package my-first --to schema_myfirstapp --yes`;	      |	    const commands = `cnc deploy --database ${testDb.name} --package my-first --to schema_myfirstapp --yes`;
    const commands = `lql deploy --database ${testDb.name} --package my-first --yes`;				      |	    const commands = `cnc deploy --database ${testDb.name} --package my-first --yes`;
      lql deploy --database ${testDb.name} --package my-first --yes						      |	      cnc deploy --database ${testDb.name} --package my-first --yes
      lql deploy --database ${testDb.name} --package my-second --yes						      |	      cnc deploy --database ${testDb.name} --package my-second --yes
      lql deploy --database ${testDb.name} --package my-third --yes						      |	      cnc deploy --database ${testDb.name} --package my-third --yes
    const deployCommands = `lql deploy --database ${testDb.name} --package my-first --yes`;			      |	    const deployCommands = `cnc deploy --database ${testDb.name} --package my-first --yes`;
    const revertCommands = `lql revert --database $database  --package my-first --yes`;				      |	    const revertCommands = `cnc revert --database $database  --package my-first --yes`;
    const commands = `lql deploy --database ${testDb.name} --package my-first --no-recursive --yes`;		      |	    const commands = `cnc deploy --database ${testDb.name} --package my-first --no-recursive --yes`;
```

</details>

## Navigation
- Prev: [M_b0cbf04212b2.md](M_b0cbf04212b2.md) | Up: [packages/cli.md](../packages/cli.md) | Index: [README.md](../README.md) | Diffs: [diffs/README.md](README.md) | Snapshots: [SNAPSHOTS.md](../SNAPSHOTS.md) | Next: [M_ae8271207e9c.md](M_ae8271207e9c.md)
