# Diff — `R081` `packages/launchql-gen/__tests__/options.test.ts` → `graphql/codegen/__tests__/options.test.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `81`)
- Numstat: `+6/-6`
- Reproduce: `git diff -M main...HEAD -- packages/launchql-gen/__tests__/options.test.ts graphql/codegen/__tests__/options.test.ts`

## Guideline token summary
- Deltas: `GraphQLCodegenOptions`: 0 → 7; `LaunchQL`: 7 → 0; `LaunchQLGenOptions`: 7 → 0; `mergeGraphQLCodegenOptions`: 0 → 4; `mergeLaunchQLGenOptions`: 4 → 0; `defaultGraphQLCodegenOptions`: 0 → 3; `defaultLaunchQLGenOptions`: 3 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { defaultLaunchQLGenOptions, mergeLaunchQLGenOptions } from '../src'					      |	import { defaultGraphQLCodegenOptions, mergeGraphQLCodegenOptions } from '../src'
describe('mergeLaunchQLGenOptions', () => {									      |	describe('mergeGraphQLCodegenOptions', () => {
    const base = { ...defaultLaunchQLGenOptions }								      |	    const base = { ...defaultGraphQLCodegenOptions }
    const merged = mergeLaunchQLGenOptions(base, overrides)							      |	    const merged = mergeGraphQLCodegenOptions(base, overrides)
    const base = { ...defaultLaunchQLGenOptions }								      |	    const base = { ...defaultGraphQLCodegenOptions }
    const merged = mergeLaunchQLGenOptions(base, overrides)							      |	    const merged = mergeGraphQLCodegenOptions(base, overrides)
```
