# Diff — `M` `packages/cli/__tests__/cli-deploy-fixture.test.ts`

## Navigation
- Prev: [M_c890f80cecb9.md](M_c890f80cecb9.md) | Up: [packages/cli.md](../packages/cli.md) | Index: [README.md](../README.md) | Diffs: [diffs/README.md](README.md) | Snapshots: [SNAPSHOTS.md](../SNAPSHOTS.md) | Next: [M_61580404e6fb.md](M_61580404e6fb.md)

## Context
- Diff base (merge base for `main...HEAD`): `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Main tip: `2492d840ab18e5c30cc7a24a7b4959abf5d6b21e`
- Head: `refactor/ensure-new-name-mappings` @ `27ba20cfc1e66cb921f229963ca63a201fcf6952`
- Module: `packages/cli`
- Numstat: `+6/-6`
- Reproduce: `git diff main...HEAD -- packages/cli/__tests__/cli-deploy-fixture.test.ts`

## Changes (line-aligned)
- ``    const commands = `cnc migrate status --database ${testDb.name}`;``
  - `lql` → `cnc`
- ``    const commands = `cnc init workspace test-workspace`;``
  - `lql` → `cnc`
- ``    const commands = `cnc init test-module`;``
  - `lql` → `cnc`
- `      cnc deploy --recursive --database ${testDb.name} --yes --package my-first`
  - `lql` → `cnc`
- `      cnc revert --recursive --database ${testDb.name} --yes --package my-first`
  - `lql` → `cnc`
- `      cnc deploy --recursive --database test_db --createdb --yes --package my-first`
  - `lql` → `cnc`

## Full diff (side-by-side, changed lines only)
<details>
<summary>Show</summary>

- Left = diff base, Right = `HEAD`
- Legend: `|` changed, `<` only in base, `>` only in `HEAD`

```text
    const commands = `lql migrate status --database ${testDb.name}`;						      |	    const commands = `cnc migrate status --database ${testDb.name}`;
    const commands = `lql init workspace test-workspace`;							      |	    const commands = `cnc init workspace test-workspace`;
    const commands = `lql init test-module`;									      |	    const commands = `cnc init test-module`;
      lql deploy --recursive --database ${testDb.name} --yes --package my-first					      |	      cnc deploy --recursive --database ${testDb.name} --yes --package my-first
      lql revert --recursive --database ${testDb.name} --yes --package my-first					      |	      cnc revert --recursive --database ${testDb.name} --yes --package my-first
      lql deploy --recursive --database test_db --createdb --yes --package my-first				      |	      cnc deploy --recursive --database test_db --createdb --yes --package my-first
```

</details>

## Navigation
- Prev: [M_c890f80cecb9.md](M_c890f80cecb9.md) | Up: [packages/cli.md](../packages/cli.md) | Index: [README.md](../README.md) | Diffs: [diffs/README.md](README.md) | Snapshots: [SNAPSHOTS.md](../SNAPSHOTS.md) | Next: [M_61580404e6fb.md](M_61580404e6fb.md)
