# Diff — `R091` `packages/server/src/server.ts` → `graphql/server/src/server.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `91`)
- Numstat: `+6/-6`
- Reproduce: `git diff -M main...HEAD -- packages/server/src/server.ts graphql/server/src/server.ts`

## Guideline token summary
- Deltas: `launchql`: 5 → 0; `constructive`: 0 → 3; `@constructive-io/`: 0 → 2; `@launchql/`: 2 → 0; `GraphQLServer`: 0 → 1; `LaunchQL`: 1 → 0; `LaunchQLServer`: 1 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { getEnvOptions } from '@launchql/env';									      |	import { getEnvOptions } from '@constructive-io/graphql-env';
import { middleware as parseDomains } from '@launchql/url-domains';						      |	import { middleware as parseDomains } from '@constructive-io/url-domains';
export const LaunchQLServer = (rawOpts: PgpmOptions = {}) => {							      |	export const GraphQLServer = (rawOpts: PgpmOptions = {}) => {
  const launchqlEnvOptions = getEnvOptions(rawOpts);								      |	  const envOptions = getEnvOptions(rawOpts);
  const app = new Server(launchqlEnvOptions);									      |	  const app = new Server(envOptions);
    app.use(poweredBy('launchql'));										      |	    app.use(poweredBy('constructive'));
```
