# Diff — `R056` `packages/launchql-gen/sql/test.sql` → `graphql/codegen/sql/test.sql`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `56`)
- Numstat: `+9/-9`
- Reproduce: `git diff -M main...HEAD -- packages/launchql-gen/sql/test.sql graphql/codegen/sql/test.sql`

## Guideline token summary
- Deltas: `constructive`: 0 → 9; `launchql`: 9 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
DROP SCHEMA IF EXISTS launchql_gen CASCADE;									      |	DROP SCHEMA IF EXISTS constructive_gen CASCADE;
CREATE SCHEMA launchql_gen;											      |	CREATE SCHEMA constructive_gen;
CREATE TABLE launchql_gen.users (										      |	CREATE TABLE constructive_gen.users (
CREATE TABLE launchql_gen.posts (										      |	CREATE TABLE constructive_gen.posts (
    user_id int NOT NULL REFERENCES launchql_gen.users(id),							      |	    user_id int NOT NULL REFERENCES constructive_gen.users(id),
CREATE VIEW launchql_gen.active_users AS									      |	CREATE VIEW constructive_gen.active_users AS
SELECT id, username FROM launchql_gen.users WHERE username IS NOT NULL;						      |	SELECT id, username FROM constructive_gen.users WHERE username IS NOT NULL;
CREATE FUNCTION launchql_gen.user_count() RETURNS integer AS $$							      |	CREATE FUNCTION constructive_gen.user_count() RETURNS integer AS $$
  RETURN (SELECT count(*) FROM launchql_gen.users);								      |	  RETURN (SELECT count(*) FROM constructive_gen.users);
```
