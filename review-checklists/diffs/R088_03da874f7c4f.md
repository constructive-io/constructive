# Diff — `R088` `packages/launchql-env/src/env.ts` → `graphql/env/src/env.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `88`)
- Numstat: `+3/-3`
- Reproduce: `git diff -M main...HEAD -- packages/launchql-env/src/env.ts graphql/env/src/env.ts`

## Guideline token summary
- Deltas: `Constructive`: 0 → 3; `LaunchQL`: 3 → 0; `ConstructiveOptions`: 0 → 2; `LaunchQLOptions`: 2 → 0; `launchql`: 2 → 0; `@constructive-io/`: 0 → 1; `@launchql/`: 1 → 0; `constructive`: 0 → 1; `launchqlGraphqlDefaults`: 1 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { LaunchQLOptions, launchqlGraphqlDefaults } from '@launchql/types';					      |	import { ConstructiveOptions } from '@constructive-io/graphql-types';
 * These are the env vars that LaunchQL packages need but pgpm doesn't.						      |	 * These are the env vars that Constructive packages need but pgpm doesn't.
export const getGraphQLEnvVars = (): Partial<LaunchQLOptions> => {						      |	export const getGraphQLEnvVars = (): Partial<ConstructiveOptions> => {
```
