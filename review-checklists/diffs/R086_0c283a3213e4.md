# Diff — `R086` `packages/react/src/use-table-rows.ts` → `graphql/react/src/use-table-rows.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `86`)
- Numstat: `+17/-17`
- Reproduce: `git diff -M main...HEAD -- packages/react/src/use-table-rows.ts graphql/react/src/use-table-rows.ts`

## Guideline token summary
- Deltas: `Constructive`: 0 → 5; `useConstructiveQuery`: 0 → 5; `useLaunchqlQuery`: 5 → 0; `constructive`: 0 → 1; `launchql`: 1 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { useLaunchqlQuery } from './use-launchql-client';							      |	import { useConstructiveQuery } from './use-constructive-client';
  const lqlClient = useLaunchqlQuery();										      |	  const queryBuilder = useConstructiveQuery();
    if (!lqlClient) return null;										      |	    if (!queryBuilder) return null;
    const result = lqlClient.query(tableName).getMany({ select }).print();					      |	    const result = queryBuilder.query(tableName).getMany({ select }).print();
  }, [lqlClient, select, tableName]);										      |	  }, [queryBuilder, select, tableName]);
  const lqlClient = useLaunchqlQuery();										      |	  const queryBuilder = useConstructiveQuery();
    if (!lqlClient) return null;										      |	    if (!queryBuilder) return null;
    const result = lqlClient.query(tableName).getMany({ select }).print();					      |	    const result = queryBuilder.query(tableName).getMany({ select }).print();
  }, [lqlClient, select, tableName]);										      |	  }, [queryBuilder, select, tableName]);
  const lqlClient = useLaunchqlQuery();										      |	  const queryBuilder = useConstructiveQuery();
    if (!lqlClient) return null;										      |	    if (!queryBuilder) return null;
    const result = lqlClient.query(tableName).create().print();							      |	    const result = queryBuilder.query(tableName).create().print();
  }, [lqlClient, tableName]);											      |	  }, [queryBuilder, tableName]);
  const lqlClient = useLaunchqlQuery();										      |	  const queryBuilder = useConstructiveQuery();
    if (!lqlClient) return null;										      |	    if (!queryBuilder) return null;
    const result = lqlClient.query(tableName).delete().print();							      |	    const result = queryBuilder.query(tableName).delete().print();
  }, [lqlClient, tableName]);											      |	  }, [queryBuilder, tableName]);
```
