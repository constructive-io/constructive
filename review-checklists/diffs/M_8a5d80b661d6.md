# Diff — `M` `packages/cli/__tests__/package-alias.test.ts`

## Navigation
- Prev: [M_5489b74dbfb0.md](M_5489b74dbfb0.md) | Up: [packages/cli.md](../packages/cli.md) | Index: [README.md](../README.md) | Diffs: [diffs/README.md](README.md) | Snapshots: [SNAPSHOTS.md](../SNAPSHOTS.md) | Next: [M_cfb76d8e6adf.md](M_cfb76d8e6adf.md)

## Context
- Diff base (merge base for `main...HEAD`): `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Main tip: `2492d840ab18e5c30cc7a24a7b4959abf5d6b21e`
- Head: `refactor/ensure-new-name-mappings` @ `27ba20cfc1e66cb921f229963ca63a201fcf6952`
- Module: `packages/cli`
- Numstat: `+5/-5`
- Reproduce: `git diff main...HEAD -- packages/cli/__tests__/package-alias.test.ts`

## Changes (line-aligned)
- ``    const commands = `cnc deploy --database ${testDb.name} --package @test-scope/my-first --yes`;``
  - `lql` → `cnc`
- ``    const commands = `cnc deploy --database ${testDb.name} --package my-first --yes`;``
  - `lql` → `cnc`
- ``    const commands = `cnc deploy --database ${testDb.name} --package @test-scope/my-first --to schema_myfirstapp --yes`;``
  - `lql` → `cnc`
- ``    const deployCommands = `cnc deploy --database ${testDb.name} --package @test-scope/my-first --yes`;``
  - `lql` → `cnc`
- ``    const revertCommands = `cnc revert --database ${testDb.name} --package @test-scope/my-first --yes`;``
  - `lql` → `cnc`

## Full diff (side-by-side, changed lines only)
<details>
<summary>Show</summary>

- Left = diff base, Right = `HEAD`
- Legend: `|` changed, `<` only in base, `>` only in `HEAD`

```text
    const commands = `lql deploy --database ${testDb.name} --package @test-scope/my-first --yes`;		      |	    const commands = `cnc deploy --database ${testDb.name} --package @test-scope/my-first --yes`;
    const commands = `lql deploy --database ${testDb.name} --package my-first --yes`;				      |	    const commands = `cnc deploy --database ${testDb.name} --package my-first --yes`;
    const commands = `lql deploy --database ${testDb.name} --package @test-scope/my-first --to schema_myfirstapp --ye |	    const commands = `cnc deploy --database ${testDb.name} --package @test-scope/my-first --to schema_myfirstapp --ye
    const deployCommands = `lql deploy --database ${testDb.name} --package @test-scope/my-first --yes`;		      |	    const deployCommands = `cnc deploy --database ${testDb.name} --package @test-scope/my-first --yes`;
    const revertCommands = `lql revert --database ${testDb.name} --package @test-scope/my-first --yes`;		      |	    const revertCommands = `cnc revert --database ${testDb.name} --package @test-scope/my-first --yes`;
```

</details>

## Navigation
- Prev: [M_5489b74dbfb0.md](M_5489b74dbfb0.md) | Up: [packages/cli.md](../packages/cli.md) | Index: [README.md](../README.md) | Diffs: [diffs/README.md](README.md) | Snapshots: [SNAPSHOTS.md](../SNAPSHOTS.md) | Next: [M_cfb76d8e6adf.md](M_cfb76d8e6adf.md)
