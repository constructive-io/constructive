# Diff — `R089` `packages/launchql-gen/src/codegen.ts` → `graphql/codegen/src/codegen.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `89`)
- Numstat: `+12/-12`
- Reproduce: `git diff -M main...HEAD -- packages/launchql-gen/src/codegen.ts graphql/codegen/src/codegen.ts`

## Guideline token summary
- Deltas: `GraphQLCodegenOptions`: 0 → 15; `LaunchQL`: 15 → 0; `LaunchQLGenOptions`: 15 → 0; `defaultGraphQLCodegenOptions`: 0 → 6; `defaultLaunchQLGenOptions`: 6 → 0; `mergeGraphQLCodegenOptions`: 0 → 2; `mergeLaunchQLGenOptions`: 2 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { defaultLaunchQLGenOptions, LaunchQLGenOptions, mergeLaunchQLGenOptions } from './options'		      |	import { defaultGraphQLCodegenOptions, GraphQLCodegenOptions, mergeGraphQLCodegenOptions } from './options'
function getFilename(key: string, convention: LaunchQLGenOptions['documents']['convention']) {			      |	function getFilename(key: string, convention: GraphQLCodegenOptions['documents']['convention']) {
function applyQueryFilters(map: GqlMap, docs: LaunchQLGenOptions['documents']): GqlMap {			      |	function applyQueryFilters(map: GqlMap, docs: GraphQLCodegenOptions['documents']): GqlMap {
async function writeOperationsDocuments(docs: Record<string, string>, dir: string, format: 'gql' | 'ts', convention:  |	async function writeOperationsDocuments(docs: Record<string, string>, dir: string, format: 'gql' | 'ts', convention: 
export async function runCodegen(opts: LaunchQLGenOptions, cwd: string) {					      |	export async function runCodegen(opts: GraphQLCodegenOptions, cwd: string) {
  const options: LaunchQLGenOptions = {										      |	  const options: GraphQLCodegenOptions = {
    input: { ...(defaultLaunchQLGenOptions.input), ...(opts.input || {}) },					      |	    input: { ...(defaultGraphQLCodegenOptions.input), ...(opts.input || {}) },
    output: { ...(defaultLaunchQLGenOptions.output), ...(opts.output || {}) },					      |	    output: { ...(defaultGraphQLCodegenOptions.output), ...(opts.output || {}) },
    documents: { ...(defaultLaunchQLGenOptions.documents), ...(opts.documents || {}) },				      |	    documents: { ...(defaultGraphQLCodegenOptions.documents), ...(opts.documents || {}) },
    features: { ...(defaultLaunchQLGenOptions.features), ...(opts.features || {}) }				      |	    features: { ...(defaultGraphQLCodegenOptions.features), ...(opts.features || {}) }
  const merged = mergeLaunchQLGenOptions(defaultLaunchQLGenOptions, overrides as any)				      |	  const merged = mergeGraphQLCodegenOptions(defaultGraphQLCodegenOptions, overrides as any)
  return runCodegen(merged as LaunchQLGenOptions, cwd)								      |	  return runCodegen(merged as GraphQLCodegenOptions, cwd)
```
