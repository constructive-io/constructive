# Diff — `M` `packages/cli/src/commands/codegen.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Numstat: `+11/-11`
- Reproduce: `git diff main...HEAD -- packages/cli/src/commands/codegen.ts`

## Guideline token summary
- Deltas: `LaunchQL`: 10 → 0; `GraphQLCodegenOptions`: 0 → 9; `LaunchQLGenOptions`: 9 → 0; `constructive`: 0 → 3; `launchql`: 3 → 0; `mergeGraphQLCodegenOptions`: 0 → 3; `mergeLaunchQLGenOptions`: 3 → 0; `@constructive-io/`: 0 → 2; `@launchql/`: 2 → 0; `defaultGraphQLCodegenOptions`: 0 → 2

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { runCodegen, defaultLaunchQLGenOptions, mergeLaunchQLGenOptions, LaunchQLGenOptions } from '@launchql/codegen |	import { runCodegen, defaultGraphQLCodegenOptions, mergeGraphQLCodegenOptions, GraphQLCodegenOptions } from '@constru
import { fetchEndpointSchemaSDL } from '@launchql/server'							      |	import { fetchEndpointSchemaSDL } from '@constructive-io/graphql-server'
LaunchQL Codegen:												      |	Constructive GraphQL Codegen:
  lql codegen [OPTIONS]												      |	  cnc codegen [OPTIONS]
  --out <dir>                Output root directory (default: packages/launchql-gen/dist)			      |	  --out <dir>                Output root directory (default: graphql/codegen/dist)
async function loadConfig(path: string): Promise<Partial<LaunchQLGenOptions>> {					      |	async function loadConfig(path: string): Promise<Partial<GraphQLCodegenOptions>> {
  let fileOpts: Partial<LaunchQLGenOptions> = {}								      |	  let fileOpts: Partial<GraphQLCodegenOptions> = {}
  const overrides: Partial<LaunchQLGenOptions> = {}								      |	  const overrides: Partial<GraphQLCodegenOptions> = {}
  const merged = mergeLaunchQLGenOptions(defaultLaunchQLGenOptions, fileOpts as any)				      |	  const merged = mergeGraphQLCodegenOptions(defaultGraphQLCodegenOptions, fileOpts as any)
  const finalOptions = mergeLaunchQLGenOptions(merged, overrides)						      |	  const finalOptions = mergeGraphQLCodegenOptions(merged, overrides)
    const tmpSchemaPath = join(cwd, '.lql-codegen-schema.graphql')						      |	    const tmpSchemaPath = join(cwd, '.constructive-codegen-schema.graphql')
```
