# Diff — `M` `packages/cli/src/commands/get-graphql-schema.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Numstat: `+6/-6`
- Reproduce: `git diff main...HEAD -- packages/cli/src/commands/get-graphql-schema.ts`

## Guideline token summary
- Deltas: `constructive`: 0 → 3; `launchql`: 3 → 0; `@constructive-io/`: 0 → 1; `@launchql/`: 1 → 0; `Constructive`: 0 → 1; `LaunchQL`: 1 → 0; `cnc`: 0 → 1; `lql`: 1 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
import { buildSchemaSDL, fetchEndpointSchemaSDL } from '@launchql/server'					      |	import { buildSchemaSDL, fetchEndpointSchemaSDL } from '@constructive-io/graphql-server'
LaunchQL Get GraphQL Schema:											      |	Constructive Get GraphQL Schema:
  lql get-graphql-schema [OPTIONS]										      |	  cnc get-graphql-schema [OPTIONS]
  --database <name>       Database name (default: launchql)							      |	  --database <name>       Database name (default: constructive)
  const database = (argv.database as string) ?? 'launchql'							      |	  const database = (argv.database as string) ?? 'constructive'
```
