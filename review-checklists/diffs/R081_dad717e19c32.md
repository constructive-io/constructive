# Diff — `R081` `packages/core/src/utils/debug.ts` → `pgpm/core/src/utils/debug.ts`

## Context
- Base: `main` @ `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Head: `refactor/ensure-new-name-mappings` @ `bd9be723c96aeb1f9f69e4946acbd9241ee8da50`
- Merge base: `86d74dc4fce9051df0d2b5bcc163607aba42f009`
- Type: rename/copy (similarity `81`)
- Numstat: `+14/-14`
- Reproduce: `git diff -M main...HEAD -- packages/core/src/utils/debug.ts pgpm/core/src/utils/debug.ts`

## Guideline token summary
- Deltas: `Constructive`: 0 → 1; `LaunchQL`: 1 → 0; `constructive`: 0 → 1; `launchql`: 1 → 0

## Side-by-side diff (only changed lines)
- Left = `main`, Right = `HEAD`
- Legend: `|` changed, `<` only in `main`, `>` only in `HEAD`

```text
    const enabled = process.env.LAUNCHQL_DEBUG === 'true' || process.env.DEBUG === 'launchql*';			      |	    const enabled = process.env.CONSTRUCTIVE_DEBUG === 'true' || process.env.DEBUG === 'constructive*';
    const logLevel = (process.env.LAUNCHQL_DEBUG_LEVEL as any) || 'debug';					      |	    const logLevel = (process.env.CONSTRUCTIVE_DEBUG_LEVEL as any) || 'debug';
    const showStackTrace = process.env.LAUNCHQL_DEBUG_STACK !== 'false';					      |	    const showStackTrace = process.env.CONSTRUCTIVE_DEBUG_STACK !== 'false';
    const showQueryParams = process.env.LAUNCHQL_DEBUG_PARAMS !== 'false';					      |	    const showQueryParams = process.env.CONSTRUCTIVE_DEBUG_PARAMS !== 'false';
    const showFullSQL = process.env.LAUNCHQL_DEBUG_SQL !== 'false';						      |	    const showFullSQL = process.env.CONSTRUCTIVE_DEBUG_SQL !== 'false';
  process.env.LAUNCHQL_DEBUG = 'true';										      |	  process.env.CONSTRUCTIVE_DEBUG = 'true';
  log.info('   Set LAUNCHQL_DEBUG=false to disable');								      |	  log.info('   Set CONSTRUCTIVE_DEBUG=false to disable');
  log.info('   Set LAUNCHQL_DEBUG_LEVEL=info|warn|error|debug to change log level');				      |	  log.info('   Set CONSTRUCTIVE_DEBUG_LEVEL=info|warn|error|debug to change log level');
  log.info('   Set LAUNCHQL_DEBUG_STACK=false to hide stack traces');						      |	  log.info('   Set CONSTRUCTIVE_DEBUG_STACK=false to hide stack traces');
  log.info('   Set LAUNCHQL_DEBUG_PARAMS=false to hide query parameters');					      |	  log.info('   Set CONSTRUCTIVE_DEBUG_PARAMS=false to hide query parameters');
  log.info('   Set LAUNCHQL_DEBUG_SQL=false to hide full SQL scripts');						      |	  log.info('   Set CONSTRUCTIVE_DEBUG_SQL=false to hide full SQL scripts');
  summary.push('=== LaunchQL Debug Summary ===');								      |	  summary.push('=== Constructive Debug Summary ===');
  summary.push('  1. Run with LAUNCHQL_DEBUG=true for more details');						      |	  summary.push('  1. Run with CONSTRUCTIVE_DEBUG=true for more details');
```
